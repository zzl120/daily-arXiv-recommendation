 [ BoldFont=texgyretermes-bold.otf, ItalicFont=texgyretermes-italic.otf, BoldItalicFont=texgyretermes-bolditalic.otf] [ BoldFont=texgyreheros-bold.otf, ItalicFont=texgyreheros-italic.otf, BoldItalicFont=texgyreheros-bolditalic.otf] [ BoldFont=DejaVuSansMono-Bold.ttf, ItalicFont=DejaVuSansMono-Oblique.ttf, BoldItalicFont=DejaVuSansMono-BoldOblique.ttf, Scale=0.90] {} {HTML}{F5F6F8} {HTML}{B8C4D0} {HTML}{264B70} {HTML}{70513C} { basicstyle= {10.5} , columns=fullflexible,keepspaces=true,showstringspaces=false, upquote=true,breaklines=true,breakatwhitespace=false, backgroundcolor=, frame=l,rulecolor=,framerule=0.6pt,framesep=4pt, xleftmargin=6pt,xrightmargin=4pt, aboveskip=4pt,belowskip=6pt, keywordstyle= , stringstyle=,commentstyle= , tabsize=4 } {caption} {7pt} {hyperref} ,pdfauthor={Damian Delmas}} {{0pt}{0pt}} {2} {Algebraic Retrieval} } {2pt} Algebraic Retrieval lets AI agents compose search strategies at query time. Relevance criteria, eligibility constraints, and ranking preferences can be expressed together in a mathematical query. The query surface exposes available operations, so an agent can combine them for the question at hand and revise a program after inspecting results. We evaluate execution parity, not agent behavior or retrieval quality. Building on Programmatic Embedding Modulation , which exposes vector and score arithmetic during retrieval, we demonstrate contrastive scoring, candidate-pool reranking, and weighted ranking as composable queries, alongside executable SQL and PyTerrier counterparts. On the public 11,429-document Vaswani fixture, each program's implementations select the same document set with score differences below ; one tied pair orders differently across scoring paths. { } Retrieval is often presented to an agent as one search call with a query and a few parameters. An agent seeking implementation architecture may also want to suppress marketing material, restrict candidates to engineering discussions, and weight scores by recency. These choices can be written together as one expression. Algebraic Retrieval lets the agent compose that expression at query time. It chooses scoring directions, eligible records, and per-record weights, then can revise the expression after inspecting results. Programmatic Embedding Modulation { } exposes vectors and scores for arithmetic at query time. Algebraic Retrieval gives that surface a mathematical query notation. We compare three programs with SQL and PyTerrier { }, a Python library for composing retrieval pipelines. { } An embedding represents a record or query as a vector of numbers in a shared space. Let \ hold one record vector per row and \ be a query vector. Matrix multiplication \ produces one score per record. With unit-length record and query vectors, these scores equal cosine similarity. The scores and their record identities form a scored relation \ . PEM { } exposes the embedding matrix and score array for arithmetic before selection. A caller can change the query vector before scoring, or add, subtract, and weight scores afterward. SQL filters choose the participating records. Algebraic Retrieval gives these operations a mathematical query notation. Its contract uses normalized stored and primitive query vectors but leaves composed queries unnormalized. For query vectors in the same embedding space, \ and \ combine their scores, and scalar scales a contribution. For example, \ -0.5\, \) favors the first direction while suppressing the second. Selection happens after composition; subtracting two already-truncated top-ten lists would be a different operation. In displayed formulas, \ and \ denote the source tokens and . A mask \ is a set of eligible identities. \ removes other records from a scored relation \ . A weight relation \ supplies a scalar for each eligible identity, and \ multiplies its score. Here \ covers every eligible identity; a missing weight is represented as zero. Masks remove records; a zero weight leaves a record present with score zero. Finally, \ \) selects up to ten results, ordered by score descending and identity ascending. The ASCII spelling is . { } The runtime uses a materializer: code that executes retrieval operations and stores the results in a temporary SQL table. It resolves named operands through SQLite, while NumPy performs vector arithmetic. The parser reads the expression; the planner chooses its execution steps. {figures/retrieval-program.pdf} The agent submits a program through , which can also appear inside a larger SQL query to join results to record text or metadata. Changing a coefficient or adding a mask changes the retrieval strategy through the same call. Because the query vectors remain explicit in the expression, the planner can combine them before scoring. It rewrites two scoring passes as one using: \[ -0.5\, = E . \] The rewrite preserves the mathematical scores and uses one matrix multiplication. It is exact in real arithmetic; 32-bit floating-point rounding can cause small differences. For a fixed \ , changing \ leaves the scored identities intact. A mask changes which records participate, while per-record weighting generally cannot be absorbed into a change of \ . The SQL listings are executable equivalents, not compiler output. They calculate cosine similarity with the SQLite extension sqlite-vec { }, binding and to the query vectors. They query and the views and defined in Example bindings. { } Before writing a query, the agent calls: algebra The response lists available operands, their source relations, and whether they are usable. For this comparison, the proof runner supplies the following fixture-specific bindings; a live contract-backed cell returns the same kind of rows: {@{}llll@{}} section & token & domain & status \\ relation & & & \\ matrix & & & \\ query & & & \\ query & & & \\ mask & & & \\ weight & & & \\ The live response also advertises the operators used below. Whoever integrates the corpus declares how each symbol maps to data; the example runner declares the bindings used here. The agent composes expressions over those named inputs. The examples use the 11,429-document collection { }, with deterministic 128-dimensional signed-hash vectors and normalized stored rows. is the stored query vector for “What are chemical reactions?”; is a fixed vector from document . BM25 ranks documents by query-term matches; the mask contains the 52 candidates in its recorded PyTerrier result for that query. Each weight is its BM25 score divided by the maximum score in that result. The vectors make the computation reproducible; they are not a learned-embedding retrieval evaluation. { } The examples introduce contrastive scoring and candidate restriction, then combine both with per-record weights. In PyTerrier, passes results to the next stage and { } The agent can use a second query or an example document to specify an unwanted direction. Here document fixes the subtraction operand reproducibly; it is not assigned an irrelevance judgment. \[ _{10}\! -0.5\, ) \] SELECT docno AS id, ) - 0.5 * ) AS score FROM embeddings ORDER BY score DESC, id ASC LIMIT 10; * B) >> T) PyTerrier adds the first scorer to a negatively weighted second scorer. The SQL alternative calculates two cosine similarities per row; Algebra combines the vectors and scores once. All select the same ten-document set, with document first. PyTerrier returns the same order, with a maximum score difference of . Documents and tie exactly in Algebra and PyTerrier, where identity ordering places first. The SQL path separates them by approximately and places first; its maximum returned-score difference is also approximately . Replacing subtraction with addition blends the two scoring directions. { } The agent restricts vector scoring to an existing first-pass result. Here the mask is the 52-document BM25 candidate pool exposed by . \[ _{10}\! ) \] SELECT e.docno AS id, 1 - vec_distance_cosine AS score FROM embeddings AS e JOIN m ON m.id = e.docno ORDER BY score DESC, id ASC LIMIT 10; The candidate pool is scored again and then reduced to ten results. The Algebra planner applies the mask before its vector scoring pass. The SQL join expresses the same allowed identities, and PyTerrier passes those candidates to the scorer. All return the same ordered identities, with document first. PyTerrier scores match Algebra exactly; the SQL scores have a maximum observed difference of approximately . { } Within the candidate pool, the agent combines target and suppression scores, applies the declared weights, and selects the top ten. Normalized BM25 weights serve as a mechanical demonstration. \[ _{10}\! ( w (m -0.5\, ) ) ) \] SELECT e.docno AS id, w.weight * ) - 0.5 * )) AS score FROM embeddings AS e JOIN m ON m.id = e.docno JOIN w ON w.id = e.docno ORDER BY score DESC, id ASC LIMIT 10; * B) >> W >> T) All 52 eligible identities have weights. All three forms return the same ordered identities, with document first. Maximum score differences relative to Algebra are approximately for SQL and for PyTerrier. The third program composes the choices introduced earlier: combine scoring directions, restrict the candidates, and apply per-record preferences before selection. Its output remains a scored relation that the agent can inspect and reuse. { } The composition works because intermediate scores remain paired with identities. Arithmetic changes scores, masks determine which records participate, and top-k selects the final result. The PyTerrier 1.1.2 programs use scores independently computed by FAISS 1.15.0 , an exact inner-product scorer. The SQL alternatives use sqlite-vec 0.1.9. All document sets match Algebra and score differences are below ; §5.1 reports the ordering difference. The masked cases use the recorded upstream BM25 pool; the unmasked contrastive case scores all 11,429 rows. These checks do not compare timing or BM25 implementations. Score tolerance does not imply rank agreement. Algebra and PyTerrier tie and at , a reference gap of zero; SQL favors by approximately . Identity ordering resolves only computed ties. If each alternative score differs from its reference by at most \ , a reference gap greater than \ preserves their order. SQL deviations of approximately also occur in §5.2, which uses a single query vector. A two-pass NumPy calculation of §5.1 preserves the tie, so the ordering difference does not require the rewrite. Stored float32 rows are approximately unit length; sqlite-vec 0.1.9 { } recomputes vector lengths using float32 accumulation, then rounds cosine distance before SQL subtracts it from one. Reproducing those steps reproduces the pair's split: a deterministic rounding effect. The repository's {six-case validation} also checks score cutoffs, which retain records that meet a chosen minimum score. The {portable compiler witness} exercises support, signed scores, weights, and cutoff ties; companion tests check {restriction versus zero-weighting on negative scores} and {tie ordering, the rewrite in §3, and score cutoffs}. A {feedback test} selects the highest-scoring record, uses its vector as a new query, and scores again. Applying the mask before that selection changes the final ordering, so the planner must preserve where restriction occurs. PyTerrier composes retrieval stages, called transformers { }, and supports {pipeline optimizations}. Here the agent writes one expression over discovered operands; the matrix and query vectors remain explicit for rewrites such as §3's. Earlier work develops vector scoring and query revision from example documents { }, and top-k selection over database relations { }. Vespa lets users define document-scoring expressions { }; LOTUS uses language models to evaluate natural-language conditions on table rows { }. The experiments do not measure retrieval-quality gains, approximate nearest-neighbor recall, comparative agent query-writing accuracy, or universal compiler correctness. { } An agent can write retrieval as arithmetic over similarity scores, restrict it with a mask, and change the program at query time. The existing runtime executes these short expressions and returns relations the agent can continue to compose. On the public fixture, three implementations select the same document sets with score differences below , yet one tied pair orders differently. Numerical agreement must be checked separately from rank agreement. { *{References}} {section}{References} .} G. Salton, A. Wong, and C. S. Yang. ``A Vector Space Model for Automatic Indexing.'' , 1975. J. J. Rocchio. ``Relevance Feedback in Information Retrieval.'' , 1971. Craig Macdonald and Nicola Tonellotto. ``Declarative Experimentation in Information Retrieval using PyTerrier.'' , 2020. Sean MacAvaney et al.~``Simplified Data Wrangling with ir\_datasets.'' , 2021. Damian Delmas. ``flexvec: SQL Vector Retrieval with Programmatic Embedding Modulation.'' arXiv:2603.22587, 2026. DOI: 10.48550/arXiv.2603.22587. Chengkai Li, Kevin C.-C. Chang, Ihab F. Ilyas, and Sumin Song. ``RankSQL: Query Algebra and Optimization for Relational Top-k Queries.'' , 2005. DOI: 10.1145/1066157.1066173. Ronald Fagin, Amnon Lotem, and Moni Naor. ``Optimal Aggregation Algorithms for Middleware.'' , 2001. DOI: 10.1145/375551.375567. Vespa. ``YQL Query Language,'' ``Ranking Expressions,'' and ``Phased Ranking.'' Vespa Documentation, accessed 2026. Liana Patel, Siddharth Jha, Melissa Pan, Harshit Gupta, Parth Asawa, Carlos Guestrin, and Matei Zaharia. ``Semantic Operators and Their Optimization: Enabling LLM-Based Data Processing with Accuracy Guarantees in LOTUS.'' , 2025. Alex Garcia. ``sqlite-vec: API Reference.'' https://alexgarcia.xyz/sqlite-vec/api-reference.html. Version 0.1.9 used in the SQL comparisons; {cosine implementation}. { *{Example bindings}} {section}{Example bindings} The bindings use PyTerrier , pandas , and NumPy . lists every document; and map identities to their FAISS scores for the two query vectors. contains the recorded first-pass result. contains the single fixture query, passed to each pipeline through . weight = dict )) D = pt.Transformer.from_df ) M = pt.Transformer.from_df A = pt.apply.doc_score ) B = pt.apply.doc_score ) W = pt.apply.doc_score def score_id_order : out = frame.sort_values( , ascending= , kind="mergesort" ).reset_index out = np.arange ) return out T = pt.apply.generic The explicit ordering stage supplies the comparison's tie policy. PyTerrier's default tied ranks follow input order. Code, fixture instructions { }, and dependencies are available at . After building the fixture and installing the pinned dependencies described there, run the {} comparison from the repository root: python proofs/pyterrier/examples.py The native SQL alternatives bind to the stored vector for query and to document 's embedding, and use these views. SQL's window maximum and PyTerrier's both cover the same 52 rows with 1}; the maximum is , and the normalized weights agree exactly. CREATE TEMP VIEW m AS SELECT docno AS id FROM pyterrier_bm25_results WHERE qid='1'; CREATE TEMP VIEW w AS SELECT docno AS id, score / max OVER AS weight FROM pyterrier_bm25_results WHERE qid='1'; 