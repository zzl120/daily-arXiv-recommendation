 {icml2026} {} {cleveref} {Theorem} {Proposition} {Lemma} {Corollary} {Definition} {Assumption} {Remark} {Example} {Pr} {4pt} {4pt} }{}{} {4pt} {4pt} }{}{} {4pt} {4pt} }{}{} {todonotes} [ {yyy} {sch} {yyy} {School of Computer Science, Peking University, Beijing, China} {School of Information, Renmin University of China, Beijing, China} {wuyinjun@pku.edu.cn} 0.3in ] To reduce LLM costs and latency, semantic caching systems must accurately identify when a new prompt matches a cached one. Current methods often rely on simplistic similarity measures, which limit their effectiveness. We introduce , a novel semantic caching approach that significantly improves retrieval accuracy by integrating Multi-Vector Retrieval . \ is built upon a learnable segmentation model that intelligently splits prompts, enabling fine-grained similarity comparisons via MaxSim. We derive the model's training objective from a rigorous theoretical analysis. This can ensure that optimizing this objective directly maximizes cache hits under strict correctness constraints. To solve the resulting non-differentiable combinatorial optimization problem, we leverage a reinforcement learning-based training strategy with the theoretically grounded objectives as the reward. Experimental results on established benchmarks across diverse tasks confirm that in comparison to the state-of-the-art, \ consistently increases the cache hit rates by up to 37\ *{Acknowledgements} This work is supported by ``The Fundamental Research Funds for the Central Universities, Peking University''. {{YW: #1}} {{ali: #1}} {MVR-cache } {MVR-Dist } {MVR} {SVR} {LLM-ranker} Let ${x_1, ,x_n}$ be prompts sequentially inserted into a semantic cache. For each $x_i$, mainstream solutions like vCache store three items: its embedding $ ^d$, its LLM-generated response $r = $, and associated metadata $ $. $ $ is a sequence of pairs, where each pair links $x_i$ to a later prompt $x_j$ that identified $x_i$ as its nearest neighbor. Formally: = \{ ,c )|nn =x_i\}_{j=i+1}^n, in which $nn $ denotes the nearest neighbor of $x_j$ among $\{x_1,x_2, ,x_{j-1}\}$, $s $ is defined as the similarity score between $x_j$ and $nn $, and a Boolean label $c $ indicating whether the true response $r $ matches the cached response $r )$. The goal of performing semantic caching is to reduce LLM inference costs by reusing cached responses for new, sufficiently similar prompts. The core of this approach is to { develop a caching policy} that decides, for each new query, whether to { exploit} or { explore} . To achieve the above goal, vCache proposes a { stochastic caching policy} based on the similarity of one new prompt, $x$, to its nearest neighbor in the cache, $s $. This policy aims to maximize the cache hit rate while ensuring that the probability of returning the correct response is provably high enough. Due to the dependency of this policy on $s $, vCache begins by modeling the conditional { correctness probability} that the LLM response of a new prompt, $x$, matches that of its nearest neighbor given $s $ using the following sigmoid formula: \! =1 s ) &= ;t, ) = {1+e^{- }}. In the above formula, $t $ and $ > 0$. These two parameters are { dependent on specific cached prompts}, and estimated via maximum likelihood estimation using all the metadata of the nearest neighbor of $x$, $ )$: = _{t, }\; _{ )}\; _{} , c_i), in which, $ _{}$ represents the binary cross-entropy loss. These two estimated parameters are then incorporated into Equation to estimate the correctness probability for each new prompt $x$. This probability is then utilized to dynamically determine the exploration probability, $ $, which governs when to bypass the cache and query the LLM directly. Specifically, to compensate for potential overfitting of the parameters $ $ in Equation , which could lead to { an overestimate of $ =1 s )$}, $ $ is conservatively estimated as: = _{t', '}{1- }, = \,\! , where the minimization is over $ $ within a $ $ confidence region of $ $. This formulation guarantees, under mild assumptions, { the probability of a correct response is at least $1 - $ for any user-defined error rate $ $} . As noted in Section , cosine similarity between full-prompt embeddings may not reflect true semantic similarity, leading to suboptimal cache hit rates. To address this issue, we employ multi-vector retrieval ~, which represents each text sequence with a set of embedding vectors rather than a single vector. Specifically, let a cached prompt \ and a new prompt \ be represented by sequences of embeddings \ }), }), \}\) and \ }), }), \}\). Their similarity is then measured by the following { MaxSim} score: = _{t} _{s} }), }_j)). Intuitively speaking, $ $ first selects the most similar vector, $ }_j)$, from $x_j$ for each $ })$, and then aggregates this similarity across all $t$. Suppose one cached prompt $x_1$ is represented by three vectors, $ $ while the new prompt $x$ is associated with two vectors, $ $, we compute the pairwise similarity scores between each vector from $x_1$ and each vector from $x$ as follows: {c|ccc} & x_1^{ } & x_2^{ } & x_3^{ } \\ x^{ } & 0.01 & {0.83}} & 0.02 \\ x^{ } & & {0.80} & For each $x^{ }$ from $x$, we identify its most similar vector among $ $ and highlight its similarity to $x^{ }$ using red color, which is the same as the highest similarity score at each row. These scores are then plugged into Equation , yielding the final MaxSim score $ =0.83+0.80=1.63$ In Multi-Vector Retrieval , the effectiveness of MaxSim scores depends critically on { the strategy used to decompose text into segments}. While existing methods like ColBERT generate embeddings at the token level, this overly fine-grained approach can impair retrieval performance. To address this, introduces a method that dynamically decomposes text into coarser, semantically meaningful segments. This approach uniquely leverages retrieval performance as an optimization signal to iteratively refine the decomposition process. Building on this insight, we adapt the strategy of to semantic caching. Our method { automatically decomposes both cached and new prompts}, then uses a { segmentation-aware MaxSim score} to more reliably retrieve the real nearest neighbors. The ultimate goal is to { maximize the cache hit rate while maintaining the correctness guarantees}. The overall inference process of \ is visualized in Figure , which is described in detail below. {Figures/inference_overview.png} $ to produce a multi-vector representation for $x$. This representation is compared to cached entries using the segmentation-aware MaxSim score to retrieve the nearest neighbor $nn $ and its similarity score. Finally, the vCache module uses $nn $ and its SMaxSim score to determine whether to utilize the cached prompt. } Given an arbitrary text sequence $x$—including both incoming new prompts and cached prompts— \ first identifies a set of candidate split positions, e.g., punctuation boundaries. These split positions are denoted by $_x=\{p_1,p_2, ,p_i, \}$. A segmentation model $ $ then selects a subset of these positions to split the sequence. Formally, the model outputs an ordered list of split indices: = = , 1 i_1 < < i_{m-1} < |_x|, Using these indices, $x$ is partitioned into $m$ contiguous sub-sequences, $ }, ,x^{ })$, such that each $x^{ }$ consists of tokens from position $ +1$ to position $ $ in $x$, with the conventions $p_{i_0}=0$ and $p_{i_m}=|x|$. Each $x^{ }$ is then embedded using a shared encoder $ $, producing $m$ vectors, $ }), }), , }))$ as the multi-vector representation for $x$. The overall segmentation and embedding process can be summarized as follows: ) &= }, ,x^{ })\\ }, ,x^{ }))&= }), , })) For instance, $_x$ could be defined as an ordered sequence of positions where punctuation marks appear in $x$. For the following prompt $x$, $_x=$ since the comma occurs at the token position 6 and 14, and a period occurs at the token position 28. The period at the final position may also be treated as a special ``$<$stop$>$'' token. x= { }{ } Given $ $, a segmentation model $ $ may output a subset of these positions to indicate where the prompt should be divided. For instance, $ $ might output , indicating that the second comma at position 14 is used to split $x$ into two sub-subsequences: \[ x^{ } &= { }{ }\\ x^{ } &= { }{ } \] Note that $_x$ is prompt-dependent, which has { variable sizes} between different prompts. Similarly, the output of $ $ is a variable-length subset of $_x$. The above segmentation-and-embedding procedure can generate a multi-vector representation for each cached prompt $x_i$ and each new prompt $x$: & }), }), ) = ))\\ & }), }), ) = )) These multi-vector representations can then be incorporated into Equation to compute the MaxSim score between each $x_i$ and $x$. However, the MaxSim score is inherently { asymmetric}, which poses a critical issue in semantic caching. Returning to Example , $ $ is computed by aggregating the highest score at each column, resulting in a score of 0.89, which is only roughly half of $ $. In semantic caching, a cache hit should reflect { mutual semantic similarity} between the new and cached prompts. A high MaxSim score, which is unidirectional, however, can only guarantee partial matching between prompts-specifically, that each segment of the new prompt is similar to some segment of the cached prompt, but not necessarily vice versa. Hence, we propose to average the two normalized unidirectional MaxSim scores as the { Segmentation-aware MaxSim score} for semantic caching: &_{ } \\ &:= 0.5 where $|x_i|$ and $|x_j|$ denote the number of segments in each prompt, and normalization ensures scale invariance across prompts of different lengths. It is parameterized by $ $ since it relies on the segmentation produced by the model $ $. This symmetric measure better captures bidirectional semantic relevance, making it more suitable for cache hit determination. Using $_{ }$, the nearest neighbor for a new prompt $x$ is identified as s_{ } &= _{ } ) \\ nn_{ } &= _{x_j}_{ } . In the above formula, $s_{ } $ and $nn_{ } $ are parameterized by $ $ due to its reliance on the segmentation model, $ $. During the inference phase, $nn_{ } $ can be quickly retrieved using indexes constructed for multi-vector retrieval, e.g., PLAID . The corresponding similarity score, $s_{ } $, is then passed to the vCache module described in Section~ to determine the exploration probability $ $ for prompt $x$. Note that according to Equation , $$ and $$ also implicitly depend on $ $, which are thus dynamically updated during the training process as $ $ evolves . The segmentation model $ $ is a critical component for ensuring segmentation quality and the quality of subsequent multi-vector representations. Ideally, $ $ should possess two key properties. First, it must be sufficiently { lightweight}—ideally, much smaller than the underlying LLM—to enable real-time segmentation of new prompts during online inference. If $ $ were comparable in size to the LLM, processing each prompt would incur near-LLM-level computational overhead, negating the acceleration benefits of caching. Second, $ $ must accommodate { variable-length inputs and outputs}, as both the prompt and the number of segments are dynamic. {Figures/network_architecture.png} To fulfill these requirements, we design a lightweight model based on the pointer network architecture , which naturally handles sequences of variable sizes . Given an input prompt $x$ of length $L$, our model first encodes $x$ using a BERT encoder $ _1$ to obtain token embeddings $ $. A single-layer MLP encoder $ _2$ then transforms each $e_i$ into a pointer state $h_i$ for $i=1, ,L$. These states are aggregated into an initial hidden state $d_1$ via a single-layer LSTM $ _3$: d_1 = _{ _3} . The resulting $d_1$ represents the entire prompt context. Following the pointer network mechanism, an attention layer $ _4$ uses $d_1$ and the pointer states ${h_i}$ to compute a probability distribution over all potential split positions. However, unlike the standard pointer network which considers all input indices, our candidate split positions are a restricted subset $_x$ of $ $. We therefore mask invalid positions by assigning them zero probability. The attention mechanism for this step is formulated as: u_{1j} &= v^ , j \\ a_{1j} &= , d_1' = _{j} a_{1j} h_j\\ where $v$, $W_1$, and $W_2$ are learnable parameters in the attention layer. Hence, $ _4= $. The position $j$ with the highest probability $a_{1j}$ is selected as the first segmentation boundary. Overall, the segmentation model consists of one Bert encoder , one MLP layer , one single-layer LSTM and one simple attention layer . In our implementation, $ $ only takes around 500-600 MB GPU memory in total, which is much smaller than the capacity of typical LLMs. To select subsequent boundaries, this process proceeds recurrently. The attention output $d_i'$ is fed back into the LSTM, $ _3$, to update the context state for the next step: d_2 = _{ _3} . We then compute a new distribution by replacing $d_1$ with $d_2$ in Equation . To prevent reselection, all candidate positions up to and including the previously chosen index are masked. This recurrent selection continues until the termination token is predicted. Recall that our goal is to while preserving vCache’s through appropriate prompt segmentation. To this end, we adapt the segmentation-aware MaxSim score, $_{ }$, by training the segmentation model to minimize the MLE loss in Equation~. This aligns the similarity scores $_{ } $ with the binary label indicating whether $x_i$ and $x_j$ yield equivalent LLM responses. However, as mentioned in Section , a complication arises because the optimal $t$ and $ $ implicitly depend on $ $, thus leading to a complex dependency of the final cache hit rate on $ $. Nevertheless, we can formally show that under the following assumptions, optimizing the MLE loss maximizes the hit rate while maintaining the correctness guarantee. The similarity score $s $, conditioned on the annotation label $c$, follows a normal distribution: $ $, where $ _c$ differs between different classes, $c$ . The class distribution is balanced: $ = =0.5$. Under Assumptions and , optimizing the prompt segmentation model to minimize Equation~ maximizes the cache hit rate under an arbitrary user-specified error bound $ $. In practice, the balanced-class assumption often does not hold. We therefore employ a class rebalanced version of Equation~. The following lemma extends our guarantee to this setting. Under Assumption alone, minimizing the class-rebalanced version of Equation~ increases the cache hit rate under the same error bound $ $. We defer the full proofs of Theorem~ and Lemma~ to Appendix~. We include some empirical results in Appendix that can justify Assumption on real datasets. Following the above theoretical analysis, the training objective is to minimize the following MLE loss over all training prompts $x_i$ and their associated nearest neighbors $x_j$: _{i} _{nn_{ } =x_i} _{} ;t_i, _i), c_j) where $nn_{ } $ denotes the cached prompt most similar to $x_i$ under the segment-aware similarity measure $_{ }$. Optimizing this objective requires adapting the segmentation model $ $ to select an optimal set of split indices from the candidate set $_x$ for any prompt $x$. However, this poses two critical training challenges: 1) : the search space consists of all possible combinations of split indices in $_x$, which grows exponentially with the prompt length; 2) : the output of $ $—a discrete set of indices—does not carry gradients, preventing the direct use of gradient-based optimization. To overcome these challenges, we frame the problem as a { Reinforcement Learning for Combinatorial Optimization } task and model $ $ as a stochastic policy $ _{ }$. In the RL4CO formulation, an input prompt $x$ represents the { state}. The { action} to take from this state is a candidate segmentation, defined by a sampled subset of split indices $$ from the candidate set $_x$. The segmentation model $ $ serves as the { policy}, defining a probability distribution $ _{ } $ over all possible segmentations conditioned on the input prompt. For each training step, we randomly sample a prompt $x_i$ and consider all prompts $x_j$ where $nn_{ } =x_i$. We then sample segmentations: $} _{ } $ and $} _{ } $ for $x_i$ and each $x_j$. Using these segmentations, we compute the segment-aware similarity $_{ } $ and the corresponding Binary Cross-Entropy loss. The reward is the negative sum of these losses: & = _{nn_{ } =x_i} - _{} ;t_i, _i), c_j) We optimize $ $ with by maximizing the following expected reward: _{ }\; _{} _{ } ,} _{ } nn =x_i} . The expectation is approximated via Monte Carlo sampling from the policy $ _{ }$. As discussed in Section , the values of $t_i$ and $ _i$ used in the reward depend implicitly on the current segmentation policy $ $. Therefore, at each training step, we temporarily freeze $ $ and update $t_i$ and $ _i$ by solving the maximum likelihood estimation in Equation over $x_i$ and its current set of nearest neighbors. While the segmentations for $x_i$ and its neighbors $x_j$ are generated online at each training step, the nearest neighbor mapping $nn_{ } $ itself is also dependent on $ $. Ideally, updating $ $ would require recalculating this mapping for the entire cache at every step, as segmentations for all cached prompts would change. However, performing this full-neighbor recalculation online in every iteration is prohibitively expensive. To resolve this efficiency bottleneck, we { keep the nearest neighbor mapping fixed} at each training step and only update it periodically—once every $K$ steps—where $K$ is a hyperparameter. This strategy decouples the costly neighbor search from the frequent policy updates, achieving an effective balance between model performance and computational cost. Following , we assume access to ground-truth LLM responses for all training prompts. The label $c_j$ for a pair of prompts $ $ is determined via exact string matching of their corresponding LLM responses. { Input:} A training set of prompts with ground-truth LLM responses $$; The policy model $ _ $ $T$} Sample a prompt $x_i$ from $$, determine all $x_j$'s in $$ satisfying $nn =x_i$ Sample segmentation $$ from $ _{ } $ for each $x=x_i$ and $x_j$'s satisfying $nn =x_i$ Perform inference on $x_i$ and each $x_j$ Compute $_{ } $ between $x_i$ and each $x_j$. Update $ $ by optimizing Equation with REINFORCE Update $t_i$ and $ _i$ by solving Equation for $x_i$ $ using $ $ and update the set of prompts taking $x_i$ as the nearest neighbor Semantic caching has emerged as an effective mechanism for reducing the latency and computational cost of large language model inference . Traditional caches based on exact string matches fail to exploit the underlying semantic similarity between different, but conceptually equivalent, prompts—requiring redundant model invocations. To address this limitation, semantic caches can serve paraphrased or semantically related queries by embedding prompts into a semantic vector space and reusing responses based on similarity, substantially increasing the { cache hit rate, i.e., the ratio of reusing the cache's response}, and thus improving system efficiency~. Production examples include Azure semantic caching option for LLM APIs { }, and LiteLLM caching system { } Existing semantic caching methods primarily focus on how to design reasonable cache policies to determine when to reuse the cache or not, based on the similarity scores between incoming prompts and their nearest neighbors in the cache. For instance, the static policy approaches~ compare such similarity scores against a global threshold to decide the cache's reuse, while the recently proposed vCache method~ employs dynamically learned, prompt-specific thresholds to provably guarantee correct response. Only when such similarity scores are high enough, the cached response of the nearest neighbors is reused. However, the similarity measures used in these methods are all simple ones, such as cosine similarity between the prompts' embeddings, which may not accurately capture the subtle semantic differences in prompts, particularly for those semantically complex ones. Consequently, semantically dissimilar prompts may be incorrectly matched, degrading the cache hit rate. For instance, we draw a prompt, $x$, from the SemCacheClassification dataset in Figure , which is a positive review of an adult crime drama. In a standard cosine similarity lookup, this prompt returns $x_1$ as the nearest neighbor. While $x_1$ is also a review for the same type of drama—sharing salient keywords like ``crime'' that define their primary topic—it contains negative comments. This subtle semantic difference leads to a divergent LLM response for $x_1$ compared to $x$, resulting in a cache miss despite their high topical similarity. {Figures/intro_example.png} . Using a single embedding per prompt, a new query $x$ about whether an adult crime drama review is friendly finds its nearest neighbor via cosine similarity. However, their LLM responses differ, thus causing a cache miss. In contrast, \ learns to segment prompts, particularly extracting the sentences with positive comments as individual segments for $x$ and its real nearest neighbor, $x_2$. By embedding these segments, the segmentation-aware MaxSim score of $x_2$ to $x$ exceeds that of other cached prompts, thus allowing the reuse of the cached response of $x_2$ for a cache hit. } To address this issue, we propose to employ Multi-Vector Retrieval to facilitate the retrieval of the real nearest neighbor for a new prompt. Originally proposed for information retrieval by ColBERT , MVR decomposes both queries and documents into fine-grained pieces and encodes each piece into one embedding, thus producing multi-vector representations for queries and documents. The query-document similarity is then computed using the { MaxSim score}: for each query vector, its maximum similarity with any document vector is identified, and these scores are aggregated. As revealed by ColBert and its follow-up works, MVR provides a nuanced matching mechanism that excels at capturing partial matches, thereby enhancing retrieval accuracy. However, its performance hinges critically on the strategy used to segment text since the multi-vector representations are produced by embedding each of these segments. While ColBERT's default approach uses single-token embeddings, recent work has shown this to be suboptimal, demonstrating that optimized segmentation is key to unlocking MVR's full potential. We therefore propose a novel prompt segmentation method, , for semantic cache, which aims to { maximize the cache hit rate while strictly adhering to user-specified error guarantees}. \ begins with the development of { a lightweight segmentation model} designed with two essential properties: { minimal model capacity to ensure efficiency} so that the decision of whether to exploit the cached prompts is made instantly online, and the ability to produce { a variable number of segments per prompt}, which is fundamental to the flexible nature of MVR where the vector count per sample varies. However, two critical challenges arise for effective training over this model. First, the { cache hit rate is a discrete and non-differentiable metric}, which thus cannot serve as the training objective for the segmentation model. Instead, we propose a surrogate objective, which aims to align the segmentation-aware MaxSim score of a new prompt to its nearest neighbor in the cache with identical LLM responses. As a consequence, for the example in Figure , the MaxSim score between $x$ and its real nearest neighbor $x_2$ could become higher than others. A { rigorous theoretical analysis} reveals that optimizing this objective guarantees maximizing the vCache cache hit rate without violating the correctness constraints. In addition, due to the discrete nature of segmentation decisions, the output space of the segmentation model is large and complex, which is a { combinatorial space of all possible segmentation points on each prompt}. Hence, optimizing the above objective remains { non-differentiable}. To overcome this, we frame the problem of training the segmentation model as a { reinforcement learning for combinatorial optimization } task, where we design a reward function directly informed by our theoretical objective, enabling effective gradient-based learning of the segmentation model. We further perform extensive experiments on a variety of benchmark datasets, covering diverse tasks such as classification, search and open-ended generation. The results demonstrate that in comparison to the state-of-the-art, \ can consistently increase the cache hit rate by up to 25\ Our contributions are summarized as follows: , a method that learns an { efficient, lightweight segmentation model} to segment prompts to facilitate multi-vector retrieval to identify the real nearest neighbors in the cache for a new prompt. A { rigorous theoretical analysis} that formalizes a training objective, proving that optimizing it is equivalent to maximizing cache hit rate under correctness guarantees. A { reinforcement learning solution} to train this segmentation model by framing it as a { combinatorial optimization problem}. { Extensive experiments} on a variety of benchmark datasets covering diverse tasks demonstrate the effectiveness of . {Figures/multi_dataset_cache_hit_comparison_01.png} protocol with $ =0.01$} {Figures/multi_dataset_error_rate_comparison_01.png} protocol with $ =0.01$} {0.68 } {Cumulative end-to-end inference latency . Values in parentheses denote algorithm running time excluding LLM calls.} {!}{ {c|cccc} & SemCacheClassification & SemCacheSearchQueries & PromptBench & QNLI \\ vCache & & & & \\ ColBert & & 6521.89 & 2294.38 & 1626.37 \\ POQD & & 6990.08 & 2945.20 & 2648.80 \\ & } & } & } & } \\ } {0.28 } {Figures/gneralizability_comparison.png} {Generalization of the segmentation model trained on PromptBench to QNLI.} We compare \ against the state-of-the-art semantic caching method { vCache}, which provides a correctness guarantee on its caching policy. In addition, we attempt to incorporate the two representative segmentation methods used in MVR, { ColBert} and { POQD}, into vCache. The details of adapting these two methods to vCache are provided in Appendix . Detailed configurations of \ are provided in Appendix . We evaluate semantic caching under two prompt-insertion protocols. Our default setting follows the standard vCache protocol~, which we denote as { cache-on-miss only}: an incoming prompt is inserted into the cache only when it results in a cache miss, while prompts served by cache hits are not inserted. This protocol is used for all main cache hit rate, error rate, and latency results. We also evaluate a { always-cache} protocol, where every incoming prompt is inserted into the cache regardless of whether it is a hit or miss. This setting keeps the cache contents identical across methods and isolates the effect of retrieval quality from differences in cache growth. We evaluate \ and baseline methods on the following four datasets, covering a variety of tasks. { SemCacheSearchQueries}: a subset of 150K prompts from the ORCAS dataset for the { web search} task. { SemCacheClassification}: a benchmark containing 45K short prompts for { classification}, collected from three diverse ecommerce text classification dataset~.} { PromptBench}: PromptBench provides a framework to comprehensively evaluate the robustness of LLMs by perturbing prompts. We follow to perturb each prompt of the SQUAD-V2 dataset in different manners such that perturbed prompts can ideally have the same LLM responses to the original ones. We follow to determine the equivalence of the LLM responses between prompts. This finally yields 38K prompts for the { question answering} task. { QNLI:} is another { question answering} benchmark . We follow the same procedure as above to perturb prompts and generate labels, which results in 29K prompts. While the first two datasets have been used to evaluate vCache , they consist primarily of short prompts that do not reflect more complex, emerging scenarios—such as multi-turn conversations or reasoning tasks with lengthy, semantically rich prompts. To address this, we include the PromptBench and QNLI datasets, which capture these previously overlooked settings. Unlike prior systems such as vCache, our approach involves offline training of a segmentation model. Accordingly, we split each dataset into training, validation, and test subsets. The model is trained on the training split, with the validation split used for model selection. Notably, { the training split is intentionally limited—containing only 3K prompts per dataset}—to reflect the practical constraints of obtaining labeled data for this task. Indeed, according to the ablation studies in Appendix , a training set of 3K is sufficient to train the segmentation model. To ensure fair comparison with baseline methods like vCache, all approaches are evaluated on the same set of test prompts. Throughout the experiments, the BGE model is configured as the default embedding model to embed prompt segments, while GPT-4o-mini is employed to generate the ground-truth response for each prompt. We report the following metrics: 1) { cache hit rate}, defined as the ratio of the prompt that exploits the cache to the total number of prompts; 2) { error rate}, calculated as the ratio of false positive cache hits to the total number of new prompts; 3) { latency}, which measures the overall inference time, including the segmentation time, embedding time, retrieval time, and LLM invocation time . Due to space limits, we report results for $ =0.01$; the results for other $ $'s are in Appendix~. We follow to plot the curve of the cumulative cache hit rate as the number of incoming prompts increases, which is shown in Figure . We also examine the real error rate in this setting. As reported in Figure , the error rate of all methods, including , gradually increases to a stable value below the user-specified $ $, indicating that \ respects the correctness guarantees. The similar performance pattern indeed occurs under the { always-cache} protocol. As shown in Figures~ and~ , \ maintains much higher cache hit rates while keeping the error rate below the user-specified bound under this protocol. Figure~ suggests that \ can consistently achieve higher cache hit rates than all baseline methods, where the gains are up to 37\ Although \ introduces additional computation from prompt segmentation and multi-vector retrieval, it achieves higher cache hit rates, which reduces costly LLM invocations and accelerates end-to-end inference. As shown in Table~ , \ reduces the inference overhead by up to 6\ {Figures/multi_dataset_cache_hit_comparison_01_insert_all.png} protocol with $ =0.01$. } Unlike vCache, which is a training-free method, \ requires performing additional training to obtain one segmentation model for prompt segmentation, which is conducted per dataset in the above experiments. However, we additionally evaluate the generalizability of the segmentation model trained on the PromptBench dataset to the QNLI dataset, which is reported in Figure . This figure surprisingly reveals that our segmentation model still outperforms all baseline methods in terms of the cache hit rate, even in the out-of-the-distribution setting. MVR-cache uses labeled prompt pairs for offline training, collecting ground-truth LLM responses for 3K prompts per dataset this introduces a one-time labeling cost. This one-time cost is outweighed by downstream benefits: on SemCacheClassification, MVR-cache increases cache hits by 9\ Further analyses, including ablations on the embedding model, training set size, and a detailed per-prompt online overhead breakdown, are provided in Appendix~. Current efforts to improve semantic cache hit rates largely focus on optimizing caching policies through similarity measures between prompts. While some works fine-tune embedding models to align prompts and responses , these approaches lack generalizability to closed-source models and are susceptible to distribution shifts . Hence, the state-of-the-art methods avoid model tuning and instead rely on prompt similarity. These include static threshold policies and adaptive approaches like vCache , which learns prompt-specific thresholds online with correctness guarantees. { In contrast, our work is the first to enhance cache hit rates by improving the similarity measure itself, integrating multi-vector retrieval and MaxSim}. Multi-vector retrieval overcomes the representational limitations of single-vector dense retrieval by encoding queries and documents as sets of vectors, scored via the MaxSim operator from ColBERT . While subsequent work has primarily focused on improving the efficiency of MVR systems , recent research demonstrates the critical impact of query segmentation granularity on performance and introduces an adaptive segmentation strategy guided by retrieval accuracy. { Diverging from this approach—which uses heuristic document segmentation—our method jointly optimizes the segmentation of both new queries and cached documents.} Additional query reformulation work is discussed in Appendix~. We present \ that integrates MVR with a learnable segmentation model. We derive a training objective that maximize hit rates while preserving correctness guarantees and formulate segmentation as a reinforcement learning for the combinatorial optimization problem. {section}{Appendices} {0} {} Let $s_{}$ be the similarity score between one prompt and its nearest neighbor in the cache store using the conventional SVR-based cosine similarity score, $s_{} $. Likewise, let $s_{}$ be the similarity score under the learned similarity metric $s_{} $ in . Both are assumed to be random variables in $ $. We write $s_m$ for $m \{,\}$. Assume each $s_m$ admits class-conditional densities on $ $: \[ f_1 := f , f_0 := f_m , \] and corresponding CDFs $f_1,f_0$. The marginal density is the mixture \[ p := f = f_1 + f_0 , \] where $ = $. The population-level MLE loss is defined as: \[ = \! [ , c) ] = {2}\, _{P_1} [ }) ] + {2}\, _{P_0} [ }) ]. \] We further assume that $ = = = 0.5$, For each method $m \{,\}$, we assume that the similarity score conditioned on the label $c$ , $s $, follows Gaussian distribution: \[ s P_1 = , s P_0 = , \] where $s_m $ denotes the similarity score between a prompt $x$ and its nearest neighbor from the cache store using either vcache or , $ _{c}$ is the mean of the Gaussian distribution, dependent on the method $m$ and the annotation label, $c$. We further assume an identical standard deviation of these normal distributions across different $c$ and $m$. Hence, $f_{c}$ defined in Definition denotes the density function of a Gaussian distribution, i.e.: f_{c} = f = { } ^2}{2 ^2}) Under Assumption~, the correctness probability defined in Equation , $ := $ is rewritten as follows: = {1+ \{- \}}, with = - _{0}}{ ^2}, t = + _{0}}{2}. By Bayes' rule, \[ {1- } = { } = { } = {f_0 }. \] Taking logs and substituting $f_{c}$ with Equation , yielding {1- } &= {f_0 }\\ &= -)^2- ^2}{2 ^2}\\ &= -+ _{1}^2)- }{2 ^2}\\ &= - _{0}}{ ^2}s - ^2- _{0}^2}{2 ^2}\\ &= - _{0}}{ ^2} . Furthermore, according to the definition of $ $ in Equation , $ {1- }$ is further derived as: {1- } = { \{- \}} = By comparing the above formula against Equation , we can obtain $ := / ^2$ and thus $t = + _{0}}{2}$. } Let $ := _{1} - _{0}$. Under the above assumptions, the population-level MLE loss, $ $ satisfies: $ $ depends on $ $ only through $ $. $ $ is strictly decreasing in $ $ for $0 < < 1$. Consequently, the global minimum of $ $ over all feasible $ $ is attained at \[ _{1} = 1, _{0} = 0. \] { Step 1: Explicit form of the risk} Since the class prior is balanced, the population risk can be written as = {2}\, _{P_1} [ }) ] + {2}\, _{P_0} [ }) ]. Substituting \( = / ^2 \) and \( t = /2 \), we obtain = {2} _{P_1} \! [ \! ( 1+ \! ( -{ ^2} ( s-+ _{0}}{2} ) ) ) ] + {2} _{P_0} \! [ \! ( 1+ \! ( { ^2} ( s-+ _{0}}{2} ) ) ) ]. { Step 2: Translation invariance.} Define the centered variable \[ u := s - + _{0}}{2}. \] Then \[ = {2}, = -{2}. \] Under equal variance and bounded support, the distributions of $u y=1$ and $u y=0$ are translations of each other. Therefore, the joint law of $u$ depends on $ $ only through $ $. Hence, \[ = , \] which proves statement . { Step 3: Derivative with respect to $ $.} Differentiating under the expectation , \[ }{d } = -{2 ^2} _{P_1} \! [ {1+ \! } ] + {2 ^2} _{P_0} \! [ {1+ \! } ]. \] Using the symmetry \( u y=0 {=} -u y=1 \), this simplifies to \[ }{d } = -{ ^2} _{P_1} \! [ {1+ \! } ]. \] { Step 4: Sign of the derivative.} Under $P_1$, the random variable $u$ has strictly positive mean $ /2$ and bounded support. The function \[ g = {1+ \! } \] has strictly positive expectation for $ > 0$. Therefore, \[ }{d } < 0 , \] which proves statement . { Step 5: Optimality under bounded scores.} Since $s $, we have \[ 0 _{0} _{1} 1 0 1. \] Because $ $ is strictly decreasing on $ $, its minimum is attained at $ =1$, i.e., \[ _{1} = 1, _{0} = 0. \] This proves statement . Theorem indicates that adapting the similarity score $s$ by minimizing the population-level MLE loss can push $ _{1}$ and $ _{0}$ to 1 and 0, respectively, thus maximizing the gap between these two variables, which is equivalent to maximizing $ $. Plus, the following lemma suggests the stability of the value of $t$ throughout training: Under the above assumptions, the value of $t$ is invariant under the gradient flow of the following logistic loss: $$ = -y g - ),$$ in which, $$g = ) $$ If each score $s$ is updated via \[ s = -{ s} = - - y), \] then \[ t = 0, \] The logistic loss for a sample $ $ is \[ = -y g - ), \] with gradient \[ { s} = - y). \] The dynamics of the class means are: \[ _1 = = - \, = \, , \] \[ _0 = = - \, = - \, . \] Thus the midpoint evolves according to \[ t = {2} = {2} . \] Since $f_1$ and $f_0$ are the density function of two normal distributions of the same variance while $t$ is the mid point of the means of these two normal distributions, then $f_1 = f_0 $. Plus, based on the definition of the function $g $, $g = 1 - g $ holds. Hence, we have \[ = ) \, f_1 \, d = g \, f_0 \, d = . \] Hence, \[ m = {2} = 0. \] At the same time, the inter-class separation evolves as \[ {d } = _1 - _0 = > 0, \] which is strictly positive as long as $ _1 < 1$ and $ _0 > 0$. Therefore, $t$ is stable while the separation grows monotonically. The above analysis suggests that minimizing the MLE loss can maximize $ _1$, minimize $ _0$, maximize $ $, and maintain the value of $t$. Hence, for those samples with positive labels, $ $ increases, and $ $ defined in Equation decreases, thus decreasing the exploration probability and increasing the cache hit rate while the same error bound $ $ remains fixed. } In the case of the imbalanced class distribution, we can then consider the following class-balanced version of the MLE loss: _i , in which the class prior $ = $ could be estimated by the ratio of the positive samples in the training set. The corresponding population-level MLE loss is thus derived as: & = \! [ , c) ] = \, _{P_1} [ { } }) ] + \, _{P_0} [ {1- } }) ]\\ & = _{P_1} [ }) ] + _{P_0} This indicates that the population-level MLE loss remains the same. Hence, we can follow the same proof of Theorem to prove Lemma . We provide the details of adapting ColBert and POQD to vCache as follows, which both use the symmetric MaxSim score as the similarity measure as : { ColBert}: As a pioneering MVR method, it decomposes text into individual tokens and embeds each separately. We adapt it for vCache by encoding tokens from cached and new prompts to produce sequences of embeddings. { POQD}: A state-of-the-art MVR segmentation method that fine-tunes the prompt prefix for a general-purpose LLM to segment queries. For documents in storage, POQD applies heuristic decomposition . Accordingly, we segment cached prompts by sentences, while segmenting incoming prompts using POQD, where fine-tuning is performed using the same training set as . Throughout the experiments, the candidate split positions $_x$ are defined as the indices of all punctuation marks in the prompt $x$. To enable efficient retrieval during inference, we use a two-stage retrieval pipeline. We first construct an HNSW index~ on the single-vector representations of cached prompts and retrieve the Top-20 nearest-neighbor candidates. We then rerank these candidates using the learned segmentation-aware MaxSim score $_{ }$ to select the final nearest neighbor. Thus, MVR-cache uses single-vector retrieval only as a coarse candidate generator and does not require the single-vector Top-1 result to be correct, as long as the correct neighbor is included in the Top-20 candidates. We validate this design on PromptBench: this two-stage retrieval pipeline achieves a nearest-neighbor recall of 0.179, which is close to 0.183 from a full MVR scan over the entire cache, while avoiding the substantially higher overhead of exhaustive multi-vector retrieval. This small gap indicates that Top-20 single-vector retrieval preserves most relevant candidates, and the MVR reranker can correct many single-vector Top-1 errors. While recent methods such as PLAID~ enable efficient retrieval directly over multi-vector data, our preliminary tests show that they introduce substantial computational overhead and do not meet our real-time inference requirements. A related line of retrieval work improves matching quality through query expansion and pseudo-relevance feedback , which reformulate the original query using related or feedback-derived terms~. These methods offer a different latency--quality trade-off from embedding-based approaches: they can improve recall with relatively low overhead, but primarily rely on lexical reformulation rather than learned semantic matching. Recent work further extends this direction with learning-based query reformulation, including reinforcement-learning methods~ and neural rewriting for conversational search~. Similar ideas also appear in semantic and approximate caching systems~, including ROSE~, which improves cache robustness to misspellings and approximate matches through rewriting. Our method is related in spirit to query reformulation, but differs in both objective and mechanism. Instead of generating rewritten queries, \ learns a variable-number segmentation of each prompt and matches the resulting segments to cached prompts using a learned segmentation-aware similarity function. This allows the cache to compare prompts at a finer semantic granularity without replacing the original prompt with a rewritten textual form. MVR-cache currently segments prompts as linear text sequences. This design applies directly to multi-turn conversations by concatenating the system prompt, dialogue history, and current user query before segmentation. Extending the method to multimodal inputs requires new decomposition mechanisms, such as region-level units for images or temporal units for audio, and is left for future work. We include the experimental results with varied values of $ $ in this section. We further report the cache hit and error rate by varying $ $ among $\{0.015,0.02,0.03,0.05,0.07,0.08\}$. These results are included in Figure -, which suggest that consistent performance gains of \ in comparison to the baseline methods. {Figures/multi_dataset_cache_hit_comparison_02.png} {Figures/multi_dataset_error_rate_comparison_02.png} {Figures/multi_dataset_cache_hit_comparison_03.png} protocol with $ =0.03$} {Figures/multi_dataset_error_rate_comparison_03.png} protocol with $ =0.03$} {Figures/multi_dataset_cache_hit_comparison_05.png} protocol with $ =0.05$} {Figures/multi_dataset_error_rate_comparison_05.png} protocol with $ =0.05$} {Figures/multi_dataset_cache_hit_comparison_07.png} protocol with $ =0.07$} {Figures/multi_dataset_error_rate_comparison_07.png} protocol with $ =0.07$} {Figures/multi_dataset_cache_hit_comparison_08.png} protocol with $ =0.08$} {Figures/multi_dataset_error_rate_comparison_08.png} protocol with $ =0.08$} {Figures/multi_dataset_cache_hit_comparison_015.png} protocol with $ =0.015$} {Figures/multi_dataset_error_rate_comparison_015.png} protocol with $ =0.015$} {Figures/multi_dataset_error_rate_comparison_01_insert_all.png} protocol with $ =0.01$.} Table~ reports the average per-prompt latency breakdown. This table complements Table~: Table~ reports cumulative end-to-end latency, while Table~ decomposes the per-prompt cost into segmentation, embedding, retrieval/reranking, and one LLM call. All values are reported in milliseconds. As shown in Table~, the online non-LLM overhead of MVR-cache is small compared with the latency of a single LLM call. The main additional cost comes from the lightweight segmentation step, while retrieval/reranking remains below 0.4 ms per prompt. Therefore, even for infrequent queries where this overhead cannot be amortized over many cache hits, LLM inference remains the dominant cost. 0.15in {llccccc} Dataset & Method & Seg. & Emb. & Ret./rerank & Non-LLM total & LLM call \\ SemCacheCls. & MVR-cache & 23.00 & 32.00 & 0.35 & 55.35 & 1234.60 \\ & vCache & -- & 25.00 & 0.20 & 25.20 & 1234.60 \\ SemCacheSQ & MVR-cache & 28.00 & 32.00 & 0.35 & 60.35 & 3004.20 \\ & vCache & -- & 23.00 & 0.20 & 23.20 & 3004.20 \\ PromptBench & MVR-cache & 22.00 & 32.00 & 0.35 & 54.35 & 3352.00 \\ & vCache & -- & 25.00 & 0.30 & 25.30 & 3352.00 \\ QNLI & MVR-cache & 23.00 & 32.00 & 0.35 & 55.35 & 4273.00 \\ & vCache & -- & 20.00 & 0.30 & 20.30 & 4273.00 \\ -0.1in {Figures/embedding_models_comparison.png} We further perform ablation studies for \ using the promptbench dataset with the error bound $ =0.01$. First of all, in Figure , we compare the effect of \ using varied embedding models including the default BGE model, GTE Large model and E5-large , which suggest that there is almost no performance difference for \ between different embedding models. {Figures/training_samples_comparison.png} In addition, we also vary the number of training samples for training the segmentation model. As Figure shows, regardless of the training set sizes, \ ends up with almost the same performance. This thus indicates that a training set with 3K training samples is sufficient to obtain a reasonable segmentation model. {Figures/maxsim_comparison.png} In the main experiments, we use punctuation marks as candidate split positions. To test whether this design choice limits the segmentation policy, we compare it with three alternative candidate sets on PromptBench while keeping the rest of the MVR-cache pipeline fixed: keyword-level boundaries, token-level boundaries, and sentence-level boundaries. Keyword-level boundaries include punctuation marks and selected keywords such as ``and'' and ``or''; token-level boundaries include punctuation marks and spaces; sentence-level boundaries include punctuation marks excluding commas; and punctuation-level boundaries correspond to our default setting. Figure~ shows the cumulative cache hit rate as the number of incoming prompts increases. The curves are highly similar across all candidate sets, indicating that giving the policy a larger set of possible split positions does not lead to a meaningful improvement. In particular, punctuation-level splitting remains competitive with, and slightly better than, the larger candidate sets in the final cache hit rate. This suggests that punctuation marks already provide a compact and effective search space for prompt segmentation in our setting. {Figures/multi_dataset_cache_hit_comparison_01_sensitivity_analysis_split_position.png} We also report the number of segments selected by the learned policy in Table~. The average segment count is close to one for short search queries and increases for longer question-answering prompts. This shows that the policy does not use a fixed segmentation length, but adapts the number of segments to the input structure. 0.15in {lccc} Dataset & Min & Max & Mean \\ SemCacheSearchQueries & 1 & 4 & 1.01 \\ SemCacheClassification & 1 & 93 & 2.64 \\ QNLI & 1 & 149 & 5.31 \\ PromptBench & 1 & 262 & 7.67 \\ -0.1in In the end, we also replace the symmetric MaxSim score, i.e., SMaxSim, used in \ with the vanilla MaxSim score, which is unidirectional. The results are reported in Figure , which reveals a slight performance gain if the symmetric MaxSim score is used. We further examine whether MVR-cache remains effective for very long prompts. Long-context inputs are challenging for semantic caching because a single-vector representation can dilute local semantic details. A long prompt may contain many repeated or related sub-questions, and the information that determines response equivalence may appear only in specific parts of the input. As a result, single-vector retrieval may retrieve a broadly related but response-inequivalent nearest neighbor. Table~ shows a representative example. The incoming prompt contains around 10K tokens and consists of many sub-questions about the CASA 1000 electricity transmission project. To make the example readable, we show only representative excerpts from the incoming prompt and the retrieved nearest-neighbor prompts, omitting repeated sub-questions with ellipses. 0.15in {5pt} {1.08} {p{0.13 }p{0.72 }p{0.08 }} {l}{} \\ {p{0.94 }}{ ``Describe the project named CASA 1000. What is the intended purpose of the CASA 1000 project? CASA 1000 will transmit 1000 MW of surplus electricity from Tajikistan to Pakistan with power transit through Afghanistan. Further clarify the role and involvement of Tajikistan, Pakistan, and Afghanistan. Describe potential risks, timeline, budget, revenue sharing, and regional impacts.'' } \\ Method & Retrieved nearest-neighbor prompt excerpt & Result \\ vCache & ``Explain in detail the capacity of a solar hot water system including global installations as of 2007, approximately 154 thermal gigawatt . What does this capacity represent in terms of the number of homes, hospitals, or businesses that can be supported? Describe the geographical distribution of these installations and the average system size.'' & {c}{Miss} \\ MVR-cache & ``The CASA 1000 project promotes regional energy security through regional cooperation. Explain how regional cooperation on energy projects like CASA 1000 enhances regional energy security. Describe the planned project objectives, participating countries, infrastructure deployment, risks, and regional economic implications.'' & {c}{Hit} \\ -0.1in The difference comes from how the two methods represent the long prompt. vCache compresses the entire prompt into one embedding. In this example, the retrieved prompt is broadly energy-related, but it concerns solar hot water capacity rather than the CASA 1000 project. Since the two prompts require different LLM responses, vCache misses the cache. In contrast, MVR-cache decomposes the long prompt into multiple segments and compares them with cached prompts using $_{ }$. The learned segments preserve specific local semantics, such as the project goal, the participating countries, implementation risks, and regional impacts. These segment-level matches allow MVR-cache to retrieve a nearest neighbor from the same response-equivalence set and produce a correct cache hit. This example illustrates why variable-length segmentation is useful for long-context prompts: the relevant matching evidence may be distributed across several local spans and can be obscured by a single global representation. } Recall that our theoretical results rely on Assumption~, which states that the class-conditional distributions of the learned segmentation-aware similarity scores approximately follow normal distributions. To empirically validate this assumption, we evaluate the learned $_{ }$ scores on all four datasets. Specifically, for each dataset, we randomly sample prompts and compute their similarity scores against their retrieved nearest-neighbor prompts, then group the scores according to their corresponding labels. As shown in Figures ~ - ~, the resulting class-conditional score distributions are approximately normal across all datasets, providing empirical support for Assumption~. For the additional assumption used in Theorem~, i.e., Assumption~, we acknowledge that it may be strong in practice. To address this, Lemma~ shows that under Assumption~ alone, minimizing the class-rebalanced objective still improves the cache hit rate under the same error bound. Therefore, since Assumption~ is empirically supported across all four datasets, the theoretical implication of Lemma~ remains meaningful. {Figures/histogram_SemCacheSearchClassification.png} on SemCacheSearchClassification dataset} {Figures/histogram_SemCacheSearchQueries.png} on SemCacheSearchQueries dataset} {Figures/assumption.png} on PromptBench dataset} {Figures/histogram_QNLI.png} on QNLI dataset} *{Impact Statement} This paper presents work whose goal is to advance the field of machine learning. There are many potential societal consequences of our work, none of which we feel must be specifically highlighted here.