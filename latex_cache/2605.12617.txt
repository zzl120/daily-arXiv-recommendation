 summarizes the Amazon Reviews 2023~ datasets used in the main experiments. User sequences are chronologically ordered, truncated to 20 items, and split via the leave-last-out protocol. Computed from our 5-core preprocessed splits . Four training targets per user sequence ; evaluation uses leave-one-out.} {lrrrrc} Dataset & \# Users & \# Items & \# Interactions & Sparsity & Avg.\ Seq.\ Len \\ Musical Instruments & 57{,}439 & 24{,}587 & 511{,}836 & 99.96\ Industrial \& Scientific & 50{,}985 & 25{,}848 & 412{,}947 & 99.97\ Video Games & 94{,}762 & 25{,}612 & 814{,}586 & 99.97\ checks whether the attention-ablation pattern in is specific to Instruments dataset. We repeat the $m{=}0$ 1-layer decoder ablation on all three datasets. Removing self-attention causes a modest loss, while removing cross-attention causes a large loss on every dataset. This supports the design choice in : retain an encoder-conditioned context readout and remove repeated decoder attention. {{6pt}{0.95} 0$ 1-layer decoder.} SA and CA denote self-attention and cross-attention, respectively. Values are N@10; relative drops are computed against the first row within each dataset.} {ccccc} SA & CA & Instruments & Scientific & Games \\ \ & \ & 0.0318 & 0.0236 & 0.0480 \\ \ & \ & 0.0295 ($-7.2\ \ & \ & 0.0130 ($-59.1\ \ & \ & 0.0111 ($-65.1\ } reports the per-dataset branching factors behind the search-space collapse discussed in . In each evaluated catalog, the first digit has the full 256-way choice, the second digit has about 38 valid continuations on average, the third digit has about 2.2, and the fourth digit is close to singleton. Later digits still have occasional multi-choice prefixes, but their average search space is already near singleton within each dataset. Number of valid continuations at digit $t$ given a valid prefix $c_{<t}$, computed separately within each dataset's item vocabulary. No cross-dataset average is used.} {lccccc} Dataset & \# items & Avg. $|c_1|$ & Avg. $|c_2\! \! c_1|$ & Avg. $|c_3\! \! c_{1:2}|$ & Avg. $|c_4\! \! c_{1:3}|$ \\ Instruments & 24{,}587 & 256 & 37.7 & 2.19 & 1.17 \\ Scientific & 25{,}848 & 256 & 37.7 & 2.24 & 1.19 \\ Games & 25{,}612 & 256 & 38.1 & 2.19 & 1.20 \\ This appendix checks whether also applies to an LLM-based generative recommender. LC-Rec~ releases public checkpoints on Amazon Reviews 2018~, so we follow its protocol on Musical Instruments, Arts Crafts and Sewing , and Video Games. The dataset statistics are shown in . Users and items with fewer than five interactions are filtered.} {lrrrrc} Dataset & \# Users & \# Items & \# Interactions & Sparsity & Avg.\ Seq.\ Len \\ Musical Instruments & 24{,}773 & 9{,}923 & 206{,}153 & 99.92\ Arts Crafts and Sewing & 45{,}142 & 20{,}957 & 390{,}832 & 99.96\ Video Games & 50{,}547 & 16{,}860 & 452{,}989 & 99.95\ Two design philosophies exist for accelerating LLM-based generative recommenders: jointly fine-tune the LLM backbone with a draft head , which breaks plug-and-play deployment; or keep the LLM frozen and train a plug-and-play student, preserving compatibility with existing checkpoints. follows . We distill from , a 7B LLaMA-2-based generative recommender. Since LC-Rec only releases public checkpoints on Amazon Reviews 2018, we adopt its protocol; retraining TIGER at ${ }$1M/5M/13M on the same categories gives a single cross-scale Pareto view . LC-Rec has no separate encoder, requiring us to redesign the multi-head attention sources in . Recent studies on LLMRec identify a layer-wise attention sparsity inversion: early layers retain dense, informative patterns that encode broad user history, while later layers become highly sparse and redundant~. Building on this insight, we extract the key--value states from all history-item tokens at an early layer of the frozen prefill . For the query, we extract the last-layer hidden state of the final prompt token, as it is directly optimized for next-token prediction and closely aligns with the immediate recommendation intent. The per-digit MLP heads, trie-constrained beam search, and KL+CE objective remain identical to the T5-based . The training protocol follows the TIGER setup in Appendix~. To accommodate the LLaMA backbone, we simply project the 4096-dimensional Q and KV states down to $d_h{=}768$ via learnable linear layers. For hyperparameters, we sweep the number of stacked attention readout layers over \ , FFN dimension $ \{512, 1024, 2048, 3072\}$, and head hidden size $ \{512, 1024, 2048, 3072\}$. sits on the Pareto frontier at both teacher scales. At the TIGER scale, it matches the teacher while delivering an $8.74 $ throughput speedup. At the 7B LC-Rec scale, recovers 83.3--97.5\ {graphs/Pareto_2018.pdf} NDCG@10 vs throughput across Instruments, Arts, and Games. All throughputs are end-to-end except LC-Rec 7B and LC-Rec , reported as optimistic upper bounds. Baselines span SASRec~ , TIGER , and LC-Rec 7B ; students are TIGER and LC-Rec , distilled from the 5M and 7B teachers respectively. Marker size encodes parameter count.} This appendix reports the full hyperparameter and $m$-mode curves for Instruments, Scientific, and Games. is organized by analysis type: each row is one sweep, and each column is one dataset. All runs use the same distillation recipe as the main experiments. {0.32 } {graphs/MI_alpha_sweep.pdf} {0.32 } {graphs/IS_alpha_sweep.pdf} {0.32 } {graphs/VG_alpha_sweep.pdf} {0.32 } {graphs/MI_hhid_scaleup.pdf} {0.32 } {graphs/IS_hhid_scaleup.pdf} {0.32 } {graphs/VG_hhid_scaleup.pdf} {0.32 } {graphs/MI_m_sweep.pdf} {0.32 } {graphs/IS_m_sweep.pdf} {0.32 } {graphs/VG_m_sweep.pdf} Top row: $ \{0,0.3,0.5,0.7,0.8,1.0\}$ sweep. Middle row: head-hidden width sweep. Bottom row: $m$-mode accuracy--throughput tradeoff. Columns correspond to Instruments, Scientific, and Games.} Instruments and Scientific peak at $0.7$, while Games peaks at $0.8$. All three datasets show smooth curves with no sharp cliff, indicating that the KL+CE mixture is robust. NDCG@10 saturates by head hidden width 512 on Instruments, 768 on Scientific, and 1536 on Games. Throughput decreases monotonically with head width, so the saturation point gives the practical operating point for each dataset. All three datasets show the same tradeoff shape. $m{=}0$ is the fastest fully distilled setting and matches the teacher in the main results. $m{=}1$ anchors only the first SID digit with the teacher and provides an intermediate accuracy--speed point, consistent with the branching analysis in . This appendix provides the multi-seed stability analysis for . Our core claim is quality preservation: replaces the autoregressive decoder without degrading the accuracy of the original TIGER-kv checkpoint. reports the seed-level NDCG@10 values and a formal non-inferiority test. We train across three random seeds ). The seeds control the model initialization, data loader order, and minibatch stochasticity. TIGER-kv is evaluated using the single fixed teacher checkpoint from the main paper, serving as a deterministic reference. Since our objective is to match rather than outperform the teacher, we employ a one-sided non-inferiority \ -test. Let \ denote the NDCG@10 for seed \ , \ denote the deterministic TIGER-kv baseline, and \ define a 1\ \[ H_0: - , H_1: > - . \] The test statistic is \ / \), with \ and degrees of freedom \ , where \ is the sample standard deviation. A \ -value \ rejects the null hypothesis, formally confirming that does not incur a statistically significant performance drop beyond the 1\ Values are computed over seeds \ . The symbol \ indicates passing the non-inferiority test against TIGER-kv with a 1\ {4.4pt} {!}{ {lccc cccc} Dataset & N@10 & N@10 & N@10 & Mean\ std & TIGER-kv & Diff. & \ \\ Instruments & 0.0333 & 0.0332 & 0.0331 & 0.0332\ 0.0001\ & 0.0323 & +0.0009 & 0.001 \\ Scientific & 0.0252 & 0.0250 & 0.0248 & 0.0250\ 0.0002\ & 0.0243 & +0.0007 & 0.007 \\ Games & 0.0512 & 0.0512 & 0.0511 & 0.0512\ 0.0001\ & 0.0512 & +0.0000 & 0.006 \\ } As shown in , all three datasets strongly satisfy the \ criterion. This multi-seed evaluation statistically grounds our claim: successfully preserves the teacher's ranking quality while delivering the substantial speedups reported in . Faster generative recommendation lowers compute and energy cost per recommendation, with positive environmental impact. No novel risks beyond generic recommender-system concerns . It does not introduce a new recommendation objective, collect new user data, or change the user-modeling assumptions of the teacher recommender. Therefore, we do not introduce qualitatively new risks beyond those already associated with recommender systems. This subsection details the training configurations deferred from~. We optimize the trainable modules using AdamW with a cosine learning-rate schedule. Offline student training takes approximately one hour on average for each dataset . This offline cost is separate from online serving and is excluded from throughput measurements. Throughput profiling uses the trained checkpoints under the protocol in Appendix~. We sweep learning rate $ \{1{-}5, 5{-}5, 1.25{-}4, 2.5{-}4, 5{-}4\}$, dropout $ \{0.1, 0.2\}$, weight decay $ \{0, 1{-}4, 1{-}3\}$, distillation weight $ \{0, 0.3, 0.5, 0.7, 0.8, 1.0\}$, attention heads $ \{4, 8\}$, attention inner dimension $ \{128, 256, 384, 512\}$, and head hidden size $ \{128, 256, 512, 768, 1024, 1536, 2048\}$. For the encoder distillation , we instantiate the position-specific MLP encoder with 4 layers. Both Stage 1 and Stage 2 utilize the same AdamW optimizer and hyperparameter search space as . We use a targeted sweep centered on the best per-dataset settings. For Stage 1, we sweep depth \ , FFN dimension \ with dataset-local candidates , learning rate \ . For Stage 2, we freeze the encoder and train the decoder side using the same hyperparameter scope as introduced before. summarizes the notations used throughout the paper. {6pt} {1.0} {@{}p{0.20 }p{0.78 }@{}} & \\ $, $ & User and item sets; $u$ and $v$ index users and items \\ $_u= $ & Historical item sequence of user $u$ \\ $_u, S_u$ & Serialized encoder tokens for $_u$ and its non-padding length \\ $c^{ }= $ & Semantic ID of item $v$; $c_t$ is digit $t$ and $c_{<t}$ its prefix \\ $, C, L$ & Per-digit codebook, codebook size, and SID length; TIGER uses $C=256$ and $L=4$ \\ $p_ $ & Teacher autoregressive distribution over SID digits \\ $B, K, m$ & Beam size, recommendation cutoff, and teacher-generated prefix length in $m$-mode \\ $E_T , G_ $ & Frozen TIGER encoder and student encoder; both map serialized user-history tokens to encoder states \\ $_u, }_u$ & Teacher states and encoder states \\ $d_h, d_e$ & Encoder hidden-state size and frozen token-embedding size \\ $e $ & Frozen SID-token embedding lookup \\ $W_q, , }, $ & Query projection/query and cached one-shot context vectors \\ $f_t $ & Digit-$t$ MLP head mapping the prefix-conditioned input to sliced student logits \\ $_t, _t$ & Input vector and student logits for digit $t$ \\ $}_t, c_t^ $ & Teacher logits and ground-truth codebook index for digit $t$ \\ $ , , _t, $ & Distillation weight, temperature, per-digit loss, and total loss \\ $_t$ & Active beam set after digit $t$ \\ $N, i, r$ & Teacher decoder layer count, encoder token position, and layer index \\ $_{u,i}^{ }, _u^{ }$ & token state and pooled global vector at encoder layer $r$ \\ $a_i, F_{a_i} $ & Digit-role id for token position $i$ and the corresponding role-specific MLP \\ {1.0} This appendix documents how we reproduced NEZHA~ in our two teacher settings. The key point is that we preserve NEZHA's hidden-state contract: a backbone pass produces one root hidden state and one hidden state per SID digit, and an MTP head predicts SID digits with per-position heads plus a recurrent state update and joint fine-tuning. The adaptation only maps NEZHA's placeholder construction to the teacher architecture: encoder-side placeholders for TIGER, response-side placeholders after the LC-Rec prompt for LC-Rec. TIGER encodes the user sequence with a T5 encoder and normally decodes the target SID with a T5 decoder. To instantiate NEZHA without changing the SID tokenizer, we use the same abstract input pattern on the encoder side: \[ [, q_{}, x_1, ,x_t, _1,_2,_3,_4], \] where \ is TIGER's user/context token and \ are the history SID tokens. The final context hidden state before \ is used as $h_0$, and the four placeholder hidden states are used as $h_1, ,h_4$. The T5 decoder is bypassed in the NEZHA path. The T5 encoder and the MTP head are jointly fine-tuned, matching NEZHA's joint backbone-plus-head training principle. The target remains exactly the four TIGER SID digits. For TIGER NEZHA, we tuned the base learning rate over \ , swept weight decay over \ , and tested NEZHA's official parameter-group learning-rate scaling; neither improved the reported TIGER result. LC-Rec is already a decoder-only LLaMA-based generative recommender trained with its own prompt template. For adaptation, the input is the same instruction prompt used by LC-Rec with an empty response field, followed by four SID placeholders: \[ [, q_{}, x_1, ,x_t, q_{}, _1,_2,_3,_4], \] where \ and \ are the LC-Rec prompt text before and after the rendered history; \ includes the response header. Thus the placeholders are appended after LC-Rec's response prompt, not directly after the final historical item SID. The hidden state of the final prompt token before \ is $h_0$, and the four placeholder hidden states are $h_1, ,h_4$. The labels at the placeholder positions are the original four LC-Rec SID tokens. This keeps the LC-Rec teacher's input semantics intact: the history and instruction remain in the prompt, while the response SID span is replaced by NEZHA placeholders. We fine-tuned the 7B LC-Rec backbone across four A6000 GPUs. Given the computational cost, we focused our sweep on the base learning rate \ and selected the best validation checkpoint. To ensure a rigorous reproduction, we extensively tested other high-impact factors: we compared raw-SID inputs versus prompt-preserving inputs, applied NEZHA's official parameter-group learning rate multipliers , and swept the inference MTP search budget over \ . Despite these efforts, none of the configurations closed the accuracy gap to the LC-Rec teacher. On both TIGER and LC-Rec, NEZHA acts as a valid high-speed joint-finetuning baseline but consistently suffers from accuracy degradation. For TIGER, the port keeps NEZHA's MTP mechanism but moves placeholders to the encoder and trains with hard SID labels. For LC-Rec, the prompt-preserving adaptation isolates the modification strictly to the response generation path. Despite mechanically well-defined ports and extensive hyperparameter sweeps, NEZHA fails to automatically recover the quality of the original teachers. All experiments run on a single NVIDIA RTX A6000 GPU using PyTorch 2.1, CUDA 11.8, and Python 3.10. details the runtime metrics behind the main results in . The timed inference region starts after model initialization and data loading, encompassing GPU forward passes, beam expansion, valid-SID masking, final item lookup, and metric computation. Offline teacher feature extraction for training is excluded from this online serving profile. We use batch size 32 and beam size $B{=}50$ across all tested methods. Throughput and speedup are averaged across the three datasets, while NDCG@10 and peak memory are reported from the Musical Instruments run . measures ranking quality. denotes end-to-end throughput, and is its inverse . is relative to TIGER-kv. captures the maximum allocated GPU memory during inference, and reports this footprint as a percentage of the TIGER-kv baseline. Throughput, speedup, and latency are averaged across the three datasets; NDCG@10 and peak GPU memory are measured on Instruments. TIGER-kv serves as the speedup and memory-ratio baseline. = best and = second-best per metric.} {3.2pt} {0.95} {lrrrrrr} Method & NDCG@10 & samples/s & Speedup & ms/sample & Peak Mem. & Mem./TIGER-kv \\ TIGER & 0.0323 & 320 & 0.75$ $ & 3.13 & 0.84 & 40.6\ TIGER-kv & 0.0323 & 424 & 1.00$ $ & 2.36 & 2.07 & 100.0\ NEZHA & 0.0308 & 3{,}082 & 7.27$ $ & 0.32 & & & & 706} & & & & & & 347} & & & 0.43 & 20.8\ {1.0} The profile reveals a practical Pareto frontier. achieves the best NDCG@10 while remaining 8.74$ $ faster than TIGER-kv end-to-end and reducing peak memory from 2.07GB to a mere 0.09GB (a 95.7\ AtSpeed is adapted to TIGER rather than used in its original open-vocabulary causal-LM setting. TIGER uses a T5 encoder--decoder and emits four constrained SID digits. The AtSpeed rows in use the same TIGER checkpoints and sentence-T5 SIDs as the main results. The profile uses the full test split and beam \ . AtSpeed-S uses strict tree verification: an accepted step requires the teacher top-\ beams to be contained in the draft tree. AtSpeed-R uses relaxed verification: draft samples are accepted by rejection sampling against the teacher distribution, and missing beams are filled from the correction distribution. Original AtSpeed targets long autoregressive LM decoding. Its speedup comes from using a cheap draft to verify many future tokens of a costly teacher. TIGER does not match this setting. The teacher has only 4.59M parameters, and generation stops after four constrained SID digits. AtSpeed-S reaches 286.3 samples/s, or \ TIGER-kv. It matches teacher quality because it verifies teacher greedy top-\ containment, but it still pays for draft beam search and teacher tree verification. With only four decoder steps, this overhead is hard to amortize. AtSpeed-R reaches 94.0 samples/s, or \ TIGER-kv and trails TIGER by 27--37\ We evaluate 's robustness to temporal item shift, where test targets represent recently introduced items. Following the timestamp-based evaluation protocols of SpecGR~ and ColdGenRec~, we compute each item's first interaction timestamp in the chronological log. A test case is assigned to the Temporal-80 or Temporal-90 subset if its target item's first interaction occurs after the global 80\ Test NDCG@10 on targets whose first interaction timestamp is after the global 80\ {3.4pt} {l rrrr rrrr} & {c}{} & {c}{} \\ {2-5} {6-9} Dataset & \# Test & TIGER & & $ $ & \# Test & TIGER & & $ $ \\ Instruments & 4{,}272 & 0.0076 & 0.0076 & +0.0000 & 2{,}051 & 0.0079 & 0.0078 & -0.0001 \\ Scientific & 5{,}573 & 0.0152 & 0.0158 & +0.0006 & 3{,}122 & 0.0134 & 0.0149 & +0.0015 \\ Games & 21{,}212 & 0.0193 & 0.0194 & +0.0001 & 10{,}434 & 0.0190 & 0.0191 & +0.0001 \\ Average & -- & 0.0140 & 0.0143 & +0.0003 & -- & 0.0134 & 0.0139 & +0.0005 \\ shows that seamlessly tracks the frozen TIGER teacher on temporally shifted targets. Across all three datasets, performs identically or slightly better, yielding average NDCG@10 improvements of $+0.0003$ and $+0.0005$ on the 80\ We compare distillation against scratch-training to justify our focus on the distilled setting. evaluates a 1-layer Transformer decoder and our decoder on Instruments dataset. For scratch-training, we train exclusively on hard SID labels by removing the KL term. {{5.5pt}{0.95} Values are NDCG@10. The relative drop is measured against the distillation row.} {lccc} Decoder Family & Distill & Scratch & Drop vs. Distill \\ 1-layer Transformer decoder & 0.0318 & 0.0313 & $-1.6\ MLP decoder & 0.0332 & 0.0323 & $-2.7\ } Without distillation, the 1-layer Transformer loses 1.6\ This subsection documents the remaining baseline ports used in . These adaptations keep the TIGER SID tokenizer and valid-SID constraints fixed, so the comparison isolates decoder-side acceleration under the same recommendation task. Unless noted otherwise, baseline hyperparameters follow the authors' recommendations unless adaptation to the TIGER backbone is required. We adapt EARN to the TIGER encoder because TIGER stores the user history in encoder states before SID decoding. One prefix register and one suffix register are placed at the first and last valid encoder positions. After encoder layer \ , all non-register states are pruned; the remaining encoder layers and the decoder cross-attention use only the register states. We jointly fine-tune the resulting T5 model and the register embeddings, sweep \ , and select checkpoints by validation NDCG@10. We also check whether TIGER supports EARN's head/tail register placement. On 1,280 validation samples, TIGER encoder attention does not form the dual sink reported by EARN: across the four encoder layers, the first three positions receive 10.1--14.2\ GDN and Mamba2 use the same frozen TIGER encoder features as . Each model receives the encoder hidden states followed by a learned start token or a bridge token and prefix embeddings from the frozen TIGER token embedding. It outputs 256-way logits for each SID digit and applies the same valid-SID constraints during beam search. We train only the SSM-side modules, bridge/start parameters, and output head with the same KL+CE objective as . During beam search, each active prefix carries an SSM recurrent state; every digit step gathers parent states, updates them with the selected prefix token, and forks states for the expanded beams. This makes the short-SID setting dominated by state movement rather than long-sequence scan complexity. We ablate the encoder distillation along two key design choices. reports NDCG@10 across Instruments, Scientific, and Games; the $ $ columns show the relative change versus the full . Sharing a single MLP across all positions consistently degrades NDCG@10 by 1.0--1.4\ The variant skips the Stage-1 hidden-state matching and trains the encoder directly using only the final KL+CE ranking objective. This causes severe degradation: $-6.0\ Values are test NDCG@10 on Amazon Reviews 2023. $ $ represents the relative change compared to the full .} {4.5pt} {lcccccc} & {c}{} & {c}{} & {c}{} \\ {2-3} {4-5} {6-7} Variant & N@10 & $ $ & N@10 & $ $ & N@10 & $ $ \\ & & --- & & --- & & --- \\ Shared MLP & 0.0325 & $-1.0\ End2End & 0.0308 & $-6.0\ =1 {natbib} {neurips_2026} {inputenc} {fontenc} {Appendix}{Appendices} {Section}{Sections} {caption} {../figures/}{figures/}} @figure{!t} @table{!t} {0.5pt plus 1pt minus 1pt} {0.5pt plus 1pt minus 1pt} {} {} {}{Sid-Mlp} } {}{Sid-Mlp++} } , Yupeng Hou, Clark Mingxuan Ju, Neil Shah, Julian McAuley \\ University of California, San Diego, Snap Inc.\\ }@ucsd.edu, \\ }@snap.com } Generative recommendation models employing Semantic IDs exhibit strong potential, yet their practical deployment is bottlenecked by the high inference latency of beam-expanded autoregressive decoding. In this work, we identify that standard attention-heavy Transformer decoders represent a structural overkill for this task: the hierarchical nature of SIDs makes prediction difficulty drops sharply after the first token, rendering repeated attention computations highly redundant. Driven by this insight, we propose , a lightweight MLP-centric distillation framework that fundamentally simplifies the decoding paradigm for GR. Instead of executing complex, step-by-step attention mechanisms, our approach captures the global user context in a single operation, decoupled from sequential token prediction. We then distill the heavy autoregressive teacher into position-specific MLP heads, eliminating the dense attention overhead while preserving prefix and context dependencies. Extensive experiments demonstrate that matches the accuracy of teacher models while accelerating inference by $8.74 $. Crucially, this distillation strategy can serve as a plug-and-play accelerator for different backbones and tokenizer settings. Furthermore, we introduce , extending our distillation framework to replace the Transformer encoder, unlocking further latency reductions. Ultimately, our work reveals that decoder-side MLPs distillation is an effective acceleration path for structured SID recommendation, while full encoder replacement offers an additional speed--accuracy trade-off. Our code is available at: ~. Generative recommendation approaches~ diverge from the conventional sequential recommendation paradigm~, which models user histories with atomic item IDs; instead, each item is often represented by a semantic ID , an ordered tuple of discrete tokens from a compact shared vocabulary. This formulation enables semantically similar items to share token prefixes and avoid large embedding tables which scale with item cardinality . In this way, next-item prediction is framed as autoregressive SID generation, which has shown promising performance~. However, the high inference latency of GR has hindered its deployment in production systems. Consider a GR model that represents each item with a 4-token SID. Unlike conventional models, which can produce top-$K$ predictions with a single model forward pass~, GR models usually rely on beam search to generate multiple candidate SID sequences, with each sequence requiring four model forward passes due to the autoregressive generation mechanism. To achieve competitive performance, the beam size $B$ is often set even larger than $K$, leading to a total of roughly $4 B$ model forward passes during inference. This cost can be prohibitive in recommender systems, where latency is critical to user experience. Developing broadly applicable methods for accelerating GR is non-trivial. Speculative decoding~ has been widely used to accelerate large language model inference. However, GR requires generating top-$K$ candidate sequences, which makes the verification step difficult to adapt, as it would need to verify and rank multiple candidate sequences simultaneously~. Multi-token, parallel, and non-autoregressive methods~ remove the left-to-right decoding dependency, but often require jointly fine-tuning the teacher model or relying on a specific tokenizer, limiting their applicability to general model architectures and other recent advances in the field. Knowledge distillation ~ offers a promising pathway by transferring knowledge from a teacher model to a more efficient student model. However, standard same-family KD only reduces model size while retaining much of the decoding time complexity. Note that the decoding space of GR is typically well structured, leaving room for inference acceleration. Unlike large language models~, whose generations are open-ended and variable-length, GR models produce short and fixed-length outputs, such as 4-token SIDs in TIGER~, with a limited set of valid token sequences. As empirically observed in~, once the first few tokens are determined, the number of valid continuations rapidly drops to only a few choices, far below the number of all theoretically possible combinations. This observation motivates a natural question: To this end, we investigate whether a simpler model architecture can be used for efficient GR inference. Our hypothesis is that a heavy model is still needed to learn from raw token sequences, but the structural regularity of SID generation enables a much cheaper approximation at inference time. We begin by replacing the original Transformer decoder with one of the simplest neural architectures: multilayer perceptrons ~. However, naive MLPs do not perform well, since such regularities become useful only when the first few tokens are predicted correctly, as also demonstrated by our empirical results in~. Taking the above considerations together, we propose , an MLP-centric distillation framework for efficient GR inference. Given the importance of accurately predicting the first token, we retain a lightweight one-layer attention module to obtain a user-aware summary representation. A series of cascaded MLPs is then used to generate the remaining tokens sequentially. Since the decoding spaces at different positions are usually disjoint, we use a separate MLP with independent parameters for each position. To condition each position on the previously generated prefix, the input to each MLP is constructed by concatenating the embeddings of the generated prefix tokens. Experiments on public benchmarks demonstrate that matches Transformer-based teacher model's performance with an average of $8.74 $ speedup. We further introduce , which extends the distillation to the encoder side and increases the speedup to $10.25 $ while maintaining competitive performance. {0.38 } {motivation_collapse} {SID search-space collapse on Instruments. Average Valid next-token choices and top-1 accuracy.} {0.29 } {{2.5pt}{0.90} {Decoder depth redundancy on Instruments. Teacher generates first $m$ digits; a 1-layer decoder predicts the remaining $4{-}m$.} {lcc} Variant & N@10 & $ $ vs teacher \\ TIGER & 0.0323 & --- \\ $m{=}3$ & 0.0323 & $ 0.0\ $m{=}2$ & 0.0320 & $-0.9\ $m{=}1$ & 0.0320 & $-0.9\ $m{=}0$ & 0.0318 & $-1.5\ } {0.29 } {{2.5pt}{0.90} {Attention ablation on the $m{=}0$ 1-layer decoder on Instruments. SA and CA denote self-attention and cross-attention, respectively.} {cccc} SA & CA & N@10 & $ $ vs baseline \\ \ & \ & 0.0318 & --- \\ \ & \ & 0.0310 & $-5.4\ \ & \ & 0.0131 & $-60.0\ \ & \ & 0.0111 & $-65.1\ } Generative retrieval stores items as discrete identifiers and retrieves by generating those identifiers autoregressively, as in DSI~. TIGER~ brings this idea to recommendation with RQ-VAE semantic IDs and a T5 encoder--decoder; prompt, LLM, and large-scale GR systems extend backbones or serving regimes~. SID studies mainly revise identifiers: indexing and semantic/collaborative tokenization~, context-aware or multimodal IDs~, end-to-end or adaptive SID learning~, and analysis or deployment recipes~. These methods mostly change how IDs are built, analyzed, or how the recommender is trained. is complementary: given a trained TIGER-style tokenizer and encoder, it targets the repeated Transformer-decoder computation inside constrained beam search. Existing GR accelerators reduce latency in different ways. AtSpeed~ and SpecGR~ draft candidate IDs and still use the target generative recommender for verification. NEZHA~ adds self-drafting heads and a hash-based validity check, while EARN~ compresses LLMRec context with register tokens. Other efficient or alternative-generation designs use collaborative tokenization, compact modeling, parallel decoding, non-autoregressive reranking, or diffusion/masked-generation pipelines~. These approaches improve serving through verification, architecture changes, identifier redesign, or modified generation procedures. instead keeps the existing 4-digit SID space and replaces online decoder calls with prefix-conditioned MLP heads, without target-model verification. Knowledge distillation transfers teacher behavior to smaller students~; in recommendation and retrieval it is usually used for quality transfer to discriminative recommenders or generative retrievers~. GLNN~ and LLP~ show that an MLP student can absorb a teacher's structural inductive bias while removing inference-time dependencies. LLM inference accelerators span speculative decoding, KV-cache serving, and draft heads~; they target general text decoding or still require verification. State-space sequence models~ provide efficient recurrent backbones, but do not directly exploit the low-branching SID prefix structure as prefix-conditioned MLP heads do. Let $$ and $$ denote the sets of users and items, respectively. For a user $u $, their historical interaction sequence is represented as $_u = $, where $v_i $. Each item $v$ is uniquely mapped to a Semantic ID, which is a tuple of $L$ discrete tokens: $c^{ } = $. For ease of notation, we assume $L=4$ in our subsequent examples, yielding $c^{ } = $. In TIGER, the first three tokens are residual quantization codes of item embeddings from a shared codebook $$ , and the fourth is a conflict avoidance token. TIGER serializes $_u$ into an encoder token sequence $_u$ containing $S_u$ non-padding tokens. A multi-layer T5 encoder-decoder then generates the next item's SID by factorizing the joint probability autoregressively: $p_ } _u) = _{j=1}^{L} p_ $. Autoregressive SID generation relies on beam search. For an SID of length $L$, beam size $B$, and an $N$-layer decoder, a cached decoder still runs about $L B N$ decoder-block evaluations. Across all digit steps, this includes $ $ prefix self-attention work and $ $ cross-attention reads over the encoder states. Since digit $c_j$ depends on prefix $c_{<j}$, SID positions cannot be decoded in parallel, making this sequential process the main inference bottleneck. Given the inference latency caused by autoregressive decoding, we investigate the structural necessity of the full Transformer decoder through data-level and architecture-level analyses. We first characterize the inherent difficulty of the SID prediction task. As shown in Figure~, the valid codebook branching factor given a valid prefix drops sharply. On the Instruments dataset, the average branching factor drops from $256\! \!{ }38\! \!2.2\! \!{ }1.2$. Per-dataset statistics in Appendix~ show the same collapse pattern on Scientific and Games. The recommendation problem remains history-dependent, but decoder-side uncertainty is highly uneven: most branching is concentrated in the early digits, while later valid digits are strongly constrained by prefixes. For $c_3$ and $c_4$, the full $N$-layer decoder spends computationally expensive forward passes on a nearly collapsed search space. Given this sharp drop in prediction difficulty, we investigate how much decoder depth is actually utilized. We train a minimal 1-layer student decoder distilled from the frozen TIGER teacher . The teacher generates the first $m \{0,1,2,3\}$ digits, and the student predicts the remaining $4-m$ digits. As shown in Table~, a 1-layer student perfectly matches the 4-layer teacher when predicting only $c_4$ , and loses a mere $1.5\ We focus on distillation because the accuracy gap between scratch-training and distillation widens as the architecture simplifies into MLPs . Since decoder depth can be safely reduced, we investigate whether we can further decompose the remaining Transformer block to remove redundant internal components. Table~ evaluates the impact of removing specific attention modules from the $m{=}0$ 1-layer decoder. Removing prefix self-attention causes a $5.4\ These ablations present a clear structural dilemma. A context-free MLP decoder offers high inference efficiency by eliminating attention overhead, but fails severely because it loses both the fine-grained historical alignment and the position-aware prefix dependency. Conversely, the standard Transformer decoder captures this necessary context but remains impractically slow: even with caching, each active beam still runs decoder-block updates and cross-attention reads at every digit. Any successful decoder replacement must explicitly preserve both the dynamic historical context and the prefix structure while avoiding these repeated computations. This objective motivates our design of in~. We instantiate on ~, a T5-based autoregressive generative recommender where items are tokenized into 4-digit semantic IDs. freezes the TIGER encoder and replaces decoder-side generation with prefix-conditioned MLP heads. We evaluate on three categories from the latest Amazon Reviews 2023 dataset~: Musical Instruments , Industrial \& Scientific , and Video Games . Full dataset statistics are in Appendix~. We organize baselines into three distinct groups. The TIGER model serves as our autoregressive upper bound, using a four-layer T5 to generate SIDs token-by-token via beam search. We report both the standard no-cache teacher TIGER and TIGER-kv, which enables the decoder key--value cache during generation. We further evaluate our method on the LC-Rec teacher, with experimental details in Appendix~. These baselines add draft/verification modules or jointly fine-tune TIGER, whereas keeps TIGER frozen. ~, originally instantiated on LC-Rec~, uses a compact draft model for speculative decoding and teacher verification; we port it with a compact T5 draft and evaluate strict verification and relaxed sampling . ~ uses boundary register tokens to retain early-layer history information and prunes other states; we port it to the TIGER encoder with joint fine-tuning. ~ uses SID placeholders and an autoregressive draft head with recurrent state updates for self-drafting; we feed the placeholders to the TIGER encoder and use the draft head instead of the T5 decoder, with joint fine-tuning. compares these models. These baselines test whether existing linear-recurrent sequence models can serve as lightweight replacements for the TIGER decoder. They keep the TIGER encoder frozen, feeding the encoder hidden states, a bridge token, and prefix embeddings into a causal SSM stack. The SSM outputs digit logits at these added positions and is trained with the same KL+CE objective as . We evaluate on ~ and ~. We report Recall@$K$ and NDCG@$K$ over valid 4-digit SIDs. For the main row, performance metrics are averaged over random seeds \ ; reports mean\ std and a competitive performance against the fixed TIGER-kv teacher. All methods use matched TIGER checkpoints. Throughput is end-to-end samples/s on the test split with batch size 32 and beam size \ ; speedup is relative to TIGER-kv. Hyperparameters are in Appendix~; baseline adaptation and diagnostic details are in Appendices~, , and~; hardware profiling details are in Appendix~. Ranking metrics are mean values; reports seed stability over \ . Tput is the averaged throughput across three datasets, and Spd. is the speedup relative to TIGER-kv. = best, = second-best.} {3.2pt} {!}{ {p{1.98cm} rr cccc cccc cccc} & & & {c}{} & {c}{} & {c}{} \\ {4-7} {8-11} {12-15} Method & Tput & Spd. & R@5 & R@10 & N@5 & N@10 & R@5 & R@10 & N@5 & N@10 & R@5 & R@10 & N@5 & N@10 \\ TIGER & 320 & 0.75$ $ & 0.0386 & 0.0606 & 0.0252 & 0.0323 & 0.0295 & 0.0457 & 0.0191 & 0.0243 & & & & \\ TIGER-kv & 424 & 1.00$ $ & 0.0386 & 0.0606 & 0.0252 & 0.0323 & 0.0295 & 0.0457 & 0.0191 & 0.0243 & & & & \\ GDN & 510 & 1.20$ $ & 0.0385 & 0.0598 & 0.0253 & 0.0321 & 0.0278 & 0.0439 & 0.0183 & 0.0235 & 0.0585 & 0.0914 & 0.0380 & 0.0486 \\ Mamba2 & 517 & 1.22$ $ & 0.0388 & 0.0605 & 0.0256 & 0.0326 & & & & & 0.0589 & 0.0931 & 0.0386 & \\ AtSpeed-R & 94 & 0.22$ $ & 0.0246 & 0.0375 & 0.0163 & 0.0204 & 0.0216 & 0.0320 & 0.0144 & 0.0178 & 0.0436 & 0.0637 & 0.0296 & 0.0361 \\ AtSpeed-S & 286 & 0.68$ $ & 0.0386 & 0.0606 & 0.0252 & 0.0323 & 0.0295 & 0.0457 & 0.0191 & 0.0243 & 0.0612 & 0.0951 & 0.0403 & 0.0512 \\ EARN & 822 & 1.94$ $ & 0.0383 & 0.0596 & 0.0250 & 0.0319 & 0.0288 & 0.0454 & 0.0186 & 0.0239 & 0.0584 & 0.0921 & 0.0377 & 0.0486 \\ NEZHA & 3{,}082 & 7.27$ $ & 0.0371 & 0.0567 & 0.0245 & 0.0308 & 0.0254 & 0.0402 & 0.0164 & 0.0212 & 0.0560 & 0.0865 & 0.0368 & 0.0467 \\ {0.93} & 706} & & & & & & & & & & & & & \\ {0.93} & 347} & & & & & & 0.0295 & 0.0459 & & 0.0244 & 0.0578 & 0.0916 & 0.0378 & 0.0486 \\ } {{ }} {{\ }} : acts as a drop-in accelerator for already deployed models without requiring modifications to the original tokenizers, or fine-tuning of the base model. : requires no target-model verification passes. : isolates attention computation from the autoregressive loop, eliminating repeated self-attention and cross-attention during generation. : executes beam expansion as batched matrix multiplications without maintaining KV-cache updates or recurrent hidden states.} {5.0pt} {1.05} {lcccc} Paradigm & Plug-and-Play & Verify-Free & Attn-Free Decoding & Update-Free Beam \\ Speculative & & & & \\ State Pruning & & & & \\ Self-Drafting & & & & \\ {0.93} & & & & \\ compares against all baselines on the three categories. We highlight four findings: matches or exceeds TIGER NDCG@10 on all datasets while keeping the TIGER encoder frozen and requiring no target-model verification. This supports the motivation study: for short hierarchical SIDs, the teacher decoder's ranking behavior can be distilled into per-digit MLP heads as long as the student preserves the encoded user context and the ordered SID prefix. Additional hyperparameter analyses are in Appendix~. reaches 3{,}706 samples/s , with a 95.7\ AtSpeed-S and AtSpeed-R are both slower than TIGER-kv because even our smallest single layer Transformer draft is too large relative to the 4.59M TIGER teacher, unlike the 68M-vs-7B LC-Rec setting~. EARN reaches only $1.94 $ because it only compresses the encoder states, leaving the autoregressive decoder blocks and per-step computation intact. Its quality also drops because TIGER's bidirectional encoder lacks the head/tail attention-sink pattern that motivates EARN's registers. NEZHA reaches $7.27 $ by replacing the decoder with a draft-head path, but it still loses up to $15\ Mamba2 and GDN are only slightly faster than TIGER-kv and much slower than despite their linear sequence complexity. In four-digit beam search, each step must update, fork, and gather recurrent states across active beams, so state movement dominates. is update-free after the context readout and scores all active prefixes with batched dense projections. We ablate along three design axes. reports NDCG@10 and Recall@10 across Instruments, Scientific and Games; $ $ columns show relative NDCG@10 change vs . NDCG@10 and Recall@10 on three datasets; $ @10$ is relative to .} {1.4pt} {0.76} {!}{ {l ccc ccc ccc} & {c}{} & {c}{} & {c}{} \\ {2-4} {5-7} {8-10} Variant & R@10 & N@10 & $ $ & R@10 & N@10 & $ $ & R@10 & N@10 & $ $ \\ & & & --- & & & --- & & & --- \\ {l}{} \\ w/o prefix conditioning & 0.0175 & 0.0092 & $-72.0\ summed prefix embeddings & 0.0603 & 0.0324 & $-2.4\ shared MLP head & 0.0564 & 0.0301 & $-9.3\ cascaded hidden state & 0.0613 & 0.0328 & $-1.2\ {l}{} \\ w/o multi-head attention & 0.0597 & 0.0316 & $-4.8\ per-digit context readout & 0.0618 & 0.0330 & $-0.4\ w/o context FFN & 0.0616 & 0.0330 & $-0.7\ {l}{} \\ w/o KL & 0.0602 & 0.0323 & $-2.7\ full-vocab logits & 0.0619 & 0.0330 & $-0.6\ w/o teacher embeddings & 0.0616 & 0.0330 & $-0.6\ } {1.0} The variant without prefix conditioning predicts all four digits from the same context vector in parallel, without feeding prefix embeddings into later heads. It loses 64--72\ The variant without multi-head attention replaces the one-shot attention readout with a linear projection of the mean-pooled encoder states. It loses 3.1--6.4\ Removing KL distillation and training only on hard SID labels loses 2.7--6.6\ We evaluate under three settings: SID tokenizer, batch size, and beam size . A temporal item-shift diagnostic is reported in Appendix~. We distill from teachers trained with popular tokenizers: RQ-KMeans~, RQ-VAE~, and PSID~. As shown in Figure~ , retains 99.6\ Figure~ plots throughput and peak GPU memory as batch size scales from 8 to 512. TIGER-kv saturates early because each digit step still runs the Transformer decoder over all active beams. instead turns the post-context computation into batched dense projections. It's throughput scales until batch size reaches $256$, where speedup peaks at $16.6 $. At batch size $512$, keeps peak memory below $1.5$\,GB, compared with up to $30$\,GB for TIGER-kv. In Figure~ , TIGER’s throughput drops by 37\ avoids this bottleneck: after the one-shot context readout, larger beams merely widen the batched MLP projections without adding sequential steps, maintaining stable throughput and outperforming the teacher's NDCG@10 across all beam sizes. {0.32 } {graphs/robustness_tokenizer.pdf} {0.32 } {graphs/robustness_batchsize.pdf} {0.32 } {graphs/robustness_beamsize.pdf} NDCG@10 recovery is the ratio between and its teacher across tokenizers. Peak GPU memory and throughput as batch size changes. NDCG@10 recovery and throughput as beam size changes.} In this paper, we propose to eliminate the inference latency of autoregressive generative recommendation. Instead of relying on a heavy Transformer decoder, our key idea is to distill the generative process into cascaded, lightweight MLP heads. By computing the multi-head attention context exactly once, reduces beam expansion to efficient batched dense MLP operations without requiring repeated state/cache update. Extensive experiments demonstrate that matches the teacher's performance, achieving an $8.74 $ throughput speedup and a 95.7\ To resolve the autoregressive latency and heavy attention overhead identified in~, we propose . Our core idea is to replace the redundant attention operations in autoregressive generative recommendation with lightweight MLP heads. As illustrated in , we train these heads via knowledge distillation from a frozen Transformer teacher. This enables to achieve highly efficient inference while preserving the teacher's strong performance. In this section, we first detail the extraction of a global user context from the frozen encoder , then introduce the prefix-conditioned MLP heads . Finally, we describe how we transfer the teacher's knowledge to MLPs via knowledge distillation . We conclude with an extension, , that distills the encoder for further acceleration . {arch.pdf} The architecture is composed of two components: and . MH Attn is multi-head attention, $_i = e $ represents the embedding of prefix $c_i$. Snowflakes mark frozen modules, and flames mark trainable modules.} The motivation study shows that encoder-conditioned user context is essential. therefore keeps an explicit multi-head attention readout, but computes it once outside the beam loop. Let $_u=E_T ^{S_u d_h}$ be the frozen TIGER encoder states for user $u$. We mean-pool these states, project the result into a query, and use a multi-head attention block to produce a context vector that is reused across all SID digit steps: = \,W_q, } = ), = ). where MHA is multi-head attention, LN is layer normalization, and FFN is feed-forward network. Standard decoders compute cross-attention at every digit step, beam, and decoder layer, yielding an $ $ historical alignment cost. Conversely, extracts the global context exactly once using the aggregated query $$. By caching $$, we entirely remove cross-attention from the beam search loop, collapsing this alignment cost to $ $. After $$ is cached, each digit prediction only requires the current SID prefix. completely removes standard self-attention. Instead, at step $t$, each beam simply concatenates the context $$ with $t-1$ frozen token embeddings $e $ retrieved from the teacher. Because SID digits have different output distributions, each position $t \{1, , L\}$ uses a dedicated 1-hidden-layer MLP head $f_t$ to predict the next token: _t = ^{d_h+ d_e}, _t = f_t ^{C}. where $ $ is concatenation, $c_1, ,c_{t-1}$ are the available prefix digits, $d_e$ is the frozen embedding dimension, and $C$ is the codebook size. The input dimension of $f_t$ grows with $t$ and reaches at most $d_h+ d_e$. Standard decoders process prefixes via step-wise self-attention, yielding an $ $ cost alongside incremental KV-cache updates. Conversely, scores all active beams via batched dense MLP operations over static concatenated embeddings. Prefix processing thus becomes an update-free, constant-size operation per beam. We train via offline knowledge distillation from the frozen teacher. Only the single multi-head attention block and the MLP heads are trainable; the TIGER encoder is frozen. Each MLP head predicts the codebook slice for its digit, and the teacher logits are sliced to the same support. During teacher-forced training, takes the ground-truth SID prefix. Let $}_t$ denote the teacher logits and $c_t^ $ the ground-truth codebook index. We train each digit with a Kullback--Leibler distillation term plus cross-entropy: _t = ^2 D_{} \,\|\, ) + where $ $ is the distillation temperature, $ $ is softmax, and $ $ balances teacher mimicry and task grounding. The total loss is $ = _{t=1}^{L}_t$. During inference, the context vector $$ is computed exactly once. Beam search then evaluates the MLP heads sequentially, batching all active prefixes at each digit step. We use constrained beam search: a valid-prefix mask built from the fixed item-to-SID mapping during tokenization stage, removes invalid prefixes before expansion. Algorithm~ summarizes the complete pipeline. : Per-digit MLP decoder distillation and beam-search inference.} Frozen encoder $E_T$ and token embeddings $e $; serialized encoder tokens $_u$; 256-way teacher logits $\{}_t\}_{t=1}^{L}$; training records; beam size $B$. Trained ; top-$K$ item list during inference. _u, ^ , }_{1:L})$} $_u E_T $ $ $ per user} $_t $ $_t f_t $ Compute $$ using and update the trainable modules via AdamW $_u E_T $ $ $ per user} Initialize active beams $_0 \{ \}$ $_t f_t $ for all $B$ prefixes in $_{t-1}$ Apply the valid-prefix mask, expand beams within the codebook slice, and retain top-$B$ candidates into $_t$ top-$K$ items decoded from the final beam $_L$ removes the Transformer decoder stack but retains the teacher encoder. extends distillation to this encoder. It replaces the teacher encoder with an MLP encoder and produces encoder hidden states $}_u = G_ ^{S_u d_h}$; the following structure is unchanged. The encoder input is the serialized user-history sequence $ $. We assign four role-specific MLPs to process the tokens, where $F_4$ handles $c_4$, , and , and the others handle their respective $c_j$ digits. Let $a_i \{1,2,3,4\}$ denote the role index for token $i$. The initial state $_{u,i}^{ }$ combines the frozen token embedding and learnable position embeddings. To replace self-attention, we achieve global context modeling at each layer $r$ by mean-pooling all non-padding token states into a global vector $_u^{ }$. Each token state $_{u,i}^{ }$ is then concatenated with this global vector and passed through its role-specific MLP to form a residual update: _u^{ } = {S_u} _{i=1}^{S_u}_{u,i}^{ }, _{u,i}^{ } = _{u,i}^{ } + F_{a_i} . Because direct logit distillation exhibits optimization instability for the encoder , we adopt a two-stage process: We pre-train the encoder to match the frozen teacher's encoder states $_u$ using Mean Squared Error loss. We freeze the student encoder, pass its output $}_u$ to the decoder, and train the decoder using the per-digit KL+CE loss . At inference, replaces the teacher encoder states $_u=E_T $ with student states $}_u=G_ $. The cached context computation and sequential MLP heads are unchanged.