 {article} {acl} {fontenc} {inputenc} { } } } } \\ } } } \\ Microsoft, London, UK Inception42, Abu Dhabi, UAE \\ Mubadala Investment Company, Abu Dhabi, UAE } Institutional investors search visually dense pitch decks, board packs, and diligence materials that change hourly near deal closing. OCR followed by figure verbalisation is costly to refresh at this scale and can lose chart detail. We present PULSAR, a production vision-first retrieval system deployed at Mubadala Investment Company. PULSAR indexes page images with a frozen ColPali-style backbone and uses a pooled two-stage late-interaction index: compact page summaries support initial retrieval, followed by exact MaxSim rescoring over a finer pooled representation. On ViDoRe V3, this design reduces median vector-search latency by 15.1 times against an unpooled configuration with less than 0.01 absolute NDCG@10 and Recall@10 loss; production median vector-search latency is 156 ms. Under concurrent load, the pooled index sustains approximately 88 times higher QPS than an unpooled index. The event-driven ingestion path is estimated to be approximately 20 times cheaper per page than the OCR+verbalisation baseline it replaced. Since March 2026, PULSAR has served 78 thousand documents and approximately 2.4 million pages across more than 3,000 deals. At the production top K, it more than doubles answer-fact recall over the OCR+verbalisation baseline. Enterprise RAG is harder than text-only benchmarks : corporate decks and board packs contain tables, legends, chart annotations, and spatial cues that text-first pipelines lose even with image verbalisation. Verbalisation is information-reducing. The standard pipeline applies OCR or layout-aware extraction , chunks text, optionally adds vision-language model captions , and retrieves with hybrid search. Each page is run through a document OCR+layout model and a per-figure VLM, then the verbalised text is embedded: three passes where vision-first uses one. At firm scale , this is an order of magnitude more work per page than a single embedding pass and is repaid every time a deal document changes near closing. Vision-first inverts this. The page image becomes the unit of indexing and grounding. Ingestion collapses to a single forward pass of the retriever's own embedding model per page, dropping the separate OCR+layout and per-figure captioning passes and making cost scale with page count alone. On enterprise corpora, report visual retrievers outperforming textual ones. We build on VLM-based multi-vector page retrievers with late-interaction scoring , rather than OCR-free document transformers . Late interaction keeps many embedding vectors per page (one per image patch) rather than collapsing the page to a single vector, so chart values and layout cues stay addressable. At firm scale this puts billions of vectors in the index, and per-query scoring cost grows linearly in patches per page. PULSAR , our production retrieval system, is built around this cost. It composes a frozen ColPali-style backbone with a pooled two-stage retrieval pipeline and an event-driven ingestion path. Our contribution is not a new retrieval primitive but the engineering and empirical work of scaling this vision-first paradigm to production at firm scale. A decoupling of embedding-time and answer-time resolution: 150~DPI suffices for retrieval, while 500~DPI at answer time drives page-legibility errors on firm decks to zero. A two-stage pooled late-interaction index that combines mean row/column pooling for the first-stage lookup with hierarchical-pooled exact rescoring and binary quantisation, selected by a pooling-and-quantisation sweep. It cuts single-stream median vector search latency $15.1 $ at under 0.01 NDCG@10 and Recall@10 loss, and holds a 156\,ms median under production concurrency. An ingestion recipe (rolling rasterisation, strict-priority work queues, page-hash resume) that scales with page count rather than visual complexity. It is ${ }20 $ cheaper per page than the OCR+verbalisation baseline it replaces . A production deployment at Mubadala Investment Company (78k documents across ${>}3{,}000$ deals, ${ }2.4$M pages, live since March~2026). An in-house end-to-end answer evaluation shows PULSAR exceeding an OCR+verbalisation baseline on context-fact recall, answer-fact recall, and completeness at every top-$k$. Late interaction began with ColBERT , which gives each document one vector per token instead of a single dense vector and scores a query--document pair by MaxSim: for each query token, take its best match against any document token and sum. The index grows, but matching becomes finer-grained. ColPali carries the idea to page images. A vision-language backbone splits the page into patches, each patch becomes a token, and MaxSim then runs between query text tokens and image patches. The alternative to indexing the image is to verbalise it first. Systems such as SERVAL verbalise page images before text-encoding and themselves flag VLM-generated text as an unmeasured hallucination source. Both paradigms run a VLM, but verbalisation bakes its output into the index at ingestion; vision-first keeps the page image as the stored unit and defers generation to answer time, grounded on the retrieved page. Verbalisation often captures the concrete values on a chart, but not how the items relate visually: the groupings and orderings a reader uses to interpret it. Some of what analysts query for is lost in that gap. The verbalisation route has one edge. report that OCR-based RAG generalises better than vision-first retrieval . That finding applies to fine-tuning, which we avoid: PULSAR uses a pretrained ColQwen3-4B with no firm-specific fine-tuning, and still beats the baseline on the firm's own decks . Dense retrieval leans on Hierarchical Navigable Small World with quantisation as its approximate nearest neighbour backend. Late interaction strains that backend, since each page now carries many vectors rather than one. Prior multivector engines answer with a prefetch-then-rerank split: PLAID~ prefetches on centroids before a token-level rerank, and EMVB~ compresses the prefetch to bit-vectors. PULSAR keeps this split but combines two existing pooling methods across its stages: mean row/column pooling~ for the prefetch and hierarchical pooling~ for the exact rerank . Both are post-hoc and need no retraining. ColMate~ instead modifies training: its TopKSim objective averages each query token's top-$K$ patch similarities, reverting to plain MaxSim at inference. Recent hybrid-vector work such as HEAVEN~ uses a single-vector first stage over visually summarised pages followed by multi-vector reranking. PULSAR instead keeps multivectors at both stages and serves them from a continuously updated production index. Late interaction buys finer-grained matching at a cost: each page becomes many patch vectors rather than one, inflating both index size and per-query scoring. PULSAR keeps this cost in check at each stage. A single backbone pass embeds each page at a render DPI decoupled from the higher-resolution image kept for answering , so cost scales with page count, not visual complexity. Ingestion re-embeds only changed pages; retrieval scores pooled summaries rather than full patch grids, keeping exact MaxSim affordable. {figures/ingestion.pdf} ): document- and page-level hashes skip unchanged work, changed pages are re-embedded with ColQwen3-4B and upserted, and the document hash is stamped once every page is durable.} PULSAR's embedding backbone is Tomoro ColQwen3-4B , a ColPali-style multivector retriever on Qwen3-VL. At deployment, it was the strongest Apache-2.0-licensed model within our half-A10 serving budget ; the 8B sibling scored higher on ViDoRe~V3 but exceeded that budget. The ingestion and retrieval paths of PULSAR ; the rest of the system and the evaluation are this paper's contributions.} share two stores: a Qdrant vector index and a page-image store. Figs.~ and~ show the corresponding components in execution order. A blob-storage event enters one of three priority queues. The dispatcher takes the next document; a document-level hash skips it if unchanged, otherwise the indexer rasterises each page at 150~DPI for retrieval and 500~DPI for answer-time grounding, and a page-level hash of the render marks which pages changed. ColQwen3-4B embeds the 150~DPI render of each changed page. The indexer derives mean-pooled row and column vectors for HNSW prefetch and hierarchical-pooled vectors for exact reranking, then upserts them into Qdrant. The 500~DPI render is written to the page-image store. The document hash is stamped only after every changed page has been indexed and its page image persisted; queries filter out pages whose document has not yet been stamped. ColQwen3-4B embeds the text query once and leaves its token multivectors unpooled. Two HNSW searches compare the query against the mean-pooled row and column page representations. Their candidates jointly form a top-$k'$ shortlist. Exact MaxSim over the hierarchical-pooled page vectors reranks that shortlist. The top-$k{=}5$ pages are returned. Their stored 500~DPI renders are fetched from the page-image store. The downstream VLM reads those page images and generates the grounded answer. Three ingestion streams compete for the same GPU pool, fed to the indexer as priority lanes: high for real-time analyst uploads on a live deal, medium for hourly deal-document syncs, and low for slower syncs of non-deal material. Analyst workflows must not block behind either background stream. Scheduling splits across two layers: a custom dispatcher chooses which to ingest next, and vLLM~ chooses which the GPU runs next. Both preempt lower-priority work and resume evicted jobs from the last durable page, so a burst of analyst uploads never queues behind background syncs . The indexer skips redundant work at two levels . A SHA-256 of raw blob bytes gives a full-document skip. A SHA-256 of each JPEG-encoded page image gives page-level skip, so re-indexing a modified document re-embeds only changed pages. Orphan pages are deleted when page count shrinks. The document hash is stamped only after every page is durable in the vector index, so the retrieval path can filter pages whose document is not yet stamped. We render each page : a 150~DPI render feeds ColQwen3-4B for embeddings, and a separate 500~DPI render is stored and served to the VLM at answer time. Both values come from ablations . Rendering runs in a bounded producer--consumer window, so the GPU stays fed while resident memory is bounded by the window, not document length. {figures/retrieval.pdf} ): the query is embedded with ColQwen3-4B, then two quantised HNSW prefetches over mean-pooled row/column representations produce a shortlist that exact MaxSim reranks over the hierarchical-pooled vectors; the stored 500\,DPI render of each returned page grounds the VLM.} ColQwen3-4B emits over 2,000 patch vectors per page, each of dimension $D{=}320$. At firm scale this is billions of vectors live in HNSW , and exact MaxSim costs $|q|{ }|d|$ dot products per scored page. We therefore cut that count by pooling~, and keep exact MaxSim only on a short rerank list . treats the per-page patch sequence as a 2D grid and averages along each axis, yielding a handful of row- and column-mean vectors of dimension $D$. clusters similar patch vectors and replaces each cluster by its mean; the sets the average cluster size, so pool factor~3 retains ${ }1/3$ of the vectors, at which report 97.8\ retention. Mean gives a small, fixed-size summary per page; hierarchical keeps more detail at a higher per-page vector count. , or retrain the scoring rule .} These pooling methods are known individually, but no settled recipe combines them at scale, and the usual two-stage index reranks on the full multivectors. PULSAR pools that stage too, then sweeps the pooling-and-quantisation space for the fastest configuration that holds retrieval quality . The resulting index runs two pooled stages. Each page is summarised by 8--16 mean-pooled row/column vectors, binary-quantised in RAM . The query multivectors are left unpooled; two HNSW prefetches against the row-pooled and column-pooled page representations jointly produce a top-$k'$ shortlist . The shortlist is rescored with exact MaxSim over the hierarchical-pooled multivectors~, which sums, for each query text token, its best dot product against any document patch~: s \;=\; _{i=1}^{|q|} _{j }\; q_i^{\! } d_j. The top-$k$ are returned. Pooling also lets the full per-page patch vectors be discarded after ingestion, cutting the index's RAM footprint about $14 $ . The OCR+verbalisation baseline is the stack PULSAR replaced: per-page OCR+layout extraction, figure verbalisation by the query-time VLM (only filter-kept verbalisations are indexed; ), semantic chunking, and dense embedding into HNSW for dense+keyword hybrid retrieval. We evaluate the retrieval index and its footprint , ingestion cost and refresh lag , then answer quality ( , ), where answer-fact recall more than doubles over the OCR+verbalisation baseline. Embedding DPI drives per-page GPU time and stored-vector count. This GPU time dominates ingestion cost at ${ }2.4$M pages, so we take the lowest DPI whose retrieval quality is statistically equivalent to 150 and let ingestion cost break the tie. We sweep DPI with a fixed-protocol ablation on ViDoRe~V3 (single index, identical query set). ColQwen3-4B on ViDoRe~V3 ($n{=}14{,}514$ queries). Per-query paired Wilcoxon with Holm correction; cluster-bootstrapped 95\ ablation we declare practical equivalence to 150 when the TOST 90\ on the mean paired $ $NDCG@10 falls inside $ 0.01$ ; Cohen's $d$ is reported as the accompanying effect size.} {=}1280 caps input at ${ }1$\,MP ; we raise it to 65{,}536 so each render is seen at full resolution. The plateau above 150~DPI is therefore a property of ColQwen3 on these inputs.} Across the seven DPIs compared with 150 , only 72 and 100 fall outside the $ 0.01$ NDCG@10 band (Cohen's $d{=}{-}0.33$ and ${-}0.10$; both $p_{}{<}10^{-29}$), so lower DPI costs measurable quality. Every higher DPI is equivalent to 150 (equivalence-test CI inside $ 0.01$; effect sizes $|d|{<}0.05$) but costs more per page to ingest. PULSAR therefore uses 150~DPI. {figures/dpi_forest.png} {4pt} {@{}lrrrr@{}} & $ $ & $ $ & & $ $\\ O & 0$} & 0$} & 752.7 & 1.0\\ O$_q$ & $+$.0004$} & $+$.0001$} & 439.4 & 1.7\\ H+O & $-$.0002$} & $-$.0001$} & 429.8 & 1.8\\ H+O$_{qp}$ & $+$.0001$} & $-$.0002$} & 257.1 & 2.9\\ M+O & $-$.0014$} & $-$.0023$^{**}$$} & 216.2 & 3.5\\ M+H & $-$.0077$^{***}$ & $-$.0065$^{***}$ & 185.3 & 4.1\\ M+O$_{qp}$ & $-$.0026$} & $-$.0043$^{***}$ & 170.9 & 4.4\\ H & $-$.0059$^{***}$ & $-$.0037$^{***}$ & 135.4 & 5.6\\ H$_q$ & $-$.0061$^{***}$ & $-$.0039$^{***}$ & 73.9 & 10.2\\ $} $ $ & $-$.0088$^{***}$ & $-$.0074$^{***}$ & 49.9} & \\ // are no/mean/hierarchical pooling; is -pooled prefetch, -pooled rerank; $q$ marks in-place quantisation, $qp$ prefetch-only. $ $ columns are paired differences vs.\ ; ms is median vector search time; $ $ is speedup over . ~$p_{}{<}.001$, ~${<}.01$; $ $ is production.} Lower vector search latency lets the same vector-database capacity serve more concurrent searches at unchanged retrieval quality, so we target the lowest latency that holds quality. Exact MaxSim over billions of patch vectors is the dominant per-query cost. We run a ten-configuration sweep crossing pooling strategy with binary quantisation, taking the faithful no-pooling no-quantisation configuration as the paired baseline and gating on a small effect size .), this is not an equivalence question: pooling and quantisation trade retrieval quality for cost, so rather than certify parity we bound the acceptable loss by effect size on both NDCG@10 and Recall@10, and let ingest or serving cost break the tie.} Table~ summarises the sweep. All nine non-baseline configurations pass the quality gate, so the choice reduces to latency. Latencies are medians over the same $n{=}14{,}514$ ViDoRe~V3 queries, each issued single-stream against a single-replica vector database holding all ViDoRe~V3 pages.${=}128$, with ${=}200$ for the two-stage configurations.} $_{qp}$ (mean-pooled binary-quantised HNSW prefetch with hierarchical-pooled rerank) gives the lowest median vector search latency at 49.9\,ms , a $15.1 $ speedup over the 752.7\,ms baseline . The quality cost ($ $NDCG@10 $=-0.0088$, $ $Recall@10 $=-0.0074$; Cohen's $d{=}{-}0.09$ and $-0.06$) clears the $|d|{<}0.1$ gate, so PULSAR runs $_{qp}$. The speedup comes from two effects that compose. shrinks the per-page vector count the reranker scores; hierarchical pooling alone already gives $5.6 $. shrinks the per-comparison cost . Stacked, quantised hierarchical rerank reaches $10.2 $, and adding a quantised mean-pooled prefetch in front takes it to $15.1 $., $4.1 $) it is slower than hierarchical rerank alone , because the prefetch HNSW lookup itself costs more than it saves the reranker.} On the chosen $_{qp}$ index we also swept the two Qdrant search knobs. rises in quality to a knee at 200 and is flat above; has no measurable quality effect across the swept range (Friedman $p{=}0.14$; Table~). PULSAR serves {=}200 and {=}64. We validate the sweep's pooled two-stage choice on the deployment: storage and latency against an unpooled index on the dev slice . The pooled $_{qp}$ index keeps only the quantised mean-pooled prefetch vectors in RAM, cutting the RAM footprint $14 $ against the unpooled baseline . At production scale the full per-page vectors would exceed cluster RAM, so an unpooled index cannot be served live at all. {figures/pooled_vs_unpooled_latency.pdf} .} On a single query the pooled index answers in a median 175\,ms (95\ 165--186) against 1.9\,s for unpooled, a $10.9 $ gap that widens to $13 $ at p95 and $20 $ at p99.074-page dev slice on identical hardware. Both indexes retrieve by HNSW ANN. The latency/throughput curve is single-client at 1--4-way and distributed at 8--64-way, with completion/timeout accounting; the single-query result uses fixed sample counts (pooled $n{=}300$, unpooled $n{=}100$) after a 60\,s warmup, with bootstrap 95\ load test uses the dev Qdrant ; production's stack has ${ }2 $ the Qdrant CPU and disk IOPS, so these curves are a conservative lower bound on production latency.} As load rises the pooled index scales: its throughput climbs to ${ }47$ QPS at a sub-second median, every request completing. The unpooled index does not: its throughput stays flat near 0.5 QPS while latency runs to 53\,s, and it drops over half its requests at 64-way . In production, the same pooled index holds a 156\,ms median vector-search latency over a rolling 7-day window , with a concurrency-driven tail (p95 0.6\,s, p99 1.5\,s) under a load that peaks at 62~QPS. On the dev slice, we estimate ingestion cost from call counts priced at list rates, not from metered cloud spend. The OCR+verbalisation baseline runs one OCR+layout call per page plus one VLM call per figure; PULSAR runs one ColQwen3-4B forward pass per page. On this basis the baseline's per-page ingest cost is ${ }20 $ PULSAR's. 934 figures for verbalisation across the slice; only 38{,}868 (27\ downstream filter kept. Every call is still paid for, so nearly three-quarters of the per-figure VLM cost buys output that is discarded.} Per-document end-to-end ingestion latency on the dev cluster , measured across the dev slice, follows a rolling median $T_{} 2.9 + 1.41\,p$ seconds : a ${ }3$\,s steady-state per-document overhead (object fetch, document open, index upsert) plus ${ }1.4$\,s per page. Freshness comes from skip-aware ingestion : each sync re-embeds only the pages that changed, not the whole corpus, so refresh lag tracks edited-page count, not document length. A single-page edit to a multi-hundred-page deck is searchable in seconds. Over the 7-day window the pipeline cleared 50{,}368 changed pages, but the change rate is bursty: the busiest hour alone cleared 5{,}491. Change-to-searchable lag had a median of 2.0\,s and a p95 of 43\,s; in that busiest hour p95 held at 63\,s and the worst case at 328\,s. Priority scheduling runs live-deal uploads continuously and background deal material on an hourly cadence, keeping analyst uploads ahead of the background refresh. With embedding DPI locked at 150 for retrieval, we run an answer-time DPI ablation on two firm investment-committee decks . Both are firm-authored IC packs with the chart, table, and figure density typical of production traffic. Only the page image served to the VLM at answer time varied . For each page, a large language model generated three questions on its chart, table, and figure content; SMEs validated the questions and authored the gold answers. The same question set was reused at every DPI. SMEs labelled each system answer as correct or as a page-legibility error: an unreadable page image at that DPI, not generation drift on a faithful page . Page-legibility errors fall monotonically with DPI on both decks, reaching zero at 500, so PULSAR serves 500~DPI at answer time. {5pt} {@{}l rrrr@{}} & $k{=}1$ & $k{=}3$ & $k{=}5$ & $k{=}10$\\ {@{}l}{}\\ Completeness & 1.68\,$ $.02 & 2.34\,$ $.04 & 2.55\,$ $.03 & 2.85\,$ $.04\\ ContextFactRecall & .134\,$ $.005 & .322\,$ $.004 & .388\,$ $.013 & .507\,$ $.008\\ AnswerFactRecall & .102\,$ $.002 & .226\,$ $.010 & .271\,$ $.005 & .355\,$ $.003\\ {@{}l}{}\\ Completeness & & & & \\ ContextFactRecall & & & & \\ AnswerFactRecall & & & & \\ {@{}l}{75$ SME-authored questions ; cells are mean\,$ $\,std over 5 passes. Completeness is a 1--5 LLM judgement of the system's answer against the gold answer; Context/AnswerFactRecall are the shares of gold facts in the retrieved context and in the answer. Bold marks PULSAR higher at the same $k$.} We compare PULSAR end to end against the OCR+verbalisation baseline on answer quality. PULSAR scores higher on Completeness and both context- and answer-fact recall at every $k \{1,3,5,10\}$ . The evaluation set is $n{=}75$ SME-authored investment questions with SME-authored gold answers ; both retrieval paths run the same questions and generate answers with the same answer-time VLM, so only the indexing path differs (verbalised text chunks vs.\ page images). An LLM judge (validated against blinded SME re-scoring; App.~) scores each system's output against the gold. The two paths index different units, so we report answer-level, modality-independent metrics, not retrieval rank. To separate run-to-run model noise from sampling uncertainty, we cluster by deal (the correlated unit) and report in the bottom block of Table~ the deal-weighted PULSAR$-$baseline delta with a 95\ 7 deals. is temperature-0 run-to-run non-determinism in the answer-time VLM and LLM judge, not sampling error.} Every interval excludes 0 at every $k$; PULSAR beats the baseline on all seven deals in all twelve metric-by-$k$ cells (exact Wilcoxon signed-rank $p{=}.016$, the floor at $n{=}7$), and the advantage survives leave-one-deal-out. The advantage is retrieval-led. ContextFactRecall is $1.9$--$2.3 $ the baseline from $k{=}3$ up, and larger still at $k{=}1$, where the baseline retrieves almost nothing . This is consistent with vision-first retrieval preserving the chart values and legend assignments condensed during verbalisation. AnswerFactRecall inherits this gap: it is widest at low $k$, where PULSAR reaches roughly $5 $ the baseline , and narrows as the baseline catches up. At the production $k{=}5$ , PULSAR already exceeds the baseline's $k{=}10$ on every metric. PULSAR runs vision-first RAG in production at firm scale. Two choices keep it affordable: decoupling embedding DPI from answer-time DPI , and a pooled two-stage late-interaction index that cuts median vector-search latency $15.1 $ and sustains ${ }88 $ an unpooled index's throughput under load, at under 0.01 NDCG@10 and Recall@10 loss. Ingestion is ${ }20 $ cheaper per page than the OCR+verbalisation baseline, and answer-fact recall more than doubles. *{Limitations} Even with hierarchical pooling and the two-stage index , each page is still stored and scored as many pooled vectors rather than one, and MaxSim remains costlier than a single dot product. These optimisations cut late interaction's footprint and scoring cost but do not erase this overhead: it stays heavier than single-vector retrieval. All production measurements come from one firm's chart- and table-heavy private-markets deal documents, run on ColQwen3-4B. The DPI sweet spot, ingestion-cost gap, and factuality margins may shift on corpora with different chart density or page complexity, on other vision-first backbones, and against text-extraction stacks that preserve more chart detail than the OCR+verbalisation baseline here. Our retrieval-quality evidence for the index design, the embedding-DPI and pooling/quantisation equivalence tests , is measured on the public ViDoRe~V3 benchmark rather than the firm corpus; we have not confirmed these equivalences hold on the firm's own documents. The ${ }20 $ gap covers ingestion calls only; standing serving and answer-time inference are excluded. The 516-question DPI result checks each DPI's answers against factual ground truth the finance SMEs authored ahead of time. Labels are value-level matches (e.g.\ does the answer's stated internal rate of return equal the SME's), not subjective quality judgements. The question-generating LLM is in the same model family as the answer-time VLM, so both may share the same visual blind spots. The protocol isolates the answer-generation step: each question is generated from a specific page, which is fed directly to the VLM, so retrieval is not exercised. It does not speak to end-to-end behaviour, where retrieval can also fail. The 516 questions cluster on two decks, so the effective sample size for between-deck generalisation is two; the result speaks to behaviour on IC-pack-style pages, not arbitrary documents. The OCR+verbalisation baseline vs vision-first comparison in runs 75 SME-authored questions through both systems and scores three LLM-judged metrics against SME-authored gold answers, repeated over 5 independent passes. A blind 20\ judge and independently favours PULSAR , so the comparison is human-validated, not judge-only. We do not compare against alternative vision-first stacks (e.g.\ OCR-free document transformers ) or generate-and-encode hybrids , so we do not claim late-interaction page-image retrieval is the best approach for this task. *{Ethical Considerations} PULSAR informs investment decisions, so even a low residual error rate carries downstream consequences. It is surfaced to analysts as an advisory aid, and outputs that propagate into client-facing artefacts go through human review. We do not recommend deploying vision-first RAG as an unsupervised decision-maker on investment material. Retrieval quality is benchmarked on the public ViDoRe~V3 suite. The production deployment, the end-to-end answer evaluation, and the load and ingestion measurements run on Mubadala's confidential deal documents, which are not released and are not used to train or fine-tune any model: PULSAR runs a frozen pretrained backbone, and all indexing and evaluation of that corpus happen within the firm's own environment under its data-governance controls. The evaluation questions and gold answers were authored by SMEs already authorised to access the underlying documents. *{Acknowledgements} We thank colleagues at Microsoft, Inception42, and Mubadala for their internal review of the paper and their help in evaluating outputs, and the anonymous reviewers for their feedback. {4} {3} {6} {0.95} {0.95} {0.05} {0.6} Table~ lists the index and serving configuration and Table~ the Kubernetes cluster sizing, each across the benchmark, dev, and prod settings. The benchmark columns give the settings behind the and ablations; the dev and prod columns describe the deployed system. {4pt} {!}{ {@{}llll@{}} & & & \\ Backbone & {l}{ColQwen3-4B }\\ Model dtype & {l}{bfloat16}\\ Vector dim $D$ & {l}{320}\\ / & {l}{16 / 2}\\ Distance metric & {l}{cosine }\\ & 65{,}536 & {l}{4{,}096}\\ Image cap & 67{,}108{,}864 & {l}{4{,}194{,}304}\\ Embedding/index DPI & swept & {l}{150}\\ Stored/served DPI & n/a & {l}{500}\\ Vector DB engine & Qdrant 1.16 & {l}{Qdrant 1.17}\\ HNSW graph degree $m$ & {l}{16}\\ HNSW & {l}{100}\\ Index configuration & swept & {l}{$_{qp}$}\\ Prefetch pooling & swept & {l}{mean row + column}\\ Rerank pooling & swept & {l}{hierarchical, pool factor 3}\\ Quantisation & swept & {l}{binary, prefetch-only}\\ & swept & {l}{64}\\ & swept & {l}{200}\\ & {l}{false}\\ & false & {l}{true}\\ Serving engine & transformers & {l}{vLLM 0.19.1}\\ & n/a & {l}{3{,}072}\\ GPU KV cache size & n/a & {l}{4{,}560 tokens}\\ & n/a & {l}{0.3}\\ KV cache dtype & n/a & {l}{bf16}\\ & n/a & {l}{0.89}\\ & n/a & {l}{true}\\ & n/a & {l}{8{,}192}\\ & n/a & {l}{16}\\ Returned pages & @10 & {l}{top-$k{=}5$}\\ Query issue & single-stream & {l}{concurrent}\\ } marks parameters varied in the and ablations; cluster sizing is in Table~. Binary quantisation rescores at $2{ }$ oversampling.} ) and sequence-length caps are set so a standard A4 page embeds without clipping.} {4pt} {!}{ {@{}llll@{}} & & & \\ Transformers inference & $1{ }$ & -- & --\\ vLLM inference & -- & $2{ }$ & $4{ }$ \\ GPU node & {l}{ }\\ & -- & $2{ }$ & $4{ }$ \\ Doc indexer & -- & $2{ }$ & $4{ }$ \\ Vector DB & $1{ }$ & $3{ }$ & $4{ }$ \\ Vector DB PVC & 512\,GiB & 512\,GiB P20 & 1\,TiB P30 \\ } , so the GPU count equals the inference replica count. The benchmark ran as Azure ML pipeline jobs on a single-replica stack : GPU work on , CPU work on . It uses no separate doc-indexer component .} Scheduling splits across two layers. The dispatcher pulls from the highest non-empty lane and a higher-priority arrival evicts any in-flight lower-priority job. Each page is stamped with its page hash at upsert (not the document hash, which is stamped only once the whole document is done, ), so on redelivery the evicted job resumes from the last durable page; wasted work is bounded by the in-flight batch, not document length. Within a lane, each embedding request is stamped with a numeric priority derived from remaining pages, so the GPU drains shortest remaining processing time first and short documents do not queue behind long ones. A time-in-flight term ages low-priority work up so it cannot starve. Search queries enter at top priority, so foreground search is never blocked by background embedding. Table~ gives the full and sweeps behind the search-knob choices in . {4pt} {@{}lrrrrr@{}} & $ $ & $p_{50}$ & $p_{90}$ & $p_{95}$ & $p_{99}$\\ {@{}l}{ }}\\ 25 & $-$.032$^{***}$ & 36 & 57 & 66 & 85\\ 50 & $-$.016$^{***}$ & 38 & 59 & 68 & 88\\ 100 & $-$.006$^{***}$ & 43 & 66 & 76 & 105\\ 150 & $-$.002$^{***}$ & 46 & 71 & 81 & 105\\ $^{ }$ & 0$} & 50 & 77 & 88 & 110\\ 250 & ns$} & 54 & 81 & 93 & 121\\ 300 & ns$} & 57 & 86 & 97 & 123\\ 400 & ns$} & 62 & 92 & 104 & 131\\ 800 & ns$} & 87 & 133 & 153 & 193\\ {@{}l}{ }}\\ $^{ }$ & ns$} & 50 & 79 & 90 & 113\\ 128 & 0$} & 53 & 82 & 94 & 119\\ 256 & ns$} & 53 & 82 & 94 & 120\\ 512 & ns$} & 54 & 81 & 91 & 113\\ ), on the selected $_{qp}$ index at 150~DPI, $n{=}14{,}514$ queries. $ $NDCG@10 is the mean paired difference vs.\ each sweep's baseline (Wilcoxon + Holm within sweep; = indistinguishable on NDCG@10). $p_{50}$--$p_{99}$ are per-query Qdrant server-side latencies . ~$p_{}{<}.001$. $ $ marks the deployed setting.} Table~ gives the storage footprint summarised in , on the 106{,}074-page dev slice. {6pt} {@{}lrrr@{}} & & & \\ & & & $ $ smaller\\ RAM-resident vectors & 12\,GB & 169\,GB & 14\\ On disk & 69\,GB & 166\,GB & 2.4\\ $_{qp}$ configuration against the unoptimised baseline on the 106{,}074-page dev slice. Pooled keeps only the quantised mean-pooled prefetch vectors resident and pushes the hierarchical rerank vectors to disk; unpooled keeps every full per-page multivector resident. GB figures are approximate.} Fig.~ reports the throughput behind the latency comparison in : throughput over a 4--64-way sweep for both indexes at corpus scale. {figures/pooled_vs_unpooled_qps.pdf} 074-page dev slice. Pooled climbs to ${ }47$ QPS and holds 100\ tops out at 0.54 QPS and its completion collapses under load, from 100\ full-multivector scan is so costly the system saturates at one-way concurrency. Per-query latency in Fig.~.} We also isolate per-query latency from queueing with a single-concurrency run : warm, no concurrent load, fixed sample counts, bootstrap 95\ against 1.9\,s for unpooled, and the gap widens through the tail. {5pt} {@{}lrrrr@{}} & & & & \\ Pooled & 175 & 387 & 546 & 686\\ Unpooled & 1{,}907 & 4{,}628 & 6{,}966 & 13{,}639\\ 074-page dev slice, warmup discarded (pooled $n{=}300$, unpooled $n{=}100$). Bootstrap 95\ unpooled p50 1{,}860--1{,}966, p95 4{,}351--8{,}748.} Fig.~ plots the per-document ingestion latency summarised in , on the dev slice. {figures/ingestion_latency_paper.png} Table~ gives the per-deck page-legibility error counts behind the answer-time DPI ablation in . {4pt} {@{}lrrccc@{}} & & & & & \\ Deck A & 94 & 282 & 18 & 8 & \\ Deck B & 78 & 234 & 21 & 7 & \\ ).} We categorise the $n{=}75$ head-to-head evaluation questions on two axes, assigning each question one label per axis. Examples are real questions with company, deal, and competitor names removed. ~Operating and financial metrics (22; 29.3\ ``What is the projected trajectory for EBITDA and margins over the forecast period for the company?'' ~Market and competitive context (20; 26.7\ ``What does the slide indicate about the projected growth and CAGR of the overall sector market over the forecast period?'' ~Valuation and returns (14; 18.7\ ``What overall company valuation is implied by the proposed entry share price?'' ~Capital structure and funding (12; 16.0\ ``What is the aggregate amount and denomination of the rated senior and junior debt facilities?'' ~Segment and revenue mix (7; 9.3\ ``How is the mix of contracted and non-contracted revenue projected to change over the forecast period?'' ~Value extraction (54; 72.0\ ``What is the reported daily active user count for the platform?'' ~Cross-entity or benchmark comparison (13; 17.3\ ``What are a peer's total payment volume and EBITDA margin, and how does its profitability measure up against the company's?'' ~Temporal classification, reported vs projected (8; 10.7\ ``Which year does the company report actual net revenue and EBITDA margin figures, rather than a forward projection?'' To check that the LLM-judged head-to-head reflects human judgement, three domain-expert SMEs blindly re-scored a deal-stratified 20\ the pass-1 answers. The two systems were shown as ``Answer~A''/``Answer~B'' in randomised order with modality cues removed, so raters scored content only, on the same two metrics (Completeness 1--5, AnswerFactRecall 0/1 per fact). Per rater, Completeness tracks the judge at Pearson $r{=}0.83$--$0.86$ and quadratic-weighted $0.79$--$0.81$, and AnswerFactRecall at $r{=}0.94$--$0.98$ . Agreement between raters is ``almost perfect'' : pairwise weighted $0.84$--$0.96$ on Completeness, and Fleiss $0.89$ on AnswerFactRecall over 128 fact-answer judgements . All three SMEs independently rank PULSAR above the baseline, each by a margin at least as large as the judge's (Completeness $+1.07$ to $+1.60$ vs the judge's $+1.07$; AnswerFactRecall $+0.32$ to $+0.41$ vs $+0.32$), so the judge is conservative about the gap rather than inflating it; where human and judge diverge, the human penalises PULSAR's own hallucinations harder. {6pt} {@{}lccc@{}} & {c}{} & \\ & $r$ & wt.\ $ $ & $r$\\ SME1 & 0.86 & 0.81 & 0.98\\ SME2 & 0.84 & 0.80 & 0.98\\ SME3 & 0.83 & 0.79 & 0.94\\ median rater & 0.85 & 0.81 & --\\ correlates the per-answer median of the three SMEs, taken as one consensus rating, against the judge.} 