 {acmart} }} {ulem} {algorithm2e} *{problemstatement}{ } {Theorem} {Lemma} {Corollary} {Definition} {Example} {Remark} {Discussion} { } { } { } { } { } { { #1}} } {RGB}{30,90,255} {xcolor} } } {RGB}{30,90,255} } } {RGB}{58,191,153} } } {{ } } {} {} {} {h} {\{ , , \}} {} {}} {}} {}} {} {T_} {} {9pt} {9pt} { { {0.5pt} {0.5pt} #2} } } Dear reviewers and meta-reviewer, We sincerely appreciate your constructive feedback and believe that the revised manuscript has thoroughly addressed all concerns raised in the original submission. As requested, the main changes in the paper are highlighted in different colors for different reviewers, for Reviewer \#1, for Reviewer \#2, for Reviewer \#3, and for issues across reviewers. Following the revision plan, we have carefully addressed all comments raised by the three reviewers. Please find our point-by-point responses below. We kindly refer the reviewers to the revised submission for further details. We fully agree with the reviewer that more details are necessary about the training set construction. In the revised version, we have added clarifications at 3 places, summarized as follows: In , we have explicitly defined the training pairs as $ $, where $P$ is a human-annotated ground-truth to answer the query $Q$. We have further clarified that constructing these training pairs does not require additional labeling effort or exhaustive kNN search. In this work, we simply reuse training sets that are widely used to train LLMs for generating vector representations of data. To be more specific, we have used MSMARCO as an example and have explicitly quoted its dataset description: where a positive passage means that In this work, we randomly sample from these existing training pairs to construct the shortcuts. This has clarified how the training set is created and reused in our setting. In , we have added a more detailed description of four multi-vector retrieval benchmarks used in our experiments. Specifically, all four datasets provide training and test splits, where each query is paired with one or more human-annotated relevant document IDs. The training split is used for graph enhancement. We have also added description links for all datasets in , which includes more detailed info for each dataset. In , we have added a clarification that, by default, 20\ Thank you for this constructive suggestion. In the revised manuscript, we have added a set of experiments in and to study how the amount of training data used for shortcut construction affects query performance. Specifically, we randomly sampled 5\ Such experimental results indicate that sparse shortcuts fail to sufficiently enrich long-range connectivity in the graph, limiting search performance for some hard-to-find cases. In contrast, excessive shortcuts densify the graph, forcing the search to perform more distance computations per step during traversal, thereby offsetting the gains from improved connectivity. Therefore, we set the sampling ratio to 20\ We agree with the reviewer that the quantization method needs further elaboration. In the revised manuscript, we have addressed this concern from 3 perspectives, summarized below: In , we have provided a detailed description of how the codebook is selected and used to approximately compute EMD. The key idea is to replace expensive token-level distance computations in EMD with efficient centroid-level approximations by leveraging the shared centroid vocabulary $ _$ derived from k-means clustering as described in . Specifically, each token vector is first mapped to its nearest centroid in $_$, and the distance between raw vectors is then approximated by the distance between their corresponding assigned centroids, formalized as: d_X d_X , ), where \ \) and \ \) $ _$. This quantization brings two key computational benefits. First, all centroid-to-centroid distances can be precomputed and stored in a \ codebook, enabling constant-time distance lookups instead of costly high-dimensional distance calculations. Second, the reduced centroid space generates significantly sparser transport plans, which further accelerate EMD computation. In , we have added explanations of how the number of codebooks $|_{}|$ is determined. To recap, we follow an empirical formula from prior studies , the nearest power of two to $16 }$. We have also included a concrete example to illustrate, step by step, how $|_{}|$ is computed for MSMARCO: $|_{}|= 2^{ _{2}\! } 262k$. Similarly, $|_| = 262K$ for LoTTE, and $32K$ for the OK-VQA and EVQA. In _{quant}|$ } and _{quant}|$ }, we have added new experiments to systematically study the impact of different quantization configurations on retrieval performance. Specifically, we vary $|C_{}| \{131, 262, 524\}$, where $262k$ is the default value for MSMARCO derived from the above empirical formula. As shown in Figure 16, the retrieval performance of GEM is robust to the choices of $|C_{}|$. Although increasing $|C_{}|$ leads to higher distance computation overhead, it yields more accurate approximations and thus requires fewer candidates for reranking. In contrast, reducing $|C_{}|$ lowers the computational cost but necessitates probing more candidates to achieve a comparable accuracy level. Therefore, we simply adopt the empirical formula suggested by prior work to set the default value of $|C_{}|$. Thank you for your valuable suggestion. We have provided more detailed instructions on how parameters should be set in . Specifically, the number of codebooks $|_{}|$ follows an empirical formula suggested by prior studies , the nearest power of two to $16 }$. This yields $|_{}| 262k$ for MASMARCO and LoTTE, and $32K$ for OK-VQA and EVQA. The number of clusters $|_{}|$ is scaled with dataset size to balance granularity and indexing cost: we set $|_{}| = 40K$ for MSMARCO, $10K$ for LoTTE, and $1K$ for OK-VQA and EVQA, so that each cluster contains about $10^{3}\!-\!10^{4}$ objects. For the graph construction parameters, we adopt widely-used settings in graph-based indexes, $M = 24$ and $ = 80$. By default, 20\ small parameter range is typically sufficient. The reviewer’s comment highlights an important aspect of our work. As discussed in the revision plan, we have added 2 clarifications in the revised submission, summarized below: In , we have added a rationale explaining: unlike single-vector approaches, which alleviate information loss by increasing dimensionality but exacerbate the curse of dimensionality, multi-vector models capture complex semantics from a different angle: they represent an object with multiple simpler vectors. We think this is an inherent advantage of the multi-vector paradigm. In , after carefully reviewing the dimensionality settings and dataset selections adopted by state-of-the-art methods -- spanning both academic and industrial methods -- we have confirmed 128d as a widely used per-vector dimensionality. For a fair comparison, we have adopted the same datasets as these methods, and additionally have included two multi-modal datasets with more complex distributions to validate the effectiveness of our method. We thank the reviewer for bringing this point to our attention. As outlined in the revision plan, rather than listing numerous unsuccessful attempts, we have added more discussion on the motivation and design rationale behind each optimization before detailing its specific implementation. The main clarifications are summarized as follows: In , we have further explained the motivation for adopting a TF-IDF–style strategy to assign sets to clusters. We observe that a naive way--associating a set with every cluster if one or more of its tokens are assigned to that centroid--can cause a vector set to appear in an excessive number of clusters. For example, the uninformative tokens "is", "the", and "of" contribute little semantic value but significantly inflate cluster memberships. Motivated by such unequal semantic contribution of different tokens, we therefore consider: retain only clusters associated with tokens, while ignoring ones. This way, a set is expected to be assigned to only a few clusters, which help reduce index redundancy while preserving the semantic richness of multi-vector representations. To realize this idea, we design a TF-IDF–guided cluster pruning mechanism. In , we have reiterated the rationale for why the Chamfer distance cannot be directly used for graph construction. This point has already been elaborated in Section 3.2. To recap, the Chamfer distance lacks the necessary metric properties required to serve as a reliable foundation for constructing a stable, navigable APG. This limitation motivates us to decouple the metrics, using EMD as the edge weight metric, while performing search based on the original Chamfer distance. In , we have pointed out that while cluster-level graphs offer strong local navigability, cluster-by-cluster traversal is inefficient, and duplicating vector sets across clusters introduces substantial redundancy. This motivates us to weave cluster-level graphs into a single, globally connected graph using carefully selected cross-cluster bridge sets. In , we have further explained the motivation for using multiple entry points. Since sets that are relevant under the Chamfer distance may be distributed across multiple clusters, relying on a single entry point can lead to biased initialization and unnecessarily long traversal paths. We thus identify one entry point per relevant cluster and employ a cluster-guided multi-path search strategy to traverse the graph efficiently. Thank you for your questions. We have addressed all of them in the revised manuscript, summarized as follows: For ground truth construction, please refer to our response to {R3.O1}, as well as the revised , and for more details. We have reorganized for improved clarity. Specifically, the Success@$k$ metric indicates whether any relevant item is present in the top-$k$ results, defined as follows: k = {|Q|} _{Q } , where $_Q$ is the ground-truth set for each query $Q$ in the query set $$, and $_Q^{ }$ is the top-$k$ retrieved results of $Q$. Due to space constraints, we are unable to include more illustrative examples of how success is computed. We kindly refer the reviewer to the concrete example provided in the revision plan for further clarification. We have added recall results. Please refer to our response to {R3.O5}, as well as the revised and for more details. In , we have explicitly stated our experimental setup. Specifically, we disable key modules individually and compare the query latency required to reach comparable retrieval accuracy, MRR@10 = 0.42 on MSMARCO, and MRR@10 = 0.30 on OKVQA. We fully agree with the reviewer that it would be useful to further discuss why MVG can sometimes outperform other specialized approaches. During the interactive discussion, we have already clarified that the MVG evaluated in our experiments is not exactly the same as the baseline solution described in Section 3.2. The solution in Section 3.2 performs much worse than other special-purpose techniques in both index construction efficiency and query performance. Thus, as stated in Section 5.1.2: For MVG, In the revised submission, we have further discussed why MVG can sometimes outperform other special-purpose techniques, summarized as follows: In , we observe that MVG can compete with several well-tuned baselines for small $k$. We have provided an analysis as follows: Though non-metric distance in MVG induces fragmented neighbors and local optima, this issue is not overly severe when the target $k$ is small. By slightly increasing , greedy graph traversal can escape local minima and reach the ground truth eventually. As a result, MVG retains the key advantage of graph-based indexes, it accesses fewer candidates than LSH-based and IVF-style methods to achieve the same recall. When further combined with our qCH quantization, which reduces the cost of per-candidate distance computation, MVG exhibits a favorable accuracy–latency trade-off. In , we observe that MVG does not consistently outperform other methods. For instance, on MSMARCO, other methods reach around 97\ In , we observe that MVG can outperform some competitors on certain datasets, such as complex multi-modal OKVQA and EVQA datasets. This advantage can be attributed to its tailored set-level indexing structure, which preserves fine-grained semantics. In contrast, methods like MUVERA map a multi-vector set to a single vector, inevitably causing non-trivial information loss. This loss worsens with data complexity, leading to degraded query performance. We fully agree with the reviewer that the dataset descriptions need to be more clearly elaborated. We have clarified how the ground truth is constructed in the revised manuscript and how it is used in the code, summarized as follows: In , we have added a more detailed description of the ground truth. The ground truth is not computed via brute-force kNN search. Instead, all four datasets used in our experiments provide training and test splits, in which each query is paired with one or more human-annotated relevant document IDs. The training instances are used to enhance the graph, while queries from the test split are used to evaluate retrieval performance, with the corresponding relevance annotations treated as ground truth. We have also added download links for all datasets in , which includes more detailed info for each dataset. In , we have added a remark to further explain how the ground truth is used when computing the evaluation metrics. To recap, the ground-truth set $_Q$ in Equations , , refers to results with , as stated in MSMARCO. Other datasets follow the same principle. Unlike ANN benchmarks, where the ground truth is typically defined as the exact $k$NN obtained by brute-force search in the embedding space, our results below directly reflect end-to-end semantic similarity retrieval performance. In our code, the brute-force Chamfer kNN you mentioned is only for debugging, not for the final results reported in the paper. We have removed this part, along with other debugging functions, from the public code repository to avoid confusion. In our code, we strictly follow the dataset-provided relevance labels as the ground truth for all evaluations. In the main example code : when loading a dataset, via function , we store the ground-truth document IDs for each query in . During evaluation, is computed by comparing the index’s retrieved results with these annotated labels, . Then, we report the average across all queries. Other evaluation metrics follow the same procedure. _$ the same cap as $r_{max}$?} This is a good point. In , we have added an explanation for why the choice for \# of clusters is 3 when disabling TF-IDF, which is neither arbitrary nor the same as $r_{max} = 10$ . Specifically, we set $|_| = 3$ because, with TF-IDF enabled, the average $|_|$ is 2.9 and 3 on MSMARCO and OKVQA, respectively . Since the focus of this ablation study is on $_$ values, using nearly identical values can isolate the effect of cluster granularity and allow a direct, fair comparison. In the revised manuscript, we have clarified how training pairs are generated and confirmed that this process does not require an exhaustive search. Please refer to our response to {R1.C5}, as well as the revised , and for more details. We apologize for the confusion and have clarified in the revised manuscript why $R@100 == S@100$ is correct on EVQA. The confusion comes from how the ground truth is defined: it is not generated via brute-force kNN search in the embedding space, but via human annotation: each query is paired with document that are human-labeled as containing relevant info to answer it . Specifically, as explained in , each query in EQVA has annotated relevant document, thus $_Q _Q^{ }$ can only be size $0$ or $1$. If the ground truth is in the retrieved top-$k$: $ = 1/1=1, = 1$; otherwise: $ = = 0$. Hence, its averaged {recall} and {success} reported in Table 2 and Figure 8 are always identical. Due to space constraints, we are unable to include more illustrative examples. We kindly refer the reviewer to the concrete example provided in the revision plan for further clarification. not used in Fig 8?} We fully agree that recall results should also be included. These were omitted in the original version due to space constraints and their similar trends to the success–latency curves, as stated in . In the revised manuscript, we have added the recall–latency curves in . As expected, the recall–latency curves show almost the same trend as the success–latency curves. Across all metrics—MRR, success, and recall—GEM consistently achieves the best accuracy–efficiency trade-off, indicating the effectiveness of GEM’s design. We apologize for the confusion and have revised to address the alignment issue. The figure is now aligned. As further clarified in , the symbols at the bottom of Figure 1 are just intended to illustrate that diverse data types can be embedded into vectors by LLMs, making vector representations a unified semantic abstraction. values for Table 3? Why switch / between datasets?} We appreciate the opportunity to clarify this point. In , we have explicitly reported the values used in Table~3. Specifically, on MSMARCO, GEM uses = 800, 2000, 8000 for $k$ = 10, 100, 1k, respectively. On LoTTE, GEM uses = 1000, 5000, 8000 for $k$ = 10, 100, 1k, respectively. Our competitors, PLAID and DESSET , report recall on MSMARCO and success on LoTTE. We therefore follow this convention in Table 3 for direct comparison, as explicitly stated in . To provide a more comprehensive evaluation, we have conducted additional experiments reporting all metrics for all methods across all datasets in and Table 2: End-to-end Retrieval Performance Overview . The results show that GEM consistently achieves the best query performance, requiring the least query time to reach the same or better accuracy. Once again, we thank you for your valuable time reading our revision letter and revised manuscript. Please let us know if our revisions have addressed all the concerns you have. {acmart} }} {ulem} {algorithm2e} *{problemstatement}{ } {Theorem} {Lemma} {Corollary} {Definition} {Example} {Remark} {Discussion} { } { } { } { } { } { { #1}} } {RGB}{30,90,255} {xcolor} } } {{ } } {} {} {} {h} {\{ , , \}} {} {}} {}} {}} {} {T_} {} {9pt} {9pt} { { {0.5pt} {0.5pt} #2} } } Dear reviewers, We sincerely appreciate your time and valuable feedback. The detailed and insightful suggestions provide a clear roadmap for further strengthening this work. Below are point-by-point responses to all comments from the three reviewers and our specific revision plans. Thank you for this valuable comment. The training set used for adding semantic shortcuts does not require additional labeling effort or exhaustive kNN search. In this work, we adopt public benchmarks for end-to-end information retrieval tasks, each offering a training set with human-annotated query–document relevance pairs. For example, in MSMARCO dataset{MS MARCO WebLink}} description, it says These training sets are widely used to train LLMs for generating vector representations of data. Our semantic shortcuts here simply re-use this existing training data to augment the graph, so no extra effort is needed. For coverage, we randomly sample 10\ This is an excellent suggestion! In our ablation study, we have examined the effectiveness of semantic shortcuts: removing them leads to a modest degradation in query latency, demonstrating their positive impact on search efficiency . We fully agree that evaluating their behavior under different quality/coverage levels of the training set would provide a more comprehensive understanding. In the revision, we will include additional experiments that vary the portion of training data used to construct semantic shortcuts, sampling 5\ Thank you for this constructive suggestion. We will provide further elaboration on the codebook construction. For the number of codebooks $_{}$, we follow an empirical formula from prior studies, ColBERT : set it to the nearest power of two to $16 }$. For example, on the MSMARCO dataset, this yields $|_{}| = 2^{ _{2}\! } 262k$. We will add a concrete example in the revision to clarify how this value is computed. As recommended, we will also include new experiments that vary the quantization granularity, using $2 |C_{}|$ and ${2} |C_{}|$ centroids, to systematically study the impact of different quantization strategies on retrieval performance. Good question! All parameters in GEM follow either prior work or simple heuristics with user-friendly recommendations. Specifically, the fine-grained quantization clusters $|_{}|$ follow an empirical formula suggested by prior work . The coarse cluster count $|_{}|$ is chosen so that each cluster contains about $10^{3}\!-\!10^{4}$ objects, ensuring effective graph construction. The graph parameters $M$ and follow widely-used settings in graph-based indexes. The search-related parameters, $t$, } , $\#rerank$ depend on the user’s desired recall-latency trade-off. As a practical guideline, users may start with the default settings reported in our paper. For further optimization, a lightweight grid search over a small parameter range is typically sufficient. We will clearly summarize the selection logic for each parameter in the revision. We really appreciate you pointing this out. This question helps us clarify an important aspect of our work. In our experiments, we noticed that in the multi-vector setting, the dimensionality of each individual vector does not need to be very high. Different from single-vector approaches that preserve more information by increasing dimensionality and make the curse of dimensionality worse, multi-vector models capture complex semantics from a different angle: they represent an object with multiple simpler vectors. We think this is an inherent advantage of the multi-vector paradigm. In the revision, we will add this rationale and provide commonly used per-vector dimensionalities in both academic and practical production cases. We agree that sharing optimizations that we tried but did not work could help readers better follow the work. However, during GEM’s development, we experimented with numerous design variants and unsuccessful cases far outnumbered successful ones. We fear a long list of these failures would distract from the main ideas rather than clarify them. As an alternative, we propose adding more details on the motivation and design rationale behind each optimization before elaborating on its specific implementation. We believe this will make the optimization pipeline more intuitive to follow, without overwhelming readers with excessive negative results. Thank you for the question. For ground truth construction, please see our response to {R3.O1}. Success@k checks whether any ground-truth document appears in the top-$k$ results. Its formal definition is given in Eq. 19 and Section 5.1.4. Please also see our response to {R3.O4}, where we provide an example of how is computed. We use instead of in Figure 8 to follow prior work. Please see response to {R3.O5} for details. We will also add results in the revision. In the ablation study, all variants were tuned to achieve the same accuracy level for top-10 retrieval, R@10 = 0.69 on MSMARCO, and then we compared the latency required by each variant to reach this target. We should have explicitly state this setup. Apologies for the confusion. Just to confirm, you are referring to MVG , not MVQ, right? We agree that discussing why MVG performs well on some datasets would be helpful. We will add further analysis to explain the possible factors driving its superiority over other special-purpose techniques. So glad you love this idea! Yes, it is an original contribution of this paper. Thank you for your question and careful check! The ground truth is provided by the datasets, not computed via brute-force kNN search. For example, MSMARCO is a widely used information retrieval benchmark, where each query is paired with one or more human-annotated relevant document IDs. As stated in the official dataset description}, Other datasets{LoTTE WebLink}, {OK-VQA Weblink}, {EVQA Weblink}} follow the same principle. In our code, we strictly follow these dataset-provided relevance labels as the ground truth for all evaluations. In the main example code : when loading a dataset, via function , we store the ground-truth document IDs for each query in . During evaluation, is computed by comparing the index’s retrieved results with these annotated labels, . We then report the average across all queries. Other evaluation metrics follow the same procedure. The brute-force Chamfer kNN you mentioned is only for debugging, not for the final results reported in the paper. We acknowledge that this may have caused confusion. In the revision, we will more clearly explain the construction of ground truth, and delete debugging code to avoid potential misunderstandings. _$ the same cap as $r_{max}$?} Just to confirm, your question refers to the setting in Section 5.3.3 "w/o dynamic TF-IDF cluster pruning", right? The choice for \# of clusters when disabling TF-IDF is not arbitrary, and it is not the same as $r_{max} = 10$ . We set $|_| = 3$ because we observed that, with TF-IDF enabled, the average $|_|$ on MSMARCO is 2.9 . Since the focus of this ablation study is on $_$ values, using nearly identical values helps isolate the effect of cluster granularity and allows a direct, fair comparison. We will clarify this in the revision. Thank you for the question. The training pairs do not require an exhaustive search to generate. Please see our response to {R1.C5} for more details. {O1} for details). For example, document IDs $\{1,2,3\}$ are labeled as relevant to a query $Q$, then the ground-truth set is $_Q = \{1,2,3\}$. Suppose the top-10 retrieved results are $_Q^{ } = \{1,2,4,5,6,7,8,9,10,11\}$. Following prior work, we compute and as follows: { k = {||} _{Q } _Q _Q^{ }|}{|_Q|}, ~ k = {||} _{Q } . } So, $|_Q _Q^{ }| = 2$, $ = 2 / 3 = 0.66$, $ = 1$. For the EVQA dataset, each query has annotated relevant document, $|_Q|=1$ for all queries, and $_Q _Q^{ }$ can only be size $0$ or $1$. Thus, if the ground truth is in the retrieved top-$k$: $ = {1}=1, = 1$; otherwise: $ = 0, = 0$. Hence, and on EVQA are identical for every query, and thus $ == $ after averaging. not used in Fig 8?} The reason we use instead of in Figure 8 is simply to follow prior studies . For example, on multi-modal datasets , PreFLMR adopted and as key metrics . On widely-used multi-vector search datasets , both and are used. For example, PLAID and DESSET use when evaluating on MSMARCO, and for LOTTE. Due to space constraints, we selected shared metrics, and , in Figure 8, while is still reported in Table 2 and Table 3. In our experiments, we evaluated both and , and they show similar trends. In the revision, we can also include recall–latency curves for all datasets. We apologize for the confusion. The symbols in the bottom right of Fig 1 were intended to illustrate that diverse data types can be embedded into vectors. For example, in document retrieval tasks, document text is processed by the data EM, while query text is sent to the query EM. We will revise Fig 1 to resolve any ambiguity. values for Table 3? Why switch / between datasets?} Graph-based indexes typically balance accuracy–latency by tuning . For Table 3, we aligned recall/success across methods to compare latency under different top-$k$. Specifically, on MSMARCO, GEM uses = 800, 2000, 8000 for $k$ = 10, 100, 1k, respectively. On LoTTE, GEM uses = 1000, 5000, 8000 for $k$ = 10, 100, 1k, respectively. For why we use on MSMARCO and on LOTTE, please see our response to your {O5}. We will add more results in the revision. Once again, we thank you for your valuable time reading the revision plan. Please let us know if our replies have addressed all the concerns you have. {acmart} } } } } } }} {algorithm2e} *{problemstatement}{ } {Theorem} {Lemma} {Corollary} {Definition} {Example} {Remark} {Discussion} { } { } { } { } { } } } } {{ } } {} {} {} {h} {\{ , , \}} {} {}} {}} {}} {} {T_} {} {9pt} {RGB}{30,90,255} {RGB}{58,191,153} {ulem} In multi-vector retrieval, both queries and data are represented as sets of high-dimensional vectors, enabling finer-grained semantic matching and improving retrieval quality over single-vector approaches. However, its practical adoption is held back by the lack of effective indexing algorithms. Existing work, attempting to reuse standard single-vector indexes, often fails to preserve multi-vector semantics or remains slow. In this work, we present GEM, a native indexing framework for multi-vector representations. The core idea is to construct a proximity graph directly over vector sets, preserving their fine-grained semantics while enabling efficient navigation. First, GEM designs a set-level clustering scheme. It associates each vector set with only its most informative clusters, effectively reducing redundancy without hurting semantic coverage. Then, it builds local proximity graphs within clusters and bridges them into a globally navigable structure. To handle the non-metric nature of multi-vector similarity, GEM decouples the graph construction metric from the final relevance score and injects semantic shortcuts to guide efficient navigation toward relevant regions. At query time, GEM launches beam search from multiple entry points and prunes paths early using cluster cues. To further enhance efficiency, a quantized distance estimation technique is used for both indexing and search. Across in-domain, out-of-domain, and multi-modal benchmarks, GEM achieves up to 16× speedup over state-of-the-art methods while matching or improving accuracy. The research work described in this paper was supported by Hong Kong Research Grants Council , HKUST-WeBank Joint Laboratory , HKUST-MetaX Joint Laboratory for Advanced AI Computing . It was partially conducted in JC STEM Lab of Data Science Foundations funded by The Hong Kong Jockey Club Charities Trust. We introduced GEM, a native graph-based framework designed for multi-vector retrieval. GEM constructs a set-level dual-graph with metric decoupling to represent complex inter-set relationships, and further refines it through adaptive TF-IDF–guided clustering and semantic shortcuts. By incorporating quantized distance estimation and cluster-guided multi-path search, GEM jointly enhances efficiency and accuracy, effectively bridging the gap between semantic expressiveness and retrieval efficiency. Extensive experiments showed that GEM consistently surpasses state-of-the-art methods in retrieval quality and latency, while maintaining comparable or smaller index size and construction cost. These results confirmed GEM as a practical solution for large-scale multi-vector retrieval. For future work, integrating ultra-low-bit quantization could further reduce computational and memory costs, paving the way for broader industrial adoption. In this section, we conduct extensive experiments on real-world datasets to evaluate our GEM. We implement GEM in C++ compiled with g++ using -Ofast optimization and openMP for parallelism. All experiments are run on a Ubuntu server with 4 Intel Xeon Gold 6218 CPUs and 1.5 TB RAM. } } {c|c|c|c|c} {Datasets} & {\# Corpus} & {\# Vectors} & {\# Queries} & {Modality} \\ & 8.8M & 597M & 6980 & Text-only\\ & 2.4M & 339M & 2931 & Text-only\\ & 114K & 14M & 5046 & Text + Image \\ & 50K & 9.7M & 3750 & Text + Image \\ {{MSMARCO weblink}, {LoTTE webLink}, {OK-VQA weblink}, {EVQA weblink}}} We evaluate GEM's performance on four widely-used multi-vector retrieval benchmarks, which are detailed in Table . is widely used for in-domain evaluation with a retriever trained specifically for this task, while serves as an out-of-domain benchmark. To further evaluate performance in complex multi-modal settings, we include two vision-language retrieval datasets, and , both involving queries with textual and visual content. For and , we employ ColBERTv2 to encode queries and documents into sets of vectors. , the dimensionality of each individual vector is set to $d=128$ by default.} For multi-modal datasets, we adopt preFLMR , a fine-grained late-interaction retriever that jointly encodes text and image content into multi-vector representations . Both ColBERTv2 and preFLMR adopt the late interaction paradigm with set-to-set similarity computed via Chamfer distance, and have shown strong performance in text-only and multi-modal retrieval, respectively. } We compare GEM with four representative methods in multi-vector retrieval, as mentioned in Section : PLAID , DESSERT , MUVERA and IGP . We also include a graph-based baseline as discussed in Section , denoted as the multi-vector graph . To ensure basic competitiveness, here we use qCH for both indexing and search in our experiments. } Parameter settings of competitors follow the original papers or their source codes. For PLAID, we set the number of centroids to $262K$ for MSMARCO and LoTTE, $32K$ for OKVQA and EVQA. All vectors are quantized to 2 bits per dimension for MS MARCO and LoTTE, and to 8 bits for OKVQA and EVQA. For DESSERT, {we set $C = 7$, $L = 64$ for MSMARCO, LoTTE, and set $C = 4$, $L = 768$ for OKVQA, EVQA. For MUVERA, we adopt $_{reps} = 20$, $_{sim} = 5$, and $_{proj} = 32$ to generate single-vector representations, which are then compressed using PQ-256x8 and indexed with HNSW . For IGP, we use $262K$ centroids for MSMARCO and LoTTE, $32K$ for OKVQA and EVQA, with graph construction parameters set to $M = 24$, $ = 200$. For our GEM, _{}|$ follows the same empirical formula from prior studies , the nearest power of two to $16 }$. This yields $|_{}| = 2^{ _{2}\! } 262k$ for MASMARCO. Similarly, $|_| = 262K$ for LoTTE, and $32K$ for OK-VQA and EVQA.} _{}|$ is scaled with dataset size to balance granularity and indexing cost. Specifically, $|_{}|$ is set to $40K$ for MSMARCO, $10K$ for LoTTE, and $1K$ for OK-VQA and EVQA, so that each cluster contains about $10^{3}\!-\!10^{4}$ objects. The graph construction parameters $M = 24$ and $ = 80$ and} , $\#rerank$ to trade off efficiency and accuracy. For MVG, we adopt the same indexing parameters as GEM. All competitors are also tuned to their best query performance. } }, we adopt four metrics to evaluate query performance: , , , and query latency; and two metrics to evaluate indexing performance: indexing time and index size. Given a query set $$, with ground-truth set $_Q$ for each query $Q $, and top-$k$ retrieved results $_Q^{ }$, Recall@$k$ measures the proportion of relevant items retrieved: k = {||} _{Q } _Q _Q^{ }|}{|_Q|}. MRR@$k$ captures the reciprocal rank of the first relevant item: @k = {|Q|} _{Q } {_Q}, k = {|Q|} _{Q } . } Query latency is reported as the average runtime over $10$ repeated runs for each dataset. _Q$ in the above definitions refers to results with , as stated in MSMARCO. Other datasets follow the same principle. Unlike ANN benchmarks, where the ground truth is typically defined as the exact $k$NN obtained by brute-force search in the embedding space, our results below directly reflect end-to-end semantic similarity retrieval performance.} {1.3} {3.5pt} {l|cccc|cccc|cccc|cccc} {*}{Method} & {c|}{MSMARCO} & {c|}{LOTTE} & {c|}{OKVQA} & {c}{EVQA} \\ {2-5} {6-9} {10-13} {14-17} & R@100 & S@100 & MRR@10 & T & R@100 & S@100 & MRR@10 & T & R@100 & S@100 & MRR@10 & T & R@100 & S@100 & MRR@10 & T \\ PLAID & 0.913 & 0.919 & 0.395 & 352 & 0.745 & 0.910 & 0.558 & 462 & 0.400 & 0.735 & 0.225 & 570 & 0.895 & 0.895 & 0.476 & 211 \\ DESSERT & 0.890 & 0.905 & 0.372 & 344 & 0.748 & 0.893 & 0.541 & 362 & 0.371 & 0.723 & 0.163 & 642 & 0.837 & 0.837 & 0.370 & 552 \\ MUVERA & 0.902 & 0.915 & 0.385 & 310 & 0.736 & 0.901 & 0.547 & 441 & 0.369 & 0.723 & 0.219 & 605 & 0.878 & 0.878 & 0.471 & 446 \\ IGP & 0.895 & 0.901 & 0.389 & 340 & 0.731 & 0.899 & 0.553 & 338 & 0.402 & 0.732 & 0.215 & 549 & 0.892 & 0.892 & 0.440 & 458 \\ MVG & 0.895 & 0.905 & 0.421 & 360 & 0.701 & 0.879 & 0.555 & 231 & 0.381 & 0.719 & 0.230 & 427 & 0.880 & 0.880 & 0.468 & 450 \\ & & & & & & & & & & & & & & & & \\ } In this section, we study the end-to-end retrieval performance of all methods under default settings, as summarized in Table~, Table~, and Figure~. Table provides an overview of recall, success, MRR, and end-to-end latency for all methods across all datasets. Clearly, GEM offers the best overall query performance on all datasets. It adapts well to in-domain, out-of-domain, and more complex multi-modal datasets, outperforming all competitors on every query quality metric while maintaining the lowest latency. In particular, GEM achieves speedups of up to 2.5$ $ on MSMARCO, 2.2$ $ on LoTTE, 3.9$ $ on OKVQA, and 2.8$ $ on EVQA, all with better query quality. _Q _Q^{ }$ can only be size $0$ or $1$. If the ground truth is in the retrieved top-$k$: $ = 1/1=1, = 1$; otherwise: $ = = 0$. Hence, its averaged {recall} and {success} reported in Table ~ and Figure are always identical.} These experimental results validate our design goal: preserving the semantic richness of multi-vector representations to achieve high accuracy, while leveraging an efficient graph index for speed. The reason GEM achieves the best performance can be concluded as follows: 1) Compared with PLAID, GEM indexes at a set-level granularity, treating the entire vector set as the basic unit. This is fundamentally different from PLAID, which indexes all vectors individually. By doing so, GEM significantly reduces the number of false positive candidates returned due to token-level matches, avoiding the need for expensive refinement and boosting efficiency. 2) Compared with DESSERT, which is often bottlenecked by an exhaustive scan of all set sketches, GEM's graph structure provides a powerful pruning mechanism, enabling significantly faster navigation to the most promising candidates. 3) Compared with MUVERA, which suffers significant accuracy degradation by compressing a vector set into a single vector, GEM preserves set-level semantic nuances, thereby easily achieving superior query quality that MUVERA struggles to match even when its FDEs are tuned to an impractically high $20480$ dimensions. 4) Compared with IGP, which extends token-level indexing into a graph structure over quantized centroids for incremental candidate filtering, GEM constructs a set-level graph where each node represents a complete vector set. This design captures inter-set relations that centroid-level graphs overlook, enabling faster and more accurate retrieval. 5) Compared with the naive MVG, while it preserves multi-vector inputs, its naive graph suffers from fragmented neighbors, local optima, and lacks effective distance approximation and pruning strategies. {exp_fig/query_lantency_3_4.pdf} } {1.3} {3.5pt} {l|ccc|ccc} {*}{Method} & {c|}{MSMARCO} & {c}{LoTTE} \\ {2-4} {5-7} & R@10/T & R@100/T & R@1k/T & S@10/T & S@100/T & S@1k/T \\ PLAID & 68.8/222 & 91.3/352 & 97.5/352 & 76.8/288 & 91.0/462 & 94.5/462 \\ DESSERT & 66.2/298 & 89.0/344 & 97.2/336 & 74.7/362 & 89.3/362 & 94.3/477 \\ MUVERA & 67.7/286 & 90.2/310 & 97.1/444 & 74.1/397 & 90.1/441 & 94.4/461 \\ IGP & 66.9/279 & 89.5/340 & 96.9 /399 & 75.5/311 & 89.9/338 & 94.0/412 \\ MVG & 69.2/293 & 89.5/360 & 94.2/436 & 74.7/303 & 87.9/331 & 94.2/442 \\ & / & / & / & / & / & / \\ Table reports how query quality and latency change as the number of returned results $k$ increases. Due to space limitations, we only present results on two datasets. , we report recall on MSMARCO and success on LoTTE.} = 800, 2000, 8000 for $k$ = 10, 100, 1k, respectively. On LoTTE, GEM uses = 1000, 5000, 8000 for $k$ = 10, 100, 1k, respectively. } We make the following observations: GEM consistently achieves the best query performance across all $k$ values, offering higher Recall@$k$ and Success@$k$ while maintaining significantly shorter query times. For example, on MSMARCO, GEM requires only $88$ ms, $140$ ms, and $212$ ms for $k=10,100,$ and $1000$ respectively, whereas the best baselines require $222$ ms , $310$ ms , and $336$ ms to reach comparable recall. On LoTTE, GEM completes in $133$ ms, $210$ ms, and $251$ ms, while the fastest baselines still require $288$ ms , $338$ ms , and $412$ ms to achieve similar quality. , greedy graph traversal can escape local minima and reach to the ground truth eventually. As a result, MVG retains the key advantage of graph-based indexes, it accesses fewer candidates than LSH-based and IVF-style methods to achieve the same recall. When further combined with our qCH quantization, which reduces the cost of per-candidate distance computation, MVG exhibits a favorable accuracy–latency trade-off.} As expected, in the out-of-domain setting , retrieval is more challenging than in-domain setting , and all methods require more time to achieve good quality. GEM remains consistently more accurate and faster, often requiring only $1/2$ or even $1/3$ of the time of other methods. Figure provides a more comprehensive view of the trade-off between query accuracy and efficiency of all methods. From the figures, we have the following observations: GEM consistently achieves the best trade-off. Among all methods, GEM requires the least time to reach the same MRR, success, , indicating the most favorable balance between accuracy and efficiency. For the same MRR@10, GEM is up to 16x faster than DESSERT , up to 10x faster than MUVERA , up to 8x faster than IGP , and up to 5x faster than PLAID . Visual-language retrieval is inherently more challenging. On OKVQA and EVQA, even with increased search time, Success@100 reaches only about 75–89\ GEM and PLAID are highly robust, maintaining stable performance across all datasets. PLAID is often the second-best method. In contrast, IGP, DESSERT, and MUVERA exhibit substantial performance variation across datasets. {exp_fig/index_wo_data_new.pdf} } The indexing time and index size of all methods under default settings are summarized in Figure~. Here, the index size includes only the index files, excluding raw data, to clearly highlight the differences among indexing methods. We make two key observations: GEM’s index size is small , significantly smaller than MUVERA and DESSERT . This compactness is attributed to the graph structure of GEM, which primarily stores edge information. This storage overhead is independent of whether the setting is under single- or multi-vector, and is negligible compared to the dataset size. In contrast, DESSERT requires storing a large number of hash values generated by LSH, while MUVERA needs to store Fixed Dimensional Encodings for all vector sets, which scale directly with the dataset size and make it less suited to large-scale scenarios. GEM’s index size is comparable to that of IGP and PLAID , but GEM delivers a substantial advantage in both efficiency and accuracy. On MSMARCO and LoTTE, all methods have comparable indexing time. This is because PLAID, DESSERT, IGP, and GEM all rely on a clustering step, which dominates the construction cost. MUVERA does not involve clustering but has to convert every vector set into a single vector, which is also time-consuming. Both clustering and vector conversion can be accelerated on GPUs, making this step less of a practical concern. On smaller datasets, OKVQA and EVQA, the clustering cost becomes less significant, and GEM’s graph construction is slower than PLAID’s IVF, IGP's single vector graph, and DESSERT’s LSH indexing. MUVERA, which requires both vector conversion and graph construction, remains the slowest. Overall, GEM achieves competitive indexing time and compact index size while providing superior retrieval performance. {exp_fig/ablation.pdf} In this section, we evaluate how much each component of GEM contributes to the overall performance. As shown in Figure , removing different components results in varying degrees of latency increase. } We replace the qEMD with qCH for graph construction, which doubles the latency on both datasets. The absence of metric properties degrades the graph quality, confirming the importance of the decoupling strategy in GEM. } We disable multi-path search from multiple entry points, instead enqueuing all entry points into a single search queue $$ and expanding greedily. This change increases latency by 70–80\ Without synchronized paths sharing the visited and best-found result sets, pruning is less aggressive and the search space contracts more slowly. To reach the correct results, a larger is needed, which increases latency. These results confirm that multi-entry search accelerates convergence. }} We disable the adaptive adjustment of clusters per document and fix $|_| = 3$ for all documents_|$ on MSMARCO and OKVQA when TF-IDF is enabled . This setup isolates the effect of cluster granularity and allows a direct, fair comparison of $_$ values. As observed, performance degrades} because a fixed threshold hurts recall for long, topically diverse documents, while short documents suffer efficiency loss from retaining unnecessary clusters. This result highlights the importance of dynamic TF-IDF–guided pruning in balancing accuracy and efficiency. } In this variant, when the degree of a bridge $P$ exceeds the limit, we retain only its $M$ closest neighbors, rather than enforcing it to keep at least one neighbor from each cluster in $_ $. This change greatly degrades performance, with the drop on MSMARCO more pronounced than on OKVQA due to the larger corpus size. This result shows the importance of bridges in maintaining graph connectivity, especially on large datasets. } We remove shortcuts that directly connect sets that may be close under Chamfer but require many hops to reach over the EMD graph. This leads to a modest degradation in query latency. While the co-designed architecture of GEM is already robust for most queries, the shortcut mechanism is a useful addition for handling a few hard-to-find cases. It is worth noting that these components are co-designed to work synergistically. The optimal performance of GEM relies on their combined effect. Removing all of them leads to a severe degradation, with latency increasing by 5–6x. {exp_fig/search_vs_nprob.pdf} on Retrieval Performance} {exp_fig/search_vs_rerank.pdf} } In this experiment, we study the impact of parameter $t$, which controls the number of nearest clusters each $q_i Q$ identifies in the early pruning step. We vary $t \{1, 2, 4, 8\}$ and report results on MSMARCO and OKVQA for brevity. Larger $t$ makes the pruning more conservative: pruning fewer clusters to potentially increase recall, but at the cost of higher latency. As shown in Figure~, both datasets exhibit similar trends. When $t=1$, pruning is too aggressive, leading to insufficient accuracy. Performance improves substantially at $t=2$, while the gains begin to saturate as $t$ increases further to $8$. Therefore, we set $t=4$ by default as it provides a favorable balance between accuracy and efficiency. } In this experiment, we evaluate the impact of . This parameter determines the size of the candidate pool retrieved from the graph search, which is then re-ranked using the exact Chamfer distance to obtain the final top-k results. Figure~ reports results for $k=10$ and $100$. For $k=10$, the choice of has a clear effect on accuracy: a larger increases the likelihood that the true best results are included and ranked near the top. For $k=100$, increasing has a more modest impact. This is because our algorithm is already highly effective, with a strong probability of placing the true results within its top-100 candidates, leaving limited room for further improvement. } In this experiment, we investigate the impact of two key indexing parameters: the maximum number of neighbors per vertex and the size of the candidate list during construction . As shown in Figure~ on MSMARCO, with $M=8$ and = 24, the index is small and fast to build, but the graph quality is insufficient, resulting in lower query accuracy. Increasing to $M = 48$ and = 200 significantly enlarges the index size and construction time, but query performance improves only marginally. This is because each vertex connects to too many neighbors, increasing the number of candidates to verify at each hop and thereby hurting latency. Our default configuration of $M=24$ and = 80 achieves the best overall trade-off, offering competitive indexing performance and superior query efficiency and accuracy. {exp_fig/search_and_size_vs_para.pdf} and $$} } {5pt} {exp_fig/search_and_size_vs_Nm_new.pdf} In this experiment, we evaluate scalability with respect to the corpus size and the average number of vectors per set . We randomly sample ${8}N$, ${4}N$, ${2}N$, and $N$ vector sets from the original corpus, and randomly sample ${8}m$, ${4}m$, ${2}m$, and $m$ vectors from each set to form new subsets. We vary one factor at a time. Figure~ reports the effects of these variations on both query latency and index construction time. Increasing $N$ causes a slight rise in latency, from $67$ ms to $85$ ms , consequently increasing indexing time due to the search-based construction procedure. This trend aligns with the logarithmic query cost of graph-based indexes, demonstrating GEM's strong scalability across different corpus scales. Increasing $m$ exerts a much stronger impact on efficiency, query latency rises from $44$ ms to $85$ ms and indexing time from $0.06~$ to $1.79$ h. The higher sensitivity to $m$ arises from the set-to-set distance calculations. This also explains why many studies focus on optimizing the embedding training itself to preserve semantics more effectively with fewer vectors. As discussed in Section , our method can be seamlessly integrated with such advances. {0.235 } {exp_fig/search_vs_shortcuts_single.pdf} } {0.235 } {exp_fig/search_vs_cquant_msmarco.pdf} _|$}} }} , increasing the sampling ratio from 5\ The recall–latency and success–latency curves are omitted as they follow almost the same trend. Such results indicate that sparse shortcuts provide insufficient long-range connectivity, limiting search performance for some hard-to-find cases. In contrast, excessive shortcuts densify the graph, forcing the search to perform more distance computations per step during traversal, thereby offsetting the gains from improved connectivity. Therefore, we set the sampling ratio to 20\ _|$.}}} }|$ on retrieval performance by varying $|C_{}| \{131, 262, 524\}$. Recall that $262k$ is the default value for MSMARCO derived from the empirical formula in Section . As shown in Figure~, the retrieval performance of GEM is robust to the choices of $|C_{}|$. Although increasing $|C_{}|$ leads to higher distance computation overhead, it yields more accurate approximations and thus requires fewer candidates for reranking. In contrast, reducing $|C_{}|$ lowers the computational cost but necessitates probing more candidates to achieve a comparable accuracy level. Therefore, we simply adopt the empirical formula suggested by prior work to set the default value of $|C_{}|$. } The advent of Large Language Models has profoundly reshaped the information retrieval landscape in recent years. }. This shifts the retrieval paradigm from lexical matching to semantic similarity. At its core lies approximate nearest neighbor search, which now underpins many modern applications, such as web search , recommendation systems , and retrieval-augmented generation systems . However, compressing rich semantics into a single embedding vector—like summarizing an entire passage with a single word—inevitably leads to the loss of fine-grained details. This loss often results in irrelevant retrievals and propagates hallucinations in downstream LLMs, especially in long-context inference scenarios . To overcome this limitation, modern information systems are increasingly representing complex objects as sets of vectors . ColBERT is a prominent example, which produces multiple embeddings per query or document by generating one embedding per token. The relevance score is then evaluated using the MaxSim operator , which aggregates the maximum similarity of each query embedding to any document embedding. As illustrated in Figure , both query and document are represented as sets of vectors, $Q = \{q_0, q_1, q_2\}$ and $P = \{p_0, p_1, p_2, p_3\}$, where $q_i, p_j ^d$. Then, each query vector $q_i$ interacts with all vectors in the document $P$: for example, $q_0$ achieves the highest similarity $0.94$ with $p_0$, $q_1$ best matches $p_0$ with score $0.7$, and $q_2$ aligns most with $p_4$ at $0.22$. The final relevance score is computed by summing or averaging these individual scores, $0.94+0.7+0.22 = 1.86$. This new paradigm has demonstrated significant improvements in retrieval quality , interpretability , and generalization ability across various IR benchmarks, emerging as a core functionality in current industrial vector database systems, such as Weaviet , Vespa and Pinecone . {figures/intro1.pdf} } However, these gains come at steep costs in storage, computation, and the complexity of indexing and retrieval. First, representing each item with multiple vectors inflates storage requirements by several orders of magnitude. Second, pairwise distance calculations across all vectors in the set-to-set comparison incur higher costs. These issues can be alleviated by low-bit quantization techniques or by strategically retaining only the most informative vectors ; however, accurate and efficient indexing and search algorithms for multi-vector representation remain an open challenge. To address this, one straightforward idea is to pool all vectors from all vector sets into a single ANN index, IVFPQ or diskANN . At query time, each query vector retrieves its top-$k$ NNs, generating the initial candidate pool for refinement. However, this approach leads to two issues: vector-level similarity does not always imply set-level relevance, resulting in a large number of irrelevant candidates for refinement. For example, a document about "rocky beaches in Portugal" may be retrieved for the query "family-friendly beaches in Europe" due to the shared term "beaches" despite the clear mismatched intent; indexing all individual vectors bloats the index. For example, $10k$ documents with $100$ vectors each lead to a million-level ANN task. Another proposal , from Google, attempted to map the vector set into a single vector using distance-preserving mappings, enabling the use of existing ANN indexes. However, a theoretically sound distance-preserving mapping requires prohibitively high dimensionality of the projection space that even well-optimized ANN indexes struggle to handle efficiently. Moreover, this approach contradicts the motivation for using multi-vector representations and begs the question of why not directly train a single-vector embedding model from the outset? DESSERT is the first solution designed natively for multi-vector retrieval. It compresses the vector set into a sketch via locality-sensitive hashing , and retrieves top-$k$ candidates by comparing hash codes. However, without effective sketch-level pruning, the speedup is limited, and storage pressure increases due to the large number of hash values. Motivated by the aforementioned limitations, we propose GEM, a native }raph-based ind}x framework for }ulti-vector retrieval. The key idea of GEM is set-level indexing: constructing a proximity graph where each vertex represents a vector set, in the hope of preserving the semantic richness of multi-vector representations while leveraging the strong pruning capability of graph-based indexes. Specifically, GEM incorporates a series of co-designed architectural and algorithmic optimizations. First, GEM designs a clustering scheme for vector sets. It evaluates the informativeness of the vectors in each set using a TF-IDF–style score and adaptively assigns the set only to the clusters of $r$ highest-scoring vectors. This design preserves semantic coverage while avoiding excessive redundancy in the subsequent graph construction. Next, GEM proposes a dual-graph structure consisting of intra-cluster graphs and a global graph. The intra-cluster graphs capture compact local structures, while sets shared across clusters naturally form bridges, yielding a globally connected graph that enables efficient cross-region traversal and pruning. To reconcile the non-metric nature of the MaxSim operator with the metric assumption underlying graph-based search, GEM decouples the similarity measures used for graph construction and search. Specifically, the graph is built using Earth Mover's Distance . As EMD is a well-established metric that satisfies the triangle inequality and upper-bounds the MaxSim, the EMD-based graph could provide a stable and reliable basis for navigating toward high-quality results. In addition, GEM introduces semantic shortcuts to bridge the gap between the two similarity measures. These shortcuts directly connect vertices that are semantically similar but distant in the graph, accelerating search and helping escape local optima. Furthermore, GEM employs quantized versions of EMD and MaxSim. This replaces expensive set-to-set distance calculations with efficient lookups over a centroid codebook, significantly enhancing both indexing and querying efficiency. Finally, a cluster-guided multi-entry search algorithm is developed on top of the proposed index. The search begins from multiple entry points within multiple promising clusters and expands over the graph in parallel. Meanwhile, a cluster-guided pruning mechanism prevents the search from drifting into irrelevant regions, effectively balancing speed and accuracy. In summary, we make the following contributions: We propose GEM, the first native graph index for multi-vector retrieval. It successfully unifies the semantic richness of multi-vector representations with the pruning efficiency of graph-based indexing, delivering both high accuracy and efficiency. GEM introduces a series of architectural and algorithmic innovations tailored to the unique challenges of multi-vector search. The architecture adopts a dual graph design with metric decoupling and semantic shortcuts, ensuring a navigable graph topology for accurate search. These are integrated with a novel semantic clustering algorithm, a cluster-aware multi-entry search algorithm, and quantized distance estimation, achieving full end-to-end efficiency across large-scale vector-set collections. Extensive experiments on in-domain, out-of-domain, and multi-modal benchmarks show that GEM significantly outperforms the state-of-the-art methods in both retrieval quality and latency, with a comparable or even smaller index size and indexing time. The rest of the paper is organized as follows. Section defines the problem and introduces a straightforward graph-based solution. Our GEM framework is detailed in Section , followed by experimental results in Section . Section reviews related work, and we conclude in Section . } To address the limitations of existing approaches and the challenges exposed by the naive graph-based solution, we propose GEM — a native }raph-based ind}xing framework for efficient }ulti-vector retrieval. The key innovations are as follows: We propose a set-level clustering scheme, where each document is assigned to only a small number of semantically relevant clusters, guided by adaptive TF-IDF–style weighting over centroids. This reduces index redundancy while preserving semantic coverage . We design a novel dual graph structure, featuring both intra-cluster graphs and a unified global graph naturally bridged by sets shared between clusters. This design guarantees global connectivity while preserving local structure . We decouple the similarity measures used for graph construction and search: Earth Mover’s Distance is used for graph construction due to its metric properties, while Chamfer distance is retained for search. Since EMD upper bounds Chamfer, vertices that are close under EMD are also close under Chamfer, ensuring reasonable navigation. We further augment the graph with shortcuts, directly linking semantically close pairs under Chamfer, but may require several hops under EMD, which improves search efficiency and helps escape local optima . We introduce quantized versions of EMD and Chamfer to alleviate the high cost of set-to-set similarity computation. By leveraging centroid-level codebooks and quantized representations derived from cluster centroids, we improve the efficiency of both the indexing and search phases . We devise a new cluster-guided, multi-entry search strategy. The search is initialized from multiple semantically relevant clusters, enabling parallel, layer-wise expansion over the global graph. A cluster-guided early pruning mechanism aggressively culls unpromising paths, preventing the search from drifting into irrelevant regions . {figures/two-stage.pdf} {figures/tfidf.pdf} $ to obtain a set of centroids $_ = \{C_1, C_2, , C_{k_1}\}$.} These centroids serve as a for our subsequent quantized distance approximations, qEMD during graph construction to be introduced in Section and qCH during search in Section . In the second stage, we cluster $_$ into a smaller set of coarser centroids $_ = \{C'_1, C'_2, , C'_{k_2}\}$, which defines a cluster space for set-level indexing. As illustrated in Figure , all vectors are first grouped into 8 fine-grained clusters, which are then organized into 3 coarse clusters that serve as higher-level semantic bins. Then, we assign each vector $p P$ to its nearest cluster centroid in $_$, denoted as $ $. As a result, a set $P = \{p_1, p_2, , p_{m}\}$ is approximately represented as a set of centroids: = \{ , , , \}. To obtain a set-level clustering, a naive way is to associate a set with every cluster if one or more of its tokens are assigned to that centroid. For example, $P_3$ is linked to all three clusters $C'_1, C'_2, C'_3$. However, this can cause a vector set to appear in an excessive number of clusters, especially when it contains uninformative tokens, such as "is", "the", and "of". This leads to index redundancy and increased query-time overhead. tokens, while ignoring ones. This way, a set is expected to be assigned to only a few clusters, which help reduce index redundancy while preserving the semantic richness of multi-vector representations.} To achieve this, we design a TF-IDF–style importance score over the centroid vocabulary $_$ to reflect how salient the cluster is to the set $P$. Specifically, we define as: = |\{p P : = C'_j, C'_j _\}|, which counts how many token vectors in $P$ are associated with a cluster $C_j$. Then, we apply an to downweight clusters shared across many sets: = {1 + |\{P' : > 0\}|}, where $N$ is the number of vector sets in the database. The overall TF-IDF score is: = . We then construct a weighted centroid profile for each document: = , where $s_{j_t} = $ indicates how strongly cluster $C'_{j_t}$ represents the semantics of $P$. Based on this profile, we assign $P$ to the top-$r$ clusters with the highest TF-IDF scores: _{} = r_{C'_j C }\, . Figure provides a concrete example of this pruning. For $P_3$, our TF-IDF guided mechanism identifies that its connection to cluster $C'_3$ is weak . Therefore, only the two clusters with high semantic relevance are selected as its indexing targets. This pruning step ensures that each document is linked only to its most representative clusters, improving both graph construction efficiency and candidate generation efficiency. {figures/cluster-tfidf.pdf} Clearly, there is no single $r$ value that works best for all: some sets are well represented by a few clusters, while others may require broader cluster coverage. We defer the details of how to determine $r$ adaptively to Section . PLAID adopts a similar clustering idea, but differs from our approach in two aspects. It performs only fine-grained token-level clustering and directly assigns each document to every cluster that any of its tokens maps to. However, we found this strategy less suitable for our graph-based indexing framework. In particular, PLAID's large number of fine-grained clusters results in very few documents per cluster , making it unnecessary and inefficient to build a graph index per cluster. Moreover, the high redundancy in cluster memberships results in costly graph construction and many redundant edges during query processing. Therefore, to better support our graph-based design, we adopt the fine-grained clustering in the first stage for accurate distance approximation, while second-stage coarser clusters, combined with TF-IDF pruning, significantly reduce per-document cluster memberships, striking a better balance between granularity and efficiency. Figure~ compares the average number of clusters each document is assigned to under different strategies across four datasets. The bars correspond to PLAID’s default configuration with $260K$ fine-grained clusters, resulting in high redundancy. The bars represent a coarser 40K-cluster setup without applying TF-IDF guided cluster filtering, while the bars show the results after pruning. As shown, our approach dramatically reduces the number of cluster memberships, from 43.8 to 2.9 on MSMARCO (a 93.4\ {figures/globalgraph.pdf} , multi-vector similarity lacks the metric properties required to serve as a reliable foundation for building a stable and navigable APG.} To address this issue, we propose using EMD as the edge weight metric, while performing search based on the original Chamfer distance. EMD is a well-established distance function that satisfies the triangle inequality, ensuring a well-structured graph topology with guaranteed monotonicity. A key insight supporting this metric decoupling is the bounding relationship between EMD and Chamfer distance. It can be proven that for any two vector sets \ and \ : . This inequality implies that if two sets are close under EMD, they are at least as close--if not closer--under Chamfer distance. Consider a search path that is currently at vertex $P_1$, then the distance from $P_1$ to its neighbor $P_2$ has a predictable upper bound: $CH EMD EMD + EMD $. Consequently, navigating toward the nearest neighbor under EMD is a reliable heuristic for finding a reasonable candidate in the Chamfer space. This mitigates oscillatory search behavior and the local optima trap. EMD is formally defined as follows: given two vector sets \ and \ , and a transport plan \ , then = _T _{i=1}^{m_1} _{j=1}^{m_2} t_{ij} d_X subject to the following constraints: _{j=1}^{m_2} t_{ij} = {m_1}, _{i=1}^{m_1} t_{ij} = {m_2}, t_{ij} 0 where $d_X = 1 - p_i, p_j' $ when cosine similarity is used, and $d_X = \| p_i, p_j' \|_2$ when the $L_2$ distance is adopted. While EMD offers desirable properties for graph construction, its exact computation is prohibitively expensive . To make EMD practical at scale, we introduce an efficient quantized approximation. _$ derived from k-means clustering . Specifically, each token vector is first mapped to its nearest centroid in $_$, and the distance between raw vectors is then approximated by the distance between their corresponding assigned centroids, formalized as: d_X d_X , ), where \ \) and \ \) $ _$. This quantization brings two key computational benefits. First, all centroid-to-centroid distances can be precomputed and stored in a \ codebook, enabling constant-time distance lookups instead of costly high-dimensional distance calculations. Second, the reduced centroid space generates significantly sparser transport plans, which further accelerate EMD computation.} Formally, the quantized EMD is as follows: = _T _{i=1}^{n_1} _{j=1}^{n_2} t_{ij} d_X , ). $, two-stage cluster counts $k_1, k_2$, degree limit $M$, parameter $f$, training pairs $$,} Initialize $G$ with $V = $ and $E = $\; $_{}, _{}, _{}\; $ {$, k_1, k_2$} \; _{}$}{ $G $ {$C_i, _{}, _{}, f, M$}; ~}} \\ } $G $ {$G, , M$};}} \\ Building upon the set clustering and distance function introduced earlier, this section details the construction of GEM's dual-graph structure. Algorithm~ outlines the indexing pipeline. For each cluster $ C_i$, we invoke to build a cluster-level graph while handling cross-cluster bridges. Specifically, for each vector set $P C_i$, we find its top-$f$ ANNs by performing the search algorithm over the current cluster graph, starting from an arbitrary entry point in $C_i$ and using qEMD as the distance metric . If $P$ is new to the current graph $G$ , we directly connect it to its neighbors and ensure that each neighbor $P'$ does not exceed the degree limit $M$ by removing the least similar edges if necessary . Otherwise, \ has already been inserted in previous clusters \ \} \), which means $P$ can serve as a cross-cluster bridge. In this case, we invoke to merge the old and new neighbor candidates , as detailed in the next section. _ $, $f$, $M$} { $C $ $f$ ANNs of $P$ in the current graph of $C_i$ by qEMD; }}\\ }{ { Add a bidirectional edge $ $ to $E$\; }{ Remove the least similar edge from $E $\; } } } {$P, C, G, _ , M$}; }} } } As shown in Algorithm , for a bridge set $P$, we gather both the neighbors already connected to $P$ from earlier clusters and the new neighbors identified in the current cluster . If the total number of neighbors is within the degree limit $M$, we retain them all . Otherwise, only top-$M$ closest neighbors are retained . To ensure $P$ preserves its role as bridges, we enforce that at least one neighbor from each cluster in \ \) is included in the final neighbor set . For example, if P belongs to clusters $C'_2$ and $C'_4$, this step ensures its final neighbor list will contain at least one neighbor from $C'_2$ and one from $C'_4$. This prevents newly added connections from overwriting prior inter-cluster links and preserves global connectivity. Finally, the neighborhood of $P$ is updated to reflect the new connection set . Note that each \ corresponds to a vertex in the global graph: logically assigned to multiple clusters, but physically stored as a single shared node. This design maintains intra-cluster structural coherence, supports efficient cross-cluster traversal via inter-cluster bridges, and eliminates redundant vertex duplication. Figure further illustrates the graph construction process. In Figure , $P_3$ is assigned to cluster $C'_1$ based on its informative vectors . Within the cluster, a local graph is constructed using qEMD-based similarity. Since $P_3$ also belongs to another cluster $C'_2$, it becomes a natural bridge between clusters, as shown in Figure . Figure~ presents a complete toy example of the resulting dual-graph structure. }$, $G = $, $_ $, $M$} $C_{} $ existing neighbors of $P$ in $E$\; $C_{} C_{} C_{}$\; }| M$}{ $C_{} C_{}$\; } } $ $M$ closest vertices of $P$ in $C_{}$ by qEMD\; _ $}{ } C_i = $}{ $S_i \{P C_{} P C_i\}$\; Replace farthest in $C_{}$ with closest node from $S_i$\; } } } Replace $E $ with edges $ $ for all $P' C_{}$\; $, $M$} $}{ $C $ $f'$ ANN results for Q in $G$\; , P $ have degree $ M$ }{ $P_{top} $ 1-ANN in $C$\; Add a undirected edge $ $ to $E$\; } } We observed that some queries may require many short hops to reach a relevant result. This issue arises because, while EMD upper-bounds the Chamfer distance, the reverse does not hold. As a result, two vector sets may be close under Chamfer but remain distant in EMD space, preventing direct connections during graph construction and leading to longer traversal paths at query time. To mitigate this, we propose a lightweight supervised graph augmentation technique that injects semantic shortcuts into the graph index. A shortcut is a direct edge connecting two vertices that are labeled as semantically close but might be distant in the graph. As detailed in Algorithm , we leverage training pairs \ \), where \ is the , to augment the graph. , provides where a positive passage means Here, we randomly sample from these existing training pairs to construct the shortcuts. } For each pair, we run the search algorithm over the current graph to find the query's top-$f'$ ANNs . If \ is not among them and both $P_{top}$ and $P$ have remaining degree capacity, we add an edge between them . Since test queries are generally assumed to follow a similar distribution as the training data, such edges act as a shortcut, allowing future queries to reach the correct region more directly. As illustrated in Figure~, the shortcut helps the search bypass long paths and suboptimal results. By adding EMD-based structural connectivity with Chamfer-based semantic alignment, we enhance graph reachability, search efficiency, and accuracy. {figures/shortcut.pdf} As discussed in Section , a fixed cut-off threshold $r$ is suboptimal. To address this, we introduce an adaptive technique that employs a lightweight decision tree classifier to predict an optimal per-set $r$. Our key idea is to retain the minimum cluster representation required for search-time discoverability. Specifically, we first generate $r_{label}$ for each training pair $ $. The label is defined as the rank of the first cluster in the TF-IDF-sorted profile $ $ that intersects with $Q$'s relevant cluster set $_ $. If no intersection occurs within the top-$r_$ , the label is set to $r_$. Then, the decision tree is trained with the top-$r_$ TF-IDF scores and the number of vectors in $P$ as input, and learn to predict the number of clusters $r$ to retain. During the indexing pipeline, the trained model infers per-set $r$, which is then used for final cluster assignments. This strategy effectively reduces the number of redundant assignments while preserving the necessary semantic coverage for effective retrieval. _$, $_$, parameters $t, ef, k$} Compute relevance scores $S_{c,q} = C Q^ $\; $_{} $ union of top-$t$ centroids for each $q_i Q$\; $E $ one random node from each $C_j _{}$\; Initialize a shared result heap $ E$, a shared visited set $ E$\; Initialize a local queue $_{ep}$ for each $ep E$\; _{ep}$ is not empty}{ _{ep}$ }{ $P $ pop the element in $_{ep}i$ cloest to $Q$ by \; $ $ current furthest distance in $$\; $ > $}{mark $_{ep}$ as empty\; \;} $}{ $ and $_ _{} $ }{ compute $ $\; insert $P'$ into $_{ep}$, $$, and $$\; | > ef$}{Remove the furthest from $R$\; } } update $ $\; } } } $ $ rerank top-$k$ results in $$ by \; $} Building on our index structure, we propose a cluster-guided beam search algorithm tailored for efficient multi-vector retrieval. The complete search procedure is detailed in Algorithm~. At a high level, the algorithm improves upon traditional single-vector greedy search from three complementary perspectives: coarse-grained cluster filtering, informative multi-entry point initialization, and cluster-aware early pruning during graph traversal. In multi-vector retrieval, distance computations are significantly more expensive than in the single-vector setting. To reduce this cost, it is crucial to eliminate highly unlikely vector sets as early as possible—ideally before any graph traversal begins. For this purpose, we draw inspiration from the initial filtering phase of ColBERTv2, which is extremely fast for coarse candidate selection. While this strategy alone may yield a large number of false positives, our downstream graph traversal module can refine these results with high precision. This rationale leads us to retain ColBERTv2’s initial filtering strategy to generate coarse-grained cluster candidates. Specifically, we compute a query–centroid relevance matrix: S_{c,q} = C Q^ , where \ is the centroid matrix and \ is the query token matrix. For each query token \ , we select its top-$t$ nearest centroids and take the union across all tokens to form a relevant cluster set \ . This early pruning step is shown in Line - in Algorithm and visually illustrated in Figure , where irrelevant clusters are filtered out before the graph traversal. after identifying the relevant clusters, we initialize the search from multiple entry points, one from each cluster in \ . As shown in Figure , two yellow vertices from two different clusters are randomly selected as the initial entry points. This multi-entry strategy, combined with the semantic shortcuts on the index side, effectively mitigates the fragmentation of Chamfer nearest neighbors and enhances overall retrieval effectiveness. Given the selected entry points, we perform a cluster-guided, multi-path parallel search over the graph \ using quantized Chamfer distance . Similar to qEMD, qCH serves as a lightweight yet effective proxy for the expensive pairwise computation between vector sets, formalized as follows: = _{q Q} _{p P} d_X , ), where $ $ is the nearest centroids in $_$. As elaborated before, although Chamfer is not used during index construction, it is upper-bounded by the EMD metric, leading that EMD-near neighbors are also reasonable candidates under Chamfer. Line - in Algorithm details the query process. First, we initialize a global result heap $$, a global visited set $$, and a local priority queue $_{ep}$ for each entry point $ep E$ . The search then proceeds in parallel from these entry points. Within each thread, we repeatedly pop the element $P$ with the lowest qCH to the query until $_{ep}$ is empty . We denote $ $ as the current furthest distance to $Q$ in $$. If $ $ is already worse than $ $, the current path is deemed unpromising and terminated . Otherwise, we continue expanding $P$'s neighbors. To boost query efficiency, we apply a cluster-aware pruning condition . That is, if $P'$ would pull the search into an irrelevant cluster , it is directly filtered out. If the $P'$ passes this filter, the qCH score is computed, and the local queue $_{ep}$, result heap $$, and visited set $$ are updated accordingly . $$ maintains a maximum of $ef$ candidates, evicting the farthest node when necessary . Note that both $$ and $$ are globally shared across threads, enabling implicit communication. If one path discovers a closer node, it updates the $ $ , which allows other paths to prune their search space earlier. Similarly, once a vertex is visited by any thread, it is skipped by all others, preventing redundant computation. The search terminates when all $_{ep}$ queues are exhausted. In the final step, the top-$k$ candidates in \ are re-ranked using exact Chamfer distance before being returned. {figures/search.pdf} Figure~ gives an example of the GEM search process for 1-ANN with \ . \ is the ground-truth nearest neighbor. Assume that the cluster filtering stage identifies two relevant clusters, \ , while pruning $C'_3$ and $C'_4$. The search then begins in parallel from two entry points: ep1 and ep2 . In the first iteration , the path of ep1 expands to \ , while ep2 discovers \ . These two candidates are inserted into the shared result heap \ . In the second iteration , the path from ep1 continues by expanding from \ . When considering its neighbors, \ is immediately pruned without a distance calculation because it resides in a pruned cluster. Concurrently, the other path expanding from \ jumps directly to \ via a semantic shortcut , bypassing what would otherwise have been a multi-hop traversal. As a graph-based method, GEM naturally supports dynamic index maintenance, including both insertion and deletion of data points. To insert a new vector set into the current index, GEM first maps its vectors to the fine-to-coarse centroid hierarchy and prunes uninformative clusters using the TF-IDF–guided strategy, as described in Section . Then, the set is incorporated into the graph by linking it to its nearest neighbors under the qEMD metric, followed by updating the cross-cluster bridges to maintain both local proximity and global connectivity, as detailed in Section~. For deletions, we follow the lazy deletion strategy , which is commonly used in graph-based indexes: a vertex to be removed is simply marked as inactive, so it is skipped during search and can be physically pruned in later maintenance passes. In this section, we formulate the multi-vector retrieval problem and introduce a baseline solution. We first clarify the similarity measurement used throughout this paper. Following ColBERT and other state-of-the-art multi-vector methods , we adopt the MaxSim operator, Chamfer similarity, to compute the similarity score between two multi-vector sets. In the following, we choose the terminology Chamfer due to its historical precedence . Given two sets of vectors $A, B ^d$, the similarity score is defined as the sum of the maximum similarity between each vector in $A$ and all vectors in $B$. Formally, = _{a A} _{b B} , where cosine similarity or $L_2$ distance are typically used as the default choices for $ $ in prior work. Given a database $ = \{P_1, P_2, , P_N\}$, where each $P_i = \{p_1, p_2, , $ $ p_{m_i}\}$ is a set of vectors with each $p_j ^d$, and a query $Q = \{q_1, q_2, , $ $q_{m_q}\}$ with each $q_j ^d$, the multi-vector retrieval problem, the vector set search problem, aims to return the top-$k$ sets in $$ that are most similar to $Q$, namely: = k \{ |P \}. Multi-vector retrieval has been widely adopted in various domains and systems . A prominent example is ColBERT for document retrieval, where queries and documents are encoded into sets of token-level vectors. Throughout this work, we use the terms "document" and "vector set" interchangeably, as well as "token" and "vector" interchangeably. However, it is worth noting that multi-vector retrieval itself is a general problem, agnostic to the data's origin. A "vector" could represent an image patch, a chunk of text, or other forms of data. Recall the ANN search problem in the traditional single-vector setting: both data and queries are represented as high-dimensional vectors, and the goal is to find the top-$k$ most similar data points to a given query based on certain distance functions . Formally, given a dataset $ = \{o_1, ..., o_N\}$ with each data object $o_i ^d$ and a query $q ^d$, it aims to return = k \{\|q, o_i\|_2|o_i \}. Among ANN solutions, graph-based methods have demonstrated the best trade-off between search accuracy and efficiency on real-world datasets. These methods organize the base vectors into an approximate proximity graph , where each vertex corresponds to a data vector, and edges connect vertices that are sufficiently similar. During query time, a greedy search is performed on the graph: it starts from an arbitrary vertex and iteratively traverses edges toward neighbors that are closer to the query, until convergence to a local optimum. Inspired by the success of graph-based indexes in the single-vector setting, we pose a question: In such a graph, each vertex represents a vector set, and an edge connects two sufficiently similar vertices based on the Chamfer distance. The graph construction and search processes are the same as those in single-vector methods, so we omit them here for brevity. Unfortunately, this direct extension does not work due to two critical issues: Fragmented neighbors and local optima. Chamfer distance is not a metric and lacks triangle inequality, which undermines core assumptions behind graph-based search. For example, even if $Q$ is close to $P_1$ and $P_1$ is close to $P_2$, $Q$ may still be far from $P_2$. As a result, the nearest neighbors of $Q$ are scattered across the graph, making them difficult to reach via local hops and slowing down convergence. Without strong connectivity among these neighbors, greedy search paths become oscillatory and unstable, often getting trapped in local optima—terminating early even when better candidates exist several hops away, ultimately degrading recall. Lack of cheap distance approximation and pruning. While graph structures provide a powerful pruning framework, the exorbitant cost of computing the Chamfer distance remains a critical bottleneck. It is impractical to perform set-to-set comparisons between the query and all candidate neighbors at each step of the search traversal. A practical solution, therefore, necessitates a cheaper distance approximation, effective pruning strategies, and more informed entry points. We will empirically validate these analyses in Section , where this naive approach, named MVG, is included as a baseline. The ANN search problem has been extensively studied in the database community for decades . However, nearly all well-known methods make the single-vector representation assumption, both queries and data are encoded as single high-dimensional vectors in the same Euclidean space. Prominent approaches include LSH-based methods , quantization-based methods , and graph-based methods . LSH-based methods are well-known for the strong theoretical guarantees on recall. Quantization-based methods significantly reduce memory usage and accelerate distance calculations. Graph-based methods, which construct a proximity graph among vectors and find neighbors via greedy traversal, have become the de facto state-of-the-art, demonstrating superior performance in both secch accuracy and efficiency. Multi-vector search is a relatively new paradigm that has seen rapid adoption across various domains due to its high effectiveness . It is first introduced for document retrieval by ColBERT , which represents queries and documents as sets of token-level embeddings and employs a architecture to achieve strong performance. Subsequent work further improved the effectiveness of this paradigm, for instance, by learning different weights for vectors of varying importance , or expanding it to other domains such as visual-language retrieval tasks . However, early research relies on simple inverted lists to index all individual embeddings, which hinders large-scale deployment. To improve scalability, ColBERTv2 introduces a centroid-based compression scheme, where each token embedding is stored using the ID of its closest centroid and a low-bit residual , significantly reducing storage overhead. However, the search process remains costly. PLAID builds on this by further exploiting the centroids to aggressively prune irrelevant documents via a mechanism, substantially accelerating retrieval. EMVB enhanced PLAID with system-level optimizations, including bit-vector filtering and SIMD-accelerated scoring. More recently, IGP proposes to replace the inverted list with a proximity graph index over centroid vectors and develops an incremental next-similar retrieval technique to efficiently filter low-relevance candidates. Alternative indexing strategies have also been explored. MUVERA introduces fixed-dimensional encodings that approximate multi-vector similarity, enabling the use of optimized single-vector ANN indexes. While efficient, this method inevitably loses token-level alignment, risking reduced recall. DESSERT proposes compressing vector sets into compact sketches, but its speedup is often limited by a lack of effective set-level pruning. Orthogonal to the research on indexing and search algorithms, XTR improves retrieval efficiency by simplifying the scoring mechanism during training. CITADEL reduces computation cost by selectively considering only a subset of tokens. POQD enhances accuracy by optimizing query decomposition with the assistance of LLMs. Since these approaches optimize the set-level representation itself, they can complement the above indexing methods for higher search accuracy and efficiency. 