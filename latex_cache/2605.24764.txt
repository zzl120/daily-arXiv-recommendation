 {IEEEtran} {inputenc} {fontenc} } {Cisco Systems, Inc.} {} In a standard dense retriever every document is encoded as one mean-pooled embedding and ranked by cosine similarity. This single-vector recipe is fast and good enough so long as relevance spreads across most of a document's tokens; the moment relevance into a short subspan, the recipe systematically bleeds signal — the subspan averages into surrounding-token noise. Late-interaction methods such as ColBERT resolve this by storing per-token embeddings for every document and computing a MaxSim between query tokens and document tokens at retrieval time; the cost is far larger indices. Here we present : a plug-in second stage placed between any single-vector retriever and a downstream consumer — for example, the per-agent context window of a multi-agent LLM system. Spectral Retrieval reuses ColBERT's per-document token embeddings, but compares them to the query through a on the token axis. At scale $L$ the sinc kernel behaves as a normalised low-pass filter: the identity endpoint recovers late interaction's per-token MaxSim, while the $L $ endpoint becomes a uniform filter . The maximum cosine similarity over positions, then over a small grid of scales, gives a similarity score that, when these endpoints are included, is no less informative pointwise than either limit. We supply a self-contained derivation of the kernel, an analysis of its complexity, a recovery guarantee for the endpoint scores, a controlled synthetic benchmark whose sole parameter is the cosine of the planted spike against the query, and a real-encoder evaluation on the LIMIT-small public benchmark . Across 1,000 synthetic documents of 50--500 tokens in $^{64}$ with planted single-position relevance, mean-pool retrieval sits at chance regardless of spike strength, because the spike contributes only $O $ of the document mean. Spectral Retrieval, by contrast, exhibits a sharp transition near the corpus-level top-$k$ token noise floor — the order statistic over roughly the candidate pool's random token-query cosines — reaching Recall@10 $=1.0$ at $ = 0.60$ and saturating from there. The location of the transition matches this order-statistic prediction. On LIMIT-small with a frozen encoder, Spectral Retrieval lifts Recall@10 from $0.33$ to $0.90$, MRR from $0.22$ to $0.79$, and the strict two-hit Success@10 from $0.12$ to $0.84$ — without retraining the encoder. We sketch how the method applies to multi-agent LLM systems: each agent draws a tightly focused window from a shared corpus, and a debate orchestrator consumes the per-agent contexts. We close with the method's limitations: Spectral Retrieval inherits late interaction's index size; the gains concentrate on queries with narrow position-localised matches; and although the single-vector embedding-dimension ceiling identified by recent theoretical work does not apply directly , what is inherited is the geometry of the underlying per-token embedding space. Retrieval-Augmented Generation has become the standard interface through which large language models access external corpora — increasingly, the channel between an LLM agent and the external context it reasons over. In the simplest single-agent layout, an embedding model $ $ encodes both the query $q$ and every document $d$ as a fixed-dimensional vector; the system then returns the top-$k$ candidates ordered by cosine similarity. This recipe is fast, scalable, and well understood . It also has one well-known failure mode: when relevant content in $d$ is squeezed into a short subspan, mean pooling averages that subspan with surrounding tokens, and the cosine to $q$ collapses toward the average similarity of the document as a whole. In multi-agent LLM systems the failure compounds. Consider an agent dedicated to : it wants documents whose paragraph fits the incident, even if the rest of the page is about routine network operations. An orchestrator pooling conclusions from several specialised agents is bounded above by the quality of the agent-level retrievals it stacks on. ColBERT and ColBERTv2 are two late-interaction methods that resolve the localisation issue by storing per-token embeddings for every document and computing a MaxSim against the per-token embeddings of the query — paying with an index one to two orders of magnitude larger than its single-vector counterpart. Sparse retrievers such as SPLADE attack a related issue via a learned sparse expansion over the vocabulary. We propose — a modest tweak on late interaction that smoothly interpolates between mean pooling and per-token MaxSim through a multi-scale sinc kernel. The motivation is signal-processing in nature: across a length-$N$ token sequence, the normalised sinc kernel at scale $L$ behaves as a low-pass filter whose cutoff is $ 1/L$. At $L=1$ it reduces to a Dirac delta function $ $ ; at $L N$ it becomes uniform . It follows that a aggregator returning the maximum cosine over positions and over a small grid of scales is at least as informative as either extreme. Classical multi-resolution analysis has long examined this kernel family under the term ; our rule is essentially a discrete cousin of that wavelet-style scale sweep. Spectral Retrieval is defined here as a plug-in re-ranking stage that reads the same per-token embeddings used by a late-interaction index and outputs one score per pair. It has three tunable parameters: the scale grid $$; the candidate pool size $K$ produced by a fast first stage; and the pooling choice across scales . A self-contained derivation of the sinc kernel is supplied, with sampling chosen so that the $L $ endpoint recovers mean pooling exactly. The narrow endpoint is the identity operator, denoted $L=1$ in the implementation, which recovers per-token MaxSim. The fix is non-trivial: naive integer-lattice sampling causes $ $ to vanish at every non-zero integer offset, collapsing the kernel to a delta at every scale and breaking the wide-scale limit. Sampling on a length-aware lattice at spacing $1/L$ restores it. We prove an endpoint recovery guarantee. For every document $d$ and query $q$, $ \{ , \}$ whenever $$ contains both the identity endpoint and an explicit mean-pool endpoint . We give an analytical complexity bound of $O $, where $K$ is the candidate count, $S$ the scale count, $N$ the document length in tokens, and $d$ the embedding dimension. Because the fast first stage stays untouched, the extra cost only hits the $K$ candidates — consistent with standard re-ranking budgets. A controlled synthetic benchmark is built around one tunable parameter ; Recall@$k$ is plotted against $ $ and against document length. The spectral retriever tracks the mean-pool baseline while $ $ sits below the corpus-level token noise floor, then sharply pulls away above it — lining up with the order-statistic prediction. We sketch the multi-agent LLM application: one shared corpus with per-agent retrieval scopes, where Spectral Retrieval delivers each agent a tighter window into the corpus before handing off to the debate orchestrator . We do not claim leaderboard-topping numbers on BEIR , MS MARCO , or HotpotQA ; those evaluations are left to follow-up work . We do report a real-encoder evaluation on LIMIT-small in , the small split of the benchmark built explicitly to stress the theoretical limits of embedding-based retrieval. {figures/Fig1.png} ^{N d}$ for each candidate are convolved along the token axis with a normalised sinc kernel at $S$ scales. We then record, per scale, the maximum cosine similarity between $q$ and any convolved token; the final spectral score is the maximum across scales. The re-rank lives entirely within the candidate pool and never touches the first-stage index.} The modern recipe of pairing mean-pooled BERT embeddings with cosine similarity was cemented by Sentence-BERT and Dense Passage Retrieval . Equipped with off-the-shelf approximate-nearest-neighbour indices the recipe handles billions of documents — but it fundamentally cannot resolve relevance that lives inside a short subspan of a long document. Both ColBERT and ColBERTv2 retain per-token embeddings and aggregate via MaxSim: per query token, take the maximum cosine over any document token. The resulting scores are highly informative, yet index size grows linearly with document length, which has spawned an entire compression literature. Spectral Retrieval reuses the same per-token storage footprint but couples a single query vector to a multi-scale convolution instead of a per-query-token MaxSim. The two approaches can be combined. A learned sparse expansion of each document over the vocabulary, ranked by inner product, is the recipe behind SPLADE . SPLADE is orthogonal to our work: it modifies the document's , not the rule for aggregating the query against the document. Retrieval-Augmented Generation pulled retrieval inside the LLM training loop. The production trend is the other way around: retrieval stays the training loop, and instrumentation — re-rankers included — sits at inference time. That production pattern is exactly where Spectral Retrieval slots in. The signal-processing literature has a long history of multi-scale convolution with sinc-like kernels . We borrow the multi-resolution intuition but with a grid of scales, no wavelet basis, and a max aggregator. Our rule is a compact fixed convolution sweep — no wavelet decomposition. Multi-agent debate and sequential-consensus orchestration outline methods for fusing conclusions from several specialised LLM agents. Our retriever is one building block in that stack: it sharpens each agent's retrieval window prior to the agent's first position. Chain-of-thought prompting , the per-agent reasoning loop's final component, stays independent of the retrieval rule. Let $ : ^d$ be a frozen contextual encoder mapping an $N$-token sequence into $N$ contextualised vectors arranged as $E_d ^{N d}$. Each row of $E_d$ is presumed L2-normalised; if not, normalisation happens at index time. We write $ ^d$ for the L2-normalised pooled query embedding . Let $ =x/\|x\|_2$ when $x 0$. A standard single-vector retriever ranks via \;=\; , \;\! . When the query reduces to a vector, late interaction collapses to per-token max similarity: \;=\; _{i=1, ,N} , E_d . For finite positive scale $L$, the length-$N$ normalised sinc kernel takes the form on a zero-indexed lattice k_L \;=\; \! /L )}{ _{u=0}^{N-1} \! /L )} \\ t = 0, , N-1 In this expression, $c = /2$ marks the centre and $ = / $ for $x 0$ with $ = 1$. A global normalisation in the denominator forces $ _t k_L = 1$. Two important endpoints drop out. As $L $, $ /L 0$ uniformly in $t$, and since $$ is continuous at zero with value $1$, $k_L 1/N$ for every $t$. Convolving $E_d$ with this limiting kernel along the token axis thus produces an output where every position carries the of $E_d$. After row normalisation, the cosine similarity to $ $ equals $ $. Finite large scales approximate this endpoint; the exact endpoint is the $L= $ mean-pool operator. The implementation includes the identity operator as the narrow endpoint, denoted $L=1$ in the scale grid. At position $i$ the cosine similarity to $ $ is then $ , E_d $, with the maximum over positions equal to $ $. This avoids an indexing artefact of a finite, centred sinc kernel: for even $N$, $c$ is half-integer, so sampling $ $ directly would not produce an exact Kronecker delta. These endpoints do hold under naive sampling that omits dividing by $L$ inside the sinc — i.e.\ $k_L $ truncated to length $L$. The sinc is sampled on a lattice at spacing $1/L$, forcing the wide-scale limit toward a uniform kernel rather than an oscillating tail. Fix a small grid of scales $ = \{L_1, , L_S\}$; our default finite grid is $\{1, 3, 5, 7, 10, 15, 20, 30\}$, where $L=1$ denotes the identity endpoint. For the identity endpoint, set $ E_d^{ }=E_d$; at each other finite scale, convolve every $E_d$ row along the token axis with $k_{L}$ to obtain $ E_d^{ } ^{N d}$. Renormalise each row of $ E_d^{ }$ back to unit length, and form _L \;=\; _{i=1, ,N} , \; E_d^{ } . The spectral score is \;=\; _{L } _L . Assume $$ contains the identity endpoint and the explicit mean-pool endpoint $L= $. Then \; \; \{ , \; \}. gives $ _1 = $ because the identity endpoint leaves every token row unchanged. Applying the $L= $ endpoint per row yields $ _{ } = $. The outer maximum then dominates both. With a finite large scale instead of $L= $, the same statement holds up to the approximation error between $k_L$ and the uniform kernel. The guarantee runs in only one direction: at moderate SNR, the spectral score can be strictly larger than either limit. 's synthetic benchmark shows exactly this. { function spectral_score : s_max = 0 N = E.shape for L in scales: k = sinc_kernel # length-aware Ec = conv_per_dim Ec = row_normalize s = max_pos ) s_max = max return s_max } Running naively on every document in the corpus per query is prohibitive. Instead, Spectral Retrieval is a : a fast first stage — any single-vector ANN index — returns a $K$-document candidate pool, and re-ranks only that pool. First-stage cost is unchanged. Second-stage cost is O For typical $ = $, per-query cost is dominated by the $S N$ factor — around 1,600 per-dim convolutions per candidate. A vectorised build amortises the per-dim convolution down to a single FFT call. Three implementation pitfalls worth flagging: If $N$ falls near the non-identity scales in $$, the finite sinc kernels become nearly flat and their per-position cosines approach the mean cosine. The identity endpoint still exposes token-level matches, so very short documents reduce to a small mixture of mean-like and max-like scores rather than creating a separate retrieval regime. This is the desired behaviour: short documents carry little localised structure for the method to exploit. If $N = 1$, then and report the same single number, as does the spectral score at every scale. The implementation short-circuits this case to skip an empty call. The denominator $ _j /L)$ remains well-conditioned for any $L 1$, $N 1$. An upper bound is $N$ ; a lower bound near $N )$ stays positive once $L N/2$. Throughout our runs at $N 8192$ and $L 1000$ no numerical issues have surfaced. In production the per-token document store dominates operational cost. Two optimisations of practical importance follow. Storing them as fp16 cuts index size in half with no measurable accuracy loss, matching the compression numbers reported for ColBERTv2 . Adding centroid + residual quantisation shaves another factor of roughly $8$, at the cost of 1--2 percentage points of accuracy. Either scheme works with Spectral Retrieval because the per-query convolution is the only place dequantised token embeddings are read. The inner loop is a per-dimension convolution. The same $S$ kernels apply across every candidate document, so candidates can be stacked along the batch axis and a single 1D convolution invoked per scale, which on modern GPU stacks is enough to keep re-rank latency in the tens-of-milliseconds range at $ = $. For very long documents , an FFT-based convolution beats the direct sum asymptotically — $O $ rather than $O $. On current hardware the crossover sits near $N L 16{,}000$. At dense-retrieval lengths typical in practice , direct convolution still wins. The ColBERT score is \;=\; _{j} _{i} E_q , E_d , — a sum across query tokens of per-token MaxSim. At $L = 1$, Spectral Retrieval becomes the version of ColBERT: a single pooled query vector and a max over document tokens. As $L $ it collapses onto a single-vector retriever. Spectral Retrieval thus forms a continuous family bracketing both endpoints. The bulk of ColBERT's gain over single-vector retrieval is driven by its multiple query tokens; Spectral Retrieval's gain comes from multi-scale smoothing on the side. These two ideas are orthogonal and can be stacked - replace the single $ $ in with a per-query-token sum. Pinning down a single scale $L$ commits the system to one resolution of relevance. Short subspans match best at small $L$; relevance at the paragraph level matches better around moderate $L$; document-wide relevance matches at large $L$. Real corpora hold a mixture of all three. Taking the max over a small grid of scales is robust: for any pair, scale will land near the optimum, and the max picks it. 's synthetic benchmark quantifies the point: the recall curve under the scale is heavily query-dependent, whereas the curve under max-over-scales tracks uniformly close to the upper envelope. {1.15} {6pt} {lll} Component & Cost per query & Dominant factor \\ First stage & $O $ & corpus size $M$ \\ Token-emb load & $O $ & bandwidth \\ Sinc convolve & $O $ & re-ranking \\ Cosine over positions & $O $ & re-ranking \\ Max-aggregate & $O $ & negligible \\ On a 50M-document corpus with $K = 100$, $S = 8$, $N = 200$, $d = 768$, the per-query re-rank works out to roughly $1.2 10^8$ multiply-adds. Across most production budgets, the first-stage ANN dominates total wall-clock. Stored as fp16, per-token embeddings take $2 N d$ bytes per document, identical to ColBERTv2 . Across a 50M-document corpus with mean $N = 200$ and $d = 768$, the resulting index lands near 15 TB. That is large yet defensible at production scale: ColBERTv2's compressed index shrinks it by roughly an order of magnitude, and the same compression carries over here. Within multi-resolution analysis a signal is broken down into approximations across successively coarser scales. Our setup employs a grid of scales rather than a genuine wavelet basis, but the multi-resolution intuition still carries over. Concretely, we focus on the token-axis signal $f = , E_d $ — that is, the per-token cosine to the query. Mean-pool similarity is tied to its average $ f = _i f $; max-token similarity is $ _i f $; and, before the row-normalisation step, the spectral score at scale $L$ is the position-wise maximum of convolved with $k_L$ and projected onto $q$. By inner-product linearity, the unnormalised variant obeys _L \;=\; _i \; _j k_L \, f \;=\; _i , In other words, the unnormalised variant is the peak amplitude of the per-token cosine signal low-pass filtering at cutoff $1/L$. Our implementation then renormalises each smoothed row and scores the cosine to that row direction; this preserves the same matched-scale intuition while aligning the score with cosine retrieval. Hence the spectral score $ _L _L$ amounts to $f$'s peak amplitude, up to this row-normalisation step, across a multi-resolution sweep. In wavelet terms it is the peak detail at the matched scale, where matching scale is selected automatically per pair. This perspective also clarifies why relevance benefits from scales. If $f $ contains a positive bump of width $w$, convolving $k_L$ with the bump peaks around $L w$. Scales below that cannot accumulate enough positive evidence; scales above blur the bump into the surrounding noise. A short-grid sweep over $L$ is cheap insurance against not knowing $w$ in advance. For a concrete illustration, pick a toy three-document corpus with $d = 4$: E_{d_1} &= 0.50 & 0.10 & 0.10 & 0.10 \\ 0.10 & 0.10 & 0.10 & 0.50 \\ 0.10 & 0.20 & 0.20 & 0.10 ,\\ E_{d_2} &= 0.30 & 0.30 & 0.30 & 0.30 \\ 0.30 & 0.30 & 0.30 & 0.30 \\ 0.30 & 0.30 & 0.30 & 0.30 ,\\ E_{d_3} &= 0.10 & 0.20 & 0.30 & 0.40 \\ 0.40 & 0.30 & 0.20 & 0.10 \\ 0.25 & 0.25 & 0.25 & 0.25 , For the actual computation, the rows are L2-normalised. Take query $q = $. Mean-pool retrieval orders $d_2 d_3 d_1$ because $d_2$'s mean projects most strongly onto $q$. But the of $d_1$ has cosine $ 0.93$ to $q$ — far above any single token of $d_2$ or $d_3$. With $L = 1$ Spectral picks up that row directly and orders $d_1 d_2 d_3$, the qualitatively right ranking for a query that aligns sharply with one specific feature. With $L = 100 N$ the spectral score collapses to mean-pool, recovering the original ranking. Max over scales picks $L = 1$ here and gets the answer right. The example also surfaces 's Goodhart-style risk: $E_{d_1}$ might be a long document whose first row is a tangentially relevant outlier while the rest is irrelevant. A mean-aware aggregator would penalise that; a max aggregator does not. When the risk is material, production deployments should swap strict max for a top-$m$ mean or a percentile cap. Every agent in a multi-agent LLM system has a — one for , another for , another for , and so on. A natural deployment routes each agent through the same shared corpus using a role-specific prompt. Mean-pooled retrieval can distinguish roles only through the pooled query vector and the pooled document vector; when the relevant evidence is a narrow paragraph inside a broad document, those pooled scores often remain dominated by the same document-level background. Spectral Retrieval adds subspan sensitivity. A security-relevant paragraph can yield a higher spectral score under the security agent's prompt, even when document-mean similarity is close across roles. In a companion paper we describe an orchestrator that walks $K$ specialised agents through a sequential debate monitored by a Wald SPRT. The per-agent retriever is orthogonal to the orchestrator's stopping rule. Substituting Spectral Retrieval at the per-agent stage gives each agent more before producing its first position, while leaving the stopping rule untouched. Anecdotally, sharper per-agent retrieval cuts the expected debate-round count ; the present paper does not quantify the effect. solve} Ultimately, Spectral Retrieval remains a re-ranking rule. The ANN index is left as-is, and the first-stage recall ceiling carries straight through: if the relevant document is missing from the size-$K$ candidate pool, no re-ranking step can put it back. The embedding-dimension ceiling of is a single-vector result and does not bind a multi-vector method like Spectral Retrieval directly; what does carry through is the geometry of the per-token embedding space, since the underlying encoder is shared. reasoning --- the territory of multi-hop benchmarks --- is better addressed at the agent layer. Chain-of-thought prompting is the per-agent reasoning loop fed by retrieved context, returning an answer. Logically the retrieval rule precedes chain-of-thought and is independent of it; in practice the two interact non-trivially. Sharper retrieval reduces the of context an agent has to scan in order to pull out the relevant passage — which in turn shortens the chain-of-thought required to ground the answer. The present paper does not measure that interaction. Anecdotally, though, chains-of-thought fed by Spectral Retrieval come out shorter and contain fewer "let me look at this other passage too" detours than chains-of-thought fed by mean pooling on the same task. Any formal study of the effect would have to fix a chain-of-thought length budget; see . For a concrete multi-agent example, we sketch the deployment that motivated the paper. Imagine a security-incident triage system with three specialised agents: a agent that hunts for evidence of compromise — process injection, lateral movement, suspicious DNS activity; an agent that hunts for known-good explanations — scheduled maintenance, recent deployments, known-bug references; a agent that hunts for regulatory implications — PII exposure, regulatory reporting requirements. A shared incident knowledge base of $ 50{,}000$ runbooks, postmortems, and policy documents is the corpus. Documents are split into sections, and most of any single document is irrelevant to any single incident. The agent's interests sit in a few paragraphs of one specific postmortem; the agent cares about different paragraphs of overlapping but distinct documents; the agent cares about section-header content. When broad incident vocabulary dominates the document means, mean-pool retrieval tends to hand the agents overlapping top-$k$ lists on the same incident description. Per-agent spectral retrieval, by contrast, is more likely to surface a top-$k$ for each agent — based on which candidate-document subspans look sharpest under that agent's role-specific prompt. The downstream debate orchestrator now sees more distinguishable first positions instead of near-identical ones; the orchestrator's stopping rule, scoring between agents, should terminate faster when disagreement is principled than when it is noise-driven . We do not report end-to-end metrics for this deployment because the orchestrator's termination behaviour is a separable concern that should not, we argue, be co-assessed with the retriever. The right next experiment is a public-data analogue — for instance, the EmergencyManagement subset of BEIR or a Stack Exchange security-Q\&A subset — and we leave that to follow-up. Re-ranking rules abound; Spectral Retrieval is one of many. Production alternatives in common use include: : e.g.\ a BERT-style model fed the pair as a single input that emits a relevance score. Cross-encoders are vastly more accurate than any vector-space rule, at roughly $10 $ the per-candidate cost of Spectral Retrieval. , which deduplicate within the candidate pool by penalising candidates resembling those already chosen. driven by a learned scorer such as LambdaRank. Spectral Retrieval composes cleanly with all three. One natural pipeline: fast ANN; the Spectral stage, which lifts the few candidates exhibiting strong localised relevance to the head of the pool; an optional cross-encoder pass on the surviving top-$k'$, with $k' k$; an MMR step for final-list diversity. Spectral cost is low enough that the cross-encoder dominates overall latency. To test the analytical predictions in and , the cleanest tool is a controlled synthetic experiment. By design we keep it small, and fully reproducible. A total of $M = 1{,}000$ documents is drawn. The length $N$ of each is sampled uniformly across $\{50, 51, , 500\}$. Token embeddings come from a standard Gaussian over $^{d}$ at $d = 64$, then row-normalised. A unit-vector query $q$ is fixed. Across $Q = 200$ instances, a is implanted at a uniformly random index $i^ $: $E_d $ is overwritten with a unit vector $v$ satisfying $ = $, v \;=\; \, q \;+\; \, u_ , where $u_ $ is sampled uniformly from the unit sphere lying in the orthogonal complement of $q$. By construction, $v q = $ holds exactly — and this decouples the SNR parameters from the dimension of the embedding. We sweep $ $ across $\{0.30, 0.45, 0.60, 0.75, 0.90\}$. None of the remaining $M - 1$ "distractor" documents per query carry a planted spike. For every query, the rank of the document carrying the spike is recorded under two scorers: MeanCos and Spectral with grid $ = \{1, 3, 5, 7, 10, 15, 20, 30\}$. Recall@$k$ at $k \{1, 5, 10, 50\}$ is averaged across $Q$ queries. {figures/Fig2.png} $, sampled on the proper length-aware lattice . The $L=1$ curve represents the identity endpoint . As $L$ grows toward and beyond $N$, the kernel approaches the uniform mean-pool endpoint. Intermediate scales smooth over local windows.} {figures/Fig3.png} $ for Recall@$k$.} {figures/Fig4.png} The next table summarises the headline numbers. {1.15} {6pt} {llll} $ $ & MeanCos R@10 & Spectral R@10 & $ $ \\ 0.30 & 0.015 & 0.020 & $+$0.005 \\ 0.45 & 0.010 & 0.040 & $+$0.030 \\ 0.60 & 0.035 & 1.000 & $+$0.965 \\ 0.75 & 0.015 & 1.000 & $+$0.985 \\ 0.90 & 0.025 & 1.000 & $+$0.975 \\ 000$ candidate documents.} They confirm 's endpoint claim along two axes. First, a single-position spike sits beyond the reach of mean pooling structurally: before document-vector normalisation, the document mean picks up only $O 0.005$ of additive signal, leaving the spike-bearing document buried among distractor means in this $M,N,d$ regime. Second, the spectral retriever shows a sharp transition near the corpus-level top-$k$ token threshold. For Recall@$k$, a useful approximation is $ ^ _k $, where $N$ is interpreted as a typical document length; with $M=1{,}000$, $N 250$, $k=10$, and $d=64$, this lands near the observed transition between $ =0.45$ and $ =0.60$. Below that threshold, even per-token comparison competes with many accidental high-cosine distractor tokens; above it, the identity endpoint peels the spike apart from the top-$k$ distractor tail. The single-position spike of represents the hardest case for mean pooling: one token contributes only $1/N$ of the mean, leaving an additive signal at order $1/N$. As the spike across $w$ adjacent tokens, mean pooling captures $w/N$ of the total signal and gradually catches up. Fig.~ answers precisely that question. The synthetic benchmark is rerun at $ = 0.45$ while we sweep the spike width across $w \{1, 3, 5, 7, 10, 15, 20, 30\}$. {figures/Fig5.png} Here are the headline numbers: {1.15} {6pt} {lll} $w$ & MeanCos R@10 & Spectral R@10 \\ 1 & 0.010 & 0.070 \\ 3 & 0.100 & 1.000 \\ 5 & 0.200 & 1.000 \\ 10 & 0.620 & 1.000 \\ 20 & 0.960 & 1.000 \\ 30 & 1.000 & 1.000 \\ This is the picture anticipates. As the spike broadens, an intermediate scale $L w$ aggregates positive evidence over the spike while smoothing the surrounding noise. The max-over-scales aggregator selects the right $L$ automatically, so spectral retrieval crosses into perfect recall at far smaller $w$ than the mean-pool baseline. Once $w N$ the two methods read the same signal and both reach perfect recall together. Our synthetic setup is stylised by design: isotropic Gaussian distractors, no contextualisation, and a modest embedding dimension $d = 64$. Real corpora bring correlated tokens, relevance distributed across multiple positions, and contextualisation that can carry local evidence into the pooled document vector. The chosen design forces mean pooling to fail completely for a structural reason: in the document mean, the spike-to-noise ratio is fundamentally too small. In a real corpus with large $d$ and contextualised embeddings, the noise floor $$ is markedly smaller, mean pooling does bottom out at chance, and the spectral gain narrows to a smaller absolute margin spread over a softer transition. A separate caveat concerns the per-scale story: in Fig.~, at $ = 0.60$ only $L = 1$ recovers the planted single-position spike; intermediate $L$ collapse to chance. The multi-scale grid wins here because $L = 1$ is in it, not because intermediate scales add lift. The case for intermediate $L$ rests on the width sweep of , where Spectral retains perfect recall down to $w = 3$ while the baseline lags; a clean per-scale decomposition for $w > 1$ is left to future work. See for a real-encoder evaluation on a public benchmark. To check whether the synthetic-benchmark picture survives a real encoder, contextualised embeddings, and a public benchmark corpus, we evaluate Spectral Retrieval on --- the small split of a benchmark constructed explicitly to stress the theoretical limits of embedding-based retrieval. LIMIT-small consists of short profile-like documents: each document names an entity and lists many associated attributes, while each query asks for one attribute and has exactly two relevant documents. The task therefore probes whether a retriever can isolate a small matching fact inside a compact list, rather than rely only on a document-level average. We deliberately pick an off-the-shelf encoder trained for single-vector mean-pool retrieval and apply no task-specific tuning, so the comparison isolates the contribution of the aggregation rule. The corpus comprises $M = 46$ documents and $Q = 1{,}000$ queries, with exactly two relevant items per query. Both baseline and spectral retriever sit on top of frozen transformers/all-mpnet-base-v2} embeddings . The baseline returns the top-$k$ by cosine similarity against the mean-pooled document vector. The spectral retriever consumes the same per-token embeddings and applies the multi-scale sinc convolution of with the default grid $ = \{1, 3, 5, 7, 10, 15, 20, 30\}$, taking the max over positions and scales. Since the corpus is tiny, we set $K = M$ ; this isolates the aggregation rule from any first-stage recall ceiling. Both methods run on CPU at fp32. We report Recall@$k$ at $k \{1, 2, 5, 10, 20\}$ , the strict Success@$k$ at $k \{2, 5, 10\}$ , and the standard MRR, MAP, and NDCG@10. {1.15} {6pt} {lrrr} metric & MeanCos baseline & Spectral & $ $ \\ Recall@1 & 0.044 & & $+$0.313 \\ Recall@2 & 0.078 & & $+$0.573 \\ Recall@5 & 0.181 & & $+$0.636 \\ Recall@10 & 0.329 & & $+$0.570 \\ Recall@20 & 0.565 & & $+$0.388 \\ Success@2 & 0.005 & & $+$0.501 \\ Success@5 & 0.036 & & $+$0.695 \\ Success@10 & 0.124 & & $+$0.712 \\ MRR & 0.219 & & $+$0.575 \\ MAP & 0.166 & & $+$0.568 \\ NDCG@10 & 0.194 & & $+$0.595 \\ evaluation: frozen encoder, $M = 46$ documents, $Q = 1{,}000$ queries, two relevant items per query, $K = M$. Spectral Retrieval re-ranks the same per-token embeddings the baseline averages over.} Two features of Table~ are worth flagging. First, the regime LIMIT was built to expose --- queries demanding multi-attribute matches over short documents, where any single mean-pooled document vector cannot disentangle the two relevant items from each other --- is exactly the regime spectral retrieval was constructed for. The strict Success@2 metric is the cleanest demonstration: the baseline finds both relevant items in the top-2 on only $5$ queries out of $1{,}000$ --- barely above the $ 0.1\ LIMIT-small is small. A 46-document corpus admits a re-rank over the entire corpus , so the candidate-pool ceiling discussed in is structurally absent; production deployments at $K 100$ on a corpus of millions of documents face a recall ceiling that this experiment cannot probe. The companion LIMIT-full split, with two orders of magnitude more documents, is the right next stress test . Latency is also a real concern: under the present unoptimised single-CPU configuration, spectral re-rank costs $ 1.84$ s per query against the baseline's $ 50$ ms, a $37 $ overhead. This is consistent with the operation count of at $S = 8$, $N = 384$, $d = 768$ --- but it is well above the interactive latency budget of . The bridge between the two figures is a GPU implementation with batched scale convolutions and the standard two-stage pipeline at $K 100$ instead of $K = M$; both are routine engineering that this paper does not include. We collect the limitations we are aware of below. Spectral Retrieval requires per-token document embeddings. Uncompressed, this is an $N$-fold blow-up over a single-vector index ; centroid-plus-residual quantisation as in ColBERTv2 brings the effective blow-up down to roughly $20 $, and the same compression carries over here. On very large corpora, index size becomes the dominant operational cost, not per-query compute. Being a re-ranker, Spectral Retrieval cannot recover documents missing from the size-$K$ candidate pool. The synthetic experiment uses $K = M$, which trivially satisfies the candidate-pool requirement; production usually runs $K$ around 100. lists this as the most important next experiment. A multi-scale aggregation with max picks up high-similarity feature — spurious ones included . The same risk affects ColBERT's MaxSim. Mitigations include a top-$m$ mean in place of max, or a percentile cap on per-scale similarity. Recent theoretical work establishes that any retriever faces a hard ceiling, set by the embedding dimension $d$, on the number of distinct top-$k$ relevance patterns it can represent over a fixed corpus. Spectral Retrieval is a multi-vector method: each document is encoded as $N$ token vectors and scored by a sinc-convolution-and-max aggregator, so the single-vector ceiling does not apply directly --- exactly as for ColBERT-style MaxSim, which also escapes that bound by replacing the document mean with a per-position pool. What Spectral inherits from the underlying encoder is the geometry of the per-token embedding space; corpora that are pathological for the encoder in the sense of will remain hard. Our method changes the , not the . reports a full LIMIT-small evaluation. The larger public benchmarks --- BEIR, MS MARCO, HotpotQA --- are not run in the present paper: a credible BEIR sweep requires GPU-days that this submission's page and compute budget did not accommodate. discusses the next-experiment plan. We pick max over scales and max over positions. Average and softmax variants are reasonable alternatives. 's synthetic ablation studies only max; a fuller ablation over aggregators belongs in a follow-up. sketches how to combine Spectral Retrieval with ColBERT's multi-vector query, but the combination is neither implemented nor evaluated. For an interactive RAG pipeline a defensible production target is roughly $200$ ms of overall retrieval latency. Below we compare three different re-rankers against an identical first-stage ANN: {1.15} {6pt} {llll} component & per-cand. ms & $K = 100$ ms & notes \\ First-stage ANN & $ 0.3$ & $ 30$ & independent of re-ranker \\ Spectral & $ 0.5$ & $ 50$ & direct convolution, fp16 \\ ColBERT MaxSim & $ 0.3$ & $ 30$ & per-query-token max-sum \\ Cross-encoder & $ 8.0$ & $ 800$ & full BERT pair encoding \\ The per-candidate millisecond figures are order-of-magnitude estimates derived from the operation counts in and from published throughput numbers for ColBERTv2 and BERT-pair cross-encoders on a single A100; we have not run a head-to-head benchmark in this paper, and the next-experiment plan in calls one out explicitly. The relative ordering — Spectral comparable to ColBERT MaxSim, roughly an order of magnitude cheaper than a cross-encoder — is what matters for the pipeline argument below. 's combined Spectral $ $ cross-encoder pipeline only runs the cross-encoder on the surviving top-$k' = 10$ candidates, which is what brings the combined cost back inside an interactive latency envelope. Below are four failure modes we have informally observed and that production deployments would do well to monitor. One token whose random cosine to the query happens to be high can drag a non-relevant document into the top-$k$. The risk matches what ColBERT MaxSim carries, and the mitigation matches: a top-$m$ mean rather than strict max, or alternatively a percentile cap. If a document repeats the same body many times — a templated form being one example — the convolved signal is identical across scales, and the spectral score collapses onto the mean-pool score. Neither gain nor loss. On a one-word query, the encoder produces an embedding roughly equal to that word's embedding in an average context; spectral retrieval will then match document holding a token close to that word. Frequently this is the desired outcome, but very short queries push the per-token noise floor higher, so spurious matches arise more often than at longer queries. Encoders that propagate information aggressively across positions — long-context decoder-only models, for instance — leak document-level summary into every token embedding. As a consequence, the per-token cosine against the query becomes less localised than the document's underlying topic structure would suggest. Spectral retrieval continues to work, although the gain over mean pooling narrows. Empirically the encoder-only family yields larger spectral gains than instruction-tuned decoder-only embedders. Two operational parameters jointly govern quality and cost: candidate-pool size $K$ and scale grid $$. We summarise deployment guidance briefly below. The non-negotiable invariant is that the relevant document must lie inside the candidate pool — by construction, a re-ranker cannot recover anything missing from the pool. If the first-stage ANN reaches roughly $95\ A workable grid covers three things: $L = 1$ ; the corpus's typical width of localised relevance; either an explicit mean-pool endpoint or a scale large enough to approximate it for the document lengths being re-ranked. For technical-documentation corpora where relevance concentrates inside 5--20-token paragraphs, the default grid $\{1, 3, 5, 7, 10, 15, 20, 30\}$ is reasonable. For corpora made up of short documents the grid can shrink to $\{1, 3, 5\}$. For corpora dominated by very long documents it pays to include $L = 100$ or the explicit mean-pool endpoint. This paper does not include a systematic ablation across grids. Yet 's synthetic results indicate that recall is largely insensitive to grid choice provided $L = 1$ is in the grid and the grid spans an order of magnitude. By far the most informative follow-up is a public-benchmark study. We list a realistic plan in increasing order of compute: — the full split of the LIMIT theoretical-limits benchmark , where the corpus grows by two orders of magnitude over LIMIT-small and Weller et al.\ report Recall@100 below $20\ : a suite of 18 zero-shot retrieval benchmarks. End-to-end evaluation of the whole suite costs a few GPU-days; the headline metric --- NDCG@10 averaged across the 18 tasks --- is the 2024--2025 standard for cross-retriever comparison. : the canonical supervised benchmark for dense retrieval. : multi-hop QA. Spectral Retrieval is expected to outperform multi-hop baselines that explicitly model document-level dependencies; HotpotQA functions as a stress test. A second direction worth pursuing is 's setup — multi-vector query joined with multi-scale document. On long-document, narrow-relevance corpora we expect the combination to outperform either component in isolation, although we have not measured it. The synthetic experiment runs in under three minutes on a single CPU core . The script fixes a random seed and emits 's reported numbers verbatim. The LIMIT-small evaluation of runs end-to-end on a single CPU core in roughly $32$ minutes on top of a frozen transformers/all-mpnet-base-v2} encoder; the dataset is downloaded automatically from the public release. calls out three further public benchmarks --- BEIR, MS MARCO, HotpotQA --- and the LIMIT-full split as the obvious next experiments. Three subtle but important caveats close the paper. With the endpoint grid, the pointwise inequality Spectral$ \{$MaxSim, MeanCos$\}$ holds, but a corpus-level is assembled out of pointwise scores. Two documents whose spectral scores both sit at $0.9$ yet whose mean-pool scores split $0.1$ and $0.5$ will be ordered differently under spectral retrieval than under mean-pool retrieval, despite the two methods agreeing on the spectral score. The guarantee only asserts that under spectral the document's score is no smaller than under either endpoint; it does not guarantee that the produced by spectral retrieval dominates either endpoint's ranking. 's synthetic results show the ranking effect is typically positive — but one can construct a corpus-specific pathology where the spectral max binds to a non-relevant outlier across many distractors. Our default grid is $\{1, 3, 5, 7, 10, 15, 20, 30\}$. A geometric grid chosen more carefully — for instance, $L = 1, 2, 4, 8, 16, 32$ — might attain comparable recall using fewer scales, with lower per-query cost. We have not optimised these parameters. As noted in , Spectral Retrieval corresponds to the single-query-vector analogue of ColBERT. Under a decoder-only encoder that already retains several query tokens , 's $ $ can be swapped for a sum over those query tokens, restoring ColBERT's full multi-vector query while keeping the multi-scale document side. The combined method strictly costs more than either piece alone , but it is strictly more expressive too. The code's deployments forgo the multi-vector query because the use case is per-agent retrieval, in which each agent issues one role-specific query; the option still stands. In short, Spectral Retrieval is a compact plug-in re-ranking rule that interpolates smoothly between per-token MaxSim and ordinary mean-pool retrieval through a multi-scale sinc convolution. A clean endpoint recovery guarantee accompanies the interpolation: when the identity and mean-pool endpoints are included, the spectral score never falls below either endpoint score. Our controlled synthetic study reproduces the predicted phase transition near the corpus-level token noise floor — mean pooling sits at chance, whereas spectral retrieval climbs to perfect recall the moment the planted-spike cosine moves above the top-$k$ distractor tail. On LIMIT-small with a frozen encoder, the same aggregation change raises Recall@10 from $0.33$ to $0.90$ and MRR from $0.22$ to $0.79$, while moving strict Success@10 from $0.12$ to $0.84$. As a building block, the method is a natural fit for multi-agent LLM systems, since every agent benefits from a tighter, role-specific retrieval window before the orchestrator handles debate. Logical next steps include scaling the public evaluation to LIMIT-full and larger benchmarks, experiments with broader spikes, and pairing with multi-vector queries. 