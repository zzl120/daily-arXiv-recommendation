 {IEEEtran} {subfig} {tcolorbox} , group-minimum-digits = 4, table-number-alignment = right } {RGB}{102,153,255} {RGB}{204,153,0} {RGB}{0,110,60} {} { 0} { 0} {\,{ {$ $\,#1}}} {\,{ {$ $\,#1}}} {\,{ {$ $\,#1}}} $, Chongming~Gao$^{*, }$, Chenxiao~Fan$^{*}$, Haoyan~Liu$^{*}$, Qingpeng~Cai$^{ }$, Peng~Jiang$^{ }$, and Xiangnan~He$^{*, }$\\ $^{*}$University of Science and Technology of China, Hefei, China\\ $^{ }$Zhongguancun Academy, Beijing, China\\ $^{ }$Independent Researcher\\ $^{ }$Corresponding author\\ Email: cjj01@mail.ustc.edu.cn, chongming.gao@gmail.com, simonfan@mail.com, liuhaoyan@ustc.edu.cn,\\ cqpcurry@gmail.com, jp2006@139.com, xiangnanhe@gmail.com } {Chen }: Position-Aware Drafting for Inference Acceleration in LLM-Based Generative Recommendation} Large language model –based generative list-wise recommendation has advanced rapidly, but decoding remains sequential and thus latency-prone. To accelerate inference without changing the target distribution, uses a small to propose several next tokens at once and a target LLM to verify and accept the longest prefix, skipping multiple steps per round. In generative recommendation, however, each item is represented by multiple semantic-ID tokens , and current drafts typically treat these tokens uniformly. This overlooks two practical facts: a token’s semantics depend on its , and uncertainty tends to increase with . Without modeling these effects, SD’s speedups can be limited. We introduce PAD-Rec , a lightweight module that augments the draft model with two complementary signals. explicitly encode the within-item slot of each token, strengthening structural awareness. encode the draft step, allowing the model to adapt to depth-dependent uncertainty and improve proposal quality. To harmonize these signals with base features, we add simple gates—a learnable coefficient for item slots and a context-driven gate for draft steps. The module is trainable, easy to integrate with standard draft models, and adds negligible inference overhead. Extensive experiments on four real-world datasets show up to wall-clock speedup and about With the rapid rise of large language models , their application in generative list-wise recommendation systems has gained significant momentum, particularly in areas like end-to-end recommendation and conversational recommendation . These systems generate personalized item lists by constructing user profiles and iteratively invoking LLMs in an auto-regressive manner. However, the auto-regressive nature of LLMs results in relatively slow generation speeds, leading to high latency and hindering their application in real-time recommendation scenarios~. Therefore, accelerating inference in LLM-based generative recommendation systems has become an urgent issue to address . {0cm} {-0.3cm} {figures/hass.pdf} A prominent line of work for {lossless} acceleration of autoregressive LLMs is , which follows a scheme . A small, compatible model proposes several future tokens at once , and the LLM verifies these candidates in a single batched call. Verification accepts the longest prefix that matches the target distribution; decoding then resumes from that prefix. By committing multiple tokens per target call, SD reduces the number of expensive target invocations while preserving the exact decoding distribution . Building on this paradigm, later studies focus on raising the {acceptance rate}, which directly improves end-to-end speed. EAGLE enriches the draft input with token embeddings and , leading to more faithful proposals and higher acceptance; however, its training is , whereas inference drafts {multiple} steps, creating a train–decode mismatch. HASS~, as illustrated in Fig.~\, , addresses this gap by training the draft to , so the training context mirrors the multi-step drafting used at inference. The common goal is clear: keep the draft fast and well-aligned with the target LLM, achieving larger speedups with SD. Yet, even with stronger inputs and multi-step training, directly applying SD to exposes two task-specific gaps that prior SD works---including HASS---do not explicitly resolve. As highlighted in Fig.~\, , recommendation outputs are {item-structured}: each item is emitted as a fixed tuple of $K$ semantic-ID tokens ~, corresponding to different codebook levels in RQ-VAE-style representations~. We refer to these fixed positions as {within-item slots}, indexed by $1{:}K$ in the semantic-ID tuple. Meanwhile, the {autoregressive} nature of decoding---together with SD’s “stop at first mismatch” verification---tends to make later draft steps harder to be accepted. Concretely, two challenges arise for the {draft model}: Because each item occupies fixed slots, a token’s semantics depend on its {within-item slot}; however, existing draft models treat tokens uniformly and do not encode slot identity, weakening intra-item modeling and alignment with the target head. Independently of the fixed layout, uncertainty often {increases with the draft depth}: later tokens depend on longer histories and are more likely to trigger early rejection under SD’s verification. Standard training remains largely step-agnostic, so the draft model does not adapt its proposal strategy or confidence by depth, leading to miscalibration and lower acceptance at deeper positions. To close these gaps, we introduce PAD-Rec—a position-aware acceleration module that plugs into standard draft models and directly targets the two gaps above. The draft model is equipped with two complementary positional signals: that encode a token’s slot within the item to strengthen item-structure awareness, and that index the current draft step so the draft can learn depth-conditioned behavior from data and adapt to step-wise uncertainty. To balance these signals with base features, we use simple gates: a learnable coefficient for IPE and a small, context-driven gate for SPE. The module is lightweight, trained jointly with the draft, easy to integrate, and adds negligible inference overhead—yielding higher verification acceptance and larger end-to-end speedups. Extensive experiments on four real-world datasets demonstrate that our method achieves up to speedup and an average acceleration for LLM-based generative recommendation tasks while largely preserving recommendation performance. Speculative decoding accelerates LLMs via a loop: a small draft model proposes several future tokens, and the target LLM verifies them in one batched call, committing the longest accepted prefix~. Early SD under decoding drafts a short block per round, preserving the target distribution but limiting acceptance and speedup~. Later works adopt nucleus sampling~ and generalize to proposals that branch and verify in parallel, improving acceptance length at the cost of extra memory when trees widen or deepen~. By drafting strategies, SD methods can be grouped into three broad categories. SD retrieves candidate continuations from the input prompt itself via suffix matching and submits them for batched verification, avoiding a separately trained draft model~. SD augments the prompt with contexts retrieved from external stores or prior outputs, leveraging cached knowledge to guide drafting~. SD attaches efficient predictors to generate candidates, including token-head~, RNN-based models~, or smaller models~ for parallel or regressive proposals. Recently, the third group has given rise to SD. EAGLE~ is a representative approach that autoregresses over hidden-state sequences to fuse candidate token embeddings. This line has inspired follow-ups such as HASS~, GRIFFIN~, FSPAD~, and CORAL~, which refine draft-model training and proposal construction. Other studies examine broader efficiency questions, including the trade-off between draft-model size, draft length, and latency~, context-dependent uncertainty in SD~, and systems optimizations such as KV-cache reduction~. However, these methods are mainly designed for generic language generation and do not explicitly model the structured semantic-ID outputs in generative recommendation. This motivates studying structure-aware drafting for list-wise recommendation generation. Motivated by the rise of generative modeling, recommendation has been reframed from to ~. A widely used blueprint comprises two stages: a stage that maps items to discrete symbols, and a stage that predicts future items conditioned on user history. As a representative, TIGER~ encodes each item as a short tuple of semantic-ID tokens, typically learned via RQ-VAE~, and uses an encoder--decoder to generate recommendation lists. On the tokenization side, recent work trains more expressive and well-aligned codebooks~; for instance, LETTER integrates both collaborative and contextual priors to guide semantic IDs~. On the generation side, methods enhance how user profiles and item signals are fused for list-wise decoding~, exemplified by EAGER's two-stream fusion of semantic and collaborative cues~ and LC-Rec's instruction-driven generation over semantic IDs~. Recent studies further apply planning, reasoning, reinforcement learning, and alignment techniques to generative recommendation~. Personalization is also emerging as a clear trend in generative recommendation and related LLM-based user modeling settings~. Some systems optimize both stages jointly~, such as SEATER, which learns a hierarchical tree-structured tokenizer together with an autoregressive encoder--decoder, and IDGenRec, which learns concise textual IDs within the LLM vocabulary for generation. Despite these advances, most methods primarily improve modeling, alignment, or personalization, while the latency of autoregressively generating semantic-ID lists remains underexplored. LLM-based generative recommenders~ are increasingly competitive with strong traditional systems~, but real-time deployment remains constrained by the latency of autoregressive decoding over multi-token item representations. Beyond compression-based acceleration such as distillation, pruning, and quantization~, recent work has begun to improve efficiency more directly in recommendation scenarios. DARE~ accelerates LLM-generated user or item knowledge for downstream ranking rather than directly generating recommendation lists. AtSpeed~ studies SD for top-$K$ item generation, where beam search produces multiple candidate semantic-ID sequences and strict $N{ }K$ verification is required. SpecGR~ formulates speculation as retrieval-based candidate proposal and verification for inductive recommendation. NEZHA~ uses self-drafting and efficient verification for industrial candidate generation. EARN~ reduces inference overhead from a systems/cache perspective under item-identifier generation settings. These studies broaden the design space of efficient generative recommendation, but they mainly target ranking, top-$K$/candidate-generation-oriented settings, retrieval speculation, or cache management rather than full list-wise decoding. In such settings, the generated ``list'' is typically a set of alternative candidate items produced from the same user-history prefix, whereas our setting generates an ordered multi-item response in which previously generated items become part of the decoding context. This full-list setting introduces additional structure: tokens have fixed within-item roles, and later draft steps must account for both deeper speculation and previously generated list tokens. This motivates our focus on structure-aware lossless speculative decoding for full recommendation-list generation. We focus on the setup for LLM-based generative recommendation, where a user's interaction history is split into a and a . The history, denoted as $_{1:n}= $, consists of items $a_i$ that the user has interacted with, and the future, denoted as $_{1:m}= $, is the target list of items to generate. In addition to item history, the LLM input includes an describing the task~ . Following prior work~, we encode items with an RQ-VAE~ so they can be handled by a token-based generator. Concretely, each is mapped to a short tuple of . With $K$ codebooks , item $a_i$ is represented as $ }, ,a_i^{ })$ and item $b_j$ as $ }, ,b_j^{ })$, where the superscript indexes the codebook level. To separate adjacent items, we insert special . Let $X= $ be the token stream that concatenates the instruction and context tokens, the history items encoded as their $K$ semantic-ID tokens with separators, and the future items in the same format. Let $t_0$ be the index of the first token of the future segment. Generation proceeds autoregressively over $X$: p_ = _{t=t_0}^{T} p_ , where each $x_t$ is a semantic-ID token or separator token, and $X_{1:t_0-1}$ denotes the instruction and history prefix. Autoregressive decoding requires one forward pass per token and is strictly sequential. For a list of length $m$ with $K$ semantic-ID tokens per item and $K'$ separator tokens per item on average, the response segment contains approximately $m\, $ tokens , leading to $m\, $ decoding steps. This limits parallelism and increases latency as models scale, motivating inference acceleration tailored to generative recommendation. To reduce autoregressive latency, employs a small model to propose multiple future tokens in parallel as a —or a in tree-based variants—after which the target LLM scores these proposals and accepts a target-verified prefix, allowing decoding to advance by multiple tokens in one round. Subsequent work refines this pipeline . strengthens the draft by augmenting it with so proposals better match the target head and thus improve acceptance, but its training is while inference drafts steps—creating a train–decode mismatch. addresses this by training the draft model to roll forward multiple steps so the training context mirrors multi-step drafting at test time. For clarity going forward, we denote by \ the , i.e., the number of draft steps proposed per round; we unroll up to \ steps during training and expand candidates up to depth \ at inference, and—unless otherwise noted—use the same depth for both ). While these advances raise acceptance in general language tasks, directly applying SD to {LLM-based generative recommendation} exposes two task-specific gaps. First, : each item is realized as a fixed tuple of semantic-ID tokens , and a token’s role depends on its {within-item slot}, yet standard draft models treat tokens uniformly without encoding slot identity, weakening intra-item modeling and alignment. Second, : later draft steps—both across items and within an item’s token tuple—are typically harder, but the draft model’s behavior is largely step-agnostic, missing the opportunity to adapt proposal confidence as depth grows. These observations motivate augmenting the draft model with lightweight position signals that encode the within-item slot and the draft step. We introduce such a position-aware drafting module in the next section. In this section, we address the two challenges identified for SD in LLM-based generative recommendation—underused item-structural cues and unmanaged depth-driven uncertainty. We propose a that injects two complementary signals into the draft model: to encode the within-item slot, and to encode the generation depth. Fig.~ illustrates the integration of the two embeddings in PAD-Rec. To balance these signals with the base representation, we further introduce : a learnable gate for item-position cues and an uncertainty-aware gate for step-position cues. We first detail each component and its integration with SD, and then outline the overall training/inference pipeline. {2pt} {0pt} {figures/framework.pdf} {Blue} blocks denote token embeddings, and {orange} blocks denote intermediate features. Tokens already verified by the target LLM are outlined in {black}, while draft predictions are outlined in {red}. The draft input is augmented by adding IPE $_t$, concatenating features, and adding SPE $_j$ before the auto-regressive draft layer. Different IPE colors indicate different within-item positions, while darker SPE colors indicate larger draft-step indices \ .} {2pt} {-2pt} {figures/attn.pdf} Each block combines the base feature with an IPE marker for the within-item slot and an SPE marker for the draft step. During unrolled training, target features are progressively replaced by draft features from earlier depths under a causal mask, while red dashed boxes group tokens belonging to the same item.} As outlined in Section~, each item is emitted as a fixed tuple of $K$ semantic-ID tokens, typically $K{=}4$, corresponding to different codebook levels in RQ-VAE~. We index the as $1{:}K$, while separator tokens and instruction/context tokens are treated as special markers. A typical instruction–response pair looks like: [ colback=white, colframe=black, arc=2mm, boxrule=0.5pt, left=6pt,right=6pt,top=6pt,bottom=6pt, breakable, before upper= ] \\ After interacting with items , what are the next 10 items that could be recommended for the user? \\ To make the slot identity explicit to the draft model, we introduce . Each token in the flattened decoding stream is associated with a slot or marker label $ _t \{1, ,K,,\}$, determined by a simple rule based on the known item structure, where uniformly denotes both commas and spaces and denotes instruction/context. The corresponding IPE is obtained by a lightweight embedding lookup: _{t} = ^{} , where $^{} $ is a small embedding table shared across all positions. By injecting IPE into the draft model, we provide explicit slot-aware cues that strengthen intra-item structural modeling, while adding negligible computational overhead. Predictions made at later draft steps are typically less certain due to longer dependency chains and error accumulation. To enable depth-aware adaptation, we introduce , which explicitly encode the current draft step. Here, $j$ denotes the draft depth associated with the current token position during speculative decoding. At draft step $j$, the draft model retrieves the corresponding SPE via an embedding lookup: _{j} = ^{} , where $^{} $ is a small embedding table indexed by the draft step. In our implementation, draft depth indices start from $j{=}1$, and the SPE table is indexed accordingly. SPE is applied consistently during both training and inference, providing explicit conditioning on draft depth without introducing additional computation or model complexity. We fuse the two position signals--- and ---into the draft model with lightweight gates, so that positional cues strengthen structure/depth awareness without overwhelming the base representations. Let $_{t} ^{d}$ be the token embedding at position $t$, $_{t-1} ^{d}$ the draft feature from the previous step, $_{t} ^{d}$ the IPE of token $t$, and $_{j} ^{d}$ the step position embedding corresponding to the at position $t$. We first inject IPE into the token embedding and concatenate it with the previous-step draft feature: '_{t-1} = , where $g_{} $ is a that controls the overall strength of slot cues. The concatenation follows the feature-level drafting paradigm of EAGLE~, which explicitly conditions the current prediction on both token-level embeddings and previously drafted features, thereby reducing feature uncertainty during speculative decoding. We then project the concatenated feature back to the draft-feature space and add SPE with a gate: _{t-1} = _{} , _{t-1} ^{d}, _{t} = _{t-1} + g_{} _{j}. Here, $_{} $ maps the concatenated $2d$-dimensional feature to the $d$-dimensional draft-feature space, so $_{t-1}$ and $_{j}$ have matched dimensions. Unlike $g_{}$, which is global and static, the step gate adapts to the current draft context: g_{} = \! , where $ $ is the sigmoid function and $$ is a learned vector. This design allows the model to increase the influence of depth cues when drafting becomes more uncertain, while down-weighting SPE when the draft features are confident. PAD-Rec adopts a HASS-style distillation regime with multi-step rollout. We train the draft model while keeping the target LLM frozen. The trainable parameters include the draft layer, the IPE/SPE embedding tables, and the gating parameters. Let $_{1:T}$ be the token stream and $t_0$ the index of the first response token. We define the $B$ as the maximum number of future tokens that the draft model is allowed to propose in a single speculative round. For each draft depth $j \{1, ,B\}$, we distill the draft distribution to match the target distribution on response positions $t $: _{} &= _{j=1}^{B} \{ _{t=t_0}^{T} ( P^{ } , \\ & P^{ }_{ ,\,j} ) + \}. $$ is the Top-$K$ distillation loss of HASS, which we adopt unchanged. $P^{ }$ denotes the target LLM distribution, and $P^{ }_{ ,\,j}$ denotes the draft distribution conditioned on the $j$ with PAD-Rec’s position-aware inputs: P^{ }_{ ,\,j} =\; ( _{ } ( ) ), where $_{ }$ is the trainable draft backbone , and $$ is the frozen LM head copied from the target LLM. $ $ implements the gated position-aware fusion defined in Eqs.~ -- . During training we use the HASS causal mask, adjusted so that each replaced position only attends to states available at its own timestep . Fig.~ illustrates the unrolled rollout. At draft depth $j{=}1$, the context uses target features for all past positions . At draft depth $j{ }2$, the most recent $ $ positions in the attention context are progressively by draft features produced at earlier depths, while earlier positions remain teacher features. At each depth $j$, we inject the corresponding SPE $_j$ for the currently replaced window and queries, while providing per-token IPE to all positions. At decoding time, the draft model augments inputs with the corresponding IPE and the SPE indexed by the . It then proposes a candidate tree whose maximum depth is bounded by the fixed $B$ . The target LLM verifies all candidates in one batch and commits the longest accepted prefix, advancing the output by multiple tokens per verification round. PAD-Rec adds minimal overhead to the draft: two embedding lookups and lightweight scalar gates per token. The additional parameters introduced by PAD-Rec are negligible compared with the draft backbone (e.g., about $0.01\ We comprehensively evaluate on four real-world datasets, comparing its inference acceleration against strong speculative-decoding baselines. Beyond the main comparison, we conduct ablations to disentangle the contribution of our and the , and we analyze key hyperparameters that influence speedup. To assess PAD-Rec for LLM-based generative list-wise recommendation, we answer the following research questions: How does PAD-Rec compare with state-of-the-art SD methods in terms of wall-clock speedup, accepted length, and recommendation recall? What are the individual and joint effects of item position embeddings, step position embeddings, and gating on PAD-Rec's inference acceleration? How do hyper-parameters like speculation depth affect PAD-Rec’s efficiency? How well does PAD-Rec scale across different backbone model sizes? We evaluate PAD-Rec on four real-world datasets drawn from two widely used recommendation benchmarks. The dataset statistics are summarized in Table~. , , and are three subsets of the Amazon Reviews corpus~, covering user interactions with beauty and personal care products, musical instruments and related accessories, and video games, respectively. is derived from the Yelp Open Dataset}, which records user reviews and interactions with local businesses such as restaurants and services. Following , we first obtain item using a pretrained encoder that integrates semantic and collaborative signals, and then discretize items into semantic IDs as described in Sec.~. User interactions are sorted chronologically, and users with fewer than 11 interactions are removed. For each remaining user, the most recent $10$ items are used as the , while all earlier interactions constitute the . We split users into training, validation, and test sets with an 8:1:1 ratio at the user level. This ensures that all interactions of a given user are assigned to the same split, thereby preventing information leakage across data partitions. We first train the target LLM-based list-wise recommender following LC-Rec . Its hidden features on the instruction-augmented token stream are then used to supervise the draft model. {2pt} {1.1} {l S S S S } & & & & \\ Beauty & 12101 & 198504 & 22363 & 6562 \\ Instruments & 9922 & 206153 & 24772 & 5753 \\ Games & 17332 & 342329 & 49156 & 7721 \\ Yelp & 20033 & 316354 & 30431 & 13516 \\ “\#Items”, “\#Inter.”, and “\#Seqs” are counted after preprocessing; “\#SD Seqs” denotes sequences used for SD training/evaluation. We compare our proposed PAD-Rec against several strong SD baselines. } It uses a with feature-level autoregression to raise verification acceptance and improve end-to-end speed. } It uses to match decoding-time context and improve acceptance. } It uses and to reduce feature uncertainty and stabilize alignment, improving candidate quality and speed. } It uses with to mitigate train–decode misalignment and improve acceptance length. All baselines share the same target LLM and evaluation protocol; we adopt their official configurations for a fair comparison. {3pt} {1.15} { }{@{} l p{3.2cm} c c c c c c c c} & & {c}{} & {c}{} \\ {3-6} {7-10} & & & & & & & & & \\ {*}{} & & -- & -- & 0.0486 & 0.0593 & -- & -- & 0.0569 & 0.0629 \\ {2-10} & & 3.00 & 6.83 & 0.0480 & 0.0588 & & 5.65 & 0.0531 & 0.0582 \\ & & & 6.87 & 0.0483 & 0.0591 & 2.30 & 5.59 & 0.0563 & 0.0628 \\ & & 2.83 & 7.21 & 0.0485 & 0.0592 & 2.31 & 5.96 & 0.0541 & 0.0603 \\ & & 2.82 & & 0.0480 & 0.0588 & 2.27 & & 0.0523 & 0.0593 \\ & & & & 0.0486 & 0.0593 & & & 0.0546 & 0.0619 \\ {*}{} & & -- & -- & 0.0337 & 0.0462 & -- & -- & 0.0323 & 0.0382 \\ {2-10} & & 2.39 & 4.55 & 0.0337 & 0.0462 & 1.71 & 3.88 & 0.0336 & 0.0411 \\ & & 2.76 & 5.75 & 0.0339 & 0.0463 & 2.24 & 5.01 & 0.0298 & 0.0383 \\ & & 3.01 & 7.13 & 0.0339 & 0.0463 & 2.35 & 6.22 & 0.0322 & 0.0390 \\ & & & & 0.0351 & 0.0476 & & & 0.0341 & 0.0400 \\ & & & & 0.0337 & 0.0462 & & & 0.0291 & 0.0376 \\ {*}{} & & -- & -- & 0.0182 & 0.0221 & -- & -- & 0.0206 & 0.0220 \\ {2-10} & & 2.82 & 7.00 & 0.0182 & 0.0220 & 2.17 & 5.66 & 0.0188 & 0.0202 \\ & & & 7.15 & 0.0184 & 0.0222 & 2.21 & 5.93 & 0.0213 & 0.0242 \\ & & & & 0.0182 & 0.0220 & & & 0.0197 & 0.0229 \\ & & 2.72 & & 0.0180 & 0.0217 & 2.18 & & 0.0201 & 0.0217 \\ & & & & 0.0182 & 0.0221 & & 6.06 & 0.0201 & 0.0221 \\ {*}{} & & -- & -- & 0.0055 & 0.0075 & -- & -- & 0.0085 & 0.0088 \\ {2-10} & & 2.10 & 5.58 & 0.0053 & 0.0074 & 1.68 & 4.80 & 0.0076 & 0.0079 \\ & & 2.29 & 5.84 & 0.0053 & 0.0074 & 1.82 & 4.47 & 0.0076 & 0.0073 \\ & & 2.31 & & 0.0053 & 0.0074 & 2.06 & & 0.0083 & 0.0079 \\ & & & & 0.0052 & 0.0070 & & & 0.0082 & 0.0086 \\ & & & 6.08 & 0.0054 & 0.0074 & & 4.77 & 0.0087 & 0.0090 \\ {0.98 } Results are reported on , , , and . For acceleration metrics, the best results are shown in and the second-best results are underlined. For and , colored arrows indicate deviations from the target LLM: {$ $} denotes an increase and {$ $} denotes a decrease, followed by the absolute change. Following prior SD work, we report two efficiency metrics and two recommendation-quality metrics. measures end-to-end acceleration, defined as the ratio between the inference time of standard autoregressive decoding and that of the speculative decoding pipeline under identical decoding settings. measures the average number of drafted tokens accepted per verification step. A larger $ $ implies fewer target-model invocations and thus higher potential speedup. measures recommendation accuracy, computed as $| }|/||$ for a ground-truth list $$ of size 10 and the generated list $}$, and averaged over all users. evaluates ranking quality by accounting for the positions of correctly recommended items. We compute NDCG@10 between $}$ and $$, normalize by the ideal DCG, and report the average across users. Higher NDCG@10 indicates better-ranked recommendations. For the main comparison, we implement PAD-Rec on top of LC-Rec with Llama-3.2-1B-Instruct~ as the target LLM; the model-scaling study further considers Llama-3.2-3B-Instruct and Llama-3-8B-Instruct. We adapt LC-Rec to our list-wise setting by formatting each supervision target as an ordered top-10 list of semantic-ID item tuples and training the model to autoregressively generate the flattened list token stream. We first fine-tune this list-wise LC-Rec model with full-parameter tuning to obtain the target model. The target LLM is then frozen, and only the lightweight draft module is trained. PAD-Rec keeps the target model and draft--then--verify framework unchanged, and only augments the draft-side representations with IPE/SPE and their gating parameters. For a fair comparison, all SD baselines and PAD-Rec use the same target LLM, datasets, decoding length, evaluation metrics, and EAGLE-2-style tree-based verification framework. We sweep the learning rate in \{1e{-4},\,5e{-4},\,1e{-3}\}, set the speculation depth \ to \ , and use a tree width of \ at inference time. All reported results use the best validation setting under these choices. The draft module is built from a single Transformer layer of the same backbone; PAD-Rec keeps the target model and tree-based verification unchanged and only adds IPE/SPE tables and gating parameters on the draft side. For the system protocol, speculative training uses bf16 mixed precision. Experiments with the 1B target model are trained on a single NVIDIA RTX 3090 GPU, while the 3B and 8B scaling experiments are trained on four NVIDIA A100 GPUs. All latency measurements are conducted on NVIDIA RTX 3090 GPUs in fp16 with batch size 1. We perform three warmup generations and exclude them from all reported numbers. During timing, CUDA is synchronized immediately before and after each generation call. The timed region includes draft proposal construction and target-model verification for SD methods, and the standard generation loop for the autoregressive target LLM; tokenization, post-decoding, file I/O, and metric computation are excluded. Both speculative and autoregressive decoding use the target model with KV cache enabled. We evaluate all methods on four datasets, namely , , , and , under identical inference configurations. Table~ reports wall-clock speedup, average accepted length $ $, and recommendation quality measured by Recall@10 and NDCG@10 under two sampling temperatures. At temp$=\!0$, PAD-Rec matches or nearly matches the target LLM's recommendation metrics; at temp$=\!0.5$, it largely preserves recommendation quality with limited absolute fluctuations under stochastic sampling. To contextualize the reported speedups in real time, Table~ reports the absolute autoregressive decoding latency of the target LLM in ms/query under the 1B setting. The latency is computed as \ , where \ is the number of evaluation queries and each query generates a top-10 recommendation list with approximately 59 output tokens. Since speedup in Table~ is measured relative to the same target-LLM baseline, the corresponding PAD-Rec latency can be estimated by dividing the target-LLM latency by the reported speedup. {5pt} {1.08} {l c c c} & & & \\ & 652 & 693.55 & 725.64 \\ & 588 & 676.70 & 725.43 \\ & 757 & 1054.68 & 1060.07 \\ & 1311 & 882.77 & 917.73 \\ {0.95 } Latency is reported as mean wall-clock decoding time per query for the 1B target LLM with batch size 1. Across all datasets, HASS consistently outperforms EAGLE-2 in terms of accepted length $ $ and wall-clock speedup. This trend is observed on , , and , and also holds on the smaller-scale dataset. The results indicate that exposing the draft model to multi-step speculative contexts during training better aligns it with the decoding-time distribution, resulting in fewer verification rejections and higher end-to-end efficiency, while maintaining comparable Recall@10 and NDCG@10. Both FSPAD and GRIFFIN improve draft--target alignment and yield longer accepted prefixes, with GRIFFIN typically achieving the largest $ $. However, GRIFFIN’s token-guided fusion introduces additional MLP layers during draft-time inference, increasing memory traffic and latency; as a result, gains in $ $ do not consistently translate into higher wall-clock speedup. Across datasets, Recall@10 and NDCG@10 remain comparable to the Target LLM, indicating that these methods mainly affect efficiency rather than recommendation quality. PAD-Rec achieves the highest or near-highest wall-clock speedup across all datasets and temperature settings. At temp$=\!0$, PAD-Rec reaches speedup on and on , with similarly strong acceleration on and . These gains are obtained without introducing heavy fusion modules or changing the target-model verification path. Instead, PAD-Rec relies on two lightweight positional signals. IPE provides within-item slot awareness for semantic-ID tuples, while SPE conditions the draft on the current speculative depth, allowing the model to adapt its behavior as decoding progresses. Both signals are integrated via simple gating mechanisms, achieving better end-to-end speedup with competitive accepted length and lower draft-side overhead. Sampling temperature has a systematic impact on speculative decoding. At temp$=\!0$, decoding is deterministic and closely follows the target model’s top-1 trajectory, resulting in strong proposal--target alignment; consequently, all SD variants achieve long accepted prefixes, stable Recall@10/NDCG@10, and near-maximal wall-clock speedup. When the temperature increases to temp$=\!0.5$, speedup consistently decreases and variability in Recall@10 and NDCG@10 becomes more pronounced across methods. This behavior is expected, as stochastic sampling increases the divergence between draft proposals and target continuations and shifts generation away from the regime used during draft training, leading to earlier verification termination and shorter accepted lengths $ $. Moreover, under higher stochasticity, the space of plausible continuations expands rapidly, while the finite draft tree can only cover a limited subset, further increasing the likelihood of early rejection during verification. Overall, these results highlight an inherent efficiency--robustness trade-off in speculative decoding; within this trade-off, PAD-Rec consistently maintains strong acceleration while largely preserving recommendation quality under increased sampling stochasticity. {0pt} {-2pt} {figures/ablation1.pdf} and $ $ on {Beauty} and {Instruments}. In each subplot, temp$=\!0$ is read from the left $y$-axis, while temp$=\!0.5$ is read from the right $y$-axis.} To evaluate the effects of IPE and SPE, we compare under the same SD setting with its variants and report wall-clock and accepted length $ $. Due to space constraints, we present results on two representative datasets, {Beauty} and {Instruments}, and conduct the remaining analytical experiments on the same datasets, with similar trends observed on the others. The ablations comprise , , , and . In Fig.~, the two temperatures use separate $y$-axes for readability: temp$=\!0$ corresponds to the left axis and temp$=\!0.5$ corresponds to the right axis. From Fig.~ we observe: The full PAD-Rec yields the strongest or near-strongest speedup across datasets and temperatures while keeping $ $ among the top two, indicating more tokens verified per target call without notable complexity. Removing IPE reduces speedup on structured outputs , showing that explicit slot cues help the draft model form proposals that verify more readily. Removing SPE is most harmful at temp$=\!0.5$, where deeper draft steps are noisier; step-aware conditioning improves stability at depth, preserving speedup. IPE and SPE are complementary: the full model consistently outperforms either single ablation. Lightweight slot and step signals—modulated by simple gates—improve draft–target alignment and deliver consistent acceleration with minimal overhead. {0pt} {0pt} {figures/ablation_gate.pdf} and accepted length $ $ on and . In each subplot, temp$=\!0$ is read from the left $y$-axis, while temp$=\!0.5$ is read from the right $y$-axis.} To assess the necessity of the two gates modulating positional signals, we conduct another ablation study. We compare four variants: , , , and the full model . As in Fig.~, Fig.~ uses the left $y$-axis for temp$=\!0$ and the right $y$-axis for temp$=\!0.5$. The results show: Without any gating, speedup drops sharply across all settings , accompanied by the lowest $ $. This shows that ungated addition of position signals destabilizes proposals and lowers acceptance. Disabling only the {IPE gate} remains far from the full model, close to the variant. This indicates the item gate is essential to scale slot cues properly and avoid overwhelming token semantics. Disabling only the {SPE gate} retains most of the performance , with $ $ very close to the full model. Thus, the step gate yields improvements by fine-tuning depth-wise conditioning rather than being the primary driver. {PAD-Rec } attains the highest speedup in all settings and the top $ $, confirming that gates together deliver the most reliable acceleration. The item gate serves as the primary stabilizer for slot-aware cues, while the step gate provides a lighter depth-adaptive adjustment; together they maximize acceptance and end-to-end acceleration with negligible overhead. {0pt} {-5pt} {figures/hyper_depth.pdf} }$ on and accepted length $ $ at temp$=\!0$ and $0.5$ on and .} We study the effect of inference speculation depth $B_{}$ while fixing the training speculation depth at $B_{}{=}12$ . We vary $B_{} \{1, 2, 4, 6, 8, 10, 12\}$ across both datasets and report and $ $ . The main observations are as follows: For both datasets and temperatures, $ $ increases monotonically as $B_{}$ grows. Initially, it increases linearly up to $B=6$, while speedup also rises significantly with $ $. However, after $B=6$, $ $ grows more slowly, converging to around 7, and speedup starts to drop. Speedup follows a unimodal trend: it increases with depth, peaks at moderate values , and then decreases as the depth continues to grow. This reflects the trade-off between longer accepted prefixes and the overhead and diminishing returns in acceptance when proposing too deep a draft. In our structured outputs, each item consists of a small, fixed number of semantic-ID tokens plus separators, and moderate $B_{}$ already spans roughly one item, which is generally the sweet spot for end-to-end latency. The trends above hold for both temp$=\!0$ and temp$=\!0.5$; quantitatively, temp$=\!0$ is slightly higher in speedup at its peak, yielding more stable, longer accepted prefixes at the same $B_{}$. Although we trained with $B_{}{=}12$ to cover all step indices for SPE, the optimal depth is smaller . Therefore, for deployment, we default to a moderate speculation depth of $B_{}{=}6$ to balance acceptance length and verification/branching overhead. {0pt} {-4pt} {figures/size.pdf} on and accepted length $$ at temp$=\!0$ and $0.5$ on and .} We analyze the scaling behavior of speculative decoding by varying the backbone model size and comparing PAD-Rec with HASS, while fixing the other hyper-parameters . Specifically, we evaluate three instruction-tuned backbones, , , and ~. For each backbone, we first perform recommendation fine-tuning under the same protocol, and then report inference and accepted length $ $ at temp$=\!0$ and $0.5$ on both {Beauty} and {Instruments} . The main observations are summarized as follows: Across both datasets and temperatures, speedup consistently improves as the backbone scales from 1B to 8B. This indicates that speculative decoding benefits more from larger backbones, where the relative overhead of draft generation and verification becomes smaller. Although speedup grows monotonically with model size, the improvement from 3B to 8B is smaller than that from 1B to 3B, exhibiting a clear diminishing-return effect. This suggests that scaling the backbone yields progressively smaller marginal benefits for speculative acceleration. PAD-Rec consistently achieves higher speedup than HASS across all model sizes and temperatures. This demonstrates that PAD-Rec can more effectively exploit speculative acceptance for acceleration, and its advantage remains stable as the model scales. The accepted length $ $ increases mildly with model size and remains relatively stable compared to speedup. This implies that scaling behavior is mainly driven by efficiency gains rather than substantial changes in acceptance behavior. In this work, we study speculative decoding for LLM-based generative list-wise recommendation and highlight that effective acceleration must respect both the structured nature of item representations and the multi-step decoding process. To this end, we propose PAD-Rec, a position-aware drafting module that augments the draft model with item position embeddings and step position embeddings, modulated by lightweight gates to better align draft proposals with structured recommendation outputs. Extensive experiments on four real-world datasets demonstrate that PAD-Rec consistently achieves substantial inference acceleration—up to wall-clock speedup—while incurring negligible overhead and largely preserving recommendation quality. Further analyses show that PAD-Rec is robust across sampling temperatures, hyper-parameter choices, and backbone model scales, and consistently outperforms prior speculative decoding baselines. These results indicate that incorporating structure-aware inductive biases into speculative decoding is a practical and effective approach for accelerating LLM-based generative recommendation systems. 