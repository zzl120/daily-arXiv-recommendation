 {acmart} {RGB}{30,90,160} {RGB}{105,105,105} {RGB}{255,220,180} {RGB}{180,220,255} } } } } } } Ads click-through rate prediction is constrained by sparse user supervision: most users engage with ads infrequently while generating dense behavioral evidence in organic surfaces such as feed. Transferring these cross-domain signals into ads ranking is difficult due to domain mismatch, serving cost, and production complexity. We introduce derived from organic feed activity and establish that : SIDs from user profile text yield $+0.036\ We propose , a method that pairs per-dimension scalar quantization with residual VAE quantization to discretize pre-trained embeddings while matching dense-embedding AUC. RQ-FSQ matches or slightly exceeds the AUC of the dense source embedding on two heterogeneous sources --- Feed Activity ($+0.351\ On a large-scale industrial production ads system, cold-start segment analysis shows gains reaching $+1.522\ <ccs2012> <concept> <concept_id>10002951.10003317.10003318</concept_id> <concept_desc>Information systems~Recommender systems</concept_desc> <concept_significance>500</concept_significance> </concept> <concept> <concept_id>10002951.10003317.10003331.10003271</concept_id> <concept_desc>Information systems~Online advertising</concept_desc> <concept_significance>500</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010258.10010260</concept_id> <concept_desc>Computing methodologies~Neural networks</concept_desc> <concept_significance>300</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010293.10010294</concept_id> <concept_desc>Computing methodologies~Learning latent representations</concept_desc> <concept_significance>300</concept_significance> </concept> </ccs2012> {Information systems~Recommender systems} {Information systems~Online advertising} {Computing methodologies~Neural networks} {Computing methodologies~Learning latent representations} Ads recommendation systems operate under extreme interaction sparsity. Compared with organic surfaces, where users generate frequent and diverse behavioral feedback, ad clicks are relatively rare and unevenly distributed across the user population. This creates a persistent challenge: for the large fraction of users with sparse ad interaction history, the model has little behavioral signal from which to infer intent. The problem is most acute in the regime --- new or infrequent ad engagers --- where the model must estimate user preferences from profile attributes alone. A natural source of supplemental evidence is organic feed activity. Users interact with feed content orders of magnitude more often than with ads, and those interactions encode evolving topical interests, content-type affinities, and engagement intent. However, transferring feed signals into an ads CTR model is difficult for three reasons: ~domain mismatch between feed and ads feature spaces; ~the high dimensionality and serving cost of dense behavioral embeddings; and ~integration complexity within large-scale production training pipelines that cannot accommodate bespoke preprocessing per feature source. Semantic IDs ~ have recently emerged as an effective way to discretize dense embeddings into compact token sequences via residual quantization , enabling standard embedding-table lookup and end-to-end fine-tuning under the downstream task objective. Prior SID work focuses almost exclusively on use: the embeddings being quantized originate in the same domain as the recommendation target~. In this paper we study a different setting --- --- where user representations derived from organic feed behavior are quantized and used as input features for ads CTR prediction. To our knowledge, this is the first empirical study of cross-domain viewer SIDs for industrial ads CTR modeling. We integrate cross-domain viewer SIDs into a production decoder-only Transformer ranking model for ads CTR. The key design challenge is the embedding module: how to map a $K$-level discrete token sequence into a dense representation that is trainable end-to-end. We introduce the , which encodes each SID via prefix n-gram sparse embedding tables --- prefix unigram, bigram, and trigram keys are each hashed to indices in dedicated trainable tables with bounded memory. We propose , a quantization method for pre-trained embeddings that pairs per-dimension scalar quantization with residual VAE quantization , preserving global geometry and per-dimension fine structure . RQ-FSQ matches or slightly exceeds dense-embedding AUC on two heterogeneous sources --- Feed Activity ($+0.351\ We introduce and establish the : the quality of cross-domain SID transfer scales monotonically with the amount of behavioral activity encoded in the source embedding --- profile text ($+0.036\ We introduce : a structured 9-code representation where $c_1$--$c_3$ come from Activity-Tuned LLaMA , $c_4$--$c_6$ from Profile Qwen , and $c_7$--$c_9$ from Feed Activity. Multi-Source SID achieves $+0.036\ Transformer-based~ sequential architectures are now the dominant backbone for recommendation and ads CTR. SASRec~ applies left-to-right self-attention to user interaction sequences; BERT4Rec~ extends this with bidirectional masked prediction. For ads CTR specifically, BST~ introduces behavior-sequence Transformers, while DIN~ and DIEN~ model adaptive interest activation and evolution. DCN-V2~ combines explicit feature crossing with deep networks for large-scale ranking. The production baseline used in our experiments is a decoder-only Transformer for ads CTR with context-conditioned attention, timestamp-based RoPE, session masking, and FlashAttention~. This paper treats that model as a fixed backbone and focuses entirely on improving viewer-side input representations. TIGER~ is the seminal work on SIDs for recommendation: it quantizes item content embeddings via RQ-VAE~ into hierarchical codeword tuples and trains a seq2seq Transformer to autoregressively decode the SID of the next item. extend SIDs to the YouTube ranking stage using SentencePiece tokenization over codebook sequences, demonstrating improved generalization on tail and new items in production. IDGenRec~ aligns LLMs with recommendation via jointly learned textual item IDs; LC-Rec~ integrates collaborative filtering signals via vector quantization. LETTER~ proposes learnable tokenization that adapts codebook assignment jointly with the downstream recommendation objective. deploy semantic ID prefix n-grams for item IDs in production ads ranking at Meta. introduce SIDE, which converts VQ codewords to collision-free discrete IDs via positional base-$C$ encoding, enabling parameter-efficient embedding table lookups for long sequences. A common thread in all of these works is the assumption: SIDs are derived from the same interaction space as the recommendation target. We break this assumption by constructing SIDs from cross-domain organic activity and using them for ads CTR prediction. Cross-domain recommendation has been studied through embedding-and-mapping , shared hidden layer activations , and through discrete SID tokens for item vocabulary alignment across domains . These methods typically require overlapping item or user sets between domains, or explicit domain-adaptation objectives. Our approach uses discrete SID tokens as a with no explicit alignment loss: the CTR objective drives adaptation of the cross-domain embedding tables to the target task. VQ-VAE~ introduces vector quantization with straight-through gradients for discrete latent spaces. RQ-VAE~ extends this with residual quantization, enabling hierarchical multi-level codebooks where successive residuals capture progressively finer structure --- the foundation of SID hierarchies. We use RQ-KMeans for production stability. Our HDE Module shares the goal of sparse trainable tables for discrete IDs with~, but adopts prefix n-gram lookups with bounded memory $H^{ }$, in contrast to SIDE's collision-free positional base-$C$ encoding which scales as $O $. This trade-off --- controlled collision for bounded memory --- is required for industrial-scale deployment where unbounded tables are infeasible. We further extend the design to a Multi-Source SID setting where prefix tables are applied per-source independently . The baseline system is a decoder-only Transformer~ trained for industrial ads CTR prediction. Given a user's interaction sequence $ = $ over a trailing window, the model predicts click probability autoregressively. Each event $e_t$ carries embeddings for ad creative ID, campaign ID, format and charge type, OS, objective type, and other contextual features. Context-conditioned attention with timestamp RoPE handles positional information; session masking aligns the attention pattern at training with the causal structure at serving time. Training uses DDP/FSDP2~ on H200 clusters. This paper extends the baseline with cross-domain viewer SIDs while leaving the Transformer backbone, attention stack, and prediction head : all improvements come from richer user input representations. For a dense user embedding $ ^d$, residual quantization produces a $K$-tuple of discrete tokens: = , c_k \{1, , C\}, where $C$ is the codebook size and $K$ is the number of quantization levels. We use two quantization methods, chosen based on whether the SID is trained or must : Operates deterministically over $K$ residual stages: assign $$ to its nearest centroid in a $C$-entry codebook, recording $c_1$; compute residual $_1 = - _{c_1}$; repeat on successive residuals to obtain $c_2, , c_K$. Because the uniform-variance assumption of $k$-means fits well when quantizing embeddings from scratch, RQ-KMeans achieves excellent codebook utilization and produces reproducible offline assignments. A quantization method for pre-trained embeddings that preserves information at two complementary scales: global geometry and per-dimension fine structure . FSQ~ independently quantizes each dimension of $$ to a finite integer alphabet $ = \{-L, , L\}$: }_{} = = \! L ). RQ-VAE~ quantizes successive residuals across $K$ stages, yielding $}_{} = $ where $c_i$ is the nearest codebook entry at stage $i$. The two streams are fused additively in the downstream model: ^{} = ^{}_{}} + f , where $^{}_{}}$ is the downstream embedding for the RQ codes and $f$ is a linear projection to the model dimension. We apply RQ-FSQ to the Feed Activity source . RQ-KMeans is simple, deterministic, and delivers strong AUC gains on every source we tested at several times smaller storage than RQ-FSQ; RQ-FSQ matches the dense float baseline at modestly higher storage by adding a reconstruction objective that preserves source geometry RQ-KMeans' nearest-centroid assignment can discard. Both produce the same $K$-level token sequence consumed by the HDE Module ; the choice between them is a storage--fidelity trade-off rather than a binary preference. We study three cross-domain user SID sources. All viewer SIDs are : constant across all sequence positions for a given user, keyed on user ID, and generated from data strictly available before the prediction timestamp. We use $K{=}3$ codes per source in all experiments. A combines three cross-domain user embedding sources --- forming a progression of increasing behavioral activity richness --- into a single structured 9-code representation consumed by the HDE Module without architectural changes: $c_1$--$c_3$ : embedding from a LLaMA-3.1-based model contrastively fine-tuned on cross-domain activity data with user profile as input. Encodes implicit behavioral intent even though input is profile text. $c_4$--$c_6$ : embedding from a Qwen-based language model encoding user profile text . Encodes text semantics with no direct behavioral activity signal. $c_7$--$c_9$ : embedding aggregated from feed engagement signals over a 1-year trailing window. Provides the richest direct behavioral activity signal. Activity-Tuned LLaMA plays a second, distinct role beyond contributing $c_1$--$c_3$: it serves as the for cross-source imputation when Profile Qwen or Feed Activity is unavailable for a user. We pick it for this role because ~it has the highest user coverage among the three sources --- so it is the source most often available when others are missing --- and ~it is already activity-trained, making it a reasonable estimator of the missing source's content. Imputation preserves population coverage rather than silently zeroing out users with missing sources --- an important property in production deployments where no single source achieves 100\ Concretely, let $$, $$, $$ denote the Activity-Tuned LLaMA, Profile Qwen, and Feed Activity embeddings respectively, with $ $ denoting a missing embedding. When $$ is observed, the Profile Qwen SID is $C_P = _P $; when $$ is missing it is imputed from $$ through a dedicated residual VAE quantizer: _P = & = ,\ = \\ _P\! ) & = ,\ \\ C_P & , where $g$ is a linear projection from $ $ to $ $ and $_P$ is a $K$-level residual VQ-VAE trained on users with both $$ and $$ present, with reconstruction target $$ and the standard objective: _{_P} = \| - _{l=1}^K ^{ } \|_2^2 + _{l=1}^{K} ( \| - ^{ } \|_2^2 + \, \| ^{ } - \|_2^2 ), with $^{ }$ and $^{ }$ the residual and codebook embedding at level $l$, $$ the stop-gradient operator, and $ $ the commitment loss coefficient. An independently trained $_F$ is used for the Feed Activity source when $$ is missing. Each source uses its own codebook trained via RQ-KMeans: $C_A = _A $, $C_P = _P $, $C_F = _F $. When the imputation backbone $$ is also missing , the affected source emits the padding code $c_k{=}0$ for all $K$ levels. The HDE Module maps padding codes to the zero embedding at every level , so the user's representation cleanly degrades to the contribution of the remaining sources without spurious lookups. An RQ-FSQ token sequence requires ${8} 36$ bytes for our production configuration . The reduction relative to the float32 source scales with the source dimensionality: $ 30 $ for the lower-dimensional Feed Activity source, and $ 280 $ for the higher-dimensional Activity-Tuned LLaMA source --- making RQ-FSQ particularly valuable for LLM-based encoders, with no custom preprocessing required at serving time. The HDE Module encodes any $K$-level SID into a dense user embedding using with hash-based lookups bounded by a memory cap $H^{ }$. For a $K$-level SID $ = \{0, ,C\}^K$ , each prefix $ $ is hashed to an integer index and looked up in a dedicated trainable sparse table $_k$, yielding level-$k$ embedding $_k$. This is analogous to standard categorical embedding tables in recommendation systems, with prefix $k$-grams serving as the composite feature ID. Direct embedding lookup: _1 = _1 , _1 ^{ d}, _1 = . Polynomial hashing over the prefix $ $: _k = H, _k = _k , _k ^{H d}. All hash tables are capped at the same maximum size: H = \! , where $ $ is a compression factor and $H^{ }$ caps memory. Level embeddings are summed to produce the user embedding $ = _{k=1}^{K} _k$. All tables $\{_k\}_{k=1}^{K}$ are initialized randomly and trained jointly under the CTR objective. For the Multi-Source SID , prefix n-gram tables are applied independently to each source's $K$-code block and all embeddings are summed: ^{} = _{s=1}^{S} \! } ), with $^{ }{=} $, $^{ }{=} $, $^{ }{=} $, yielding $S{ }K{=}9$ table lookups . [ font= , tok/.style={rectangle, rounded corners=2pt, draw, minimum width=1.0cm, minimum height=0.65cm, text centered, inner sep=3pt}, tbl/.style={rectangle, rounded corners=2pt, draw=accentblue, fill=accentblue!10, very thick, minimum width=1.5cm, minimum height=0.60cm, text centered, inner sep=3pt}, arr/.style={-{Stealth }, thick}, darr/.style={-{Stealth }, thick, color=mutedgray}, ] at {$c_1$}; at {$c_2$}; at {$c_3$}; at {$_1$}; at {unigram}; at {$_2$}; at {bigram $ $}; at {$_3$}; at {trigram $ $}; -- ; -- ++ -| ; -- ; -- ++ -| ; -- ++ -| ; -- ; [rectangle, draw=orange!60, fill=orange!10, rounded corners=2pt, minimum width=4.6cm, minimum height=0.55cm, text centered] at {$ = _1 + _2 + _3$}; -- ; -- ; -- ; [rectangle, draw=green!60!black, fill=green!10, rounded corners=2pt, minimum width=4.6cm, minimum height=0.55cm, text centered] at {User embedding $ ^d$}; -- ; illustrated for a single-source $K{=}3$ SID. looks up the prefix unigram $c_1$ in a direct table $_1$. hashes the prefix bigram $ $ into table $_2$. hashes the prefix trigram $ $ into table $_3$ . All tables use hash size $H = $ and are trained end-to-end under the CTR objective; level embeddings are summed to produce $$. In the Multi-Source SID setting, the same module is applied independently to each source's 3-code block and all embeddings are summed .} User SIDs are : the same $K$-token sequence is broadcast across all $L$ positions in the interaction sequence for a given user. The input representation at event $e_t$ is: _t = \! ( _{f } _t^{ } + ^{} ), where $$ is the standard feature set and $^{} = $ is the user embedding from the HDE Module, constant across positions. HDE embedding tables use a higher learning rate than Transformer weights , following established practice for embedding-heavy ranking systems~. User SID tokens are materialized offline, normalized to exactly $K$ levels during data loading, and broadcast to $ $ integer tensors during batch collation, ensuring strict train-serve schema parity. Figure~ illustrates the integration. [ font= , box/.style={rectangle, rounded corners=3pt, draw, minimum height=0.60cm, text centered, inner sep=4pt}, boldbox/.style={rectangle, rounded corners=3pt, draw=accentblue, very thick, minimum height=0.60cm, text centered, inner sep=4pt, fill=accentblue!8}, arr/.style={-{Stealth }, thick}, ] at {Viewer SID\\$ $}; at {HDE Module\\ }; at {Event features\ position $t$}; at {Embedding Tables\\$\{_f\}_{f }$}; at {$_t = \! } + ^{} )$, $t = 1, ,L$}; at {Transformer Stack\\ }; at {$ $}; -- ; -- ; -- ; -- ; -- node {$\{_1, ,_L\}$} ; -- ; at {$^{}$}; at {const.\ across $t{=}1, ,L$}; at {$\{_t^{ }\}$}; : The viewer SID $ $ is encoded once per request by the HDE Module , producing $^{}$ --- a dense vector in the sequence. : Per-event features at position $t$ are encoded through standard embedding tables. : At every position $t$, the user embedding is summed with the event-level embeddings before a shared LayerNorm, yielding input tokens $\{_t\}$. The resulting sequence is processed by the Transformer stack and scored by the CTR head. All HDE embedding tables are trained end-to-end under the CTR objective.} Our experiments answer five research questions: : Can cross-domain viewer SIDs improve ads CTR ranking over the no-SID production baseline? : Does the AUC gain from a cross-domain SID scale with the amount of behavioral activity encoded in its source embedding? : Does the structured Multi-Source SID with backbone-based imputation beat na\"{i}vely summing independent single-source SIDs? : Does RQ-FSQ match the AUC of the dense source embedding across heterogeneous pre-trained sources? : Are cross-domain viewer SIDs most valuable for users with sparse ad interaction history ? Production ads logs from a large-scale industrial recommendation platform; 60 days of data for training and the subsequent 1 day for evaluation. All results are reported as relative AUC gains over the no-SID baseline; absolute values are withheld per confidentiality policy. The production ads click prediction ranking model described in Section~. User SID default: codebook $C{=}1024$, levels $K{=}3$, hash size $H = $, embedding dimension $D=64$. For RQ-VAE, commitment loss coefficient $ $ is 0.25. AUC relative to the no-SID baseline. At industrial production scale, $+0.1\ Random seed, batch size, optimizer schedule, and model depth are held fixed across all variants to isolate SID effects. We selected $K{=}3$, $C{=}1024$, and $D{=}64$ based on internal sweeps on pilot configurations: smaller $K$ underfit, and larger $K$ or $C$ yielded no measurable improvement at our deployment scale. The hash-table cap $H^{ }$ is a memory budget that bounds embedding-table size to keep downstream serving costs manageable, with no observed quality cost in our pilot studies. For RQ-FSQ, we use $L{=}16$ levels per dimension , chosen to keep the FSQ branch compact while retaining enough per-dimension resolution to preserve fine structure. The HDE Module adds negligible inference latency over the no-SID baseline: all lookups are local memory accesses on standard categorical embedding tables, and per-request user SIDs are precomputed offline . Training cost is essentially unchanged because the HDE tables share the same SGD step as the rest of the model. Table~ consolidates all SID results. The upper block covers single-source SIDs ; the lower block covers multi-source combinations . All methods use the same production Ads ranking model and HDE Module with $K{=}3$ codes per source. 3$ codes per source). $ $AUC relative to no-SID baseline. : single-source SIDs ordered by behavioral activity richness. : multi-source combinations. The RQ-FSQ comparison against the same sources appears in Table~.} {llc} & & $$ \\ No SID & Reference & --- \\ {l}{} \\ Profile Qwen SID & Text semantics only, no behavioral signal & $+0.036\ Activity-Tuned LLaMA SID & Activity-trained, profile-prompted & $+0.107\ Feed Activity SID & Direct behavioral signals & $+0.213\ {l}{3$ per source, 9 total codes)}} \\ Independent combination & 3 SIDs, separately indexed and summed & $+0.260\ & Structured 9-code, Activity-Tuned LLaMA backbone & $ The upper block of Table~ reveals a systematic relationship: the downstream AUC gain scales monotonically with the amount of behavioral activity encoded in the source representation. Profile Qwen SID yields $+0.036\ We term this the : for cross-domain SID transfer, the quality of the transferred signal is determined by how much behavioral activity is encoded in the source representation, regardless of whether that activity is encoded directly or implicitly through an activity-trained model . Profile attributes alone provide a comparatively weak signal ($+0.036\ Feed activity captures high-frequency, evolving behavioral intent absent from both ads interaction logs and user profile descriptions. Discretizing these signals and fine-tuning end-to-end under the CTR loss performs : the gradient re-specializes the embedding tables to map feed behavioral clusters toward ad engagement probability, with no explicit cross-domain alignment objective. The lower block of Table~ shows the multi-source results. Independent combination --- summing three separately-indexed single-source SIDs with no structural coordination --- already yields $+0.260\ The gain comes from two factors. First, per-source prefix n-gram tables avoid cross-source hash collisions that arise when independently-indexed tables are summed without source partitioning. Second, the Activity-Tuned LLaMA backbone fallback for missing Profile Qwen codes preserves population coverage rather than silently zeroing out the affected users. We evaluate RQ-FSQ on two heterogeneous pre-trained sources --- a lower-dimensional Feed Activity embedding and a higher-dimensional Activity-Tuned LLaMA embedding --- against the dense float baseline, RQ-KMeans, and FSQ alone. $ is measured against the --- the production ranking model with no member SID input. is the per-user serialization footprint relative to the of the same source. RQ-FSQ matches the dense embedding's AUC on both sources at $30$--$280{ }$ smaller storage.} {8pt} {1.15} {lcccc} & {c}{} & {c}{} \\ & {c}{ lower-dimensional source} & {c}{ higher-dimensional source} \\ {2-3} {4-5} & & $$ & & $$ \\ Raw float embedding & $1{ }$ & $+0.349\ RQ-KMeans & ${ }\,0.004{ }$ & $+0.213\ FSQ & ${ }\,0.03{ }$ & $+0.343\ & $}$ & $ The cold-start hypothesis --- that cross-domain viewer SIDs are most valuable for users with sparse ad interaction history --- is directly testable by stratifying by user activity level. We partition the validation set into three segments by number of distinct ad impressions in the trailing history: (near-zero ad history; bottom 8\ {lc} & $$ \\ Most cold-start & $+1.522\ Infrequent & $+0.874\ Frequent & $+0.131\ Overall & $+0.213\ The pattern directly validates the cross-domain transfer mechanism: gains scale monotonically with sparsity. Most cold-start users benefit the most ($+1.522\ To confirm the RQ-FSQ $>$ RQ-KMeans ordering generalizes beyond our production data, we replicate the comparison on the public MovieLens-100K benchmark. Movie text is encoded with the open-weight sentence encoder; user embeddings are the mean of liked-movie embeddings, the public analog of cross-domain behavioral aggregation. We quantize user embeddings via RQ-KMeans, FSQ, and RQ-FSQ and train a small MLP to predict whether a held-out user--movie pair has rating $ 4$; RQ-FSQ is warm-started from the trained RQ-KMeans weights, mirroring deployment of the FSQ residual on top of an existing RQ-KMeans system. Table~ confirms RQ-FSQ achieves the highest AUC, exceeding both RQ-KMeans and the dense user embedding --- matching Table~ on a public benchmark. {6pt} {lcc} & & $$ \\ No SID & $0.7689$ & --- \\ Dense user emb & $0.8078$ & $+3.89\ RQ-KMeans & $0.8215$ & $+5.26\ & $$ & $ The monotonic progression from profile text ($+0.036\ RQ-FSQ matches or slightly exceeds dense-float AUC across two heterogeneous pre-trained sources because the branches are complementary: FSQ preserves per-dimension fine structure RQ-VAE can compress away; RQ-VAE preserves global geometry FSQ ignores. The design should generalize to any pipeline needing a compact discrete code over a pre-trained vector --- item embeddings, multimodal encoders, LLM-based representations. Our results provide the first empirical evidence that viewer SIDs from a different domain are effective input features for ads CTR, and can outperform within-domain profile SIDs. The key enabler is the combination of and : quantization strips domain-specific embedding geometry while preserving semantic cluster structure; the CTR gradient re-specializes the embedding tables toward the target task. This is qualitatively analogous to transfer learning via pre-training and fine-tuning, but operating in the discrete token space. Our methodological contributions --- RQ-FSQ, the HDE Module, Multi-Source SID with backbone-based imputation, and the behavioral activity richness principle --- depend only on pre-trained user embeddings and a downstream ranking objective, applying to any system meeting those conditions. Our three sources are platform-specific, but each maps to a category readily available in most large recommendation systems --- a profile-text encoder, an activity-tuned user encoder, and a behavioral aggregation --- reproducible with open-weight analogues. The public-data replication on MovieLens-100K confirms the RQ-FSQ $>$ RQ-KMeans ordering on an open benchmark. The primary effect of cross-domain user SIDs in deployment is improved cold-start ranking quality, which reduces low-relevance impressions for users with sparse ad-domain history. The same mechanism increases the granularity at which inferred user interests can be associated across domains, so production use should remain subject to the standard consent and privacy controls already governing behavioral feature pipelines. The discrete bottleneck of SIDs is privacy-favorable relative to dense vector exposure: only $K$ low-bit codes are materialized per user, limiting the resolution at which raw activity is preserved. We presented , a quantization method that pairs per-dimension scalar quantization with residual VAE quantization, matching or slightly exceeding dense-float AUC at $ 30 $--$280 $ storage reduction across two heterogeneous embedding sources. We established the --- cross-domain transfer scales monotonically with the behavioral signal encoded in the source ($+0.036\ The same SID interface extends naturally to other ranking surfaces and multi-task heads beyond CTR --- the discrete-token bottleneck enables a single unified user representation across the production stack, with shared HDE tables serving as the common substrate. 