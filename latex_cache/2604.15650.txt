 {acmart} {footmisc} {} -0.8em }}} } } } } } } } } } {Wang et al.} Scaling industrial recommender models has followed two parallel paradigms: ---enriching the information content of each training sample through deeper and longer behavior sequences---and ---unifying sequence modeling and feature interaction within a single Transformer backbone. However, these two paradigms still face two structural limitations. Firstly, sample information scaling methods encode only a subset of each historical interaction into the sequence token, leaving the majority of the original sample context unexploited and precluding the modeling of sample-level, time-varying features. Secondly, model capacity scaling methods are inherently constrained by the structural heterogeneity between sequential and non-sequential features, preventing the model from fully realizing its representational capacity. To address these issues, we propose , which encodes each historical Raw Sample directly into the sequence token---maximally preserving sample information while simultaneously resolving the heterogeneity between sequential and non-sequential features. SIF consists of two key components. The quantizes each historical Raw Sample into a Token Sample via hierarchical group-adaptive quantization , enabling full sample-level context to be incorporated into the sequence efficiently. The then performs deep feature interaction over the homogeneous sample representations via token-level and sample-level mixing, fully unleashing the model's representational capacity. Extensive experiments on a large-scale industrial dataset validate SIF's effectiveness, and we have successfully deployed SIF on the Meituan food delivery platform. Recommender systems are a core infrastructure for large-scale internet platforms such as Meituan, connecting hundreds of millions of users to relevant content across e-commerce, short video, and local services. At Meituan, the food delivery platform alone serves hundreds of billions of daily recommendation requests, where even marginal improvements in ranking quality translate directly into business value. With the advancement of large-scale neural models, scaling---continuously improving model performance by expanding training data, sequence length, or model capacity---has become one of the most active research directions in industrial recommender systems. Existing scaling efforts fall into two complementary paradigms. The first, , enriches the information content of each training sample by extending and deepening historical behavior sequences. This has been pursued along two sub-directions: , which extends behavior history from tens to thousands of interactions , and , which enriches each historical token with additional context beyond the bare item embedding . The second, , enlarges model expressiveness by unifying sequence modeling and feature interaction within a single Transformer backbone. Rather than processing the behavior sequence and feature interactions in separate modules, methods such as HyFormer~, OneTrans~, and MixFormer~ feed all feature fields---including historical tokens---into a unified deep architecture, enabling richer cross-field interactions in a single forward pass. However, despite their respective advances, these two paradigms still face two structural limitations. Firstly, sample information scaling methods encode only a subset of each historical interaction into the sequence token: due to storage and serving cost constraints, they either retain bare item embeddings or enrich tokens with only selected feature subsets, leaving the majority of the original sample context---user profile, contextual signals, behavioral outcome---unexploited. More critically, this architectural choice renders these methods structurally incapable of modeling ---signals such as real-time item popularity, competitive exposure, and temporal demand shifts that are unique to each individual impression and cannot be recovered from any static item representation. Secondly, model capacity scaling methods are inherently constrained by the structural heterogeneity between sequential and non-sequential features: item-level history tokens and rich multi-field current-request tokens differ fundamentally in information density, and placing them in the same attention space creates a representational asymmetry that prevents the model from fully realizing its capacity. Consider two users who both clicked the same restaurant: one during a late-night session with a discount coupon active, the other during a lunch rush on a weekday. A bare item embedding treats these interactions as identical, yet their full request contexts carry very different signals about user intent. Preserving the complete Raw Sample for each interaction---rather than a stripped item embedding---could substantially improve the model's ability to reason about temporal and contextual patterns. To address the aforementioned challenges, we propose , which encodes each historical Raw Sample directly into the sequence token---maximally preserving sample information while simultaneously resolving the heterogeneity between sequential and non-sequential features. The key insight is that, at training time, every historical interaction already has an associated full request record---user profile, item features, contextual signals, and pre-computed cross features---stored in the training log; the bottleneck lies not in data availability---every historical interaction is already logged---but in representation: the rich sample context stored in training logs has not been fully exploited. SIF closes this gap with two key components. The compresses each Raw Sample offline into a Token Sample via hierarchical group-adaptive quantization , jointly optimized with the ranking objective, so that full sample-level context can be retrieved at serving time with negligible overhead. The resulting sequence consists entirely of structurally homogeneous Token Samples, eliminating the representational asymmetry between historical and current-request features. The then performs deep feature interaction over these homogeneous representations via token-level and sample-level mixing, enabling the model to fully exploit its representational capacity across both intra-sample and inter-sample dimensions. In summary, our contributions are as follows: We propose , a framework that elevates each historical sequence token from item-level to sample-level, simultaneously resolving the incomplete sample utilization in sample information scaling and the feature heterogeneity in model capacity scaling. We introduce the , which compresses Raw Samples into Token Samples via hierarchical group-adaptive quantization for efficient serving, and the , which performs token-level and sample-level feature interaction over the resulting homogeneous sample representations. We conduct extensive experiments on a large-scale industrial dataset, demonstrating that SIF consistently outperforms strong baselines with up to Sample Information Scaling improves recommendation quality by enriching the information carried per historical interaction. It has two complementary sub-directions: and . extends behavior history from tens to thousands of interactions. DIN~ introduced attention-based interest extraction; DIEN~ modeled interest evolution via GRU. SIM~ and ETA~ scaled to ultra-long sequences via two-stage retrieval, and LONGER~ generalized this with hierarchical compression. Despite growing sequence lengths, all these methods leave each historical token as a bare item embedding---intra-token information density is an untouched bottleneck. enriches each historical token with additional context beyond the item embedding. DSAN~ appends context features to sequence tokens. TWIN~ concatenates features to each history token to enable target-aware retrieval. SINE~ incorporates label and intent signals via contrastive learning. Most relevant to SIF is HSTU~, which encodes rich per-interaction side information at Meta's scale via linear attention over thousands of interactions. However, none of these methods can fully preserve the original sample information in each historical token: due to storage and serving cost constraints, they enrich tokens with only feature subsets---direct concatenation of all feature fields would multiply the sequence token dimension by an order of magnitude, making attention prohibitively expensive at $L = 200$, and storing full float16 snapshots at scale incurs prohibitive storage cost that significantly limits practical applicability. SIF completes the sequence widening direction by compressing the complete Raw Sample---rather than selected feature subsets---offline via group-wise RVQ; the per-token serving cost at inference time remains identical to a standard item embedding lookup, while the information content approaches that of the original sample. A recent generation of models unifies sequence modeling and feature interaction within a single Transformer backbone. ~ interleaves FI and sequence layers. ~ uses multi-task learning to jointly optimize sequence and FI objectives. ~ processes all feature fields as tokens in a single Transformer. ~, our primary baseline, introduces a hybrid attention mechanism that separates intra-field and cross-field attention for efficiency. ~ combines MLP-Mixer and Transformer layers. The common limitation across all these architectures is that historical tokens remain at the item level. Vector quantization has seen growing adoption in recommender systems for item tokenization. ~ learns discrete item codes via VQ for transferable recommendation. ~ uses residual quantization to generate item identifiers for generative retrieval. SIF instead quantizes ---multi-field, multi-modal feature concatenations---for efficient storage and serving in a discriminative ranking model. This requires group-wise decomposition to handle heterogeneous feature fields and a label-aware supervision signal to keep the codebook meaningful for ranking, neither of which arise in item-VQ settings. Beyond retrieval, VQ has recently demonstrated strong potential in ranking. ~ pioneers discrete semantic tokenization for deep CTR prediction, achieving ${ }$200$ $ storage compression with negligible ranking loss. ~ further introduces orthogonal rotation for semantic tokenization in ranking, achieving online AUC $+$1.195\ {figure/SIF.pdf } $ via HGAQ into a Token Sample $$. Sample splicing replaces item embeddings with Token Samples $$, yielding a token-homogeneous sequence. Sample serialization orders historical Token Samples chronologically with temporal position embeddings. SIF-Mixer embeds each Token Sample $$ into $^0$ via codebook lookup, then stacks $N$ SIF Blocks, each comprising a Token-level Mixer , a Sample-level Mixer , and a Token-level FFN, followed by a prediction head.} {ll} & \\ $L$ & Behavior sequence length \\ $N$ & Number of SIF Blocks \\ $G$ & Number of semantic feature groups \\ $K_g$ & Number of sub-tokens for group $g$ \\ $T$ & Total token count per sample \\ $B$ & Sub-token granularity: target feature fields per sub-token \\ $M$ & RVQ residual levels per sub-token \\ $V$ & Codebook size per level \\ $ ^{d_s}$ & Raw Sample \\ $$ & Token Sample: $T M$ discrete codebook indices \\ $^{ }_l ^{d_0}$ & Sub-token $ $ embedding \\ $^0$ & SIF-Mixer input: all $L{+}1$ Token Sample embeddings \\ $W_{}^{ }$ & Target sub-token projection } = W_{}^{ }^{ }_{ }$) \\ Let $$, $$, and $$ denote the sets of users, items, and contextual signals, respectively. At request time $ $, a ranking model receives a feature tuple $^ = $ for candidate item $j$, and a $ = \{i_l\}_{l=1}^{L}$ recording the $L$ most recent positive interactions of the user, strictly before $ $, ordered chronologically. The model outputs a score $ $ predicting the probability of engagement. In standard unified architectures, the behavior sequence is encoded as a set of item embeddings $\{_{i_1}, , _{i_L}\}$. augments each entry $i_l$ with the complete feature snapshot of the historical request, and compresses this Raw Sample to a Token Sample $$ via the Sample Tokenizer. In standard unified architectures such as HyFormer~, the behavior sequence $$ is represented as a set of item embeddings $\{_{i_1}, , _{i_L}\}$ placed alongside rich field-level current-request tokens in the same attention space. This token heterogeneity is the common root of both blind spots that SIF addresses. We define a at interaction time $t$ as the complete feature tuple that characterized that historical event: = where $^{}$ are user profile features, $^{}$ are item features, $^{}$ are contextual signals, and $^{}$ are pre-computed cross features capturing joint user-item-context interaction patterns . The Raw Sample $ ^{d_s}$ is high-dimensional. The Sample Tokenizer compresses $$ into a Token Sample. We distinguish two types of features in a sample: ---user attributes, item attributes, and contextual signals---which are amenable to quantization; and ---the user's historical interaction sequence $$---which carry temporal ordering information and are therefore excluded from sample quantization. Sequential features are modeled directly by the Sample-level Mixer in SIF-Mixer , which captures inter-sample dynamics via cross-temporal attention. The Sample Tokenizer $: $ maps each Raw Sample to a Token Sample $ = })$---a compact set of $T M$ discrete codebook indices. At serving time only these indices need to be stored and retrieved; the corresponding embedding vectors are looked up on-the-fly from the codebook maintained in the SIF-Mixer. Different components of $$ capture qualitatively different information. Rather than quantizing the entire snapshot with a single codebook, we first partition $$ into $G = 4$ semantic groups: = This grouping is flexible: high-cardinality key features that carry strong identity signals can be assigned to a dedicated singleton group, ensuring their discriminative information is not diluted by lower-cardinality features within a shared codebook. Within each semantic group $g$, the number of features $|_g|$ varies considerably. Compressing a large group into a single token forces semantically distinct sub-concepts into one vector, limiting the Token-level Mixer's ability to disentangle them. We therefore further divide each group into $K_g$ sub-tokens, where the number of sub-tokens is determined adaptively by the group's feature count: K_g = _g|}{B} where $B$ is the ---the target number of feature fields assigned to each sub-token . Groups with more features naturally produce more sub-tokens, granting them proportionally more positions in the Token-level Mixer. The $|_g|$ features in group $g$ are evenly partitioned into $K_g$ non-overlapping subsets $\{_g^{ }\}_{k=1}^{K_g}$; each subset is projected to a fixed sub-token dimension $d_0$ via a group-and-slot-specific linear layer: }^{ } = W^{ }_{}\, ^{ } ^{d_0} where $^{ }$ denotes the raw features in the $k$-th subset of group $g$. All sub-tokens share the same dimension $d_0$ , making the total token count per sample $T = _{g=1}^{G} K_g$ and the total token embedding dimension $d = T d_0$. Each sub-token has its own codebook $_{g,k} = \{^{ }_v\}_{m,v}$ with $V = 256$ codes per level of dimension $d_0$. Within each sub-token $ $, we apply $M$-level residual vector quantization to the projected sub-token vector $}^{ }$: q^{ } = _v \|^{ } - ^{ }_{v}\|_2, ^{ } = ^{ } - ^{ }_{q^{ }} where the initial residual $^{ } = }^{ }$. The Token Sample concatenates all sub-token index sequences across all groups: = _{g=1, ,G;\; k=1, ,K_g} With $G=4$, $B=32$, $M=3$, $V=256$: the total number of sub-tokens is $T = _g K_g$ , and each $$ is compressed to $T M _2 V = 27{ }3{ }8 = 648$ bits. The Sample Tokenizer is jointly trained with the ranking objective. During training, the RVQ reconstruction $}$ is fed to a lightweight MLP to predict CTR: } = _{g,k}, ^{} = ) This auxiliary pCTR loss $_{} = _{} $ is added to the overall training objective, ensuring that the codebook is organized by predictive relevance rather than purely by reconstruction error. Importantly, for important categorical features such as item IDs, we retain a dedicated per-field quantization group to preserve individual item identity, preventing high-cardinality identifiers from being merged with less-discriminative features within a shared group. Standard behavior sequences record only item IDs. SIF upgrades each entry with the full feature snapshot and compresses it into a Token Sample, transforming the sequence from item-level to sample-level: }_{} \;\;} _1,\; ,\;_L\}}_{} Each $_l \{1, ,V\}^{T M}$ encodes the complete multi-field snapshot of the $l$-th interaction as $T{ }M$ discrete indices , a substantially more compact representation than the original float32 features. The Token Samples are pre-computed offline and stored in a key-value store; the sample sequence, ordered chronologically, constitutes the offline-prepared input to SIF-Mixer. is a backbone architecture, inspired by MLP-Mixer-style factored designs~, that stacks $N$ identical . Each block decomposes the interaction into three sequential sub-operations: Token-level Mixer, Sample-level Mixer, and Token-level FFN. This factored design explicitly separates intra-sample feature interaction from inter-sample temporal interaction, reducing attention complexity while preserving both interaction types. The input to SIF-Mixer consists of two parts, which together form the initial hidden state $^0 ^{ T d_0}$, where $T = _{g=1}^{G} K_g$ is the total number of sub-tokens per sample. : the sample sequence $\{_1, ,_L\}$ from is embedded via codebook lookup with recency encoding. For the sub-token at position $ $: ^{ }_l = _{m=1}^{M} ^{ }_{q^{ }_l} ^{d_0}, ^0_{l,*} = + _{L-l} : unlike historical Token Samples, the current-request features $\{^{ }_{ }\}$ are only available at serving time and cannot be pre-quantized offline. They are instead projected per sub-token into the same codebook space on-the-fly via a learned linear projection: ^0_{0, } = W_{}^{ }^{ }_{ } $_{}$ ensures each $W_{}^{ }$ maps into the same space as its codebook, making all $L+1$ rows compatible for cross-temporal attention. Each block $n \{1, ,N\}$ operates on $^{n-1}$ viewed as a $ T$ matrix of $d_0$-dimensional vectors---rows are samples, columns are sub-token positions : ^n \;=\; ^n_{0,1} & & ^n_{0,T} \\ ^n_{1,1} & & ^n_{1,T} \\ & & \\ ^n_{L,1} & & ^n_{L,T} ] }^{ \; T \; \;} .\\ \\ \\ \\ } \} \!\!{l} L{+}1\\ \\ \\ \\ Three sub-operations are applied in sequence : runs self-attention along each , modeling interactions among all $T$ sub-tokens within a single sample. Because sub-tokens from different groups occupy distinct column positions, the attention can capture both inter-group correlations and intra-group sub-concept interactions : }^n_l = ^{n-1}_l + ), l = 0, ,L runs self-attention along each , modeling interactions among all $L+1$ samples at the same sub-token position---crucially, the Target Token Sample can attend to all Seq Token Samples to extract relevant historical context: }^n_{*,p} = }^n_{*,p} + ), p = 1, ,T applies a position-wise non-linear transformation to each entry: ^n_{l,p} = }^n_{l,p} + ) After $N$ SIF Blocks, we extract the target sample representation by mean-pooling its $T$ sub-token outputs: = {T} _{p=1}^{T} ^N_{0,p} The ranking score is produced by a two-layer MLP with sigmoid output: = \! + b_2 ) The overall loss is: = _{} + \,_{} + \,_{} where $ = 1.0$ and $ = 0.25$. $_{}$ is the standard VQ commitment loss~ between the group encoder output and the RVQ reconstruction. The alignment loss trains the target token projection $W_{}^{ }$ to be consistent with the codebook space, so that target and historical token representations are compatible at inference time: _{} = _{g=1}^{G} _{k=1}^{K_g} \| W_{}^{ }^{ }_{ } - \|^2 where $ $ denotes stop-gradient and $_{g,k} = _m ^{ }_{q^{ }}$ is the Tokenizer's codebook reconstruction for sub-token $ $. During training, $_{g,k}$ is obtained by passing the current-request Raw Sample $_ $ through the Sample Tokenizer to produce the RVQ reconstruction $_{g,k} = _m ^{ }_{q^{ }_ }$, which serves as the alignment target; at serving time this Tokenizer forward pass is skipped and only $W_{}^{ }$ is applied. Because each Raw Sample $$ incorporates the behavioral outcome of the original interaction, the ranking supervision signal $_{}$ propagates through VQ into codebook learning, organizing snapshots by their predictive context without requiring an additional contrastive term. Each SIF Block incurs $O d_0 + ^2 T d_0)$ attention operations. Since $T L+1$ in practice , the sample-level mixer dominates: $O $, matching the complexity of standard sequence attention at the same sequence length. The adaptive sub-tokenization increases $T$ relative to the fixed-$G$ design, but $T$ remains small , keeping the token-level mixer overhead negligible. We evaluate SIF on one large-scale industrial dataset: : 1B+ impression records , 50M+ users, 5M+ items. Each sample includes ${ }$600+ feature fields spanning user profile, item attributes, contextual signals, and a 1000-item behavior sequence. All 4 semantic groups with adaptive sub-tokenization are fully utilized. All baseline models receive the same current-request feature set as SIF ; the sole difference across methods is the representation of the ---baselines use standard item embeddings, while SIF replaces each historical token with an HGAQ-compressed Token Sample. We report , , and on the industrial dataset. All results are averaged over 5 independent runs; statistical significance by paired $t$-test . Each model is characterized by its sequence modeling component and feature interaction component. We organize comparisons along two axes: : ~ , ~, ~. : ~, ~, ~. : ~, ~. All models are implemented in PyTorch on 8$ $A100-80G GPUs. For the SIF-Mixer: $N = 4$ SIF Blocks, 8 attention heads, sub-token dimension $d_0 = 16$, FFN dimension $4{ }d_0$, pre-norm LayerNorm, mean pooling over $T$ sub-tokens. The Sample Tokenizer: $G = 4$, $B = 32$, $M = 3$, $V = 256$, $d_0 = 16$. Optimizer: Adam~ , batch size 4096. Sequence length $L = 1000$. Table~ presents the main comparison results. Bold indicates the best result; $ $ denotes statistically significant improvement over HyFormer . {!}{ {ll|cc|cc|cc} {*}{} & {*}{} & {c|}{} & {c|}{} & {c}{} \\ {3-4} {5-6} {7-8} & & AUC$ $ & GAUC$ $ & AUC$ $ & GAUC$ $ & Params & TFLOPs \\ DCNv2~ & DIN~ & 0.7832 & 0.7614 & 0.8103 & 0.7891 & 48 & 0.31 \\ Wukong~ & SIM~ & +0.41\ Wukong~ & LONGER~ & +0.53\ RankMixer~ & SIM~ & +0.67\ RankMixer~ & LONGER~ & +0.79\ {l|}{HyFormer~ } & +1.12\ {l|}{OneTrans~ } & +1.08\ {l|}{} & SIF outperforms all baselines across all metrics, with improvements that are statistically significant in every setting. SIF outperforms HyFormer by +0.91\ Among specialized models, sequence-modeling approaches consistently outperform feature-interaction approaches on CTR, confirming the importance of sequential modeling in behavior-rich industrial settings. Unified frameworks further outperform all specialized models by jointly modeling sequence dynamics and feature interactions, validating the architectural paradigm of unification. The observed AUC improvement of SIF over HyFormer (+0.91\ We organize the ablation into two parts: ablation of the Sample Tokenizer, evaluating different token representations along both quality and compression efficiency ; and ablation of the SIF-Mixer backbone, isolating the contribution of each mixing component. Table~ compares four token representation strategies. Metrics are the GAUC gap relative to SIF full on CTR and CVR, and the , defined as the ratio of the original raw snapshot storage to the per-sample token storage: = }}{b_{}}, where $b_{}$ is the bit-size of the full non-item snapshot stored in float32 , and $b_{}$ is the bit-size of the stored token. In our setup, $|_{}|{=}600$, $d_e{=}8$, giving $b_{}{=}153{,}600$~bits. For HGAQ, only $T{ }M$ discrete indices are stored: $b_{} = 27{ }3{ }8 = 648$~bits, yielding a storage compression ratio of ${ }237{ }$. Crucially, this high ratio is achieved through genuine compression---the full snapshot context is recovered at inference via codebook lookup---rather than by discarding features. 8{ }32{=}153{,}600$~bits) / stored token bits; higher is more compact. $ $std over 5 runs.} {!}{ {lccc} & $ $ & $ $ & \\ & --- & --- & $ $237$ $ \\ Item ID only & $-$1.00\ Item ID + key features & $-$0.60\ Raw sample emb & $-$0.27\ } each historical position is represented by a single item-ID embedding, equivalent to a standard item-embedding sequence backbone. Cross-feature groups and the Token-level Mixer are inapplicable and thus disabled. Storage: 1 int64 item ID index $= 64$~bits . item-ID embedding concatenated with a small set of high-importance handcrafted features , projected to the same token dimension. Storage: 1 int64 item ID index + $ $24 int32/float32 scalar fields $= 64 + 24{ }32 = 832$~bits . all raw snapshot features concatenated and projected via a linear layer to a 512-dim dense vector, without any quantization. Storage: 512 float32 values $= 512{ }32 = 16{,}384$~bits . Token-level Mixer applies but quantization structure is absent. only $T{ }M$ discrete codebook indices are stored offline; at serving time embeddings are retrieved via codebook lookup. Storage: $27{ }3{ }8 = 648$~bits , with full snapshot semantics recovered on-the-fly. Item ID only performs worst ($-$1.00\ The dense raw-sample embedding retains all snapshot features yet still underperforms HGAQ ($-$0.27\ We sweep the sub-token granularity $B \{2, 4, 8, 16, 32, 64\}$, which controls how many sub-tokens each group produces via $K_g = |_g| / B $, giving $T 600 / B $ total sub-tokens . $B = 32$ achieves the best GAUC , striking the optimal balance between intra-group disentanglement and model compactness. At $B = 2$ , GAUC drops slightly to 0.7750 due to excessive token sequence length making the Token-level Mixer harder to optimize. At $B = 64$ , GAUC falls to 0.7735, approaching the behavior of the fixed-$G$ design with insufficient intra-group decomposition. SIF outperforms HyFormer for all tested values of $B$, demonstrating robustness across a wide range of granularity choices. We adopt $B = 32$ as the default. {figure/k_sweep_gauc.png} {=}0.7691$) across all tested $B$; the red dot marks the optimal $B{=}32$ .} Given that each sequence position carries $T$ side-information sub-tokens, there are multiple ways to apply attention over the resulting $ { }T$ token matrix. Table~ compares three strategies on the industrial dataset to identify which attention pattern best exploits the sample-level token structure. Metrics are the GAUC gap relative to SIF full on CTR and CVR. {!}{ {lcc} & $ $ & $ $ \\ & --- & --- \\ Flat attention & $-$0.24\ Pooled-then-attend & $-$0.81\ } all $ { }T$ sub-tokens are flattened into a single sequence and processed by standard full self-attention, with complexity $O ^2)$. each sample's $T$ sub-tokens are first mean-pooled into a single vector, then standard sequence attention is applied across $L{+}1$ pooled representations, with complexity $O $. This is the typical approach in existing sequence models that incorporate side information. intra-sample Token-level Mixer $) followed by inter-sample Sample-level Mixer $), total $O O $. Pooled-then-attend ($-$0.81\ We study two dimensions of SIF's scaling behavior: model depth and sequence length. Figure~ plots GAUC against TFLOPs as the number of layers $N$ is varied from 1 to 6 for all three models. SIF achieves a consistently better GAUC-FLOPs trade-off across the entire depth range: at matched FLOPs , SIF reaches GAUC 0.7803 vs.\ HyFormer's 0.7715 and OneTrans's 0.7710 , consistent with the default-configuration results in Table~. HyFormer saturates early due to the heavy per-layer cost of full attention, while OneTrans plateaus at a lower GAUC ceiling. SIF continues to improve until $N=4$ and we adopt this as the default. {figure/k_sweep_single.png} $ varies for SIF, HyFormer, and OneTrans. SIF achieves a consistently better CTR GAUC--FLOPs trade-off; the red dot marks the optimal $N{=}4$.} A key property of SIF is whether its advantage persists or grows as the history budget increases. We evaluate SIF, HyFormer, and OneTrans across $L \{100, 200, 500, 1000, 2000\}$ on the industrial dataset . {!}{ {lcccc} & & & & $ $ \\ 100 & 0.7674 & 0.7680 & 0.7693 & +0.0013 \\ 200 & 0.7689 & 0.7695 & 0.7745 & +0.0050 \\ 500 & 0.7706 & 0.7712 & 0.7782 & +0.0070 \\ 1000 & 0.7710 & 0.7715 & 0.7803 & +0.0088 \\ 2000 & 0.7713 & 0.7718 & 0.7820 & +0.0102 \\ } All three models improve with longer sequences; however, the gains diverge substantially. HyFormer and OneTrans saturate quickly: from $L=500$ to $L=2000$ the GAUC improvement is only +0.0005 and +0.0006 respectively, consistent with the encoding bottleneck in item-level sequence architectures. All three models benefit substantially from longer sequences, but SIF's gains are markedly steeper. At $L{=}100$, SIF is already competitive with HyFormer at $L{=}200$ , and by $L{=}500$ SIF surpasses HyFormer at $L{=}1000$ . The performance gap over HyFormer widens monotonically from +0.0013 at $L=100$ to +0.0102 at $L=2000$. This reflects SIF's token design: each additional history position contributes a fully-contextualized Raw Sample to cross-temporal attention, while item-level methods compress each position to a bare item embedding and hit a representational ceiling. Figure~ visualizes these trends. {figure/scaling_curve.png} SIF is deployed in Meituan's local-service recommendation pipeline. Results from a $L$---the number of logged interactions available for each user. Table~ reports the per-stratum CTR, CVR, and GMV/session lifts. {!}{ {lccc} & $ $ & $ $ & $ $ \\ $L < 10$ & +0.53\ $10 L < 100$ & +1.18\ $100 L < 500$ & +2.07\ $L 500$ & +3.12\ & The gains scale monotonically with $L$ across all three metrics. Heavy users benefit most (+3.12\ We presented , a framework that elevates each historical sequence token from a bare item embedding to a full sample-level representation. Every past interaction already has a complete request record in the training log---yet existing architectures discard it in favor of a bare item ID. SIF closes this gap via the and the . Offline experiments show up to 