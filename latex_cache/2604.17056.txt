 =1 {hyperref} {url} [ a4paper, 11pt]{article} {- } {fontenc} {inputenc} {Ligatures=TeX,Scale=1} {}{} { {microtype} {basicmath} }{} \@ifundefined{KOMAClassName}{ { }{ {0pt} {6pt plus 2pt minus 1pt}} }{ } { @noskipsec }{}{} {}{} @box * { @box{#1} @div\@tempa{ }{ @box+ @box } @div\@tempb{ }{ @box} \@tempb @<\@tempa @ \@tempa\@tempb \@tempa @< @{ @box} } @figure{htbp} {3em} { {0pt}{0pt}} {geometry} {0} {0.4pt} } {0.5em} {0pt} {}{} } , WordLift {0.5pt} When does an LLM controller outperform rule-based traversal for knowledge graph exploration? We study this question through RLM-on-KG, a retrieval system that treats an LLM as an autonomous navigator over an RDF-encoded mention graph for grounded question answering. Unlike GraphRAG pipelines that rely on offline LLM indexing, RLM-on-KG performs entity-first, multi-hop exploration at query time using deterministic graph construction and a fixed tool set. Our central finding is a : the LLM controller's value depends on two factors --- the scatter of required evidence and the model's tool-calling sophistication. , not a generic win over GraphRAG. On GraphRAG-Bench Novel , the Gemini 2.0 Flash controller achieves +2.47 pp F1 over a rule-based heuristic baseline (p 0.0001, 95\ against the GraphRAG-local variant, the same controller achieves only +0.16 pp . With a more capable controller --- Claude Haiku 4.5 on the same 519 questions --- the heuristic advantage grows to and extends to a (p 0.001; 170 wins, 259 ties, 90 losses): the first significant improvement over entity-expanded retrieval, contingent on strong tool-calling capability. The gain is largest when gold evidence is scattered across 6--10 chunks (+3.21 pp, 65\ model families (different experiment scales --- see Section 7 for the matched-scale comparison): Claude Gemini Gemma 4 E2B ($-$0.78 pp, n=100, 83\ for future work. Cross-scale validation on MuSiQue confirms the LLM-over-heuristic advantage transfers, with expected attenuation on smaller per-question graphs. The core architectural insight is the separation of from (pure vector re-ranking): the LLM adds value through exploration breadth, not judgment. Beyond retrieval, exploration traces serve as a proposed , producing diagnostics for the underlying graph substrate (coverage, connectivity, provenance, and queryability). {0.5pt} LLM-guided graph traversal is costly and unnecessary for many retrieval problems. A pure vector search handles single-hop factoid questions. A one-hop entity expansion handles moderate multi-hop questions. So when, exactly, does deploying an LLM as an active graph navigator become worth the additional latency and token cost? We study this question by placing four retrieval strategies on a continuum of controller complexity: { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Controller & { } Mechanism & { } Cost \\ Vector-only & top-k embedding similarity & 2K tokens, 1s \\ GraphRAG-local & 1-hop entity expansion + vector re-rank & 2K tokens, 1s \\ Heuristic RLM & BFS multi-hop traversal, fixed rules & 5K tokens, 5s \\ & Adaptive multi-hop, LLM-driven tool selection & 50K tokens, 2--5 min \\ } All four share the same deterministically-built mention graph, the same embedding index, and the same evaluation pipeline. The only variable is the controller. Retrieval-augmented generation has become the standard approach for grounding LLM responses in external knowledge . The core recipe is straightforward: embed a query, retrieve the top-k most similar chunks from a vector index, and pass them as context to a generator. This works well for single-hop factoid questions, but breaks down when answers require synthesizing information scattered across multiple document sections, entities, and relationships. GraphRAG addresses this by layering entity-based expansion on top of vector retrieval. In its full pipeline, GraphRAG uses an LLM to extract entities, builds a community graph via the Leiden algorithm, and generates community summaries at indexing time. At query time, these precomputed structures enable multi-hop retrieval by expanding from seed chunks to related entities and their neighborhoods. This improves recall on complex questions, but at significant offline cost: LLM-based entity extraction, community detection, and summary generation must be performed before the first query can be answered. For evolving corpora, this pipeline must be re-run whenever the underlying documents change. Recursive Language Models offer an alternative paradigm. Introduced by Zhang, Kraska, and Khattab , RLMs treat the LLM itself as an environment explorer rather than a passive consumer of retrieved context. The LLM proposes actions , observes results, and decides what to explore next, iterating until it has gathered sufficient evidence. The original RLM work demonstrated this on Python REPLs and web browsing; we adapt it to mention-graph traversal. This setting is motivated by RDF-based knowledge graphs, such as the WordLift KG, where entity-centric identifiers, provenance links to source text, and structured publishing workflows are already available and can be reused as a retrieval substrate. We present five findings: .} On the same mention graph and tool set, LLM-driven exploration statistically significantly outperforms rule-based heuristic traversal: +2.47 pp F1 with Gemini (p 0.0001, n=519), +4.37 pp with Claude (p 0.001, n=519). The gain is not uniform but conditional on evidence scatter: +3.21 pp when gold evidence spans 6--10 chunks, +1.85 pp when concentrated. The LLM adds value precisely when graph structure demands adaptive navigation. The advantage scales with tool-calling sophistication: Claude Gemini (+0.84 pp, n=100) Gemma 4 E2B . These deltas span different experiment scales; at matched n=100, only Claude achieves a statistically significant advantage (see Section 7 for the matched-scale comparison). At full scale , Claude is the first controller to statistically significantly outperform the GraphRAG-local variant . The LLM adds value through candidate discovery, not ranking judgment. Pure vector re-ranking of LLM-discovered candidates outperforms all composite scoring strategies . Exploration traces from strong controllers serve as a proposed KG diagnostics framework and training signal for distilling navigation policies into smaller models . At its core, RLM-on-KG implements a cyclic retrieval process : the LLM seeds entities from the question, expands neighborhoods via mention links, reads and verifies chunks, then either collects evidence and cites, or refines sub-queries and loops back. ]{figures/retrieval_loop.png}} Throughout this paper, ``mention-graph traversal'' refers to traversal over the co-mention graph stored in RDF. We use ``WordLift Knowledge Graph'' only for the platform layer and identifiers . Where we use the shorthand ``KG traversal,'' we mean traversal over this mention graph. {0.5pt} Lewis et al.~ introduced RAG as a way to ground generative models in retrieved passages. Subsequent work has improved retrieval quality through better embeddings , query rewriting , and multi-step retrieval . RLM-on-KG builds on this tradition but replaces fixed retrieval pipelines with LLM-driven exploration. Edge et al.~ proposed a graph-based approach to RAG that constructs an entity graph from documents, detects communities, and generates summaries at each level. The system operates in two modes: retrieval and summarization . Our GraphRAG-local variant implements the local recipe without community detection or community summaries; see Section 3.5 for which components are intentionally excluded and why. The full GraphRAG pipeline requires substantial offline LLM processing; our approach avoids this entirely. Several recent systems address graph-structured retrieval with complementary approaches. HippoRAG uses an LLM to build a hippocampus-inspired knowledge graph offline and performs Personalized PageRank at query time. IRCoT interleaves retrieval and chain-of-thought steps without an explicit graph structure. LightRAG constructs a lightweight entity-relation graph with incremental updates and performs dual-mode retrieval . Our comparison design intentionally holds graph construction, embedding model, chunk budget, and generator fixed to isolate the retrieval logic itself; integrating these external systems as baselines would conflate retrieval policy differences with differences in graph construction, NER, and embedding, making attributions impossible. Our heuristic RLM baseline plays a role analogous to a ReAct-style agent baseline: same tools and graph, but rule-based tool selection, isolating the contribution of LLM-driven control. Buehler transformed scientific literature into an ontological knowledge graph and used structural analysis (node degrees, isomorphisms, path sampling) to synthesize novel material designs and interdisciplinary relationships. While they perform structural analysis offline to discover insights, RLM-on-KG adapts these principles to query-time retrieval, using an LLM to actively navigate structural relationships and gather scattered evidence. Zhang, Kraska, and Khattab introduced the RLM framework, where an LLM recursively applies tools to explore an environment. Their work demonstrated the approach on Python REPLs and web browsing tasks. We adapt the core RLM idea to knowledge graph traversal, replacing the REPL with KG tools and adding domain-specific scoring and selection mechanisms. Ouro demonstrates that improves reasoning: a looped transformer applies the same block recurrently, with an entropy-regularized objective that allocates more iterations to complex inputs and fewer to simple ones. RLM-on-KG performs the analogous optimization over external structure rather than latent space: it applies the same LLM-driven tool-calling loop over a mention graph, with evidence scatter determining how many hops are needed. Simple questions saturate after 1--2 hops; complex scattered evidence requires 8--12. This connection positions adaptive retrieval depth as the external analog of adaptive computational depth. The GraphWalks benchmark (OpenAI, 2025) tests whether LLMs can perform structured graph traversal (BFS, parent retrieval) when a graph is placed in-context. Performance degrades sharply as graph size grows, confirming that in-context multi-hop reasoning over graph structure does not scale. RLM-on-KG sidesteps this limitation by externalizing traversal through tools: each tool call is a local operation the LLM can handle reliably, and multi-hop structure emerges from the iteration loop rather than from in-context reasoning over a flattened graph. The GraphRAG-Bench benchmark (Xiang et al., 2025) provides a standardized evaluation for graph-structure-aided question answering. The Novel split contains 20 fiction novels with 2,010 questions spanning four types: Fact Retrieval, Complex Reasoning, Contextual Summarization, and Creative Generation. Questions are designed to test scenarios where graph structure genuinely helps retrieval. {0.5pt} RLM-on-KG operates in two phases: an offline mention-graph construction step using deterministic NLP, and an online exploration step using an LLM navigator. . Documents are split into paragraph-aligned chunks (max 240 words, 40-word overlap). Named entities are extracted using spaCy for four types: PERSON, ORG, GPE, and LOC. Each chunk and entity is represented as an RDF node using schema.org and SEOntology vocabularies. The graph is stored as in-memory triples and indexed with a FAISS vector index (using embeddings). The mention graph is a bipartite structure with three node types and three edge types: { {@{}lll@{}} Node type & Vocabulary & Example \\ Document & & The Great Gatsby \\ Chunk & & paragraph segment \\ Entity & typed by NER label & ``Jay Gatsby'' \\ } { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Edge type & { } Direction & { } Semantics \\ & Chunk $ $ Entity & chunk mentions this entity \\ & Chunk $ $ Document & chunk belongs to document \\ co-mention & Entity $ $ Entity & entities that appear in the same chunk \\ } The tool returns entities connected through shared chunks , not typed semantic relations. We use the term ``mention graph'' rather than ``knowledge graph'' to make this scope explicit: the graph captures , not . No relation extraction, coreference resolution, or entity aliasing is performed. This deliberately minimal graph structure is an experimental control, not a limitation. By using only co-mention edges and surface-form entity matching (no typed relations, no ontological reasoning, no LLM-based extraction), we isolate the variable under study --- the controller policy --- from confounding effects of graph richness. Any performance gains we observe are attributable to the LLM's adaptive traversal decisions, not to a more informative graph substrate. Richer graph structures (typed relations, coreference chains, ontological hierarchies) would likely amplify the LLM controller's advantage, since they expand the action space for adaptive navigation; we leave this investigation to future work. . At query time, a Gemini 2.0 Flash model navigates the mention graph using nine tools exposed via the Gemini function-calling API. The LLM receives a structured system prompt and iterates in a multi-turn conversation, proposing tool calls and receiving results, until it decides to stop or hits a budget limit. ]{figures/architecture_diagram.png}} The nine tools are: { {@{} >{ }p{ * } >{ }p{ * }@{}} { } Tool & { } Description \\ & NER + fuzzy-match entity search \\ & Return chunks mentioning an entity \\ & Semantic similarity search over chunks \\ & Find co-mentioned entities via shared chunks \\ & Read full text and entity list of a chunk \\ & Recursive sub-query over a chunk subset \\ & Compress gathered evidence \\ & Mark a chunk as relevant evidence \\ & Re-rank collected chunks by vector similarity \\ } All tools except are deterministic for a given KG state; spawns a child LLM. Input and output types follow a uniform pattern: { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Tool & { } Input & { } Output \\ & query string & list of (entity\_uri, label, chunk\_count) \\ & entity URI & list of (chunk\_id, text\_preview) \\ & query string, k & top-k (chunk\_id, similarity, text\_preview) \\ & entity URI & list of (neighbor\_uri, label, shared\_chunks) \\ & chunk ID & full text, entity list, document source \\ & question, chunk\_ids & LLM-generated answer \\ & chunk\_ids, focus & condensed summary text \\ & chunk\_id, relevance & confirmation \\ & question & reordered evidence list with similarity scores \\ } We define a as one LLM decision step that may emit multiple parallel tool calls. A is one tool-calling turn . When we report ``10.3 hops'' , this means 10.3 tool-calling turns on average per question. A single turn may invoke multiple tools in parallel (e.g., + + ), so the total tool invocation count is higher. The heuristic baseline averages 2.3 hops , reflecting its fixed breadth-first strategy that saturates quickly. The LLM navigator follows a three-phase exploration strategy, encoded in the system prompt: The LLM calls with the question to find seed entities, then calls for each seed to surface entity-linked chunks. It reads the most promising chunks with to build initial context. For key entities, the LLM calls to discover related entities via shared chunk co-mentions. For new neighbor entities, it calls to surface additional evidence. It uses to mark relevant chunks and with targeted sub-questions (not the original question verbatim) to fill gaps. The LLM uses to verify facts across chunk clusters by spawning recursive child LLMs. It collects final evidence with . After each turn, the system injects a status summary listing the chunks collected so far, the entities explored, and the frontier of unexplored entities. This prevents the LLM from re-exploring already-visited nodes and guides it toward unexplored neighborhoods. Algorithm 1: RLM-on-KG Exploration Input: question Q, KG G, tool set T, budget B Output: ranked evidence chunks E State <- {explored: {}, collected: {}, frontier: {}} stalled <- 0 for t = 1..B do prompt <- FORMAT actions <- VALIDATE_AND_DEDUP , T) if INVALID then actions <- FALLBACK for each action a in actions do result <- EXECUTE State <- UPDATE end if |State.collected| unchanged then stalled <- stalled + 1 else stalled <- 0 if stalled >= 4 and t >= B/2 then break end E <- VECTOR_RERANK // Section 3.4 return TOP_K The loop terminates when any of the following hold: the LLM emits a final text response instead of tool calls; evidence collection has stalled for 4 consecutive turns and at least half the budget has been used; the maximum turn budget B is reached. During exploration, the LLM discovers chunks through two channels: entity traversal (, ) and semantic search . Chunks are not automatically added to the evidence pool --- the LLM must explicitly call to mark a chunk as relevant. This forces the LLM to make deliberate relevance judgments rather than accumulating noise through passive collection. After the exploration loop, a scoring pipeline converts the collected chunks into a ranked evidence list. The final ranking uses : each collected chunk is scored by cosine similarity between its embedding and the question embedding, with a small boost for chunks the LLM explicitly selected: final_score = min + boost , 1.0) where: - : cosine similarity via the FAISS index. Values in { }. - : +0.10 if the LLM called for this chunk during exploration. This is a binary signal: collected = +0.10, not collected = 0. Chunks are sorted by and the top k are returned as evidence. If the exploration loop collects fewer than k chunks, remaining slots are filled with pure vector search results (at a 0.9$ $ score discount) to ensure the output always contains k evidence chunks. Earlier iterations used a composite scoring formula blending vector similarity with entity-derived scores . However, ablation experiments showed that these scoring refinements, when applied to GraphRAG's fixed one-hop expansion, performance. The scoring formula only helped when paired with deeper LLM-driven exploration --- but even then, the improvement was modest compared to the contribution of exploration itself. This led us to simplify: RLM-on-KG's advantage is (finding chunks that vector search alone misses via multi-hop KG traversal), not ranking. Pure vector re-ranking --- the same strategy GraphRAG uses --- is the most effective way to select the final evidence from an expanded candidate pool. Our GraphRAG-local variant implements a five-step retrieval pipeline: .} : retrieve the top-8 most similar chunks to the query. : count entities mentioned in seed chunks. : for the top-12 entities, pull all chunks that mention them. : combine seed chunks and expansion chunks. : re-rank all candidates by vector similarity to the query and keep the top-20. This follows the local retrieval recipe from Edge et al.~ community detection, community summaries, or LLM-based entity extraction. These components were intentionally removed to create a controlled comparison that isolates query-time retrieval logic: both systems share identical offline indexing (same chunking, same spaCy NER, same embedding model, same FAISS index). The resulting comparison supports claims about live query-time exploration versus a controlled local graph-retrieval pipeline, not about RLM-on-KG versus the full GraphRAG system as generally understood. We use the label ``GraphRAG-local variant'' rather than ``GraphRAG'' throughout to make this scope explicit. { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Aspect & { } GraphRAG-local variant & { } RLM-on-KG \\ Offline LLM indexing & None & None \\ Query-time LLM use & None & 10 to 25 tool-use calls \\ Exploration depth & Fixed: 1 hop & Adaptive: 1 to 12+ hops \\ Candidate discovery & Entity expansion + vector seed & LLM-driven multi-hop KG traversal \\ Final ranking & Vector similarity only & Vector similarity + 0.10 collection boost \\ Failure mode & Misses chunks not reachable in 1 hop & Exploration drift \\ } RLM-on-KG is implemented on top of the WordLift Knowledge Graph, an RDF-based knowledge graph used to publish, query, and maintain entity-centric data for web content and product catalogs. The WordLift KG provides RDF storage and reasoning-compatible identifiers, entity resolution and canonical IRIs, and graph-aware retrieval primitives used by the navigator tools. All navigator tools are thin wrappers over WordLift KG capabilities: entity lookup and fuzzy matching , entity-to-evidence expansion via links , neighborhood expansion through co-mention graphs , and provenance-preserving chunk retrieval . The same KG layer also supports downstream use cases beyond retrieval, including entity page generation, structured data publishing , and monitoring of data quality constraints. The tool is fully specified and deterministic. It does not use the LLM and is shared across all systems (RLM-on-KG, GraphRAG-local variant, heuristic RLM). Run spaCy on the question text to extract named entity spans. This is the same spaCy model used during offline KG construction. For each extracted entity span, look up exact matches in the KG's label index . Labels are case-insensitive and whitespace-normalized. If fewer than 3 entities are found via exact match, perform fuzzy matching using against all KG entity labels. Threshold: $ $90. Top-k: 20 per target. Return matched entity URIs sorted by chunk count , capped at . The tool is deterministic for a given question and KG state. It introduces no query-time advantage for RLM-on-KG that is unavailable to the baselines: The GraphRAG-local variant uses the same function to identify seed entities in step 2 of its pipeline. Beyond retrieval performance, RLM-on-KG exploration traces offer a natural substrate for diagnosing the quality of the underlying graph. Each question produces a complete exploration trace (seed entities, visited neighborhoods, collected chunks, stall events, and fallback usage). We propose treating these traces as diagnostics for KG quality along four dimensions. We present this as a ; validating it through concrete improvement cycles (diagnosis $ $ fix $ $ re-evaluation) is future work. Missing entities, aliases, or weak typing are indicated when fails to surface relevant seeds or when gold evidence is only found through vector backfill. Unreachable evidence and low reachability within a small hop budget indicate missing links (e.g., absent aliases or coreference) or over-pruned neighborhoods. Conversely, degree explosions in reveal noisy hubs that increase drift risk. Repeated retrieval of topically similar but non-supporting chunks indicates issues with chunk boundaries, missing sentence-level anchoring, or low-precision mention links. High hop counts and repeated tool calls for common primitives indicate missing indices or insufficient caching. From the same logs, we define KG health metrics that require no additional manual labeling: { {@{} >{ }p{ * } >{ }p{ * }@{}} { } Metric & { } Definition \\ Seed hit rate & Fraction of questions where a seed entity links to $ $1 gold chunk \\ Reachability@h & Fraction of gold chunks reachable within h co-mention expansions \\ Hop efficiency & Gold chunks found per tool call \\ Neighborhood noise & Median neighbors returned by vs.~neighbors used \\ Backfill reliance & Fraction of final top-k filled by vector backfill \\ Evidence redundancy & Average entity-overlap across selected chunks \\ } These diagnostics suggest a closed-loop improvement process: run questions , categorize failures , apply targeted KG fixes (aliasing, merge/coreference, chunking adjustments, edge weighting, additional relation extraction where available), and re-run evaluation to quantify improvements in KG health metrics and retrieval F1. We propose this as a natural workflow but have not yet validated it through a concrete improvement cycle; doing so is future work. In this sense, RLM-on-KG functions not only as an adaptive retriever, but also as a proposed stress-test harness for the knowledge graph. {0.5pt} We evaluate on , a benchmark of 20 fiction novels containing 2,010 questions designed to test scenarios where graph structure aids retrieval. Question types and counts across the full benchmark: { {@{}lll@{}} Type & Count & Description \\ Fact Retrieval & 971 & Locate specific facts \\ Complex Reasoning & 610 & Multi-hop reasoning \\ Contextual Summarization & 362 & Summarize themes or relationships \\ Creative Generation & 67 & Open-ended creative tasks \\ } Due to cost constraints ( 50K tokens per question for RLM-on-KG), we evaluate on a containing 519 questions. The subset consists of the first 5 novels in the benchmark's alphabetical ordering . No questions were excluded or cherry-picked within these novels; all 519 questions for the 5 novels are included. This subsetting strategy avoids selection bias while keeping evaluation costs manageable 26 million tokens total). We report the evaluation subset size (519 of 2,010 = 26\ limitation; extending to the full 20-novel benchmark is future work. To test whether our findings generalize beyond fiction texts and larger shared-corpus KGs, we also evaluate on , a multi-hop decomposable QA benchmark constructed from single-hop questions (Natural Questions, SQuAD) composed into 2--4 hop chains. MuSiQue provides gold supporting paragraphs and distractor paragraphs per question. We build a from each question's paragraphs rather than a shared corpus KG, ensuring isolated evaluation with no cross-question leakage. We evaluate 50 questions sampled from the validation split. On a 2-novel subset : - 98\ at least one entity link - 100\ gold evidence chunks are reachable via entity links from at least one seed entity This high coverage confirms that the mention graph provides sufficient connectivity for multi-hop traversal to reach relevant evidence. GraphRAG-Bench provides sentence-level gold evidence annotations. Since our retrieval operates at the chunk level, we must map evidence sentences to chunk IDs. We use a two-stage mapping algorithm: For each gold evidence sentence, normalize whitespace and attempt case-insensitive substring containment against all chunk texts. If a chunk contains the evidence sentence verbatim, it is marked as a gold chunk. If substring matching fails (the evidence sentence spans a chunk boundary or is paraphrased), compute cosine similarity between the evidence sentence embedding and all chunk embeddings using the same index. Accept the top-k matches with similarity $ $ 0.25 as gold chunks. On the 2-novel subset (20 questions, 170 evidence sentences), we observe: { {@{}lll@{}} Mapping method & Sentences & \ Substring match & 0 & 0\ Semantic fallback & 170 & 100\ Unmapped & 0 & 0\ } All gold evidence maps exclusively through the semantic fallback path. This is consistent with the benchmark's construction process: GraphRAG-Bench uses an LLM-in-the-loop pipeline for question generation and evidence annotation, which can introduce reformulation, synonym substitution, or syntactic restructuring relative to the source text. Manual inspection of 10 evidence sentences confirms that each expresses the meaning of the corresponding source passage but differs at the character level , which is sufficient to prevent substring containment from firing. Manual inspection of 20 semantic matches reveals that high-similarity matches consistently identify the correct chunk, while low-similarity matches sometimes map to topically related but incorrect chunks. This is a limitation of our evaluation setup: the threshold of 0.25 trades precision for recall in gold labeling. Any noise in the gold set affects all systems equally and does not advantage RLM-on-KG over the GraphRAG-local variant. The head-to-head comparison remains valid under the same noisy gold labels; absolute F1 numbers should be interpreted with this caveat. The same model used for the semantic fallback gold assignment is also used for FAISS retrieval and vector re-ranking. This creates a shared representation space between gold labels and the retrieval mechanism. While this does not invalidate head-to-head comparisons (all systems are evaluated against the same labels), it limits confidence in interpreting absolute F1 values. Evaluating with an alternative gold-assignment method (e.g., cross-encoder scoring or substring-based assignment on a corpus with less paraphrased evidence) would strengthen absolute claims and is planned as future work. .} : pure dense retrieval with no entity expansion or graph structure. The simplest possible baseline, isolating the contribution of graph-based exploration. : the five-step pipeline described in Section 3.5. Uses deterministic spaCy NER (same as RLM-on-KG). No community detection, no community summaries. : the same GraphRAG pipeline but with the same composite scoring formula and MMR diversity selection as RLM-on-KG. This ablation isolates whether RLM-on-KG gains come from LLM exploration or from scoring refinements. : rule-based multi-hop traversal using the same KG, tools, and chunk budget as LLM RLM, but with fixed, deterministic tool selection rather than LLM-driven decisions. This is the paper's most direct control for testing whether LLM-driven tool selection outperforms rule-based traversal on the same graph and tool interface. Input: question Q, KG G, budget k , D Output: ranked evidence chunks E Seeds <- entity_search // NER extraction + fuzzy KG lookup frontier <- queue depth_map <- {s: 0 for s in Seeds} visited <- {}; collected <- {} stall <- 0 while frontier != {} and |collected| < k do entity <- POP_FRONT // breadth-first order if entity in visited then continue visited <- visited U {entity} chunks <- get_chunks_for_entity new_this_iter <- 0 for each chunk c in chunks do collected <- vector_score new_this_iter <- new_this_iter + 1 if depth_map < D then neighbors <- expand_neighbors for each n in neighbors do if n not in visited then APPEND depth_map <- depth_map + 1 if new_this_iter = 0 then stall <- stall + 1 else stall <- 0 if stall >= 2 then break // early stop: no new chunks if |collected| < k then backfill <- vector_search // gap fill collected <- MERGE return TOP_K , k) : , , , . The heuristic does call , , , or , as these require LLM calls and are exclusive to the LLM controller. : D = 3 , k = 20 (maximum chunks). Stopping triggers in priority order: frontier empty, k chunks collected, 2 consecutive BFS iterations with no new chunks. The heuristic F1 varies across sections (43.3\ because these report results on different question subsets: the 519-question primary set , a 519-question run with a freshly rebuilt FAISS index , and a 100-question cross-model subset . The heuristic implementation is identical across all runs; variation reflects different question distributions and independent FAISS index builds, not implementation changes. Precision, Recall, and F1 computed at the chunk level. Gold evidence chunks are identified via the mapping described in Section 4.3. We report per-question type breakdowns, head-to-head win/tie/loss counts, and 95\ Answer accuracy via LLM-judge rubric , grounding score (percentage of answer sentences supported by retrieved evidence), faithfulness, on-intent, citation coverage, and citation precision. We focus on chunk-level F1 as the primary metric because it isolates the contribution of exploration and scoring independent of generation variance. We report both query-time cost and offline indexing cost, separating retrieval from generation to ensure transparent comparison. { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Cost category & { } GraphRAG-local variant & { } RLM-on-KG \\ & Chunking + NER + vector index & Chunking + NER + vector index \\ & 0 & 10 to 25 \\ & 1 & 1 \\ & 0 & 48K \\ & 2K & 2K \\ & 2K & 50K \\ & 1 s & 2 to 5 min \\ } Both systems use identical offline indexing (same chunking, same spaCy NER model, same embedding model, same FAISS vector index), the same maximum retrieved chunks , the same generator model, and the same context window. The only difference is that RLM-on-KG spends additional LLM tokens at query time to explore the mention graph adaptively. The 48K retrieval tokens per question break down approximately as: tool results returned to the LLM ( 35K, dominated by chunk text from and ), LLM reasoning and tool-call generation , and system prompt with state injection . All experiments use the same KG construction and storage layer. The KG is exported as RDF and the chunk and entity identifiers are stable across runs, enabling deterministic re-evaluation of retrieval outputs. Tool calls and intermediate exploration states (visited entities, collected chunks, scores) are logged per question to support trace-level analysis and ablation studies. The LLM navigator uses Gemini 2.0 Flash with and default top-p.~No random seed is set, as the Gemini API does not expose seed control for function-calling mode. During development, we observed $ $2 pp F1 variance across repeated runs of the same configuration on a 50-question subset (V4 vs V6 in our optimization journal, same code, different API calls). At 519-question scale, variance is expected to be smaller due to averaging. We report only single-run results; repeated full-scale runs to estimate variance are planned as future work. The primary results use Gemini 2.0 Flash as the navigator. To test whether the navigation policy transfers across model families, we built a provider-agnostic tool-calling interface and evaluated the same KG, tools, and prompts with three additional controllers: Claude Haiku 4.5 (Anthropic, via Vertex AI), Gemini 2.5 Flash Lite , and Gemma 4 E2B (local via Ollama). Results are reported in Section 5.8. {0.5pt} We first validate the approach on a deterministic 20-question subset drawn from 2 novels. All questions, KG construction, and embeddings are fixed; only the retrieval logic varies between systems. This experiment is designed for regression detection, not significance claims. The following table shows how RLM-on-KG performance improved through successive engineering changes: { {@{}llll@{}} System & Precision & Recall & F1 \\ RLM-on-KG & 22.0\ RLM-on-KG & 25.0\ RLM-on-KG & The progression reveals two independent improvements: .} : prioritizing entity exploration over vector search surfaces more relevant chunks that the LLM can then score. : replacing with the composite blend preserves entity exploration signal in the final ranking, boosting chunks the LLM discovered through graph traversal. Importantly, the v3 system reported here uses composite scoring, which helped in these early iterations because it compensated for weaker discovery . As we show in Section 5.2, once discovery improved through better prompting and tool design , composite scoring became redundant or harmful. The reported at scale in Section 5.4 uses pure vector re-ranking , not composite scoring. The smoke-test progression is historical context that motivated the simplification. Bootstrap 95\ : { {@{}lll@{}} System & Mean F1 & 95\ GraphRAG local & 31.6\ RLM-on-KG & 35.2\ & & $-$2.0 pp, +9.7 pp{]}} \\ } Paired bootstrap test: p=0.49 . The difference is not statistically significant at n=20, as expected. Head-to-head: . We treat this smoke test as directional evidence and validate at scale in Section 5.2. To determine whether the F1 gains come from LLM exploration or from scoring refinements , we compare four systems using identical KG, questions, and gold labels: { {@{}llll@{}} System & Precision & Recall & F1 \\ Vector-only top-20 & 21.0\ GraphRAG local & 28.0\ GraphRAG + composite + MMR & 18.8\ RLM-on-KG & { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Transition & { } $ $F1 & { } What changes \\ Vector-only $ $ GraphRAG local & +2.0 pp & Entity expansion \\ GraphRAG local $ $ + composite + MMR & $-$6.8 pp & Composite scoring + MMR \\ GraphRAG + composite + MMR $ $ RLM & +10.0 pp & LLM-driven exploration \\ Vector-only $ $ RLM & +5.2 pp & All components \\ } Composite scoring and MMR diversity, when applied to GraphRAG's fixed one-hop expansion, actually performance . This is because the composite formula blends in entity scores that, for GraphRAG's shallow expansion, are not discriminative enough to improve on pure vector re-ranking. The +10.0 pp delta from GraphRAG+composite+MMR to RLM conflates two effects: removing the harmful composite scoring and adding LLM-driven exploration. The actual contribution of LLM exploration over the best non-LLM system (GraphRAG local at 32.0\ 20-question smoke test, which is suggestive but not statistically significant at this sample size. The definitive comparison between LLM-driven and rule-based traversal is reported at full scale in Section 5.4. { {@{}lllll@{}} Type & n & RLM F1 & GR F1 & Delta \\ Complex Reasoning & 7 & 35.3\ Contextual Summarization & 2 & 55.2\ Fact Retrieval & 11 & 31.5\ } RLM-on-KG shows the largest gains on Fact Retrieval , where entity-driven exploration surfaces chunks containing specific facts that vector search alone misses. Complex Reasoning is roughly tied, suggesting that both systems reach similar evidence for multi-hop questions when the hop depth is shallow. The Contextual Summarization sample is too small for reliable conclusions. We validate the approach at full scale on a 5-novel subset of GraphRAG-Bench Novel containing 519 questions. The same KG construction, embedding index, and evaluation pipeline are used as in the smoke test; only the question set changes. { {@{}llll@{}} System & Precision & Recall & F1 \\ Vector-only & 27.6\ GraphRAG-local variant & 41.6\ RLM-on-KG & Head-to-head : (win rate 52.5\ significantly outperform the vector-only baseline (+7.4 pp and +7.2 pp respectively), demonstrating the absolute necessity of graph navigation for recovering scattered context. Bootstrap 95\ : { {@{}lll@{}} System & Mean F1 & 95\ GraphRAG-local variant & 45.6\ RLM-on-KG & 45.8\ & & $-$0.72 pp, +1.05 pp{]}} \\ } The paired bootstrap p-value is 0.36 . While the overall delta is small and not statistically significant, the head-to-head record shows that RLM-on-KG provides consistent marginal gains across questions rather than winning big on a few outliers. { {@{}llllll@{}} Type & n & RLM F1 & GR F1 & Delta & W/T/L \\ Fact Retrieval & 255 & 39.0\ Complex Reasoning & 156 & 55.2\ Contextual Summarization & 93 & 50.4\ Creative Generation & 15 & 34.6\ } RLM-on-KG shows the strongest gains on (+0.7 pp, 55 wins vs 39 losses), where entity-driven traversal surfaces chunks containing specific facts scattered across the corpus that vector search alone misses. Complex Reasoning is the one category where GraphRAG holds a slight edge , likely because broad multi-hop exploration introduces noise for questions with concentrated answer evidence. On average, the LLM explored 26.7 entities and used 10.3 hops per question. This is more targeted than the 30+ entity explorations observed in earlier iterations that used automatic chunk collection --- removing auto-collection naturally constrained exploration breadth. The heuristic RLM baseline uses the same mention graph, same tools, and same chunk budget as LLM RLM, but follows fixed traversal rules instead of LLM-driven tool selection. This comparison isolates the value of the LLM as a control policy. { {@{}llll@{}} System & Precision & Recall & F1 \\ Vector-only & 27.6\ & 39.6\ & 41.5\ & 5.4 and 5.8.1 arises because 5.8.1 is a separate experiment run for the Claude arm with a freshly rebuilt FAISS index; cumulative floating-point variance across multiple independent rebuilds produces slightly larger drift. Neither gap affects head-to-head comparisons, which are computed within the same run.} Head-to-head : (win rate 63.3\ Bootstrap 95\ : { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Comparison & { } Mean $ $F1 & { } 95\ & { } p-value \\ & & +1.41, +3.56{]}} & 0.0001} \\ LLM $-$ GraphRAG & +0.16 pp & { } & 0.36 \\ Heuristic $-$ GraphRAG & $-$2.12 pp & --- & --- \\ } . Crucially, heuristic traversal alone GraphRAG-local by $-$2.12 pp, demonstrating that multi-hop traversal per se is insufficient --- the LLM controller is what makes it competitive. { {@{}llllll@{}} Type & n & LLM F1 & Heur F1 & $ $F1 & W/T/L \\ Fact Retrieval & 255 & 39.0\ Complex Reasoning & 156 & 55.2\ Contextual Summarization & 93 & 50.4\ --- \\ Creative Generation & 15 & 34.6\ } { {@{}llllll@{}} Scatter & n & LLM F1 & Heur F1 & $ $F1 & Win Rate \\ 1--5 chunks & 219 & 38.9\ 6--10 chunks & 193 & 54.6\ 11+ chunks & 107 & 43.9\ } The LLM controller's advantage is , where graph structure is complex enough to reward adaptive navigation but not so scattered that both systems struggle. The advantage holds across all scatter levels, confirming that LLM-driven tool selection consistently outperforms fixed rules. The most compelling subgroup is Complex Reasoning with 11+ gold chunks : the LLM controller achieves +4.55 pp over heuristic with a 71\ win rate . This is where the adaptive navigation policy provides the clearest value --- when the question demands multi-hop reasoning across scattered evidence, rule-based traversal is insufficient. Taken together, the scatter analysis shows that the heuristic comparison peaks at moderate scatter overall , while the hardest high-scatter subgroup shows the clearest adaptive-navigation benefit (+4.55 pp, 71\ the heuristic, and a 62\ anchors the same story against a stronger baseline. These are complementary views of one principle: adaptive control adds value proportionally to the structural complexity of the required evidence path. These results support a nuanced thesis: the LLM controller is not universally superior, but a conditional advantage. For simple questions with concentrated evidence, heuristic traversal (or even pure GraphRAG) suffices. The LLM controller adds value precisely when graph structure demands adaptive reasoning --- choosing which paths to explore, which to abandon, and when enough evidence has been gathered. ]{figures/f1_by_question_type.png}} ]{figures/f1_delta_distribution.png}} ]{figures/exploration_vs_f1.png}} The overall F1 delta masks a more nuanced story. To understand benefit most from KG-guided exploration, we cross-tabulate RLM's win rate by question type and --- the number of gold evidence chunks a question requires. { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Type & { } Gold 1--5 & { } Gold 6--10 & { } Gold 11+ \\ Fact Retrieval & 57\ Win rate = RLM wins $ $ , excluding ties. Two patterns emerge: .} When the gold evidence is distributed across 11+ chunks, RLM-on-KG's win rate rises to 56\ +1.38 pp.~For Fact Retrieval with 11+ gold chunks, the win rate reaches 100\ traversal follows entity links to discover chunks that are semantically distant from the query but structurally connected --- precisely the chunks that vector search misses. Fact Retrieval benefits most because fact-bearing chunks are often entity-specific . Complex Reasoning benefits when evidence is scattered (69\ at 11+ chunks) but suffers when evidence is concentrated (33\ chunks), where over-exploration adds noise. The n=6 result for high-scatter Fact Retrieval (100\ be interpreted with caution due to the very small sample size. { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Characteristic & { } RLM excels & { } GraphRAG excels \\ Evidence distribution & Scattered & Concentrated \\ Required traversal & Multi-hop entity chains & Single-hop or direct similarity \\ Question structure & ``What happens across'', ``Trace the'' & ``What does X say about'' \\ Entity involvement & Multiple entities co-mentioned & Single entity or no entity needed \\ RLM recall advantage & +1.2 pp & $-$0.7 pp \\ } When RLM wins, the recall gap is the driver: RLM achieves 61.9\ vs GraphRAG's 45.8\ discovers relevant chunks that similarity search cannot reach. When RLM loses, the pattern reverses --- over-exploration introduces irrelevant chunks that dilute precision without improving recall. ]{figures/win_rate_heatmap.png}} ]{figures/recall_delta_vs_scatter.png}} To illustrate the behavioral difference between LLM-driven and heuristic traversal, we present two representative cases from the 519-question evaluation: one where the LLM controller wins decisively, and one where it fails. The heuristic baseline explored 27 entities in 2 hops but saturated at hop 1, achieving F1 = 0.348. The LLM controller explored 32 entities across 12 hops , achieving F1 = 0.609 . The mechanism: after the initial entity expansion covered nearby evidence, the LLM pursued indirect entity connections that the heuristic's breadth-first strategy did not prioritize. This additional exploration surfaced 8 gold chunks that the heuristic missed --- chunks semantically distant from the query but structurally connected through the mention graph. GraphRAG achieved F1 = 0.522 on this question, showing that even its 1-hop expansion missed the most distant evidence. The heuristic baseline explored 26 entities in 2 hops and achieved F1 = 0.800. The LLM controller explored 54 entities across 12 hops and achieved F1 = 0.400 . The failure mode: the LLM followed tangentially related entity paths that introduced non-gold chunks into the candidate pool, diluting precision without improving recall. The heuristic's early saturation was actually advantageous --- the correct evidence was concentrated near the seed entities, and broader exploration only added noise. GraphRAG achieved F1 = 0.800, confirming that 1-hop expansion was sufficient. These cases illustrate the conditional advantage thesis: the LLM controller wins when evidence is scattered across entity neighborhoods that require adaptive, multi-step navigation to reach. It loses when evidence is concentrated and the exploration budget is better spent on precision than breadth. The heuristic's 2-hop / early saturation behavior is a feature, not a bug, for concentrated evidence; the LLM's 12-hop / deep exploration is essential for scattered evidence. To test whether the LLM controller advantage transfers across model families, we run the complete 519-question evaluation with Claude Haiku 4.5 as the controller. The same shared KG, tools, system prompt, and evaluation pipeline are used as in the primary Gemini results . { {@{}lccc@{}} System & Precision & Recall & F1 \\ Vector-only & --- & --- & 38.4\ GraphRAG-local variant & --- & --- & 44.8\ Heuristic RLM & --- & --- & 42.8\ & --- & --- & Head-to-head : (win rate 73.2\ Head-to-head : (win rate 65.4\ Bootstrap 95\ : { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Comparison & { } Mean $ $F1 & { } 95\ & { } p-value \\ & & +3.33, +5.44{]}} & 0.001} \\ & & +1.51, +3.33{]}} & 0.001} \\ } With Claude Haiku as controller, RLM-on-KG not only beats the heuristic baseline (+4.37 pp, p 0.001) but also . The Gemini primary results showed a non-significant advantage over GraphRAG (+0.16 pp, p=0.36); with Claude, the advantage is both larger and statistically significant, confirming that the LLM controller's value scales with tool-calling capability. To map the relationship between controller capability and retrieval performance, we compare three model families on 100 questions: { {@{}lcccc@{}} Controller & Heuristic F1 & LLM F1 & $ $ & Latency \\ & 0.376 & & & \\ Gemini 2.5 Flash Lite & 0.371 & 0.380 & +0.84 pp & 68 s \\ Gemma 4 E2B & 0.350 & 0.343 & $-$0.78 pp & 237 s \\ } {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Controller & { } Mean $ $ & { } 95\ & { } p-value \\ & & +2.16, +5.18{]}} & 0.001} \\ Gemini 2.5 Flash Lite & +0.84 pp & { } & 0.52 \\ Gemma 4 E2B & $-$0.78 pp & { } & 0.45 \\ } .} : Claude Gemini Gemma E2B . This ordering is consistent across both evaluation sets . across controllers: Gemma 4 E2B averages 237 s per question; Gemini 2.5 Flash Lite averages 68 s. Claude latency was not reliably recorded for this run (see footnote above). The spread across reliably measured controllers is roughly 3.5$ $. Per-question cost (estimated at 50K tokens/question, 80\ { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Controller & { } Delta & { } \$/question & { } Latency & { } Delta per dollar \\ Claude Haiku 4.5 & +3.60 pp & \ & & \\ Gemini 2.5 Flash Lite & +0.84 pp & \ & & \\ Gemma 4 E2B & -0.78 pp & \$0.000 & 237 s & n/a \\ } For context, full GraphRAG offline indexing (LLM entity extraction + community summaries on 20 novels) costs an estimated \$15 as a one-time cost, amortized across all subsequent queries on a stable corpus. RLM-on-KG with Gemini on 519 questions costs \$3.11 total in query-time tokens --- a favorable where the offline index must be refreshed frequently and its cost cannot be amortized. For stable corpora with many repeated queries, GraphRAG-local remains more cost-efficient. The cross-model results reveal that the LLM controller advantage is not a property of any specific model but of . The monotonic ordering (Claude Gemini Gemma) and the Gemma behavioral tie rate establish a clear spectrum: models with strong function-calling and multi-turn reasoning exhibit adaptive exploration; models with weaker tool-calling default to heuristic-like patterns. This behavioral gap motivates a distillation approach . To test whether the LLM controller advantage scales to a different graph structure and domain, we evaluate on MuSiQue , a multi-hop decomposable QA benchmark. Each question's supporting and distractor paragraphs are used to construct a rather than the shared corpus KG used for Novel . This structural difference is key: smaller per-question KGs have fewer hub entities and lower co-mention density, which limits the LLM controller's structural-navigation advantage. { {@{} >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * } >{ }p{ * }@{}} { } Controller & { } GraphRAG F1 & { } Heuristic F1 & { } LLM F1 & { } $ $ & { } Latency \\ & 0.355 & 0.311 & & & 20 s \\ Gemini 2.5 Flash Lite & 0.356 & 0.311 & 0.324 & +1.33 pp & 27 s \\ } .} over heuristic traversal on MuSiQue, confirming the LLM controller advantage is not unique to the Novel benchmark. , consistent with the Novel results. The than Novel , which is expected: MuSiQue per-question KGs contain only 20 chunks with a high distractor ratio, making retrieval harder. Claude's MuSiQue delta is 62\ pp). This is exactly what the paper's theory predicts: on smaller, lower-hub graphs with fewer co-mention chains, the LLM controller's structural- navigation advantage has less room to operate. We interpret this as cross-scale attenuation, not cross-benchmark validation of identical gains. ($-$2.8 pp for Claude, $-$3.2 pp for Gemini). This is a scope limitation of the main claim: the +2.42 pp vs GraphRAG advantage established for Claude at n=519 does not transfer to 20-chunk per-question KGs. The GraphRAG-local pipeline's 1-hop entity expansion is better matched to the small, distractor-dominated graph structure of MuSiQue, while the LLM controller's deeper exploration introduces more noise when the graph has few meaningful hops to exploit. {0.5pt} RLM-on-KG is most effective for questions that require reaching chunks not directly similar to the query but connected through entity relationships. At full scale, the largest per-type gain is on , where entity-driven traversal surfaces chunks containing specific facts scattered across the corpus. The largest single-question improvements occur when the gold evidence is distributed across chunks mentioning different entities involved in the same event or relationship. This ability to traverse structural links to find distant evidence aligns with findings in other domains where graph reasoning surfaces less obvious connections. For example, Buehler used path sampling over a knowledge graph to reveal structural parallels between biological materials and Beethoven's 9th Symphony. Similarly, our results show that RLM-on-KG excels precisely when the required evidence is highly scattered and unattainable by vector search. This makes RLM-on-KG particularly suited to: - Multi-hop questions requiring evidence from entity intersections - Fact retrieval across narratively distant text passages - Evolving corpora where reindexing costs make offline pipelines impractical - Applications requiring provenance: each retrieved chunk links to its source entity and document via RDF identifiers, supporting citation and auditability The GraphRAG-local variant excels in low-latency settings. Its heuristic retrieval completes in under 1 second with zero LLM calls, making it suitable for interactive applications. It also benefits from amortized cost: once the vector index is built, repeated queries on a stable corpus are essentially free. For simple single-hop questions where the answer appears in a chunk directly similar to the query, the additional exploration of RLM-on-KG provides no benefit and adds unnecessary latency and cost. The two approaches are complementary: the balanced head-to-head record confirms that neither system dominates overall. The more informative comparison is LLM vs.~Heuristic RLM , which shows that the LLM controller consistently adds value. The advantage is conditional: for questions whose gold evidence is scattered across 11+ chunks, the LLM controller's win rate over heuristic traversal is 62\ If GraphRAG is a static expansion graph, RLM-on-KG is a --- one that activates selectively when the retrieval problem demands structural navigation. A practical deployment could use the GraphRAG-local variant as a fast first pass, then invoke RLM-on-KG when scatter indicators suggest the answer requires evidence from multiple entity neighborhoods. The most important architectural lesson from our iterative optimization is the separation between and . Early iterations attempted to use entity-derived scores, co-mention boosts, and MMR diversity penalties to improve ranking. These refinements either had no effect or performance. The breakthrough came from simplifying: let the LLM's multi-hop mention-graph traversal discover a broad pool of candidate chunks, then rank them with the same pure vector similarity that GraphRAG uses. This insight --- that LLM exploration adds value through discovery, not judgment --- suggests that future work should focus on expanding the quality of the candidate pool (e.g., via cross-encoder re-ranking) rather than complex scoring formulas. Separate discovery from ranking; let the LLM explore but let the vectors decide. The controller continuum (vector-only $ $ GraphRAG-local $ $ heuristic RLM $ $ LLM RLM) suggests a practical deployment strategy: . Use the cheapest sufficient controller for each question. GraphRAG-local handles 53\ LLM controller . For the remaining 47\ indicators could trigger escalation to LLM-guided traversal. A learned routing policy trained on exploration traces could automate this decision, reducing average cost to near GraphRAG-local levels while preserving LLM-level quality on hard questions. The cross-model results reveal that the gap between Gemma 4 E2B (83\ behavioral tie rate) and Claude is not architectural but behavioral: both use the same tools, but Claude makes adaptive exploration decisions while Gemma defaults to shallow patterns. Each question produces a complete trajectory that can serve as training signal. Concretely: : Claude exploration traces on MuSiQue and Novel provide 519 trajectories of multi-turn tool-calling sequences, with per-question F1 as reward signal. : Gemma 4 E2B or E4B, fine-tuned with TRL to close the behavioral gap. : The same traces can train a lightweight classifier to predict when LLM exploration will outperform heuristic traversal, enabling selective escalation without running the full LLM loop. This positions the current work not just as a retrieval system but as a for training smaller, deployable KG navigators. Effective use of these traces also requires that target models manage their context window across multi-turn exploration sequences; techniques for teaching LLMs to manage their own context are directly applicable to this training regime. The current per-question cost is a significant limitation but not a fundamental barrier. Several reduction strategies are available: : and results can be cached across questions sharing the same seed entities, reducing redundant KG lookups. : Capping by node degree and filtering by edge relevance prevents exploration drift into densely connected but uninformative entity neighborhoods. : Monitoring composite score growth across turns and terminating when marginal improvements fall below a threshold can halve the average turn count with minimal F1 impact. : Using a smaller, faster model for navigation and tool selection, and a stronger model only for final answer generation, can reduce latency and cost by an estimated 3--5$ $. : RLM-on-KG requires 2 to 5 minutes per question (10 to 25 LLM calls), compared to sub-second retrieval for the GraphRAG-local variant. This makes it unsuitable for real-time interactive applications in its current form. : at approximately 50K tokens per question, RLM-on-KG is roughly 25$ $ more expensive than the GraphRAG-local variant on a per-query basis. : the LLM may follow entity paths that lead away from relevant evidence. Our stall detection mitigates this but does not eliminate it. Complex Reasoning questions show a slight negative delta , likely because broad exploration introduces noise for questions with concentrated evidence. : All gold evidence maps through semantic similarity using the same embedding model as retrieval, introducing noise at low similarity scores and a shared-representation confound. While head-to-head comparisons remain valid, absolute F1 values should be interpreted with caution (Section 4.3). : On 519 questions, the overall RLM vs GraphRAG delta is not statistically significant . The LLM vs Heuristic delta statistically significant , supporting the core architectural claim. : The GraphRAG-Bench Novel corpus consists of public-domain fiction texts that are likely present in the training data of Gemini 2.0 Flash. Since RLM-on-KG uses this model at query time for navigation decisions, parametric knowledge could influence tool selection in ways that do not generalize to novel corpora. Importantly, the LLM cannot fabricate evidence --- it can only collect chunks that exist in the KG --- and final ranking uses pure vector similarity, not LLM judgment. Nevertheless, evaluation on non-public-domain corpora would strengthen the external validity of these results. : Our GraphRAG-local variant intentionally removes community detection, community summaries, and LLM-based entity extraction to isolate retrieval logic. This is not a comparison with the full GraphRAG system; performance against the complete pipeline may differ. : Our graph captures entity mentions, not typed semantic relations. Systems with richer relation extraction may achieve better performance on questions requiring relation-level reasoning. : The primary results are on a single benchmark focusing on fiction texts. Cross-scale validation on MuSiQue shows that the LLM-over-heuristic delta transfers but attenuates ( 62\ smaller on 20-chunk per-question KGs); evaluation on scientific, medical, and legal domains remains future work. : We evaluate three model families in Section 5.8. The LLM controller advantage holds for API-served models with strong tool-calling capabilities but not for local models with weaker function-calling behavior. Coverage of additional model families remains future work. : The cross-model experiments (Section 5.8) use n=100 questions and n=50 , smaller than the primary n=519 evaluation. Bootstrap confidence intervals are not computed for these subsets. : For current locally-runnable open-source models , LLM control is net negative ($-$0.78 pp, 53\ is a deployment boundary, not only a distillation opportunity: practitioners deploying on-device KG navigation should default to the heuristic baseline until model tool-calling capability matures or distilled navigators are available. {0.5pt} We have studied when an LLM controller outperforms rule-based heuristic traversal for knowledge graph exploration. The answer is: . On GraphRAG-Bench Novel , LLM-driven traversal achieves +2.47 pp F1 over a heuristic baseline using the same graph and tools (p 0.0001). The gain is largest at moderate scatter (+3.21 pp at 6--10 gold chunks; +4.55 pp on high-scatter Complex Reasoning, 71\ win rate) and smallest for concentrated evidence . Heuristic multi-hop traversal alone single-hop expansion, confirming that the value lies in the LLM's adaptive decisions --- not in multi-hop traversal per se. Cross-model validation reveals that the advantage is a function of . At matched scale , the ordering is: Claude Haiku 4.5 Gemini 2.5 Flash Lite Gemma 4 E2B . Claude is the only controller to achieve a statistically significant advantage at this scale. For context, Claude's full-scale result (n=519, +4.37 pp over heuristic and +2.42 pp over GraphRAG, both p 0.001) uses the primary Gemini 2.0 Flash experiments as the comparison baseline; the +2.47 pp Gemini number in the abstract and Section 5.5 is from n=519 with Gemini 2.0 Flash, not the n=100 cross-model run. These numbers come from different experiments and should not be read as a single monotonic ladder without that caveat. The Claude result is the paper's strongest: it is the first controller to . Cross-scale results on MuSiQue show cross-scale attenuation ( 62\ KGs) consistent with the theory. The Gemma 4 E2B result establishes both a deployment boundary and a concrete distillation target: exploration traces from strong controllers can serve as training signal for fine-tuning smaller models via TRL, enabling on-device KG navigation without API dependencies. The core architectural insight is the separation of from : let the LLM explore the mention graph to assemble a broad candidate pool, then rank with pure vector similarity. This resonates with recent work on adaptive computation depth , where looped transformer models allocate more iterations to complex inputs. RLM-on-KG performs the analogous optimization over external structure: allocating more tool-calling iterations to questions whose evidence scatter demands deeper exploration. The approach is deployable on existing RDF knowledge graphs with entity-centric identifiers and provenance links, without additional offline indexing pipelines. Code and data are available at https://github.com/wordlift/rlm-on-kg. {0.5pt} Buehler, M. J. . Accelerating Scientific Discovery with Generative Knowledge Extraction, Graph-Based Representation, and Multimodal Intelligent Graph Reasoning. arXiv:2403.11996. Carbonell, J. and Goldberg, J. . The Use of MMR, Diversity-Based Reranking for Reordering Documents and Producing Summaries. SIGIR 1998. Edge, D. et al.~ . From Local to Global: A Graph RAG Approach to Query-Focused Summarization. arXiv:2404.16130. GraphWalks . GraphWalks: A Benchmark for Long-Context Graph Reasoning. OpenAI, github.com/openai/graphwalks. Released alongside GPT-4.1, April 2025. Guo, Z. et al.~ . LightRAG: Simple and Fast Retrieval-Augmented Generation. Findings of EMNLP 2025, pp.~10746--10761. Gutierrez, B. J. et al.~ . HippoRAG: Neurobiologically Inspired Long-Term Memory for Large Language Models. NeurIPS 2024. Izacard, G. and Grave, E. . Leveraging Passage Retrieval with Generative Models for Open Domain Question Answering. EACL 2021. Kontonis, V. et al.~ . Memento: Teaching LLMs to Manage Their Own Context. Microsoft Research. Lewis, P. et al.~ . Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS 2020. Ma, X. et al.~ . Query Rewriting for Retrieval-Augmented Large Language Models. EMNLP 2023. Trivedi, H. et al.~ . MuSiQue: Multihop Questions via Single-hop Question Composition. TACL 2022. Trivedi, H. et al.~ . Interleaving Retrieval with Chain-of-Thought Reasoning for Knowledge-Intensive Multi-Step Questions. ACL 2023. Xiang, Y. et al.~ . When to use Graphs in RAG: A Comprehensive Analysis for Graph Retrieval-Augmented Generation. arXiv:2506.05690. Accepted at ICLR 2026. Zhang, S., Kraska, T., and Khattab, O. . Recursive Language Models. arXiv:2512.24601. Zhu, R.-J. et al.~ . Scaling Latent Reasoning via Looped Language Models . arXiv:2510.25741. {0.5pt} The authors used Antigravity to assist with the research and the writing to improve clarity and readability. All content was carefully reviewed and approved by the authors, who retain full responsibility for the accuracy of the work and for any errors or omissions. 