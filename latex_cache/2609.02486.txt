 {article} {natbib} {arxiv_pkg} {inputenc} {fontenc} {hyperref} } {\@arabic{#1}} Marc Plantevit\\ EPITA Research Laboratory , FR-94276, Le Kremlin-Bicêtre, France\\ Julien Gallois\\ Lowit, FR-69003 Lyon, France\\ Céline Robardet \\ INSA Lyon, CNRS, LIRIS UMR 5205, F-69621 Villeurbanne, France\\ } {https://github.com/adrienmialland/ViSAR} { } #1 } Document Visual Question Answering often leverages Retrieval-Augmented Generation , where late-interaction encoders are commonly used to identify document pages relevant to a user query, before answer generation by a Large Vision-Language Model . Existing approaches typically retrieve a fixed top-$k$ number of pages regardless of query complexity, which increases LVLM latency and may degrade answer accuracy. We introduce ViSAR , a training-free adaptive-$k$ retrieval method for late-interaction visual document retrieval. ViSAR operates directly in the embedding space to construct a query-conditioned page-level similarity matrix that highlights query-relevant semantics and dynamically determines the number of pages to retrieve. Across multiple encoders and LVLMs, ViSAR retrieves compact, query-adapted page sets that reduce RAG latency by up to 58.7\ {ViSAR_vs_SOTA.pdf} Document Visual Question Answering aims to answer a user query about visually rich documents using page screenshots, and has attracted growing interest across many applications . These documents often include text, images, charts, or tables that blend into document-specific layouts, requiring methods capable of jointly understanding the query and heterogeneous visual content to localise relevant information and generate an answer. DocVQA is therefore commonly addressed using Large Vision-Language Models , which reason over textual and visual modalities jointly. Recent progress has demonstrated close-to-human performance in single-page DocVQA , but effectively processing multiple-page documents requires processing only the relevant subset of pages. In this setting, Retrieval-Augmented Generation emerges as a promising paradigm, where an encoder projects a query and document pages into a shared embedding space, and a retrieval step selects the most relevant pages for an LVLM to generate an answer. While late-interaction models have significantly improved page representation , determining how many pages should be retrieved remains an open challenge, despite its critical impact: too many pages increase LVLM latency and may introduce irrelevant context that degrades answer accuracy, whereas too few pages risk omitting relevant evidence. State-of-the-art retrieval methods rely on a late-interaction mechanism , were the encoder independently maps the query and pages into multi-vector representations. Page relevance is obtained by aggregating the best matches of each query embedding across page embeddings, enabling fine-grained semantic matching, offline page encoding, and scalable online retrieval. However, late-interaction produces independent page relevance scores. As a result, it relies on a fixed top-$k$ retrieval that cannot adapt to the query and may include irrelevant pages or omit relevant ones. Moreover, by treating pages independently and assigning uniform importance to embeddings, late-interaction does not exploit the semantic structure across pages . Text retrieval has mitigated this through embedding weighting based on token frequency statistics , or adaptive-$k$ retrieval , but these approaches rely on discrete token structures that do not extend to visual embeddings and often require additional training. In contrast, we show that late-interaction representations reveal sparse, query-dependent semantic content across pages, enabling dynamic localization and adaptive retrieval. We introduce ViSAR , a training-free retrieval mechanism that operates directly in the embedding space of late-interaction encoders. ViSAR constructs a query-conditioned page-level similarity matrix that highlights query semantics and enables adaptive-$k$ retrieval, dynamically determining the number of retrieved pages at inference time . We also show that the structure of the similarity matrix correlates with answer accuracy, suggesting a general principle for quality-aware document understanding. Our main contributions are as follows: A training-free adaptive-$k$ retrieval mechanism for visual retrieval using late-interaction encoders. A query-conditioned page-level similarity matrix whose structure reflects evidence localization and correlates with answer accuracy. ViSAR retrieves fewer pages than fixed top-$k$ retrieval on average and reduces RAG latency by up to 58.7\ Multi-vector retrieval via late-interaction was introduced by ColBERT for text documents. It represents queries and pages as independent sets of token-level embeddings, and page relevance is computed by aggregating query embeddings' best similarity score to page embeddings. Subsequent works improved its efficiency , introduced token weighting mechanisms based on frequency statistics, learned importance estimation, or sparse representations , and explored retrieval refinement through pseudo-relevance feedback . However, these approaches rely on Optical Character Recognition to extract the text content, cannot capture visual content, and often require additional training. Recent works such as ColPali extend late-interaction by encoding each page as a set of visual patch embeddings, emerging as the state-of-the-art paradigm for OCR-free visual document retrieval . However, it still relies on independent page scores and fixed top-$k$ retrieval, leaving the semantic organisation of pages unexplored. ViSAR instead exploits this structure to enable adaptive-$k$ retrieval. Adaptive retrieval has mainly been explored via iterative approaches, where LLMs or LVLMs reason over multiple rounds of fixed top-$k$ retrieval, making $k$ vary implicitly across iterations . However, adaptive-$k$ methods directly estimate the number of pages to retrieve in a single pass. Prior work determines the retrieval cut-off via heuristics applied to late-interaction scores by identifying the largest gap between consecutive scores or by clustering scores to detect transitions . However, these methods are text-based or require additional training, and rely solely on score distributions, while ViSAR performs training-free adaptive-$k$ retrieval by exploiting the semantic structure encoded in late-interaction representations. These advances in retrieval have directly influenced recent OCR-free DocVQA systems, where effective page selection is a key component of the reasoning pipeline combined with LVLM-based reasoning . VisRAG introduced a dedicated single-vector visual retriever, while M3DocRAG demonstrated the effectiveness of multi-vector late-interaction retrieval with ColPali. Other frameworks improve visual encoders, iterative retrieval, or LVLM reasoning. However, none exploit the semantic structure induced by late-interaction representations, which ViSAR addresses while remaining agnostic to the encoder and LVLM. {ViSAR_schema.pdf} depicts our method, ViSAR, which addresses visual retrieval in DocVQA. ViSAR enables adaptive-$k$ retrieval by weighting Query-to-Page, Page-to-Query, and Page-to-Page interactions without encoder retraining. It uses the MaxSim operator from late-interaction to exploit the fine-grained semantic structure in the embedding space and improve retrieval. Algorithm S1 summarizes the full pipeline. We denote a document $$ as a set of pages $ = \{ P^{1}, , P^{N} \}$. Each page $P^p$ and query $Q$ are denoted as $P^{p} = \{ v^{p}_j \}_{j=1}^{n_p}$ and $Q = \{q_i\}_{i=1}^{m}$, their respective sets of multi-vector representations in a common embedding space $^D$, such that $P^{p} ^{n_p D}$ and $Q ^{m D}$. The relevance score of $P^{p}$ to $Q$ is estimated via late-interaction between their sets of embeddings , summing each $q_i$ maximum similarity over all $v^{p}_j$, where $ \! = , $ defines the cosine similarity between two embeddings. S_{Q, P^p} = ^m_{i=1} _{j \{1, , n_p\}} \! Given the uniform contribution of $q_i$ and $v^{p}_j$ in , we estimate their importance directly from the embedding space. Since cosine similarity measures embedding alignments in a semantically structured space, the MaxSim operator over all $v^{p}_j$ captures how strongly the semantic encoded by $q_i$ is realized in $P^p$. We refer to $A_{p,i}$ as the score. Unlike late-interaction , which aggregates the activations over $q_i$ into a single score, we exploit the full activation matrix to identify discriminative query semantics. A_{p,i} = _{j \{1, , n_p\}} \! = _{j \{1, , n_p\}} q_i,\; v^{p}_j We use $A_{p,i}$ to derive weights for vectors $q_i$ and pages $P^p$ to highlight strong and spatially localized semantics. $A_{p,i}$ is rescaled by its mean across pages into $_{p,i}$, and modulated by its normalized inter-page standard deviation $_i$, yielding $_{p,i}$ . _{p,i} = } {_{p} \! } _i = _p \! } {_{i'} \! )} _{p,i} = _{p,i} _i Aggregating $_{p,i}$ over pages penalizes ubiquitous semantic content and highlights sparse activations, resulting in a weight $w_i$ for each $q_i$ . w_{i} = {1 + a_{i}}, a_{i} = _{p} _{p,i} We then compute a page-level semantic co-activation $C_{i,i'}^{p}$, which is normalized by the mean activation of each $q_i$ across pages, and a weight $w_p$ is obtained for each page by aggregating the mean co-activation of all $q_i$ . C_{i,i'}^{p} = _{p,i} _{p,i'} _i = _{p} w_{p} = ^m_{i=1} _{i'} Min-Max normalization is applied to $w_{i}$ and $w_{p}$, yielding $_{i}, _{p} $, which modulate the activations $_{p,i}}$ and weight the cosine similarity between all $q_i$ and $v^{p}_j$. The MaxSim operator is then applied over all $q_i$ to capture how strongly the semantic encoded by $v^{p}_j$ is realized in the query $Q$. We use this weighted maximum similarity as the relevance score $r^{p}_{j}$ of $v^{p}_j$. A weight $w^{p}_{j}$ is obtained for each $v^{p}_j$ of a page $P^p$ by centering and thresholding the relevance $r^{p}_{j}$ . r^{p}_{j} = _{i \{1, , m\}} w^{p}_{j} = \! ) A Min-Max normalization is applied to $w^{p}_{j}$, yielding $^{p}_{j} $, which modulates the importance of page embeddings and enables a similarity measure between pages. For a source page $P^p$ and target page $P^{p'}$, the cosine similarity between all $v^{p}_j$ and $_{j'}}$ is weighted by the target embedding weights $^{p'}_{j'}$. The MaxSim operator is then applied over all $_{j'}}$ to capture how strongly the semantic encoded by $v^{p}_j$ is realized in $}$. The resulting score is then weighted by the source embedding weight $^{p}_{j}$ to obtain $S^{p p'}_{j}$ . S^{p p'}_{j} = ^{p}_{j} _{j'} Finally, the similarity matrix $ ^{N N}$ is computed by averaging the $T$ largest interactions of $S^{p p'}_{j}$ across source embeddings $v^p_j$, denoted by $ \{1, ,n_p\}$, and taking the square root . Since each source embedding independently searches for its best match in the target page , this formulation naturally produces a directional page similarity, where $ $. = {T} _{j } S^{p p'}_{j} } We leverage the structure of $ $ to develop an adaptive-$k$ retrieval method that adapts to the query. The self-similarity $s_p = $ of a page is used to rank the pages and define a candidate relevant set $_k$, containing the $k$ highest-scoring pages. The remaining $N-k$ pages constitute the irrelevant set $_k$. For each page $p _k$, we compute its coherence with the relevant set, $c^{p}_{k}$, and the leakage from the irrelevant set, $l^{p}_{k}$, as the mean similarity to pages in $_k$ and $_k$, respectively . The scores $s_p$ are also normalized to yield $w_{s_p}$ summing to one, and combined with $c^{p}_k$ and $l^{p}_k$ in the cost function $ $. The optimal number of pages $k^ $ is obtained by minimizing $ $, requiring the evaluation of at most $N$ candidate sets, while an exhaustive search would require evaluating $2^N$ sets. c^{p}_{k} = _{p' _k} l^{p}_{k} = _{p' _k} = _{p _k} w_{s_p} The parameter $ > 0$ controls the leakage penalty. In addition, since the minimum $ $ may imply residual leakage from pages in $_{k^ }$, we evaluate whether $k^ $ corresponds to a sharp transition by accepting $_{k^ + 1}$ only if $$ varies more sharply beyond $k^ $ than before it, indicating a large drop in leakage caused by the transitioning page . We exploit the natural sparsity induced by ViSAR. produces a subset of inactive pages whose patch weights are all zero, which cannot contribute to the similarity matrix . These pages are excluded from the computation of and the optimization of $ $. is also evaluated block-wise rather than as a dense tensor, reducing peak memory usage. These implementation choices preserve mathematical equivalence while avoiding unnecessary computation. We evaluate three research questions: Can ViSAR effectively adapt the number of retrieved pages compared with fixed top-$k$ late-interaction retrieval, and how does this affect DocVQA accuracy? How does ViSAR's adaptive retrieval strategy impact latency? Can ViSAR's similarity matrix provide insights into answer quality? All experiments were conducted on an NVIDIA A6000 GPU and run once per configuration. We set $T=50$ in and $ = 10^{5}$ in . Further details on compute resources and sensitivity analyses for both hyperparameters are provided in the . MMLongBench and LongDocURL datasets were used, which provide the answer evidence pages for page ranking evaluation. They cover multiple scenarios to evaluate the answer generation, requiring textual and visual reasoning across pages, and efficient retrieval over multi-page documents. We evaluate ViSAR on multi-vector embeddings from three OCR-free visual encoders: ColPali , ColQwen2.5 , and ColModernVBERT . For broader comparison, we consider ColBERTv2 , a multi-vector text encoder operating on OCR-extracted text, and VisRAG-Ret , a single-vector visual encoder. Multi-vector embeddings use late-interaction with fixed top-$k$, whereas single-vector embeddings use cosine similarity with fixed top-$k$ . We also adapt two adaptive-$k$ heuristic methods originally proposed for late-interaction text retrieval to visual retrieval: a criterion and a approach . Using the late-interaction ranking, the Oracle adapts the number of retrieved pages by selecting the smallest top-$k$ that contains all evidence pages. It provides an upper bound on adaptive-$k$ under a standard and fixed ranking, which may include irrelevant pages. We examine the effectiveness of ViSAR's adaptive behaviour by reporting retrieval statistics, Precision, Recall, and F1-scores at rank $k^ $, and comparing ViSAR with Oracle, Largest-Gap, and Score-Cluster adaptive methods. We evaluate ViSAR's ranking quality against late-interaction ranking using Recall and normalized discounted cumulative gain at ranks 5 and 10. We then assess the impact of adaptive retrieval on answer generation using Qwen2.5-VL-7B-Instruct as the primary model, plus additional generation models with default configurations. All methods are evaluated under a maximum budget of Max-$k$ pages, i.e, $k=k$ for fixed top-$k$ retrieval. We report the answer accuracy, leveraging LLM-as-a-judge evaluation , with Qwen2.5-14B-Instruct LLM as the evaluator. It receives the query, the reference answer and the generated answer, and predicts a binary correctness label based on semantic equivalence, using few-shot examples and a constrained structured output. We then analyse how ViSAR latency scales with document size, and compare its retrieval and generation costs with fixed top-$k$, Largest-Gap, and Score-Cluster. Finally, we provide an ablation study to assess the contribution of ViSAR's components. $), medians , Recall@$k^ $ , Precision@$k^ $ , and F1@$k^ $. Oracle retrieves substantially more pages than the number of evidence pages, reflecting the known limitations of late-interaction ranking. Adaptive methods balance Recall and Precision by adjusting the number of retrieved pages, with ViSAR favouring more compact retrieved sets on average, avoiding unnecessarily large retrieval sets. Full distributions in Figure~S1--S2} {0mm} { }{ >{ }p{0.14 } >{ }p{0.15 } >{ }p{0.055 } >{ }p{0.050 } >{ }p{0.085 } >{ }p{0.085 } >{ }p{0.085 } >{ }p{0.055 } >{ }p{0.050 } >{ }p{0.085 } >{ }p{0.085 } >{ }p{0.085 } } & & {c}{} & {c}{} \\ {3-7} {8-12} & & $$ & $md$ & R@$k^ $ & P@$k^ $ & F1@$k^ $ & $$ & $md$ & R@$k^ $ & P@$k^ $ & F1@$k^ $ \\ ColQwen2.5 & Oracle & 8.3 & 2 & 100.0 & 67.30 & 80.45 & 10.7 & 3 & 100.0 & 61.07 & 75.83 \\ & Score-Cluster & 18.6 & 8 & 85.57 & 29.51 & 43.89 & 36.6 & 20 & 89.49 & 18.85 & 31.14 \\ & Largest-Gap & 15.4 & 2 & 81.12 & 45.13 & 58.00 & 28.7 & 3 & 82.09 & 45.15 & 58.26 \\ & ViSAR & 4.7 & 3 & 75.16 & 50.37 & 60.32 & 7.9 & 5 & 81.24 & 38.41 & 52.16 \\ ColPali & Oracle & 8.8 & 2 & 100.0 & 62.16 & 76.67 & 12.4 & 3 & 100.0 & 57.70 & 73.18 \\ & Score-Cluster & 18.7 & 8 & 82.64 & 26.12 & 39.70 & 35.8 & 18 & 87.56 & 19.15 & 31.43 \\ & Largest-Gap & 16.2 & 3 & 79.16 & 41.94 & 54.83 & 24.7 & 2 & 79.37 & 46.79 & 58.87 \\ & ViSAR & 5.3 & 3 & 73.57 & 46.11 & 56.69 & 8.1 & 6 & 80.16 & 34.38 & 48.12 \\ ColModern & Oracle & 10.5 & 2 & 100.0 & 58.87 & 74.11 & 13.0 & 3 & 100.0 & 53.35 & 69.58 \\ VBERT & Score-Cluster & 23.0 & 12 & 84.28 & 23.59 & 36.86 & 47.4 & 44 & 87.90 & 15.61 & 26.51 \\ & Largest-Gap & 20.0 & 4 & 80.57 & 36.81 & 50.53 & 39.3 & 4 & 83.57 & 34.94 & 49.28 \\ & ViSAR & 7.5 & 4 & 74.07 & 34.43 & 46.99 & 13.5 & 11 & 83.99 & 17.47 & 28.92 \\ {l}{evidence pages} & 1.9 & 1 & -- & -- & -- & 1.9 & 2 & -- & -- & -- \\ {k_diff_and_F1_score.pdf}\\ }$, using ColQwen2.5 on MMLongBench . : mean difference $k - k_{}$. ViSAR closely follows Oracle at low $k_{}$ and retrieves fewer pages as $k_{}$ increases, limiting irrelevant pages retrieval. Largest-Gap and Score-Cluster over-retrieve across most $k_{}$. : F1-score with the corresponding percentage of queries. ViSAR achieves higher F1, especially at lower $k_{}$.} {1mm} { }{ >{ }p{0.32 } *{4}{>{ }X} } & {c}{Recall} & {c}{NDCG} \\ {2-3} {4-5} & @5 & @10 & @5 & @10 \\ {l}{} \\ Late-Interaction & 75.00 & 84.49 & 0.730 & 0.746 \\ ViSAR & & & & \\ {l}{} \\ Late-Interaction & 78.60 & 86.82 & 0.780 & 0.793 \\ ViSAR & & & & \\ {l}{} \\ Late-Interaction & 72.79 & 83.14 & 0.704 & 0.723 \\ ViSAR & & & & \\ Preliminary experiments revealed numerical instabilities using ColModernVBERT on a few LongDocURL pages (7.31\ reports mean, median, Recall@$k^ $, Precision$@k^ $, and F1$@k^ $ across encoders for ViSAR, Oracle, Largest-Gap and Score-Cluster methods. The mean of Oracle shows that retrieving all evidence pages often requires significantly more pages than the number of evidence pages, while its lower median indicates that compact retrieval is sufficient for many queries. This reflects the known limitations of late-interaction ranking , which may struggle to capture all relevant pages for complex queries, requiring the retrieval of intermediate irrelevant pages that may lead to unnecessarily large contexts. Adaptive retrieval methods exhibit a similar pattern to Oracle but explicitly balance Recall and Precision, with ViSAR retrieving fewer pages than Oracle on average while Largest-Gap and Score-Cluster retrieve more. -a further characterises this behaviour as a function of $k_{}$. ViSAR closely follows Oracle at low $k_{}$ and retrieves fewer pages as $k_{}$ increases, limiting the inclusion of irrelevant pages that accumulate in complex queries. In contrast, Largest-Gap and Score-Cluster over-retrieve across most $k_{}$, consistent with their higher mean $k^ $ . Consequently, ViSAR tends to favour precision, while Largest-Gap and Score-Cluster tend to favour recall. Accordingly, dataset-level F1-scores show that ViSAR performs best on MMLongBench, while Largest-Gap performs best on LongDocURL. However, this aggregate metric depends on the query distribution over $k_{}$. When analysed across $k_{}$, ViSAR achieves higher F1-scores on MMLongBench , particularly for lower $k_{}$ where most queries are located. On LongDocURL , ViSAR outperforms Largest-Gap for most $k_{}$, except $k_{}=1$, suggesting that Largest-Gap is mostly effective for single-page queries, which constitute the majority of queries. This contributes to its higher dataset-level F1-score, whereas ViSAR provides a more consistent adaptive retrieval across query complexities. Overall , ColQwen2.5 shows the best results, followed by ColPali, while ColModernVBERT exhibits a weaker trend. confirms this trend, showing that ViSAR's adaptive behaviour is grounded in improved ranking, outperforming late-interaction ranking across Recall and NDCG at ranks 5 and 10, indicating that its weighting mechanism better aligns page scoring to the query. Yet, ColModernVBERT shows more modest improvements, consistent with its smaller size , which likely limits its ability to separate semantics, resulting in less compact sets and lower ranking performance overall. compares ViSAR with literature baselines, where Qwen2.5-VL-7B-Instruct LVLM and Qwen2.5-7B-Instruct LLM are used, respectively, as generation models for visual retrieval methods and text-based retrieval method . Max-$k$ denotes an input budget of at most $k$ pages, where fixed top-$k$ methods always use the full budget while adaptive methods only do so when required. All adaptive methods outperform fixed top-$k$ retrieval, with ViSAR achieving competitive or superior accuracy across all settings. Notably, Largest-Gap and Score-Cluster also improve over fixed top-$k$ despite their simplicity, indicating the inherent benefit of adaptive-$k$ retrieval. shows that ViSAR generalizes across encoders, maintaining or improving accuracy over late-interaction fixed top-$k$ : ColQwen2.5 and ColPali show a general trend toward improvement, with significance on LongDocURL. ColModernVBERT shows a non-significant trend toward fixed top-$k$, consistent with its weaker retrieval performance . Supplementary Tables S3--S7 confirm this trend using additional generation models: Across 60 configurations , ViSAR improves accuracy in 24 cases and maintains in the remaining 36. The largest improvements are observed for LVLMs more sensitive to longer contexts , consistent with ViSAR reducing both retrieved and irrelevant pages . ) with Qwen2.5-7B-Instruct. Max-$k$ denotes an input budget of at most $k$ pages. Bold indicates highest results, with confidence intervals and Adaptive-$k$ using other encoders in Table~S2. ViSAR achieves competitive or superior accuracy across settings.} {1mm} { }{ >{ }p{0.32 } *{4}{>{ }X} } & {c}{} & {c}{} \\ {2-3} {4-5} & Max-5 & Max-10 & Max-5 & Max-10 \\ {l}{} \\ ColBERTv2 & 24.51 & 24.70 & 47.18 & 47.70 \\ M3DocRAG & 34.86 & 35.08 & 59.31 & 58.71 \\ VisRAG-Ret & 34.48 & 35.69 & 57.29 & 58.02 \\ {l}{} \\ Largest-Gap & 35.79 & 35.88 & 61.01 & 60.89 \\ Score-Cluster & 36.25 & 35.97 & 60.00 & 59.83 \\ ViSAR & & & & \\ {1mm} { }{ >{ }p{0.32 } *{4}{>{ }X} } & {c}{} & {c}{} \\ {2-3} {4-5} & Max-5 & Max-10 & Max-5 & Max-10 \\ {l}{} \\ Fixed top-$k$ & 35.04 & 35.69 & 59.79 & 59.27 \\ ViSAR & 36.53 & 36.63 & & \\ {l}{} \\ Fixed top-$k$ & 34.86 & 35.08 & 59.31 & 58.71 \\ ViSAR & 35.42 & 35.88 & & \\ {l}{} \\ Fixed top-$k$ & 34.75 & 34.95 & 58.42 & 58.84 \\ ViSAR & 34.01 & 34.28 & 58.28 & 58.33 \\ The reduction in context size enabled by adaptive retrieval lowers LVLM processing cost. While fixed top-$k$ retrieval always maximises the LVLM input budget, ViSAR dynamically adjusts the number of pages and only uses the full budget when required. reports retrieval, generation, and end-to-end latencies, showing that ViSAR introduces a retrieval overhead that contributes only marginally, achieving a substantial reduction in generation latency that dominates the total cost. This result in an end-to-end latency reductions of up to 58.7\ Although ViSAR's overhead increases with document size , it only becomes noticeable for the largest MMLongBench document . In such cases, the supplementary material presents strategies that approximate the similarity matrix to preserve latency gains. {latency_RAG_ViSAR_vs_top-k.pdf}\\ {curve_heatmap.pdf}\\ $, with $k^ _{}$ and $k^ _{}$ marked. : A sparse matrix allows the query semantics to be effectively localized, which produces a sharp minimum in $ $, enabling accurate adaptive retrieval and correct answer generation. : A dense matrix yields a shallow minimum, causing over-retrieval and incorrect answer generation. : Answer accuracy as a function of the percentage of inactive entries in the similarity matrix, showing that higher sparsity consistently correlates with higher accuracy across encoders.} ViSAR's query-conditioned similarity matrix $ $ provides an explicit representation of how query-relevant information is distributed across a document. illustrates representative examples and shows that when query semantics are localised, $ $ is sparse and produces a sharp minimum in $ $, enabling a reliable retrieval boundary . Conversely, when query semantics are distributed across many pages, $ $ becomes denser, yielding a shallow minimum and a less reliable stopping decision . -c confirms this trend, showing that higher similarity matrix sparsity consistently correlates with higher answer accuracy across encoders on MMLongBench . While $ $ is designed to maximise coherence and minimise leakage within this structure, these results provide post-hoc validation that its components capture semantically meaningful retrieval properties. This suggests that analysing the structure in $ $ is a promising direction for understanding the conditions of retrieval success and failure, potentially enabling the conception of a label-free feedback signal for future retrieval strategies, such as iterative query refinement or evidence selection, without additional models or training. We evaluate simplified variants by disabling the query embedding weights $w_i$, page weights $w_p$, patch embedding weights $w^{p}_{j}$, and by replacing the optimization objective $ $ with fixed top-$k$, Largest-Gap, and Score-Cluster retrieval. Table~S10 shows that each modification degrades retrieval quality and downstream answer accuracy, indicating that both the weighting strategy and the optimization objective contribute to ViSAR's performance. We introduced ViSAR, a visual embedding weighting mechanism enabling for document visual question answering. It operates in the embedding space of late-interaction encoders to construct a query-conditioned page-level similarity matrix that drives compact and adaptive page selection, reducing RAG latency by up to 58.7\ Figure S1--S14 and Table S1--S10 are available in the supplementary material pdf. 