 {acmart} {fontenc} {5pt} {5pt} {2pt} {3pt} {5pt} {} {} {} {} {} {} {} {} {} {} {_{u}^{ }} {PushDualGen} }} {Make sure to enter the correct conference title from your rights confirmation email}{June 03--05, 2018}{Woodstock, NY} } } } } } } {Lin et al.} Push recommendation in proactively delivers personalized content to nearly one billion users to facilitate their engagement. Recently, generative recommendation has achieved end-to-end user personalization through semantic ID. However, their black-box characteristics make recommendation logics difficult to trace, hindering their deployment. OneRec-Thinking addresses this by incorporating CoT before generating SIDs, but this significantly increases inference cost. To support large-scale industrial applications, we propose , a lightweight generator, which first generates the SID and then produces a copy as a skippable explanation. ~ has been deployed in Kuaishou's push recommendation system. Online A/B tests demonstrate the effectiveness of , delivering significant improvements in both user attraction and satisfaction. The effective play rate for videos recommended to users has relatively increased by 8.50\ In the long term, ~optimises the content ecosystem, providing more exposure for long-tail videos. <ccs2012> <concept> <concept_id>10002951.10003317.10003347.10003350</concept_id> <concept_desc>Information systems~Recommender systems</concept_desc> <concept_significance>300</concept_significance> </concept> </ccs2012> {Information systems~Recommender systems} {framework.pdf} The core of push recommendation is to infer user preferences from interaction history. However, user behavioral sequences often exceed the context window of LLMs, while heuristic strategies such as truncation or random sampling may discard signals. To make full use of the input context, we replaced the original input with SIDs inspired by the parallel embedding learned from CREM~. Unlike residual SID methods, which suffer from hierarchical dependency and error accumulation, parallel SID approaches derive all SID tokens from a set of parallel embedding slots rather than from a single dense item representation. We construct SIDs from multiple parallel embedding slots. The details of the parallel representation model are described in Appendix~. Specifically, each video $v$ is represented by $M$ embeddings $\{^{ }\}_{m=0}^{M-1}$. We quantize each embedding independently using K-means clustering. This yields $M$ codebooks $\{^{ }\}_{m=0}^{M-1}$, each containing $K$ centroids. For slot $m$, the SID token is obtained by nearest-centroid assignment: s^{ } =arg\,min_{k \{0, ,K-1\}} \|^{ } -^{ }_k\|^2, where $^{ }_k ^{ }$ is the $k$-th centroid of the $m$-th codebook. The final SID obtained through quantization is defined as = . By replacing each video with its SID, we compress long behavioral histories into compact, discrete semantic sequences, allowing the LLM to retain more interaction history within a limited context. In practice, we set $M=8$ and $K=512$. We adopt K-means due to its simplicity and efficiency. Exploring more advanced quantization strategies is left for future work. Since SIDs are discrete codebook indices absent from the LLM vocabulary, LLMs cannot understand or generate them without alignment. We address this in two steps. First, we register all $L{ }K$ codebook indices as special tokens with random initialization. Second, we train LLMs via two complementary tasks over video $v_i$ with push copy $c_i$ and description $d_i$. We begin by utilising the text $c_i$ and $d_i$ to predict the corresponding SID of video $v$ , compressing scenario-related information into the discrete SID. Simultaneously, we designed the symmetrical task, utilising the SID of the video to generate $c_i$ and $d_i$ , enabling LLMs to learn to utilise the semantic information contained in SIDs: &s_i^0\,s_i^1\, \,s_i^{L-1} = , \\ &c_i\,d_i = , where $s_i^l$ denotes the SID of $v_i$ in the $l$-th codebook, with $l \{0, , L-1\}$. Both tasks are jointly optimized on Qwen3-0.6B~ via next-token prediction: _{} = - _{m=0}^{M-1} P , where $x$ denotes the input and $y^m$ is the $m$-th target token. To ensure training efficiency and stability, we freeze the original vocabulary embeddings and optimize only the SID token embeddings. After compression, each video can be encoded as a parallel SID, and the generator gained the ability to understand and generate SIDs. Based on these foundations, we fine-tune the generator using the interaction history of user $u$, enabling it to generate the SID and the copy corresponding to the video of interest to the user. In addition to the interaction history, the input $ $ also includes a task instruction, the user profile, and temporal recency markers. We train the generator to generate SIDs first, and then produce the corresponding copy. To separate them, we insert a special token $ id2text\_sep $ into the middle. Consequently, the training objective is divided into two parts. The first is the prediction of the SID, formalised as follows: _{SID} = - _{l=1}^{L} P (s_{T+1}^l ,\, s_{T+1}^{<l} )\,. The second is the generation of copy based on the predicted SID: _{Copy} = - _{j=1}^{|c_{T+1}|} P (t_j ,\, ,\, id2text\_sep \,, t_{<j} )\,. The overall objective is the weighted sum of them: _{gen} = _{SID} + _{Copy}\,_{Copy}\,, where $ _{Copy}$ is a hyperparameter used to balance these different training objectives. Although SIDs compactly encode video, the input also contains a large amount of text, which results in a high computational cost. We address this by merging frequent token sequences into single tokens, which are sourced from high-frequency $n$-grams and semantically atomic entities such as SID. And each new token embedding is initialized by mean pooling over its constituents. We train ~based on click history. Although the click contains direct signals of interest, it can only partially reflect users’ preferences; user interests are widely distributed across different situations, such as browsing and commenting. We incorporate the SIDs generated by ~into the original user feature and fuse them with heterogeneous online features to yield a more comprehensive preference representation of the user. Specifically, we use three independent encoders to project user features into $_u ^d$, candidate video features into $_v ^d$, and the Top-$N$ SIDs into $_s ^d$, which summarizes the preference signal captured in . In practice, we set $N{=}20$. To incorporate the generative signal into the user representation, we fuse $_u$ and $_s$ via a weighted combination, formalised as follows: _u' = \,_u + \,_s, where $ $ and $ $ are hyperparameters, which are set to 1 in applications. $_u'$ is used to search for videos of interest to users from a collection consisting of $_v$, using ANN search. Push recommendations utilise notifications to recommend content of interest to users without requiring app entry~. Push serves as a primary re-engagement channel, reaching nearly one billion users on Kuaishou. Unlike passive feed recommendations, push notifications reach users proactively. Any misaligned recommendation thus risks directly triggering user dissatisfaction, imposing stricter requirements on item selection accuracy and traceability. Recently, the generative recommendation paradigm based on the semantic ID ~, such as OneRec~, has demonstrated competitive performance across a range of applications. Unlike traditional methods, they learn user preferences in an end-to-end manner and generate the corresponding semantic IDs for content of interest to users. OneRec-Thinking~ incorporates a chain-of-thought before generating the SID, enhancing performance while providing the reasons for the recommendation. This provides a promising solution for the push recommendation, enabling the platform to deliver precise and reliable recommended content for user engagement. However, considering the efficiency and the throughput requirements, it is difficult to deploy in the push recommendation. In this work, we propose , a generative recommendation framework tailored for push notifications. It includes three stages: to encode videos into the SIDs and train LLMs to use these SIDs; enables LLMs to generate the SID first, and then selectively produce copy as an explanation; . Currently, ~is deployed in the push recommendation service of Kuaishou, serving nearly a billion users with approximately 100K QPS. Online A/B testing across 150 million users demonstrated that ~delivers direct performance gains for the platform. Furthermore, ~optimises the content ecosystem, providing exposure for long-tail videos and enhancing the attraction of the Kuaishou. ~retains the ability to generate copy then SIDs. This helps us to understand what is contained within SIDs, as well as the recommendation logic behind . To gain a clear understanding, we conducted qualitative studies on representative examples. In addition to the recommended videos, we allow ~to generate subsequent copy. We present these results in Figure~. It is clear that ~captures the users' preferences effectively, which is capable of adaptively taking into account user preferences manifested through a single video click, as well as across the entire click history. {case_study.pdf} {Red} indicates the link between the recommended video and the interaction history. } In this paper, we introduced , a lightweight generative framework for push recommendation. Unlike OneRec-Thinking, ~first generates SIDs and then produces subsequent copies as skippable interpretations. This design enhances performance while ensuring that the reasoning logic remains interpretable. Moreover, its flexible inference enables ~to be broadly deployed. In the future, we plan to experiment with increasing the size of the ~, while expanding the application of ~to scenarios beyond push recommendations. Push recommendation determines what content to deliver to users. Traditional methods retrieve candidates according to the atomic-level scoring. TIGER~ proposes generative retrieval paradigm by reframing candidate generation as an autoregressive sequence generation problem over SIDs. OneRec~ scales this paradigm for large-scale industrial deployment using a unified generative model and further incorporates iterative preference alignment to improve generation quality. OneRec-Think~ further augments SID generation with chain-of-thought; the added decoding overhead is prohibitive for latency-sensitive push scenarios~. And RecGPT~ improves semantic coverage by augmenting online retrieval with LLM-predicted item tags as an additional retrieval signal. Generative recommendation formulates the recommendation task as a sequence generation problem, where each item is represented by a SID, a structured and discrete codeword sequence obtained from continuous item embeddings via quantization for generation. Recent studies, such as the OneRec series~, have demonstrated the effectiveness of SIDs in generative recommendation. Most existing methods construct SIDs with Residual Quantization , which hierarchically quantizes item embeddings to capture fine-grained semantics. However, RQ suffers from error accumulation: each level quantizes only the residual from the previous stage, making deeper SID tokens increasingly noisy and unreliable~. To alleviate this issue, several works have explored parallel SID generation~. Nevertheless, these methods still derive SIDs from a single embedding. For content platforms, sustained core competitiveness relies on the long-term health of the entire content ecosystem. In this part, we analyse how ~ reshapes the content ecosystem. Based on their content, videos shared on the Kuaishou platform are automatically categorised into 39 different categories. We grouped the 39 categories into three groups: (>5\ As shown in Figure~, ~adjusts the exposure distribution across different groups on the platform. This allows some long-tail videos to be recommended to users on Kuaishou. From a long-term perspective, this helps the platform attract more users to share videos. This also demonstrates that ~provides additional results from different preferences for the online service. Unlike online services that tend to favour historically popular content, the SIDs generated by ~offer better generalisation capabilities and perform better in cold-start scenarios. {ecosystem.pdf} Our training data is derived from the push-click logs of Kuaishou, which are collected from nearly a billion users and span millions of candidate videos. For , we use over 4 million videos sampled from the content pool. We filtered various types of videos to ensure content coverage and diversity. Finally, the corpus contains approximately 0.72B tokens. For , we conduct incremental training based on user click logs with approximately 3.6B tokens every day. We instantiate the three components of ~as follows. For , we adopt Qwen2.5-Omni-3B~ with 8 learnable compression tokens to map the representation of videos into SIDs. For and , we employ Qwen3-0.6B~ as the backbone generator. The two parts are trained with learning rates of \ and \ for 3 epochs and 1 epoch, respectively. We conduct a 14-day A/B test by allocating 15\ {!}{ {lccr} & & & \\ {*}{}} & $ $ & 82.04M & M\,($+0.43\ & $ $ & 414.89M & M\,($+0.05\ {*}{}} & $ $ & $0.053\ & $ $ & $64.46\ } We evaluate ~in the online push recommendation system through a controlled A/B test. And the results are shown in Table~. We can find that ~achieves statistically significant improvements across all metrics. For notification, the gains in Click PV and DAU indicate that ~ effectively attracts users back into Kuaishou and the user behaviors after clicking further confirm that the pushed content aligns with user interests. In post-click, Dis. Rate is relatively reduced by $37.70\ To analyse the contributions of components in , we use the task of SID prediction to evaluate the performance of ~ and its variants. And Pass@k is introduced to measure the probability that at least one of $k$ independently sampled SIDs matches the ground-truth SID of a user-interested item. We construct these variants by replacing or disabling modules one by one and retraining the model. For the variant w/o Parallel SID, we replace the parallel SID with the SID used in OneRec. And for the variant w/o SID Adaptation, we retrained Qwen3-0.6B to generate SIDs and text corresponding to videos directly. We also analysed the training strategies of Token Freeze and Multi-token Binding. Results are shown in Table~. Furthermore, we show the performance of Qwen3-0.6B after training on the SID used by OneRec~. and , respectively.} {!}{ {lccc} & Pass@1 & Pass@4 & Pass@20 \\ & & & \\ & & & 0.412 \\ & 0.332 & 0.384 & \\ & 0.319 & 0.352 & 0.366 \\ & 0.326 & 0.379 & 0.401 \\ Qwen3-0.6B & 0.312 & 0.347 & 0.358 \\ } We found that without the training phase of SID Adaptation, ~exhibited a significant drop in performance. This is because LLMs have never encountered SID-related data during their previous training, so an additional training process is necessary to enable them to understand and use SIDs. In contrast, the token freeze and multi-token binding primarily optimize the training process, so the performance gains are relatively slight. Compared with the results of the vanilla Qwen3-0.6B, each module we have introduced has had a positive impact on , and they can be combined for further performance improvements. {sidmodel} Attention mask of the parallel embedding / SID model, where “+” indicates visible tokens and “-” indicates masked ones. Training and inference of the parallel embedding / SID model. } This section provides supplementary details of the parallel embedding model used in the main text. We build the embedding model on Qwen2.5-Omni~, following the collaborative representation learning paradigm of CREM~ and CoMa~. The model is trained to support both video retrieval and copy generation with a shared set of embeddings. To obtain compact latent representations from multimodal inputs, we prepend a set of learnable compression tokens to the original input sequence. In our implementation, we use $8$ compression tokens, so that each video is encoded into $8$ parallel embeddings. As illustrated in Figure~ , the encoder is optimized with a hybrid objective: _{r} &= - {B} _{i=1}^{B} / )} { _{j=1}^{B} / )}, \\ _{g} &= - {B} _{i=1}^{B} _{t=1}^{T_i} P , \\ _{} &= _{r} + _{g}. Here, $_{r}$ is an in-batch InfoNCE retrieval loss and $_{g}$ is the standard autoregressive language modeling loss. $B$ denotes the batch size, $_i$ and $_i$ denote a matched query--passage pair, $ $ is the similarity function, $ $ is a temperature hyperparameter, and $ $ balances the two objectives. The corresponding attention mask is shown in Figure~ . The resulting parallel embeddings are discretized into SIDs following the procedure described in Sec.~. In practice, we use $M=8$ embedding slots and $K=512$ centroids for each slot-specific codebook. We adopt K-means for its simplicity and scalability. 