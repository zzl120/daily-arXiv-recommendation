 {acmart} {{ }} {{ }} {{ }} {UniFormer } -0.8em }}} {Proceedings of the 31st ACM SIGKDD Conference on Knowledge Discovery and Data Mining V.2}{August 3--7, 2025}{Toronto, ON, Canada} {{ ]}} $, Jinlong Jiao$^{*}$, Tijian Hu$^{*}$, Ruihao Zhang, Yanzhi Liu, Chenghou Jin, Qinglin Jia, Baixuan He, Hechang Pan, Yiwu Liu, Jian Liang, Chaoyi Ma$^{ }$, Ruiming Tang$^{ }$, Han Li, Kun Gai } } @kuaishou.com} $ Equal contributions. \\$^{ }$ Corresponding authors.} {Bo Chen et al.} {xxx, et al.} <ccs2012> <concept> <concept_id>10002951.10003317.10003347.10003350</concept_id> <concept_desc>Information systems~Social tagging; Language Models</concept_desc> <concept_significance>500</concept_significance> </concept> </ccs2012> {Information Retrieval~ Tagging Systems} In this paper, we advocated a paradigm shift from component-centric scaling to model-centric scaling for industrial recommender systems and systematically discussed the key challenges in building an efficient and unified scaling framework. To this end, we proposed , which integrates Feature-space Interaction Modules and Task-space Interaction Modules to jointly scale feature and task modeling. With standardized attention operators and multi-view FFNs, enhances modeling capacity while enabling flexible and scalable parameter allocation across different components. Extensive offline experiments and online A/B testing in Kuaishou industrial recommendation scenarios verify the effectiveness and practical value of , highlighting model-centric co-scaling as a promising direction for scaling industrial recommender systems. Deep learning-based industrial recommendation models typically consist of several modular components, including an embedding layer, a behavior modeling module, a feature interaction module, and a task modeling module~. Given input features $x$, the embedding layer produces dense representations $= $~. The overall process can be formulated as follows: = _{} ( _{} ( _{} ,\; _{seq}} ),_{} ), where $_{} , _{} ,$ and $_{} $ denote behavior modeling ~, feature interaction ~, and task modeling functions ~, respectively. Despite their effectiveness, these modules are typically designed independently, resulting in a fragmented architecture that limits cross-module interaction and unified modeling~. Recently, with the rapid advancement of large language models and the significant increase in computational power, model scaling has emerged as a key factor driving the emergence of powerful capabilities across various domains. Inspired by this trend, recommendation models have also begun to explore scaling strategies. However, these methods primarily focus on scaling within the feature modeling space~, overlooking the role of task modeling in the scaling paradigm. To address this limitation, we propose a unified scaling framework that extends beyond feature space and incorporates task modeling as a first-class component: = _{} ( _{}, _{},_{} ). By jointly scaling sequential behavior modeling, feature interaction, and task modeling in a unified manner, our approach enables more expressive and efficient representation learning. Recently, substantial progress has been made in industrial recommendation through component-centric model scaling, where individual components such as behavior modeling, feature interaction, or task modeling are independently scaled to improve model capacity. Although recent methods such as HyFormer and OneTrans further explore cross-module co-scaling by jointly modeling behavior and interaction, their designs are still confined to the feature space and lack a unified model-centric scaling framework over the overall modeling space. In this paper, we propose , an efficient and unified model-centric scaling framework for industrial recommender systems. To improve efficiency, decomposes the overall modeling space into feature and task spaces, which are modeled by stacked Feature-space Interaction Modules and Task-space Interaction Modules, respectively. Moreover, introduces semantic-based tokenization scheme to enable user-item decoupling, thereby achieving request-level inference acceleration. To prevent preference collapse, employs multi-sequence cross-attention to separately capture heterogeneous behavior patterns, followed by the self-attention to enhance interaction modeling. Besides, dedicated multi-view FFNs are introduced to support flexible and scalable parameter scaling across different modeling components. Extensive online A/B testing in two production scenarios, Kuaishou and Kuaishou Lite, shows that consistently improves user engagement and interaction metrics, achieving gains of +0.101\ {figure/model_compa.png} Industrial recommender systems leverage life-long user behavior histories and heterogeneous features to estimate the utility of various user-item interactions. For instance, short video sharing platforms predict a wide range of user feedback, such as , and , to determine final video exposure~. To manage this complexity, industrial recommendation models typically comprise several modular components built upon the embedding layer: a for sequential pattern extraction~, a for cross-feature dependencies modeling~, and a for multi-objective optimization~. To this end, a variety of fragmented, hand-crafted operators have been developed~. Despite their effectiveness in improving predictive performance, these specialized designs complicate model optimization and limit the efficient utilization of computational resources~. The remarkable success of large language models has demonstrated the effectiveness of model scaling in achieving superior performance~, motivating industrial recommender systems to scale up their model capacity~. For model scaling, transformer-based architectures such as LONGER~, and STCA~ improve long-sequence behavior modeling, while HHFT~, and RankMixer~ scale feature interaction capacity through self-attention or token-mixing operators. Meanwhile, MoE-based methods such as SMES~ enhance multi-task modeling via scalable experts in . Despite their effectiveness, these approaches primarily follow a component-centric scaling paradigm as illustrated in Figure~ - , which limits the efficient utilization of computational resources and prevents the model from fully realizing the benefits of holistic scaling. Therefore, to fully realize the potential of model scaling for recommendation, the scaling paradigm should evolve . Recent studies, such as OneTrans~ and MixFormer~, have made preliminary attempts to explore cross-module co-scaling. However, these approaches remain largely confined to the feature space - ), jointly scaling behaviors modeling and feature interaction while leaving task modeling underexplored. To enable efficient and unified model-centric scaling, several key challenges need to be addressed. First, due to the large number of features and tasks in industrial recommendation systems, as well as their high-concurrency and low-latency serving requirements, the unified co-scaling model must maintain to mitigate the resource overhead introduced by scaling up model capacity~. Second, the unified co-scaling model should effectively capture heterogeneous user behaviors and short/long-term interests, ensuring comprehensive personalization and ~. Third, the unified co-scaling model should support , enabling balanced capacity scaling across different components and avoiding excessive parameter concentration in any single module~. To address the above challenges, we propose , an efficient and unified model-centric scaling framework designed for industrial recommender systems. Specifically, to improve efficiency, decomposes the unified architecture into feature-space interaction and task-space interaction , as illustrated in Figure~ . The former scales the modeling of sequential and non-sequential features, while the latter captures interactions between high-order features and task-specific signals. By omitting the costly intra-sequence interactions and interactions between sequences and tasks, achieves substantial efficiency improvements. Besides, a semantic-based tokenization scheme is proposed, enabling user-item decoupling during inference and achieving request-level inference acceleration. To prevent preference collapse, FIM employs a multi-sequence cross-attention mechanism to model heterogeneous behavior sequences separately, followed by the self-attention to enhance interaction modeling within the feature space. Moreover, to enable flexible and scalable parameter allocation, introduces multi-view Feed-Forward Networks for sequential features, non-sequential features, and task signals respectively, thereby supporting balanced capacity scaling. In summary, our main contributions are summarized as follows: We advocate a paradigm shift from component-centric scaling to model-centric scaling for recommendation models and systematically identify the key challenges in building an efficient and unified scaling framework. We propose , an efficient and unified model-centric scaling framework that integrates Feature-space Interaction and Task-space Interaction modules, leveraging standardized attention operators and multi-view FFNs to enhance modeling capacity while enabling flexible and scalable parameter allocation. We conduct extensive offline experiments and analyses in Kuaishou's industrial recommendation scenarios, demonstrating the effectiveness of . Furthermore, online A/B testing in two production scenarios, Kuaishou and Kuaishou Lite, shows consistent improvements in user engagement and interaction metrics. We evaluate on Kuaishou's single page short-video recommendation platform, which is the largest recommendation scenario in Kuaishou and serves more than , generating over . Each sample is a $ , $ pair together with the associated multi-type feedback, where user-side behavioral features include short-term browsing sequences, item-dependent long-term behaviors retrieved based on the target item, and compression-based lifelong interest representations. Aligned with the practical deployment requirement, we focus on four key prediction tasks: , , , and . We adopt the ~ to evaluate the predictive performance of our model, which is the most critical offline metric in our short-video service. GAUC calculates the AUC independently for each user and then aggregates the per-user values in a sample-weighted manner, \;=\; _{u} w_u _u, w_u \;=\; _u}{ _{i}\#_i}, where $w_u$ denotes user $u$'s sample proportion in the evaluation set. Notably, given the stability provided by the massive scale of training samples, a relative GAUC improvement of $ We compare against several strong representatives that cover both pre-scaling and scaling recommendation models, that perform sequential modeling and feature interaction: , a classical pre-scaling architecture that combines the Search-based Interest Model ~ for ultra-long behavior modeling with the Deep Cross Network ~ for feature interaction. replaces the DCN component with HoME~, a Hierarchical-Mask MoE module for multi-task supervision. combines SIM with RankMixer~, a component-centric scaling module that employs a token-mixing network with scalable parameter capacity. ~ and ~ are cross-module co-scaling methods that jointly scale behavior modeling and feature interaction modeling within the feature space. For a fair comparison, all models were trained from scratch on the same GPU cluster using identical optimization settings. Specifically, both the sparse and dense components were optimized with AdamW~ using a learning rate of 1e-3 with 1e-3 weight decay, and a per-GPU batch size of 2048. For our model , it adopts a 3-layer architecture by default, with hidden dimensions $d_{}$ of 1280. {!}{ {lcc|cc|cc|cc|c} {*}{Model} & {c}{Effective-view} & {c}{Long-view} & {c}{Like} & {c}{Follow} & {*}{\#Params} \\ {2-3} {4-5} {6-7} {8-9} & GAUC & Impr. & GAUC & Impr. & GAUC & Impr. & GAUC & Impr. & \\ SIM+DCN & 0.7418 & - & 0.7734 & - & 0.8486 & - & 0.8361 & - & 115.8M \\ SIM+HoME & 0.7424 & +0.08\ & 0.7740 & +0.08\ & 0.8494 & +0.09\ & 0.8374 & +0.16\ & 114.8M \\ SIM+RankMixer & 0.7443 & +0.34\ & 0.7757 & +0.30\ & 0.8507 & +0.25\ & 0.8374 & +0.16\ & 492.0M \\ HyFormer & 0.7447 & +0.39\ & 0.7762 & +0.36\ & 0.8512 & +0.31\ & 0.8381 & +0.24\ & 496.5M \\ MixFormer & & & & & & & & & We conduct experiments against mainstream industrial ranking models, and the results are reported in Table~. Across all objectives, consistently outperforms all state-of-the-art baselines, and we have the following observations. RankMixer significantly outperforms both DCN and HoME, with GAUC improvements of 0.34\ HyFormer and MixFormer further improve performance through cross-module co-scaling, which jointly scales behavior modeling and feature interaction within a unified framework. Their significant gains over SIM+RankMixer demonstrate the necessity of co-scaling, suggesting that jointly scaling multiple components contributes to larger improvements than component-centric scaling. achieves the best performance among all compared methods, obtaining a significant improvement over the strongest baselines MixFormer. Unlike HyFormer and MixFormer, which mainly focus on feature-space scaling, further incorporates the task space into the unified modeling process. Besides, enables flexible and scalable parameter allocation in a model-centric manner, leading to more balanced parameter scaling across the entire model. -Large further improves over by +0.11\ We conduct an ablation study on the Kuaishou industrial dataset to verify the effectiveness of the key designs of . Specifically, we consider the following variants: replacing Multi-sequence Cross-Attention with shared Cross-Attention , removing Interaction Enhancement , , $^{ } = ^{ }_{}$, removing the Task-space Interaction Module , replacing Sequential-oriented Feed-Forward Networks with a shared FFN , and replacing Non-sequential-oriented Feed-Forward Networks with a shared FFN . {figure/decoder-ablation-with-xlabels.pdf} and tasks.} {figure/decoder_scaling_up_delta_gauc_percent_evtr_like_1x2_powerlaw.pdf} As shown in Figure~, consistently outperforms all ablated variants across all tasks, demonstrating the effectiveness of the model design. The results of the first three variants validate the advantages of our unified model design in the Unified Interaction Block. By jointly considering preference completeness, comprehensive interaction modeling, and task relevance, achieves more robust and consistent improvements across different tasks. Notably, removing Multi-sequence Cross-Attention results in a significant performance drop, particularly for interaction-related metrics , demonstrating the effectiveness of preventing preference collapse. For the last two variants, replacing S-FFNs or NS-FFNs with a shared FFN leads to the most pronounced performance deterioration. This result demonstrates the effectiveness of our flexible and scalable parameter allocation design, highlighting its crucial role in achieving balanced and effective capacity scaling. To investigate the capacity potential of , we conduct a scaling analysis by increasing the hidden dimensions of multi-view FFNs , whose results are shown in Figure~. As observed from the results, exhibits an evident scaling law, where the model performance consistently improves with increasing parameter capacity. Moreover, benefiting from the multi-view FFNs tailored to different modeling components, enables more balanced and scalable parameter allocation, which helps maintain a stronger performance growth trajectory. Such a model-centric scaling paradigm effectively mitigates the early often observed in traditional component-centric scaling~. To illustrate the advantage of semantic tokenization, we compare it with global tokenization~, which does not explicitly preserve semantic grouping. As shown in Figure~, semantic tokens exhibits more diverse and structured attention behaviors across layers for FIM. Across different cross-attention layers, semantic tokens attend to different behavior patterns, enabling to capture multi-view user preferences. In contrast, global tokens exhibit less diverse attention patterns, with attention repeatedly concentrated on similar positions across layers. Overall, semantic tokenization enables more specialized and layer-adaptive information extraction, thereby mitigating the caused by global tokenization. {figure/semantic_and_global_token_attention_layers_pos0_49_top8_separate_colorbars.pdf} {figure/token_task_attention_three_semantic_groups.pdf} To investigate the relationships between features and tasks in TIM, Figure~ visualizes the attention distribution of different tasks over feature tokens. We observe that different tasks adaptively focus on distinct feature sources according to their specific optimization objectives. Some features receive consistently high attention weights across different tasks, indicating their important roles in multi-task prediction. Compared with interaction tasks, item statistical features receive higher attention weights for duration tasks, whereas certain user features are more emphasized by interaction tasks. Moreover, highly related tasks also show strongly aligned attention distributions. For instance, and exhibit highly similar feature attention patterns, suggesting that can capture task correlations through feature-task interactions. To validate the industrial feasibility of , we deployed it on the largest short-video channel of our platform Kuaishou and Kuaishou Lite APP, for a seven-day online A/B test with $5\ The evaluation metrics include and , which measures the total duration of user engagement, as well as other user interaction metrics, such as and . The online A/B testing results are shown in Table~, which demonstrates that yields a statistically significant improvement both in user engagement and interaction metrics. These results demonstrate that our model-centric co-scaling framework delivers consistent improvements across diverse online metrics while effectively mitigating the seesaw effect among competing objectives. {llcc} Category & Metric & Kuaishou Lite & Kuaishou \\ & App Stay Time & +0.260\ Engagement & Watch Time & +1.113\ & Video View & +0.252\ & Like & +1.089\ Interaction & Comment & +1.818\ & Collect & +0.930\ & Forward & +1.274\ Benefiting from our carefully designed tokenization strategy, categorizes both sequential and non-sequential features in the feature space into item-independent and item-dependent groups according to their dependency on candidate items. This design enables user-item decoupling during online serving, allowing item-independent network to be computed once per request and reused across multiple candidate items for request-level inference acceleration. In our production serving scenario, each request scores 512 candidate items simultaneously. With user-item decoupling, the inference QPS of is improved by Figure~ illustrates the proposed unified co-scaling framework , which consists of two core components: a tokenization block and a unified interaction block. The tokenization block transforms sequential features, non-sequential features, and task-related features from the input layer into compact token representations through feature grouping and projection. To avoid the prohibitive computational overhead of modeling interactions across the entire space with full attention and achieve efficient model-centric co-scaling, decouples the overall modeling space into feature and task spaces, which are modeled by stacked Feature-space Interaction Modules and Task-space Interaction Modules , respectively. Each module follows a standard Transformer-style architecture composed of attention layers and FFNs, enabling scalable parameter allocation while maintaining computational efficiency. {figure/uniformer.pdf} , which consists of two core components: tokenization and the unified interaction block. The unified interaction block is composed of stacked Feature-space Interaction Modules and Task-space Interaction Modules . FIM takes tokenized sequential and non-sequential features as input and captures feature-space interactions through attention and FFNs layers. TIM further takes the high-order representations produced by FIM together with tokenized task features, and adopts the same standardized operators to capture task-feature and inter-task interactions.} User behavioral sequence features can be categorized into two types based on their dependency on the target item. The first type is item-independent behavior, including short-term interaction sequences and long-term compressed interest representations . The second type is item-dependent behavior, such as candidate-aware search-based sequences . To effectively model these heterogeneous patterns, we design distinct tokenization strategies. For , we preserve the original sequential structure and tokenize each element in the sequence individually. For short-term interaction sequences, the $i$-th element is represented by concatenating the item's side information, such as video ID embeddings $^{}_{i}$, content type $^{}_{i}$, creator information $^{}_{i}$, watch duration $^{}_{i}$ and so on, forming a token representation $^{}_{i} = $. For long-term compressed interest representations, we directly retain the pretrained embedding for each token without further modification, i.e, $^{}_{i} = _{i}^{}$. Subsequently, following OneRec-v2~, each representation $_{i}$ of different sequences is projected into a unified hidden space via SwiGLU layer~, yielding $_{i} ^{d_{}}$ with dimension: d_{} = S_{} L_{} d_{}, where $S_{}$ is the key-value split coefficient, and $L_{}$ is the number of key-value layers. $d_{}=G_{} d_{}$, where $G_{}$ is the number of key-value heads and $d_{}$ is the dimension of the attention head. The representation $_{i}$ is further transformed into layer-specific key-value pairs for the following attention mechanism, resulting in key-value pairs $ $, where $_i^{j} ^{d_{}}$. For the $l$-th layer, the normalized key-value pair~ is obtained by: _i^{ } &= _{k,l} \\ _i^{ }&= _i^{ }, & S_{} = 1 \ \\ _{v,l} , & S_{} = 2 \ . Finally, the output results for the short-term sequence and long-term sequence are $ $ and $ $, respectively. For , we aggregate the sequence instead of preserving the original sequential structure. The key motivation is to make the key-value representations in cross-attention independent of the target item, thereby enabling request-level infer acceleration . Specifically, for search-based sequences, we apply a target attention tokenizer to aggregate the sequence into a compact token: _ {}= , where $ $ are the key-value pairs of the search-based sequence, following the same procedure as the aforementioned sequences ). For non-sequential features, we also categorize them into two groups based on their dependency on the target item: and . Within each category, features are further grouped according to their semantic meanings, resulting in $m$ item-independent feature groups and $n$ item-dependent feature groups. Notably, item-dependent behaviors are also treated as item-independent feature and aggregated into a single feature group following Eq.~ . For each group, the corresponding feature embeddings are concatenated and then projected into a unified hidden space with $d_{}$ dimensions through a SwiGLU layer, denoted as $}} ^{q d_{}}$, where $q = m+n$. The rationale for adopting semantic grouping over global grouping~ is two-fold. First, semantic grouping decomposes the computation into user request-related and candidate item-related parts. The former can be computed once per request and reused across candidates, thereby significantly improving inference efficiency. The detailed implementation will be discussed in Section~. Second, since subsequent interactions are modeled via cross-attention over sequential features, semantically grouped queries provide multiple heterogeneous views, enabling the model to extract diverse user preferences from behavior sequences . For each task, task-specific features are grouped into a single token. Similarly, these embeddings are concatenated and projected into a hidden space with $d_{}$-dimensional representations through a SwiGLU layer, obtaining $}} ^{t d_{}}$, where $t$ is the number of tasks. The unified interaction block jointly integrates an $M$-layer Feature-space Interaction Module for modeling interactions between sequential and non-sequential features and an $N$-layer Task-space Interaction Module for capturing interactions between high-order features and tasks, enabling unified co-scaling across feature and task spaces. The Feature-space Interaction Module is designed to capture interaction patterns within the feature space. It comprises two complementary components: a sequential-oriented interaction component and a non-sequential-oriented interaction component. . To effectively extract informative signals from user behavior sequences while preserving both short-term and long-term user interests, we propose a multi-sequence cross-attention mechanism rather than applying a shared cross-attention layer over concatenated heterogeneous sequences. The key motivation is to avoid preference collapse toward a specific sequence~, allowing the model to preserve diverse multi-view interests from heterogeneous behavior sequences. Taking the short-term sequence as an example, at the $l$-th layer, the previous output tokens are used as queries to perform cross-attention with a residual connection: _{}^{ } = ( }_{}), _{}^{ }, _{}^{ } ) + ^{ }_{}, where $^{ }_{} ^{q d_{}}$ denotes the query tokens and $ }, _{}^{ })$ are the $l$-th layer representations derived from the short-term behavior sequence. Inspired by the lazy decoder design in OneRec-v2~, our key-value mapping in cross layers also follows the lazy design principle. By default, we set $S_{}=1$ and $L_{}=1$, allowing all layers to share the same KV representations, which significantly reduces memory usage and improves computational efficiency. Specifically, for the first layer, the $q$ semantically grouped tokens from the tokenization block are used as queries to interact with multiple behavior sequences, , $^{ }_{}=}} ^{q d_{}}$, enabling the extraction of diverse and complementary information from heterogeneous behavior sources. Notably, to stabilize training, we also adopt a Pre-Norm RMSNorm~ operation. The resulting representations are then fed into a Sequential-oriented SwiGLU Feed-Forward Networks with residual connection to further enhance sequence-aware patterns: }_{}^{ } = ^{ }_{} ( _{}^{ } ) + _{}^{ }. Similarly, the long-term behavior sequence is processed in the same manner, producing $}_{}^{ }$. Notably, we adopt S-FFNs to model heterogeneous behavior sequences separately. The key rationale is that the Sequential-oriented Interaction component is designed to capture diverse user behavior signals, such as short-term, long-term, and cross-domain sequences. Introducing sequence-specific modeling networks allows the to better capture heterogeneous behavior patterns within dedicated latent spaces, leading to more fine-grained user interest modeling. Finally, to integrate the interaction outputs extracted from heterogeneous behavior sequences, we propose two adaptive fusion strategies. 1) learns a global coefficient $ $ to balance the relative contributions of different behavior sequences: $^{ }_{} = }_{}^{ } + }_{}^{ }$, where $ $ is a learnable global fusion coefficient. 2) Given the substantial differences in short- and long-term behavioral patterns across users, particularly between highly-active and cold-start users, we further introduce , which dynamically predicts a user-specific fusion coefficient $ _i$ based on user-related features : $^{ }_{} = _i }_{}^{ } + }_{}^{ }$. . Since the representations $^{ }_{}$ extracted by the sequence-oriented interaction are primarily dominated by sequential information, further performs by incorporating non-sequential information before sufficient high-order feature interaction modeling. Specifically, $^{ }_{}$ is concatenated with the queries from the previous layer $^{ }_{}$ to enhance feature interaction modeling, $^{ } = $. Specifically, $^{ }_{} = ^{ }_{}=}}$. Subsequently, the concatenated tensor $^{ }$ is fed into the non-sequential-oriented interaction component with self-attention and feed-forward layers for interaction modeling. Specifically, a self-attention layer with residual connection is applied: ^{ }_{} = }) ) + ^{ }, where $^{ }_{} ^{2q d_{}}$. Then, a group of Non-Sequential-oriented SwiGLU Feed-Forward Networks are applied for effective modeling of the feature heterogeneity. Specifically, for each slice $^{ }_i, i \{0, , 2q-1\}$, a feature-specific FFN is employed to enhance the interactions: ^{ }_i = ^{ }_{i} }_i ) + ^{ }_i, thus obtaining the output of the $l$-th layer FIM, denoted as $^{ } ^{2q d_{}}$. Subsequently, the tensor $^{, }$ is partitioned along the feature dimension into two components according to the layer-wise split ratio $ ^{ }$, , $^{, }= $, where $^{, }_{} ^{2q ^{ } d_{}}$ and $^{, }_{} ^{2q }) d_{}}$. The two components are then fed into the next cross-attention and self-attention layers, respectively. By default, the split ratio $ ^{ }$ is set to $0.5$. In addition, a pyramid-style design can be adopted, where $ ^{ }$ is progressively reduced across layers, resulting in progressively lightweight cross-attention computation and improved efficiency. After the Feature-space Interaction Module performs sufficient interaction modeling in the feature space, further introduces the Task-space Interaction Module to model the feature-task relationship. Similar to FIM, TIM also comprises two components: a feature-oriented interaction component for intra-task feature interaction and a task-oriented interaction component for inter-task modeling. . To perform task-aware feature interaction, incorporates a cross-attention layer to perform task-specific interactions over the final feature-space representations $^{ }$ extracted by the FIM. Specifically, at the $l$-th layer, the previous outputs are used as queries to perform cross-attention: _{}^{, } = ( }_{}), _{}^{ }, _{}^{ } ) + ^{, }_{}, where $^{ }_{} ^{t d_{}}$ denotes the query tokens and for the first layer, the task feature tokens from the tokenization block are used as queries, , $^{ }_{}=}} ^{t d_{}}$. Besides, the $ }, _{}^{ })$ of the first layer is $_{}^{ } = _{}^{ } = ^{ }$. Similar to FIM, the cross-attention layers in TIM also follow the lazy design by default, sharing key-value representations across all layers to improve efficiency. This design enables task-aware weighted aggregation over the high-order interaction information $^{ }$, providing a multi-task representation analogous to the MMoE framework~. . Subsequently, to capture inter-task interactions, incorporates a self-attention layer followed by a group of Task-oriented SwiGLU Feed-Forward Networks , further enhancing task-specific modeling capabilities. ^{ }_{} &= }) ) + _^{ },\\ ^{ }_i &= ^{ }_{i} }_i ) + ^{ }_i, where tensor $^{ }_i, i \{0, , t-1\}$ is the slice of $^{ }_{}$ and the output of the $l$-th layer TIM is denoted as $^{ } ^{t d_{}}$, which is used as the queries for the next layer, , $^{ }_{} = ^{ }$. Finally, the output results from the $N$-th layer $^{ }$ are treated as task-specific representations, which are fed into task-specific FFN heads followed by a Sigmoid activation to produce the final prediction: _i = }_i )). Besides, the weighted multi-task loss over $t$ tasks is adopted to optimize the : = _{i=1}^{t} _i _i , where $y_i$ denotes the ground-truth labels for the $i$-th task and $_i$ is the task-specific loss function such as the Binary Cross-Entropy ~. To improve training efficiency and scalability, we introduce several system-level optimizations. For recommendation, multiple candidate items within the same request or session share identical user-side behavior sequences. Due to the multi-valued nature and considerable length of behavior features, redundantly storing and repeatedly processing them will introduce substantial memory and computational overhead. Therefore, we apply common compression primarily to user-side behavior features. For each unique user, the behavior features are stored and processed only once, while an index mapping is used to associate the shared representation with the corresponding item samples. This design reduces redundant sequence embedding lookup, memory copy, and aggregation operations. For a batch containing \ samples from \ unique users, let \ denote the average number of samples per user. Let \ be the cost of processing user-side common features, and \ be the cost of the remaining features. With user-level common compression, the cost of feature processing can be reduced from \( B \) to \( U C_{} + B C_{}. \) Thus, the theoretical speedup is \( }+C_{})} {U C_{}+B C_{}} =}+C_{}} {C_{}+C_{}/}. \) We adopt variable-length FlashAttention to eliminate padding overhead in heterogeneous user behavior sequences. In UniFormer, semantically grouped query tokens attend to behavior sequences with different lengths. Given a batch of sequence lengths \ and query length \ , padded cross-attention incurs \ \) complexity, while the variable-length formulation reduces it to \ \). Together with FlashAttention's IO-aware tiling~, which avoids materializing the full attention matrix, this optimization reduces both redundant computation and memory traffic. Besides, we deploy BF16 mixed-precision training to reduce memory bandwidth and improve throughput. Compared with FP32, BF16 halves tensor storage and enables low-precision tensor cores for matrix-intensive operators, while maintaining sufficient numerical precision for training. To reduce online serving latency and resource overhead, the optimization strategy for inference is also adapted. During online serving, one user request is typically paired with \ candidate items for ranking . Benefiting from the carefully designed tokenization scheme, decomposes non-sequential features into \ item-independent tokens and \ item-dependent tokens, while aggregating item-dependent behavior sequences to ensure that the key-value representations in cross-attention remain independent of the target item. Based on this decomposition, item-independent tokens can be computed once per request and reused across all candidate items, avoiding redundant user-side computation. Notably, the cross-attention layers and FFNs in FIM can be naturally decomposed under this tokenization scheme. However, the full self-attention layers involve interactions among all tokens, making user-side representations dependent on candidate items. To address this issue, we apply an attention mask to remove the attention from user-side queries to item-side keys, thus allowing the user-side attention computation to be performed and reused once per request. Deep learning-based recommendation models typically consist of two major components: sparse embedding tables and dense neural networks . During the pre-scaling era, the majority of model parameters were concentrated in sparse embedding tables, while the DNNs remained relatively lightweight, resulting in a characteristic sparse-embedding-dominant design~. The dense modeling part was further constructed in a modular manner with independently designed components for behavior modeling, feature interaction, and task modeling. Consequently, improvements were primarily driven by component-level architectural innovations rather than by scaling the dense modeling capacity. For behavior modeling, which focuses on capturing user interests from historical interaction sequences, DIN~ and DIEN~ leverage attention mechanisms to model the relevance between user behaviors and target items. To model lifelong user interests, search-based methods such as SIM~ and TWIN~ retrieve the most relevant behavior subsequences from long behavior sequences. In contrast, VISTA~ and C-Former~ adopt compression-based paradigms to extract compact interest representations, enabling efficient modeling of lifelong user preferences. For feature interaction modeling, methods such as DeepFM~, DCN~, EENet~, and their various extensions aim to capture relationships among heterogeneous features through different interaction operators, including inner product, outer product, and attention mechanisms. These models improve recommendation performance by explicitly or implicitly modeling high-order feature interactions within representation spaces. For task modeling, multi-task recommendation models such as MMoE~, PLE~, and HoME~ improve overall multi-task learning capability by designing various task-shared and task-specific network architectures to balance knowledge sharing and task specialization across multiple objectives. Despite their effectiveness, these models mainly optimize individual modules independently and rely on relatively small-scale DNNs, without exploring parameter scaling across different modules~. In recent years, parameter scaling in large language models has demonstrated remarkable emergent capabilities in world knowledge and reasoning, inspiring the evolution of recommendation models toward scaling paradigms. Recent recommendation architectures mainly focus on component-centric scaling. Transformer-based architectures such as LONGER~ and STCA~ enhance long-sequence behavior modeling, while HHFT~ and RankMixer~ model feature interaction through self-attention or token-mixing mechanisms. And, MoE-based methods such as SMES~ introduce scalable experts for multi-task learning. To enable unified modeling of sequential and non-sequential features, OneTrans~ and TokenFormer~ adopt self-attention architectures over the entire feature space. They further incorporate pyramid-style progressive shrinking and sliding-window attention to mitigate the computational overhead. Furthermore, works such as HyFormer~, MixFormer~, HeMix~, and EST~ design more efficient interaction paradigms, where interactions between sequential and non-sequential features are modeled through lightweight cross-attention, while feature interactions are handled via token mixing or self-attention, thereby avoiding the substantial computational cost introduced by full self-attention. However, existing unified co-scaling recommendation models primarily focus on feature-space interaction modeling, with limited exploration of unified modeling across feature and task spaces. 