 {acmart} }} {Conference '26}{Location}{} {Proposition} } {2em} {HTML}{2E78C2} {HTML}{3A8F3C} {HTML}{D4820E} {HTML}{DCEEFB} {HTML}{DFF0D8} {HTML}{FEF3CD} } } } } } } {Wenzhuo Cheng et al.} Transformer-based CTR models face a growing bottleneck at the residual connection: under Pre-Norm, early user-interest signals are diluted layer by layer; the identity skip cannot forget stale interests; and each layer sees only its immediate predecessor, losing long-range cross-layer dependencies. Recent attention-based residual variants address parts of this in language models, but they drop the protective identity skip and have not been tried in recommendation. Drawing on Dual Path Networks and the HORNN view of residuals, we present , which routes each layer through two paths in parallel---an that preserves first-order feature reuse and gradient flow, and a that allows each layer to attend over compressed outputs from all earlier blocks for high-order recall. A vector-wise gate determines, for each hidden dimension, the relative weight assigned to each path. We further propose , replacing the Softmax in the cross-layer attention with SiLU so that multiple past blocks can be activated simultaneously and irrelevant ones can receive negative weights---a setting better aligned with CTR's parallel multi-interest patterns. On a large-scale industrial dataset , Criteo , and Avazu , DeRes outperforms twelve baselines including OneTrans, TokenMixer-Large, UniMixer, mHC, and AttnRes, achieving up to $+0.32\ <ccs2012> <concept> <concept_id>10002951.10003317.10003347.10003350</concept_id> <concept_desc>Information systems~Recommender systems</concept_desc> <concept_significance>500</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010293.10010294</concept_id> <concept_desc>Computing methodologies~Neural networks</concept_desc> <concept_significance>300</concept_significance> </concept> </ccs2012> {Information systems~Recommender systems} {Computing methodologies~Neural networks} Click-through rate prediction sits at the center of search, social feeds, and e-commerce ranking, where small accuracy gains translate to real revenue~. The field has moved from shallow feature-interaction models such as FM~, DeepFM~ and DCN-v2~ to deep Transformer-based architectures~ that jointly model behavior sequences and feature interactions~. Recent industrial ranking systems have likewise scaled up backbone depth and width: OneTrans~ at TikTok stacks deep causal Transformers, while TokenMixer-Large~ at ByteDance and UniMixer~ at Kuaishou pursue parallel token-mixing designs. All three report clear online gains and motivate a closer look at how depth is actually used inside such models. As these models grow, a less obvious component becomes a bottleneck: the . The standard form $_l = _{l-1} + f_l $~ keeps gradients alive, but it carries three side effects we observe in CTR Transformers: Under Pre-Norm~, layer outputs are added to normalized residuals, so early-layer features are gradually washed out. In CTR, this includes the layers that encode the user's earliest click signals. The identity skip preserves everything with weight $1.0$. When user interests drift, stale signals stay in the residual stream and interfere with the prediction. Each layer can only read $_{l-1}$, so any pattern that lives across non-adjacent layers is invisible. Several recent works rethink the inter-layer connection in language models. DenseFormer~ uses static depth-weighted averaging---48 layers match a 72-layer Transformer. Attention Residuals ~ replaces fixed weights with Softmax cross-layer attention and reports $ $25\ CTR Transformers are not just shallow language models. They are typically 4--12 layers; the input is a heterogeneous mix of categorical, numerical, and sequential features rather than a token stream; and a single prediction often has to encode several interests in parallel. Zhang et al.~ show via Rademacher complexity that the relevant scaling lever is the number of feature fields $F$, not the sequence length $n$. Wukong~ pushes this empirically: a dual-path design keeps scaling across two orders of magnitude on 146B examples while single-path baselines saturate. Building on Dual Path Networks ~---which combined ResNet's first-order reuse with DenseNet's~ high-order exploration under the HORNN view of recurrent depth~---we propose , a dual-path inter-layer connector for CTR Transformers with three components: an with fixed identity weights, keeping gradient flow and first-order reuse intact; a that attends, layer by layer, over compressed block summaries of every earlier layer for high-order recall; a that lets each hidden dimension decide how much weight to give either path. We then propose , swapping the Softmax in the cross-layer attention for a SiLU activation~. This breaks the zero-sum competition that Softmax imposes: several earlier blocks can be activated at the same time, which matches CTR's parallel multi-interest pattern, and the negative branch of SiLU lets the layer mute irrelevant history---following the same intuition behind HSTU's removal of intra-layer Softmax~. The design rests on a single principle: Plain residuals are stable but static; DenseFormer and AttnRes are adaptive but drop the identity protection; learnable residual matrices add mixing capacity but accumulate spectral attenuation across depth. DeRes splits the two roles---the identity path handles low-order and shallow signals, the AttnRes path retrieves higher-order ones from earlier layers---which fits the heterogeneous structure of CTR inputs and, as our results show, optimizes more cleanly. The contributions of the paper are: We introduce Attention Residuals into CTR Transformers for the first time and show that the resulting gain is comparable to that observed in language models, and in relative terms larger given the shallower depth and heterogeneous inputs characteristic of CTR data. We introduce DeRes, a DPN-style dual-path connector that pairs an identity residual with a block-level attention residual, grounded in the HORNN view. We introduce Pointwise AttnRes with SiLU, a CTR-specific replacement for Softmax cross-layer attention that supports non-competitive multi-interest encoding. We evaluate against twelve baselines on a large-scale industrial dataset, Criteo, and Avazu---six feature / sequence models and six inter-layer connectors ---with ablations, a scaling-law fit, and per-segment analyses. CTR prediction has evolved from feature engineering and shallow models to deep neural architectures. Factorization Machines ~ model pairwise feature interactions. Wide\&Deep~ combines memorization and generalization. DeepFM~ integrates FM with deep networks; xDeepFM~ adds a compressed interaction network for explicit high-order feature crossing. DCN-v2~ models explicit cross features via cross networks. AutoInt~ and FiBiNET~ use attention to learn feature interactions automatically. For user behavior modeling, early RNN-based approaches such as GRU4Rec~ encoded sessions with recurrent units; DIN~ and DIEN~ then introduced attention-weighted interest extraction for CTR. Transformer-based approaches~ brought self-attention to sequential CTR: BST~ applies Transformer to behavior sequences; SASRec~ uses causal self-attention for next-item prediction. HSTU~ demonstrated trillion-parameter sequential Transformers for recommendation. Recent industrial work has focused on scaling CTR Transformers. OneTrans~ unifies heterogeneous features into a single Transformer with standard residual connections, reporting +0.748\ A summary of inter-layer connection methods across the full evolution is provided in Table~. for measured CTR overhead under our unified protocol.} {2.5pt} {lcccccc} & & & & & & \\ & & & & & & \\ ResNet & fixed 1.0 & & $l$-1 only & General & 0\ DenseNet & concat & $ $ & all prev. & Vision & high & $ $ \\ DPN & fixed+concat & & all prev. & Vision & moderate & $ $ \\ DenseFormer & static & $ $ & all prev. & LLM & $ $5\ HC/DHC & learnable & $ $ & $n$-way & LLM & $ $3\ AttnRes & Softmax & $ $ & all prev. & LLM & $<$5\ mHC & doubly stoch. & $ $ & $n$-way & LLM & 6.7\ MUDDFormer & MLP & $ $ & all prev. & LLM & 0.4\ ANCRe & softmax & $ $ & all prev. & LLM & $<$1\ & & & & & The standard residual connection $_l = _{l-1} + f_l $~ has been the default inter-layer mechanism since 2015. DenseNet~ concatenates all previous layer outputs, providing direct access but with linearly growing memory. DPN~ combines both paradigms in parallel, justified by the HORNN framework~ that views ResNet as first-order and DenseNet as higher-order recurrent networks. In the Transformer era, DenseFormer~ learns depth-weighted averaging coefficients, making 48 layers equivalent to 72. HC~ expands the residual stream to $n d$ dimensions with learnable $n n$ transition matrices. Its manifold-constrained variant mHC~ uses Sinkhorn-projected doubly stochastic matrices for stability. However, recent experiments on Qwen3-1.7B/8B show that Identity residual matrices outperform learned alternatives---doubly stochastic matrices with $| _{ }| 0.49$ cause cumulative signal collapse: $ _{ } 0.49^{56} 4.5 10^{-18}$ over 28 layers. AttnRes~ replaces fixed residual weights with Softmax-based dynamic cross-layer attention, with clear gains on Kimi-48B. MUDDFormer~ uses four independent dynamic connections for Q/K/V/R, getting 2.8B-parameter performance from 6.9B-parameter quality. ANCRe~ shows that residual topology has exponential impact on convergence speed, and that ingoing softmax normalization is needed for stable training. CTR Transformers are different in three concrete ways: heterogeneous inputs , much shallower depth , and parallel multi-interest encoding instead of a single autoregressive context. To our knowledge, dynamic inter-layer connections have not previously been adapted to CTR, and DeRes does so with DPN-style dual-path protection and a CTR-specific pointwise activation. Given a user's feature set and behavior sequence, the input is embedded as $^{ } ^{n d}$ where $n$ is the sequence length and $d$ the hidden dimension. A stack of $L$ Transformer layers~ processes this input, with each layer $l$ consisting of multi-head self-attention and feed-forward network sub-layers with Pre-Norm~ and LayerNorm~: ^{ } &= ^{ } + })), \\ ^{ } &= ^{ } + })). The final prediction uses the target item's representation: $ = }_{})$. The identity skip connection $_l = _{l-1} + f_l $ provides a direct gradient path but constrains information flow: layer $l$ can only access its immediate predecessor, earlier signals are progressively diluted, and once information enters the residual stream, it cannot be selectively forgotten. A CTR-friendly inter-layer connector should satisfy four properties that the standard residual does not jointly provide. The connector must keep gradients clean. CTR data is sparse and high-cardinality, so rare-feature signals are easily lost when depth-wise transformations are unstable. The identity skip remains the most reliable way to preserve this path. The connector should determine which earlier representations matter for the current prediction. Behavior sequences contain repeated clicks, stale preferences, and noisy exposures, and a fixed skip cannot distinguish long-term interests from outdated ones. A user often holds several interests simultaneously---electronics, apparel, sports---which must be encoded together. Forcing cross-layer weights to sum to one suppresses the secondary interests. The connector must fit within a ranking-stage budget. Dense layer-to-layer attention scales poorly with depth, so we compress earlier layers into blocks and attend over block summaries rather than over every layer. DeRes addresses these requirements through complementary components: the Identity path provides stability, the AttnRes path provides selectivity, the Pointwise activation provides parallelism, and block compression keeps the cost down. {figures/DeRes_architecture.pdf} ~CTR pipeline: embedding, channel split , $L$ DeRes layers with a shared block bank, target-aware pooling, and prediction head. ~A DeRes layer: Path~1 and Path~2 ; Path~2 accumulates layer outputs and writes to the block bank when $l {=}0$, then attends over the bank with $ _{l,b}{=} )$. The two paths fuse via a vector-wise gate $_l$. The four design goals map to Identity, AttnRes, SiLU, and block compression respectively. ~Pointwise AttnRes replaces Softmax with SiLU for parallel multi-interest activation and negative-valued soft forgetting.} Figure~ summarizes the design: panel~ places DeRes in the full CTR pipeline, panel~ zooms into a single layer {=}0$, and the vector-wise gate), and panel~ contrasts Softmax with Pointwise SiLU cross-layer attention. The Algorithm~ listing later in this section makes the bank-write and read steps explicit. The first path maintains the standard residual connection with identity skip weights: _l = _{l-1} + f^{}_l where $f^{}_l$ denotes the Transformer sub-layer . Under the HORNN framework~, this corresponds to a recurrence that reuses features from the immediate predecessor. We use a fixed identity matrix rather than a learnable residual matrix, both for the theoretical reason in Proposition~ and because Qwen3 experiments report that $^{res}{=}$ ends up beating the learned alternatives in practice. The second path dynamically attends to outputs from all previous layers, grouped into $B$ blocks for efficiency. Given $L$ layers divided into $B$ blocks, the $b$-th block output is the sum of layer outputs within that block: _b = _{l _b} _l where $_l$ is the Transformer sub-layer for the AttnRes path. Let $b $ denote the index of the most recent block written before layer $l$. The cross-layer attention weight for block $b$ at layer $l$ is computed as : _{l,b} = _l )}{ _{b'=0}^{b } )} where $_l ^{d/2}$ is a static trainable query vector for layer $l$ . The AttnRes path output is: _l = _{b=0}^{b } _{l,b} _b Under the HORNN framework, this path is a recurrence: later layers can read directly from compressed summaries of any earlier stage rather than only from the immediate predecessor. The outputs of both paths are fused via a vector-wise sigmoid gate: _l = _l = _l _l + _l where $_g ^{ d}$, $_g ^{d/2}$, $ $ is the sigmoid function, and $ $ denotes element-wise multiplication. Here $_l, _l ^{d/2}$ , so the gate $_l ^{d/2}$ acts at the same per-path resolution. The vector-wise gate allows each hidden dimension to independently select its information source---some dimensions may favor stable reuse while others benefit from cross-layer exploration. The standard AttnRes uses Softmax normalization, which imposes a among layers---increasing the attention weight for one layer necessarily decreases others. In language modeling, where the primary task is next-token prediction from a single context, this competitive selection is appropriate. CTR prediction is different in shape: a user usually carries several interests at once , and the model has to encode them in parallel for a single prediction. HSTU~ demonstrated that removing Softmax inside the attention block of recommendation Transformers helps; the same intuition motivates us to remove Softmax across layers. We therefore propose , which replaces Softmax with the SiLU activation~, defined as $ = x $: _{l,b} = ) SiLU offers three advantages: ~several blocks can take large positive weights simultaneously, which is required for parallel multi-interest encoding; ~the negative branch of SiLU \!<\! 0$) acts as a soft forgetting mechanism for irrelevant layers; ~the absence of a Softmax denominator yields slightly lower latency. We illustrate the difference between Softmax and Pointwise attention in Figure~ . Under Softmax normalization, increasing the weight for one layer must decrease others , forcing the model to select a single dominant layer at each step. In contrast, Pointwise attention allows independent weighting: a user interested in both electronics and sports can simultaneously attend to the layers that encode each interest, without suppression. We give a theoretical account of why dual-path connections beat single-path alternatives in CTR Transformers, organized around three propositions: representational capacity , gradient stability , and a scaling-law lower bound . A short gradient-flow argument links the first two. The Higher-Order Recurrent Neural Network framework~ provides a unified view of inter-layer connections. A standard residual connection corresponds to a first-order recurrence: _l = _{l-1} + f_l , which provides reliable feature propagation but limits the network to additive refinement. By contrast, dense connections correspond to an $L$-th order recurrence: _l = _{k=0}^{l-1} _{l,k} \, g_{l,k} , which enables direct access to all earlier states, at the cost of $O $ memory. DPN~ proved that combining both orders yields strictly larger functional capacity than either alone. We formalize this for the DeRes setting. Let $_{}^L$, $_{}^L$, $_{}^L$ denote the families of $L$-layer functions realizable by, respectively, identity-residual, AttnRes-only, and DeRes networks with the same per-layer $f_l$ and the same parameter budget. Then \[ _{}^L _{}^L \; \; _{}^L. \] Setting the gate $_l \!=\! $ for all $l$ recovers the residual-only architecture; setting $_l \!=\! $ recovers the AttnRes-only architecture. Hence $_{}^L _{}^L _{}^L$. To show strict containment, consider any function $h^* _{}^L$ that uses non-degenerate gates $_l ^d$ on at least one layer to mix dimension-wise outputs from the two paths. Such $h^*$ cannot be realized by either single-path model because: ~residual-only outputs lie in the affine span of $\{_0, f_1 , , f_L \}$ with depth-$1$ access; ~AttnRes-only outputs are convex combinations of block outputs without identity protection. Vector-wise gating breaks both restrictions simultaneously. Let $^{}_l ^{d d}$ be a learnable residual transition at layer $l$, and assume $\|^{}_l\|_2 $ with smallest singular value $ _{ } \! \! < 1$. Define the cumulative residual map $_L = _{l=1}^{L} ^{}_l$ acting on a shallow signal $_0$. Then \[ \|_L _0\|_2 \; \; ^{L} \, \|_0\|_2, \] i.e.\ the signal decays at least exponentially in depth. In contrast, replacing $^{}_l$ with the identity map yields $_L = $ and $\|_L _0\|_2 = \|_0\|_2$ for all $L$. By submultiplicativity of singular values, $ _{ } _{l=1}^{L} _{ } ^L$. Hence there exists a unit vector $$ with $\|_L \|_2 ^L$. Taking $_0$ aligned with this direction gives the stated bound. For the manifold-constrained mHC~, the doubly stochastic matrices observed empirically on Qwen3-1.7B/8B exhibit $ 0.49$. Treating each MHA and FFN sub-layer as a separate transition , Proposition~ gives the worst-case bound \[ _{ } \; \; 0.49^{16} \; \; 1.1 10^{-5}, \] i.e.\ a vector aligned with the worst direction is attenuated by nearly five orders of magnitude before reaching the prediction head. Identity residuals avoid this collapse , which is precisely why DeRes Path~1 fixes $^{}_l \!=\! $ rather than learning it. Combining Proposition~ with the chain rule, the gradient of the loss $$ with respect to a shallow representation $_0$ contains a contribution from the identity residual path, }{ _0} \;\,=\;\, }{ _L} _{l=1}^{L}\! \;+\; , where the residual-path product contains the unobstructed identity term $$ at every layer. The AttnRes path contributes additional terms of the form $ _{b} _{l,b}\, $ that depend on the learned attention weights $ _{l,b}$. The dual-path design therefore provides two complementary gradient routes: a guaranteed identity route from Path~1 and a content-adaptive route from Path~2. Zhang et al.~ proved via Rademacher complexity that the generalization gap of CTR Transformers is upper-bounded by a quantity depending on the number of feature $F$ and not on sequence length $n$. Concretely, for an $L$-layer CTR Transformer with width $d$ and effective interaction order $K$, the generalization bound scales as $} $ with $N$ training samples. The exponent $K$ is therefore the dominant lever for capacity. Under the FAT generalization bound~, let $K_{}$ and $K_{}$ denote the effective cross-layer interaction orders of a residual-only and a DeRes network, respectively. If $K_{} K_{} + 1$, then their fitted scaling-law exponents in $ = - C^{- }$ satisfy $ _{} > _{}$. A higher effective interaction order $K$ grows the achievable hypothesis class without proportionally inflating the Rademacher complexity bound . Each unit of extra compute $C$ then buys a strictly larger AUC gain, which shows up as a steeper power-law exponent $ $ in the empirical scaling fit. Standard residuals support only $K_{}{=}1$ effective cross-layer interaction , whereas DeRes's AttnRes path can attend to all $B$ block summaries and therefore satisfies $K_{} K_{}{+}1$ for any $B 2$ at near-constant cost, which is the precondition of Proposition~. This qualitatively predicts $ _{} > _{}$, which matches our scaling fit in Section~ . The exact functional form of $ $ versus $B$ depends on data-specific feature-interaction statistics and is therefore left to empirical fitting rather than closed-form derivation. The Identity path's role parallels Wukong's Linear Compressed Block for low-order field interactions, while the AttnRes path mirrors Wukong's Factorization Machine Block for high-order exploration~. Wukong observed power-law scaling across two orders of magnitude on 146B examples while single-path baselines saturated, which provides external evidence for the dual-path design principle. The per-layer overhead of DeRes over a standard Transformer is $O $: $O $ for the cross-layer block dot products and $O $ for the gate projection $_g ^{ d}$ $ to leading order). With $B \! \! d$ and the dominant Transformer cost $O $, this adds $<5\ Input embeddings $^{ } ^{n d}$; layers $L$; blocks $B$ Output $^{ }$ $_0 ^{ } $ $_0 ^{ } $ $ $ $ $ } $_l _{l-1} + _l^{} $ } $ + _l^{} $ $. $ $ $ $ _{b=0}^{||-1}$ $ _{l,b} $ for each $b$ {=}x $; see Eq.~} $_l _b _{l,b} $ } $_l $ } $_l _l _l + _l$ } $^{ } = $ AttnRes was developed for language models, so a natural question is whether the gain transfers to CTR. Four properties of CTR amplify it. Industrial CTR Transformers run at 4--12 layers~ versus 32--128 in LLMs, so each layer carries a larger fraction of the prediction and improvements to inter-layer flow yield proportionally larger benefits. ID lookups, attributes, context, and behavior sequences~ naturally live at different depths; the dual path keeps shallow signals on the identity path while the AttnRes path retrieves the appropriate depth on demand. A CTR model encodes several interests in parallel for a single prediction~, so Softmax across layers is the wrong inductive bias---HSTU~ demonstrated this for inside-block attention. An unweighted residual cannot down-weight stale signals~; the AttnRes path can, and Fig.~ shows that the trained SiLU weights actually use the negative branch to suppress irrelevant early layers. We evaluate on a proprietary industrial dataset and two public CTR benchmarks . The dataset is a sequential recommendation log from a large-scale social-media platform, comprising $8.6$M users, $35.8$M items, and $331.1$M user--item interactions, with an average behavior-sequence length of $38.5$. Each item is represented by a pre-computed 128-dimensional embedding fusing text, image, and behavior signals; sequences are truncated at $1{,}000$ recent actions and split chronologically. Its scale and structure mirror those reported for OneTrans, TokenMixer-Large, and UniMixer. ~ contains $45.8$M ad impressions with $13$ numerical and $26$ categorical features . ~ contains $40.4$M mobile click records with $22$ categorical fields . For Criteo, we replace missing values with field means, discretize numerical features into 10 equal-frequency buckets, and apply log transformation to count features. For Avazu, we hash high-cardinality categorical fields to at most 100K unique values. All datasets are split chronologically. We compare against twelve methods spanning four categories: : DeepFM~, DCN-v2~, AutoInt~, FiBiNET~. : DIN~, SASRec~. : DenseFormer~, mHC~, AttnRes~. : OneTrans~, TokenMixer-Large~, UniMixer~. For fair comparison, all Transformer-based methods share an 8-layer backbone ; each baseline is implemented by changing only the inter-layer mechanism, with the specific design summarized in Table~. The inclusion of mHC and AttnRes lets us directly contrast our Identity+SiLU dual path against ~learnable residual matrices that may suffer cumulative spectral collapse and ~Softmax cross-layer attention without residual protection. DeRes uses $L{=}8$ Transformer layers with hidden dimension $d{=}128$ , $4$ attention heads, and $B{=}8$ blocks. We train with Adam~ , linear warm-up over $5\ All experiments run in PyTorch~2.1 with mixed-precision on a single node with $8 $ NVIDIA A100-80GB GPUs and AMD EPYC 7742 CPUs. Each Industrial epoch takes $156$\,s; latency in Table~ is measured on a single GPU. We report AUC and LogLoss; a $0.1\ Table~ presents the main experimental results across all three datasets. We report two DeRes variants: DeRes-S and DeRes-P . , second-best . $ $: methods whose primary contribution is an inter-layer connection design. DeRes-P obtains the best AUC, GAUC, and LogLoss on all three datasets; Industrial numbers are averaged over three seeds .} {3.5pt} {l ccc cc cc} & {c}{} & {c}{} & {c}{} \\ {2-4} {5-6} {7-8} & AUC$ $ & GAUC$ $ & LogLoss$ $ & AUC$ $ & LogLoss$ $ & AUC$ $ & LogLoss$ $ \\ DeepFM~ & 0.7891 & 0.7718 & 0.4452 & 0.8076 & 0.4434 & 0.7984 & 0.3743 \\ DCN-v2~ & 0.7943 & 0.7774 & 0.4404 & 0.8129 & 0.4384 & 0.8004 & 0.3729 \\ AutoInt~ & 0.7912 & 0.7741 & 0.4429 & 0.8104 & 0.4407 & 0.7991 & 0.3737 \\ FiBiNET~ & 0.7926 & 0.7757 & 0.4416 & 0.8121 & 0.4391 & 0.8013 & 0.3722 \\ DIN~ & 0.7988 & 0.7825 & 0.4358 & 0.8071 & 0.4437 & 0.7986 & 0.3742 \\ SASRec~ & 0.8004 & 0.7846 & 0.4341 & 0.8086 & 0.4424 & 0.7993 & 0.3736 \\ DenseFormer$^ $~ & 0.8003 & 0.7843 & 0.4345 & 0.8108 & 0.4402 & 0.8001 & 0.3732 \\ mHC$^ $~ & 0.8011 & 0.7852 & 0.4336 & 0.8118 & 0.4393 & 0.7997 & 0.3738 \\ AttnRes$^ $~ & 0.8029 & 0.7873 & 0.4317 & 0.8101 & 0.4411 & 0.8014 & 0.3721 \\ OneTrans$^ $~ & 0.8019 & 0.7861 & 0.4327 & 0.8124 & 0.4387 & 0.8011 & 0.3724 \\ TokenMixer-L$^ $~ & 0.8038 & 0.7884 & 0.4308 & & & 0.8016 & 0.3720 \\ UniMixer$^ $~ & 0.8032 & 0.7878 & 0.4313 & 0.8133 & 0.4378 & & \\ DeRes-S$^ $ & & & & 0.8137 & 0.4376 & 0.8024 & 0.3713 \\ $^ $ & & & & & & & \\ & & & & & & & \\ {r}{ } & +0.32\ {r}{ } & +1.49\ Table~ reveals three observations. First, on the Industrial dataset DeRes-P obtains a $+0.32\ Table~ isolates the contribution of each component on the Industrial dataset. ~Path contributions . ~Attention activation and parameter strategy. Block-granularity sweep: Fig.~. Bold: default DeRes-P.} {3pt} {l c c c c} {c}{} \\ & & & & \\ Residual Only & Identity & --- & 0.8019 & 0.4327 \\ Learnable Res & Doubly stoch. & --- & 0.8011 & 0.4336 \\ AttnRes Only & --- & Softmax & 0.8029 & 0.4317 \\ AttnRes Only & --- & SiLU & 0.8041 & 0.4304 \\ DeRes w/ Add Fusion & Identity & SiLU & 0.8052 & 0.4293 \\ DeRes w/ Learnable Res & Doubly stoch. & SiLU & 0.8044 & 0.4301 \\ & & & & \\ {c}{} \\ & {l}{} & & \\ {*}{Attn.\ Type} & {l}{Softmax} & 0.8056 & +4.4\ & {l}{Sigmoid} & 0.8059 & +4.2\ & {l}{ReLU} & 0.8048 & +4.0\ & {l}{} & & {*}{Params.} & {l}{Shared, $d$} & 0.8043 & +2.1\ & {l}{} & & {l}{Indep., $d$} & 0.8068 & +9.7\ Several findings emerge from panel . Replacing Identity with a doubly stochastic learnable matrix the residual-only baseline by $0.08\ Each item carries a pre-fused 128-dimensional embedding aggregating text, image, and behavior signals. Retraining DeRes-P and OneTrans with one modality masked , DeRes-P's advantage is largest for the text$+$image pair without behavior ($+0.48\ {5pt} {lccc} & & & \\ Text + Image + Behavior & 0.8019 & & +0.45 \\ Text + Image & 0.7894 & & +0.48 \\ Text + Behavior & 0.7972 & & +0.31 \\ Image + Behavior & 0.7948 & & +0.36 \\ Behavior only & 0.7841 & & +0.26 \\ Text only & 0.7738 & & +0.17 \\ Image only & 0.7706 & & +0.25 \\ {fig_bsweep.pdf} 8$ is the empirical optimum; $B{=}16$ and $B{=}L$ sit within seed noise. Grey zone: diminishing returns.} {lccccc} & & & & & \\ & & & & & \\ OneTrans & 12.4 & 1.82 & 142 & 3.1 & 0.8019 \\ DenseFormer & 12.9 & 1.91 & 158 & 3.4 & 0.8003 \\ mHC & 12.7 & 1.86 & 149 & 3.3 & 0.8011 \\ AttnRes & 13.0 & 1.89 & 153 & 3.4 & 0.8029 \\ TokenMixer-L & 13.1 & 1.88 & 151 & 3.3 & 0.8038 \\ UniMixer & 12.8 & 1.89 & 155 & 3.5 & 0.8032 \\ DeRes-P & 13.2 & 1.90 & 156 & 3.4 & \\ DeRes-P achieves the best AUC at $+6.5\ Figure~ plots the cross-layer attention weights $ _{l,b}$ for DeRes-P on the Industrial dataset. Three patterns are apparent in the heatmap: ~a dominant diagonal where each layer focuses on near-recent blocks, ~persistent $B_0$ attention from deep layers that retrieves embedding-level signals, and ~near-zero weights on irrelevant blocks. Figure~ decomposes the SiLU-specific forgetting behavior: negative entries concentrate at early blocks in deep layers ($B_0$: 18\ {fig_interpretability.pdf} ~Cross-layer attention heatmap: diagonal flow with persistent $B_0$ recall from deep layers . ~SiLU forgetting: deep layers suppress early blocks (18\ Across four orthogonal slices of the Industrial dataset---sequence length, model depth, user activity level, and item popularity---DeRes-P's advantage over OneTrans grows monotonically with slice difficulty, reflecting a common mechanism: cross-layer recall is most useful when a single immediate-predecessor residual cannot carry enough information by itself. : as the cap grows from $200$ to $2{,}000$, the gain widens from $+0.23\ {fig_seqlen.pdf} 000$; DenseFormer stays flat at $+0.09$~pp.} ~Users by history length . ~Items by popularity; tail benefits most. $ $: DeRes-P $-$ OneTrans .} {3pt} {!}{ {lccccccc} {c}{} \\ & & {c}{} & {c}{} & {c}{} \\ {3-4} {5-6} {7-8} & & OneTrans & DeRes-P & OneTrans & DeRes-P & AUC & GAUC \\ Short & 31.4\ Medium & 46.7\ Long & 21.9\ }\\ {3pt} {lcccccc} {c}{} \\ & & & & & & \\ Head & 0.8128 & 0.8123 & 0.8133 & 0.8146 & & +0.26 \\ Torso & 0.8017 & 0.8009 & 0.8021 & 0.8038 & & +0.45 \\ Tail & 0.7798 & 0.7773 & 0.7818 & 0.7822 & & \\ A central motivation of DeRes is that structured inter-layer connections are a prerequisite for scalable CTR Transformers. Following the scaling law methodology of Wukong~ and FAT~, we fit power-law curves to the relationship between model FLOPs and test AUC on the Industrial dataset. We train OneTrans, DenseFormer, and DeRes-P at five complexity levels by varying depth $L \{2,4,8,12,16\}$ and hidden dimension $d \{64, 128\}$, yielding FLOPs ranging from 0.3G to 4.2G. For each configuration, we fit the empirical scaling law: = - C^{- } where $C$ denotes total FLOPs per sample and $ , , > 0$ are fitted parameters. A larger exponent $ $ indicates more efficient scaling: each unit increase in compute produces a bigger AUC gain. Figure~ plots the results on a log-linear scale. DeRes-P fits a steeper power-law exponent than OneTrans and DenseFormer ---each unit of compute buys $1.66 $ more AUC for DeRes than for OneTrans. The AUC gap grows with compute, reaching roughly $0.4$~pp by $2$G FLOPs and widening further at larger scale, while DenseFormer trails OneTrans throughout. In operational terms, DeRes attains the AUC of OneTrans-16L using only 8 layers, a $2 $ compute saving at equivalent quality. The pattern aligns with the FAT~ prediction that architectures aligned with the interaction structure of CTR data scale more efficiently. The asymptote $ $ is not directly comparable across methods with different $ $: a flatter curve extrapolates to a numerically larger $ $ in the $C\! \! $ limit, so the practically informative quantity is $ C^{- }$ at finite $C$, where DeRes-P dominates throughout. {fig_scaling.pdf} fits $ $: OneTrans $ $, DenseFormer $ $, . DeRes-P attains $1.66 $ compute efficiency; the grey annotation marks the iso-AUC equivalence . Bands: $ 1$ residual std.} The scaling advantage aligns with the HORNN view and FAT's Rademacher analysis: standard residuals support only first-order feature propagation, whereas DeRes's AttnRes path raises the effective interaction order so that each additional layer both refines and recombines signals from earlier stages---the same mechanism behind Wukong's sustained scaling~. Training dynamics agree: DeRes-P reaches $99\ DeRes is a drop-in replacement for residual connections in CTR Transformers, leaving features, candidate generation, loss, and serving interfaces unchanged. The $+0.32\ We use chronological splits and identical preprocessing across models. All Transformer baselines share the same depth, width, heads, optimizer, and stopping criterion---only the inter-layer mechanism varies---to isolate the effect of DeRes. For each public-dataset result we report the mean over three seeds; for the Industrial dataset we report mean $ $ std over three seeds and confirm statistical significance using paired bootstrap . DeRes targets inter-layer flow and is orthogonal to feature engineering, sampling, and candidate generation; gains may vary when those dominate. The optimal $B$ may depend on $L$ and sequence length; we did not derive a closed-form rule. We study pointwise CTR; multi-task ranking needs further task-specific routing study. All experiments use Pre-Norm following recent CTR practice~; Post-Norm transfer is unverified. SiLU is supported by HSTU~ and our ablation, but we lack a closed-form proof that it is the unique optimum among pointwise activations. We presented DeRes, a dual-path inter-layer connector for CTR Transformers. The Identity residual path preserves shallow signals and gradient flow; the Block Attention Residual path attends over compressed summaries of all earlier blocks; a vector-wise gate mixes the two per hidden dimension; and the Pointwise AttnRes variant replaces Softmax with SiLU so that multiple blocks can be active simultaneously and irrelevant ones can be muted. On the Industrial dataset, Criteo and Avazu, DeRes beats twelve baselines by up to $+0.32\ 