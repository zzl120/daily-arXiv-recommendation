 {article} {inputenc} {fontenc} {algorithm2e} {} {} {natbib} l@{}l@{}} Karamvir Singh} & Arvind Jain} \\ Staff AI Engineer & Senior Engineering Manager \\ HighLevel, Inc. & HighLevel, Inc. } Fixed-cardinality retrieval injects a constant top-$K$ chunks into the generator regardless of query complexity, causing over-retrieval for narrow queries and under-retrieval for compositional ones. We describe , a lightweight score-space decision mechanism that controls at inference time using two scores already produced by the standard pipeline: bi-encoder similarity $s_i$ and cross-encoder reranker score $r_i$, with no additional model inference calls required. Its core insight is that cross-encoder affirmation can rescue semantically relevant chunks that bi-encoder retrieval ranks poorly due to vocabulary mismatch---a failure mode unaddressed by fixed-$K$ or single-score thresholding. On MS~MARCO , ScoreGate achieves MRR@10\,=\,0.401 with 35\ On an internal benchmark , ScoreGate observed zero false positives (95\ 97.77--99.34\ 31\,ms added latency. Results on both MS~MARCO and real-world production traffic suggest that adaptive retrieval cardinality can improve retrieval efficiency without degrading retrieval quality. Retrieval-Augmented Generation augments a language model prompt with passages retrieved from an external corpus, enabling document-grounded responses~. The retrieval stage operates in two phases: a embeds the query and corpus documents into a shared vector space and retrieves the top-$N$ candidates by cosine similarity; a scores each candidate by performing full query--document attention, producing measurably more accurate relevance estimates than embedding similarity alone~. The generator then receives a fixed-cardinality subset of top-$K$ chunks, where $K$ is a hyperparameter set before deployment. The fixed-$K$ constraint imposes a hard upper bound on retrieval cardinality that is mismatched to query complexity. A navigational query such as ``'' is fully answered by a single chunk, whereas a compositional query such as ``'' may require eight or more chunks. Setting $K$ conservatively causes coverage gaps on broad queries; setting it liberally injects low-relevance distractors that increase generation error rate and token cost~. Neither a larger nor a smaller $K$ resolves this tension because the optimal cardinality is a function of the query, not a constant. Existing approaches address retrieval but generally do not treat cardinality as a first-class decision. Rerankers reorder candidates but do not select a subset. LLM-based chunk filters~ prompt a language model for a binary keep/discard decision per chunk, achieving good precision but introducing an additional inference call per query. Single-score threshold cutoffs are unreliable: high embedding similarity does not guarantee contextual relevance, and low embedding similarity does not exclude relevance . We observe that the score pair $ $ carries measurably richer signal than either score in isolation. The two scores are complementary: $s_i$ captures lexical and semantic proximity in the embedding space, while $r_i$ captures contextual fit via full cross-attention. Chunks where both scores agree can be classified deterministically; chunks where they disagree require an empirically motivated fusion strategy. We observe a further structural failure in standard pipelines: bi-encoder retrieval systematically under-ranks chunks that are semantically relevant but expressed using vocabulary absent from the query (paraphrases, domain-specific synonyms, indirect references). The cross-encoder reranker, which performs full query--document attention, often correctly scores these chunks as relevant despite their low embedding similarity. A single-score threshold on either $s_i$ or $r_i$ cannot recover these chunks reliably. The joint score pair $ $ carries the signal needed to identify and rescue them. This motivates . ScoreGate is a lightweight score-space decision mechanism rather than a new retrieval architecture. It slots into any existing two-stage RAG pipeline without architectural changes, additional training, or extra model inference calls. Its main design contributions are: A four-region partition of the $ $ score space, with deterministic rules for agreement regions and threshold-based fusion for disagreement regions---requiring no model calls beyond the standard reranker. Bucket-specific retention thresholds derived from the observation that reranker-affirmed chunks in B3 are more likely true positives than reranker-rejected chunks in B2 at equivalent fusion scores. Observed precision within 95\ filtering), 34.8\ a 300-triple annotated benchmark and a real-world query dataset of 1,247 live queries. A MAX-$K$ ceiling prevents context-window overflow and preserves backward compatibility with fixed-context downstream components. Lewis et al.~ established RAG as a paradigm for knowledge-intensive NLP. Subsequent work extended RAG to iterative retrieval--generation loops~ and pre-/post-retrieval optimisation strategies~. Wang et al.~ showed that injecting low-relevance passages degrades generation quality proportionally to the fraction of irrelevant content in the context window; they address this via learned context filtering at generation time. A concurrent line of work~ addresses the same problem via query-oriented passage scoring before the generator sees any context. ScoreGate is complementary to both: we determine chunks to pass to the generator, rather than filtering or reranking within a fixed-cardinality set. Bi-encoders retrieve by nearest-neighbor search over pre-computed document vectors~. Cross-encoder rerankers compute a relevance score via full self-attention over the query--document pair, producing more accurate estimates at higher cost~. ColBERT~ achieves a favourable accuracy--efficiency trade-off using token-level late interaction. In production RAG, the standard architecture is bi-encoder retrieval of top-$N$ candidates followed by cross-encoder reranking to produce top-$K$ chunks for the generator. ScoreGate intervenes at this second stage, replacing fixed-$K$ truncation with score-adaptive selection. FLARE~ conditions retrieval triggering on generator token-level confidence. Self-RAG~ trains the generator to emit control tokens indicating retrieval necessity and passage relevance. Both methods address retrieval ; ScoreGate addresses retrieval (how many to retain)---orthogonal problems that can be composed. A small body of work addresses dynamic stopping and cardinality control in retrieval. ~ truncates the ranked list when the score gap $r_i - r_{i+1}$ exceeds a threshold, exploiting the intuition that large gaps signal relevance boundaries. in Self-RAG~ conditions the number of retrieved passages on token-level generation confidence. methods~ determine retrieval necessity from entity popularity, bypassing cardinality decisions for well-memorised facts. ScoreGate is complementary to these approaches: it operates after reranking using scores already computed, requires no additional model calls, and specifically targets the vocabulary-mismatch recovery problem unaddressed by gap-based or confidence-based methods. Table~ positions ScoreGate relative to common retrieval methods. ScoreGate is the only method that provides adaptive cardinality using a reranker without extra inference. } {lcccc} & & & & \\ Fixed Top-K & Truncation & & & \\ Score-gap stop & Truncation & & & \\ LLM Filter & Filter & & & \\ Self-RAG & Gen.\ ctrl & & & \\ & Filter & & & \\ CombSUM, CombMNZ~, and Reciprocal Rank Fusion~ combine ranked lists from multiple retrieval systems for rank aggregation. ScoreGate instead fuses two continuous scores from a single candidate set to make a keep/discard decision---a fundamentally different application of score fusion. Let $Q$ denote a user query. A two-stage RAG pipeline produces a candidate set $C = \{c_1, , c_N\}$ of the top-$N$ chunks ranked by the cross-encoder reranker. Each chunk $c_i$ is associated with a score pair: c_i = where $s_i $ is the bi-encoder cosine similarity between the $L_2$-normalised query and chunk embeddings, and $r_i $ is the cross-encoder relevance score normalised to $ $ by min--max scaling over $C$. Per-query min--max normalisation is intentional: the thresholds $ _s$ and $ _r$ are derived from the same normalised distributions, so they are internally consistent. More importantly, the relative ordering of chunks within a query is preserved under any monotone transformation. A chunk classified as B3 is affirmed by the cross-encoder --- the signal we want: whether the reranker considers this chunk relevant given the competition, not whether it exceeds an absolute threshold. The false-positive rate of 5\ was computed on the same per-query normalised scores, so the 5\ claim is consistent with this normalisation. Table~ confirms empirically that per-query min--max scaling outperforms raw logits and sigmoid normalisation on both ARB recall and MS~MARCO MRR@10. Per-query min--max scaling outperforms alternatives by producing the strongest B3/B4 score separation. Results on ARB and MS~MARCO . } {lccc} & & & \\ Raw logits & 96.8\ Sigmoid & 97.5\ & \\ Per-query min--max normalisation produced the strongest separation between B3 and B4 regions, likely because it preserves within-query score ranking while adapting to query-specific reranker score distributions. The standard pipeline selects $C' = \{c_{ }, ,c_{ }\}$ by fixed truncation at rank $K$. We seek instead an adaptive selection function $F$ such that: F C' C, 1 |C'| K_{ } where $F$ requires no model inference beyond the reranker call already performed, and $|C'|$ is determined by the score distribution of $C$ rather than fixed a priori. Figure~ illustrates the complete ScoreGate pipeline. We use the term rather than to emphasise that ScoreGate does not re-rank or post-process a fixed-size set---it determines the of the set passed to the generator, which can be anywhere from 0 to MAX-$K$ depending on the query's score distribution. The distinction matters: a filter operates on a fixed-$K$ input; ScoreGate replaces the fixed-$K$ decision itself. {fig_pipeline.png} A user query triggers bi-encoder vector retrieval of top-$N$ candidates, followed by cross-encoder reranking to obtain score pairs $ $. Data-driven thresholds partition each chunk into one of four buckets . B1 and B3 are retained unconditionally; B4 is discarded unconditionally; B2 candidates are retained only if the fusion score $f_i 0.255$. The filtered set $C'$ is bounded above by MAX-$K$ and passed to the generator. } We obtain thresholds $ _s$ and $ _r$ from query log statistics. $ _s$ is the median bi-encoder cosine similarity over the top-$N$ candidate set aggregated across a representative query sample . $ _r$ is the reranker score corresponding to a false-positive rate of approximately 5\ From our production logs: _s = 0.70 _r = 0.08 Both thresholds are computed once from log data and applied at inference time without per-query recalculation. The threshold derivation procedure is domain-agnostic: re-running the median-similarity and 5\ sample produces new $ _s$, $ _r$ values without any model retraining. Whether the four-bucket structure and fusion weight $ \!=\!0.3$ generalise across domains is an empirical question this is evaluated empirically in Section~4.3 . Figure~ shows empirical score distributions for $s_i$, $r_i$ across the three ARB relevance categories. Irrelevant chunks cluster at low $s_i$ and low $r_i$; Relevant chunks at high $s_i$ and high $r_i$. Semantically Relevant chunks span a wide $s_i$ range but maintain elevated $r_i$---confirming the B3 pattern that motivates ScoreGate's asymmetric thresholds. The right panel shows the distribution of retained chunk counts $|C'|$ across ARB queries, confirming adaptive behaviour: most queries retain 2--5 chunks rather than a fixed $K\!=\!10$. {fig_distributions.png} Score distributions by relevance category on the ARB. Dashed lines mark $ _s\!=\!0.70$ and $ _r\!=\!0.08$. Semantically Relevant chunks have elevated $r_i$ despite low $s_i$---the B3 pattern. Distribution of retained chunks per query on the ARB, demonstrating adaptive cardinality . } The thresholds $ _s$ and $ _r$ partition the unit square $ { } $ of score pairs into four axis-aligned, non-overlapping regions. Each candidate $ $ falls into exactly one bucket, as shown in Figure~ and Table~. {fig2_quadrant.png} Vertical dashed line at $ _s\!=\!0.70$; horizontal dashed line at $ _r\!=\!0.08$. : both scores affirm relevance---keep unconditionally. : high similarity, low relevance---apply fusion threshold $f_i 0.255$. : low similarity, high relevance ---apply lower threshold $f_i 0.15$. : both scores reject---discard unconditionally. Data points are representative examples from the Annotated Relevance Benchmark . } The B3 threshold is intentionally lower than B2 because the cross-encoder has already affirmed relevance in B3; the only disconfirming signal is lexical distance, which we treat as less informative than cross-encoder affirmation. } {llll} & & & \\ B1 & $ _s$ & $ _r$ & Always keep \\ B2 & $ _s$ & $< _r$ & Keep if $f_i 0.255$ \\ B3 & $< _s$ & $ _r$ & Keep if $f_i 0.15$ \\ B4 & $< _s$ & $< _r$ & Always discard \\ For candidates in B2 and B3, a deterministic rule based on a single score is unreliable because the two scores carry conflicting evidence. We compute a weighted linear fusion score: f_i = s_i + r_i, = 0.3 The weight $ \!=\!0.3$ assigns 70\ and 30\ This asymmetry is justified by two properties of cross-encoder rerankers: cross-encoders are trained directly on human relevance judgements via supervised contrastive learning over query--document pairs, producing scores better calibrated to human relevance than cosine similarity in the bi-encoder embedding space; cross-encoder scores are conditioned on the specific query, whereas bi-encoder similarity measures proximity in a shared space optimised for retrieval recall rather than precision. The retention thresholds $ _{B2}\!=\!0.255$ and $ _{B3}\!=\!0.15$ are asymmetric by design. In B2, the cross-encoder has rejected the chunk , so a higher fusion score is required to override that rejection. In B3, the cross-encoder has affirmed the chunk , and the only disconfirming signal is low bi-encoder similarity---a ``lexical gap'' we treat as less informative than cross-encoder affirmation. Figure~ shows $F_1$ sensitivity to $ $ across seven values; the surface peaks at $ \!=\!0.3$ and declines monotonically. {fig_alpha.png} $F_1$ peaks at $ \!=\!0.3$ and declines monotonically in both directions, confirming that reranker-dominant weighting (70\ $r_i$, 30\ The unimodal shape indicates the optimum is stable under evaluated conditions: performance degrades gracefully rather than catastrophically away from $ \!=\!0.3$. } After applying the four-region rules, the retained set $C'$ is bounded by: |C'| When the ceiling is binding , we retain the $K_{ }$ chunks with the highest fusion scores $f_i$. This ranking is empirically motivated: $f_i$ is the same weighted combination of both relevance signals used throughout ScoreGate, so the dropped chunks are precisely those with the weakest combined evidence for relevance among an already-filtered set. In our deployment $K_{ }\!=\!10$. The ceiling was binding in 14 of the 300 ARB queries (4.7\ resulting in 31 dropped chunks. Of these, 23 were Irrelevant , 4 were Relevant, and 4 were Semantically Relevant (8 total false negatives introduced by the ceiling). By bucket, dropped chunks were: B2\,=\,4, B3\,=\,9, B4\,=\,18 . The 9 dropped B3 chunks are the most operationally significant: these are vocabulary-mismatch chunks affirmed by the reranker but with lower $f_i$ than other retained chunks due to their low $s_i$. This confirms the vocabulary-mismatch edge case described in Section~4.1 and motivates raising MAX-$K$ in knowledge-intensive deployment contexts. The complete ScoreGate procedure is summarised in Algorithm~1. Time complexity is $O $ dominated by the final sort. With $N\!=\!40$, this contributes negligible overhead relative to cross-encoder inference. $, thresholds $ _s, _r, _{B2}, _{B3} $, weight $ $, MAX-$K $} K$} $C' $\; { $f_i s_i + r_i$\; { $C' C' \{i\}$ *{B1: always keep} } { $}{$C' C' \{i\}$} * {B2: high bar} } { $}{$C' C' \{i\}$} * {B3: low bar } } } } top-MAX-$K$ of $C'$ ranked by $f_i$ descending\; We evaluate ScoreGate on three datasets spanning public benchmarks and internal deployment data, ordered from most to least reproducible. MS~MARCO~ is a large-scale public passage retrieval benchmark with 8.8M passages and official relevance judgements . We randomly sample 200 queries from the official dev set for evaluation. MS~MARCO provides the primary reproducible evidence for ScoreGate's generalisation, as both the corpus and qrels are publicly available and the evaluation protocol is standard. To validate ScoreGate's behaviour in the specific production deployment context, we constructed 300 (query, chunk, relevance label) triples from the internal knowledge base (approximately 12,000 chunks, average length 180 tokens, split at paragraph boundaries with 20-token overlap). Queries were sampled uniformly at random from the production query log and stratified post-hoc into three equal categories of 100 triples each: , , and . We note that the ARB knowledge base is shared with the corpus used to derive $ _s$, $ _r$, $ $, $ _{B2}$, and $ _{B3}$; queries are drawn from a disjoint time window but corpus-level statistics may overlap. The ARB is therefore best interpreted as deployment validation rather than an independent generalisation test. The 100/100/100 balanced split is a deliberate diagnostic design; Table~ contrasts this with observed production distribution. The ARB uses a balanced design for diagnostic control; real production traffic is dominated by irrelevant candidates. } {lccc} & & & \\ ARB & 33\ RWD & 71\ Annotators were blind to model scores $ $ during labelling. Labels were assigned by three domain-expert annotators with disagreements resolved by majority vote (inter-annotator agreement $\!=\!0.87$ by Fleiss' $ $ for three raters~; per-category $ $: Irrelevant 0.91, Relevant 0.88, Semantically Relevant 0.82). We additionally evaluated retrieval candidate set sizes $N \{20, 40, 60\}$ on the ARB. Recall improves measurably from $N\!=\!20$ to $N\!=\!40$ but saturates beyond $N\!=\!40$ while latency increases linearly. $N\!=\!40$ was selected as the operating point. } {ccc} & & \\ 20 & 95.8\ & To evaluate real-world behaviour we collected 1,247 consecutive live queries from a real-world AI assistant deployment with full retrieval logs. Each query carries a Business Query Detection score in $ $ estimating whether knowledge-base retrieval is warranted or can be skipped . This dataset captures the diversity of real-world production traffic including navigational, compositional, and ambiguous queries. : returns a fixed $K\!=\!10$ chunks regardless of score distribution. : prompts with a zero-shot binary relevance judgement prompt that instructs the model to distinguish topically-related but non-answering passages from passages that directly address the query, without access to $ $. The full prompt and decoding configuration are provided in Appendix~. Before reporting precision and recall, we examine the empirical distribution of chunks across the four buckets on the ARB. Understanding bucket frequency is important because the B3 region is the key design feature of ScoreGate---it recovers semantically relevant chunks that bi-encoder retrieval ranks poorly due to vocabulary mismatch. . B3 accounts for 18.7\ vocabulary-mismatch chunks are a substantial fraction of production retrieval output. } {lccc} & & & To isolate the contribution of dual-score fusion, we evaluated a reranker-only baseline that retains chunk $c_i$ iff $r_i _r$ without using embedding similarity $s_i$. Adding embedding similarity $s_i$ via dual-score fusion improves recall on both categories. Both methods observed zero false positives. } {lccc} & & & \\ Reranker-only & 0 & 94.8\ & & extends the baseline comparison with score-gap stopping and relative-score cutoff , both parameter-matched to maximise $F_1$ on the validation set. ScoreGate outperforms all single-signal baselines on Sem.\ Relevant recall while maintaining zero false positives. All baselines are parameter-matched on the held-out validation set. Precision: zero observed false positives for all methods. } {lcc} & & \\ Relative-score cutoff & 95.4\ Score-gap stopping & 96.1\ Reranker-only & 95.1\ Single-threshold fusion & 96.96\ & Table~ reports internal deployment validation results on the ARB. As noted in Section~5.1, the ARB knowledge base is shared with the threshold-tuning corpus; the ARB precision and recall figures should be read as deployment validation on the tuned domain, not as independent generalisation evidence. MS~MARCO provides the only clean external test. Standard Top-K achieves trivially 100\ because it retains every candidate, but this comes at the cost of injecting all irrelevant and low-relevance chunks into the generator context---a precision measurement requires relevance labels for every retrieved chunk, not just the 300 ARB triples, so we report N/A. The fundamental trade-off is: Standard Top-K maximises recall at unmeasured precision; ScoreGate achieves observed precision within 95\ 97.77--99.34\ ScoreGate achieved observed precision within 95\\ [96.4\ no Irrelevant chunk was retained in this sample. Recall on Semantically Relevant chunks is 99.34\ LLM Filter, a gap of 6.93 percentage points. Applying McNemar's test to the paired binary outcomes on the same 100 Semantically Relevant chunks (ScoreGate retained 99, LLM Filter retained 92; discordant pairs: $b\!=\!7$, $c\!=\!0$) yields $ ^2\!=\!5.14$, $p\!=\!0.023$, confirming the difference is statistically significant at $ \!=\!0.05$. This gap arises from B3 chunks: the LLM Filter, operating without access to the cross-encoder score, tends to reject chunks with vocabulary absent from the query, whereas ScoreGate trusts the cross-encoder affirmation and applies the lower B3 threshold. . Queries are from a production window disjoint from threshold tuning; the knowledge base is shared with the tuning corpus . Best recall values per category are . $^ $``none = zero observed false positives (95\ For the Irrelevant category the value is specificity. $^ $Standard Top-K retains all top-10 retrieved chunks without filtering, so recall is trivially 100\ categories but precision cannot be computed without relevance labels for every retrieved chunk; N/A indicates this. The key trade-off is precision vs.\ recall: ScoreGate achieves precision within 95\ achieves 100\ } {lcccc} & & & & \\ Irrelevant & 0/100 & 100\ Relevant & 0/100 & visualises recall by category. {fig3_recall.png} ScoreGate outperforms LLM Filter by 6.66 and 6.93 percentage points on Relevant and Semantically Relevant categories respectively. Both methods achieve zero false positives across all categories. } Table~ reports efficiency metrics on the ARB. ScoreGate reduces average token consumption by 34.8\ by discarding low-relevance chunks. The 31\,ms latency overhead is attributable entirely to the $O $ bucket classification and sort; no additional model inference is performed. Latency is end-to-end pipeline latency (bi-encoder retrieval + cross-encoder reranking + ScoreGate selection) averaged over 300 queries, measured on a single-threaded process on an AWS exttt{m5.2xlarge} instance ; reported as mean\,$ $\,std across five independent runs (Standard Top-K: $405 12$\,ms; ScoreGate: $436 14$\,ms). Token counts are exact . Avg.\ Relevance is rated on a 1--5 Likert scale by three domain-expert annotators on a randomly sampled subset of 50 generated answers per condition; reported value is the mean\,$ $\,std across raters and samples (Standard Top-K: $4.23 0.31$; ScoreGate: $4.27 0.29$). $ $ denotes reduction is beneficial. } {lccc} & & & \\ Avg.\ Relevance & $4.23 0.31$ & $4.27 0.29$ & $+0.04$ \\ Avg.\ Pipeline Latency & $405 12$\,ms & $436 14$\,ms & $+31$\,ms \\ Avg.\ Tokens/Query & 637 & & $ $\, Table~ reports results on MS~MARCO Passage Ranking~ (200 dev queries, public corpus with official qrels). We test two threshold configurations: derived from our production domain without re-tuning, to assess zero-shot domain transfer; and ($ _s\!=\!0.63$, $ _r\!=\!0.11$) obtained by applying the same calibration procedure to 200 MS~MARCO queries held out from the evaluation set, to assess in-domain performance. (200 dev queries, public benchmark with official qrels). ScoreGate with zero-shot transferred thresholds outperforms Standard Top-K on MRR@10; re-derived thresholds further improve both MRR@10 and Recall@10. Precision and Avg.\ $|C'|$ are computed against MS~MARCO qrels. Avg.\ $|C'|$ is the mean chunks retained per query. } {5pt} {lcccc} & & & & \\ Standard Top-K & 0.387 & 0.903 & 0.712 & 10.0 \\ LLM Filter & 0.361 & 0.812 & 0.957 & 4.8 \\ ScoreGate & 0.392 & 0.871 & 0.944 & 6.1 \\ ScoreGate & & & 0.938 & 6.5 \\ The re-derived $ _s\!=\!0.63$ reflects a broader similarity distribution in MS~MARCO embeddings, while $ _r\!=\!0.11$ reflects sharper reranker separation. Adaptive cardinality is confirmed on MS~MARCO: ScoreGate retains an average of 6.1 chunks/query and 6.5 , vs.\ a fixed 10 for Standard Top-K---a 35--39\ reduction, consistent with the ARB observation. The LLM Filter achieves MRR@10\,=\,0.361, below Standard Top-K . This result is expected and methodologically consistent: the LLM Filter was prompted with production-domain instructions and applied without re-calibration to MS~MARCO. More importantly, the LLM Filter retains only 4.8 chunks/query on average---an aggressive truncation that improves precision but discards MS~MARCO-relevant passages that Standard Top-K retains, reducing MRR@10. This trade-off illustrates the core tension ScoreGate addresses: aggressive filtering gains precision at recall cost; ScoreGate achieves higher recall than the LLM Filter while preserving precision gains over Standard Top-K . Applying original production thresholds without re-calibration still yields competitive performance (MRR@10\,=\,0.392 vs.\ 0.387 for Standard Top-K), demonstrating that ScoreGate degrades gracefully under domain shift. Re-deriving thresholds recovers the full performance gap, confirming that the calibration procedure generalises across domains. Table~ reports abstention behaviour on the RWD. For queries with BQD $<\!50$ , ScoreGate returns an empty set in 87\ Of the 13\ contain genuinely relevant content, indicating classifier false negatives that ScoreGate correctly overrides. The effective correctness rate is $ \!95\ For queries with BQD $ \!50$ , ScoreGate returns non-empty results in 63\ The 37\ queries for which the knowledge base genuinely contains no relevant content, where an empty result correctly prevents hallucination; and queries that appear retrieval-warranted to the BQD classifier but are actually non-retrieval in nature (single-word inputs, navigation commands). Both cases represent correct retrieval decisions. . For BQD $<\!50$, ScoreGate correctly returns an empty set in 87\ of cases; of the 13\ by manual review , yielding $ $95\ For BQD $ \!50$, the 37\ knowledge-base gaps or non-retrieval query types---both correct abstentions. } {llll} & & & \\ $<50$ & Return empty & Empty 87\ $ $95\ $ 50$ & Return chunks & Chunks 63\ Valid --- KB gaps or non-RAG \\ { } The 7:3 weighting of $r_i$ over $s_i$ in Equation~ reflects training objective differences. Bi-encoder similarity $s_i$ comes from embeddings optimised for recall at high-$N$ rather than precision at low-$K$; the embedding space represents topical relatedness rather than answer-level relevance. The cross-encoder reranker is fine-tuned on query--document relevance pairs with direct supervision, making $r_i$ a stronger predictor of context-inclusion appropriateness~. To verify that the four-bucket structure contributes beyond simply applying a single threshold on the fusion score $f_i$, we compare ScoreGate against a single-threshold baseline: keep chunk $c_i$ if $f_i ^*$. $ ^*$ was selected by grid search over $ $ in steps of 0.01, maximising $F_1$ on the same 200-query held-out validation set used to derive $ _s$, $ _r$, $ _{B2}$, and $ _{B3}$ ; the optimal value was $ ^*\!=\!0.19$. On the ARB, ScoreGate achieves 99.34\ chunks vs.\ 94.1\ Relevant chunks . The gain arises because a single $ ^*$ cannot simultaneously accommodate B2 and B3 --- the asymmetry is precisely what the four-bucket structure encodes. ScoreGate produces 3 false negatives on the ARB (2 Relevant, 1 Semantically Relevant missed chunks). All three originate from the B2 or B3 borderline regions; none are from high-confidence B4 rejection cases. All missed chunks involve semantic abstraction or indirect phrasing that reduces both $s_i$ and $r_i$ below the fusion threshold. } {p{3.0cm}p{3.5cm}cp{3.5cm}} & & & \\ ``How does the arbitration clause affect cross-border disputes?'' & Discussed jurisdictional enforceability indirectly without mentioning ``cross-border disputes'' & B3 & Low $s_i$ from indirect phrasing; $r_i$ insufficient to cross $ _{B3}$ \\ ``What are the tenant obligations for repair costs?'' & Used legal terminology (``habitability obligations'', ``lessor responsibilities'') without query vocabulary & B2 & Terminology mismatch reduced both $s_i$ and $r_i$; $f_i$ below $ _{B2}$ \\ ``Can prior approval be revoked after submission?'' & Referenced revocation via procedural exceptions without direct revocation language & B3 & Sparse lexical overlap and low reranker rank; $f_i$ below $ _{B3}$ \\ Table~ classifies each false negative by failure type. All three fall into the category: relevant information present but expressed through indirect phrasing, domain-specific terminology, or procedural references with low lexical overlap. The taxonomy of ScoreGate failure modes is: --- query and chunk use different words for the same concept ; --- answer embedded in procedural/exception language rather than direct statement ; --- relevant content obscured by domain register ; --- chunk answers the query indirectly . These failures are attributable to representation limits of the underlying retrieval models, not to ScoreGate's threshold design. Lowering $ _{B3}$ would recover some but at the cost of increased false positives. We performed sensitivity analysis by varying $ _s $ and $ _r $ in steps of 0.01, and $ $ in steps of 0.05. Across all 75 evaluated combinations, zero false positives were observed and recall varied by at most 3.2 percentage points. This pattern arises because the four-bucket structure confines sensitivity to the B2 and B3 boundary regions; the dominant B1 and B4 regions are stable under evaluated conditions to threshold perturbation by construction. ScoreGate operates entirely on scores already produced during the standard embed-and-rerank pipeline. The only additional computation is $O $ fusion arithmetic and an $O $ sort. With $N\!=\!40$, the observed 31\,ms overhead confirms this analysis. Table~ compares end-to-end latency across methods. The LLM Filter requires one inference call per retrieved chunk and processes top-$N\!=\!40$ candidates independently without batching. At approximately 210\,ms average end-to-end API latency per chunk judgement (consistent with published p50 latency figures), the effective per-query latency is $ $8{,}400\,ms--- a 19$ $ increase over ScoreGate. ScoreGate introduces 31\,ms over the standard pipeline; the LLM Filter adds approximately 8{,}400\,ms due to 40 sequential model inference calls. } {lc} & \\ Standard Top-K & $405 12$\,ms \\ ScoreGate & $436 14$\,ms \\ LLM Filter & $ $8{,}400\,ms \\ We additionally conducted a hallucination analysis on 300 generated answers. A hallucination was defined as a factual claim unsupported by either the retrieved context or the underlying source document. Three annotators independently labelled hallucination presence . ($n\!=\!300$ generated answers; Fleiss' $\!=\!0.79$). ScoreGate reduces hallucination incidence by 4.7 percentage points relative to Standard Top-K by preventing low-relevance context from reaching the generator. } {lc} & \\ Standard Top-K & 11.8\ ScoreGate & Thresholds $ _s$, $ _r$, $ _{B2}$, $ _{B3}$, and $ $ were derived and evaluated on data from the same production system. Although query windows are disjoint and the ARB knowledge base is acknowledged as shared, temporal correlation in production logs may reduce true independence. The 200-query MS~MARCO calibration set and 200-query ARB evaluation set are fully independent. ScoreGate is validated on one proprietary domain and MS~MARCO. Threshold values reflect the score distributions of our specific bi-encoder and cross-encoder models; different model pairs will produce different distributions requiring re-calibration. Hallucination is measured by annotator agreement on factual claim support, which may not capture all generation failure modes. The Avg.\ Relevance Likert proxy is a weak surrogate for end-to-end answer quality. McNemar tests assume paired binary outcomes from the same queries. We confirm this holds for all reported comparisons. The hallucination reduction (11.8\ reported as an observed difference on $n\!=\!300$ answers; a formal significance test on this proportion difference yields $ ^2\!=\!4.82$, $p\!=\!0.028$. In the real-world dataset, irrelevant candidates constituted approximately 71\ expected long-tail retrieval distribution observed in production RAG systems. ScoreGate's four-bucket design is specifically suited to this regime: the B4 discard region handles the dominant irrelevant mass, while B3 recovers the sparse but high-value vocabulary-mismatch chunks that standard filtering misses. Sensitivity analysis across 75 parameter configurations showed stable behaviour, with zero observed false positives and recall varying by at most 3.2 percentage points. Figure~ visualises the $ $ joint $F_1$ surface and the $ $ sensitivity curve. {fig_robustness.png} $F_1$ (\ star marks the selected point ($ _s\!=\!0.70$, $ _r\!=\!0.08$). Surface ${ }\,96\ $F_1$ and Recall vs.\ $ $, both peaking at $ \!=\!0.3$ . } Bootstrap analysis further showed the optimal single-threshold baseline remained within $ ^* $. These results indicate that ScoreGate is not highly sensitive to narrow parameter choices, and that the threshold derivation procedure produces stable operating points across perturbations. The additional computational cost of ScoreGate is limited to fusion arithmetic and sorting over $N\!=\!40$ candidates, adding only 31\,ms average latency relative to the standard retrieval-and-reranking pipeline. This compares favourably against the LLM Filter baseline, which requires 40 sequential inference calls per query at an estimated latency of $ $8,400\,ms --- a 19$ $ overhead. ScoreGate achieves higher recall at a fraction of the inference cost of the LLM Filter. The threshold derivation procedure is domain-agnostic and transfers without architectural modification. On MS~MARCO, ScoreGate with original production thresholds achieves MRR@10\,=\,0.392, outperforming Standard Top-K , and improves further to MRR@10\,=\,0.401 after domain-specific threshold re-calibration using the same derivation procedure described in Section~4.1. The shift in derived thresholds ($ _s$: 0.70\,$ $\,0.63; $ _r$: 0.08\,$ $\,0.11) reflects domain-specific score distributions and confirms that the calibration procedure correctly adapts to new corpora without retraining. ScoreGate's performance degrades when the candidate pool itself has low recall (i.e., the bi-encoder fails to retrieve the relevant chunk in top-$N$). It inherits reranker errors: a cross-encoder that systematically misscores a domain will propagate those errors. Multilingual and code-heavy corpora may exhibit score distributions that require significant threshold re-calibration. Multi-hop queries---where evidence must be synthesised across passages---are not evaluated and represent a known open problem. Evaluation on dedicated multi-hop reasoning benchmarks such as HotpotQA~ and MuSiQue remains future work, as does extension to biomedical, legal, and scientific retrieval corpora. An empirically motivated cost-sensitive derivation of $ _{B2}$ and $ _{B3}$ from asymmetric false-positive/false-negative cost models could replace the current grid-search procedure and enable automatic per-domain calibration. We presented , a practical adaptive retrieval filter that controls context cardinality in two-stage RAG systems. Its key mechanism---using cross-encoder affirmation to recover semantically relevant chunks that bi-encoder retrieval ranks poorly ---accounts for the majority of observed recall improvements over both LLM-based filtering and reranker-only thresholding. On the internal benchmark, observed precision fell within 95\ per category) at 97.77--99.34\ MRR@10\,=\,0.401 on MS~MARCO, with 34.8\ On a real-world dataset of 1,247 live queries, ScoreGate correctly abstains when the knowledge base lacks relevant content and returns non-empty results when retrieval is warranted, with $ \!95\ overall correctness. The 34.8\ to lower operational cost at scale. These results suggest that query-adaptive retrieval cardinality can be determined practically using scores already available in a standard two-stage RAG pipeline, without additional model inference. ScoreGate is best understood as a pragmatic engineering solution: it combines scores already available in any two-stage pipeline with an empirically motivated decision rule to achieve measurable improvements in retrieval efficiency, with results that are stable under the evaluated conditions. Additional out-of-domain evaluation (e.g., BEIR subsets for biomedical and legal retrieval) would strengthen generalisability claims beyond the current MS~MARCO result, which rests on 200 dev queries. *{Data Availability} The ARB triples, RWD query logs, and production knowledge base are proprietary and cannot be released. To facilitate reproduction on other domains, we provide: the complete ARB annotation protocol ; the full LLM Filter prompt and decoding configuration ; the threshold derivation procedure ; and the ScoreGate algorithm in full . A researcher with access to any two-stage RAG pipeline can replicate the full experimental protocol using domain-specific query logs following the procedures described in Section~5. *{Acknowledgements} The authors thank Anshul Chandra , Rohit Singh , Aniruddha Banerjea , and Pratik Patil for their contributions to system implementation, evaluation infrastructure, and annotation support. We also thank the HighLevel Engineering Leadership for supporting this research and providing access to production telemetry data. Bi-encoder: , 1536-dim, cosine similarity, $L_2$-normalised query and chunk vectors. Cross-encoder: , raw logit output normalised per-query via min--max scaling over the top-$N$ candidate set. Paragraph-boundary splitting, target chunk length 180 tokens , 20-token overlap between adjacent chunks. Sample 200 queries from the production log . For each query, retrieve top-$N\!=\!40$ candidates and compute $ $ pairs. Set $ _s$ = median $s_i$ across all retrieved pairs. Set $ _r$ at the 5th percentile of $r_i$ values for human-annotated relevant chunks on a 50-query labelled subset. Grid-search $ _{B2} $ and $ _{B3} $ maximising $F_1$. Grid-search $ $ maximising $F_1$ on the same held-out set. AWS , single-threaded inference. Random seed: 42 for all bootstrap resampling. The LLM Filter baseline uses with the following verbatim prompt : }\\ \\ \\ \\ \\ \\ \\ \\ \\ \\ \\ \\ \\ \\ \\ \\ \\ }\\ }\\ \\ \\ }\\ \\ \\ \\ \\ Model: Temperature: 0 Max tokens: 5 Top-p: 1.0 Presence penalty: 0, Frequency penalty: 0 A chunk was retained if the model response was . The score pair $ $ was provided to the LLM Filter. Each chunk was judged independently; no batch prompting was used. 