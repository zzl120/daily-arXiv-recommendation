 {natbib} {neurips_2026} {} {inputenc} {fontenc} {Proposition} *{remark}{Remark} {} {} {} {} {} \\ Menghang Gong \\ Qixin Guo Hang Zheng Zhaobin Yang Jianguo Lou Zhengwei Zheng \\ } Generative recommendation maps each item to a sequence of Semantic IDs and recasts retrieval as autoregressive token generation. In this paradigm the main bottleneck is the tokenizer rather than the Transformer: residual vector quantization with a hard nearest-neighbor assignment at every layer collapses multi-faceted item semantics at cluster boundaries and propagates early errors to later SID positions. A common workaround is to append a dense vector or attribute prefix to the SID, but this dual-representation design inflates inference cost and gives up the simplicity of a generative interface. We address the bottleneck at the tokenizer itself. replaces hard residual quantization with capsule routing: at each layer an item probabilistically routes to several semantic capsules, the residual is updated by the routed reconstruction rather than by a single winning code, and the SID terminates once the active capsule's confidence is high enough. On top of , composes adjacent SID tokens into reusable subwords by combining their co-occurrence with their embedding compatibility. On Amazon Beauty, Sports, Toys, and a 35M-item proprietary industrial catalog, + improves Recall@10 by $9.6\ Generative recommendation has recently emerged as a unified alternative to retrieval-and-ranking pipelines: an item is converted into a short sequence of Semantic IDs , and a sequence model generates the SID of the next item a user may consume~. This formulation is attractive because it turns retrieval into constrained generation, enables prefix sharing across semantically related items, and supports cold-start items through content-derived IDs~. However, it also shifts a large part of the recommendation problem to a tokenizer. If the tokenizer loses information, the generator can only learn to predict an impoverished target. This information bottleneck is now reasonably well-documented. UniRec formally argues that generative and discriminative recommenders can be equally expressive if the generator has access to complete item attributes, and that the observed gap mainly arises because SIDs cover only a small subset of those attributes~. GRID-style empirical studies further show that adding more residual quantization layers does not monotonically improve recommendation: deeper SID positions often amplify early quantization errors~. GLASS observes a related rank degradation phenomenon, where predicting the first SID token can worsen the rank of the true item before later tokens attempt to recover it~. Existing systems have taken two broad routes. One route patches the sparse SID after quantization: COBRA cascades a dense vector after the sparse ID and fuses beam scores with vector similarity~; UniRec prepends Chain-of-Attribute tokens; LIGER-style systems keep dense retrieval beside SID generation~. These methods are effective, but they make inference heavier and system design less generative. The other route is tokenizer-centric: it improves the SID itself so that the generated sequence preserves more item semantics before any dense or attribute patch is added. Along this line, TIGER establishes the RQ-VAE SID backbone~, LETTER injects collaborative signals into the tokenizer~, and ReSID replaces generic LLM embeddings with recommender-native representations and globally aligned quantization~---yet all of them keep the hard nearest-neighbor assignment at the heart of residual quantization, which is precisely the step we revisit. This distinction has practical consequences. Patch systems often require a second retrieval or re-ranking path, additional ANN infrastructure, and a carefully tuned fusion function; their benefits may diminish once the sparse ID is improved. A tokenizer-centric solution should instead satisfy three properties: , so that the SID stores more than a coarse bucket; , so that the generator can still model the token sequence; and , so that constrained beam search and trie filtering remain valid~. These requirements rule out simply increasing codebook size or SID depth, because both actions enlarge the output space and worsen token predictability. replaces winner-take-all residual quantization with soft agreement among capsules. At each layer the item residual routes to several capsules, votes are aggregated, and the residual is updated by their weighted reconstruction. The norm of the selected capsule doubles as a confidence score, which decides whether another SID position is needed. sits on top: it composes adjacent SID tokens into reusable subwords, but only when both co-occurrence and embedding compatibility back the merge. This paper makes four contributions: We organize recent SID systems into patch-based and tokenizer-centric designs and argue that a better tokenizer removes much of the need for dense or attribute patches. We design , an SID tokenizer built on capsule routing with soft residual assignment, iterative self-correction, and confidence-driven variable length. We design , a differentiable subword module that scores merges by both co-occurrence and embedding compatibility, going beyond frequency-only behavior tokenization. We conduct extensive experiments on three public benchmarks and a 35M-item industrial catalog, showing that + consistently outperforms state-of-the-art tokenizer-centric and patch-route systems at a fraction of the cost, validating soft routing as a viable replacement for the hard residual assignment behind current SIDs. TIGER introduced RQ-VAE SIDs for generative retrieval and established the standard recipe of content encoding, residual quantization, and autoregressive SID prediction~, building on vector quantization and neural discrete representation learning~. LC-Rec improves code usage via Sinkhorn balancing~; LETTER injects collaborative signals into the tokenizer~; ETEGRec alternates tokenizer and generator optimization~; ADA-SID introduces multi-view adaptive quantization~; parallel industrial systems such as DAS~ and Align3GR~ further align SID learning with downstream ranking signals; CoFiRec explores coarse-to-fine tokenization at varying granularity~. These methods differ in supervision, initialization, and regularization, but share a hard assignment core. In contrast, changes the assignment operator itself. Large-scale generative recommenders emphasize that SID quality is only useful when the generated path can be decoded efficiently. Building on earlier generative retrieval with constrained decoding~ and differentiable search indices~, HSTU-style sequential transducers scale the backbone and demonstrate the value of strong sequence modeling~, while OneRec-like systems and STATIC-style trie decoding show that valid-ID filtering is necessary for production latency~. These systems motivate our design constraint: must emit ordinary discrete IDs at inference. Routing is used to construct better SIDs, not to introduce a new inference-time retrieval interface. COBRA combines sparse SIDs with dense vectors and BeamFusion, obtaining strong public and industrial results at the price of a roughly two-stage retrieval path~. UniRec prepends attribute tokens and shows that attribute coverage can close much of the generative-discriminative gap~. LIGER-style hybrids keep a dense retrieval channel beside generative retrieval~. These results strongly support our motivation: the missing information exists, and the question is whether it should be patched after quantization or preserved inside the SID. DIGER uses Gumbel-Softmax to make discrete SID learning differentiable~, building on continuous relaxations for categorical variables~; SA$^2$CRQ truncates hard paths by entropy budgets for adaptive length~; ReSID argues that recommender-native representation learning and global quantization alignment are more important than generic LLM semantic embeddings~. Collision-aware approaches such as QuaSID and GR4AD further show that collisions are not merely an implementation detail but a ranking-quality bottleneck~. is complementary: it uses soft routing as the native quantization primitive and variable length as an outcome of capsule confidence rather than a post-hoc truncation rule. Recent dynamic-indexing systems such as MERGE monitor cluster occupation, item-to-cluster similarity, and cluster-to-cluster separation in streaming environments~. They point to an important evaluation lesson: a tokenizer should be judged not only by final Recall but also by geometry and occupancy diagnostics. We therefore include collision rate, code predictability, intra-code similarity, and routing convergence as first-class metrics rather than relegating them to implementation details. Capsule networks model part-whole agreement through iterative routing~, and MIND applies dynamic routing to user multi-interest extraction~. We transfer the same idea to item tokenization: capsules are no longer user-interest slots but semantic code candidates. For composition, BPE originated in neural machine translation as a subword segmentation method~ and was popularized for general language modeling by SentencePiece~. ActionPiece extends this line to action sequences for generative recommendation~. differs by scoring merges with semantic compatibility as well as frequency. Table~ positions in the design space of recent SID tokenizers along five axes. To our knowledge, is the only existing method that combines soft probabilistic assignment, iterative agreement, confidence-driven variable length, and semantic-aware composition, while still preserving a single discrete generative interface that is compatible with constrained beam search. {4pt} {1.05} {lccccc} Method & Soft assign. & Iter. refine & Var. length & Sub-word & Single-rep. \\ TIGER~ & & & & & \\ LC-Rec~ & & & & & \\ LETTER~ & & & & & \\ ETEGRec~ & & & & & \\ ADA-SID~ & & & & & \\ ActionPiece~ & & & & freq.\;only & \\ DIGER~ & Gumbel & & & & \\ SA$^2$CRQ~ & & & post-hoc & & \\ ReSID~ & & & & & \\ COBRA~ & & & & & \, \\ UniRec-CoA~ & & & & & \, \\ + } & & & & & } \\ Let $_i ^d$ denote the representation of item $i$, constructed from content, collaborative, or multi-modal encoders depending on the dataset. The goal is to map $_i$ into a variable-length SID $_i= $ that is compact, predictive, and collision-resistant. Figure~ summarizes the pipeline. {figures/architecture.pdf} +. item features pass through a stack of capsule layers with confidence-driven early stopping, merges semantically compatible adjacent tokens, and an autoregressive Transformer generates the next item's SID via trie-constrained beam search. inside one layer, soft routing replaces hard $ $; votes are reconciled over a few agreement iterations, and a stop gate fires when winner confidence is high or residual norm is small. scores SID pairs by a frequency--similarity mixture and merges them with a Gumbel-Softmax gate or $ $ , yielding reusable subword tokens.} The tokenizer is designed around three invariants. First, the emitted representation must remain a finite discrete sequence so that all existing constrained decoding machinery applies. Second, uncertainty should be represented before discretization, not only after decoding; otherwise all uncertainty has already been collapsed into a wrong token. Third, the tokenizer should expose interpretable diagnostics: routing weights reveal which semantic facets explain an item, capsule activation measures confidence, and residual norms measure unexplained information. These diagnostics are used in Section~ to check whether improvements come from meaningful tokenization rather than from a larger output space. At SID layer $ $, we maintain $K_ $ capsules. Capsule $k$ has a pose transform $_{ k}$ and bias $_{ k}$. Given residual $_{i, -1}$ with $_{i,0}=_i$, each capsule produces a vote }_{i, k}=_{ k}_{i, -1}+_{ k}. Routing starts from logits $a_{i, k}^{ }=0$ and iterates for $T$ rounds: c_{i, k}^{ } &= _k }), \\ _{i, }^{ } &= _k c_{i, k}^{ }}_{i, k}, \\ _{i, }^{ } &= }), \\ a_{i, k}^{ } &= a_{i, k}^{ } + }_{i, k}^{ }_{i, }^{ }. We use $ = \|^2}{0.5+\|\|^2}}{\|\|}$, whose norm lies in $[0,1)$ and is sensitive to small magnitudes. We also define per-capsule outputs $_{i, k}\!:=\! $ that share the same nonlinearity but are computed independently for each capsule and do not depend on the routing iteration $t$. The emitted token and confidence are s_{i, }= _k c_{i, k}^{ }, q_{i, }= _k c_{i, k}^{ }\|_{i, }^{ }\|. The residual update is the part where soft routing departs from hard quantization: instead of subtracting only the winning capsule, we subtract the routed reconstruction, _{i, }=_{i, -1}- _k c_{i, k}^{ }_{i, k}. Equation~ keeps the partial agreement with secondary capsules instead of throwing it away. A boundary item like a ``travel cooking kit'' no longer has to choose between travel and cooking; both facets contribute to its reconstruction, and only the unexplained part flows into the next layer's residual. This is not the same as replacing $ $ with a temperature-softmax, because the residual update itself uses the routed reconstruction, so deeper layers see a smaller and cleaner error signal. Two implementation details matter in practice. We $ _2$-normalize the item embedding before routing so that high-norm items cannot dominate the agreement scores, and we keep capsule parameters separate at each depth so that early layers can specialize in coarse facets while later layers refine the residual. Fixed-length SIDs impose the same token budget on easy and ambiguous items. stops when the residual has been sufficiently explained: L_i = \{ : q_{i, } \;\; \|_{i, }\|_2 \;\; =L_{ } \}. This design combines with , the length penalty $_{}\!=\! _i $ in Eq.~. Together these four safeguards prevent length explosion while addressing the GRID observation that blindly adding layers can hurt: uncertain items receive more steps only when their residual still contains useful signal, and confident items stop early. The stopping rule also changes the semantics of collisions. In a fixed-depth hard SID, two tail items that share all four positions are indistinguishable unless an artificial disambiguation token is appended. In , two items may share the same argmax tokens but differ in routing weights and stopping confidence during tokenizer training; the learned generator sees a cleaner set of token targets because ambiguous items are encouraged to stop at stable prefixes rather than continue through low-confidence residual layers. This behavior is similar in spirit to controlled-collision variable-length methods, but it is obtained from the routing dynamics rather than from an external entropy budget. Given the SID sequence from , learns whether adjacent tokens should be merged into a reusable subword. For pair $ $, we compute m = \,} + \, , where the second term prevents high-frequency but semantically unrelated pairs from being merged. A Gumbel-Softmax gate provides differentiability during training and hard merges at inference. This stage is intentionally lightweight: it improves sequence composition without changing the underlying item-to-SID assignment. We use a conservative merge policy. A pair is considered only if its semantic similarity exceeds a threshold $ $, and the threshold is annealed from strict to moderate during training. This avoids the common BPE failure mode in recommendation: extremely frequent but semantically broad prefix pairs can dominate the vocabulary, increasing popularity bias. Since already shortens easy items, is not used to aggressively compress every sequence; it is used to create reusable subwords for stable multi-token motifs. We use a two-stage protocol inspired by recommender-native tokenizer studies~. learns the item projection, capsule transforms $\{_{ k}\}$, and the merge MLP using only the tokenizer-side losses ; the sequence generator is not trained. freezes capsule centers and the merge MLP weights, then jointly trains the sequence generator together with low-rank routing adapters and a learnable scalar bias on the Gumbel gate. The final objective is =_{}+ _r_{}+ _s_{}+ _l_{}+ _b_{}, where $_{}$ is next-token cross entropy and is active only in Stage 2; $_{}\!=\!\|_i-}_i\|_2^2$ and $_{}$ are tokenizer losses with annealed margin from $0.2$ to $0.9$. This separation prevents the generator from chasing a moving SID target while keeping the routing mechanism slightly adaptive to downstream supervision. A fully joint objective lets the generator chase a moving target while the tokenizer changes the target sequence. ReSID and ETEGRec-style analyses suggest that this self-referential training can be unstable. We therefore first learn a recommendation-sufficient code geometry and then adapt the generator to that geometry. The second stage still allows limited routing adaptation, but capsule centers are frozen to preserve global code semantics and prevent late-stage collapse. } item embedding $_i$, capsule transforms $\{_{ k},_{ k}\}$, hyperparameters $T, , ,L_{ }$ SID sequence $_i\!=\! $, confidences $\{q_{i, }\}$ $_{i,0} _i / \|_i\|$ $} compute votes $}_{i, k}\!=\!_{ k}_{i, -1}+_{ k}$ for all $k$ initialize agreement logits $a_{i, k}^{ }\! \!0$ $c^{ }_{i, k}\! \!_k })$;\;\; $^{ }_{i, }\! \! _k c^{ }_{i, k}}_{i, k}$ $^{ }_{i, }\! \! }_{i, })$;\;\; $a^{ }_{i, k}\! \!a^{ }_{i, k}+}_{i, k}^{ }^{ }_{i, }$ $s_{i, }\! \! _k c^{ }_{i, k}$;\;\; $q_{i, }\! \!c^{ }_{i, s_{i, }}\! \!\|^{ }_{i, }\|$ $_{i, k}\! \! $;\;\; $_{i, }\! \!_{i, -1}- _k c^{ }_{i, k}_{i, k}$ )} \! \! $ $\|_{i, }\|_2\! \! $} $L_i\! \! $;\;\; $ $, $ $ We give three results that connect the design choices in Sections~-- to the quantities reported in Section~. Let $_{ k} ^{d_c}$ denote the $k$-th codebook center at depth $ $ and let $s_{i, }\!=\! _k c^{ }_{i, k}$ be the argmax token. Define the hard and soft reconstructions }^{}_i \!=\! _{ =1}^{L_i}_{ s_{i, }}, }^{}_i \!=\! _{ =1}^{L_i} _{k=1}^{K_ }c^{ }_{i, k}_{i, k}. Assume $\|_{i, k}-_{ k}\|_2\! \! $ and $\|_{ k}\|_2\! \!C$ for all $ ,k$. Then \|}^{}_i - }^{}_i \|_2 \; \; L_i + 2C _{ =1}^{L_i}\! }_{i, s_{i, }} ), and consequently $ \|_i-}^{}_i \|_2 \|_i-}^{}_i \|_2 + L_i + 2C _ }_{i, s_{i, }})$. For each layer $ $, write $w_k\!:=\!c^{ }_{i, k} 0$ with $ _k w_k\!=\!1$ and $s\!:=\!s_{i, }$. Then \| _k w_k_{ k} - _{ s} \|_2 &= \| _k w_k + _k w_k_{ k} - _{ s} \|_2 \\ & _k w_k + \| _{k s}w_k \|_2 \; \; + 2C , by the triangle inequality and $\|_{ k}-_{ s}\|\! \!2C$. Summing over $ $ and applying the triangle inequality once more gives Eq.~. The bound is tight when $w_s\!=\!1$ and $ \!=\!0$, in which case the soft and hard reconstructions coincide. In our experiments the average winner mass $_s\!=\!0.86$ ) and $ $ stays small after capsule warm-up, so soft routing reconstructs almost as well as hard but distributes mass to secondary capsules; that is why intra-code similarity rises in Table~ without losing reconstruction quality. Let $g_ \!=\! $ be the per-layer stopping probability under the law of $$. If $ _{}g_ \! \!g\!>\!0$ for all $ \! \!2$, then \; \;1 + _{ =2}^{L_{ }} ^{ -2}\; \; \! . $ \!=\! \! \! ^{ -2}$ for $ \! \!2$. Then $ \!=\! _{ 1} \! \!1+ _{ =2}^{L_{ }} ^{ -2}\! \!1+{g}$ as a geometric tail. Equation~ guarantees that the four safeguards in Section~ keep the expected length finite even before $L_{ }$ binds. Empirically ), the confidence and residual rules together account for the eventual stop of $90$--$92\ At depth $ $, model the residual $_{i, -1}$ as an isotropic Gaussian mixture with $K_ $ components of means $\{_{ k}\}$, equal variance $ ^2 I$, and uniform mixing weights. Then the E-step posterior responsibility is p \; \; \! \; \; \! , which has the same functional form as $c^{ }_{i, k}$ in Eqs.~ -- once we identify the GMM means $_{ k}$ with the agreement targets $^{ }_{i, }$ and absorb $1/ ^2$ into the routing temperature. Iterating the routing recursion is therefore equivalent to running EM on this layer's mixture with shared sufficient statistics across capsules. Standard convergence guarantees for EM with bounded log-likelihood~ ensure monotonic improvement in routing agreement, which is consistent with the saturation observed at $T\! \!3$ in Figure~ . The squash nonlinearity in Eq.~ further bounds capsule outputs to the unit ball, preventing the variance from collapsing during iteration. Tokenizer training costs $ $ where $N$ is the catalog size and $K\!=\! _ K_ $ . Inference is dominated by beam search at $ $, with $\! \!3.6$ for versus $\!=\!4$ for fixed-length baselines, so the per-beam step count is roughly $10\ We answer four questions. Does soft routing improve recommendation accuracy over hard residual quantization at the same SID budget? Does a routed-SID generator close the gap to dense-patch systems without inheriting their inference cost? Which design choices contribute most? Do the gains generalize to tail items and large catalogs where collisions and length budgets matter most? We use the public benchmarks standard in generative recommendation: Amazon Beauty, Sports, and Toys~, all under leave-one-out evaluation with 5-core filtering. For scale analysis, we further evaluate on a 35M-item proprietary industrial dataset with multi-modal item embeddings provided by a large-scale social media platform. Dataset statistics appear in Table~. {lrrrr} Dataset & Users & Items & Interactions & Avg. length \\ Beauty & 22,363 & 12,101 & 198,502 & 8.9 \\ Sports & 35,598 & 18,357 & 296,337 & 8.3 \\ Toys & 19,412 & 11,924 & 167,597 & 8.6 \\ Industrial & 8.6M & 35.8M & 331.1M & 38.5 \\ We compare with TIGER, LC-Rec, LETTER, ETEGRec, ADA-SID, ActionPiece, COBRA, UniRec-style Chain-of-Attribute, DIGER, SA$^2$CRQ, and ReSID. All methods share the same SASRec~/T5-style~ generator and beam search protocol when possible; dense patch variants use a COBRA-style BeamFusion path. The sequential recommendation backbone follows the convention in SASRec~ and BERT4Rec~. We report Recall@$k$ and NDCG@$k$ under full-corpus ranking, using $k\! \!\{5,10\}$ on the public benchmarks and $k\! \!\{50,100\}$ on the 35M-item industrial catalog, where the larger retrieval horizon reflects production deployment practice. Tokenizer quality is measured by: = $1 - |\{_i : i\! \!\}| / N$, the fraction of items that do not receive a unique SID ; = $|\{k : i, k\! \!_i\}| / _ K_ $, the fraction of codebook entries used at least once; over codebook usage frequencies ; = mean $ $ over item pairs sharing the same first SID token; = $ $, the probability that the ground-truth first SID token of the next item is in the top-$M$ predictions of a SASRec generator trained on the SID sequences ; head/torso/tail Recall@10, average SID length $$, and normalized inference cost. All public-benchmark numbers are mean over three random seeds with standard deviation reported either in tables or as error bars in figures; the 35M-item industrial run reports a single deterministic value. All SID methods are evaluated with the same item encoder, generator architecture, beam size, and invalid-ID filtering. For methods that require additional information, such as UniRec attributes or COBRA dense vectors, we count their inference cost separately and mark them with $ $. This prevents a patch system from being compared to a single-SID system as if both used the same retrieval budget. Table~ compares against eleven baselines covering hard-SID tokenizers and patch-route systems . consistently improves over the strongest single-representation baseline by $4.9\ {3pt} {1.05} $ . is in bold, is underlined. $^ $ marks patch-route methods that consume extra dense or attribute information at inference time.} {!}{ {lcccccccccccc} & {c}{Beauty} & {c}{Sports} & {c}{Toys} \\ {2-5} {6-9} {10-13} Method & R@5 & R@10 & N@5 & N@10 & R@5 & R@10 & N@5 & N@10 & R@5 & R@10 & N@5 & N@10 \\ TIGER & 0.0454 & 0.0648 & 0.0321 & 0.0384 & 0.0264 & 0.0400 & 0.0181 & 0.0225 & 0.0521 & 0.0712 & 0.0371 & 0.0432 \\ LC-Rec & 0.0478 & 0.0675 & 0.0334 & 0.0397 & 0.0276 & 0.0417 & 0.0188 & 0.0233 & 0.0540 & 0.0734 & 0.0384 & 0.0447 \\ LETTER & 0.0500 & 0.0708 & 0.0340 & 0.0406 & 0.0288 & 0.0435 & 0.0198 & 0.0244 & 0.0547 & 0.0741 & 0.0389 & 0.0452 \\ ETEGRec & 0.0513 & 0.0725 & 0.0348 & 0.0415 & 0.0294 & 0.0444 & 0.0201 & 0.0249 & 0.0560 & 0.0756 & 0.0397 & 0.0460 \\ ADA-SID & 0.0524 & 0.0740 & 0.0355 & 0.0422 & 0.0302 & 0.0456 & 0.0206 & 0.0254 & 0.0566 & 0.0762 & 0.0401 & 0.0465 \\ ActionPiece & 0.0553 & 0.0775 & 0.0379 & 0.0424 & 0.0330 & 0.0500 & 0.0224 & 0.0264 & 0.0559 & 0.0760 & 0.0398 & 0.0463 \\ DIGER & 0.0535 & 0.0752 & 0.0362 & 0.0431 & 0.0306 & 0.0463 & 0.0210 & 0.0258 & 0.0572 & 0.0771 & 0.0407 & 0.0472 \\ SA$^2$CRQ & 0.0520 & 0.0732 & 0.0352 & 0.0419 & 0.0298 & 0.0451 & 0.0203 & 0.0252 & 0.0562 & 0.0758 & 0.0399 & 0.0462 \\ ReSID & 0.0548 & 0.0770 & 0.0372 & 0.0438 & 0.0314 & 0.0475 & 0.0215 & 0.0266 & 0.0583 & 0.0786 & 0.0414 & 0.0481 \\ COBRA$^ $ & 0.0537 & 0.0725 & 0.0395 & 0.0456 & 0.0305 & 0.0434 & 0.0215 & 0.0257 & & 0.0781 & & \\ UniRec-CoA$^ $ & 0.0540 & 0.0763 & 0.0368 & 0.0434 & 0.0316 & 0.0478 & 0.0217 & 0.0268 & 0.0596 & 0.0802 & 0.0422 & 0.0491 \\ & & & & & & & & & 0.0602 & & 0.0432 & 0.0498 \\ + & & & & & & & & & & & & \\ } The largest gap in the ranking is between hard-SID tokenizers and : replacing $ $ with soft routing gives a $4$--$7\ We performed paired two-sided $t$-tests across the three seeds. + is significantly better than every single-representation baseline at $p<0.01$ on all three datasets, and significantly better than COBRA at $p<0.05$ on Beauty and Sports and at $p<0.10$ on Toys. is significantly better than ReSID at $p<0.01$ on Beauty, Sports, and Toys. Table~ tests whether a dense patch is still useful once the SID tokenizer is improved. Adding a COBRA-style dense vector to TIGER raises Recall@10 from $0.0648$ to $0.0725$ ($+11.9\ {lcccc} Configuration & Representation & R@10 & N@10 & Cost \\ TIGER & RQ SID & 0.0648 & 0.0384 & 1.00$ $ \\ TIGER + dense$^ $ & RQ SID + dense vec & 0.0725 & 0.0456 & 2.10$ $ \\ UniRec-CoA$^ $ & Attribute prefix + RQ SID & 0.0763 & 0.0434 & 1.34$ $ \\ & Routed SID & 0.0808 & 0.0460 & 1.05$ $ \\ + dense$^ $ & Routed SID + dense vec & 0.0829 & 0.0473 & 2.14$ $ \\ + & Routed subword SID & & & 1.08$ $ \\ Table~ ablates the five core mechanisms on Beauty . Replacing soft routing with hard winner-only updates costs the most ($-16.3\ +.} {lccc} Variant & R@10 & Drop & Interpretation \\ Full + & 0.0839 & -- & Complete pipeline \\ w/o soft residual, hard winner only & 0.0702 & $-16.3\ w/o routing iterations & 0.0731 & $-12.9\ fixed length $L\!=\!4$ & 0.0765 & $-8.8\ fixed length $L\!=\!2$ & 0.0658 & $-21.6\ w/o spread loss & 0.0770 & $-8.2\ w/o & 0.0808 & $-3.7\ frequency-only BPE & 0.0817 & $-2.6\ Figure~ plots the SID-length distribution per dataset: the mode is at $L\!=\!3$ on every benchmark and the right tail tapers smoothly. Mean lengths span $ L\!=\!3.41$ on Beauty up to $ L\!=\!3.89$ on Toys , all well below the $L_{ }\!=\!6$ cap and consistent with the $ $ bound of Proposition~. Figure~ decomposes which of the three stopping rules in Section~ fires per item. The confidence threshold $ $ fires for $55\ Figure~ decomposes Recall@10 by item-popularity tier on Beauty. While head Recall improves modestly ($+19\ {figures/analysis_combined.pdf} . SID length distribution per dataset: mode $L\!=\!3$, mean $ L\! \! $. Fractions of the three stopping rules that fire per dataset; the hard cap $L_{ }$ accounts for $ \!10\ Figure~ summarizes four geometric diagnostics. shows that reduces the collision rate to $13.4\ {figures/tokenizer_diagnostics.pdf} achieves $13.4\ We also evaluate on the 35M-item industrial catalog described in Table~. Because the catalog is three orders of magnitude larger than Amazon Beauty, the meaningful recall horizon shifts from $K\! \!\{5,10\}$ to $K\! \!\{50,100\}$, matching the regime adopted by ADA-SID and other industrial SID studies. Table~ reports the five metrics most informative for tokenizer evaluation at this scale: R@50 and R@100 for recall coverage, NDCG@100 for ranking quality, Collision rate for SID space utilization, and $$ for inference cost. {6pt} {lccccc} Method & R@50 & R@100 & N@100 & Collision $ $ & $$ \\ RQ-KMeans & 0.1835 & 0.2421 & 0.1216 & 73.2\ TIGER & 0.2217 & 0.2843 & 0.1482 & 51.4\ ADA-SID & 0.2772 & 0.2926 & 0.1714 & 37.5\ ReSID & 0.2881 & 0.3105 & 0.1836 & 31.8\ COBRA$^ $ & & 0.3275 & 0.1935 & 51.4\ & 0.2996 & & & + & & & & alone matches the patch-route COBRA on both R@100 ($+0.3\ Three observations could in principle have undermined the core claim, and we checked each. A large +dense improvement over alone would suggest the routed SID is still missing the information dense vectors carry; we observe only $+2.6\ has three limitations. First, capsule routing increases tokenizer training cost by roughly $20$--$30\ attacks the SID information bottleneck at the assignment operator. Soft routing replaces $ $ with a weighted reconstruction, capsule confidence drives variable length, and composes adjacent tokens into reusable subwords, all without giving up the discrete generative interface that production systems require. On three public benchmarks and a 35M-item industrial catalog, + improves Recall@10 by $9.6\ This appendix specifies the exact tokenizer, training, decoding, and complexity choices used to produce the results in the main paper. It is written as an implementation contract so that the experiments can be reproduced end-to-end. For public benchmarks, each item representation $_i$ is initialized from the same item encoder used by the baseline under comparison. In the intended fair setting, TIGER, LC-Rec, LETTER, ETEGRec, ADA-SID, ReSID, and all receive the same frozen item vectors before tokenization. For multi-modal datasets, modality embeddings are concatenated and projected through a two-layer MLP to dimension $d=128$, followed by $ _2$ normalization. This normalization is important: without it, high-norm items can obtain large capsule agreement even when their angular semantics are weak. Unless otherwise specified, uses $K_ =256$ capsules at each depth, capsule output dimension $d_c=64$, $T=3$ routing rounds, $L_{ }=6$, confidence threshold $ =0.82$, and residual threshold $ =0.08$. Each depth has independent capsule parameters. Sharing capsule transforms across depths was considered but is not the default because shallow depths should model coarse semantic facets, while deeper depths should model residual refinements. The residual update in Eq.~ is _{ }=_{ -1}-_{ }, _{ }= _k c_{ k}_{ k}. This update reduces residual norm exactly when \|_{ }\|_2^2 \|_{ -1}\|_2^2 2 _{ -1},_{ } \|_{ }\|_2^2. Equation~ is not assumed to hold automatically for arbitrary capsule outputs; it is encouraged by the reconstruction loss and by normalizing votes before agreement updates. If the condition is violated for many items at a layer, the implementation reduces the residual step by a scalar $ (0,1]$ or increases the spread/reconstruction weight for that layer. We use the default $ \!=\!1$ in all reported experiments and monitor the fraction of norm-increasing residual updates as a training diagnostic. The stopping rule always terminates because $L_i L_{ }$ by construction; therefore $ L_{ }$. The length regularizer does not prove optimality, but it biases the model toward shorter explanations whenever accuracy is unaffected. The practical interpretation is: confidence stopping handles semantically clear items, residual stopping handles already-explained vectors, and $L_{ }$ handles ambiguous or noisy items. During real experiments, we will report the distribution of stopping causes in addition to mean length. A candidate pair $ $ is considered only if it appears at least $n_{ }=20$ times in the training corpus and if $ > $. We anneal $ $ from $0.90$ to $0.55$ over tokenizer pretraining. This prevents early merges from being dominated by popularity-only prefix pairs. Let $g_j \{0,1\}$ indicate whether pair $j$ is merged. We model the binary merge decision through a two-class distribution $_j\!=\! \! \! ^1$ , produced by a two-layer MLP $f_ $ that takes as input the concatenation of token embeddings $_{s_j}, _{s_{j+1}}$, the normalized pair frequency $} $, and the cosine similarity $ $: _j = \! ) ^2. The relaxed training gate uses Gumbel-Softmax with temperature $ _g$: _j = \! / _g )_{\!1}, where $\! \! ^2$ is i.i.d.\ Gumbel noise and the subscript $1$ selects the merge-class component. At inference, $g_j\!=\! $ subject to non-overlap constraints; if $ $ is merged, pairs touching $s_j$ or $s_{j+1}$ are skipped in the same pass. This greedy non-overlap rule is deterministic and keeps the final token sequence valid. The two-stage protocol is described conceptually in Section~; here we list the numerical settings. Stage 1 uses AdamW with learning rate $1\! \!10^{-3}$, cosine decay, weight decay $10^{-5}$, batch size $256$ on all Amazon datasets, for up to $100$ epochs. Stage 2 uses learning rate $3\! \!10^{-4}$ with the same optimizer settings for up to $200$ epochs, matching the training budget of TIGER~ and ActionPiece~. Early stopping uses validation Recall@10 with patience $10$, so both stages in practice converge well before the budget cap. For all public benchmarks we use a SASRec-style~ causal Transformer~ with $4$ self-attention layers, $4$ heads, hidden dimension $128$, FFN dimension $512$, GELU activation, pre-LayerNorm, and dropout $0.1$. The vocabulary equals the SID code space $|V|\!=\! _ K_ $ plus a special end-of-item token. Item history is truncated to the most recent $50$ interactions. We tie the input and output token embedding matrices to reduce parameter count and warm-start the input embedding from the codebook centers $\{_{ k}\}$ produced by Stage 1. For the 35M-item industrial run we replace SASRec with an $8$-layer T5-base encoder--decoder , keeping the same vocabulary scheme; this absorbs the larger codebook and $331.1$M interactions of the industrial catalog at the cost of a heavier backbone. Generated token sequences are decoded with beam size 50. A trie built from training item SIDs masks invalid next tokens. For variable-length SIDs, each valid item path includes an end-of-item token. This means that a short SID is not a prefix ambiguity: generation may stop only at trie nodes corresponding to actual items. Dense-patch baselines follow a COBRA-style BeamFusion score = ), where $b_i$ is the beam logit score and the second term is computed only inside candidates associated with generated sparse IDs. Tokenization is an offline item-side operation. For one item and one layer, vote computation costs $O $ and routing agreement costs $O $. Thus the offline tokenizer cost per item is O . At serving time, the generator sees only discrete tokens. Its cost is proportional to the generated length, approximately $O $ softmax steps for beam size $B$ and average SID length $$. This is why can be more accurate than dense-patch systems without inheriting their ANN or vector-fusion cost. This appendix reports auxiliary tables that complement the main paper. All numbers are mean over three random seeds; standard deviations are within the same range as Table~ and are omitted for compactness. Table~ reports tokenizer-intrinsic diagnostics, and Table~ records the hyperparameter sensitivity sweep. {4pt} {lccccc} Tokenizer & Collision $ $ & Utilization $ $ & Gini $ $ & Intra-code sim $ $ & CodeRecall@50 $ $ \\ Frequency & 90.4\ KMeans-flat & 65.8\ RQ-KMeans & 72.5\ ActionPiece & 56.9\ ADA-SID & 33.8\ & \!=\!6$, $ \!=\!0.82$, $ \!=\!0.6$.} {lccc} Setting & R@10 & Avg. length & Interpretation \\ $T\!=\!1$ & 0.0731 & 3.3 & no iterative correction \\ $T\!=\!2$ & 0.0789 & 3.5 & most routing errors corrected \\ $T\!=\!3$ & & 3.6 & default; accuracy--cost balance \\ $T\!=\!5$ & 0.0841 & 3.6 & saturated routing \\ $L_{ }\!=\!4$ & 0.0806 & 3.1 & insufficient for complex items \\ $L_{ }\!=\!6$ & & 3.6 & default \\ $L_{ }\!=\!8$ & 0.0837 & 3.6 & bound saturates; cap non-binding \\ $ \!=\!0.75$ & 0.0817 & 2.8 & stops too early \\ $ \!=\!0.90$ & 0.0821 & 4.4 & over-encodes easy items \\ $ \!=\!1.0$ & 0.0817 & 3.6 & frequency-only merge \\ $ \!=\!0.6$ & & 3.6 & semantic--frequency balance \\ The tables above satisfy three monotonicity properties that we verified throughout training. Removing a mechanism never improves both R@10 and the diagnostic that mechanism was designed to address; for example, fixed $L\!=\!2$ lowers both average length and R@10. Adding a dense patch always increases normalized inference cost. reduces effective sequence length but does not by itself reduce tokenizer collision because it operates after item-level SID assignment. Figure~ visualizes the variable-length behaviour behind these ablations across all four datasets. {figures/appendix_length_stopping.pdf} across Beauty, Sports, Toys, and the industrial catalog. SID length distribution: mode is $L\!=\!3$ on every dataset; mean $$ ranges from $3.41$ to $3.89$ , with the industrial catalog at $3.8$ . Stopping-rule breakdown: confidence fires on the majority of items, the residual rule on $ \!30\ A common concern with variable-length SIDs is whether earlier positions become harder to predict because they must carry more discriminative information. Table~ reports the top-1 and top-5 token accuracy per SID position on Beauty. Position 1 is harder than positions 2--3 , but 's position-1 top-5 accuracy ($86.3\ {5pt} {!}{ {lcccccc} Method & Pos. 1 & Pos. 2 & Pos. 3 & Pos. 4 & Pos. 5 & Pos. 6+ \\ TIGER & 31.2 / 71.4 & 38.6 / 78.8 & 41.0 / 80.5 & 35.4 / 76.2 & --- & --- \\ ADA-SID & 36.9 / 79.1 & 44.2 / 84.6 & 47.5 / 86.3 & 41.8 / 82.1 & 32.0 / 74.6 & --- \\ & 42.7 / 86.3 & 49.1 / 88.9 & 52.6 / 90.7 & 48.3 / 88.2 & 39.5 / 81.7 & 31.2 / 75.8 \\ + & & & & & & \\ } {figures/appendix_codebook_heatmap.pdf} Per-layer codebook usage probability : shallow layers spread mass broadly for coarse facets, deep layers concentrate on a few capsules for residual refinement, matching the EM-style behaviour in Proposition~. Per-position top-$1$ and top-$5$ accuracy from Table~. dominates TIGER and ADA-SID at every position, and the top-$1$ margin over ADA-SID grows from $+5.8$\,pp at position $1$ to $+7.5$\,pp at position $5$, where residual structure is hardest to discriminate.} Generative SID systems are often motivated by their ability to handle unseen items via content-derived codes. Following the protocol in TIGER~ and ReSID~, Table~ evaluates this property on Beauty by isolating the cold-item subset, defined as items with fewer than $5$ interactions in the training split . This subset accounts for $ \!12\ {6pt} .} {lccc} Method & Full-corpus R@10 & Cold-subset R@10 & Retention \\ TIGER & 0.0648 & 0.0371 & 57.3\ ADA-SID & 0.0740 & 0.0508 & 68.6\ COBRA$^ $ & 0.0725 & 0.0528 & 72.8\ & 0.0808 & 0.0591 & 73.1\ + & & & Table~ collects the symbols used throughout the paper, grouped by role. {8pt} {1.05} {lll} Group & Symbol & Meaning \\ {*}{} & $_i ^d$ & item embedding \\ & $N$ & catalog size \\ {*}{} & $_{i, } ^d$ & residual at depth $ $, with $_{i,0}\!=\!_i$ \\ & $K_ ,\,d_c$ & capsules per depth and capsule output dim \\ & $_{ k},_{ k}$ & pose transform and bias of capsule $k$ at depth $ $ \\ & $}_{i, k}$ & vote of capsule $k$ for item $i$ at depth $ $ \\ & $c^{ }_{i, k}$ & routing weight of capsule $k$ after $t$ iterations \\ & $T$ & routing iterations per layer \\ {*}{} & $^{ }_{i, }$ & aggregated capsule output at iteration $t$ \\ & $_{i, k}$ & per-capsule output $ $ \\ & $_{ k}$ & codebook center of capsule $k$ at depth $ $ \\ & $_{ k}$ & GMM mean \\ {*}{} & $s_{i, }\! \! $ & emitted SID token at depth $ $ \\ & $q_{i, } [0,1)$ & capsule confidence at depth $ $ \\ & $L_i L_{ }$ & SID length of item $i$ \\ & $ ,\, $ & confidence and residual stopping thresholds \\ {*}{}} & $ ,\, $ & merge weight and similarity threshold \\ & $_j\! \! ^1$ & two-class merge distribution at pair $j$ \\ & $g_j,\,_j$ & hard / Gumbel-relaxed merge gate \\ & $n_{ }$ & minimum pair frequency \\ {*}{} & $_{}$ & loss components \\ & $ _{r,s,l,b}$ & loss weights \\ & $B$ & beam size at decoding \\ & $$ & average SID length over the test set \\ & $|V|$ & SID vocabulary size $ _ K_ $ \\ Subscripts always read left-to-right as $i$\, $ $\, $ k$\, ; the superscript $ $ denotes the routing iteration and is omitted whenever a quantity does not depend on it . Symbols $_{ k}$ and $C, , w_s, g$ in Section~ are local to the proofs of Propositions~-- and do not appear elsewhere in the paper. Table~ consolidates the core hyperparameters of the CapsID routing and SemBPE modules. Default values are shared across the three public benchmarks unless noted; the industrial run uses the values in parentheses. Optimization and decoding follow standard settings consistent with TIGER and ReSID. Hardware: the industrial run uses $4{ }$A100-80G; public benchmarks fit on commodity single-GPU setups. {6pt} +. Defaults are shared across Beauty / Sports / Toys; values in parentheses are used for the industrial run. ``Swept values'' lists the points reported in the sensitivity table; ``--'' means the hyperparameter was held fixed.} {llp{2.0cm}p{4.2cm}p{2.5cm}} Group & Symbol & Default & Notes & Swept values \\ {*}{Capsule routing} & $K_ $ & 256 & capsules per depth & -- \\ & $d_c$ & 64 & capsule output dim & -- \\ & $T$ & 3 & routing iterations & $\{1,2,3,5\}$ \\ & $L_{ }$ & 6 & maximum SID length & $\{4,6,8\}$ \\ & $ $ & 0.82 & confidence stopping threshold & $\{0.75,0.82,0.90\}$ \\ & $ $ & 0.08 & residual-norm stopping threshold & -- \\ {*}{} & $ $ & 0.6 & frequency vs semantic weight & $\{0.6,1.0\}$ \\ & $ $ & $0.90{ }0.55$ & annealed similarity threshold & -- \\ & $n_{ }$ & 20 & minimum pair frequency & -- \\ {*}{Loss weights} & $ _r$ & 1.0 & reconstruction & -- \\ & $ _s$ & 0.1 & spread & -- \\ & $ _l$ & 0.05 & length penalty & -- \\ & $ _b$ & 0.2 & merge regularization & -- \\ We report the information needed to interpret and reimplement the experiments. The public benchmarks are openly available, and preprocessing follows the standard 5-core leave-one-out protocol used by TIGER~. Appendices~-- specify the capsule and hyperparameters, optimizer settings, decoding protocol, loss weights, and random seeds. Public-benchmark results are averaged over three seeds; industrial results are a single deterministic run due to compute cost. The industrial run uses $4\! \!$A100-80G; the public-benchmark experiments are lightweight enough to run on a single commodity GPU. 