 We evaluate GraphRAG-IRL on two recommendation datasets spanning different feedback types and scales . All experiments use the same pipeline architecture described in Section~6. A widely used benchmark for movie recommendation~ containing 100,836 ratings from 610 users on 9,724 movies. After filtering users with $ 20$ ratings and movies with $ 10$ ratings, we retain 608 users and 2,269 movies. Positive interactions are defined as ratings $ 4.0$ (41,423 ratings, 48.2\ A real-world short-video recommendation dataset from Kuaishou~, containing approximately 1.44 million standard interaction logs from 27,285 users and 7,551 videos. Unlike MovieLens, KuaiRand-Pure captures implicit feedback signals: clicks, long views, likes, follows, comments, and forwards. We use as the positive engagement signal (38.5\ For both datasets, we use a time-based leave-last-two-out split per user. Each user's chronological sequence of positive interactions is divided as: all but the last two form the training trajectory, the second-to-last is validation, and the last is the test item. For evaluation, each test sample consists of 1 positive item and 99 randomly sampled negatives, forming a 100-candidate ranking task. {4pt} {lrr} & & \\ Users & 608 & 10,877 \\ Items & 2,269 & 6,508 \\ Positive interactions & 41K & 434K \\ Feedback type & Rating $ 4$ & Click \\ Test samples & 608 & 10,877 \\ Concept nodes & 514 & 46 \\ Graph edges & 9,369 & 5,322+ \\ We report five standard ranking metrics computed over the 100-candidate set: : fraction of test samples where the positive item appears in the top-$K$; : position-aware metric that rewards higher-ranked positives; : average of $1/$ of the positive item. We report HR@5, HR@10, NDCG@5, NDCG@10, and MRR. We compare GraphRAG-IRL against the following baselines: : Candidates ranked uniformly at random. : Candidates ranked by global interaction count . : $L_2$-regularized logistic regression trained with pointwise cross-entropy on the same feature set. Each candidate is scored independently. : Maximum Entropy IRL with a linear reward function $R_ = w^ + b$, trained with the listwise softmax objective . : Maximum Entropy IRL with a two-layer MLP reward , trained with the same listwise objective. : IRL-MLP trained on the extended feature set including 4 graph-derived features . For LLM re-ranking experiments, we additionally compare: : LLM re-ranks a random top-$N$ subset with no learned pre-ranking. : IRL pre-ranks top-$N$; LLM re-ranks with item descriptions only . : IRL pre-ranks top-$N$; LLM re-ranks with full user persona and community context. : GraphRAG retrieval score selects top-$N$; LLM re-ranks with plain prompt. : Blended IRL and GraphRAG scores select top-$N$; LLM re-ranks with full persona prompt. This is the full proposed pipeline. All LLM experiments use listwise ranking prompts at temperature 0 for reproducibility. The fusion weight $ $ is tuned per LLM on the validation set via grid search over $\{0.0, 0.1, , 1.0\}$, optimizing NDCG@10. Table~ presents the non-LLM results on both datasets. . $ $ is relative improvement over Supervised.} {5pt} {ll ccccc r} & & & & & & & $ $ \\ {*}{MovieLens} & Random & 0.063 & 0.038 & 0.109 & 0.056 & 0.062 & — \\ & Popularity & 0.225 & 0.148 & 0.357 & 0.198 & 0.172 & — \\ & Supervised & 0.266 & 0.175 & 0.393 & 0.223 & 0.195 & — \\ & IRL-Linear & 0.266 & 0.176 & 0.396 & 0.222 & 0.192 & $-$0.4\ & IRL-MLP & 0.278 & 0.189 & 0.415 & 0.237 & 0.206 & +6.3\ & & & & & & & {*}{KuaiRand} & Random & 0.058 & 0.035 & 0.105 & 0.050 & 0.056 & — \\ & Popularity & 0.324 & 0.214 & 0.483 & 0.265 & 0.221 & — \\ & Supervised & 0.373 & 0.245 & 0.548 & 0.301 & 0.246 & — \\ & IRL-Linear & 0.369 & 0.245 & 0.538 & 0.300 & 0.248 & $-$0.3\ & IRL-MLP & 0.416 & 0.280 & 0.589 & 0.336 & 0.279 & +11.6\ & & & & & & & On MovieLens, IRL-MLP improves NDCG@10 by +6.3\ Adding four graph-derived features yields a further +8.9\ On MovieLens, the individual contributions of IRL and GraphRAG sum to +0.019, but the actual combined gain is +0.035---nearly double. This superadditivity arises because IRL's listwise objective can exploit the relative differences in graph features across candidates, whereas pointwise supervised training cannot. We evaluate the persona-guided LLM re-ranking stage using top-$N=20$ pre-ranking. Table~ presents the four-way ablation isolating the contributions of the pre-ranker and the prompt style . {3pt} {lcccc} & & & & \\ IRL + LLM & .293 & .477 & .246 & .204 \\ GraphRAG + LLM & .203 & .326 & .158 & .131 \\ IRL + Persona LLM & .434 & .593 & .347 & .292 \\ & & & & \\ IRL+GraphRAG & .433 & .611 & .351 & .290 \\ Two key findings: Without user persona, the LLM degrades ranking quality substantially. On KuaiRand, IRL + plain LLM achieves NDCG@10 = 0.246, a 28\ GraphRAG retrieval achieves only 34.8\ To assess robustness across LLM families, we evaluate the full pipeline with four frontier models on MovieLens ml-1m . Table~ reports results with per-model tuned $ $. {3pt} {lccccr} & $ $ & & & & $ $ \\ None & — & .515 & .292 & .246 & — \\ GPT-5.2 & 0.6 & .515 & .296 & .248 & +1.4\ Qwen3-235B & 0.4 & .521 & .300 & .252 & +2.7\ DeepSeek & 0.7 & .536 & .305 & .254 & +4.5\ & 0.8 & & & & We conduct ablation experiments to isolate the contribution of each component. Table~ summarizes the results. {4pt} {lccc} & & & $ $ \\ Full & & & — \\ $-$ GraphRAG features & .415 & .237 & $-$8.1\ $-$ Nonlinear reward & .405 & .228 & $-$11.6\ $-$ Listwise objective & .405 & .228 & $-$11.6\ $-$ Both & .393 & .223 & $-$13.6\ The individual NDCG@10 gains of IRL and GraphRAG sum to +0.019. The actual combined gain is +0.035, yielding a synergy term of +0.016. This superadditivity arises because GraphRAG features are signals---most informative when compared across candidates. IRL's listwise softmax objective performs this comparison naturally, while pointwise supervised training cannot exploit relative differences. On MovieLens ml-1m, which lacks user-generated tags, the feature is identically zero for all candidates, and GraphRAG features provide no improvement ($ 0\ On KuaiRand, the step~11v2 score fusion design---where LLM re-rankings are applied only when they improve over the IRL baseline ---equalizes performance across LLM providers. Without boost-only filtering, DeepSeek R1 produces net-negative re-rankings . With the constraint, all three LLMs achieve NDCG@10 in the range 0.351--0.357, all improving over the IRL baseline of 0.337. This demonstrates that the fusion mechanism is robust to LLM quality variation. IRL-MLP + GraphRAG achieves the best non-LLM results on both datasets (+15--17\ The improvement over supervised ranking (+6--12\ The combined gain exceeds the sum of individual contributions, because IRL's listwise comparison naturally exploits the relative differences in graph-derived features across candidates. On MovieLens ml-1m, persona-guided LLM fusion with Claude Opus 4.6 improves NDCG@10 by +16.8\ Plain LLM prompts without user profile and community context degrade performance by up to 28\ The adaptive $ $-fusion automatically assigns higher weight to stronger LLMs , ensuring the pipeline never degrades below the GraphRAG-IRL baseline regardless of LLM choice. In summary, GraphRAG-IRL provides a cost-effective foundation that consistently outperforms supervised baselines. When inference budget permits, adding persona-guided LLM re-ranking yields further gains that scale with model capability, making the full GraphRAG-IRL + LLM pipeline the recommended configuration for maximum recommendation quality. {acmart} }} } } } } {Liang, Wang, Zhang, Zhou} Personalized recommendation requires models that capture sequential user preferences while remaining robust to sparse feedback and semantic ambiguity. Recent work has explored large language models as recommenders and re-rankers, but pure prompt-based ranking often suffers from poor calibration, sensitivity to candidate ordering, and popularity bias. These limitations make LLMs useful semantic reasoners, but unreliable as standalone ranking engines. We present , a hybrid recommendation framework that combines graph-grounded feature construction, inverse reinforcement learning , and persona-guided LLM re-ranking. Our method constructs a heterogeneous knowledge graph over items, categories, and concepts, retrieves both individual and community preference context, and uses these signals to train a Maximum Entropy IRL model for calibrated pre-ranking. An LLM is then applied only to a short candidate list, where persona-guided prompts provide complementary semantic judgments that are fused with IRL rankings. Experiments show that GraphRAG-IRL is a strong standalone recommender: IRL-MLP with GraphRAG improves NDCG@10 by 15.7\ Personalized recommendation aims to infer user preferences from sparse, noisy, and temporally evolving interaction histories. Classical collaborative filtering models capture user--item affinities effectively , while sequential recommenders model short-term intent from ordered behavior sequences . However, these approaches usually rely on structured interaction signals and often struggle to incorporate richer semantic evidence about why an item matches a user's underlying interests. Large language models have recently emerged as attractive components for recommendation because they encode broad semantic knowledge and can reason over natural-language descriptions of users and items. Prior work has explored prompting language models as recommenders and zero-shot rankers, showing that they can produce competitive rankings in some settings . At the same time, these studies also highlight important limitations: language models can be sensitive to prompt formulation, struggle to preserve the order information in user histories, and exhibit position and popularity biases when given direct control over candidate ranking . These observations suggest that LLMs are promising semantic reasoners, but unreliable as standalone ranking engines. In parallel, graph-based retrieval and retrieval-augmented generation provide a mechanism for grounding predictions in structured context rather than relying only on parametric knowledge . For recommendation, such context can include a user's recent interests, item attributes, and community-level interaction patterns encoded in a heterogeneous graph. Meanwhile, inverse reinforcement learning offers a principled way to learn latent reward functions from expert demonstrations, allowing recommendation to be framed as recovering the preferences that best explain user behavior . In this paper, we propose , a hybrid recommendation framework that uses graph-based retrieval and IRL for calibrated pre-ranking, and then employs an LLM as a semantic re-scoring component over only the top-$K$ shortlist. Our method first constructs an item-attribute knowledge graph and extracts both individual and community context through GraphRAG-style retrieval. These features, together with behavioral signals, are used to train a Maximum Entropy IRL model that learns a personalized reward function. We then build a user persona from historical interactions and ask an LLM to provide preference-aware semantic judgments on the shortlist, which are fused with IRL scores instead of replacing them. This design preserves the stability of a learned ranking model while still exploiting the LLM's ability to reason over item semantics and user intent. Our key contributions are as follows: : We introduce a recommendation pipeline that combines GraphRAG-based feature extraction, Maximum Entropy IRL pre-ranking, and LLM-based semantic re-scoring, treating the LLM as a complementary signal rather than a full ranking replacement. : We construct a heterogeneous knowledge graph over users, items, genres, and concepts, and derive both individual and community context features to support personalized ranking. : We build user personas from interaction histories and fuse LLM judgments with learned IRL rewards, aiming to improve semantic relevance while reducing the instability and bias of pure prompt-based ranking. : We conduct a comprehensive empirical evaluation on MovieLens~ and KuaiRand~, showing that GraphRAG-IRL consistently outperforms pointwise supervised baselines, that GraphRAG and IRL contribute superadditive gains, and that persona-guided LLM fusion further improves ranking quality across multiple LLM providers. Recent work has explored large language models as recommenders, zero-shot rankers, and prompt-based re-rankers for personalized recommendation. LLMRank formulates recommendation as a conditional ranking problem and shows that LLMs can rank candidate items in a zero-shot setting, while also revealing sensitivity to prompt design, item order, and popularity bias . Xu et al. provide a broader prompting framework for applying LLMs to recommendation tasks and systematically analyze their empirical behavior across recommendation settings . Lichtenberg et al. further study LLM recommenders through the lens of popularity bias, showing that LLM-based recommendation introduces a distinct accuracy--bias trade-off that must be measured carefully . Most closely related to our comparison setting, PersonaAgent with GraphRAG uses community-aware knowledge graphs to build personalized prompts for LLMs and serves as a strong LLM-centered baseline for persona-conditioned recommendation and personalization . These studies demonstrate the promise of LLMs as semantic ranking modules, but they also suggest that pure prompt-based recommendation can be unstable and biased when the LLM is given full ranking responsibility. Our work therefore uses the LLM as a re-ranking signal on top of a learned IRL-based scorer rather than as a standalone recommender. Graph neural networks have become a standard paradigm for recommendation because they propagate collaborative signals over user--item graphs and can naturally incorporate high-order connectivity. Neural Graph Collaborative Filtering models neighborhood aggregation directly on the interaction graph , while LightGCN simplifies graph convolution for recommendation and shows that lightweight propagation can outperform heavier architectures . Knowledge-graph-based recommendation methods such as KGAT further enrich recommendation with relational item semantics by propagating signals over structured knowledge graphs . In contrast to these end-to-end graph representation learning methods, our approach uses graphs primarily for retrieval and feature construction, and then learns preferences through IRL with LLM-assisted re-ranking. Retrieval-augmented generation grounds model outputs in retrieved external context rather than relying only on parametric memory . GraphRAG extends this idea by organizing retrieved information in graph structures that support local neighborhood exploration and community-level summarization . The PersonaAgent with GraphRAG framework further shows that graph-based community structure can be useful for constructing user-aligned prompts in personalized language-model systems . Our work adopts the intuition of graph-grounded retrieval, but applies it to recommendation: we use a heterogeneous graph to retrieve individual and community preference context, which is then consumed by an IRL ranker and an LLM re-ranker. Inverse reinforcement learning provides a principled framework for recovering latent reward functions from expert behavior . In recommendation, this perspective is attractive because user interaction sequences can be interpreted as demonstrations of preferences rather than merely labeled training examples. Prior reinforcement-learning-based recommendation work, such as the generative adversarial user model of Chen et al., shows the value of sequential decision-making views of recommendation . However, existing approaches generally do not combine IRL with graph-grounded retrieval and persona-guided LLM re-ranking. Our method bridges these directions by learning a reward model from interaction trajectories while grounding candidate evaluation in graph-derived context and LLM semantic judgments. Given a user $u$'s interaction history $_u = \{ , , \}$, where $i_j$ is an item, $r_j$ is a feedback signal , and $t_j$ is a timestamp, the task is : rank a candidate set~$C$ containing one true positive among randomly sampled negatives to surface the items a user will engage with next. We model each user's chronological sequence of positively-rated items as an ---a trajectory $ _u = $. Trajectory-based modeling has proven effective for capturing sequential dynamics in diverse domains, including biological cell differentiation~; here we apply the same paradigm to model evolving user preferences. Specifically: ~$s_t$: the user's context at time~$t$, summarizing interaction history, preference distributions, and graph-retrieved context. ~$a_t$: selecting an item from the candidate set $C_t$. ~$R_ $: a latent function to be learned from the demonstrations. The reward model scores each state--action pair independently; the trajectory defines the evolving user state, but the loss normalizes over the full candidate set at each timestep. The resulting objective is listwise learning-to-rank---a softmax loss formally equivalent to single-step Maximum Entropy IRL~. The full pipeline proceeds as: user history $ $ knowledge graph construction $ $ GraphRAG retrieval $ $ feature engineering $ $ IRL reward scoring $ $ top-$N$ selection $ $ persona-guided LLM re-ranking $ $ $ $-blended recommendation. {figures/architecture_pipeline.drawio.png} We construct a heterogeneous graph $ = $ with three node types: ~$_I$: each item in the dataset with associated metadata. ~$_G$: content categories . ~$_C$: semantic tags extracted from user-generated annotations, filtered by a minimum frequency threshold. Edges encode three relation types: Item $ $ Category: category assignments from item metadata. Item $ $ Concept: tag associations from user annotations. Item $ $ Item: co-occurrence edges between items sharing at least two concept nodes. In addition, we build a TF-IDF text index over item descriptions for content-based retrieval. For each user state~$s_t$, we retrieve two types of structured context from~$$. From the user's local neighborhood in~$$: Recent high-rated items . Category distribution: the user's normalized category preference vector over their history. Concept affinity: frequently occurring tags across items the user has positively rated. User similarity is computed as cosine similarity over category profiles, and the top-$M$ nearest neighbors form a user community---a lightweight community detection step that groups users by preference similarity without requiring complex graph clustering~. From this community we retrieve: : the fraction of~$M$ neighbors who positively rated a given candidate. : similarity-weighted average rating from~$M$ neighbors. : count of concept nodes shared between the user's history and the candidate. For each candidate item~$a$ given user state~$s$, we construct a feature vector $ ^d$ in two stages. }$)} : category distribution over the user's history; activity level $ $ where $n_t$ is the number of prior interactions; recency $ $. : the item's category vector; popularity $ $ where $p_c$ is the item's global interaction count. : cosine similarity between the user's category distribution and the candidate's category vector. } + 4$)} Four features from the knowledge graph and community structure are appended: : TF-IDF cosine between the user's aggregated history text and the candidate's text. : fraction of $M$ similar users who positively engaged with the candidate. : number of concept nodes shared between user history items and the candidate. : similarity-weighted average feedback from~$M$ neighbors for the candidate. These features bridge content-based, collaborative, and graph-structural signals into a single representation consumed by the reward model. We train the reward function via listwise ranking, formally grounded in single-step Maximum Entropy IRL~. The reward is parameterized as a two-layer MLP: R_ = w_2^ \, + b_1) + b_2, where $W_1 ^{h d}$, $w_2 ^{h}$, and $h$ is the hidden dimension. A linear ablation $R_ = w^ + b$ is also trained to isolate the contribution of nonlinearity. The policy follows a Boltzmann distribution over the candidate set~$C$: _ = { _{a' C} R_ }. Training maximizes the log-likelihood of expert actions: = - _{ } _ , where~$a_t^*$ is the user's actual next positive interaction. This listwise objective normalizes over the full candidate set, jointly comparing all candidates---unlike pointwise cross-entropy, which scores each candidate independently. Notably, this softmax objective is formally identical to the partial likelihood of the Cox proportional hazards model in survival analysis, where the risk set plays the role of the candidate set and the event individual corresponds to the expert-chosen action~. This connection provides well-studied statistical grounding for the listwise ranking loss used here. Features are standardized before training, and model selection uses validation-based early stopping. At inference, the reward model scores all candidates and selects a top-$N$ shortlist. This learned retriever filters the candidate set down to the most promising items before the more expensive LLM stage, implementing a lightweight filter-then-rerank architecture. This delegate-and-conquer strategy---where a lightweight model identifies high-potential candidates for full processing by a more powerful but expensive model---parallels recent efficiency-oriented designs in other domains such as temporal video grounding~. A personalized prompt is constructed for each user to elicit a listwise ranking from an LLM over the top-$N$ shortlist in a single call. The prompt contains: : the user's top category preferences and recently positively-rated items, derived from user-specific retrieval. : for each candidate, the fraction of the user's $M$ nearest neighbors who positively engaged with it. : title, categories, and top tags for each shortlisted item. : a discretized indicator based on where the candidate's reward score falls relative to the shortlist distribution, providing the LLM with the IRL model's assessment. The LLM returns a full ordering of all~$N$ candidates. Multiple frontier LLMs are evaluated to assess robustness across model families; each LLM receives identical prompts and is queried at temperature zero for reproducibility. The final recommendation blends rankings from both the IRL model and the LLM via rank-level fusion: = _{} + _{} , where ranks are 1-indexed . Rank-level rather than score-level fusion is used because IRL reward scores and LLM ordinal outputs are on incommensurable scales. The weight~$ $ is tuned per LLM on the validation set via grid search, optimizing NDCG@10. This design allows the fusion to adapt to each LLM's ranking quality: a stronger LLM earns a higher~$ $, whereas a weaker LLM is down-weighted in favor of the IRL model's calibrated ordering. Algorithm~ summarizes the full pipeline. Training data $$, knowledge graph $$, candidate generator, LLM Construct heterogeneous graph $$ Build user similarity index Build TF-IDF index over item text Initialize reward network $R_ $ Sample candidate set $C_t = \{a_t^*\} $ Retrieve user-specific context from $$ Retrieve community context from top-$M$ similar users Compute features $ $ for all $a C_t$ Compute rewards $R_ $ for all $a C_t$ $ - { _{a'} R_ }$ Update $ $ via gradient descent on $$ Score all candidates; select top-$N$ shortlist Build persona prompt Query LLM $ $ listwise ranking of top-$N$ $ _{} + _{} $ Rank by combined score; output recommendation Our results reveal a clear separation of responsibilities among the three components of the proposed framework. The inverse reinforcement learning formulation provides a principled approach to preference modeling by interpreting user interaction sequences as demonstrations of an underlying reward function. Unlike pointwise supervised objectives, the listwise softmax objective enforces joint comparison across candidates, leading to improved ranking calibration. This explains the consistent gains of IRL over supervised baselines observed in our experiments. From a modeling perspective, IRL can be viewed as learning an energy function over state--action pairs, where higher reward corresponds to higher likelihood under expert behavior. Formally, the same loss appears as the partial likelihood of the Cox model in survival analysis~, where it is used to rank patients by hazard given censored time-to-event data. This cross-domain equivalence provides additional statistical justification: the consistency and efficiency properties studied in survival analysis transfer directly to our listwise ranking setting~. Graph-derived features contribute structured semantic and collaborative signals that are difficult to capture with raw behavioral features alone. Importantly, these features are not merely additive: their impact is amplified under listwise training, suggesting that IRL is better able to exploit relative signals such as community support and concept overlap. This interaction highlights a key insight: . Rich features alone are insufficient without a ranking objective that can leverage them effectively. While LLMs possess strong semantic reasoning capabilities, our results confirm that they are unreliable when used as standalone rankers. Their outputs lack calibration and are sensitive to prompt structure and candidate ordering. However, when applied to a high-quality shortlist produced by IRL, LLMs provide complementary signals that improve ranking quality. The effectiveness of LLM integration scales with model quality, as reflected in the optimal $ $ values and performance gains. This supports a hybrid design in which LLMs refine rather than replace learned ranking models. A central finding of this work is that improvements in candidate retrieval and pre-ranking contribute more to overall performance than improvements in final ranking. IRL significantly improves the quality of the top-$N$ shortlist, effectively acting as a learned retrieval mechanism. Once the candidate set is sufficiently strong, LLM re-ranking yields incremental but consistent gains. This suggests a general design principle for modern recommendation systems: Despite promising results, several limitations remain. Graph construction and community retrieval introduce additional computational overhead. While feasible for medium-scale datasets, further optimization is required for web-scale deployment. Users with limited interaction history may lack sufficient graph context, reducing the effectiveness of both GraphRAG features and IRL reward estimation. Although applied only to a shortlist, LLM inference still incurs non-trivial cost and latency, which may limit real-time applicability in production environments. This work opens several directions for future research. Extending IRL-based ranking to large-scale datasets requires efficient approximation methods and distributed training strategies. More advanced IRL formulations, such as adversarial IRL or generative adversarial imitation learning , may further improve reward estimation and capture complex user behavior. Neural reward models may be susceptible to adversarial perturbations in input features, a concern studied extensively in deep learning classification~. Similarly, the LLM re-ranking component inherits the safety and reliability challenges of foundation models; systematic benchmarking of LLM behavior under adversarial prompts~ suggests that ranking outputs may degrade under distribution shift or prompt manipulation. Investigating the robustness of both IRL-based reward models and LLM re-rankers to adversarial manipulation of user profiles or item features is an important direction for deploying these systems in production. Instead of static $ $ blending, future work could learn adaptive fusion strategies that dynamically adjust the contribution of LLM signals based on user context, candidate uncertainty, or model confidence. LLMs provide a natural interface for generating explanations. Integrating explanation generation with IRL-based ranking could improve transparency and user trust. We presented , a hybrid recommendation framework that combines graph-grounded feature construction, inverse reinforcement learning, and LLM-based re-ranking. Our study demonstrates that: IRL provides a strong and stable foundation for personalized ranking through listwise reward learning, GraphRAG features significantly enhance representation quality, particularly when paired with IRL, LLMs are most effective when used as complementary semantic signals rather than standalone rankers. Overall, our results suggest a unified perspective for modern recommendation systems: combining structured behavioral learning with semantic reasoning yields robust and scalable personalization. 