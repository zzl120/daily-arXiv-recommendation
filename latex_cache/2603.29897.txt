 {acmart} {} }} {enumitem} {algorithm2e} } } } } } } } Reranking is a critical component in many information retrieval pipelines. Despite remarkable progress in text-only settings, multimodal reranking remains challenging, particularly when the candidate set contains text and image items. A key difficulty is the modality gap: a text reranker is intrinsically closer to text candidates than to image candidates, leading to biased and suboptimal cross-modal ranking. Vision-language models mitigate this gap through strong cross-modal alignment and have recently been adopted to build multimodal rerankers. However, most VLM-based rerankers encode candidates as images, and treating text as images introduces substantial computational overhead. Meanwhile, existing open-source multimodal rerankers are typically trained on general-domain data and often underperform in domain-specific scenarios. To address these limitations, we propose , a VLM-based reranking framework that natively scores and orders text-image candidates without any modality conversion. Building on this hybrid scoring interface, provides an end-to-end domain adaptation pipeline that includes: an instruction-tuning stage that learns calibrated cross-modal relevance scoring by mapping label-token likelihoods to a unified scalar score; and a hard-negative-driven preference alignment stage that constructs in-domain pairwise preferences and performs query-level policy optimization through reinforcement learning from human feedback . Extensive experiments on scientific literature retrieval and design patent search demonstrate that consistently outperforms state-of-the-art baselines, improving Recall@1 by 8.9\ Information retrieval ~ aims to identify and return the most relevant items from a large corpus in response to a user query. Modern IR systems are commonly built as a multi-stage pipeline, where and serve as two fundamental components~. In the first stage, a dense embedding model maps queries and items into a shared vector space, enabling efficient approximate nearest neighbor search to retrieve a small set of potentially relevant candidates from millions of documents. In the second stage, a reranker performs more fine-grained relevance modeling over the candidate set and produces the final ranked list, ensuring that the most relevant results are placed at the top. This retrieve-then-rerank paradigm has delivered substantial improvements in text-only IR and has become a standard practice in many real-world applications~. In the past few years, the demand for multimodal retrieval has rapidly grown~, driven by applications such as e-commerce search~, scientific literature discovery~, and content recommendation~, where both text and images may serve as queries and/or candidates. However, directly extending text rerankers to such settings remains challenging due to the ~: these models are inherently biased toward textual inputs and struggle to fairly compare relevance across heterogeneous modalities. A common workaround is to first convert images into textual descriptions and then apply a text reranker. While feasible, this approach is fundamentally limited because textual descriptions cannot always faithfully capture the rich and fine-grained semantics of images, leading to a low performance ceiling in practice~. These limitations motivate the need for dedicated multimodal rerankers that can reason over both visual and textual content. Benefiting from recent advances in VLMs and their strong cross-modal alignment capability, an emerging research direction is to build multimodal rerankers on top of VLMs~. Nevertheless, existing VLM-based rerankers still have notable limitations. First, many approaches homogenize the input format by treating candidates as images so that the reranker can operate in a single visual input space. This design simplifies modeling but incurs substantial storage and inference overhead, and becomes particularly inefficient when the candidate pool is dominated by textual items or requires cross-modal reranking between text and images. Second, while several open-source multimodal rerankers perform well on general-domain benchmarks, they are typically trained on broad web-scale data and lack specialization for domain-specific content. As a result, their performance often degrades when deployed in contexts such as e-commerce, finance, or defense, where private data and unique semantics dominate~. Crucially, there remains a lack of comprehensive tailored for building domain-adaptive multimodal rerankers—a gap that limits the deployment of high-performance retrieval systems in privacy-sensitive and application-critical scenarios. To this end, we propose , an end-to-end framework for training domain-specific rerankers that directly operate on hybrid text-image candidates without modality conversion. Starting from a pretrained VLM backbone, we first construct high-quality instruction-following data and perform supervised fine-tuning to obtain a VLM-based reranker that produces meaningful and comparable relevance scores for hybrid candidates. We then run the SFT reranker on held-out in-domain data to mine ---high-scoring but non-relevant candidates---and pair them with gold positives to build large-scale pairwise preference data. Finally, we leverage this preference dataset to train a reward model and further optimize the reranker via RLHF, improving both score calibration and ranking robustness in domain-specific settings. Our main contributions are summarized as follows: We introduce , a VLM-based multimodal reranking framework that natively operates on candidate sets containing both text and images, enabling unified and fair cross-modal relevance scoring without converting text into images, thus reducing storage and inference overhead. We propose an end-to-end, two-stage training framework for domain-specific multimodal rerankers, including instruction-driven SFT to learn calibrated cross-modal relevance scoring, and hard-negative mining followed by RLHF to construct in-domain pairwise preferences for reward modeling and policy optimization. We evaluate on scientific literature retrieval and design patent search, and show that consistently outperforms state-of-the-art multimodal rerankers, achieving up to 8.9\ Text reranking has been extensively studied under the retrieve-then-rerank paradigm, where the reranker refines a candidate set returned by a fast first-stage retriever. A dominant line of work adopts the architecture, which jointly encodes the query and a candidate document by concatenation and leverages Transformer-based full token-level interactions to estimate relevance, as in bge-rerankers~, jina-rerankers~, and mxbai-rerankers~. Another representative direction is , exemplified by ColBERT-style models, which encode queries and documents independently and perform efficient token-level matching at inference time~. Multimodal reranking extends the above problem to settings where queries and/or candidates may contain images in addition to text, and the candidate pool can be heterogeneous ~. Existing approaches can be broadly categorized into two groups. The first group reduces multimodality to text by converting images into textual representations, such as captions and VLM-extracted text, and then applying a text reranker~. While simple and compatible with mature text rerankers, this strategy may lose visual details and can be bottlenecked by the quality of the intermediate descriptions~. The second group builds rerankers directly on top of VLMs, leveraging their cross-modal alignment to compare relevance between queries and multimodal candidates~. Recent examples include VLM-based rerankers enhanced with chain-of-thought prompting and task-specific fine-tuning for page-level reranking~, as well as methods that finetune Qwen3-VL~ to obtain a reranker by framing reranking as a binary classification problem~. Different from these prior efforts, targets reranking over text-image candidate sets while emphasizing an end-to-end training framework for adaptation. Supervised fine-tuning adapts a pretrained language or vision--language model to downstream tasks by training on curated instruction--response pairs~. In the post-instruction-tuning era, SFT has become a standard recipe to elicit desired behaviors such as following task specifications, producing structured outputs, and learning task-specific scoring or classification heads. To reduce training cost and memory footprint, parameter-efficient fine-tuning methods are widely used~, such as LoRA~, QLora~, and AdaLoRA~. These techniques enable fast customization of large models while keeping most backbone parameters frozen, making them particularly appealing for domain adaptation with limited compute or privacy constraints. Beyond SFT, reinforcement learning from human feedback is commonly employed to align model outputs with human or application-specific preferences~. Traditional RLHF trains a reward model from pairwise comparisons and then optimizes the policy using reinforcement learning algorithms such as PPO~. More recent alternatives like DPO~ simplify the pipeline by directly optimizing a policy with preference data, which avoids explicit reward modeling and RL rollouts. In addition, group-based objectives~ have been explored to improve stability and sample efficiency when multiple candidates are available. These approaches provide practical tools for improving calibration, robustness, and controllability, and are increasingly adopted for domain-specific alignment where labeled relevance signals are scarce but preference signals can be collected at scale. {figures/unirank_framework.pdf} . We first perform instruction-driven SFT to obtain a VLM-based reranker. We then mine hard negatives to construct pairwise preferences, train a reward model, and further align the reranker via RLHF for domain-specific hybrid text-image reranking.} Early progress in visual representation learning was driven by Vision Transformers ~, which demonstrated that transformer architectures can serve as strong generic backbones for images. Building on such encoders, contrastive vision--language pretraining methods~ learned aligned image--text embedding spaces at scale, enabling zero-shot recognition and cross-modal retrieval. Subsequent work further improved vision--language alignment and generation capability via image--text pretraining objectives and multimodal denoising/generation~, making it possible to move beyond embedding alignment toward unified multimodal understanding and generation. Recent VLMs typically follow a unified architecture that connects a vision encoder to a large language model through a modality adapter , enabling the LLM to consume visual tokens and perform multimodal reasoning. Representative families include instruction-tuned VLMs such as MiniGPT-4~, LLaVA~, and the Qwen-VL series~, which combine large-scale pretraining with instruction tuning to support general-purpose multimodal dialogue, grounding, and reasoning. These advances provide a strong foundation for multimodal IR components, including rerankers, by offering improved cross-modal alignment and flexible interfaces for scoring and decision making over mixed visual and textual evidence. In this section, we provide a detailed description of . Specifically, we first formalize the task of reranking over hybrid text-image candidates and clarify the input/output interface of a VLM-based reranker. We then introduce our two-stage domain-specific training pipeline: supervised fine-tuning with instruction-following data to learn calibrated cross-modal relevance scoring; and preference-based alignment to further improve ranking quality via hard-negative mining, reward model training, and RLHF. An overview of the proposed framework is shown in Figure~. We study the reranking problem in a hybrid text-image retrieval setting. Given a query $q$ and an initial candidate set $=\{c_1, ,c_n\}$ returned by a first-stage retriever, the goal of reranking is to produce a permutation of $$ such that the most relevant candidates appear at the top. In our setting, each query and candidate is a multimodal item that may contain text, images, or both, requiring unified scoring across heterogeneous inputs. A reranker is parameterized by $ $ and computes a relevance score for each query--candidate pair: s_{ } , c_i . The final ranked list is obtained by sorting candidates in descending order of their scores: = ), where $ $ denotes the resulting permutation. When ground-truth relevance labels are available, we use $y_i \{0,1\}$ to indicate whether $c_i$ is relevant to $q$. The training objective is to learn $ $ such that relevant candidates receive higher scores than non-relevant ones, i.e., s_{ } > s_{ } , c^{+} ^{+},\; c^{-} ^{-}, where $^{+}$ and $^{-}$ are the relevant and non-relevant subsets of $$ for query $q$. In this work, we instantiate $s_{ }$ using a VLM that natively supports multimodal inputs. Specifically, we represent the query and candidate in their original modalities and feed them into the VLM with an instruction prompt. The VLM then generates a short judgment together with token-level likelihoods, from which we derive a scalar relevance score $s_{ } $. This interface enables a single reranker to score text--text, image--image, and cross-modal pairs with a unified scoring function. To convert a general-purpose VLM into a reranker, we cast reranking as an task: given a query $q$ and a candidate $c$, the model is prompted to output a discrete relevance label indicating whether $c$ is relevant to $q$. The label space $$ can be domain-specific in practice . Without loss of generality, we denote the two possible outputs as $=\{,\}$ throughout this paper. During inference, we derive a scalar relevance score from the model's label token logits by applying a sigmoid to the logit difference: s_{ } = -_{ } ), where $ $ denotes the sigmoid function. This score is then used for standard top-$k$ reranking. To this end, we serialize each labeled query--candidate pair $ $ into a chat-style instruction example, which supports interleaved text and images and constrains the model to output a single label in $$. Specifically, each example is represented as a three-message conversation: = , where $m^{}$ specifies the reranking role and restricts the output space to $$; $m^{}$ provides the query $q$ and candidate content $c$ in their native modalities; and $m^{}$ is the ground-truth label $y$. Figures~ and in Appendix illustrate the overall prompt structure used in , including the concrete form of the system instruction and the constrained label output. To be specific, the system message instructs the model to act as a and to respond with from $$, without any explanation. The user message is constructed by concatenating the query and candidate with explicit delimiters: m^{} = q \;\; \;\; c, where $ $ denotes string concatenation. If $q$ or $c$ contains images, we place a special placeholder token at the corresponding position in the user message and pass the raw image through the VLM's vision input channel. This design enables a single prompt template to handle text candidates, image candidates, and mixed query--candidate pairs in a unified manner, without converting one modality into the other. In practice, we construct instruction-following data from in-domain relevance annotations. For each query $q$, annotated relevant items form positive pairs $ $, while non-relevant items are used to form negative pairs $ $. We then serialize each labeled pair $ $ into the chat format in Eq.~ , producing the final SFT dataset $_{}=\{ _j \}_{j=1}^{N}$. Given the instruction-following dataset $_{}$, we train the VLM reranker by minimizing the standard language modeling loss over the assistant response tokens. Specifically, for each message sequence $$, we compute the negative log-likelihood of the ground-truth label $y $ conditioned on the system and user messages: _{} = - P_{ } , where $P_{ } $ denotes the model's conditional probability of generating the label token in $m^{}$. In practice, this is implemented as a cross-entropy loss applied only to the output positions corresponding to the label tokens , while masking all other tokens in the sequence. This objective encourages the model to learn calibrated token-level logits that capture domain-specific relevance semantics, which directly underpin the scoring function \ \) defined in Eq.~. To support the subsequent preference alignment stage, we reserve a subset of $_{}$ as hold-out data, denoted $_{}$. This subset is excluded from SFT optimization and is only used later for constructing pairwise preference data, ensuring that the preference annotations are derived from in-domain examples unseen during SFT, thereby reducing overfitting and improving generalization in the alignment phase. After SFT, we further improve the reranker via preference-based alignment. This requires a pairwise preference dataset that captures failure modes of the SFT model and provides richer supervision than pointwise labels. To this end, we construct preference data on the held-out split $_{}$ by mining query--candidate prompts on which the SFT reranker is most likely to make mistakes. Recall that each query--candidate pair is serialized into a prompt context $= $ , and the model is required to output a label $y =\{,\}$. For each query $q _{}$, we score all candidates $c _q$ using the SFT model and rank them by $s_{ } $. We then focus on the top-$N$ candidates and select prompts where the model tends to over-estimate relevance, i.e., non-relevant candidates appearing in the top-$N$: _q = \{c N \; |\; y =0 \}, where $y \{0,1\}$ denotes the ground-truth relevance. For each hard negative candidate $c^{-} _q$, we create a pairwise preference instance at the level by keeping the same prompt $= $ and contrasting the label $y^{ }$ against the one $$: , y^{ } = ,\;\; =. That is, for hard negatives the preferred response is the ground-truth label and the rejected response is the SFT model's typical error mode . Symmetrically, for positive prompts $ $ with $y =1$, we construct , y^{ } = ,\;\; =. Aggregating over queries yields the final preference dataset $_{}$. This construction directly targets label-level confusions on difficult in-domain prompts, and is well matched to our reranker formulation where relevance is inferred from label token likelihoods. Given the preference dataset $_{}$, we train a reward model to predict which is preferred under the same prompt. Concretely, the RM shares the same multimodal prompting interface as the reranker. Let $= $ denote the prompt context constructed from a query--candidate pair, and let $y $ be a label token sequence. The RM takes $ $ as input and outputs a scalar reward r_{ } , where $ $ denotes the RM parameters. Intuitively, $r_{ } $ measures how appropriate it is to respond with label $y$ under prompt $$, and thus reflects in-domain relevance preferences. We optimize the RM using a standard pairwise preference loss. For each preference instance $ _{}$, where $y^{ }$ is the preferred label and $$ is the rejected label, we minimize: _{} = - _{ _{}} , where $ $ denotes the sigmoid function. This loss corresponds to a Bradley--Terry style model~ of pairwise preferences and is widely used in RLHF for learning reward functions from comparison data. Minimizing $_{}$ encourages the RM to assign higher reward to the preferred label than to the rejected one under the same prompt. In practice, we initialize the RM from the SFT reranker to preserve domain knowledge and multimodal alignment, and then fine-tune it on $_{}$ using the above objective. With the learned reward model, we further align the reranker via RLHF to directly optimize ranking behavior under in-domain preferences. We initialize the policy from the SFT reranker, i.e., $ _{ } _{ _{}}$, and treat the reranker as a policy that, given a prompt context $= $, generates a label $y $. The reward model $r_{ } $ provides a learned training signal to encourage better calibrated and more robust relevance scoring. is compatible with a range of preference-optimization objectives and RLHF algorithms. In our experiments, we adopt ~. A key design choice is that we form rather than the standard prompt-level groups used in typical GRPO settings. That is, instead of sampling multiple responses for a single prompt, we exploit the inherent listwise structure of reranking and compare multiple that share the same query. We construct groups directly from the preference data $_{}$, which already contains prompt contexts derived from held-out in-domain queries. Let $_q$ denote the set of prompt contexts in $_{}$ associated with query $q$ . For each query-level group $\{_1, ,_G\} _q$, the current policy generates labels $y_i _{ } $ and receives rewards r_i = r_{ } , i=1, ,G. We normalize rewards within the group to obtain relative advantages: A_i = { _r + }, _r = {G} _{j=1}^{G} r_j, _r = {G} _{j=1}^{G} ^2}, where $ $ is a small constant for numerical stability. The policy is then updated using a clipped surrogate objective: _{} = - [ {G} _{i=1}^{G} ( _i \,A_i,\; , 1- , 1+ )\,A_i ) ], where $ _i = }{ _{ _{}} }$ is the policy ratio, $ $ is the clipping hyperparameter, and $ _{ _{}}$ is a fixed reference policy to regularize updates. By contrasting candidates , directly optimizes the relative ordering behavior that reranking requires, thereby improving robustness on hard in-domain cases. After RLHF optimization, we use the aligned policy to compute the final relevance score $s_{ } $ for reranking. In this section, we evaluate on two domain-specific multimodal retrieval tasks, including scientific literature retrieval and design patent search, where the reranking candidate set contains text-image items. Our evaluation is designed to answer the following research questions: Does achieve better reranking performance than strong text-only and VLM-based multimodal baselines in domain-specific settings with hybrid text-image candidates? How robust is under different first-stage retrievers and different VLM backbone sizes ? What is the contribution of each component in 's end-to-end training pipeline to the final reranking quality? We evaluate on two domain-specific multimodal retrieval tasks with hybrid text-image candidate sets. We evaluate on the benchmark~ and adopt its domain under the setting. Each document is a formal academic publication consisting of $m$ layout regions with structured layouts, citations, and academic visuals. Given a natural-language question $Q$ , the system retrieves and reranks the top-$k$ most relevant layout regions from the paper. This task naturally yields a heterogeneous candidate pool spanning multiple modalities: approximately 28.8\ We additionally evaluate on a real-world design patent search task for product appearance risk screening. Given a as the query, the goal is to score each by whether it is in overall visual impression, and rerank the recalled patents accordingly. Both queries and candidates are multimodal: a product includes a main image with optional title/description, while a patent contains structured fields such as patent title, representative figure, abstract, specification text, and metadata. We construct the dataset by first retrieving candidate patents using an off-the-shelf appearance-based retrieval system, and then assigning binary labels using ~, which we treat as supervision for training and evaluation. Dataset statistics for both domains, including the splits used for supervised fine-tuning , hold-out preference construction , and the test set , are summarized in Table~. Additional qualitative examples and details of dataset construction and labeling are provided in Appendix~. {l l r r r} & & & & \\ {*}{Scientific} & $_{}$ & 957 & 3980 & 71821 \\ & $_{}$ & 239 & 995 & 17955 \\ & $_{}$ & 75 & 386 & 6840 \\ {*}{Patent} & $_{}$ & 8007 & 7891 & 79457 \\ & $_{}$ & 998 & 986 & 9927 \\ & $_{}$ & 497 & 493 & 4953 \\ We instantiate using the ~ as the backbone VLM, which supports interleaved text and image inputs and has demonstrated strong cross-modal reasoning capabilities. Both the SFT and the subsequent GRPO-based RLHF stages are implemented within the framework~, a modular toolkit for parameter-efficient fine-tuning of large models. We employ LoRA~ to reduce memory overhead and accelerate training, while preserving the rich knowledge in the frozen VLM backbone. Detailed hyperparameters and training configurations are reported in Appendix~. We compare against the following state-of-the-art reranking baselines: BGE-Reranker-v2-m3~ and Qwen3-Reranker~, two strong rerankers designed for text-only relevance scoring. For multimodal inputs, we follow the common practice of converting non-text candidates into textual descriptions before reranking. MM-R5~, a VLM-based listwise reranking method that homogenizes candidates by representing all items in an image-like format and directly generates a ranked list for the candidate set. jina-reranker-m0} and Qwen3-VL-Reranker~, two open-source VLM-based rerankers trained with large-scale data to output relevance scores for multimodal query--document pairs. We evaluate reranking quality using standard IR metrics, including Normalized Discounted Cumulative Gain , Recall@k, and Mean Reciprocal Rank . NDCG@k assesses ranked-list quality by assigning higher gain to relevant items at higher positions, with logarithmic discounting and normalization by the ideal ranking. Recall@k indicates whether at least one ground-truth relevant candidate is retrieved within the top-$k$ positions. MRR measures how early the first relevant item appears, computed as the average reciprocal rank of the highest-ranked relevant candidate over all queries. Following common practice, we report results for $k \{1, 3, 5\}$. Formal definitions of these metrics are provided in Appendix~. {!}{ {l|c|ccc|ccc|c} {*}{Method} & {*}{Size} & {c|}{Recall@k} & {c|}{NDCG@k} & MRR \\ & & @1 & @3 & @5 & @1 & @3 & @5 & \\ BGE-Reranker-v2-m3 & 0.6B & 26.7 & 40.0 & 57.8 & 26.7 & 30.5 & 38.1 & 38.3 \\ BGE-Reranker-v2-m3 & 0.6B & 37.8 & 66.7 & 73.3 & 37.8 & 48.2 & 52.0 & 52.3 \\ Qwen3-Reranker & 8B & 37.8 & 66.7 & 77.8 & 37.8 & 49.7 & 53.8 & 53.6 \\ Qwen3-Reranker & 8B & 35.6 & 60.0 & 71.1 & 35.6 & 48.7 & 52.6 & 50.6 \\ MM-R5 & 8B & & 73.3 & 80.0 & & 59.1 & 63.0 & 63.4 \\ jina-reranker-m0 & 2B & 48.9 & & & 48.9 & & & \\ Qwen3-VL-Reranker & 8B & 37.8 & 64.4 & 66.7 & 37.8 & 49.5 & 50.7 & 52.0 \\ & 8B & & & & & & & \\ } {!}{ {l|c|ccc|ccc|c} {*}{Method} & {*}{Size} & {c|}{Recall@k} & {c|}{NDCG@k} & MRR \\ & & @1 & @3 & @5 & @1 & @3 & @5 & \\ BGE-Reranker-v2-m3 & 0.6B & 27.6 & 59.2 & 76.3 & 27.6 & 36.9 & 44.6 & 48.5 \\ Qwen3-Reranker & 8B & 35.4 & 60.6 & 76.3 & 35.4 & 41.4 & 48.5 & 53.2 \\ MM-R5 & 8B & 52.1 & & 89.5 & 52.1 & 63.3 & 67.3 & 69.6 \\ jina-reranker-m0 & 2B & 44.3 & 71.0 & 82.3 & 44.3 & 50.8 & 56.3 & 60.9 \\ Qwen3-VL-Reranker & 8B & & 81.6 & & & & & \\ & 8B & & & & & & & \\ } } Table~ and Table~ report the main reranking results on scientific literature retrieval and design patent search, respectively. Across both domains, achieves the best overall performance on all evaluated metrics, demonstrating its effectiveness for domain-specific reranking over text-image candidate sets. Notably, improves both top-rank accuracy and ranking quality at larger cutoffs , indicating better cross-modal score comparability and more robust ordering. As shown in Table~, text-only rerankers underperform in this hybrid setting even with image-to-text conversion. Following MMDocIR, we report two conversion variants for non-text layouts: OCR-extracted text and VLM-generated text descriptions. Typically, VLM-generated descriptions often outperform OCR, but the gains are not consistent across rerankers, highlighting that modality conversion can be brittle due to variable description quality. Among VLM-based baselines, MM-R5 is competitive but remains limited by its input homogenization strategy that treats candidates in an image-like format, which can be inefficient and may blur modality-specific signals. Instead, natively scores mixed-modality candidates with a unified interface, and its domain-aligned training pipeline yields consistently better Recall@k, NDCG@k, and MRR, highlighting the benefit of end-to-end adaptation for scientific documents with complex layouts. Table~ shows a similar trend in the design patent domain, where relevance is primarily determined by and subtle appearance cues. Text rerankers lag behind because cross-modal similarity cannot be reliably reduced to text, especially when fine-grained visual patterns dominate the judgment. While open-source VLM rerankers provide stronger cross-modal reasoning, their general-domain training makes them less calibrated for domain-specific similarity criteria and metadata structure in patents. By contrast, achieves the strongest performance, suggesting that combining hybrid-modality scoring with in-domain SFT and preference alignment is crucial for robust risk-oriented reranking. } Beyond effectiveness, offers significant efficiency benefits by avoiding the need to convert text into images—a common practice in VLM-based rerankers like MM-R5. To quantify this, we measure : the total size of the candidate corpus when all items are represented as images, versus their native multimodal form; and : average time per query–candidate scoring on a single H20 GPU. As shown in Table~, representing text as images inflates storage by 7.1$ $ in the scientific domain and 1.4$ $ in patents. Moreover, MM-R5’s image-heavy inputs lead to 2.6$ $ and 2.8$ $ slower inference than , which processes text natively and only encodes actual images. This confirms that ’s hybrid-input design is not only more accurate but also far more practical for real-world deployment. {!}{ {l|cc|cc} {*}{Method} & {c|}{Storage } & {c}{Inference Latency } \\ & Scientific & Patent & Scientific & Patent \\ Qwen3-Reranker & 0.7 & 0.2 & 7.6 & 9.8 \\ MM-R5 & 83.5 & 6.0 & 42.0 & 1258.9 \\ & 12.1 & 4.2 & 16.2 & 446.4 \\ } reranking . ColQwen is the default retriever used in the main experiments.} {!}{ {l|l|ccc|ccc|c} Retriever & Setting & {c|}{Recall@k} & {c|}{NDCG@k} & MRR \\ & & @1 & @3 & @5 & @1 & @3 & @5 & \\ BGE & w/o & 24.4 & 40.0 & 40.0 & 24.4 & 30.4 & 30.9 & 33.2 \\ & w/ & 48.9 & 75.6 & 77.8 & 48.9 & 59.0 & 61.2 & 62.5 \\ DPR & w/o & 15.6 & 31.1 & 35.6 & 15.6 & 21.7 & 23.0 & 24.2 \\ & w/ & 46.7 & 71.1 & 73.3 & 46.7 & 57.6 & 59.2 & 59.7 \\ ColBERT & w/o & 22.2 & 26.7 & 31.1 & 22.2 & 22.4 & 24.2 & 27.2 \\ & w/ & 51.1 & 75.6 & 80.0 & 51.1 & 60.9 & 63.2 & 64.7 \\ DSE & w/o & 35.6 & 53.3 & 60.0 & 35.6 & 42.4 & 46.1 & 46.9 \\ & w/ & 51.1 & 77.8 & 84.4 & 51.1 & 64.4 & 67.0 & 66.0 \\ ColQwen & w/o & 42.2 & 62.2 & 71.1 & 42.2 & 51.0 & 55.7 & 54.2 \\ & w/ & 60.0 & 84.4 & 95.6 & 60.0 & 70.7 & 74.3 & 74.4 \\ } instantiated with Qwen3-VL backbones of different sizes, alongside representative text and VLM reranker baselines at multiple scales.} {l|c|ccc|ccc|c|ccc|ccc|c} {*}{Method} & {*}{Size} & {c|}{Scientific} & {c}{Patent} \\ & & {c|}{Recall@k} & {c|}{NDCG@k} & MRR & {c|}{Recall@k} & {c|}{NDCG@k} & MRR \\ & & @1 & @3 & @5 & @1 & @3 & @5 & & @1 & @3 & @5 & @1 & @3 & @5 & \\ Qwen3-Reranker & 4B & 42.2 & 60.0 & 71.1 & 42.2 & 48.7 & 53.8 & 53.9 & 37.0 & 63.0 & 77.5 & 37.0 & 42.9 & 49.1 & 54.8 \\ & 8B & 37.8 & 66.7 & 77.8 & 37.8 & 49.7 & 53.8 & 53.6 & 35.4 & 60.6 & 76.3 & 35.4 & 41.4 & 48.5 & 53.2 \\ Qwen3-VL-Reranker & 2B & 33.3 & 60.0 & 62.2 & 33.3 & 44.3 & 45.9 & 46.7 & 43.9 & 72.6 & 85.5 & 43.9 & 52.3 & 58.9 & 61.5 \\ & 8B & 37.8 & 64.4 & 66.7 & 37.8 & 49.5 & 50.7 & 52.0 & 57.9 & 81.6 & 91.7 & 57.9 & 64.4 & 70.0 & 71.9 \\ & 2B & 48.9 & 82.2 & & 48.9 & 63.6 & 68.1 & 66.8 & 62.2 & & 91.8 & 62.2 & 68.6 & 73.4 & 75.0 \\ & 4B & & & 91.1 & & & & & & 83.5 & & & & & \\ & 8B & & & & & & & & & & & & & & \\ } We first study whether remains effective when the first-stage retriever changes, which alters the candidate distribution and difficulty. On the scientific literature retrieval task, we consider five representative retrievers spanning both text and visual paradigms: three text retrievers and two visual retrievers . For text retrievers, we convert non-text layouts into OCR-extracted text. As shown in Table~, adding on top of each retriever yields consistent gains across all metrics. This suggests that does not overfit to a specific candidate distribution produced by a single retriever, but instead provides a stable and complementary relevance signal for hybrid candidates. Notably, ColQwen yields the strongest initial retrieval performance, and thus is adopted as the default retriever in our main experiments. Even in this strong-retriever regime, still brings clear gains, indicating that its improvements are not merely due to fixing easy retrieval errors but also refining hard top-ranked cases. } We further examine robustness to model capacity by instantiating with different Qwen3-VL backbone sizes. We additionally report two representative baselines at multiple scales: the text-only Qwen3-Reranker and the VLM-based Qwen3-VL-Reranker . Table~ summarizes results on both domains. For Qwen3-Reranker, we report its best result among the OCR-text and VLM-text conversions. Overall, larger backbones consistently improve reranking performance, and remains the strongest method at each comparable scale. In particular, -2B already matches or exceeds larger baseline rerankers in most metrics, while -8B achieves the best overall accuracy, indicating that our hybrid-modality scoring and end-to-end alignment recipe is effective across model sizes and provides a practical accuracy--cost trade-off. . Each variant modifies one component of the full pipeline while keeping all other settings fixed.} {l|ccc|ccc|c|ccc|ccc|c} {*}{Variant} & {c|}{Scientific} & {c}{Patent} \\ & {c|}{Recall@k} & {c|}{NDCG@k} & MRR & {c|}{Recall@k} & {c|}{NDCG@k} & MRR \\ & @1 & @3 & @5 & @1 & @3 & @5 & & @1 & @3 & @5 & @1 & @3 & @5 & \\ w/o SFT & 44.4 & 77.8 & 81.1 & 44.4 & 59.5 & 65.2 & 62.2 & 49.7 & 77.3 & 89.3 & 49.7 & 57.2 & 63.3 & 66.0 \\ w/o Hard-Negative Mining & 51.1 & 80.7 & 80.9 & 51.1 & 63.6 & 65.3 & 66.8 & 63.6 & & & 63.6 & 69.5 & 73.9 & 75.5 \\ w/o RLHF & 46.7 & & 84.4 & 46.7 & 64.9 & 67.1 & 66.4 & 63.0 & & 91.5 & 63.0 & 69.1 & 73.5 & 75.3 \\ $^ $ & & & & & & & & & 83.3 & & & & & \\ & & & & & & & & & & & & & & \\ This section ablates key components of to understand which design choices are essential for effective domain-specific reranking over hybrid text-image candidates. Starting from the full pipeline, we consider the following controlled variants: skip supervised fine-tuning and directly perform preference-based training starting from the pretrained backbone. replace hard-negative mining with uniformly sampled negatives when constructing preference data, keeping the preference dataset size fixed. remove the entire preference-alignment stage and use the SFT model for reranking. $^ $):} keep the full pipeline but replace our query-level grouping in GRPO with the standard prompt-level grouping , which ignores the listwise structure induced by sharing the same query. All other settings follow the main experiments for fair comparison. Table~ summarizes the ablation results. Overall, the full performs best across both domains, and each component contributes in a complementary way. } Removing SFT leads to the largest performance drop on both datasets. This indicates that, for reranking framed as label-token scoring, instruction-following SFT is crucial to establish a usable scoring interface and to inject domain-specific relevance semantics before preference optimization. Without this initialization, preference training becomes substantially less stable and less data-efficient, resulting in degraded ranking quality. } Replacing hard negatives with random negatives consistently reduces performance. Random negatives are often too easy and provide weak learning signals, whereas hard negatives capture the SFT model's in-domain failure modes , producing more informative comparisons that improve the reward model and downstream GRPO optimization. } Compared to the SFT-only variant , the full achieves consistent gains on Recall@1/MRR as well as NDCG@3/5, suggesting that RLHF improves both top-rank accuracy and overall ordering. This supports that preference-based alignment helps correct systematic miscalibration of scores on difficult cross-modal cases, especially when candidates have heterogeneous modalities. } $^ $ underperforms our query-level GRPO despite using the same preference data. This suggests that exploiting the listwise structure---contrasting candidates under the same query---provides a better training signal for reranking than treating each query--candidate prompt independently, leading to more consistent improvements in relative ordering. } The ablation results validate our end-to-end design: SFT provides a necessary domain-adapted and well-calibrated scoring interface; hard-negative mining supplies informative in-domain comparisons; RLHF further improves robustness and top-rank quality beyond SFT; and query-level GRPO better matches the listwise nature of reranking than prompt-level optimization. In this paper, we presented , an end-to-end framework for training domain-specific rerankers over hybrid text-image candidate sets. By leveraging the strong cross-modal alignment of VLMs, enables native and unified relevance scoring over mixed-modality inputs, preserving rich semantic cues while avoiding the overhead of converting text into images. Our framework consists of two stages: instruction-driven SFT to learn calibrated relevance scoring, and preference-based alignment with hard-negative mining and query-level GRPO to further refine ranking behavior using in-domain signals. This pipeline allows to adapt effectively to specialized domains where both accuracy and efficiency are critical. Extensive experiments show that consistently outperforms strong text-only and VLM-based reranking baselines, achieving higher Recall@k, NDCG@k, and MRR while maintaining lower storage and inference costs. Ablation studies validate the contribution of each component, and robustness analyses demonstrate that generalizes well across diverse first-stage retrievers and backbone model sizes. Future work includes extending to additional modalities and richer relevance signals , supporting multi-image candidates under larger context scales, and exploring fully unsupervised domain adaptation when labeled data is scarce. We hope serves as a practical and scalable solution for domain-specific multimodal retrieval systems where hybrid candidate sets and resource constraints are prevalent. This section provides the formal definitions of the evaluation metrics used in our experiments, including Recall@k, NDCG@k, and Mean Reciprocal Rank . For a query $q$, let the reranker output an ordered list of $n$ candidates $ _q = $, sorted by decreasing predicted relevance score. Let $y_i \{0,1\}$ denote the ground-truth relevance label of candidate $c_i$ for query $q$ . Let $_q$ denote the position of the highest-ranked relevant candidate: _q = \{ i y_i = 1 \}, and $_q = + $ if no relevant candidate exists in the list. Recall@k measures whether at least one relevant candidate is retrieved within the top-$k$ results: k = = , where $ $ is the indicator function. We report the average Recall@k over all queries: k = {||} _{q } k . MRR measures how early the first relevant candidate appears: = {_q}, & _q < + , \\ 0, & , and Mean Reciprocal Rank averages this quantity over queries: = {||} _{q } . NDCG@k evaluates ranked-list quality with position-based discounting. We first define DCG@k for a query as: k = _{i=1}^{k} -1}{ _2 }. We then compute the ideal DCG@k by sorting candidates by ground-truth relevance: k = _{ } _{i=1}^{k} _i}-1}{ _2 }, where $ $ ranges over permutations and $y^{ }_i$ is the relevance label at position $i$ under $ $. Finally, NDCG@k is the normalized form: k = k }{k }, & k > 0, \\ 0, & . We report mean NDCG@k across queries: k = {||} _{q } k . All metrics are computed on the reranked top-$k$ list produced by the reranker given the candidate set returned by the first-stage retriever. In our experiments, we report $k \{1,3,5\}$. We evaluate the scientific domain on the benchmark~, which targets multimodal information retrieval over long documents. MMDocIR provides two retrieval granularities: page-level retrieval and fine-grained layout-level retrieval, where a corresponds to a localized region such as a paragraph, figure, or table. In this work, we focus on the setting in the domain, where each document is a visually rich scientific paper with structured layouts, citations, and academic figures/tables. Given a text-only question, the system retrieves and reranks the top-$k$ most relevant layout regions within the document. As reported by MMDocIR, the evidence layouts in the Academic Paper domain span multiple modalities, including text, image, and table regions, making it a representative testbed for hybrid-candidate reranking. For evaluation, we directly use the Academic Paper subset from the MMDocIR evaluation set. Figure~ illustrates a representative layout-level reranking example, where a text-only query is matched against hybrid candidate regions within a scientific paper. For training our reranker, we use the MMDocIR training set and focus on its subset to construct in-domain reranking supervision. Specifically, we form query--candidate pairs from MMDocIR-provided layout-level labels and split them into the supervised fine-tuning set $_{}$ and the hold-out set $_{}$ . Additional examples of the instruction format and preference instances are provided in Appendix~. {figures/mmdocir_example.pdf} {figures/ruiguan_example.pdf} The design patent search task is motivated by real-world product appearance risk screening. Given a as the query and a set of recalled as candidates, the goal is to judge whether the product and each patent are in overall visual impression and output a reranking score for sorting the candidate patents. Each query corresponds to multiple candidate patents, and the reranker is evaluated on its ability to place high-risk patents above low-risk ones. For each target product, we first retrieve a candidate pool of potentially similar design patents using an off-the-shelf appearance-based retrieval system.} This step provides the initial candidate set for reranking and simulates the standard retrieve-then-rerank pipeline in deployment. To construct supervision signals for reranker training, we assign binary risk labels to each product--patent pair using ~. Concretely, for each query, we provide the model with the target product's main image and optional textual fields , and each candidate patent's structured metadata together with representative patent figures. The model is instructed to compare the two designs from an overall-appearance perspective and returns a structured prediction that includes a risk label. We then treat the predicted risk label as the supervision signal for reranker training and evaluation. After labeling, we construct a standard reranking dataset where each query is paired with its recalled candidates and corresponding risk labels. We create the supervised fine-tuning set $_{}$ from labeled query--candidate pairs, reserve a disjoint subset as $_{}$ for hard-negative mining and preference construction, and use a held-out test split for final evaluation. Figure~ illustrates the reranking setup in this domain, where a target product query is compared against multiple recalled design patents. Examples of our system prompts, SFT instances, and preference pairs for this domain are provided in Appendix~. This section summarizes the key implementation details of , including training hyperparameters and computational cost. We implement both SFT and GRPO-based RLHF using the framework~. All experiments are conducted with parameter-efficient fine-tuning via LoRA~ on top of backbones. Table~ and Table~ list the hyperparameters used for SFT and GRPO, respectively. Unless otherwise stated, we keep hyperparameters fixed across domains. {!}{ {l l} & \\ Backbone model & Qwen3-VL-\{2B,4B,8B\}-Instruct \\ LoRA target modules & all-linear \\ LoRA rank $r$ & 16 \\ LoRA alpha & 32 \\ Learning rate & $5 10^{-5}$ \\ LR schedule & linear \\ Warmup ratio & 0.05 \\ Epochs & 1 \\ Micro-batch size per GPU & 1 \\ Gradient accumulation steps & 8 \\ Global batch size & $8 1 8 = 64$ \\ Max sequence length & 4096 / 16384 \\ Evaluation interval & 200 / 250 \\ Checkpoint save interval & 200 / 250 \\ } {!}{ {l l} & \\ Policy init & SFT model \\ Reference policy & SFT model \\ LoRA target modules & all-linear \\ LoRA rank $r$ & 16 \\ LoRA alpha & 32 \\ Learning rate & $1 10^{-6}$ \\ Warmup ratio & 0.05 \\ Epochs & 1 \\ Micro-batch size per GPU & 1 \\ Gradient accumulation steps & 1 \\ Global batch size & $8 1 1 = 8$ \\ KL / ref regularization & 0.04 \\ Group size $G$ & 30 / 5 \\ Sampling temperature & 1.0 \\ Top-$p$ & 0.9 \\ Max completion length & 4 / 16 \\ Truncation strategy & delete \\ vLLM for rollout & enabled, colocate mode \\ vLLM tensor parallel size & 4 \\ vLLM GPU memory utilization & 0.55 \\ Dataset shuffle & false \\ } For preference data construction, we mine hard negatives from the top-$N$ candidates ranked by the SFT reranker for each query. We set $N{=}30$ for scientific literature retrieval and $N{=}5$ for design patent search. We use greedy decoding and compute reranking scores from the label-token logits . For each query, we rerank the top-$k$ candidates returned by the first-stage retriever, where $k{=}100$ for scientific literature retrieval and $k{=}10$ for design patent search. All training runs are performed on a single compute node equipped with 8 NVIDIA H20 GPUs , 192 CPU cores, and 1.8~TB RAM. Table~ reports the approximate wall-clock time for SFT, reward model training, and GRPO optimization. Times may vary with backbone size, batch size, and the number of training steps. .} {!}{ {l c c c} & & & \\ Scientific literature retrieval & 10.2 & 1.5 & 5.6 \\ Design patent search & 30.1 & 3.7 & 18.2 \\ } This section provides the prompt templates used in our experiments and illustrative examples of SFT instances and preference pairs for both domains. We frame reranking as a binary relevance judgment over a question and a candidate layout region . The system prompt is: Each SFT instance is a single-turn chat example where the user message contains the query and one candidate , and the assistant outputs exactly one label token . Preference data is constructed as pairwise comparisons under the same prompt context, where the chosen response is the ground-truth label and the rejected response is the opposite label. Figure~ shows an illustrative SFT instance and the corresponding preference pair. { \\ \\ }$:} See section .\\ }$:} \\ ,S_{2},.,.,.,,S_{d}$.}\\ }$:} \\ \\ $m^{} + m^{}$\\ }} { \\ \\ }$:} See section .\\ }$:} \\ \\ \\ \\ $m^{} + m^{}$\\ }} For the design patent domain, the reranker predicts an infringement-risk label under a common-observer criterion. The system prompt is: Each SFT instance compares one target product against one candidate patent. The user message includes the product image with optional title/description and the patent's representative figures plus structured metadata . The assistant outputs exactly one label: or . Preference data is constructed analogously as pairwise comparisons under the same prompt context, with the ground-truth label as the chosen response. Figure~ provides an illustrative example of the SFT message structure and a preference pair. 