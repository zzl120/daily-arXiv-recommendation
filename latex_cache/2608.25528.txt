 {acmart} }} {Proceedings of the 35th ACM International Conference on Information and Knowledge Management}{November 07--11, 2026}{Rome, Italy} {1em} } } } } } } } {Zheng, Liu, Liu et al.} Applying scaling laws to recommendation retrieval is hindered by feature heterogeneity: naively stacking Transformer layers yields diminishing returns because heterogeneous fields produce severe token-norm divergence. We present , a Transformer-based retrieval framework that scales with both computational budget and cross-domain data. The key enabler is ~weighted average aggregation, which restores the homogeneous-token assumption Transformers rely on. Building on this, we introduce ~target token compression that cuts per-candidate FLOPs by 85\ On a 40-billion-interaction industrial dataset and the public KuaiRand benchmark, scaling compute from 0.1 to 2\,MFLOPs per target yields +19.3\,/\,+22.2\,pt Recall@2000, confirming robust log-linear scaling. In online A/B tests, TransRetrieval lifts platform revenue by 2.53\ <ccs2012> <concept> <concept_id>10002951.10003317.10003365</concept_id> <concept_desc>Information systems~Search engine architectures and scalability</concept_desc> <concept_significance>500</concept_significance> </concept> <concept> <concept_id>10002951.10003317.10003347.10003350</concept_id> <concept_desc>Information systems~Recommender systems</concept_desc> <concept_significance>500</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010258</concept_id> <concept_desc>Computing methodologies~Learning paradigms</concept_desc> <concept_significance>300</concept_significance> </concept> </ccs2012> {Information systems~Search engine architectures and scalability} {Information systems~Recommender systems} {Computing methodologies~Learning paradigms} Scaling laws have been the driving force behind the rapid progress of large language models, where performance improves predictably with model size, data, and compute~. Recommendation systems, despite their massive datasets and growing computational budgets, have largely failed to exhibit analogous scaling behavior. Recent studies confirm this gap: traditional deep recommendation models yield diminishing or even negative returns when simply scaled up~. Transformers, whose power-law scaling properties are well established in NLP and vision, are natural candidates for closing this gap. Nevertheless, scaling Transformers in large-scale retrieval, where candidate pools reach up to $10^{8}$ items and latency budgets are measured in tens of milliseconds, faces several challenges: Standard Transformers process homogeneous input tokens: in NLP every word embedding is drawn from the same lookup table~; in vision every patch passes through an identical projection~. Recommendation features, however, are inherently heterogeneous , and mapping them into Transformer tokens produces severe scale misalignment~. This misalignment silently degrades representation quality by structurally distorting the embedding space. Retrieval must serve corpora of up to $10^{8}$ items under strict queries per second expectation ~, where the constraint is orders of magnitude more stringent than in ranking~. Industrial systems routinely serve multiple business domains with distinct behavioral distributions~. Maintaining a separate deep model per domain multiplies both training and serving cost, and sparse domains lack the data to benefit from scaling alone. Naively merging domains, however, fails because their distributions conflict. Several recent efforts advance scaling in recommendation, including HSTU~, RankMixer~, MARM~, Climber~ and MTGR~. Each of them either flattens or pre-groups the heterogeneous feature set, and all of them target ranking, which scores only the candidates retrieval has already shortlisted. How to scale Transformers for retrieval over corpora of up to $10^{8}$ items while preserving heterogeneous features under strict latency budgets thus remains largely unexplored. We present , a Transformer-based retrieval framework deployed in the Alibaba display advertising platform, serving users across 4 business domains. Three design choices, each addressing one of the challenges above, jointly enable scaling: resolves scale misalignment at the source by decoupling embedding norms from feature cardinality, restoring the homogeneous-token assumption that Transformers rely on. projects all target-side features into a single $D$-dimensional token via a lightweight multilayer perceptron , reducing per-candidate FLOPs by 85\ Inference cost is cut further by sharing one KV cache across targets: since all candidates in a request are scored against the same user, the user-side keys and values are computed once per request and reused by every candidate. injects domain information via element-wise addition, analogous to positional encoding. It unifies 4 domains into a single model at negligible additional FLOPs and turns multi-domain data into a scaling asset, with sparse domains benefiting from cross-domain transfer. All three designs use only standard operators and no extra infrastructure, offering a transferable recipe for deploying Transformers in retrieval. Deployed in the Alibaba display advertising platform, TransRetrieval achieves a 2.53\ To our knowledge, this is among the first systematic validations of scaling laws in the retrieval stage of the recommendation architecture, complementing recent ranking-stage results demonstrated by Climber~. Wukong~ provides the first systematic diagnosis of scaling inefficiency in deep recommendation models, attributing it to upscaling mechanisms that expand embedding tables without enhancing feature interaction. Subsequent efforts bypass the heterogeneity barrier rather than resolve it: HSTU~ flattens all features into a single behavioral sequence, sacrificing structural distinction between feature types; RankMixer~ pre-groups features into fixed semantic channels processed by parameter-free token mixing, while MARM~ reaches comparable gains only with 60\,TB of cache infrastructure; Climber~ focuses on multi-scale sequence extraction where inputs are behavioral tokens from the same embedding table, and MTGR~ retains rich features under the generative paradigm. All of these validate scaling at the ranking stage only; how to enable stable deep Transformer scaling in retrieval while retaining the full heterogeneous feature set remains unresolved. NANN~ establishes the model-based retrieval paradigm: a deep neural network scoring function drives beam search over an HNSW graph~, so that only a small fraction of the corpus is ever scored: the model itself determines which candidates are evaluated. BAR~, which serves as our production baseline, extends this paradigm with bidding-awareness for multi-stage consistency, but keeps the scoring network small. On the user side, the evolution from two-tower architectures~ to sequential Transformers has enriched user representations, and KuaiFormer~ reformulates retrieval as next-action prediction under a generative paradigm. Under the model-based paradigm, however, each candidate still requires a forward pass through the target network, making per-candidate FLOPs the dominant bottleneck, which is why the scoring network has stayed small. TransRetrieval stays within this paradigm but relieves that bottleneck: compressing each candidate to a single token makes the per-candidate pass cheap enough to scale the scoring network itself. Expert-based methods share expert parameters across domains, but gate-based soft routing means sparse domains, whose gates are undertrained due to limited data, cannot fully leverage the shared capacity for effective data scaling. STAR~ unifies serving through shared and domain-specific components, but its star topology has not been shown to enable cross-domain transfer that benefits data-scarce domains. {Figs/TransRetrieval-model-ready.pdf} We first formalize the retrieval paradigm and overview the architecture in Section~, then detail our three core designs in Sections~--. TransRetrieval operates under the model-based retrieval paradigm established by NANN~: a DNN scoring function $s = F_ $ drives beam search over an HNSW graph~, which visits only a small fraction $ $ of the corpus per request. We express the scoring budget as this fraction rather than as an absolute count, because it is $ $, not the corpus size, that determines the per-request cost: for a corpus of $N$ items, the model performs $ N$ forward passes instead of $N$. Practical settings put $ $ at roughly $0.1\ Unlike two-tower models where $s = f , g $ restricts interaction to inner product, model-based retrieval permits arbitrary cross-feature interaction at the cost of a per-candidate forward pass, which makes per-candidate FLOPs the dominant bottleneck. Each feature field $f$ is represented as a set of pairs $\{ \}_{i=1}^{n_f}$, where $k_i$ indexes to an embedding table $h: ^D$ and $v_i $ is its weight. As illustrated in Figure~, TransRetrieval processes features through four stages: ~each feature field is aggregated via weighted average , producing Transformer-friendly tokens; ~target-side tokens are compressed into a single $D$-dimensional token via a lightweight MLP , while each user-side feature forms $1$ token; ~a learnable domain embedding is added element-wise to all tokens ; ~the resulting tokens enter an $N_$-layer Pre-LN Transformer with ReLU feed-forward network , where the user-side key–value pairs of a request are cached once and reused across all of its target candidates. Profiling our model under conventional aggregation reveals severe scale misalignment: user-side token norms reach up to $10 $ the target-side norms. This imbalance causes attention weights to be dominated by high-norm tokens. The root cause is that aggregation produces output norms growing with feature cardinality $n_f$: user-side fields with multi-value features inherently accumulate much larger norms than target-side fields. We resolve this with : _f = ^{n_f} v_i h }{ _{i=1}^{n_f} v_i + } Division by $ _i v_i$ simultaneously cancels both the $n_f$-dependent accumulation and the heterogeneous weight magnitudes, bounding the output norm solely by the embedding table's distribution regardless of field cardinality. introduces an extreme-value bias that grows as $O $ and discards the majority of feature information. ~ applied post-aggregation underperforms empirically : its centering and rescaling erase the per-field magnitude signal that distinguishes heterogeneous fields. is an operator that simultaneously ~bounds output scale independently of $n_f$ and ~requires no learned or estimated statistics: it is a closed-form linear combination that remains stable regardless of $D$. Because all candidates of a request share one user, the user-side K/V are computed once per request and reused by every target; each target candidate then only requires its own Q/K/V/output projections plus FFN plus cross-attending to the full user sequence of length $L_u$ . Summing across $N_$ layers and $N_$ candidates: _ 4D N_ L_t N_ where $L_t$ is the number of target tokens and $L_u$ the user sequence length. Among these factors, $D$ and $N_$ are scaling parameters we wish to maximize; $N_ = N$ is already reduced by HNSW to a small fraction of the corpus; and $L_u$ is dominated by the $4D$ term. This leaves $L_t$ as the only factor with meaningful room for reduction. In the uncompressed model, $L_t$ scales with the number of target feature fields, making it a natural target for compression. We introduce a : a 3-layer MLP with hidden sizes $ $, Parametric ReLU activations and LayerNorm between layers , that maps the concatenation of all target-side aggregated embeddings into a single token: = \! ^D Crucially, this compression is not a compromise but a strategic reallocation: the freed FLOPs are reinvested into scaling $D$ and $N_$, enabling deeper and wider architectures within the same latency envelope, trading redundant per-candidate tokens for richer cross-feature interactions. The single compressed token also naturally serves as a fixed-dimensional vector for HNSW indexing without further processing. As motivated in Sec.~, a unified multi-domain model is required to avoid per-domain cost multiplication. The remaining design question is to inject domain identity. A straightforward strategy is appending $N_t$ extra tokens encoding the domain to the input sequence, but this inflates attention cost from $O $ to $O ^2)$. We instead introduce a : a learned vector $_d ^D$ indexed by domain $d$, added element-wise to every token: }_f = _f + _d Analogous to positional encoding~, domain identity is injected without increasing sequence length or attention complexity. The key insight is that this design enables : because domain information enters only through element-wise addition, all Transformer parameters, including every attention head and FFN layer, are unconditionally shared across domains. Every training sample from every domain contributes gradients to the same parameters, so sparse domains are no longer data-starved. This contrasts with MMoE/PLE where gate routing implicitly partitions gradient flow. Deploying TransRetrieval in production requires co-designing the inference system with the model architecture to meet strict latency constraints. A request's user-side KV pairs are computed once and shared across the scoring passes of all its candidates. Standard LLM inference frameworks would replicate that cache once per candidate sequence; we instead replace the cache manager with an expand operator that broadcasts the single user cache to every candidate without copying, reducing per-pass latency by 30\ After target compression, each candidate is a single query token attending to the full user context. This extreme aspect ratio severely underutilizes modern GPU hardware, which is optimized for large matrix operations rather than single-row queries. We pack multiple candidates into a single batched computation with masking to preserve per-candidate isolation, significantly improving hardware utilization. Combined with operator fusion across LayerNorm, residual, and activation layers, the model forward latency drops by $11 $ vs.\ naive implementation. Model-based retrieval interleaves scoring with graph search across beam-search iterations. When the index resides on CPU, each iteration incurs multiple round-trip host-device transfers that fragment the GPU computation graph. We migrate all retrieval operators onto GPU, unifying scoring and search into a single device-resident pipeline and reducing retrieval latency by 89\ However, co-locating the full index on GPU introduces memory pressure. We reduce GPU memory through inference engine sharing across requests and quantizing static index data, increasing serving concurrency by $3 $. The target compressor is architecturally decoupled from the Transformer and deployed as an independent sub-graph. During full deployment, this sub-graph computes representations for all targets to build the retrieval index. To maintain freshness, an event-driven near-line service monitors target feature mutations and invokes the same sub-graph to recompute only the affected representations, propagating updates to the live index asynchronously. We focus on the following research questions: : How does TransRetrieval compare to state-of-the-art baselines under similar computational budgets? : Does TransRetrieval exhibit predictable scaling behavior as model capacity increases? : How does each of the three design pillars contribute to performance? : Does TransRetrieval deliver significant improvements in real-world deployment? {5pt} {lcccccccrrrr} {*}{Dataset} & {*}{\#Domains} & {*}{\#Users} & {*}{\#Targets} & {*}{\#Interactions} & {*}{\#f\_user} & {*}{\#f\_target} & {*}{Len\_seq} & {c}{Domain Distribution (\ & & & & & & & & Dom A/0 & Dom B/1 & Dom C/2 & Dom D/3 \\ Industrial & 4 & 250M & 52M & 40B & 75 & 87 & 100 & 32\ KuaiRand & 4 & 24,943 & 536,491 & 152M & 37 & 63 & 100 & 11.7\ We conduct experiments on two multi-domain datasets from two different recommender system cases: display advertisement and short video recommendation. . A large-scale industrial dataset sampled from the production environment of the Alibaba display advertising platform, containing $40$ billion interactions over a corpus of $52$\,M advertised items. The dataset covers 4 distinct business domains , capturing diverse user–item interaction patterns. It includes rich user behavioral histories, detailed item attributes, and real-time contextual signals. ~. A publicly available sequential recommendation dataset from Kuaishou App, featuring 12 types of user feedback signals and comprehensive interaction histories. Following previous retrieval works~, we filter the dataset with a frequency threshold of 1000 click records for users and 100 exposure records for items. We use the first 4 domains in our experiments. Table~ presents dataset statistics. Both datasets cover four domains with diverse user behaviors. In Industrial, Domain C aggregates multiple smaller sub-domains with heterogeneous user behavior patterns, resulting in significant internal distributional variance. KuaiRand includes an extremely sparse domain , which provides a challenging testbed for generalization under data scarcity. Additionally, the two datasets differ in numbers of attributes, which contributes to the different computational costs observed in subsequent experiments. For both datasets, we adopt a temporal split strategy to ensure no data leakage occurs, simulating realistic deployment domains where models are trained on historical data and evaluated on future interactions. We focus on Recall@2000 , the proportion of ground-truth positive items appearing in the top-2000 retrieved candidates. We include the following baselines: ~. The bidding-aware model-based retrieval system serving the Alibaba display advertising platform before this work: a highly optimized industrial standard, and the system TransRetrieval replaces in our online A/B test. ~. A simple two-tower retrieval baseline that encodes the user side with a Transformer and scores candidates via inner-product search. ~. Flattens all features into a single behavioral sequence to achieve token homogeneity, discarding structural feature distinctions. ~. A scalable ranking architecture adapted for retrieval; replaces attention with parameter-free token mixing and concentrates computation in per-token FFNs. For fair comparison, we implement all baselines within our framework, sharing the same preprocessing pipeline. Each baseline retains its original feature processing design. All experiments are conducted on NVIDIA H20 GPU clusters using PyTorch and RecIS~. All models and both datasets share one tuned hyperparameter set: AdamW, learning rate $13$, weight decay $35$, and the pairwise learning-to-rank loss of the production baseline~. Negative sampling ratios are dataset-specific: $5$ negatives per positive for Industrial and $200$ for KuaiRand, reflecting training data scale: the much larger Industrial volume yields sufficient negative signal at a low ratio, while KuaiRand requires a higher ratio to compensate for limited positives. All runs train to convergence, and the final checkpoint is evaluated. is the per-domain Recall weighted by each domain's share of evaluation interactions. Among baselines and TransRetrieval-128D5L, best results are in , second-best underlined. } {3pt} {lcccccl|cccccl} {*}{} & {c|}{} & {c}{} \\ {2-7} {8-13} & Domain A & Domain B & Domain C & Domain D & Overall & MFLOPs$^{'}$ & Tab 0 & Tab 1 & Tab 2 & Tab 3 & Overall & MFLOPs$^{'}$ \\ KuaiFormer-128D5L & 0.496 & 0.466 & 0.470 & 0.548 & 0.495 & 0.011 & 0.371 & & 0.379 & 0.463 & & 0.012 \\ Production Baseline & & & & 0.608 & & 0.69 & 0.401 & 0.378 & 0.299 & 0.500 & 0.378 & 0.64 \\ HSTU-128D5L & 0.518 & 0.490 & 0.474 & & 0.527 & 1.72 & 0.315 & 0.394 & 0.312 & & 0.382 & 1.72 \\ RankMixer-128D8T & 0.445 & 0.419 & 0.437 & 0.512 & 0.452 & 2.10 & & 0.357 & & 0.518 & 0.364 & 2.10 \\ {1-13} TransRetrieval-64D3L & 0.593 & 0.590 & 0.575 & 0.663 & 0.603 & 0.45 & 0.460 & 0.413 & 0.413 & 0.481 & 0.419 & 0.42 \\ TransRetrieval-128D5L & & & & & & 1.91 & & & & & & 1.81 \\ We compare the overall retrieval performance under similar backbone configurations across both datasets and all domains. For model configurations, we use the notation $xDyL$ where $x$ denotes the embedding dimension and $y$ the number of Transformer layers. Table~ presents results comparing TransRetrieval against the production baseline, HSTU , KuaiFormer , and RankMixer . MFLOPs$^{'}$ measures the per-candidate compute on the target side . Under a comparable per-candidate budget, TransRetrieval-64D3L already surpasses the highly optimized Production Baseline across all domains, achieving 0.603 vs.\ 0.576 Overall Recall@2000 with 35\ Scaling up to TransRetrieval-128D5L further lifts performance to 0.657, a gain of +5.4\,pt over the smaller configuration, demonstrating that additional compute translates reliably into retrieval quality. Against the scalable baselines HSTU and RankMixer at similar per-candidate budgets , TransRetrieval-128D5L sets a new state-of-the-art, and even TransRetrieval-64D3L surpasses both at approximately one quarter of their cost. KuaiFormer trades expressiveness for amortized cost; its scaling behavior is examined in Sec.~. TransRetrieval unlocks predictable scaling for retrieval models. We sweep embedding dimensions and layer counts , and plot Recall@2000 against per-target FLOPs in Figure~. Both datasets exhibit clean log-linear scaling: Recall@2000 improves from 0.464 to 0.657 on Industrial and from 0.251 to 0.473 on KuaiRand as the backbone grows from 32D-1L to 128D-5L, with high $R^2$ confirming the trend is consistent and predictable. {Figs/transretrieval_scaling_with_fit.pdf} )$ configuration; shaded regions are 95\ Table~ reports detailed results across model sizes. Most strikingly, our 128D-5L outperforms RankMixer-768D-16T by 12.6\,pt on Industrial while using nearly two orders of magnitude less compute . KuaiFormer shows near-flat scaling , confirming that the two-tower inner-product bottleneck cannot be overcome by enlarging the encoder alone. $ denotes per-candidate scoring cost for each dataset. } {2pt} {crrrrr} & & {c}{} & {c}{} \\ {3-4} {5-6} & & & $} & & $} \\ {*}{KuaiFormer } & 64D5L & 0.485 & 0.003 & 0.357 & 0.004 \\ & 128D5L & 0.495 & 0.011 & 0.409 & 0.012 \\ {*}{HSTU} & 32D1L & 0.380 & 0.13 & 0.269 & 0.13 \\ & 64D2L & 0.437 & 0.30 & 0.298 & 0.30 \\ & 128D3L & 0.509 & 1.09 & 0.328 & 1.09 \\ & 128D5L & 0.527 & 1.72 & 0.382 & 1.72 \\ {*}{RankMixer} & 128D8T & 0.452 & 2.10 & 0.364 & 2.10 \\ & 768D8T & 0.468 & 75.50 & 0.420 & 75.50 \\ & 768D16T & 0.531 & 150.99 & 0.453 & 150.99 \\ {*}{TransRetrieval} & 32D1L & 0.464 & 0.14 & 0.251 & 0.13 \\ & 64D3L & 0.603 & 0.45 & 0.419 & 0.42 \\ & 128D5L & 0.657 & 1.91 & 0.473 & 1.81 \\ $ as the per-user encoding cost amortized over the ${ }$40k / ${ }$28k candidates actually scored during HNSW graph traversal.} {2pt} {lccccc} & & & & & \\ Weighted Average & & & & & \\ Weighted Sum & 0.600 & 0.594 & 0.572 & 0.674 & 0.608 \\ LN + Weighted Sum & 0.582 & 0.575 & 0.568 & 0.656 & 0.593 \\ Max Pooling & 0.563 & 0.554 & 0.533 & 0.644 & 0.571 \\ We compare our weighted average aggregation against alternative feature fusion strategies in Table~, all using the same 64D-5L configuration. As shown in Table~, the weighted average consistently outperforms all alternatives across every domain. Plain Weighted Sum lags behind due to unbounded norm growth as the number of features increases. Adding LayerNorm after weighted summation also hurts substantially , because its centering and rescaling erase the per-field magnitude signal that the Transformer relies on to distinguish heterogeneous fields. Max Pooling suffers the largest degradation , because its extreme-value selection discards the majority of feature information. {3pt} {lccc} & & & $} \\ 64D-3L & 8 tokens & 0.624 & 3.04 \\ 64D-3L & 4 tokens & 0.615 & 1.55 \\ 64D-3L & 1 token & 0.603 & 0.45 \\ 128D-5L & 1 token & & 1.91 \\ Table~ presents results under the 64D-3L and 128D-5L configurations. Within 64D-3L, compressing 8 tokens to 1 cuts MFLOPs$^{'}$ by ${ }85\ Reinvesting the freed budget into a 128D-5L backbone with 1-token compression yields strictly better operating points: lower cost than 64D-3L at 8 tokens and higher Recall , confirming that compression-then-scale-up dominates more-tokens-with-smaller-backbone. {3pt} {lcccccc} & & & & & & \\ {l}{} \\ Single Domain Data & 0.591 & 0.539 & 0.537 & 0.647 & 0.579 & 41.3 \\ {l}{} \\ 0 Domain Tokens & 0.254 & 0.276 & 0.251 & 0.289 & 0.265 & 41.3 \\ 1 Domain Token & 0.321 & 0.349 & 0.339 & 0.345 & 0.337 & 41.8 \\ 20 Domain Tokens & 0.431 & 0.451 & 0.460 & 0.473 & 0.452 & 49.8 \\ TransRetrieval & & & & & & 41.3 \\ Effective multi-domain modeling necessitates capturing domain context under latency constraints. Table evaluates different strategies using the 64D-3L configuration, reporting user-side FLOPs to explicitly quantify the overhead introduced by sequence expansion. The results show that simply mixing multi-domain data without any domain signal leads to severe performance degradation. Adding a single domain token provides limited improvement but incurs extra computation. Repeating the domain token 20 times further amplifies the domain signal and improves performance, but at a significantly higher computational cost , which is prohibitive for large-scale deployment. Our proposed method, injecting domain information as a position-style embedding, achieves the best overall performance , while maintaining the exact same inference cost as the 0 Domain Tokens baseline. Additionally, the row further validates our multi-domain strategy: training on a single domain's data alone yields noticeably lower Recall than our unified model, confirming that cross-domain data sharing is a net positive. {2pt} {lccccccc} & {c}{} & {c}{} \\ {2-5} {6-8} & A & B & C & D & Lift & 95\ Revenue & 1.72\ RPM & 1.22\ A month-long online A/B test was conducted within the Alibaba display advertising system on 5\ TransRetrieval-128D5L serves at 230 QPS with P99 latency under 40\,ms, identical to the production baseline, so the gains come at no end-to-end latency cost. The lower per-engine QPS reflects the higher per-target computational cost and is absorbed by horizontal scaling. As shown in Table~, TransRetrieval achieved a significant $2.53\ : the scaling breakthrough came not from architectural novelty but from a parameter-free one-line fix that restored homogeneous token norms. : compressing 8 target tokens to 1 frees 85\ : beyond avoiding per-domain cost multiplication, position-style domain embeddings let sparse domains benefit from cross-domain transfer rather than starving in isolation. Together, these three designs unlock scaling laws for the retrieval stage: on a 40-billion-interaction industrial dataset and on the public KuaiRand benchmark, TransRetrieval delivers log-linear scaling , state-of-the-art recall, and a 2.53\ This work was supported by National Natural Science Foundation of China , Public Computing Cloud, Renmin University of China, the fund for building world-class universities of Renmin University of China, Alibaba Group through the Alibaba Innovative Research Program, and the Qiushi Academic Project of Renmin University of China . We sincerely appreciate Cheng Chen, Fei Wang, Houyuan Xiang, Huimin Yi, Jianan Chen, Jiawen Liao, Junchen Dong, Mingjie Liu, Rui Wang, Wei He, Wenchao Wang, Xingyu Wen, Yan Jiang, Yan Zhang, Yunlong Xu, Zhengxiong Zhou, Zhengyu Liu, Zhensong Yan, and Zhenyuan Lai for implementing the key components of the data, training, and inference infrastructure. *{GenAI Usage Disclosure} During the preparation of this manuscript, the authors used generative AI tools for text polishing and language editing. The authors reviewed and edited all AI-generated suggestions and take full responsibility for the content of this publication. We derive the approximate FLOPs formula used in Section~. The derivation proceeds in two steps: we first establish the per-layer FLOPs of a standard Transformer, then adapt it to the asymmetric structure of TransRetrieval where user-side KV caches are precomputed and reused. *{Standard Transformer Layer} Consider a single Transformer layer operating on a sequence of $L$ tokens with embedding dimension $D$ and batch size $B$. We ignore bias terms and normalization operations. The computation consists of three components: There are four $D D$ projection matrices. Each projection involves a matrix multiplication of shape $ $, costing $2BLD^2$ FLOPs. The total for all four projections is therefore $8BLD^2$. Computing $QK^ $ involves a multiplication of shape $ $, costing $2BL^2D$ FLOPs. The subsequent weighted aggregation over $V$ has the same cost. The total attention cost is thus $4BL^2D$. The FFN consists of two linear layers with a hidden dimension of $2D$, yielding a total cost of $8BLD^2$. This choice reflects computational efficiency considerations, where the hidden dimension is typically smaller than the $4D$ commonly used in language modeling. Summing the three components, the total FLOPs for a single standard Transformer layer is: _{} = B . *{Target Decoder in TransRetrieval} The key innovation in TransRetrieval lies in its asymmetric computation pattern, which leverages precomputed user-side KV caches to dramatically reduce per-candidate scoring cost. In TransRetrieval, the user-side tokens are encoded once and their KV cache is retained. When scoring a target candidate consisting of $L_{}$ tokens, only the target tokens require new computation. The total sequence length visible to attention is $L = L_{} + L_{} $. We now revisit each component under this setting: Since the user-side K and V have already been computed and cached, only the $L_{} $ target tokens require Q, K, V, and output projections. The cost is therefore $8BL_{} D^2$. Each of the $L_{} $ target tokens attends to the full sequence of length $L = L_{} + L_{} $. The cost of $QK^ $ is $2BL_{} LD$ and the weighted aggregation over $V$ costs the same, giving a total of $4BL_{} LD$. Only the target tokens pass through the FFN, costing $8BL_{} D^2$. Summing over a single layer and then over $N_{}$ layers, the total FLOPs for scoring all $N_{}$ target candidates is: _ = N_{} N_{} B . In our setting, $L_{} L_{}$, so $L = L_{} + L_{} L_{}$. Factoring out $4D L_{} $ and setting $B = 1$ , we obtain the approximation used in the main text: 4D N_{} L_{} N_{}, This expression makes explicit that FLOPs scale linearly with $L_{} $, which is the key insight motivating target token compression. *{Baseline FLOPs Calculations} For fair comparison, we compute FLOPs for baseline methods using consistent methodology: follows the same computational pattern as TransRetrieval but with a critical difference: it only processes user behavioral sequences, discarding all static profile features. Thus, its user-side sequence length is $L_{} = L_{}$ , while TransRetrieval includes both sequence and profile features . The FLOPs calculation uses the same formula as TransRetrieval but with the reduced user sequence length. replaces self-attention with multi-head token mixing and uses per-token FFNs. According to the RankMixer paper~, for a model with $T$ tokens, embedding dimension $D$, $L$ layers, and FFN expansion ratio $k$, the FLOPs per sample is approximately $4kLTD^2$. In our implementation, we use an FFN hidden dimension of $2D$ and $L=2$ layers, consistent with our TransRetrieval FFN configuration. The total FLOPs scales linearly with the number of tokens $T$, which includes both user and target features processed together in RankMixer's symmetric architecture. ~ is a user-only encoder: it processes only the user behavioral sequence and outputs multiple query tokens for approximate nearest-neighbor retrieval, with no per-candidate target-side computation. Its architecture follows Llama-style Transformers and introduces an mechanism that partitions a length-$n$ sequence into three segments and compresses them via single-layer bidirectional Transformers followed by mean pooling. Concretely, for a 256-length sequence, the early segment is compressed into 2 tokens , the middle segment into 5 tokens , and the latest segment is kept intact as 48 tokens, yielding an effective sequence length of $L_{} = 55$. The FLOPs computation has two parts: Each compression group of size $g$ is processed by a single-layer bidirectional Transformer. The per-group cost is $16gD^2 + 4g^2D$ . For the early segment with 2 groups of $g{=}64$, the cost is $2 $. For the middle segment with 5 groups of $g{=}16$, the cost is $5 $. The total compression cost is: _{} = 3328\,D^2 + 37{,}888\,D. The compressed sequence of $L_{} = 55$ tokens passes through $N_{}$ causal Transformer layers. Using the standard layer formula with FFN hidden dimension $4D$ , each layer costs $24 L_{} D^2 + 4 L_{}^2 D$. Over $N_{}$ layers: _{} = N_{} L_{} . The total per-user FLOPs is $_{} + _{}$. Since KuaiFormer performs no per-candidate computation , this cost is amortized across all candidates and is thus reported separately from the per-candidate MFLOPs$^{'}$ used for TransRetrieval and other model-based methods. 