 {article} {acl} {fontenc} {inputenc} \\ Ahmed El-Roby \\ Carleton University\\ ON, Canada\\ \\} Cold-start cross-domain recommender systems predict a user’s preferences in a target domain using only their source-domain behavior, yet existing CDR models either map opaque embeddings or rely on post-hoc or LLM-generated rationales that are hard to audit. We introduce , a lightweight CDR framework whose predictions are explained by construction with evidence-cited, faithful rationales. EviSnap distills noisy reviews into compact facet cards using an LLM offline, pairing each facet with verbatim supporting sentences. It then induces a shared, domain-agnostic concept bank by clustering facet embeddings and computes user-positive, user-negative, and item-presence concept activations via evidence-weighted pooling. A single linear concept-to-concept map transfers users across domains, and a linear scoring head yields per-concept additive contributions, enabling exact score decompositions and counterfactual 'what-if' edits grounded in the cited sentences. Experiments on the Amazon Reviews dataset across six transfers among Books, Movies, and Music show that EviSnap consistently outperforms strong mapping and review-text baselines while passing deletion- and sufficiency-based tests for explanation faithfulness. Real-world recommender systems frequently face cold-start users: people with interaction history in one domain but none in a target domain . Cold-start cross-domain recommendation tackles this by transferring preferences from a source domain to a target domain . However, most CDR systems remain hard to audit. Mapping-based methods learn a transfer function between latent user embeddings and scale well , but the transferred signal is opaque: the model cannot clearly state what preferences were moved or why a target item is recommended. Review-aware models often improve accuracy by encoding text , yet their explanations are typically post-hoc and may not reflect the actual scoring function . More recently, LLM-based recommenders can generate fluent justifications , but these can be costly at inference and are not guaranteed to be faithful or verifiable . We argue that CDR needs an intermediate representation that is shared across domains, grounded in verifiable evidence, and used directly by the predictor so that explanations can be tested rather than narrated . To this end, we propose , a lightweight CDR framework that produces faithful, evidence-cited explanations by construction. EviSnap operates in an evidence-grounded concept space built from reviews: an LLM distills raw reviews into compact facet cards and attaches verbatim supporting sentences. We embed facets from both domains and cluster them into a shared concept bank. For each user we compute separate positive and negative concept activations from praised vs.\ criticized evidence, and for each target item we compute concept presence activations from its evidence sentences. A single linear concept-to-concept map transfers users from the source concept space to the target space, and an additive linear scoring head produces ratings. Because the score is a sum of per-concept terms, EviSnap yields an exact score decomposition: the explanation is the model itself, paired with the highest-scoring user/item evidence sentences for each surfaced concept. The linear structure also enables transparent counterfactual edits with predictable changes in the score. Our main contributions are: We introduce an offline facet-card pipeline with verbatim evidence and induce a shared concept bank across domains, yielding sentence-traceable user-positive, user-negative, and item-presence activations. EviSnap uses a single linear concept mapping and an additive linear scorer, so reported per-concept contributions reconstruct the prediction exactly. Experiments over the Amazon Reviews dataset among , , and , EviSnap outperforms strong mapping and text-based baselines while passing deletion- and sufficiency-based tests of explanation faithfulness . We study for across two domains: a source domain $S$ and a target domain $T$. Let $$ be the user set and $_S$, $_T$ the item sets in $S$ and $T$. We observe ratings $r_{ui}$ for some user--item pairs, and we use review text as side information: $_S $ denotes the set of source-domain reviews written by user $u$, and $_T $ denotes the set of target-domain reviews written about item $i$. We adopt a : users in training and test are disjoint. At inference time for a test user $u$, we assume no target-domain history is available, i.e., only $_S $ is observed for the user, while item-side text $_T $ is available for target items. Our goal is to learn a predictor $g_{ }$ that estimates a cold-start user's preference for a target item: \[ r_{ui} = g_{ }\! , _T ), \] where $u ,\; i _T$. {figs/overview.jpg} $, additive scoring with exact, evidence-cited per-concept contributions.} Given the cold-start CDR setting in Section~, we instantiate $g_{ }$ with an interpretable, evidence-grounded model that predicts target-domain ratings using a shared concept space as shown in Figure~. EviSnap distills reviews into evidence-cited facet cards, induces a shared concept bank and computes user/item concept activations, and applies a simple linear transfer and linear scoring head. Because the final scorer is additive in concept features, the same quantities that produce $ r_{ui}$ also yield faithful, sentence-grounded explanations. In this section, we describe each module and its training objective in turn. We first preprocess review text into compact, auditable facet cards. For each source-domain user $u$ and each target-domain item $i$, we input the corresponding bundle of reviews to an LLM and obtain a single JSON object . Each card contains a small set of short, domain-agnostic facet phrases paired with verbatim supporting sentences from the input reviews. Facets are accompanied by a support count. User cards include facet polarity , while item cards use polarity $0$ . We run this extraction offline once and treat facet phrases and evidence sentences as fixed inputs to EviSnap. The LLM is not used during model training or inference. This representation denoises long reviews while preserving traceability: every downstream concept activation can cite an original sentence. User facets may include and with copied evidence sentences; an item may include with evidence such as ``The live drums give the songs amazing energy.'' {3pt} {@{}p{0.32 }p{0.32 }p{0.32 }@{}} [ colback=gray!3, colframe=black!40, title={ System Prompt: Facet Extractor}, fonttitle= , width= , boxsep=3pt, left=3pt,right=3pt,top=4pt,bottom=4pt, equal height group=facetprompts ] You are an information extractor for the Amazon Reviews 2014 dataset. Your task is to turn a bundle of reviews into a small set of domain-agnostic ``facets'' with verbatim evidence sentences copied from the input. Output JSON only, with keys:\\ , "facets": [}\\ \\ ]\} ]\}.} No chain-of-thought. Do not explain your reasoning. Facet: $ $4 words, lower-case, domain-agnostic. Avoid named entities, brands, plot spoilers, shipping/seller issues. Evidence sentences must be copied verbatim from the input; do not paraphrase. Merge duplicates/synonyms; prefer facets supported by multiple reviews. Polarity: users = +1 like / $-1$ avoid; items = 0 . & [ colback=gray!3, colframe=black!40, title={ User Prompt Template}, fonttitle= , width= , boxsep=3pt, left=3pt,right=3pt,top=4pt,bottom=4pt, equal height group=facetprompts ] You will receive a JSONL bundle of reviews for a single user from the Amazon 2014 dataset . Goal: extract 6--10 concise preference facets with polarity . Use only the provided text. Output a single JSON object. \\ \\ Fill as:\\ { \{"mode":"user", "entity\_id":"\{reviewer\_id\}", "domain":"\{source\_domain\}"\} } \\ \\ { INPUT \\ \{jsonl\_bundle\}\\ INPUT END } \\ \\ Produce ONLY the JSON object. & [ colback=gray!3, colframe=black!40, title={ Item Prompt Template}, fonttitle= , width= , boxsep=3pt, left=3pt,right=3pt,top=4pt,bottom=4pt, equal height group=facetprompts ] You will receive a JSONL bundle of reviews for a single item from the Amazon 2014 dataset . Goal: extract 6--12 item facets . Use only the provided text. Output a single JSON object. \\ \\ Fill as: \\ { \{"mode":"item", \\ "entity\_id":"\{asin\}", "domain":"\{target\_domain\}"\} } \\ \\ { INPUT \\ \{jsonl\_bundle\}\\ INPUT END } \\ \\ Produce ONLY the JSON object. \\ This stage builds a shared, interpretable feature space that supports both cross-domain transfer and faithful explanations. We want the model’s internal variables to be domain-agnostic so they align across $S$ and $T$, evidence-grounded so each activation can be traced to specific sentences, and simple enough that the final scoring function can decompose cleanly into per-concept contributions. We achieve this by inducing a concept bank from facet phrases and computing user/item concept activations by pooling sentence-to-concept evidence scores. We induce concepts by clustering facet phrases so that synonymous facets collapse into a single dimension and the same concept labels can be reused across domains. We embed every distinct facet phrase once using a frozen sentence encoder $f $: \[ = f ^d \] We then cluster all facet embeddings with $k$--means into $K$ clusters. The cluster centroids are our \[ = _1^ \\ \\ _K^ ^{K d}, \|_k\|_2 = 1 \;k \] Each concept $k$ is labeled by the facet phrase in its cluster whose embedding is closest to $_k$. These labels appear in our explanations. We score concepts at the sentence level so that explanations can cite the single strongest supporting sentence (while still accounting for multiple mentions). For any entity $e$ , we have a small set of evidence sentences \[ = \{ \}_{j=1}^{N_e}, \] where $s_j$ is an evidence sentence and $w_j$ is a non--negative weight. We use \[ w_j = ) \] so that facets mentioned in more reviews carry slightly more weight. We embed each sentence, \[ _j = f ^d, \|_j\|_2 = 1, \] and compute its alignment with each concept prototype $_k$ using cosine similarity followed by ReLU: \[ _{jk} = \] Here $ _{jk} $ measures how strongly sentence $s_j$ supports concept $k$. Negative or unrelated evidence is clipped to $0$. This pooling choice lets us produce explanations by pointing to the highest-scoring evidence sentence for each concept, while still rewarding concepts supported by multiple reviews. To obtain a fixed-length concept vector from a variable number of evidence sentences, we aggregate sentence-level alignments into one score per concept. We use weighted log--sum--exp pooling as a smooth max that highlights the strongest supporting sentence without discarding additional supporting evidence: \[ s_k = { } _j _j \, \! , _j = { _j w_j}, \] where $ >0$ is a temperature. When $ $ is large, this behaves like a soft maximum over sentences. We represent preference polarity explicitly for users because it affects whether a concept should increase or decrease a target-item score, whereas items only express presence of properties. For users, facet cards differentiate positive and negative opinions. We pool them separately: $^+ ^K$ from positive evidence sentences , $^- ^K$ from negative evidence sentences . We then form a source--domain user vector: \[ _S = ^+ - ^- \] Each entry is high and positive if the user repeatedly praises that concept, and negative if they repeatedly criticize it. For items in the target domain, facets describe item attributes rather than preferences. Therefore item facets have no polarity and we pool all item evidence sentences into a nonnegative concept vector: \[ ^K_{ 0}, \] which encodes which concepts the item offers and how strongly. Cold-start users have no history in the target domain, so we must transfer their preferences from $S$ into a target-domain concept representation that can be compared with target items. We choose a single linear map for transparency: its weights directly show which source concepts contribute to which target concepts, making the transfer step itself inspectable rather than a black-box embedding shift. We now map each user's source domain concept vector to a target domain vector. We adopt a simple linear map \[ _T = \,_S , ^{K K} \] Matrix $$ is learned from data and initialized to the identity. Intuitively, $$ says how source concepts translate into target concepts . To keep $$ easy to interpret and to avoid overfitting in the cold--start setting, we regularize it toward the identity: \[ \| - \|_F^2, \] where $$ is the $K K$ identity matrix and $\| \|_F$ denotes the Frobenius norm. Given mapped user concepts $_T $ and item concepts $ $, we need a scoring function that is accurate enough to model user-item matching, but also additively decomposable so explanations can be faithful. We therefore use a linear head over per-concept features: an interaction term captures match , while marginal terms capture user and item specific tendencies. This keeps inference lightweight and ensures each concept’s effect on the score can be computed exactly. We construct a feature vector using an element wise interaction and optional marginal terms: ^{ } &= _T \;\; ^K,\\ ^{ } &= _T ^K,\\ ^{ } &= ^K, where $ $ denotes element--wise multiplication. We then concatenate them: \[ = , \] where $ ^{3K}$. A single linear layer computes a rating score: \[ y_c = ^ + b_i, \] where $ ^{3K}$ are the head weights and $b_i $ is an item bias parameter . The final predicted rating adds back the target--domain mean rating $ _T$: \[ r_{ui} = _T + y_c \] In practice, we compute the mean rating $ _T$ over the training portion of the target domain and train on labels $r_{ui} - _T$. At evaluation time, we clamp $ r_{ui}$ to the valid rating range . The head weights $$ can be seen as three blocks: \[ = , \] each of length $K$. The centered score can then be written as a sum over concepts: \[ y_c &= _{k=1}^K ( w^{ }_k\,a_{T,k} \,b_k + w^{ }_k\,a_{T,k} \\ & +\, w^{ }_k\,b_k ) + b_i \] For explanation, we define the per--concept contribution \[ _k = w^{ }_k\,a_{T,k} \,b_k \\ + \ w^{ }_k\,a_{T,k} + w^{ }_k\,b_k , \] and we display the top few positive and negative $_k$ together with the concept label and the best--matching user and item evidence sentences. Crucially, the explanation is : there is no separate explanation module---$_k $ is defined to be exactly the $k$-th additive term in the model's scoring function. Therefore the centered score decomposes exactly as \[ y_c = _k _k + b_i, \] so the reported contributions reconstruct the predicted rating exactly. We train only the transfer and scoring parameters: the cross-domain map $$, the linear head weights $$, and item biases $b_i$, so that predicted target-domain ratings fit observed ratings while keeping transfer interpretable and stable. We center ratings to factor out the global target-domain mean, and we apply light regularization to keep $$ near-identity to reduce overfitting and preserve concept semantics, and prevent item biases from dominating the explanation. Let $_{} _T$ denote the training set of user--item pairs in the target domain, with observed ratings $r_{ui}$. We use a user--level cold--start split: users in training and test do not overlap. We first compute the mean target--domain rating over training data, \[ _T = {|_{}|} _{ _{}} r_{ui} \] We then train the model to predict the rating $r_{ui} - _T$. The main loss term is mean squared error: \[ _{} = {|_{}|} _{ _{}} - )^2 \] We add light regularization to stabilize the mapping and keep biases small: \[ _{} = _M \| - \|_F^2 + _b _i b_i^2, \] where $ _M$ and $ _b$ are small hyperparameters. The final training objective is \[ = _{} + _{} \] We optimize $$ with mini--batch stochastic gradient descent . Because all operations are linear in the learnable parameters , training is stable and efficient. We evaluate EviSnap in the cold-start cross-domain recommendation setting on review-driven transfers between , , and . This section describes the dataset construction and evaluation scenarios, and then introduces the baselines used for comparison. and , respectively.} {!}{ {llcccccc} & & \\} & \\} & \\} & \\} & \\} & \\} \\ {2}{*}{EMCDR} & MAE & 1.2894 & 0.9701 & 1.1073 & 0.9834 & 0.9860 & 1.1730 \\ & RMSE & 1.5811 & 1.2461 & 1.3679 & 1.2427 & 1.2856 & 1.4954 \\ {2}{*}{PTUPCDR} & MAE & 1.0473 & 0.9453 & 0.9384 & 0.9278 & 0.9642 & 1.0809 \\ & RMSE & 1.3794 & 1.2326 & 1.2562 & 1.2073 & 1.2760 & 1.4276 \\ {2}{*}{MACDR} & MAE & 0.8397 & 0.8987 & 0.8757 & 0.8790 & 0.9038 & 0.8579 \\ & RMSE & 1.1042 & 1.1388 & 1.1356 & 1.1376 & 1.1564 & 1.0921 \\ {2}{*}{HeroGraph} & MAE & 0.8150 & 0.8610 & & 0.8670 & & 0.8860 \\ & RMSE & & 1.1180 & 1.1010 & 1.1330 & & 1.1210 \\ {2}{*}{DeepCoNN$+$} & MAE & & & 0.8413 & & 0.9254 & \\ & RMSE & 1.0514 & & & & 1.1777 & \\ {2}{*}{} & MAE & & & & & & \\ & RMSE & & & & & & \\ } We use the Amazon Reviews 2014 dataset~ and consider the , , and domains. Following common practice in cross-domain recommendation, we evaluate all six directed transfers $S\! \!T$ among these domains. For each $S\! \!T$, we restrict to overlapping users who have source-domain text $_S $ and at least one observed rating on a target-domain item. We then create a user-level cold-start split by randomly assigning 80\ To prevent leakage through item-side text, when constructing $_T $ we exclude all target-domain reviews authored by held-out users, so the model never observes target-domain review text from test users. We use precomputed facet cards for source-domain users and target-domain items, constructed per $S\! \!T$ task using the split and leakage prevention protocol in Section~, and treat them as fixed inputs . Facet phrases and evidence sentences are embedded with a frozen sentence encoder, clustered with $k$-means to form a shared $K$-concept bank, and pooled into user/item concept activations via evidence-weighted log-sum-exp pooling over ReLU cosine similarities. We train only the linear concept map $$ and the additive linear head by minimizing MSE on centered target-domain ratings, regularizing $$ toward identity and applying a small $ _2$ penalty on item biases. We use a single hyperparameter setting for all transfers and do not perform hyperparameter tuning. We ran each scenario 5 times and took the average. We compare against five cross-domain recommendation baselines that cover both mapping-based transfer and review-text models: ~: learns latent representations in each domain and trains a mapping function to transfer user representations from $S$ to $T$. ~: It uses a meta-network to generate personalized transfer functions for different users. ~: It employs a prototype-enhanced mixture-of-experts transfer mechanism and distribution alignment to improve robustness under sparse supervision. ~: It is a text-based recommender that models users and items from review text; we use its cross-domain variant with an added mapping layer for transfer. ~: It obtains cross-domain information by a shared graph, which is constructed by collecting users' and items' information from multiple domains. {figs/faithfulness_music_to_movies.pdf} $ $. positive deletion vs.\ random, negative deletion vs.\ random, sufficiency , contribution mass.} Table~ reports MAE/RMSE on six directed transfers among , , and under the user-level cold-start split. EviSnap achieves the best performance on five of six transfer directions, and is second-best on the remaining $ $ setting, where HeroGraph is strongest . Averaged over transfers, EviSnap improves over the strongest review-text baseline DeepCoNN+ from $0.845$ to $0.818$ MAE and from $1.085$ to $1.055$ RMSE (relative $3.3\ The largest improvements occur on $ $, where EviSnap reduces MAE from $0.8413$ to $0.7768$ and RMSE from $1.0953$ to $1.0438$. Overall, these results suggest that transferring users through an evidence-grounded concept space is competitive for cold-start CDR while maintaining an additive structure that directly supports faithful, sentence-grounded explanations. Our scorer is designed to be both accurate and decomposable into per-concept effects. To isolate whether gains come primarily from or from user/item signals, we ablate which concept-feature blocks are fed into the linear head, while keeping the concept bank, pooling, mapping $$, item bias, and training objective fixed. Given mapped user concepts $_T ^K$ and item concepts $ ^K_{ 0}$, we define three blocks: \[ ^{ } =_T , \] \[ ^{ } =_T , \] \[ ^{ } = \] We then form the head input using binary switches $ _u, _i \{0,1\}$: \[ = \] This yields four variants: $ $, $ $, $ $, and $ $. {6pt} {@{}lccc cc@{}} {*}{} & {c}{ $}} & {*}{$ $} & {*}{$ $} \\ {2-4} & $^{ }$ & $^{ }$ & $^{ }$ & & \\ IntOnly & 1 & 0 & 0 & 0.9170 & 1.1522 \\ Int+User & 1 & 1 & 0 & 0.9155 & 1.1509 \\ Int+Item & 1 & 0 & 1 & & \\ Full & 1 & 1 & 1 & & \\ 128$). Best/second-best are in /.} On to , performs best, reducing error relative to by $0.0157$ MAE and $0.0054$ RMSE, indicating that marginal blocks provide additional signal beyond pure element-wise matching. Because $y_c $ is additive in concept terms , we can test faithfulness via direct concept-space interventions. On to , we report : ablate the top-$m$ positive/negative concepts ranked by $_k$ and measure the resulting score change, using random deletions as a control; and : keep only the top-$m$ concepts by $|_k|$ and compute the residual $|y_c-y_c^{ }|$. Figure~ shows that top-$m$ deletion perturbs predictions far more than random, while a small set of top concepts largely reconstructs $y_c$, supporting that the surfaced concepts are the model’s true drivers and that decisions are distributed across multiple facets. Table~ illustrates a cold-start to explanation for user and item . Each row corresponds to a concept $k$ in the shared concept bank. The reported score is the exact additive term $_k $ in our linear head, so positive values raise the predicted rating by that amount. For each concept, we cite the highest-alignment verbatim sentence from the user's reviews and the item’s reviews, making the transfer auditable at the sentence level. In this case, the prediction is supported by aligned evidence for , as well as complementary signals of and . For readability, we show only the largest-magnitude contributions. {3pt} {1.12} $ $ explanation} {@{}p{0.3 }p{0.66 }@{}} & \\ & \\ & \\ & \\ Cold-start cross-domain recommendation transfers a user’s signal from a source to a target domain, typically by learning domain-specific representations and a mapping/bridge function to cope with sparse supervision . Reviews provide fine-grained signals via aspect-style models and neural review encoders , but many “explanations” in review-aware recommenders are post-hoc and need not reflect the true scoring rule ; similarly, LLM-generated justifications can be fluent yet weakly coupled to the predictor and hard to audit . EviSnap follows interpretability-by-construction : it distills reviews into facet cards with verbatim evidence and predicts in an additive concept space with a linear transfer, yielding exact per-concept contributions that can be directly validated via deletion/sufficiency interventions . We propose EviSnap, a lightweight cold-start cross-domain recommender that transfers users through an evidence-grounded concept space derived from reviews. An offline LLM distills reviews into facet cards with verbatim evidence, and a shared concept bank with a linear map and additive head yields ratings with exact, evidence-cited per-concept contributions. Across six Amazon Reviews transfers among , , and , EviSnap outperforms SOTA baselines, and shows the faithfulness of the surfaced concepts transfers. EviSnap relies on an offline LLM step to distill reviews into facet cards. While evidence sentences are verbatim, the extracted facet phrases and user polarity labels may be sensitive to the specific LLM and prompts and may inherit noise or biases from the model and data. Our approach also assumes sufficient review text for both source-domain users and target-domain items. Performance and explanation quality may degrade in text-sparse settings where users/items have few or no reviews. Finally, interpretability is enabled by an unsupervised $k$-means concept bank and a linear mapping/additive head. Concept quality can depend on the encoder and the choice of $K$, and the linear form may miss higher-order interactions that could benefit some transfers. 