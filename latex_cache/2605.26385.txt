 We discuss additional related work in a broader context. In the broader context, the most commonly used approach for ranking and retrieval-augmented generation ~ has been accurately estimating the users' valuation on items from noisy user feedback, such as implicit click data ~. These regressed reward models are used to align items based on the predicted rewards, and many research papers have explored model architectures and user interactions models to accurately predict the users' valuation. A representative approach is LambdaMART~, which trains a boosting algorithm to regress the users' interaction signals. The Plackett-Luce model has also been invented for regressing the users' choice model, which calculates the probability that users select the target item among the items that appear in the final ranking~. However, these approaches require a careful debiasing mechanism to align items correctly from biased logged data~, and how to take the LSR choice into account has remained unclear~. Even more importantly, regression-based approaches train the ESR model with the objective of accurately estimating the users' interaction signal, leading to an objective mismatch with the goal of the reward~. Policy gradient approaches naturally resolve the above issue by deriving the gradient of the ESR model directly from the final objective of the expected reward. Our work makes PG more tractable and practically applicable in large-scale recommendation settings by tackling the variance and credit-assignment issues of two-stage PG. Subset selection methods applied in recommender systems have mainly focused on optimizing for utility, diversity and fairness. Earlier work proposing maximal marginal relevance~ and incorporating diversity as a key objective~ established the necessity of selecting a ``representative'' set of items rather than just the top-ranked ones to mitigate redundancy . Building on these, the field has increasingly moved toward framing subset selection as a sequential decision-making problem, often leveraging Reinforcement Learning and multi-armed bandit frameworks. Rather than treating selection as a static combinatorial task, contemporary approaches emphasize the need to balance exploration and exploitation in online ranking~ and to optimize recommendation policies directly from logged feedback using counterfactual techniques~. Within this narrative, and, more recently, focused on how individual item utilities aggregate into a collective reward that is optimized through the lens of maximizing expected order statistics. However, these works focus on the single-stage setting, and the credit-assignment issue in the two-stage ranking is an underexplored problem in the subset selection literature. The mixture-of-experts policy has been considered effective in sampling a diverse set of items and enhancing the efficiency of exploration in two-stage recommendation. For instance, empirically shows that using MoE can help improve the joint accessibility of two dissimilar items. shows that nominating candidate items using multiple models can accelerate the item exploration efficiency in two-stage bandit problems. This paper thus explores how to integrate the generalized family of PL policies with the MoE models, and observes that MoE further improves the performance compared to the single-logit model cases of both CA-PG and CA-PG-SwR. We first discuss the general form of the gradient, and then discuss how to combine the gradient with the Plackett-Luce policy w/ and w/o sampling with replacement approximations. Here, we provide the derivation steps of naive PG. For the derivation of credit assigned PG, please refer to Appendix~ . v_l &= \, _{p ^{ } } \\ &= \, _{p _{} _{}^{ } } \\ &= \, _{p _{} } \\ &= \, _{p _{} } \\ &= \, _{p } \\ &= _{p } \\ &= _{p } \\ &= _{p } \\ &= _{p _{} } .. \, \\ &= _{p _{} } \\ &= _{p _{} } \\ &= _{p _{} _{}^{ } } \\ &= _{p _{} _{}^{ } p } \\ & _{} _{} p } ) The last line is due to the independence of the reward $r_l$ from the other positions' actions $a_{l'}, l' l$. This means that the reward function is given as a simple form of $q = $. We also use $q^{ } = _{a_l } _{}^{ } q $ for a shorthand notation of the expected reward at position $l$ given the candidate set $^K$ and the LSR policy $ _{}$. V-PG-SwR and CA-PG-SwR use the sampling with replacement approximation of Plackett-Luce to calculate the score function in the computation of the PG. In contrast, the candidate selection process itself follows the original Plackett-Luce policy . That is, the original retrieval process recursively applies the softmax function on the actions: k, \, 1 k K, _k _{} }), _{} }) = _{}^{m } / ) }{ ^{ }}} } / )}, and $^{ }$ is the set of previously sampled actions in the candidate set. In contrast, we calculate the score function } the softmax function is recursively applied to actions, instead of actions, in the following way. k, \, 1 k K, _k _{} , _{} = _{}^{m } / ) }{ }} } / )}, Thus, when we compare V-PG and V-PG-SwR, the difference becomes as follows. V-PG: _{} &= }; \, m )} ) \\ &= _{k=1}^K _{} }; \, m ) V-PG-SwR: _{} &= )} ) \\ &= _{k=1}^K _{} ) Next, we derive CA-PG-SwR. By the definition of $S^K $, we have _{} | x) &= _{} \\ &= ) \\ &= ) ) \\ &= \,|\, x; \, m ) ) Notably, an important distinction between V-PG-SwR and CA-PG-SwR is the actions for which the action choice probabilities are computed. Specifically, V-PG requires evaluation of the probabilities for all actions in the candidate set , whereas CA-PG only includes evaluation of choice probabilities for the specific action selected by the LSR . This reflects the principled difference between these two approaches regarding credit-assignment. This section provides the proofs omitted from the main text. } We use $ ^{ } := _{} | x) ^{ } )$ to derive the policy gradient as follows. v_l &= \, _{p ^{ } } \\ &= \, _{p _{} | x) ^{ } )} \\ &= \, _{p } \\ &= _{p } \\ &= _{p } \\ & + _{p } Note that the decomposition in the last line is due to the derivative of the product, where the second term is non-vanishing since $ ^{ } )$ depends on $ _{}$. Then, &= _{p } \\ &= _{p } \\ &= _{p } \\ &= _{p } \\ &= _{p } \\ &= _{p ^{ } } \\ &= _{p ^{ } p } \\ & p } ) \\ & ) The last line follows from the independence of the rewards, i.e., $q = $ , l' l$). We also have, &= _{p } \\ &= _{p } \\ &= _{p } \\ &= _{p ^{ } } \\ &= _{p ^{ } } \\ &= _{p ^{ } } \\ &= _{p ^{ } } \\ &= _{p ^{ } } \\ &= _{p ^{ } } q ] \\ &= _{p ^{ } p } r_l ] \\ & p } r_l ] ) \\ & ) Again, the last line follows from the independence of the rewards, as used in the detailed derivation of Eq.~. } As a preparation, we first introduce the following lemma to discuss the expected rewards, which are optimized by each of V-PG and CA-PG. In expectation, V-PG and the CA-PG optimize the objective with the following expected rewards. & : \; _{ _{} )} q ^K $)} \\ & : _{ ^{ }_ } ^K$)} In the following, we denote the expected rewards of CA-PG and V-PG as $q^{ } )$ and $q^{ } $, respectively. We have already derived the expected reward of the vanilla PG in Appendix~ . Thus, we derive the expected reward of the credit-assigned PG as follows. v_l &= _{p ^{ } p } \\ &= _{p ^{ } } \\ &= _{p _{} | x) ^{ } )} \\ &= _{p _{} | x)} \\ &= _{p _{} | x)} [ _{} | x) \\& ) _{}^{ } )) ) q ] \\ &= _{p _{} | x)} q ] \\ &= _{p _{} | x)} Note that we use $ _{}^{ } ) = _{}^{ } $. The last line follows from the definition of $q^{ } )$. There are two key differences between the two expected rewards in Lemma~. First, V-PG considers the expected reward marginalized over the actions chosen by the LSR policy, i.e., $_{ ^{ }_ } $. In contrast, CA-PG considers an LSR-propensity discounted reward, where the marginalized LSR propensity $_{ _{} )} $ serves as a discount factor applied to the specific action reward. Second, while V-PG takes the expectation over all possible LSR actions to calculate the expected reward, CA-PG only considers the reward of the target action, $a_l$. This is because $^K $ has one-to-one correspondence to the final action of interest , while $^K$ is a new random variable that is conditionally independent of $a_l$. These differences contribute to the variance reduction of CA-PG, as analyzed in Section~. This suggests that while V-PG targets the marginal expected reward, CA-PG optimizes a well-defined surrogate reward that facilitates more efficient and robust policy improvement via gradient ascent. Next, using the above expected reward, we provide the proof of Theorem~ below. As we have discussed in the proof outline in the main text, we need the condition that the expected reward of credit-assigned PG should satisfy $q^{ } ) > q^{ } )$. From Lemma~, we should have: & \, q^{ } ) > q^{ } ) \\ & \, ^{ } ) q > ^{ } ) q \\ & \, )}{ ^{ } )} > {q } \\ & \, )}{ ^{ } )} < {q } where we denote $ ^{ } ) = _{ _{} )} $. This section discusses the computation of score function $ _{} )$ for both CA-PG and CA-PG-SwR, under the Plackett-Luce models. Note that, while we do not describe the details here, V-PG and V-PG-SwR also use a stable calculation of the loss function using the operation. { {c||c|c|c|c||c|c|c|c||c|c|c|c} {*}{} & {c||}{$ = 1$ } & {c||}{$ = 1/2$} & {c}{$ = 1/5$ } \\ & $K=10$ & $K=20$ & $K=50$ & $K=100$ & $K=10$ & $K=20$ & $K=50$ & $K=100$ & $K=10$ & $K=20$ & $K=50$ & $K=100$ \\ Relative abs. err. & 2.3e-3 & 9.2e-3 & 2.7e-2 & 5.7e-2 & 4.2e-3 & 1.4e-5 & 5.0e-4 & 4.2e-3 & 0.0 & 0.0 & 0.0 & 4.7e-6 \\ Mean logit gap & 3.20 & 2.78 & 2.28 & 1.89 & 6.40 & 5.55 & 4.57 & 3.78 & 16.0 & 13.9 & 11.4 & 9.45 \\ } 0.1in {9pt} The reported values represent the absolute approximation error relative to the ground-truth probability incurred when replacing $^{k-1}$ with $^{k-1}}$ , illustrating the relative magnitude of the error. The logit values of $|| = 1000$ of actions are randomly sampled from the standard normal distribution, and we recursively apply softmax on the remaining items with the temperature parameter $ $. As the exact ground-truth probability is hard to calculate due to computational complexity, we estimate the ground-truth by Monte-Carlo sampling of 1000 trials. In the actual implementation, we use the $$ operation to stabilize the loss calculation as follows. _{} | x) &= _{} \\ &= ) \\ &= \, ) \\ &= \, ) ) \\ &= \, ) ) ) \\ &= \, ) ) ) Then, we estimate $ _{} $ as follows. _{} &= _{^{k-1}} P P \\ & P \\ &= _{ } )}{ _{a' _{ } ^{k-1}}} } )} \\ &= _{ } )}{ } + } - A_{ })))} \\ &= } - A_{ } - \, } - A_{ }))) where $A_{ } = _{a' } } )$ and $B_{ } = _{a' ^{k-1}}} } )$. We also denote $_{ } = _{}^{m } / $, and $^{k-1}} = _{^{k-1} a_l} P $, which is the most probable $k-1$ candidate set excluding the target action $a_l$. While we approximate the probability of the previously sampled items by ``arg-top-$ $'', this approximation is often quite accurate. This is because it is often unlikely that lower-ranked items can be sampled first when the item logit distribution exhibits a large logit gap . Indeed, Table~ shows that the approximation error of the action choice probability is quite low across different distributions of logit values. In contrast, when the item logit distribution is near-uniform, the probability of the top-ranked and bottom-ranked items does not differ so much. These results indicate that we can avoid the Monte-Carlo sampling to calculate the score function, reducing the computational time from $ $ to $ $ in the aforementioned calculation, with only a small approximation error. This approximation is a $0$-th order low temperature Taylor expansion. It is exact in the limit $ 0$, where the probability of the most likely choice approaches $1$, and all other choices approach $0$. Interestingly, this approximation is also exact in the high-temperature limit . In this regime, all item and set selections are equally likely; the distributions $P $ and $P $ tend to uniform, and therefore the average over $^{k-1}$ values becomes an average over a constant argument, which is equal to the argument at any $^{k-1}$ and in particular also at $^{k-1}}$. Next, we implement CA-PG-SwR using operation to stabilize the loss calculation as follows. _{} | x) &= | x; m ) ) \\ &= } ) }{ _{a' } } )} ) \\ &= } ) }{ } ) ) )} ) \\ &= } ) }{ } ) )} ) \\ &= } - A_{ } ) ) \\ &= _{k = 1}^K } - A_{ } ) where we let $A_{ } = _{a' } } )$ and $_{ } = _{}^{m } / $. Note that because $ } - A_{ })$ becomes the same among the $k$s that share the same model, the computational time is reduced from $ $ to $ $, where $K$ is the size of candidates, $M$ is \# of MoE models, and $L$ is the length of LSR's output. The recursive application of softmax of the Plackett-Luce policy is often modeled by the Gumbel top-$K$ trick for the computational efficiency~. We also follow this procedure and implement the sampling process as follows. Sample a value from Gumbel distribution for each action as $ _a $. Retrieve top-$K$ based on the logit value and Gumbel noise as $^{K} = K_{a } \, + _a )$ Sample a value from Gumbel distribution for each action as $ _a $. Recursively choose $k$'th item as $a_k = _{a ^{ }} \, } + _a )$ where $_{ } = _{}^{m } / $ is the predicted value of action $a$ given context $x$. This section provides additional details on the experiment and reports the whole training process. Code is available on a GitHub repository: {https://github.com/facebookresearch/early\_stage\_retrieval}. As described in Section~, we use the following oracle Plackett-Luce policy as the given LSR policy: _{}^{ } := })}{ _{a' ^K a_{1:l-1}} / _{})}, where $q $ is the ground-truth expected reward. We can simulate the varying alignment of LSR by adjusting the temperature hyperparameter $ _{}$ in each configuration. Specifically, the ``optimal'' policy is modeled by $ _{} +0$, ``anti-optimal'' policy is modeled by $ _{} -0$, and the ``uniform-random'' policy is modeled by $ _{} $ . The ``noisy-optimal'' policy uses $ _{} = 1.0$ and the top-1 action choice probability of the best action among the candidate was around 0.6 on average. We selected the learning rate based on the training stability and policy value at the stopping or convergence point of CA-PG and V-PG on the default setting . Then, we applied the same learning rate to different configurations. This is to see the robustness of each PG method to the varying configurations. The specific learning rate used for CA-PG/CA-PG-SwR is 1e-2, and for V-PG/V-PG-SwR is 1e-1. For both methods, we use a batch size of 128. A large value of the batch size is set to mitigate the variance issues of V-PG. We also test a simple adjustment of the learning rate depending on the problem instance. First, we observe that the scale of the loss function of V-PG becomes linearly large as $K$ becomes large. Therefore, we consider a small adjustment of the learning rate, in which we normalize the loss scale by multiplying the original learning rate by $K^{-1}$, when we vary the \# of candidates. However, this adjustment further slows down the training process for large values of $K$, and thus, we reject this adjustment. Another possible adjustment is to scale up the learning rate depending on the size of the action space . However, this approach is not tractable, as the scaling factor becomes exponentially large as $K$ grows. In particular, our experimental setting involves an action space of order , making this approach infeasible. Due to this reason, we reuse the same learning rate across varying sizes of candidates $K$ for V-PGs. Next, for CA-PGs, we know from Lemma~ that only CA-PGs optimize the policy with LSR propensity discounted reward using the following policy gradient in expectation : & : \; _{p _{} |x)} } q ) ] \\ & : _{p _{} } ) ] This means that the magnitude of the loss values can change depending on the level of stochasticity of the given LSR policy -- while we have no discount when using a deterministic LSR, we have a large discount in the magnitude of the reward signal when using a uniform random LSR. Therefore, we expect that the training of CA-PG/CA-PG-SwR should be slowed down without scaling with the inverse propensity of LSR's action choice probability from theoretical analysis. To mitigate this reward scaling issue, we recommend the use of an adaptive learning rate, where the learning rate should be scaled proportionally to the LSR propensity to the greedy action as follows. = } ) ])^{-1}. We use this simple adjustment of the learning rate in the synthetic experiment when we vary the alignment of LSR. Note that the adaptive rate is consistent across the whole training step, and can be estimated by the Monte-Carlo estimation only once at the beginning of policy training, enabling the simple adjustment of the reward scale, when we use a stochastic and consistently aligned policy for LSR. As we continue using the default learning rate of 1e-2, the resulting adaptive learning rates of ``noisy'', ``uniform'', and ``anti'' settings become 2e-2, 1e-1, 1e-2, after rounding the values. We compare the computational time of V-PG, V-PG-SwR, CA-PG, and CA-PG-SwR with a single logit model and varying candidate-set size and the length of ranking , and with MoE logit models and varying candidate-set size with a single output , in the synthetic setting . For this experiment, we use a MacBook Pro and run the experiments with CPUs without explicit parallel computation. All implementations are completed by PyTorch~. Figure~ reports the results of the computational time comparison. In Figure~ , we vary the size of problem instances as $ \{ , , , \}$. The first three settings are to see the changes with varying sizes of candidates , and the last two settings are to study how the length of ranking affects the computational time. The results demonstrate that the SwR approximation, including CA-PG-SwR and V-PG-SwR, has a clear benefit over exact calculation of the Plackett-Luce, including V-PG and CA-PG. Specifically, while CA-PG and V-PG require more runtime when the configurations $ and $ $, respectively) increase, the runtime of CA-PG-SwR and V-PG-SwR stays almost constant across varying problem instances. These results indicate that CA-PG-SwR not only reduces the computational order from $ $ to $ $, but the SwR approximation itself contributes to reducing the constant multiplier of the computational time. Next, Figure~ compares V-PG-SwR and CA-PG-SwR in a larger problem configurations, e.g., $K \{ 50, 100, 200 \}$ and $M \{ 1, 5 \}$, with $L=1$. The results indicate that the computational time scales quite similarly in these two methods, where we observe the increase of computational time proportional to \# of MoE models , while the computational time does not change much from the $K=5$ case even when we use a large candidate-set size like $K=200$. This indicates that the training-time computational cost of PG methods with the SwR approximation scales well with the candidate-set size . From these results, we recommend the use of TOP1-PG in a large-scale search and ranking problem where $L$ becomes large. In contrast, TOP1-PG can limit the ability to make most of MoE, as we have seen in Table~ in Section~. This suggests a tradeoff between performance and computational cost of CA-PGs and TOP1-PG . We report the training processes of the experiments with varying alignment of ESR and varying numbers of outputs and MoE models in the synthetic setting, and varying candidate sizes in the real-data setting in Figures~-. Finally, as discussed in the related work , we test the performance of both V-PG and CA-PG combined with other variance reduction methods, especially with a prevalent choice of GRPO~. GRPO queries $m$ samples per context and action $r_j , j $, and normalizes the reward as $r'_j = ) / $. While GRPO is not directly applicable to CA-PG due to the reward positivity requirement in Theorem~, we can easily integrate this reward normalization to CA-PG by adding a positive constant to the reward as $ $. Note that, implementing GRPO is unrealistic in the recommender systems setting, as GRPO needs to query rewards for multiple rankings . This is because, unlike LLM training, which can retrieve multiple rewards from the reward simulator, in RecSys, oftentimes only a single reward feedback is available through online interaction with users. However, the main objective of this additional experiment is to provide evidence that CA-PG can be easily integrated into other variance reduction methods of RL policies. Table~ reports the expected reward of the policies learned by V-PG-SwR + GRPO and CA-PG-SwR + GRPO. To compare the results in a practical situation, we use five MoE base models and vary the candidate-set size as $K \{ 20, 50, 100, 200 \}$. The results show that both V-PG-SwR and CA-PG-SwR improve the convergence speed combined with GRPO, and the difference between the V-PG-SwR and CA-PG-SwR becomes small when comparing the expected reward at 50K gradient steps. However, CA-PG-SwR + GRPO achieves a better performance than V-PG-SwR in the large candidate-set size regime, suggesting that the policy gradient methods can gain benefits from both GRPO-type variance reduction and CA-PG-type variance reduction . } { {c||c|c|c|c} {*}{PG methods} & {c}{\# of candidates } \\ & 20 & 50 & 100 & 200 \\ V-PG-SwR + GRPO & {}} { } & 8.84 { } & 8.93 { } & 9.00 { } \\ & $ $ 8.88 { } & $ $ 8.98 { } & $ $ 9.02 { } & $ $ 9.06 { } \\ & 8.58 { } & {}} { } & {}} { } & {}} { } \\ & $ $ {}} { } & $ $ {}} { } & $ $ {}} { } & $ $ {}} { } \\ } 0.1in {9pt} The {}} font shows the best performance. The policy value is measured at either @50K or @500K gradient steps. The results are based on 10 random seeds. {figs/result/label.png} {figs/app/runtime_wo_eval.png} We ran each PG method for 10 random seeds. The result reports the mean and standard deviation of the runtime, excluding trials in which gradient overflow occurred. The left figure uses $M=1$ , and the right figure uses $L=1$ .} {figs/result/label.png} {figs/app/varying_late_stage_opt_summary.png} } {figs/result/label.png} {figs/app/varying_moe_summary.png} } {figs/app/label_kuairec.png} {figs/app/kuairec_varying_candidate_size_summary.png} } We begin by formulating two-stage ranking as a contextual bandit problem. Let $x $ be a user context in the context space $$, such as a user's demographic profile. Let $a $ be an item where $$ is the original pool of items. In applications like search or recommendation, we aim to present a ranked list of items $a_{1:L} := $ to the users, and receive item-wise user feedback $r_{1:L} := $. The aggregated reward is expressed as $r^{ } = _{l=1}^L _l r_l$, where $ _l \, $ is a pre-specified non-negative weight for each position}. For example, a simple sum of rewards is expressed as $ l , \, _l = 1$, and a representative information retrieval metric called Discounted Cumulative Gain is expressed as $ l , \, _l = 1 / _2 $~. We can also implicitly model the learning-to-rank setting where $ $ can be seen as the , representing the position-dependent item observation probability~. Given a policy $ $, which determines which item ranking should be presented to users, we aim at maximizing the following policy value as our objective function: V := _{p p } , where $p $ and $p $ are the user and reward distributions respectively, and $ $ is the probability of choosing the item ranking $a_{1:L}$ given the user context $x$. As large-scale recommender systems need to handle a large number of items, these systems often employ a }-stage policy combination. The policy first filters the , and the policy generates the ranked list of items from the candidate set. These policies are modeled as the stochastic sampling process of the candidate set $^K _{} $ and final ranking $a_{1:L} _{} $, where $^K$ is the set of $K$ different items. This paper studies PG methods for the ESR policy given an existing, fixed LSR , to improve the overall quality of the joint policy . Following existing work~, we assume that the reward at each position $l$ depends only on the corresponding action, i.e., $r_l p $ and denote $q = $. This assumption means that there is no item-to-item interaction, such as diversity effects, in the users' reward signal. This allows us to decompose the expected ranking-wise reward into position-wise expected rewards as $ = _{l=1}^L _l \, q $. Therefore, the policy value is also decomposed into $V = _{l=1}^L _l v_l $, where v_l := _{p ^{ } } and $ ^{ } = _{a_{1:L}'} \{ a_l = a_l' \} $ is the marginal probability of observing the action $a_l$ at the position $l$. Because $ V = _{l=1}^L _l v_l $, in the rest of the paper, we discuss how to estimate the PG of $v_l $. {figs/concepts/credit_assignment.png} PG.}} From the PG theorem~, the PG of the joint policy for Eq.~ is given as follows: v_l &= _{} \\ &= _{p ^{ } p } where $ := p p $ is the original data generation process. The second equality follows from the independence assumption $r_l a_{1:L } | x, a_l$. We aim to calculate the gradient of ESR given a static LSR. The joint policy is decomposed into ESR and LSR as follows, ^{ } = _{^K} _{} _{}^{ } , where $ _{}^{ } $ is the marginal probability of the LSR policy choosing the action $a_l$ at the position $l$, defined as $ _{a_{1:L}'} \{ a_l = a_l' \} _{} $. Combining Eqs.~ and for a fixed LSR policy, the ESR's PG becomes as follows: v_l = _{' } , where we denote the new data generation process as $ $, which is the joint distribution of $^K$ and $a_{1:L}$, i.e., $p } _{} }p $..} To train the ESR model via gradient ascent, the expectation in the policy gradient must be estimated from empirical data. In online policy learning, we sample $n$ tuples per batch $\{ \}_{i=1}^n$ from the current policy $ $, observing only a single candidate set $^K_i$ and $l$'th position item $a_{i,l}$ per context $x_i$. Because the action space $^K$ grows exponentially with $K$, unbiased estimators of the PG suffer from extreme variance; thus, managing the bias-variance tradeoff is critical for fast and stable learning. The main limitation of the aforementioned ``vanilla'' policy gradient is } when using empirical estimation from sampled data. Specifically, because the action space of the candidate retrieval becomes exponentially large as the size of the candidate becomes large, more samples are required to estimate PG accurately. Such a variance issue can result in learning instability or slow convergence of the policy. Moreover, taking a closer look at the source of variance reveals that the variance stems from the } issue of V-PG. As illustrated in Figure~ , V-PG aims to increase the of all the actions selected in the candidate set , regardless of which action was selected by LSR and resulted in a high reward value. This means that V-PG needs to take an expectation over the combinatorial space of all candidate sets $^K$ to derive the gradient of each specific action $a_l$. As we need to use Monte-Carlo sampling to estimate the expectation over the large action space of $^K$, V-PG exponentially increases sample complexity as $K$ grows. Instead, it might be more desirable to propagate the gradient only to the corresponding action in the candidate set, as illustrated in Figure~ . In the next section, we study how to enable such ``credit-assigned'' policy gradient and discuss whether the credit attribution can resolve the variance problem of V-PG. The key ingredient for enabling the PG} is a new decomposition of the policy that does not depend on specific samples of $^K$. Specifically, we consider the following policy decomposition: ^{ } := _{} | x) ^{ } ) where $^K := \{ ^K \,|\, a_l ^K \}$ is the set of candidate sets $^K$ that contain $a_l$ as one of the members. Here, $ _{} | x) := _{^K} \{ a_l ^K \} _{} $ is the probability that the ESR model chooses the item $a_l$ among any candidate sets, while $ ^{ } )$ is the probability that $a_l$ is selected as the $l$'th item in the final ranking, given that $a_l$ is included in the candidates $). More precisely, $ ^{ } )$ depends on both ESR and LSR and is defined as $ _{^K ^K } _{} ) _{}^{ } $, where $ _{} )$ is the normalized probability that each specific candidate set $^K$ is selected.} ) = _{} / _{} | x)$.} The greatest benefit of the above policy decomposition is that the variable $^K $ takes the marginal across candidate sets $^K$ and thus no specific $^K$ appears as a random variable. This is beneficial for variance reduction, as it eliminates the need to estimate $_{^K} $ from Monte-Carlo samples as done in V-PG. Then, deriving the gradient of ESR w.r.t. $ _{} | x)$, we have the following }: v_l &= _{ } . The key trick is that CA-PG ignores the dependence of ESR on the second term } )$), which helps avoid the dependency on each specific $^K$ in the gradient estimation. Then, we can increase the ESR's probability only for the action corresponding to the reward observation, thereby resolving the credit-assignment issue as illustrated in Figure~ . Moreover, |^K$ to $||$, we expect variance reduction and improved sample complexity}. To gain insight into how the proposed CA-PG works, we theoretically examine two important research questions: What is the connection to V-PG and when is the CA-PG sufficient for learning an effective ESR model? Below, we first show that CA-PG is a component of V-PG, designed for variance reduction. Note that all the proofs in this paper are provided in Appendix~. CA-PG is a component of V-PG, i.e., = + C . The residual term $) is given as follows. [ _{} }{ ^{ } )}}_{ } _{} ) r_l ], where the expectation is $ := _{} ]$. Note that $ $ can be seen as the normalized importance of each specific candidate set $^K$, determined by how likely $a_l$ is to be chosen by the late stage policy $ ^{ }_{} $ given the candidate set $^K$. Theorem~ indicates that V-PG has an additional gradient term $) that propagates the gradient to each specific candidate set $^K$, depending on how likely LSR is to choose the target action given the corresponding candidate set $^K$. In other words, CA-PG equally propagates the gradient to each candidate $^K$, ignoring from which specific $^K$ the action $a_l$ comes from. We intentionally use this trick to avoid dependency on $^K$ via marginalization, allowing CA-PG to reduce the variance greatly compared to V-PG by working on a smaller action space. Interestingly, it also resolves the credit-assignment issue without taking an expectation over all candidate sets. This advantage of CA-PG becomes more pronounced when the candidate-set size becomes large. Next, to discuss RQ. : ``When does CA-PG learn an effective ESR policy?'', we first introduce the following notions of ``optimal alignment'' and ``optimal partition''. Let $a^{ }_1, a^{ }_2, , a^{ }_{||}$ be the ground-truth ranking of actions for context $x$ > q > > q $).} } when the policy preserves the ranking of items in the probability space, as $ > > > $.} $}} when $ > $ holds for any $ $, that satisfies $1 k K$ and $K + 1 j ||$}. Note that the optimal partition is a weaker condition than the optimal alignment, as it allows misalignment within the top-$K$ and within the tail items . Then, the following theorem suggests that CA-PG learns the optimal partition of items for many practical choices of the LSR policy, as long as they have a reasonable level of alignment ability, as explained below. CA-PG learns an ESR policy that is optimally partitioned for top-$K$, if the following condition is satisfied for any $ , 1 k K, K+1 j ||$. _{ _{} )} }{_{ _{} )} } < _k)}{q } Note that, when optimizing a policy with $ V = _{l=1}^L _l v_l $, the LHS of the inequality becomes the policy ratio w.r.t. $ _{l=1}^L _l ^{ } )$. The proof outline of Theorem~ is as follows: From the general PG theorem~, CA-PG updates the policy in a way to align $ _{} | x)$ with the rank of the corresponding expected reward, $q^{ } )$. This expected reward, $q^{ } )$, is defined as the LSR-propensity discounted reward, i.e., $_{ _{} )} q $ from Lemma~ in Appendix~. Therefore, to evaluate the top-$K$ items higher than other items , _{} | x) > _{} | x)$), the expected reward that CA-PG optimizes should satisfy $q^{ } ) > q^{ } )$. Assuming positivity of the reward, $ , q > 0$, Theorem~ indicates that , is sufficient for learning the optimal ESR policy}. Examples of such LSRs include arbitrary interpolation between the uniform random and optimal policy . This is because the LHS of the inequality becomes 1 when using the uniform random policy as LSR, and becomes less than 1 when using a policy that aligns the items optimally. In contrast, the RHS of the inequality is always greater than 1. Moreover, Theorem~ suggests that CA-PG learns an optimally partitioned ESR policy even in the presence of certain LSR misalignments, specifically: misalignment within top-$K$ items and within tail items , and cross-top-$K$ misalignment, provided the LSR alignment error is bounded by the reward ratio. The condition in the theorem may hold even when the LSR has inconsistent alignment of items among candidate sets, e.g., due to item-diversity or item-fairness constraints~. These theoretical results indicate that CA-PG does not introduce much bias in the ESR alignment when using a practical LSR policy, while greatly reducing the variance. { {c||c|c|c|c|c|c} {*}{PG methods} & {c|}{action} & {c}{gradient} \\ & space & size & MC sample & alignment condition & variance & computation \\ V-PG, V-PG-SwR & $^K$ & {$|^K}$} & {$^K, a_{1:L})}$} & {}} & {} & $ $ \\ & $^K$ & {|}$}} & {}$}} & + w/ reasonably-aligned LSR & low & {$ }$} \\ & $ $ & {|}$}} & {}$}} & + w/ Plackett-Luce ESR, {} & {}} & $ $, { }$}} \\ } 0.1in {9pt} The {} font represents the hardest requirement, and the {}} font shows the easiest one. The alignment of the items becomes accurate when the additional condition is satisfied for the proposed method. MC is the abbreviation of ``Monte-Carlo''. Having established the general form of CA-PG, we now address the computation of the marginal probability $ _{} | x)$, which is required to derive the score function in Eq.~. This section focuses on the calculation of the score function of the Plackett-Luce policy family, as this is one of the most prevalent approaches for sampling candidate sets from a large pool of items~. The PL policy samples ranked items without replacement by recursively applying the softmax function to the remaining items . We consider the generalized family of PL using multiple sub-retrievers models), taking into account that having multiple candidate retrievers is often considered useful in practice~: k, \, 1 k K, _k _{}^{m } / ) }{ _{a ^{ }} } / )}, where $m $ is the model index for the $k$'th member. For example, when $K = 10$ and using $M=5$ experts of the MoE models, we can define $m $ as follows: members 1 and 2 use model 1 , and members 3 and 4 use model 2 , and so on. $_{} $ is the ESR model's logit value of each item $a$ to the given user context $x$, and $ > 0$ is the temperature parameter. $^{ } := })$ is the previously sampled candidate set, where $^{ } = $. Then, the marginal probability $ _{} | x)$ can be derived via a method detailed in Appendix~, in a fully differential manner without requiring Monte-Carlo sampling. However, a drawback is that this process requires $ $ of computation as the probability must be calculated for every $l$ and $k$. This is worse than the computation of V-PG $), which does not depend on the final ranking length, $L$. To mitigate the computational burden of CA-PG, we consider the sampling w/ replacement approximation of PL, as done in~. Using the SwR approximation, PL can be seen as applying independent softmax to all the actions multiple times, resulting in the following : v_l = _{ } . We provide the derivation steps in Appendix~. CA-PG-SwR reduces the computational time significantly in the MoE case due to dependency on multiple logit models $ to $ $). Moreover, as the special case of CA-PG-SwR with a single logit-based PL model, we can specifically define the following ``TOP1-PG'': v_l = _{ } . TOP1-PG dramatically reduces the computational order from $ $ to $ $, a complexity substantially lower than the $ $ cost of V-PG. TOP1-PG enables a fast and easy gradient computation on the probability that the target item $a$ is selected as the top-1 item in the candidate. This makes TOP1-PG a practical alternative to CA-PG, especially in traditional applications using a single logit model. In contrast, it is also known that the MoE-based ESR model improves the exploration efficiency due to diversity in the candidate~, and there is a tradeoff of computational efficiency and performance between TOP1-PG and CA-PGs . Table~ summarizes the key characteristics of each PG method. This section empirically compares each PG method. The experiment code is available at a GitHub repository: {https://github.com/facebookresearch/early\_stage\_retrieval}. We simulate 1000 unique users and 1000 unique items in the synthetic experiment, where users and items are associated with $d_x$ and $d_a$ dimensional embeddings $ $ sampled from a uniform distribution $U ^{ }$. Using these embeddings, we model the expected reward as q = + c, where $ := )$ is a function that ensures positive output, and $c = 1.0$ is a positive constant . $M_{a}$ and $M_{x}$ are some $ $ and $ $ dimensional projection matrices, where we let $d_x = d_a = d_h = 10$. $ , $ is the inner-product between two embeddings. Given the above expected reward $q $, the reward is sampled from a normal distribution $ , ^2 )$, where $ $ is the noise level. Finally, we use the Plackett-Luce policy defined as $ _{}^{ } := / _{}) / / _{}))$ as the LSR policy. To enable a fair comparison among different PG methods, we use the same Plackett-Luce policy with two-tower model architecture~. Specifically, a model corresponding to the $k$'th element $) generates 10-dimensional user and item embeddings $_{m } $ and $_{m } $ and calculates a logit value of action $a$ given context $x$ by the inner product between two model embeddings: $_{m } = _{m } , _{m } $, where $ , $ represents the inner product. Given this model, we compare four PG methods for the ESR training; , , , and . We use a stochastic policy during the policy training phase, and evaluate the performance of deterministic ESR for all compared methods to see the difference in the item alignment ability of the learned ESR model. We use the same procedure to set the learning rate for V-PGs and CA-PGs. The exact implementation of the policies and additional details are provided in Appendix~, , and . {figs/result/label.png} {figs/result/varying_candidate_size_summary.png} Each line shows the learning curve for a single random seed . The vertical lines indicate training termination; any line appearing before 500K gradient steps signifies an interruption due to gradient overflow. Two horizontal lines show the 100\ { {c||c|c|c||c|c|c|c} {*}{PG methods} & {c||}{\# of candidates } & {c}{alignment of LSR} \\ & 5 & 10 & 20 & opt. & noisy & uniform & anti-opt. \\ V-PG & {} { } & {} { } & {} { } & {} { } & {} { } & {}} { } & {}} { } \\ & $ $ {} { } & $ $ {} { } & $ $ {} { } & $ $ {} { } & $ $ {} { } & $ $ {} { } & $ $ 1.07 { } \\ V-PG-SwR & {}} { } & 7.80 { } & 7.88 { } & 7.80 { } & 7.86 { } & 3.80 { } & 1.05 { } \\ & $ $ {}} { } & $ $ {}} { } & $ $ {}} { } & $ $ {}} { } & $ $ {}} { } & 6.39 { } & $ $ {}} { } \\ & 7.39 { } & {}} { } & {}} { } & {}} { } & {}} { } & 5.64 { } & {} { } \\ & $ $ 8.19 { } & $ $ 8.44 { } & $ $ 8.70 { } & $ $ 8.44 { } & $ $ 8.42 { } & $ $ 6.73 { } & $ $ {} { } \\ & 7.37 { } & 7.94 { } & 8.33 { } & 7.94 { } & 8.11 { } & {}} { } & {} { } \\ & $ $ 8.11 { } & $ $ 8.40 { } & $ $ 8.62 { } & $ $ 8.40 { } & $ $ 8.41 { } & $ $ {}} { } & $ $ {} { } \\ } 0.1in {9pt} The top values report the policy value @50K gradient steps, while the bottom value reports those @500K gradient steps. If gradient overflow occurs, we use the performance at the interrupted point instead of @50K or @500K. The {} font represents the worst performance, and the {}} font shows the best performance, for each of @50K and @500K performances. To check if Theorems~ and hold empirically, we compare performance of each PG method with the following configurations with the } font representing the default value: $)} - \{ 5, }, 20 \}, $)} - \{ }, 5 \}, $)} \{ }, 5 \}, - \{ }, noisy optimal, uniform, anti-optimal \}. Config specifies whether we use a single logit model $_{}$ for the PL policy or use a different model for each position of the top-$K$ . Finally, Config tests the performance of CA-PGs when Theorem~ does not hold. We run experiments with 10 random seeds. Additionally, computational cost analysis is provided in Appendix~. We first investigate how the stability and convergence speed of each policy gradient method change with varying size of candidates in Figure~. The former, stability, is studied by checking whether the policy gradient caused gradient overflow during the training process. The results demonstrate that, without using a approximation, the policy gradient becomes unstable regardless of the choice of PG method. In particular, V-PG behaves especially catastrophically when the candidate size is large . In contrast, CA-PG increases the training stability greatly, making the training process more reliable. Moreover, Figure~ also suggests that, even if we use the SwR approximation, V-PG-SwR slows down the convergence speed as we increase the size of candidates . Thus, CA-PGs perform much better than V-PG-SwR when we compare the performance with a small number of online interactions , and achieve 95\ These differences are significant in a practical situation, as a poor performance of the policy in the initial training phase can harm both user satisfaction and business metrics. In contrast, because V-PG is an unbiased estimation of the true ESR's gradient, V-PG-SwR performs better than CA-PGs at convergence , while CA-PGs also perform comparably. Next, we also compare the performance with varying alignment of LSR in Table~. When using the optimal, noisy-optimal, and uniform LSR, the observations are quite similar to the case with varying candidate sizes -- V-PG-SwR often performs better than CA-PG at the convergence performance due to unbiasedness of the policy gradient, while CA-PG and TOP1-PG achieve better performance @50K due to faster convergence, empirically showing the tradeoff between bias and variance as Theorem~ indicates. Moreover, we also find that CA-PG and CA-PG-SwR fail only in the case with anti-optimal LSR, empirically verifying the validity of Theorem~, stating that CA-PG can align items correctly as long as using a reasonably aligned LSR policy. Finally, we further compare the performance of each PG method when using five MoE models as the base logit models, instead of using a single logit model, which is often useful in practical situations~. Specifically, Table~ reports how the convergence performance changes with a single and MoE models and varying size of LSR output . The results reveal that, while V-PG is the best when using a single logit model, CA-PG and CA-PG-SwR perform better than V-PG-SwR when using MoE models. These results show that CA-PGs can exploit more benefits of MoE models than V-PG, allowing more flexible modeling of ESR. { {c||c|c||c|c} {*}{PG methods} & {c}{\# of output } \\ & {c||}{1} & {c}{5} \\ & {c}{\# of MoE models } \\ & 1 & 5 & 1 & 5 \\ V-PG & {} & {} & {} & {} \\ & { } & { } & { } & { } \\ V-PG-SwR & {}} & 8.58 & {}} & 37.2 \\ & { } & { } & { } & { } \\ & 8.44 & {}} & 36.2 & 37.2 \\ & { } & { } & { } & { } \\ & 8.40 & 8.82 & 36.3 & {}} \\ & { } & { } & { } & { } \\ } 0.1in {9pt} The {} font represents the worst performance, and the {}} font shows the best performance. The policy value is either @500K gradient steps or at the gradient overflow point. Next, we compare the approaches on real data from the KuaiRec dataset~. This dataset provides a fully observable user-item interaction matrix of $q $ using the empirical "watch ratio" in a video streaming platform. The data consists of 1411 users and 3326 items, and we learned their embeddings with $d_h = 10$. To simulate a practical situation, we compare PG methods with larger candidate-set sizes, i.e., $K \{50, 100, 200\}$. To make policy learning computationally tractable in this setting, we need to use SwR approximation and compare only V-PG-SwR and CA-PG-SwR. All other settings remain the same as the defaults used in the synthetic experiments. Table~ summarizes the performance of each PG method with varying \# of candidates . The real-data experiment is a more challenging setting due to large candidate sizes and complex reward functions. We observe that CA-PG-SwR improves the convergence speed over V-PG-SwR and becomes increasingly beneficial as $K$ grows. This result demonstrates that the ``credit-assignment''-based variance reduction is particularly useful in practical situations with a large set of candidates. { {c||c|c|c} {*}{PG methods} & {c}{\# of candidates } \\ & 50 & 100 & 200 \\ V-PG-SwR & 5.05 { } & 5.39 { } & 5.92 { } \\ & $ $ {}} { } & $ ${}} { } & $ $ 8.26 { } \\ & {}} { } & {}} { } & {}} { } \\ & $ $ 7.19 { } & $ $ 7.82 { } & $ $ {}} { } \\ } 0.1in {9pt} The {}} font shows the best performance. The policy value is measured at either @50K or @500K gradient steps. This section discusses the most closely related works on policy learning for large-scale ranking..} The Plackett-Luce policy has been widely used in search~, video streaming~, and document retrieval~ in a single-stage ranking setting. Given that the action space can be large in this ranking setting, the efficient calculation of PL has been a key challenge for the PG approach. For example, calculates PL for the partial reward of ranking, leveraging the cascading structure of the ranking decomposition. approximates the probability that an item set is ranked higher than the remaining subset of items. calculates the probability of the selected action being one of the top-$K$ actions in the final ranking. Furthermore, proposes a PL-like stochastic policy that allows faster computation of the gradient using Gaussian modeling. However, because all of these works focus on the ranking setting, the credit assignment issue in the setting has not been previously addressed. An efficient way of propagating the gradient to the ESR policy has remained non-trivial and underexplored. is the most related work to ours in that it studies the PG approach in the two-stage setting. However, as discussed in the main text, V-PG has the issues of high variance and credit assignment, limiting the scalability of V-PG to large candidate sizes . While discusses some heuristics to reduce the variance of V-PG , the mechanism of how and why this heuristic works has not been demystified. We provide a rigorous proof of how a general idea of PG can resolve the two issues of V-PG. Another related work is , which retrieves a diverse set of candidates using an MoE-like PL policy via two-stage PG. However, focuses on the case where reward interaction among ranking matters, and credit assignment does not matter due to the dependence of the reward on multiple items. These two works also consider different learning challenges in the offline setting. Our work is the first to highlight and mitigate the credit assignment issue in two-stage PG. In another line of research, variance reduction has long been a key research question for policy gradient-based methods in RL~. The prevalent approach for variance reduction has been baseline correction, and the representative method called PPO~ calculates the function, which normalizes the reward given context and action $) by a model-predicted baseline estimating the expected reward given context $x$, i.e., $ _{ } $. Similarly, in the recent large-language model applications, GRPO~ queries $m$ samples per context and action $r_j , j $, and normalizes the reward as $r'_j = ) / $. These methods reduce the variance by normalizing the reward instead of manipulating the calculation of the score function , as CA-PG does. Therefore, the existing RL variance reduction methods are complementary to CA-PG, and we can combine the baseline correction-based variance reduction with CA-PG. As a proof of concept, we additionally report the results of combining both V-PG and CA-PG with GRPO in Appendix~, showing the benefit of using both CA-PG and GRPO together. This paper proposes a statistically and computationally efficient approach for training the early-stage ranker in a two-stage decision policy. We examined the construction of the ``vanilla'' policy gradient and identified that variance and credit-assignment issues become prohibitive when the candidate-set size is large. To tackle these challenges, we developed a novel PG method called ``credit-assigned'' PG and demonstrated its benefits in both theoretical and empirical ways. We identify two interesting directions for future work. The first is to explore the setting~, where we aim to optimize the policy using logged data. While we expect CA-PG's benefits to hold in the offline setting, exploring an efficient importance sampling strategy in our setting can be a promising future direction. The second direction is to investigate how to incorporate reward interaction within ranking. Our paper primarily focuses on the setting where the position-wise reward is affected only by the corresponding action at the same position. However, reward interactions such as diversity within the final ranking~ may arise in applications like news recommendation. Exploring how to assign credit to each action under item-item interactions can also be impactful. Reinforcement learning via policy gradient methods~ has proven to be a powerful tool for finding decision-making policies that maximize an objective of interest, a.k.a. reward. Successful applications of policy gradient include a wide range of problems, such as the training of large language models ~, retrieval-augmented generation ~, information retrieval ~, and content recommendation and ranking~. These applications require us to work with larger action spaces and more complex models as technology evolves. {figs/concepts/two_stage_rec.png} } In particular, modern recommender systems have to process a massive volume of items at web latency. By necessity, these systems handle this scale through two main stages: an early-stage ranker , which filters up to billions of items cheaply into a smaller candidate set, and a more expensive late-stage ranker that generates the final set of recommendations from this candidate set, as illustrated in Figure~. An effective ESR is of crucial importance, since the LSR policy can only be successful if the candidate set contains the relevant items~. However, while we have many methods for training the LSR policy, we lack principled approaches for end-to-end training of the ESR policy to make the overall retrieval pipeline maximally effective. Moreover, the LSR in existing pipelines often combines the results from multiple ESR models to make sure a diverse set of items is available to the LSR policy . This makes the design of effective training methods for the ESR particularly challenging ~. We begin the analysis by investigating the naive approach for the ESR training, called ``vanilla'' ~. V-PG derives the exact PG of ESR w.r.t. the probability of choosing the candidate set sampled by the ESR policy. We observed that V-PG has a significant learning instability issue, especially when we increase the candidate-set size and the number of actions . This is because the } of V-PG increases exponentially in $ $ as the action space of V-PG, is combinatorial. Because we select only one candidate set $^K$ for each query in the user interaction, this large action space makes it difficult to accurately estimate the gradient. Moreover, we also identified a } issue in the V-PG, which is closely related to the variance problem. The credit-assignment issue happens because V-PG aims to increase the joint probability of $^K$, where all actions in the candidate set are equally valued, thus ignoring which specific action contributed to the final reward. For example, consider the case where there are three actions, $A, B, C$, selected in the candidate set, and the actions $A$ and $B$ are presented in the final ranking selected by the LSR. Suppose that we get a positive reward for $A$ and a negative reward for $B$ as their position-wise reward. Intuitively, we expect that the probability of selecting $A$ in the candidate set should be increased, while the probability of selecting $B$ should be decreased by the ESR policy. However, V-PG increases the probability of selecting all members of $ $ when $A$ receives a high reward. This issue has been overlooked. Our theoretical analysis reveals that this inefficient propagation of V-PG to irrelevant actions is the key source of the variance , leading to learning instability and slow convergence. We overcome this issue by introducing a variance-reduced alternative called policy gradient }. The key point of CA-PG is that the gradient is calculated w.r.t. the probability that action $a$ is selected in candidate set, i.e. the marginal probability of $^K := \{ ^K | a ^K \}$, instead of the probability of selecting a candidate set $^K$. CA-PG is not an estimation of the true ESR's policy gradient due to marginalization. However, we show that CA-PG can learn to select the optimal candidate, as long as the LSR policy is with the true rank of actions, including arbitrary interpolation between the optimal and uniform random policies . These results indicate that ignoring distinctions between specific candidate sets can have more benefits than harms due to substantial variance reduction under the use of a practical LSR policy, achieving a better bias-variance tradeoff when the sample size is small. We turn from statistical to computational efficiency. Specifically, we discuss the computation of the CA-PG's marginal probability $) for the commonly-used Plackett-Luce ESR policy~, with and without the ``sampling w/ replacement'' approximation. Our analysis reveals that CA-PG-SwR reduces the computational cost of CA-PG from $ $ to $ $ in the special case of using a single logit model candidate retrievers), where $L$ is the length of LSR output. This ``TOP1-PG'' computes the simple probability of the target action selected as the top-1 action by ESR, providing a computationally efficient alternative to CA-PG. Our finding that TOP1-PG can be sufficient is a valuable insight, as a single logit model-based PL is still widely adopted in practical applications~. However, the combination of MoE and CA-PGs can yield a further expected reward improvement compared to TOP1-PG, due to more efficient exploration. This suggests an interesting tradeoff between computational efficiency and performance in the design of the ESR policy. Finally, we compared CA-PG and TOP1-PG to V-PG and V-PG-SwR on both synthetic and real data, using the KuaiRec dataset~. The results demonstrate that CA-PG consistently improves the training stability over V-PG, by avoiding the gradient overflow of the policy gradient. Moreover, we also observe that both CA-PG and CA-PG-SwR converge faster than V-PG-SwR, especially when the size of candidates is large, requiring minimal interaction to identify user interests. Our contributions are summarized as follows. We discuss the tractable end-to-end training of ESR to maximize users' response signal. We find that ``vanilla'' policy gradient has a high-variance issue, associated with the credit-assignment problem. We propose a ``credit-assigned'' policy gradient to reduce variance, while keeping the ability to correctly align items under a reasonably aligned late-stage policy. We provide a theoretical analysis formally characterizing the aforementioned variance and alignment properties, alongside a comparison of computational complexity. We empirically demonstrate that CA-PG improves the learning stability and convergence of V-PG when the candidate-set size is large. {xcolor} {} {{{ }}} {} {{{UW: #1}}} {{#1}} {icml2026} {cleveref} {Theorem} {Proposition} {Lemma} {Corollary} {Definition} {Assumption} {Remark} {todonotes} [ {*} {Cornell} {Meta} {Meta} {Meta} {Meta} {Meta} {Meta} {Cornell} {Cornell} {Meta} {Computer Science Department, Cornell University, Ithaca, NY, USA} {Central Applied Science, Meta, Menlo Park, CA, USA} {hk844@cornell.edu} 0.3in ] Large-scale search, recommendation, and retrieval-augmented generation systems typically employ a two-stage architecture: an early-stage ranker generates a candidate set, which is subsequently re-ranked by a late-stage ranker . While there are many reinforcement learning methods for training the LSR, end-to-end training of the ESR has proven challenging. In particular, naive application of ``vanilla'' policy gradient is not scalable for candidate-set sizes relevant for practical use due to exploding variance. This issue arises because V-PG propagates the gradient to the joint probability of the candidate sets, ignoring the contribution of each specific item in the candidate set to the reward. To mitigate this issue, we propose a novel , which computes gradients with respect to the probability that the target item is chosen in any candidate set, i.e. marginalizing over all candidate sets that contain it. Our theoretical analysis reveals that CA-PG significantly reduces the variance of V-PG by marginalizing over the specific composition of the candidate set, while preserving the ability to learn the correct ranking of items under a reasonably aligned LSR policy. Experiments on both synthetic and real-world data demonstrate that CA-PG improves the convergence speed and training stability for ESRs utilizing the canonical Plackett-Luce model, especially when the candidate-set size is large. *{Acknowledgments} This research was supported in part by NSF Awards IIS-2312865, IIS-2442137, OAC-2311521, CCF-2312774, and a gift to the LinkedIn-Cornell Bowers CIS Strategic Partnership. Sarah Dean is also partly supported by an AI2050 Early Career Fellowship program at Schmidt Sciences, and Haruka Kiyohara is partly supported by the Funai Overseas Scholarship and Quad Fellowship. Most of the work was done while Haruka Kiyohara was an intern at Central Applied Science, Meta. Additionally, we thank anonymous reviewers for valuable feedback during the discussion phase. *{Impact Statement} This paper investigates the optimization of early stage ranking policies in a two-stage ranking framework. Applications span search, recommender systems, and large language models . We expect that the paper will have a broad impact on such related domains that rely on large-scale retrieval. As we use synthetic simulations and a publicly available dataset~ in our paper, we do not expect that this paper will raise any ethical considerations. While reinforcement learning approaches in general have concerns regarding their learning instability and harm during the training process~, this work aims to overcome such difficulties. We believe this effort makes the RL approach more robust and tractable in practical settings. {} {} {rgb}{0,0.6,0} {rgb}{0.25,0.25,0.25} {rgb}{0.8,0.05,0.05} {rgb}{0.05,0.05,0.8} {{ {#1}}} {} {}} {Eq.~ } {Theorem~} {Proposition~} {Assumption~} {Table~} *{ }{arg\,min} *{ }{arg\,max} {} {} {} {} {} {} {} {} {} {w_{}} {w_{}} {} {} {} {} {{l^{ }}} {w_{1:l} } { _{1:l}^{ } } { _{1:l} } {V } {_{} } {_{} } {V_{} }; _{})} { _{}} { _{}} {rgb}{0.8,0,0} {rgb}{1.0,0,0.5} {rgb}{0,0.4,0} {rgb}{0,0.6,0} {{ }} {{ }} {{#1}} 