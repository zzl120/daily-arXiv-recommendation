 {article} {fontenc} {inputenc} } } } {HTML}{dde8ff} {HTML}{6688bb} {RAG} {LLM} { source} { } {{ }} {{ }} {} Code as a Knowledge Source for Retrieval-Augmented Generation} }} Large language models can answer questions about textbooks, lecture notes, and programming exercises more reliably when their answers are grounded in an explicit knowledge source. Retrieval-augmented generation is a common approach: relevant fragments of a document are retrieved and inserted into the model context before answering. For mathematical and technical material, the original source can be a better starting point than a PDF, because it contains structural information, labels, sectioning commands, macros, and authorial intent that are often lost or distorted in PDF extraction. However, source is not automatically AI-friendly. Cross-references must be resolved, custom macros must be interpreted, exercises and examples must be identified, and author-supplied semantic metadata may be needed. This article describes a focused preprocessing approach for turning source, together with its compiled auxiliary files and optional author annotations, into Markdown and JSONL chunks suitable for indexing in a vector database. Textbooks and lecture notes are increasingly used as knowledge sources for AI assistants. A course assistant for beginning programmers, for example, may be expected to answer questions about exercises, compiler errors, and concepts from the textbook while respecting teacher-supplied constraints: do not reveal complete solutions, use the terminology of the course, and refer students to the relevant sections. A related scenario arises when an AI coding assistant helps a researcher working on a mathematics paper. The assistant may need to consult prior theorems, notation conventions, or formal definitions from related articles and theses for which the source is available. The requirements are similar but the material is typically denser: a master's thesis may exceed 100~pages of notation-heavy mathematics where the structural integrity of the source is especially valuable. A tempting solution is to give the AI system the PDF of the textbook, article, or thesis. This is convenient, but it is rarely ideal. The PDF is the final visual artifact, not the semantic source. It may obscure structure, damage code formatting, lose labels, split paragraphs across pages, and turn mathematical notation into difficult-to-interpret text. By contrast, the contains sectioning commands, labels, environments, macro names, and often a modular file structure. These can be valuable for retrieval and grounding. The central claim of this article is that source can serve as a high-quality knowledge source for ~, provided that it is preprocessed in a way that respects both its typographical and semantic roles. The remainder of this article presents an overview of the preprocessing pipeline and then examines each core challenge in turn: cross-references, macros, figures, and author annotations. Authoring conventions that make source more AI-friendly from the start are summarized, followed by related work and a discussion of limitations. A Python implementation is openly available~. A system typically performs the following steps: Split a document into chunks. Compute an embedding vector for each chunk. Store the embedding vector together with the chunk text and metadata in a vector database. Embed the user's question. Retrieve the most relevant chunks. Provide those chunks to the as context for answering. For ordinary prose this can be straightforward. For documents it is less so. The source may contain commands such as: As explained in Section~, a while-loop repeats as long as its guard evaluates to true. See also page~. The literal source text is not what we want to embed. A student is unlikely to ask about |sec:loops|. The RAG representation should instead contain something like: As explained in Section 4.3, a while-loop repeats as long as its guard evaluates to true. See also page 71. The same issue arises with custom macros. Some macros are merely typographical: {} Such a macro can safely be converted to Markdown code notation. Other macros introduce semantic notation: {} Here | | may denote the split combinator from functional programming. Expanding it to a triangle glyph is not necessarily helpful for an AI system. The macro name may be more informative than its visual expansion, provided that the preprocessor also supplies its meaning. The preprocessor accepts the following inputs: the main file, for example |book.tex|; the compiled auxiliary file, for example |book.aux|; included files, discovered from | | and | | commands; included auxiliary files, discovered from |\@input| entries in the main |.aux| file; optional author annotations, for example |notation.yaml| or |rag.yaml|. The outputs are: a readable Markdown rendering, such as |book.rag.md|; a label table, such as |labels.json|; a chunk file, such as |chunks.jsonl|, containing one JSON object per chunk. A typical chunk object contains: { "id": "chunk-00017", "heading_path": , "source_file": "chapter4.tex", "start_line": 120, "end_line": 168, "markdown": "### While-loops while-loop repeats ...", "embedding_text": "Location: Chapter 4 > While-loops ...", "metadata": { "kind": "explanation", "labels": , "page_start": 71, "page_end": 74 } } The vector database stores one row per chunk. The embedding vector is computed from |embedding_text|. The human-readable |markdown| and the other fields are stored as metadata or payload. At query time the student's question is embedded, similar chunks are retrieved, and the retrieved Markdown is inserted into the model context together with its metadata. The preprocessing pipeline proposed here uses two kinds of structured data: author-written configuration files and machine-generated output files. These have different audiences. YAML is suitable for author-written configuration. It is relatively readable, supports comments, and allows multi-line text blocks. This matters when an author writes semantic annotations such as explanations of notation, aliases, pedagogical restrictions, or descriptions of exercise roles. For example: macros: splt: name: "split combinator" meaning: > The split combinator combines two functions with the same input into a pair-valued function. aliases: - split - triangle operator example_latex: "f \ g" example_text: "f \ g maps x to ." JSON is better suited for generated output and machine interchange. It is stricter, widely supported, and natural as a line-oriented format such as JSONL, where each line represents one chunk to be embedded and stored. For example: {"id":"chunk-00017", "embedding_text":"Location: Chapter 4 > While-loops ...", "metadata":{"source_file":"chapter4.tex", "start_line":120}} Thus the distinction is not arbitrary. YAML is used where the human author writes and maintains semantic intent; JSON or JSONL is used where software consumes generated data. The preprocessor runs the following major steps. The subsequent sections address each stage in turn. Read the main file. Recursively follow | | and | | commands. Read the main |.aux| file and recursively follow |\@input| entries. Parse | | entries to build a label table. Load optional YAML annotations. Convert selected LaTeX structures to Markdown. Resolve | |, | |, and | |. Interpret known macros according to the annotation registry. Preserve semantic notation and enrich the embedding text. Chunk the result by section, subsection, definition, example, or exercise. Write Markdown, JSON labels, and JSONL chunks. Figure~ shows a schematic view of the major pipeline stages. [ node distance=4mm, box/.style={draw, rounded corners=2pt, minimum width=74mm, minimum height=7mm, align=center, font= , fill=white}, file/.style={draw=fileboxborder, rounded corners=2pt, minimum width=74mm, minimum height=7mm, align=center, font= , fill=fileboxbg}, arr/.style={->, >=Stealth} ] {.tex source .aux files}; {load labels \& read source}; {extract figure blocks}; {convert to Markdown}; {chunk \& make figure chunks}; {write outputs}; {chunks.jsonl labels.json .rag.md}; -- ; -- ; -- ; -- ; -- ; -- ; preprocessing pipeline. Input and output files are shown with a gray background.} The preprocessor is conservative: it does not attempt to be a complete interpreter. When it encounters uncertain material, it leaves it in place and emits a warning. For , a robust partial conversion is often more useful than a fragile attempt at perfect expansion. After compilation, a LaTeX auxiliary file contains entries such as: {{4.3}{71}{While-loops}{section.4.3}{}} The preprocessor parses these entries and builds a label table: { "sec:while-loops": { "ref": "4.3", "page": "71", "title": "While-loops", "anchor": "section.4.3" } } Then the source fragment: See Section~ on page~. can be converted to: See Section 4.3 on page 71. This simple transformation is already useful. It makes chunks more self-contained and improves retrieval, because the embedded text contains the same surface forms that a student or teacher is likely to use. When the document uses the package, the auxiliary file also contains companion entries: {{ 4.3}{ 71}} The first bracketed word, , is the counter type. The preprocessor reads these |@cref| entries and resolves || to and || to , using a built-in table of 25 counter types. Not all macros should be treated alike. We distinguish at least four classes. Typographical macros affect presentation but not meaning: {} These can be expanded or normalized in Markdown: Use `while` rather than `for` in this exercise. Semantic notation macros name concepts, operators, or conventions. The split combinator is an example: {} In the source, one might write: The combinator $f g$ maps an argument to a pair. The Markdown may preserve the notation: The combinator $f g$ maps an argument to a pair. But the embedding text should be enriched: The combinator $f g$ maps an argument to a pair. Notation note: means "split combinator". It combines two functions with the same input into a pair-valued function. This is an important principle: the representation shown to students and the representation embedded for retrieval need not be identical. Some macros encode document structure: {Predict the output of the following program.} Rather than expanding such macros typographically, the preprocessor interprets them structurally: ### Exercise 4.3 Predict the output of the following program. with metadata: {"kind":"exercise", "exercise_id":"4.3"} Unknown macros should not silently disappear. The preprocessor preserves them and emits a warning. Unknown semantic macros are opportunities for improving the author's annotations. Figures require special treatment in a pipeline. A figure is not merely a visual decoration; it often contains essential semantic information that students may ask about. This applies both to figures generated directly in , such as TikZ diagrams, and to externally included images such as PDF, PNG, or JPEG files. A naive preprocessing strategy might ignore figures entirely or only retain their captions. This is insufficient. Captions typically identify a figure but do not fully explain the knowledge encoded in it. For retrieval purposes, figures need textual surrogates: descriptions that explain what the figure means. A useful principle is therefore: Figures should be treated as separate knowledge objects. The pipeline extracts the following information from each figure: the figure label; the caption; the surrounding section and textual context; the included image filename or TikZ source; the page number; an author-supplied semantic description; optionally, a rendered image representation. For example: {pipeline.pdf} may become the following figure chunk: { "id": "figure-00012", "kind": "figure", "label": "fig:pipeline", "caption": "A preprocessing pipeline for LaTeX-based RAG.", "image_file": "pipeline.pdf", "embedding_text": "Figure: A preprocessing pipeline for LaTeX-based RAG. ...", "metadata": { "section": "2.3", "page": 34 } } The vector database should primarily embed the caption, semantic description, and nearby explanatory text. The raw image file itself is not usually suitable as the primary retrieval representation. Likewise, the raw TikZ source code is often too low-level to serve directly as embedding text. TikZ figures deserve particular attention. A TikZ diagram may contain highly meaningful mathematical or categorical structure while remaining difficult to interpret from its source code alone . {{0pt} \& A \& \\ B \& B C \& C } For example: ... The caption alone is often too brief for retrieval. A better approach is to supply a semantic description explicitly: ... The command | | is a no-op during normal compilation so it has no typographic effect. It is provided by the companion package : The preprocessing script recognizes the command and extracts its argument as semantic metadata. The macro approach is preferred over comments for AI-facing annotations; Section~ explains in detail why comment-based approaches are unreliable. The same approach applies to other annotation macros provided by the package: | | for semantic notation, | | for inline explanations, and | | for explicit chunk boundaries. The resulting embedding text for the figure may then become: Figure: Commuting diagram for the split combinator. The figure shows two functions f:X to A and g:X to B combined into a single function from X to A times B, from which f and g can be obtained by left and right projections. This approach is especially important for educational material. Students often refer to figures indirectly: ``What does the triangle diagram mean?'' ``Why does the arrow split into two arrows here?'' ``What does the picture on page 42 show?'' Without semantic descriptions, such questions may fail to retrieve the relevant material. Figure~ illustrates how a figure environment is transformed into a chunk, with the | | content becoming part of the embedding text. {fig-figure-chunk} figure environment into a chunk . The AIDescription} block is a no-op during typesetting but becomes the semantic description in the embedding text .} centering, includegraphics, caption, label, and a highlighted AIDescription block with the text ``Shows five stages.''. The right box shows the resulting JSONL chunk with fields id, kind, image\_file, caption, and a highlighted embedding\_text field that reads ``Figure: A pipeline. Shows five stages.''. } The broader principle is therefore: Figures should have machine-readable semantic descriptions, not only visual renderings. This principle applies equally to externally included graphics, TikZ diagrams, commutative diagrams, automata, geometric illustrations, plots, and other forms of visual mathematical communication. An author can supply semantic metadata to the preprocessor using one of two approaches: in-source declaration macros, or an external YAML file. A third superficially attractive option---tagged comments---does not work and should be avoided. It is tempting to annotate using special comments: {} Such comments are safe for ordinary compilation, but they are invisible to the preprocessor. The preprocessor strips comments during source loading, before any other processing stage . A comment-based annotation therefore cannot reach the stages that build the macro registry, enrich the embedding text, or generate the notation glossary. This is not an implementation oversight: stripping comments early is correct behavior, because it prevents comment syntax from interfering with macro recognition. In-source annotations must use macro arguments, not comments. The preferred in-source approach uses the package, which provides all annotation macros as no-ops so that ordinary compilation ignores them: A declaration then accompanies each semantic macro definition: {} { } {split combinator} {Combines two functions with the same input into a pair-valued function.} For ordinary compilation, | | does nothing. For preprocessing, it is a machine-readable declaration of intent: because macro arguments survive comment stripping and are part of the document source tree, they are available to every subsequent processing stage. The preprocessor scans all source lines for | | declarations and merges them with any YAML annotations; YAML takes precedence on conflicts, making it easy to supplement or override in-source declarations without touching the source. This approach is well suited to new documents where the author controls the preamble, and to annotations that must be positionally coupled to their content---such as | | inside a figure environment, where the annotation cannot sensibly be expressed without knowing the exact location. The main disadvantage is that annotations are distributed across all files in a multi-file document, making it harder to audit coverage. The alternative is a separate YAML file, for example |book.rag.yaml|: macros: splt: name: "split combinator" meaning: > Combines two functions with the same input into a pair-valued function. aliases: - split - triangle operator example_latex: "f \ g" example_text: "f \ g maps x to ." pedagogy: visibility: - environment: solution access: teacher_only The YAML file is preprocessor input only: it is consumed when building the macro registry and applying pedagogy rules, and it does not appear in the vector database or in any output chunk. This distinction matters---the YAML file is an authoring tool, not a artifact. The |macros| section maps each macro name to its annotation. The preprocessor uses the registry to append a notation note to the |embedding_text| of every chunk whose Markdown contains the macro, and to generate a single |kind="glossary"| chunk listing all registered macros. The |pedagogy.visibility| rules cause the preprocessor to inject a |"visibility"| field into the |metadata| of matching structural chunks ; the RAG application is then responsible for filtering on that field at query time. A third top-level key, |suppress_macros|, accepts a list of macro names that should be silenced without annotating: suppress_macros: - graphicspath - usetikzlibrary - cite - url Macros in |suppress_macros| are excluded from the glossary and notation enrichment, and the unknown-macro warning is silenced for them. The primary use case is preamble and bibliography commands that are not relevant to retrieval and would otherwise produce spurious warnings. A secondary use case is silencing macros that the in-source scanner incorrectly picked up from verbatim code examples . This approach is well suited to existing lecture notes or textbooks where the files should not be modified, and is convenient when annotation is done by someone other than the original author, or when a single auditable view of all semantic metadata is preferred. The main disadvantage is that the YAML file can only reference content that carries a | |; positional annotations such as describing a TikZ figure without a label require the in-source macro approach. The two approaches are complementary rather than mutually exclusive. A natural combination is to use | | in figure environments together with a YAML file for the macro registry and pedagogy rules. When starting a new document from scratch, declaration macros throughout the source provide the tightest coupling between annotations and content. When working with existing documents, a YAML file avoids any changes to the source. When a macro registry is present, the preprocessor generates a notation glossary chunk listing every registered macro: # Notation glossary ## : split combinator Aliases: split, triangle operator. The split combinator combines two functions with the same input into a pair-valued function. Example: f g maps x to . This chunk is inserted into the vector database like any other chunk. It helps retrieve explanations when a student asks questions such as ``What is the triangle operator?'' or ``What does split mean here?'' In addition, whenever a registered macro appears in any other chunk, the preprocessor appends a notation note to that chunk's |embedding_text|: Notation note: means "split combinator". Combines two functions with the same input into a pair-valued function. Also known as: split, triangle operator. This inline enrichment ensures that even a chunk that only uses the notation---without defining it---is retrievable by the concept name. Original : A -loop repeats its body as long as its guard is true. As explained in Section~, the guard must be a Boolean expression. Possible Markdown: ## While-loops A `while`-loop repeats its body as long as its guard is true. As explained in Section 3.1, the guard must be a Boolean expression. Possible embedding text: Location: Chapter 4 > While-loops A while-loop repeats its body as long as its guard is true. As explained in Section 3.1, the guard must be a Boolean expression. Original : {} The split combinator $f g$ sends an argument $x$ to the pair $ $. Author annotation: macros: splt: kind: semantic_notation display: "\ " name: "split combinator" aliases: meaning: > Combines two functions with the same input into a pair-valued function. Generated embedding enrichment: Notation note: means split combinator. It combines two functions with the same input into a pair-valued function. Original : What is printed by the following program? The program prints the numbers 0 through 9. For a student-facing tutor, the preprocessor may index the exercise statement but exclude the solution, or store the solution in a separate protected index only available for teacher review. This distinction should be explicit in the metadata: {"kind":"exercise", "visibility":"student"} {"kind":"solution", "visibility":"teacher_only"} A retrieved chunk should allow the final answer to refer back to the source: metadata: source_file: chapter4.tex start_line: 120 end_line: 168 section: "4.3" title: "While-loops" page_start: 71 page_end: 74 The AI assistant can then answer: Look again at Section 4.3, especially the explanation of the loop guard on pages 71--72. The key point is that the guard is tested before each iteration. The following conventions make source more useful as a knowledge source: Use meaningful labels, such as |sec:while-loops| rather than |s4x3|. Use semantic macros for recurring concepts and notation. Distinguish typographical macros from semantic macros. Mark exercises, examples, definitions, warnings, and solutions structurally. Provide metadata for pedagogical roles, such as student-visible material and teacher-only solutions. Maintain a notation glossary, either by hand or generated from semantic declarations. Avoid relying on purely visual notation when the concept has a name. Preserve code examples in environments that can be recognized reliably. These conventions do not require abandoning ordinary practice. Rather, they make authorial intent explicit enough for software to preserve it. Retrieval-augmented generation was introduced by Lewis et al.\ as a method for grounding language-model outputs in explicit document collections. Chunking strategy and embedding quality have consistently been identified as key factors in retrieval performance. The present work addresses a gap at the knowledge-extraction layer: obtaining semantically enriched chunks from structured source rather than from rendered PDF or plain text. processing tools.} LaTeXML converts source to XML, HTML, and MathML, targeting browser rendering and accessibility. Pandoc translates between a wide range of document formats, including to Markdown or HTML. Both tools aim at faithful reproduction of the typeset document. Neither targets the use case: chunking by heading structure, breadcrumb heading paths, resolved cross-references as human-readable text, or embedding-text enrichment from author-supplied semantic annotations. The main alternative to processing source is to extract text from the compiled PDF\@. The PDF format was designed for display, not for semantic interchange. Structure such as section numbers, labels, code blocks, and mathematical notation is frequently lost, damaged, or split across page boundaries. source preserves this structure at the cost of requiring the preprocessing step described in this article. The proposed approach is not a replacement for TeX itself. It does not fully expand arbitrary macros, execute conditionals, or reproduce the exact visual document. It also cannot by itself guarantee pedagogical behavior such as never revealing a solution. Such behavior requires additional layers: prompt design, retrieval filtering, answer checking, access control, and evaluation against adversarial student prompts. There are also copyright and licensing considerations. A textbook may be technically processable but not legally usable in an AI system without permission. This is especially important if student prompts and retrieved excerpts are sent to an external API. Single-label | | and | | are resolved via the |@cref| companion entries written by into the file . Three gaps remain, in increasing order of difficulty. |{b}|, which produces output such as ``Sections~3--7'', is straightforward to add once the existing resolution infrastructure is in place: parse both labels, format each, and join with an en-dash. Comma-separated lists require splitting the argument, grouping labels by counter type, sorting numerically within each group, and assembling natural-language output such as ``Sections~2.1 and~3.4'' or ``Figures~3, 5 and~7''. The logic is well defined but has fiddly edge cases---especially when labels span multiple counter types ---and benefits from thorough testing. Custom noun overrides via |{singular}{plural}| require scanning the preamble for these declarations and merging them into the noun table at parse time. The built-in table of~25 counter types handles all standard and environments; custom environments without a | | fall back to a capitalized version of the counter name, which is usually adequate. The |suppress_macros| mechanism in the YAML file offers a partial workaround: users can suppress warnings for unresolved | | calls without fixing the noun table. The pipeline provides two author-control mechanisms for chunk boundaries. |--min-heading-level| suppresses splits at headings shallower than a given level while still tracking them in the breadcrumb path. | | inserts an explicit boundary at any point in the source, independent of heading structure and the token budget. These mechanisms address the most common cases but leave finer-grained control to future work. Token-budget splitting at paragraph boundaries is a coarse fallback: it cannot account for semantic coherence, list structure, or the relative importance of different parts of a long section. A smarter splitter might, for example, prefer to break before a numbered list rather than in the middle of one, or keep a theorem and its proof together. More generally, there is currently no mechanism to mark a passage as unsplittable or to force it to be kept together with the passage that follows. The pipeline currently represents figures only through their captions and | | text. Models that can embed image content directly could use the source or rendered to supplement the textual description. The approach has been demonstrated on a single companion document. Evaluation against real student queries on an actual course textbook---measuring retrieval precision, recall, and downstream answer quality---would establish whether the preprocessing choices generalize and where they need refinement. source is a promising knowledge source for AI systems because it contains structure and intent that are often absent from PDFs. However, it must be preprocessed with care. Cross-references should be resolved, macros should be classified rather than blindly expanded, and semantic annotations should be supplied where typography alone is insufficient. The pipeline uses YAML for human-authored annotations and JSONL for machine-generated chunks. The result is a collection of retrieval units that preserve human-readable notation while enriching the embedded text with the meanings needed for effective retrieval. Unlike rendering-focused tools such as LaTeXML and Pandoc, the pipeline targets the use case directly: the goal is not faithful typographic reproduction but semantically enriched, retrievable chunks. A Python implementation of the pipeline described in this article, together with the source of this paper, is openly available~. Ready-to-run scripts for ingesting chunks into a local vector database and for searching them from an AI coding assistant are included in the repository . The broader goal is AI-friendly : source code that remains suitable for mathematical publishing while also making its semantic and pedagogical structure available to AI systems. *{Acknowledgments} The author thanks ChatGPT~5.5 and Claude~Opus~4.7 for assistance with Python coding and article writing. The author is the creative driver behind all design decisions and bears sole responsibility for the content. {9} P.~Lewis, E.~Perez, A.~Piktus, F.~Petroni, V.~Karpukhin, N.~Goyal, H.~K\"{u}ttler, M.~Lewis, W.~Yih, T.~Rockt\"{a}schel, S.~Riedel, and D.~Kiela. Retrieval-augmented generation for knowledge-intensive {NLP} tasks. In , volume~33, pages 9459--9474, 2020. J.~MacFarlane. Pandoc: A universal document converter. , 2006. B.~R. Miller. {LaTeXML}: A { } to {XML/HTML/MathML} converter. , 2010. T.~Verhoeff. : Preprocessing source for retrieval-augmented generation. , 2026. Chroma. ChromaDB: The AI-native open-source embedding database. , 2022. Anthropic. Claude Code: An agentic coding tool. , 2025. This appendix provides practical guidance for authors who want their source to work well with the preprocessor. The instructions below cover the in-source macro approach. For the external YAML approach and a discussion of when to use each, see Section~. Add the following line to the document preamble. It loads all AI annotation macros as no-ops that are invisible to the typesetter but recognized by the preprocessor. The package defines | |, | |, | |, and | |, all as no-ops during normal compilation. A package option renders them as margin notes and logs them to the file, which can be useful when reviewing annotation coverage. Use meaningful, descriptive labels throughout. ... Avoid opaque labels such as |s4x3|, |fig1|, or |eq17|. Meaningful labels appear verbatim in the label table and in retrieved chunks, so they benefit both the AI system and human readers of the intermediate files. Every figure should have a | | and a | |. Add an | | block after the caption for any figure whose content is not fully captured by the caption alone: {diagram.pdf} Write the description in plain prose that explains what the figure means or shows, not merely what it looks like. Descriptions are especially important for TikZ diagrams, commutative diagrams, automata, and plots, where the caption alone is typically too brief. For every custom macro that introduces a named concept or operator, add a semantic declaration immediately after the definition. | | takes three arguments: the macro, its human-readable name, and a one-sentence explanation. {} { } {split combinator} {Combines two functions with the same input into a pair-valued function.} The preprocessor scans the source for | | declarations and uses them to populate the macro registry. Both approaches---in-source declarations and the YAML file---are interchangeable for the macro registry; YAML annotations override in-source ones on conflicts . Mark exercises, solutions, definitions, and similar units with standard environment names. The preprocessor recognizes and extracts the following as separate chunks: |exercise|, |solution|, |definition|, |theorem|, |lemma|, |proof|, |example|, and |remark|. What is printed by the following program? The program prints the numbers 0 through 9. The |kind| field of the resulting chunk ( |"exercise"|, |"solution"|, etc.) can be used to filter retrieval: a student-facing assistant can exclude |solution| chunks; a teacher-facing assistant can include them. The preprocessor has been tested with Python~3.14. Install it from PyPI: pip install latex-rag-preprocessor Or, within the source repository, use |uv| : uv run python -m latex_rag_preprocessor --help Before running the preprocessor, compile the document to produce the |.aux| file. Two compilation passes are recommended to settle cross-references: pdflatex book.tex pdflatex book.tex latex-rag-preprocessor book.tex --out rag_out Path to the compiled auxiliary file. Defaults to the same stem as the |.tex| file. Path to the YAML annotation file . Defaults to the same stem as the |.tex| file with a |.rag.yaml| extension; silently skipped if the file does not exist. Output directory . Defaults to |rag_out|. Approximate token limit per text chunk (default: 900, estimated as character count divided by~4). Larger values produce fewer, longer chunks; smaller values produce more, shorter ones. The preprocessor discovers and reads all files reachable through | |, | |, and |\@input| automatically; no additional flags are needed for multi-file documents. Create |book.rag.yaml| alongside |book.tex|, or pass it explicitly with |--yaml|. The file has two top-level sections. The |macros| section maps each macro name to its annotation. Only |name| is required; all other fields are optional. macros: splt: name: "split combinator" meaning: > Combines two functions with the same input into a pair-valued function. aliases: - split - triangle operator example_latex: "f \ g" example_text: "f \ g maps x to ." The |pedagogy| section declares visibility constraints on structural environments. The preprocessor injects the |access| value as a |"visibility"| field in the |metadata| of matching chunks; the RAG application is responsible for filtering on that field at query time. pedagogy: visibility: - environment: solution access: teacher_only - environment: proof access: teacher_only Three files are written to the output directory. One JSON object per line. Each object has the fields |id|, |kind|, |heading_path|, |source_file|, |start_line|, |end_line|, |labels|, |markdown|, |embedding_text|, and |metadata|. Text chunks use |kind="text"|; figure chunks use |kind="figure"|; structural chunks use the environment name, e.g.\ |kind="exercise"|; the notation glossary chunk uses |kind="glossary"|. A dictionary mapping each label to its resolved reference number, page, title, and anchor. A human-readable Markdown rendering of the processed source, useful for inspecting the conversion before indexing. The indexing workflow is vector-database-agnostic: Read |chunks.jsonl| line by line. For each chunk, send the |embedding_text| field to an embedding model to obtain a vector. Store the vector together with the chunk's |id|, |kind|, |markdown|, |heading_path|, and remaining metadata in the vector database. The repository includes a ready-to-run script for ChromaDB~ using local sentence-transformer embeddings : pip install chromadb sentence-transformers python3 examples/ingest_chromadb.py rag_out/chunks.jsonl The script is idempotent: re-running it after preprocessing updates upserts changed chunks without rebuilding the collection. An |--openai| flag switches to OpenAI |text-embedding-3-small| for higher retrieval quality at the cost of an API call per chunk. At query time, embed the user's question with the same model, retrieve the most similar chunks by vector distance, and pass their |markdown| text to the language model as context. The |kind| field allows retrieval filtering before or after the vector search: exclude |solution| chunks for student-facing assistants, or boost |exercise| chunks when the question mentions a specific task. For production deployments the same chunk structure transfers directly to Pinecone, Weaviate, pgvector, Qdrant, and similar systems. When using an AI coding assistant such as Claude Code~ while working on a mathematics paper, the assistant can search the preprocessed document base directly from its shell environment. This is particularly useful for notation-heavy source material---a research article or a master's thesis---where PDF extraction would damage the mathematical structure. The setup is a one-time operation: run the preprocessor, then ingest. Once the ChromaDB collection exists, Claude Code can issue queries from its Bash tool without loading any document into the context window: python3 examples/search_chromadb.py "Lyapunov stability" --top-k 3 The script prints the matching chunks with their heading paths, source locations, and full Markdown text---including math notation preserved from the source---as a single tool-call result that lands directly in the conversation context. Two complementary access modes are available. ranks results by embedding similarity and is effective for conceptual queries even when the source uses different terminology. retrieves chunks by their position in the document hierarchy, using a substring match on the stored |heading_path|: python3 examples/search_chromadb.py --location "Chapter 4" python3 examples/search_chromadb.py --location "Chapter 4 > Stability" python3 examples/search_chromadb.py "criterion" --location "Chapter 4" The first form returns all chunks under Chapter~4 in document order---a structural browse without a semantic query. The second narrows to a specific section. The third combines semantic ranking with a location filter. A |--kind| option further restricts retrieval to a specific chunk type, for example |--kind theorem|. No re-ingestion is required to use |--location|: the |heading_path| field is already stored as metadata by the ingest script. For small documents---up to roughly 30\,000 tokens---it may be simpler to give Claude Code the |*.rag.md| Markdown rendering directly. The rendering is self-contained, has cross-references resolved to numbers, and preserves math notation faithfully. The ChromaDB approach scales to larger corpora (a full thesis, a collection of related papers) and avoids saturating the context window when only a few relevant passages are needed. 