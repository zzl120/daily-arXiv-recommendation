 {acmart} }} {International Conference on Multimedia Retrieval}{June 16--19, 2026}{Amsterdam, Netherlands} {{Rabtriever}} } @Geely.com} {Chen et al.} Unlike traditional fact-based retrieval, rationale-based retrieval typically necessitates cross-encoding of query-document pairs using large language models, incurring substantial computational costs. To address this limitation, we propose Rabtriever, which independently encodes queries and documents, while providing comparable cross query-document comprehension capabilities to rerankers. We start from training a LLM-based generative reranker, which puts the document prior to the query and prompts the LLM to generate the relevance score by log probabilities. We then employ it as the teacher of an on-policy distillation framework, with Rabtriever as the student to reconstruct the teacher's contextual-aware query embedding. To achieve this effect, Rabtriever is first initialized from the teacher, with parameters frozen. The Joint-Embedding Predictive Architecture paradigm is then adopted, which integrates a lightweight, trainable predictor between LLM layers and heads, projecting the query embedding into a new hidden space, with the document embedding as the latent vector. JEPA then minimizes the distribution difference between this projected embedding and the teacher embedding. To strengthen the sampling efficiency of on-policy distillation, we also add an auxiliary loss on the reverse KL of LLM logits, to reshape the student's logit distribution. Rabtriever optimizes the teacher's quadratic complexity on the document length to linear, verified both theoretically and empirically. Experiments show that Rabtriever outperforms different retriever baselines across diverse rationale-based tasks, including empathetic conversations and robotic manipulations, with minor accuracy degradation from the reranker. Rabtriever also generalizes well on traditional retrieval benchmarks such as MS MARCO and BEIR, with comparable performance to the best retriever baseline. <ccs2012> <concept> <concept_id>10002951.10003317.10003338.10003341</concept_id> <concept_desc>Information systems~Language models</concept_desc> <concept_significance>500</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010282.10010290</concept_id> <concept_desc>Computing methodologies~Learning from demonstrations</concept_desc> <concept_significance>300</concept_significance> </concept> <concept> <concept_id>10010147.10010178.10010179.10003352</concept_id> <concept_desc>Computing methodologies~Information extraction</concept_desc> <concept_significance>300</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010293.10010319</concept_id> <concept_desc>Computing methodologies~Learning latent representations</concept_desc> <concept_significance>100</concept_significance> </concept> </ccs2012> {Information systems~Language models} {Computing methodologies~Learning from demonstrations} {Computing methodologies~Information extraction} {Computing methodologies~Learning latent representations} {15 April 2026} {25 April 2026} The rapid advancement of Large Language Models has elevated the importance of information retrieval , which identifies the most relevant or helpful documents in response to a textual query, to implement effective knowledge-based question-answering and retrieval-augmented generation . Empowered by deep learning, dense retrieval has emerged as the dominant paradigm of IR. It encodes both queries and documents as dense embeddings within a shared latent space, with relevance typically measured via embedding similarity. Pretrained language models, including BERT, T5, and LLaMA, are widely adopted as the backbone encoders for textual retrievers. These models capture semantic textual similarity by generating embeddings that are closely aligned for semantically related texts . Nevertheless, the growing adoption of RAG systems increasingly demands more sophisticated forms of knowledge comprehension, requiring retrievers to transcend the mere matching of explicit facts toward nuanced, often implicit, knowledge interpretations. This evolution aligns with the knowledge stratification framework proposed by Dense retrieval has emerged as a dominant paradigm of information retrieval , which encodes both queries and documents as dense embeddings within a shared latent space, with relevance typically measured via semantic textual similarity . Nevertheless, the growing adoption of retrieval-augmented generation systems increasingly demands more sophisticated forms of knowledge comprehension, requiring retrievers to move beyond the mere matching of explicit facts toward nuanced, often implicit, interpretations of knowledge . This evolution aligns with the knowledge stratification framework proposed by , which defines four ascending levels of retrieval complexity and depth:\\ $ $ , $ $ \\ $ $ , $ $ \\ While L1 and L2 correspond to conventional , L3 and L4 call for , where the system must comprehend the underlying reasoning, decision logic, or expert knowledge that connects a query to a relevant document. For instance, consider . In such advanced retrieval scenarios, the relationship between query and document often extends far beyond straightforward semantic or paraphrastic similarity, exposing a fundamental limitation of mainstream dense retrievers . Leveraging the extensive domain knowledge and sophisticated reasoning capabilities, LLMs have recently been adopted as backbone architectures for dense retrievers, offering a promising avenue for rationale-based retrieval. LLM-based typically approach this challenge either through prompting strategies or integrating with generative objectives . Nevertheless, they still encode queries and documents independently during inference, lacking a contextualized understanding of their online interactions. In contrast, LLM-based jointly encode the concatenated query–document input, thereby enabling deeper and fine-grained reasoning about rationale-based relevance. However, such rerankers require online encoding of every query–document pair, incurring prohibitive computational overheads, which limit their feasibility for dense retrievers in large-scale retrieval systems. Instead, rerankers are generally deployed as the downstream of retrievers in multi-stage text retrieval . {model_performance_recall_v2.pdf} {model_performance_rt_v2.pdf} {KL_modes.pdf} \\ achieves a similar accuracy to Reranker and a similar speed to Retriever. Bottom: The teacher has two peaks after finetuning; forward KL tends to learn averaged behaviors, while reverse KL helps the student seek the most typical mode .} To resolve this tension, the purpose of this study is to provide a reliable and practical rationale-based retriever, . The core of our approach lies in an on-policy distillation} strategy, which transfers cross-comprehension capability of a reranker into a retriever , reconstructing the reranker's embedding and logit distributions by lightweight cross-computations. To achieve this purpose, we adopt the relevance generation paradigm proposed by , which puts the document the query within the textual input, and produces the relevance score by a generative task. The casual attention mechanism of LLM then naturally ensures the document embeddings are identical between and , such that they can be pre-computed offline. In this paper, we first finetune a based on the LaHoRe framework , which prompts the LLM to answer "True" or "False" via a binary-choice instruction, and yields the relevance score as the relative log-probability ratio on binary answers. After that, we implement the with the same LLM backbone, with parameter initiated from the and set frozen. We then leverage a self-supervised framework called Joint-Embedding Predictive Architecture , in which a trainable is inserted between LLM layers and heads, to project the 's query embedding into a latent space that is implicitly conditioned on the document embedding. We implement the predictor with a 2-layer MLP and an elementary-multiplication operator to ensure it is sufficiently lightweight. Finally, we adopt both feature-based and logit-based paradigms of white-box knowledge distillation , where the first corresponds to the energy-based modeling framework of JEPA, by minimizing MSE between and embeddings; and the second is an auxiliary loss term to reduce the reverse KL between and logits, which ensures the student focus on target tokens instead of spreading across suboptimal vocabularies . On the inference stage, we reconstruct the distribution of the 's context-aware query embedding by inferring the predictor, and compute the final relevance score. Figure visualizes this paradigm, and we name it by tionale-ased Re, or for abbreviation. {paradigm.pdf} {Block diagram visualization of our paradigm. The reranker jointly encodes document and query, with all computations online; { } encodes document and query separately, with the query embedding unconditioned on document . We adopt the JEPA paradigm, where a predictor first converts the query embedding into a new latent space with the document embedding played as the latent control , then the difference energy between $e_{x y}$ and the teacher's query embedding is minimized. The {blue} outline indicates the offline computation; the {cyan} outline indicates lightweight calculation.} Following , we first conduct our experiments on two challenging rationale-based retrieval scenarios: A conversational agent identifying the optimal dialogue strategy from an annotated pool to enhance interaction quality, based on specific conversational goals .\\ A language-grounded robot selecting the most appropriate skill to accomplish a high-level instruction, requiring an understanding of procedural intent and environmental constraints . The results substantiate that { } matches the retrieval accuracy of state-of-the-art rerankers while achieving orders-of-magnitude reduction in inference latency, as highlighted by Figure . To further verify { }'s generalization across standardized large-scale retrieval benchmarks, we then evaluate it by both in-domain retrieval on MS MARCO passage ranking, as well as zero-shot retrieval on the BEIR benchmark. { } performs similarly to the most powerful retriever competitor, indicating that our framework generalizes to traditional retrieval tasks. In summary, the principal contributions of this work are fourfold: . { } first initializes from a pretrained reranker, then inserts a trainable latent projector $$. Instead of predicting the retrieval scores directly, { } employs the EBM paradigm which aims to reconstruct the latent distribution of the cross-encoding reranker. Modules in {blue} indicate the offline computation; modules in {cyan} indicate lightweight calculation. } {llll} Definition & {l}{query $D$, document $D$, embedding $e$, relevance score $s$ and $$ ; encoder $$, and the lightweight calculator $$.} \\ & Bi-encoder & Cross-encoder & \\ Initialize & $^Q, ^D $ & $^{} $ & $ ^{}, ^{}$ \\ Forward & $^{} {^{} } ^Q {^D }$ & $^{} {^{} } ^{} $ & $ { } { } { }$ \\ {*}{Objective} & {*}{$ _{^Q, ^D}}{ }$} & {*}{$ _{^{}, ^{}}}{ }$} & $ _{}{ + }$, $ 0 x y$ \\ We propose an efficient LLM-based retriever, which encodes queries and documents independently to ensure efficiency, while performing similarly to rerankers on rationale-based retrieval.\\ We design a distill paradigm on a generative reranker teacher, and employ JEPA to reconstruct its document-conditioned query embedding, by minimizing the latent distribution discrepancy.\\ We conduct comprehensive evaluations across diverse scenarios, including rationale-based tasks and two general retrieval benchmarks.\\ We provide a formal analysis for time complexities of our { } and standard rerankers, quantitatively verifying the achieved computational efficiency gains. Prevail LLM-based retrievers employ the LLM layers to encode query and document separately, and calculate the relevance score by embedding similarity. For example, RepLLama gets the last-token embedding of Llama and finetunes with the infoNCE loss. E5 further improves the performance by augmenting the training sets with diverse, multilingual synthetic datasets. Promptriever adds the prompt mechanism on RepLLama with performance improvements on instructed retrieval tasks. Recently, hybrid retrievers have been proposed, with a unified architecture on both representative and generative tasks . However, these retrievers are primarily designed for fact-based retrieval, which significantly differs from rationale-based retrieval, the main focus of this paper. Although Llama2Vec tries to bridge the gap by incorporating a simple prompt into the query encoding, explicit comprehension on the cross semantics between query and document is still missing. Within the paradigm of multi-stage text retrievals, the reranker provides a more accurate ranking by jointly encoding the query-document pair, behaving the downstream of the retriever. Its ranking score might be obtained either from the log probability, such as SGPT and rankGPT , or by a linear projection like rankLlama . In this paper, we implement a generative reranker similar to LaHoRe , which uses a relevance generation paradigm similar to , while conducting the finetuning as in . With SoTA performances obtained on several rationale-based retrieval benchmarks, this reranker is then employed as the for further distillation to the efficient retriever, so that the inference speed can be optimized dramatically. In-batch KD distills from a bi-encoder teacher; while RocketQAv2 and AR2 conduct joint training of bi-encoder retriever and cross-encoder reranker. In the contrast, in this paper we distill an LLM-based reranker into a bi-encoder retriever, such that the retrieval performance and efficiency can be balanced. Inspired by MiniLLM , we implement the loss with a reverse KL term, instead of the forward KL, to better shape the student's behavior distributions, effectively leveraging the LLM knowledge on the rationale-based retrieval. Given an encoded input-output pair $ $, a joint-embedding predictive architecture first converts $x$ into the latent space of $y$ by a predictor $ : ^{x} ^{y}$, then minimizes its distributional discrepancy with $y$: , y) where $ $ is nonnegative and can be viewed as the energy between $x$ and $y$, , $ 0$ when $x y$. Subsequently, the JEPA framework belongs to the energy-based model . $$ can be controlled by a prior, non-trainable latent vector $z$. Given a pool of $n$ document candidates $\{ D \}$ and a user query $Q$, the retriever is required to calculate document relevant scores, $\{ s_i \}, 1 i n$, then returns the Top-$k$ $\{ D_i \}$ with respect to the ranking of $s_i$ . According to the annotations in the training set, the ground-truth $s_i$ can be labeled by either positive or negative . To simplify the computational analysis, we assume the average length of $Q$ and $D$ can be represented by $L_Q$ and $L_D$. In the following subsections, we will revisit the paradigms of typical retrievers and rerankers, with corresponding time complexity analysis. We then propose the formulation of { }, with a probabilistic view of training objective. Table summarizes three paradigms. In the table , we use {blue} to denote modules which can be calculated off-the-shelf ; and use {cyan} to denote lightweight calculations. We then formulate different encoders as follows: the classical bi-encoder: s { } ^Q {^D } the cross-encoder: s^{} {^{} } ^{} our { }: & {^{} } { } { } \\ &w.r.t _{} , 0, s^{} in which $e$ is embedding vector, $$ is the encoder, $$ is the score head; the subscript $$ denotes the cross-encoder. {cl} Deficiency & Virtue \\ bi-encoder & s { } ^Q {^D } \\ cross-encoder & s^{} {^{} } ^{} \\ { } & {^{} } { } { } \\ & w.r.t _{} , 0 s^{} \\ {framework.pdf} {Pipeline to develop { }. I: A cross-encodes query and document , then generates the retrieval score . We train it by finetuning an LLM on question-answering samples using the cross-entropy loss . II: We initialize the from , with parameters frozen. A predictor is then inserted between LLM layers and heads, consisting of an MLP and a multiplication operator. III: The predictor converts the $Q$ embedding into a new embedding, with the $D$ embedding as the latent control variable . The predicted $Q$ embedding is then fed into the head, which produces the relevance score for . Both embedding and scoring differences between and are minimized . } Traditional efficient retrievers typically employ a bi-encoder architecture, which encodes $D$ and $Q$ separately: {e_D = ^D } ^d, e_Q = ^Q ^d, {^{} = <e_D, e_Q >} where $^Q$ and $^D$ are the query and document encoders, while $e_D$ and $e_Q$ are the resulted query and document embeddings, in the dimension of $d$; {$ , $} is a lightweight similarity function; The hat on top of $s$ indicates the prediction, and `be' denotes bi-encoder. Encoding of $D$ can be pre-computed off-the-shelf . Assume the encoder is has a quadratic complexity , then the total time complexity of Eq. is {n ^2}}_{} + ^2 + {n }}_{} Although efficient, the bi-encoder framework lacks the joint comprehension of $Q$ and $D$, therefore struggles to retrieve a rationale-based query reasonably . Instead, a cross-encoder can jointly study the semantic or logic correlation between $Q$ and $D$: e^{}_{QD} = ^{} ^d, {^{} = ^{} In practice, $^{}$ can also be implemented with transformers , with also quadratic time complexity. However, it must conduct all the computations online: n ^2 Since $L_D$, $L_Q$, and $n$ are larger than 1, it is obvious that Eq. is larger than Eq. , , the cross-encoder has a much higher time complexity than the bi-encoder. To tradeoff the pros and cons of bi-encoder ) and cross-encoder ), here we propose a new bi-encoder architecture: {e_D = }, e_Q &= , {e_{QD} = }, { = } Similar to the nominal bi-encoder, we separately encoded $e_Q$ and $e_D$ to obtain a high computation efficiency. To obtain a comparable retrieval accuracy to the cross-encoder, however, we insert an extra latent predictor $$ to project $e_{QD}$ into a new latent space, such that $e_{QD} e^{}_{QD}$, , to reconstruct the cross-encoded $e^{}_{QD}$ from Eq. . Note $ $ can be computed offline; as long as we design $$ and $$ to be , we obtain a lower time complexity than the cross-encoder. In the next section, we will deduce an energy-based objective which matches the JEPA formulation ). In this section, we first develop a generative LLM-based retriever, which behaves as the with quadratic time complexity. After that, we provide the methodology of the , which has a bi-encoder architecture with linear online time complexity. For the , we implement the reranker with an LLM backbone, which accepts a textual input and produces next-token probabilities: ^{} ^{} ) &:= ) where $^{}$ are the LLM blocks and $^{}$ are the LLM language heads. According to the LaHoRe framework , we employ an instruction template $$ which puts the document before the query: [ title=The instruction template $$, colback=white, colframe=pink!75!black, colbacktitle=pink, coltitle=black, label=tc:prompt, fonttitle= ] : $\{D\}$ \\ : $\{Q\}$ \\ Can be appropriately responded with ? \\ If the answer is true, choose T ; otherwise, choose F . {l} : $\{D\}$ \\ : $\{Q\}$ \\ Can be appropriately replied with ? \\ If the answer is true, choose T ; otherwise, choose F .\\ The format of the binary-choice question follows the relevance generation paradigm , which encourages LLM to thoroughly consider its preference on the relevance, and outputs either T or F , with corresponding log probabilities. Based on the causal attention mechanism of LLM, = where $ $ indicates the indexed selection on the sequence. This property precludes the need for the $D$-encoder distillation, with only the reconstruction of $Q$ needed. where $prompt$ transforms the retrieval task into a generation task, which contains two parts: \\ \\ where the first part connects $D$ and $Q$ from the sense of semantic relation, and the second part encourages LLM to thoroughly consider its preference on the relevance by augmenting a binary-choice problem , and finally provides the answer $A$. An instruction fine-tuned LLM can constrain its $A$ by , as expected. We construct the QA-format training samples from the original retrieval dataset, by augmenting with all possible negative pairs. The question is constructed by incorporating the query and document into the instruction template $$; while the answer is annotated by either T or F . Then we conduct a standard supervised fine-tuning on Eq. , with the cross-entropy loss solely on the answer. The retrieval task is then converted to a pure generative task, and the fine-tuned LLM becomes the for the subsequent distillation. Figure shows this training mechanism. During the inference stage, the relevance score is deduced from the 's relative positive-negative confidence: ^{} = / + ) This retriever adheres to the online time complexity of the typical : $n ^2$, as indicated in Eq. . This {high time complexity} prevents the application of as an efficient retriever, making the distillation necessary. is then distilled from LaHoRe and achieves the best accuracy among retrievers. indicates the best result, while indicates the second best. Numbers are in percentages.} {ll|c|ccc|ccc|cccc|c} & {c|}{Dataset } & {c|}{ESConv} & {c|}{PsyQA} & {c}{Saycan} & RT $ $ \\ & Method & Size & R@1 & R@3 & MRR & R@1 & R@3 & MRR & R@1 & R@3 & R@5 & MRR & \\ {5mm}{{*}{ {90}{ {2cm}{ Retriever}}}} & TART-Contriever & 110M & 1.4 & 38.5 & 35.1 & 11.3 & 44.2 & 36.5 & 1.2 & 19.2 & 36.5 & 17.5 \\ & E5-large-instruct & 550M & 19.2 & 47.2 & 40.5 & 12.9 & 42.8 & 36.3 & 7.7 & 17.3 & 26.9 & 18.2 \\ & InstructOR XL & 1.5B & 13.7 & 44.3 & 36.4 & 24.6 & 61.2 & 48.1 & 17.3 & 26.9 & 34.6 & 28.4 \\ & RepLLaMA & 7B & 17.4 & 49.1 & 39.7 & 29.6 & 56.0 & 49.6 & 9.6 & 25.0 & 38.4 & 23.6 \\ & Promptriever & 7B & 17.6 & 48.9 & 39.5 & 29.6 & 61.7 & 50.9 & 5.8 & 19.2 & 34.6 & 19.0 \\ & E5-Mistral & 7B & 15.5 & 50.2 & 39.3 & 11.4 & 48.0 & 36.8 & 14.9 & 19.1 & 19.1 & 17.0 \\ & GritLM & 7B & 19.2 & 50.2 & 41.7 & 26.9 & 57.5 & 48.4 & 35.8 & 43.5 & 43.1 & 46.3 \\ & OneGen & 7B & & 52.6 & 41.5 & 31.3 & 46.0 & 46.9 & & & & & \\ & LlaMA2Vec & 7B & 23.9 & & & 34.3 & & 57.5 & 17.3 & 40.4 & 46.2 & 31.1 & 93 \\ & & 7B & 32.8 & 66.4 & 54.7 & & 76.1 & & & & & & \\ {5mm}{{*}{ {90}{ {0.8cm}{ Reranker}}}} & RankLLaMA & 7B & 32.3 & 64.8 & 52.9 & 33.6 & 77.4 & 57.2 & 26.9 & 50.0 & 75.0 & 44.9 \\ & LaHoRe & 7B & 34.4 & 67.1 & 54.8 & 35.6 & 77.6 & 58.2 & 71.2 & 88.5 & 94.2 & 79.8 & 980 \\ As a bi-encoder , we implement both $D$ and $Q$ encoders with the same LLM backbone, with parameters initialized from the , , , $ ^{}$, $ ^{}$ according to Eq. and Eq. . Given ${e_D = } , e_Q = $ $ in Eq. due to the causal mechanism of LLM.}, we then insert a lightweight predictor block $$ between $$ and $$, which produces a new embedding and generates the final retrieval score: &e^{}_Q = {_{z=e_D} }, ^{} { } where $z$ is the latent variable of $$ to impose the grounding impact of $D$ on $e_Q$, and $ $ means vector concatenation. The relevance score $^{}$ is calculated from $^{}$ the same as Eq. . By minimizing the energy between and embedding, the architecture can be considered as the JEPA paradigm: _{}{ , e^{}_{QD})} Compared to the original formulation of JEPA, , Eq. , $e_Q$ plays as the input $x$, $e^{}_{QD}$ plays as the output $y$, and $e_D$ is the latent $z$. Figure visualizes this distillation paradigm. We implement $$ first with a 2-layer MLP with Relu activation, then applying a light-weight elementary multiplication: e^{}_Q = _{ } e_D where $ $ denotes the last embedding of the sequence. During training, only the MLP parameter $ $ is trainable. Although all the modules of $$ seems simple, later experiments show that they are effective enough for the distillation. Apparently, they are lightweight enough to ensure inference efficiency. We replace the next-token prediction in the with the energy-based modeling , by minimizing the energy between and on the latent space. As suggested by JEPA , we implement mean-square error instead of a contrastive loss, which helps improve the convergence and stability of EBM. Besides MSE on $Q$ embedding, we augment an auxiliary reverse KL of w.r.t , which further pushes the to approximate the 's behavior on the logit level: _{ }{} := &^{} + w KL^{} where $w$ is the KL loss weight. Apparently, the energy $$ decays to zero when perfectly reconstructs . In the ablation study, we verify that both $^{}$ and $KL^{}$ are critical for retrieval performance improvement. During the training, the student is actively updating, rollouts all samples while the teacher is frozen and infers $e^{}_Q$ and $^{}$, which corresponds to the on-policy distillation. The architecture, loss, and training mechanism are visualized in Figure . Within the predictor, the time complexity of MLP is $ $ while the dot multiplication is $ $. The entire time complexity of { } then becomes {n ^2}}_{} + ^2 + { + n }}_{} Although slower than the original bi-encoder method has only linear components), apparently, its online time complexity has been much improved from pure cross-encoder methods ). In this section, we first introduce the experimental settings, including baselines, metrics, and implementation details. Then we conduct experiments on rational-based retrieval tasks, followed by detailed ablation and efficiency studies. Finally, we investigate { }'s performance on traditional retrieval benchmarks. We set the maximum of $L_D$ to 32 and conduct to the document part. The rest of the textual input is , with the total window length of $4096$. { } is initialized from Qwen2-7B-Instruct . Training is conducted using LlamaFactory , with 8 A100 GPUs and takes less than 10 hours. The learning rate is $1.0e-6$, the batch size is 16, and the epoch number of epochs The KL loss weight is set to $w=0.25$. Inference is implemented with vLLM with the prefix cache . For the efficiency analysis, the online response time is calculated and recorded for each query-document pair in the test set, and the average is reported. Inspired by , for both and , we construct the dataset by augmenting the positive query-document pairs with all possible negative pairs to leverage the scaling of LLM. We evaluate on different retrieval metrics, including Recall@k , p-MRR@10, as well as nDCG@10 for ranking-annotated benchmarks such as BEIR. In the following contexts, we use R@k to denote Recall@k, while using MRR to abbreviate p-MRR@10. We compare { } to typical retrievers based on different architectures or methodologies, including traditional dense retrievers such as , , , and ; instructional retrievers such as , and ; and multi-task retrievers like and , which integrates the representative and generative tasks. For rerankers, we experiment with , the cross-encoder variant of ; as well as , which behaves as the of distillation in this paper. For ease of comparison, most baselines have an identical parameter size to our implementation, as highlighted in Table~. -Contriever is a task-aware bi-encoder retriever trained with natural language instructions to adapt retrieval behavior across tasks. is an instruction-tuned bi-encoder that jointly encodes task instructions and text to produce task-specific embeddings. is a family of general-purpose bi-encoder embedding models trained with large-scale weakly supervised contrastive learning. Detailed implementations include E5-Mistral and E5-large-instruct. adapts LLaMA into a bi-encoder retriever using parameter-efficient fine-tuning methods such as LoRA. It also has a cross-encoder variant called . adds the prompt mechanism on RepLLama to improve the performance on instructed retrieval. proposes a bi-encoder paradigm that provides a unified framework for both representation learning and generative learning. trains a retriever with generative representational instruction tuning. derives dense representations of query and document by prompting either `SELF' or `NEXT'. In this subsection, we conduct the experiments on two typical scenarios of rational-based retrieval: These datasets focus on emotional support and psychological counseling, where the system responses are annotated with predefined, optimal support strategies. is a multi-turn emotional support dialogue dataset containing 1.2k conversations and 12.7k utterances. Each supporter response is annotated with one of eight fine-grained support strategies, enabling the study of long-horizon strategy planning in empathetic conversations. is a single-turn psychological counseling dataset consisting of 4,012 question--answer pairs. Each response is labeled with one of seven counseling strategies, emphasizing the strategy selection under limited conversational contexts. For both datasets, the query is formalized by prepending the historical context to each user utterance, while all strategy candidates form the pool of documents. One can refer to Table for an example from . has a similar format. as a retrieval dataset. `All Strategies' corresponds to the full document list, and `Strategy' is the ground truth of the retrieved document.} {c|l} All Strategies & {[Question,Restatement or Paraphrasing,Self-disclosure,\ and Reassurance,Providing Suggestions,\ of feelings,Information, Others ]} \\ Query & {} \\ Seriously! What I am scared of now is how to \ another job.} \\ Strategy & {{Reflection of feelings}} \\ Response & { I can feel your pain just by chatting with you.} \\ In this scenario, the robot makes decisions via language grounding, each time choosing the best subskill from all textual skill candidates, in order to achieve its long-term goal. is a large-scale robotic manipulation dataset with approximately 276K trajectories, each consisting of up to 50 steps and covering 551 low-level skills. The dataset supports research on grounding natural language instructions into executable action sequences through hierarchical planning. Table shows retrieval performances on three rationale-based datasets. Although fast, most of retriever baselines fail to capture cross-semantic relations between queries and documents, with generally lower accuracies. Although this issue can be alleviated by integrating a generative subtask, such as GritLM and OneGen; or prompting the LLM for next-sentence inference, as did by LlaMA2Vec, they still perform worse than LaHoRe, the cross-encoding . In contrast, our improves the performance significantly, revealing the effectiveness of distillation. {l|ccc|cccc} {c|}{Dataset } & {c|}{PsyQA} & {c}{SayCan} \\ Method & R@1 & R@3 & MRR & R@1 & R@3 & R@5 & MRR \\ $mul$ & 2.0 & 10.7 & 21.3 & 9.6 & 15.4 & 19.2 & 14.7 \\ $^{}$ & 0.0 & 0.7 & 17.2 & 23.1 & 40.4 & 44.2 & 33.1 \\ $KL^{}$ & 24.1 & 24.9 & 37.3 & 21.2 & 30.8 & 46.2 & 31.8 \\ $KL^{}$ & 28.4 & 55.3 & 48.5 & 46.8 & 54.1 & 62.4 & 59.7 \\ { } & & & & & & & \\ An important research question can be further proposed: effectively trades off the accuracy of reranker and the computational efficiency of bi-encoder retrievers?} From Table , one can observe that { }'s performance is relatively close to , indicating the knowledge is effectively preserved during the distillation. For Saycan, { } even apparently outperforms another reranker, RankLLaMA. Table also compares the average response time of { }, , and the best retriever baseline, LlaMA2Vec, in milliseconds. { } has a similar RT to LlaMA2Vec, while is much faster than , which validates its retrieval efficiency. To verify the necessity of method components, we first attempt to remove the MSE loss , the KL divergence , and the multiplication operator . We also replace the reverse KL with forward KL , to validate the benefit of reverse KL within the on-policy distillation. Table indicates that { } outperforms all these ablations, validating the reasonability of our framework. Especially, completely fails, because the multiplication operation introduces the grounding information of $D$ on $Q$, which is critical to reconstruct the cross-encoding distribution. Removing the reverse KL term, or switching it to the forward KL, resulting in significant performance degradation, verifying that reverse KL significantly helps the focus on the 's critical behaviors , facilitating the convergence by conditioning on 's own distribution. Finally, removing the latent difference loss resulting in varied impacts, where the PsyQA performance is much more affected than SayCan. We suppose the reason is that PsyQA infers much longer context window than SayCan, in which situations distillation on the latent space becomes more critical for model convergence. We deploy { } on an end-to-end RAG pipeline, with { } behaving as the strategy retriever, and Qwen2-70B-Instruct as the response generator. We evaluate this pipeline on the test set of by LLM-as-a-Judge based on GPT4. Pairwise evaluations are conducted between our response and the original responses in the datasets, with the evaluation prompt in . { } finally has a 80.12\ {weight_recall_plot_v2.pdf} \\ {layers_recall_plot_v2.pdf} We test two critical hyperparameters, including the loss weight ratio $w_e$ in Eq. , and the number of MLP layers in the predictor. Based on Fig. , it is evident that $w_e=40$ and $\# layer = 2$ performs the best, which is adopted in the formal experimental setting. As derived in Eq. , our theoretical efficiency analysis indicates that { } achieves a dependency on the document length , in contrast with the dependency of cross-encoder ). To provide the empirical verifications, we provide the statistics of online RTs with respect to different values of $L_D$. In Figure , one can clearly observe the dependency of and the quadratic dependency of on $L_D$, verifying the theoretical conclusion. Figure exhibits the SayCan performance of { } on smaller backbones, including 0.5B and 1.5B. Compared to LlaMA2Vec which has a 7B backbone, { } performs similarly, even with much smaller backbones. This promising result not only verifies the scalability of { }, but also reveals the possibility of further improving the retrieval speed at a tradeoff with performance, by using smaller backbones if necessary. {double_y_lables_20260224.pdf} {model_size_recall_bar_v2.pdf} To verify the generalization capability of { }, we further test it on two famous fact-based retrieval benchmarks, comparing with current SoTA baselines. For in-domain test, we evaluate on MS MARCO passage retrieval tasks, which consists of approximately 500k training examples. MRR@10 are reported on the split. For out-of-domain test, models trained by MS MARCO are evaluated on BEIR , in a zero-shot manner. We test on the 13 subtasks of BEIR which are publicly available, and report the nDCG@10 results. Figure compares the MS MARCO results of different methods. The outperforms all baselines with MRR of 52.4, indicating stronger comprehensions from cross-encoding rerankers. Furthermore, our also obtains an MRR of 49.30, surpassing all retriever baselines . This promising result shows that our distillation methodology can also effectively adapt to generalized retrieval tasks, while improve the inference speed of cross-encoder dramatically. {model_ndcg_bar_v3.pdf} and the second best is . Compared to the best reranker LaHoRe, { } obtains averagely 58.2, and performs the best in 2/13 subtasks and second-best in 4/13 subtasks, which is close to the best retriever competitor . } {ll|ccccccc|cc|c} & {*}{Dataset} & {c|}{Retriever} & {c|}{Reranker} & {c}{} \\ {3-9} {10-11} {12-12} & & {*}{} & {*}{} & {*}{} & {*}{} & {*}{} & {*}{} & {*}{} & {*}{} & {*}{} & {*}{} \\ \\ {3mm}{{*}{ {90}{ {2.5cm}{ Dataset has dev/train set}}}} & DBPedia & 29.9 & 41.3 & 48.9 & 45.2 & 44.8 & 45.9 & 46.6 & 48.3 & 75.5 & \\ & FEVER & 48.1 & 82.8 & 87.8 & 82.8 & 82.9 & 81.3 & 82.7 & 83.9 & & 76.7 \\ & FiQA & 25.1 & 43.8 & 56.6 & 46.6 & 45.0 & 48.5 & & 46.5 & 91.4 & 54.9 \\ & HotpotQA & 56.9 & 71.2 & 75.7 & 69.5 & 68.8 & 70.1 & & 75.3 & 76.9 & 80.0 \\ & NFCorpus & 32.1 & 34.0 & 38.6 & 36.9 & 36.0 & 38.2 & 41.2 & 30.3 & & \\ & Quora & 80.4 & 88.2 & 89.6 & 88.0 & 86.0 & 88.3 & & 85.0 & 88.2 & 88.5 \\ & SciFact & 68.7 & 70.4 & 76.4 & 76.3 & 75.3 & 74.8 & 79.2 & 73.2 & & \\ {5mm}{{*}{ {90}{ {2cm}{ No dev/train set}}}} & Arguana & 36.6 & 54.4 & & 56.7 & 48.6 & 56.5 & 63.2 & 56.0 & 36.8 & 36.8 \\ & Climate-FEVER & 13.6 & 25.7 & & 32.1 & 29.3 & 38.2 & 30.9 & 28.0 & 42.3 & 23.7 \\ & NQ & 28.5 & 64.1 & 63.5 & 62.6 & 63.0 & 64.6 & 70.3 & 66.3 & 92.0 & \\ & SCIDOCS & 15.8 & 17.5 & 16.3 & 19.7 & 16.1 & 18.9 & & 17.8 & 17.1 & 25.0 \\ & TREC-COVID & 62.3 & 71.3 & 87.2 & 84.6 & 83.9 & 86.9 & 74.8 & & 81.6 & 81.8 \\ & Touche-2020 & 33.1 & 23.4 & 26.4 & 32.0 & 34.1 & 34.2 & 27.9 & & 49.9 & 34.3 \\ & \# of best & 0 & 0 & 2 & 0 & 0 & 1 & 3 & 0 & 5 & 2 \\ & \# of 2nd-best & 0 & 0 & 2 & 0 & 0 & 0 & 4 & 2 & 3 & 4 \\ & Average & 40.9 & 53.9 & 59.0 & 56.4 & 54.9 & 57.4 & & 56.6 & 65.8 & 58.2 \\ We finally exhibit the zero-shot results on BEIR in Table . Again, the obtained the best averaged nDCG@10 across all retrievers, performing the best on 5/13 subtasks and the second best on 3/13 subtasks. On the other hand, the obtains a comparable performance to the cross-encoder rerankers, and has the second-best averaged result among all bi-encoder retrievers. Especially, the performs the best on 2/13 subtasks and the second best on 4/13 subtasks, which is close to GritLM, the most competitive bi-encoder baseline. In this paper, we propose an efficient, LLM-based retriever called { }, to address the challenging problem of rationale-based retrieval. We balance the retrieval accuracy and the computational efficiency by implementing a bi-encoder architecture, while providing a retrieval score that considers the cross-semantic information between query and document. We achieve this target by on-policy distilling from a cross-encoder reranker, based on the JEPA framework, with both latent MSE and reverse KL losses implemented. Experiments verify that our { } can handle rationale-based retrieval problems, surpassing both bi-encoder and cross-encoder baselines, and achieve a much faster speed than a pure LLM-based cross-encoding retriever. We further generalize { } to traditional fact-based retrieval tasks, and also observe state-of-the-art performances obtained on both MS MARCO and BEIR benchmarks. Here we present the detailed instruction prompt $$: [ title=The instruction prompt $$, colback=white, colframe=pink!75!black, colbacktitle=pink, coltitle=black, label=tc:prompt, fonttitle= ] : $\{D\}$ \\ : $\{Q\}$ \\ Can be appropriately responded with ? \\ If the answer is true, choose T ; otherwise, choose F . The first part connects $D$ and $Q$ from the sense of semantic relation, and the second part encourages LLM to thoroughly consider its preference on the relevance by augmenting a binary-choice problem, and finally provides the answer. The instruction encourages LLM to thoroughly consider its preference on the relevance, and outputs either T or F , with corresponding log probabilities. where $prompt$ transforms the retrieval task into a generation task, which contains two parts: \\ \\ Table summarizes three paradigms. In the table , we use {blue} to denote modules which can be calculated off-the-shelf ; and use {cyan} to denote lightweight calculations. We then formulate different encoders as follows: the classical dual-encoder: s { } ^Q {^D } the cross-encoder: s^{} {^{} } ^{} our { }: & {^{} } { } \\ & { } \\ &w.r.t \\ & _{} , 0, s^{} in which $e$ is embedding vector, $$ is the encoder, $$ is the score head; the subscript $$ denotes the cross-encoder. One can refer to Table for an example from . has a similar format. {c|l} All Strategies & {[Question,Restatement or Paraphrasing,Self-disclosure,\ and Reassurance,Providing Suggestions,\ of feelings,Information, Others ]} \\ Query & {} \\ Seriously! What I am scared of now is how to \ another job.} \\ Strategy & {{Reflection of feelings}} \\ Response & { I can feel your pain just by chatting with you.} \\ as a retrieval dataset. `All Strategies' corresponds to the full document list, and `Strategy' is the ground truth of the retrieved document.} Here we provide the detailed introductions of the baseline retrievers and rerankers: -Contriever is a task-aware dual-encoder retriever trained with natural language instructions to adapt retrieval behavior across tasks. is an instruction-tuned dual-encoder that jointly encodes task instructions and text to produce task-specific embeddings. is a family of general-purpose dual-encoder embedding models trained with large-scale weakly supervised contrastive learning. Detailed implementations include E5-Mistral and E5-large-instruct. adapts LLaMA into a dual-encoder retriever using parameter-efficient fine-tuning methods such as LoRA. It also has a cross-encoder variant called . adds the prompt mechanism on RepLLama to improve the performance on instructed retrieval. proposes a dual-encoder paradigm that provides a unified framework for both representation learning and generative learning. trains a retriever with generative representational instruction tuning. derives dense representations of query and document by prompting either `SELF' or `NEXT'. As a capable zero-shot learner, LLM can also be employed as a classifier for textual retrieval tasks, especially when the number of documents is relatively limited . In such a case, the researcher may ask the LLM to select the optimal document by including the contents of all the document candidates in the prompt. To verify that our { } is also a competitive solution compared to the Classifier, we include more classifier-based baselines on SayCan, including: Classifier: inference the LLM with the classification prompt in a zero-shot manner. Classifier-2shot: employ the in-context-learning with random two examples. Classifier-CoT: add the chain-of-thought instruction into the prompt, , `Let's think step by step'. Classifier-Self-Refine: integrate the Self-Refine method , which infers the LLM the second time to reflect and refine the answer. Classifier-SFT: conduct the instruction tuning on the Classifier. all of which share the same LLM backbone as us. Recall@1 results are exhibited in Figure , which implies: { } apparently outperforms the zero-shot Classifier, which lacks the domain-specific knowledge. Prompting tricks such as ICL, CoT, or Refine, can improve the Classifier's performance slightly; however, they are still worse than { }. Finetuning can enhance the retrieval performance significantly, which is close to the Retriever and Reranker methods. However, our { } is still better than it, revealing that the LLM may fail to capture the key information given the long prompt with all document contents included, while the retrieval-specific learning is more effective in this experiment. From the aspect of inference latency, Figure further indicates that Classifier has a similar computation overhead as Reranker, both of which need to jointly process both query and document online. Multi-hop methods have even higher latency since they usually need to infer the LLM more than once. In contrast, our { } has a similar speed to Retriever, indicating a good balance between performance and efficiency. {0.55 } {model_performance_recall_20260316.pdf} {0.4 } {model_performance_rt_20260316.pdf} and more textual methods. { } outperforms the zero-shot Classifier apparently, as well as its prompting and finetuning variants. On the other hand, { } has a similar speed to Retriever, while is much faster than Classifier-based methods.} We test two critical hyperparameters, including the weight of the embedding loss $w_e$ ), and the number of MLP layers in the predictor. Based on Figure , it is evident that $w_e=40$ and $\# layer = 2$ performs the best, which is adopted in the formal experimental setting. {weight_recall_plot_v2.pdf} {layers_recall_plot_v2.pdf} We deploy { } on an end-to-end RAG pipeline, with { } behaving as the strategy retriever, and Qwen2-70B-Instruct as the response generator. We evaluate this pipeline on the test set of by LLM-as-a-Judge based on GPT4. Pairwise evaluations are conducted between our response and the original responses in the datasets. { } finally has a 80.12\ We assess the statistical significance by a two-sample t-test on R@1. For each method, we resample its responses 10 times, and calculate the standard deviations and corresponding p-values. Table exhibits these results on ESConv. For both and , the reported p-values are calculated against LlaMA2Vec, the best retriever baseline. They are much below the $0.001$ significance level, indicating that the performance improvements of our { } and relative to LlaMA2Vec are highly statistically significant. {lccc} & & & \\ LlaMA2Vec & 23.9 & 0.023 & - \\ LaHoRe & 34.4 & 0.038 & $< 10^{-5}$\\ & 32.8 & 0.044 & $< 10^{-5}$\\ While { } offers significant gains in efficiency, its performance is inherently constrained by the quality of the cross-encoder teacher used for the JEPA-based distillation process. Additionally, although it is substantially faster than cross-encoders, the integration of a predictor block to model latent variables introduces a modest computational cost compared to traditional, interaction-free bi-encoder architectures. Lastly, as our experiments primarily focused on specific rationale-based tasks such as conversational strategies and robotic skills using relatively short document segments, the framework's scalability to broader, massive-scale general retrieval tasks remains to be fully explored. 