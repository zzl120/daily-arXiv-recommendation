 {acmart} {caption} {ulem} {rotating} {{ }} {{ } } {{ #1}} {{ #1}} {{ #1}} { } { } {ComeIR} -0.8em }}} } } } } } {Ziwei Liu, et al.} Generative recommendation has emerged as a promising paradigm that predicts target items by autoregressively generating their semantic identifiers . Most GR methods follow a quantization-representation-generation pipeline, first assigning each item a SID, then constructing input representations from SID-token embeddings, and finally predicting the target SID through autoregressive generation. Existing item-level representation constructions mainly take two forms: directly merging SID-token embeddings into a compact vector, or enriching item-level representations with external inputs through additional networks. However, these item-level constructors still expose two practical challenges: direct merging may amplify the information loss caused by quantization and ID collision while obscuring SID code relations, whereas external-input-based methods can strengthen item semantics but cannot reliably preserve the SID-structured evidence required for token-level generation. These limitations make representation construction an underexplored bottleneck, leading to two severe problems, the Identity-Structure Preservation Conflict and Input-Output Granularity Mismatch. To this end, we propose , a }nditional }mory Enhanced }tem }epresentation framework that reconstructs SID-token embeddings into item-aware inputs and restores the token granularity during SID decoding. Specifically, MM-guided token scoring adaptively estimates the contribution of each code within the SID, dual-level Engram memory captures intra-item code composition and inter-item transition patterns, and a memory-restoring prediction head reuses the memories during SID decoding. Extensive experiments demonstrate the effectiveness and flexibility of , and further reveal scalable gains from enlarging conditional memory. With recent advances in natural language processing, large language models have demonstrated powerful capabilities in both semantic understanding and sequence modeling~, which has motivated generative recommendation ~. A general GR paradigm first assigns each item a Semantic ID , a tuple of discrete codes, and predicts the next item's SID from historical interactions via autoregressive generation~. As shown in Figure~, this pipeline involves three stages, quantization, representation, and generation. Existing research mainly focus on optimizing the first and last stages, such as learning SID with better quality~ or designing better generators~, leaving the bridge between them, the representation stage, underexplored. Yet this bridge is crucial: since the LLM only observes representations rather than SIDs, this construction determines how much quantized semantics can be preserved for generation. The most common way to construct the representation is to map each SID's code to its corresponding token embedding and flatten all such embeddings into a token embedding sequence~. Specifically, for a user-item interaction with $N$ items and an $L$-digit SID for each item, this flattening yields a GR input sequence of length $L N$, thereby introducing two severe limitations. First, flattening significantly increases the input length, sacrificing efficiency while increasing the risk of attention sink~. Second, flattening obscures the item-wise structure, item boundaries and intra-item code organization, carried by SID, forcing the LLM to recover them from weak positional cues rather than from an item-aware representation. To overcome the aforementioned limitations, an intuitive solution is to construct the item-level representations. Existing works generally follow two lines. One directly merges the SID-token embeddings of each item into a compact vector, typically through a linear projection, reducing the input length from $L N$ to $N$~. The other enriches the item-level representation with external inputs, such as user behavioral signals, and uses an additional network, a context compressor, to transform these heterogeneous signals into compact representations for GR~. Although these strategies appear effective, two challenges still undermine their practical utility. A useful item-level representation should preserve item-specific identity while retaining the structured evidence carried by SIDs. However, these two preservation goals are difficult to satisfy simultaneously under existing item-level constructors. Directly merging SID tokens keeps the input compact, but it may amplify the information loss caused by quantization and ID collision~, while also obscuring the code relations carried by SIDs~. External-input-based methods can inject multi-modal or behavioral contexts to strengthen item semantics, but such signals are not organized according to the structure of SID and therefore cannot reliably preserve the discrete evidence required for SID-based generation. Existing item-level constructors feed compact item vectors into the LLM, whereas the generative objective still requires token-level SID prediction for fine-grained item retrieval~. This forces the input and output sides to operate at different granularities, leaving the decoder to recover SID evidence from representations that have already been compressed or bypassed. {FigureMaking/twoStage.pdf} These observations call for a representation construction that is item-aware, structure-preserving, and compatible with token-level SID generation. To this end, we propose a }nditional }mory Enhanced }tem }epresentation framework, namely , to reconstruct the input of general GR models and restore the token-level granularity during SID decoding. Instead of treating SID tokens as either a flattened sequence, a simply compressed item vector, or external contexts alone, models representation construction as a memory-conditioned transformation from SID-token embeddings to item-aware representations. Specifically, MM-guided Token Scoring uses the original multimodal item embedding to estimate the contribution of each SID code, strengthening item identity during compression. To preserve the SID-structure, we develop a dual-level Engram memory that models intra-item code composition and inter-item transition patterns via two separate sparse memories. A Memory-conditioned Token Merge then integrates the scored SID-token embeddings with the retrieved dual-level memories, injecting organized SID evidence into compact item-level representations. Finally, a Memory-restoring Prediction Head reuses the same memories during SID decoding, bridging the input-output granularity mismatch between item-level inputs and token-level generation. The contributions of this paper can be summarized as follows: We identify representation construction as an underexplored bottleneck in current GR, showing that existing item-level construction strategies face two practical challenges, Identity-Structure Preservation Conflict and Input-Output Granularity Mismatch. We propose , a plug-and-play conditional-memory-enhanced representation framework for generative recommendation that constructs item-aware representations while preserving the SID structures required for decoding. Extensive experiments on three public datasets validate the effectiveness and generality of , while scaling analysis reveals clear log-linear scaling laws, highlighting scalable gains from enlarging the proposed conditional memory. The goal of GR is to directly generate the next item that a user is likely to interact with based on historical interactions. Let $$ and $$ denote the user set and item set, respectively. For each user $u $, the historical interactions are arranged in chronological order as follows: _u = , v_n , n=1, ,N where $N$ is the sequence length. For simplicity, we omit the user index $u$ in the following. In the quantization stage, each item is represented by a fixed-length SID rather than a single atomic ID, and the SID of item $v_n$ is $ _n = $, where $L$ is the length of the SID. During the representation stage, each SID code is converted into a token embedding by combining its frozen codebook embedding with a learnable token embedding. For the $ $-th code of item $v_n$, _{c_n^ } = _{E} [ ^{B}_{c_n^ }; ^{T}_{c_n^ } ] where $^{B}_{c_n^ }$ is the frozen codebook embedding, $^{T}_{c_n^ }$ is a learnable token embedding, and $_{E}$ projects their concatenation into the LLM hidden space. The SID-level representation of item $v_n$ is $ _{n}^{S} = [ _{c_n^1}, , _{c_n^L} ]$. We can then write the representation construction as $ _{n}^{I} = f ( _{n}^{S}, _{n} )$, and formulate the final GR input as $ ^{I} = [ _{1}^{I}, , _{N}^{I} ]$. Here, $_{n}$ denotes optional external inputs that provide additional information beyond the SID tokens. When no external inputs are introduced, $_{n}$ is omitted: for flattening, $f $ reduces to a simple concatenation, while for token merging, $f $ denotes a linear layer. For our ~, we also omit $_{n}$ and leverage the existing multi-modal embedding and conditional memories to enhance our representation. Finally, in the generation stage, a generative model $ $ autoregressively predicts the SID of the next item based on the constructed item-level representations, which can be formulated as: P = _{ =1}^{L} P {FigureMaking/ComeIR.pdf} The overview of our proposed framework is illustrated in Figure~. Given the SID-level representations $_{n}^{S}$ defined in Section~, our target is to model a fine-grained $f $ that maps each SID into an item-level representation while preserving the structured SID evidence needed for autoregressive decoding. Accordingly, instead of directly feeding all $L N$ SID tokens into the LLM, ~reconstructs them into $N$ memory-conditioned item representations. Specifically, our representation construction contains three coupled components. First, {MM-guided Token Scoring} reuses the cached multimodal item embeddings $= $, from which the SIDs are produced, to estimate the contribution of each code-layer embedding within an SID\@. This cached embedding acts as an identity query tied to SID construction, rather than as an external extra information. Meanwhile, {Dual-level Engram Memory} constructs two sparse memories over SID code patterns to retrieve intra-item code-composition evidence and inter-item transition evidence; the scored token embeddings and the two memory vectors are then aggregated via Memory-conditioned Token Merge to construct the final item-level representation sequence $^{I}$. Finally, in the generation stage, the constructed item-level representation sequence is fed into the LLM to produce $_u$. For layer-wise SID prediction, the {Memory-restoring Prediction Head} combines $_u$ with the intra-item and inter-item Engram contexts to reuse the token-level SID relations during decoding. As discussed in the Identity-Structure Preservation Conflict, item-level compression should retain item-specific identity without discarding SID-structured evidence. We first address the identity side through . The guidance comes from the same multimodal embedding used by the quantizer to produce the item's SID, so it acts as an identity query for historical items rather than an extra information source. Specifically, for item $v_n$, let $_{n} ^{d_{mm}}$ denote this cached multimodal embedding. We first project it into the same dimension as the SID-token embeddings by $_n=_{}_{n}$, where $_{} ^{d d_{mm}}$. Then, we calculate the contribution of each code in $_{n}^{S}$ through cross attention: _{n}^ = _n^{ }_{c_n^ }/ ) }{ _{r=1}^{L} }, _{n}^{0} = _{ =1}^{L} _{n}^ _{c_n^ } Here, $_{n}^{0}$ is the MM-guided item context used to query memory and initialize item summary before level-wise memory injection. In this way, the representation constructor emphasizes identity-relevant codes rather than treating all codes in each item's SID equally. To preserve the SID-structure side, we model two structures carried by SIDs. Within one item, an SID is an ordered code sequence $ $, where later codes refine the prefix formed by previous codes. Across a user history, the level-$ $ prefixes form a discrete sequence $ $, whose local suffixes describe recurring preference transitions. These two forms are naturally suited to Engram-style memory: both are discrete patterns that can be repeatedly addressed, stored, and reused. Inspired by the recent success of Engram in decoupling static memory from the LLM transformer block~, we introduce . Different from the original Engram module, which is inserted into Transformer blocks, our memory is placed at the representation interface: it retrieves SID-pattern evidence before item-level inputs are built and restores the same type of evidence during token-level prediction. In this section, we will first define a general Engram read function, then instantiate it for intra-item code composition and inter-item transition modeling. The general engram receives two inputs, a discrete code pattern $$ and a contextual query $$, which respectively represent {what} to look up and when the look-up evidence should be trusted. To be more specific, we divide the whole process into two steps: To perform the N-gram hash lookup, we first extract suffix N-gram keys of multiple orders from $= $. An order-$o$ key consists of the last $o$ elements $ $ from $$, where each element is defined by the memory-specific pattern introduced below. We denote the set of orders at level $ $ as $_{ }$. Each order-$o$ key is hashed into $K$ independent sparse embedding tables via deterministic multi-head hashing~, and the $K$ vectors are concatenated into one address $_{ ,o} ^{d_m}$. Since the same discrete pattern is deterministically mapped to the same table rows across training examples, the corresponding retrieved address can serve as a reusable memory slot for that recurring SID pattern. Considering the N-gram code combinations are static, which inherently lack contextual adaptability and may suffer from noise due to hash collisions or polysemy~, a context-aware gate is needed to filter the noise according to the corresponding context. Specifically, we devise a scalar gate $ _{ ,o} $ to score the compatibility between $$ and each address . Finally, the Engram memory is calculated by aggregating all gated addresses as : _{ } ( , ) = ( _{o _{ }} _{ ,o} _{V, ,o} _{ ,o} ) Here $_{V, ,o}$ projects each address into memory evidence, and $ $ normalizes the sum. Table capacities and hashing details are presented in Appendix~. The intra-item Engram memorizes how codes are composed within a single SID. For example, an SID such as $ $, the pattern $ $ describes how the second code specializes the first-level code. This conditional-prefix form matches the suffix lookup above because the key should emphasize the latest refinement under its preceding SID context. Formally, for item $v_n$, define $_{n}^{< }= $ and $_{n}^{ }= $. Since the first code has no preceding prefix, intra-item patterns start from level $ =2$. At each level, we store the conditional pattern $_{n, }^{S}= $, which records how the $ $-th code refines its preceding prefix. Using $_{n}^{0}$ as the query, we read the conditional evidence at the same level $ $ and compose accumulated reads into a single intra-item memory as follows: _{n, }^{S} = _{ } ( _{n}^{0}, _{n, }^{S} ), _{n, }^{S} = _{C, }^{S} [ _{n,2}^{S}; ; _{n, }^{S} ] + _{C, }^{S}, =2, ,L The adapter $ $ compresses the collected reads into compact SID-composition evidence $_{n, }^{S}$ for Token Merge. Further details are in Appendix~. The inter-item Engram captures transition patterns across user-item interactions at multiple granularities. Specifically, for each code level $ $, we arrange all historical $ $-level SID prefixes into a sequence $^{ }= $. Shallow prefixes track broad preference changes, different categories; deeper prefixes capture finer transitions, preference towards specific items. The sequence rearrangement only changes the unit being read by the Engram: each element in $^{ }$ is the prefix $_{a}^{ }$ of an interacted item, and the chronological order is still preserved. The transition pattern for item $v_n$ can be then defined as $ _{n, }^{T} = _{ _{ }} ( ^{ }, n )$, where the suffix keeps the most recent level-$ $ prefix transitions before and including position $n$. Detailed constructions are presented in Equation~. Since this Engram captures inter-item transition, a single item's context is insufficient. We therefore construct a transition-aware query $_{n, }^{T}$ from a local window of MM-guided item contexts and retrieve the inter-item memory as: $ _{n, }^{T} = _{ } ( _{n, }^{T}, _{n, }^{T} )$. For a specific item $v_n$, after obtaining the MM-guided item context $_{n}^{0}$ and dual-level memories, $_{n, }^{S}$ and $_{n, }^{T}$ at each code level $ $, we need to transform multiple signals into one item-level input. However, a naive merge collapses all code embeddings at once and cannot recognize item identity or the SID structure. Unlike external-input-based constructors that enrich item vectors without explicitly organizing SID evidence, we propose the that performs level-wise gated updates to ensure the retrieved memories are beneficial according to the current item context. Specifically, at each level $ =1, ,L$, the dual-level memories are stacked into $_{n, }= $ . A scalar gate $ _{n, } $ then measures whether the current summary $_{n}^{ -1}$ is compatible with the memory $_{n, }$, and the summary is updated via a gated residual: _{n, } = ( { }^{ } + b_{ }^{} ), _{n}^{ } = _{n}^{ -1} + _{n, } _{V, }^{}_{n, } After level $L$, we set $_{n}^{I}= $ and collect $^{I}= $ as the final item-level GR input. While item-level input improves efficiency, the target item still needs to be generated as a token-level SID during decoding for fine-grained item retrieval~. Consequently, we design the , which restores intra-item and inter-item memory during SID decoding. Specifically, when predicting the $ $-th code, we denote the generated prefix as $_{N+1}^{< }$ and the candidate code as $x$. The head restores memory evidence in a layer-dependent manner: 1$).} No intra-item prefix exists yet, so we set the intra-item evidence to $}_{N+1,1}^{S} =$ and rely solely on inter-item transition evidence. The inter-item memory is retrieved by appending the candidate $ $ to the historical prefix sequence and reading from the Engram: _{1} = _{T,1}\, _{1} \! ( _{u,1}^{},\; _{N+1,1}^{T} ) where $_{u, }^{}$ is a transition query constructed from $_u$ and the recent item contexts. 2$).} Once a partial prefix $_{N+1}^{< }$ is available, the head first reads intra-item evidence from the conditional pattern $ $, which captures how code $x$ refines the generated prefix within the target SID. This evidence is then supplemented by inter-item transition evidence: _{ } = _{S, }\, _{ } \! ( _{u},\; ) + _{T, }\, _{ } \! ( _{u, }^{},\; _{N+1, }^{T} ) In both cases, $_{ } $ is fused with the user state $_u$ and the candidate embedding $_{x}$ to produce a logit for $x$, which can be formulated as: _{ } = ( _{H, }_{u} + _{C, }_{x} + _{ } ), _{ } = _{ }^{ }_{ } The layer-wise probability is normalized over the set of catalog-valid codes $_{ } $, determined by a prefix tree built from all SIDs in the catalog: P ( c_{N+1}^{ }=x | ^{I}, _{N+1}^{< }; ) = ) }{ _{y _{ } } ) } More details about the implementations on different architectures can be found in Appendix~. Given the ground-truth next-item SID $_{N+1}$, we train with teacher forcing and token-level cross-entropy over SID layers. At layer $ $, the ground-truth prefix $_{N+1}^{< }$ is provided to the prediction head, so the model learns to score the next code under valid historical and prefix-conditioned memory evidence. The training loss can then be defined as: _{} = - _{u } _{ =1}^{L} P ( c_{N+1}^{ } | ^{I}, _{N+1}^{< }; ) During inference, first constructs $^{I}$ and obtains $_{u}$ from the LLM. It then performs beam search over SID layers, while each candidate is scored by restoring the same intra-item and inter-item memories used in training. Detailed settings for different architectures, normal GR and NEZHA, can be found in Appendix~. There are three public datasets applied for evaluation, Yelp, Amazon Industrial, and Amazon Instrument. More details can be found in the {Appendix~}. . We construct our experiments leveraging different quantization mechanisms, RQ-VAE~ and RQ-Kmeans~, different LLM backbones, Qwen3-0.6B and LLaMA3-1B, and different architectures, normal GR and NEZHA~. The '' denotes token merging, following the settings of previous work~, which naively concatenates all code embeddings for each item and leverages a linear layer to project back to the LLM's hidden size. We adopt the commonly used metrics, and , truncated at $K$, where $K {5,10}$. For efficiency, we also report the generation latency in milliseconds per batch, following the previous work~. For general settings, the intra-item and inter-item Engram tables, we set a base of 128 and 16, respectively. The two scaling parameters for enlarging the table scale are 1.0 and 2.0 by default. Other details can be found in the {Appendix~ {sec:implementation_details}}. values are the best, and ``*'' marks significant gains over the matched architecture.} =0.5mm { } as the LLM backbone.} {!}{ {cc|cccc|cccc|cccc} {*}{} & {*}{} & {c|}{} & {c|}{} & {c}{} \\ {3-6} {7-10} {11-14} & & {H@5} & {H@10} & {N@5} & {N@10} & {H@5} & {H@10} & {N@5} & {N@10} & {H@5} & {H@10} & {N@5} & {N@10} \\ {*}{} & & 0.0296 & 0.0474 & 0.0181 & 0.0243 & 0.0979 & 0.1275 & 0.0745 & 0.0844 & 0.0804 & 0.0982 & 0.0688 & 0.0735 \\ & & 0.0292 & 0.0470 & 0.0177 & 0.0239 & 0.0976 & 0.1271 & 0.0742 & 0.0841 & 0.0788 & 0.0966 & 0.0672 & 0.0719 \\ & } & & & & & & & & & & & & \\ & & 0.0294 & 0.0473 & 0.0176 & 0.0241 & 0.0981 & 0.1278 & 0.0749 & 0.0847 & 0.0801 & 0.0977 & 0.0684 & 0.0730 \\ & & 0.0287 & 0.0466 & 0.0169 & 0.0234 & 0.0977 & 0.1274 & 0.0746 & 0.0844 & 0.0784 & 0.0960 & 0.0667 & 0.0713 \\ & } & & & & & & & & & & & & \\ {1-14} {*}{} & & 0.0300 & 0.0481 & 0.0192 & 0.0251 & 0.1005 & 0.1299 & 0.0771 & 0.0870 & 0.0817 & 0.0999 & 0.0705 & 0.0754 \\ & & 0.0295 & 0.0476 & 0.0187 & 0.0246 & 0.1001 & 0.1295 & 0.0767 & 0.0867 & 0.0801 & 0.0983 & 0.0689 & 0.0738 \\ & } & & & & & & & & & & & & \\ & & 0.0288 & 0.0476 & 0.0187 & 0.0248 & 0.1008 & 0.1299 & 0.0775 & 0.0872 & 0.0814 & 0.0996 & 0.0702 & 0.0753 \\ & & 0.0282 & 0.0470 & 0.0181 & 0.0242 & 0.1004 & 0.1295 & 0.0771 & 0.0869 & 0.0797 & 0.0979 & 0.0685 & 0.0736 \\ & } & & & & & & & & & & & & \\ } { } as the LLM backbone.} {!}{ {cc|cccc|cccc|cccc} {*}{} & {*}{} & {c|}{} & {c|}{} & {c}{} \\ {3-6} {7-10} {11-14} & & {H@5} & {H@10} & {N@5} & {N@10} & {H@5} & {H@10} & {N@5} & {N@10} & {H@5} & {H@10} & {N@5} & {N@10} \\ {*}{} & & 0.0216 & 0.0373 & 0.0131 & 0.0182 & 0.0937 & 0.1223 & 0.0701 & 0.0794 & 0.0748 & 0.0932 & 0.0646 & 0.0696 \\ & & 0.0205 & 0.0362 & 0.0120 & 0.0171 & 0.0932 & 0.1218 & 0.0696 & 0.0788 & 0.0731 & 0.0915 & 0.0629 & 0.0679 \\ & } & & & & & & & & & & & & \\ & & 0.0212 & 0.0372 & 0.0126 & 0.0177 & 0.0941 & 0.1223 & 0.0703 & 0.0798 & 0.0747 & 0.0929 & 0.0644 & 0.0693 \\ & & 0.0200 & 0.0360 & 0.0114 & 0.0165 & 0.0935 & 0.1217 & 0.0697 & 0.0792 & 0.0729 & 0.0911 & 0.0626 & 0.0675 \\ & } & & & & & & & & & & & & \\ {1-14} {*}{} & & 0.0225 & 0.0393 & 0.0150 & 0.0209 & 0.0961 & 0.1243 & 0.0722 & 0.0821 & 0.0757 & 0.0949 & 0.0662 & 0.0716 \\ & & 0.0216 & 0.0384 & 0.0141 & 0.0200 & 0.0958 & 0.1240 & 0.0719 & 0.0818 & 0.0739 & 0.0931 & 0.0644 & 0.0698 \\ & } & & & & & & & & & & & & \\ & & 0.0218 & 0.0387 & 0.0149 & 0.0202 & 0.0962 & 0.1248 & 0.0727 & 0.0826 & 0.0754 & 0.0944 & 0.0660 & 0.0711 \\ & & 0.0208 & 0.0377 & 0.0139 & 0.0192 & 0.0959 & 0.1245 & 0.0724 & 0.0823 & 0.0736 & 0.0926 & 0.0642 & 0.0693 \\ & } & & & & & & & & & & & & \\ } To validate the effectiveness and flexibility of the proposed , we compare its performance across various quantization mechanisms, LLM backbones, and architectures, both with the general setting and with token merging. As shown in Table~, ~demonstrates superior recommendation performance and remarkable robustness. By integrating ~ into current architectures , significant gains are consistently achieved across all commonly adopted quantization mechanisms, RQ-VAE and RQ-KMeans, and both backbones. Moreover, the comparison with further shows that simply compression is insufficient; memory-conditioned construction is necessary to preserve fine-grained code evidence during item-level compression. Specifically, on the Yelp dataset with RQ-VAE, ~yields its largest average improvements over different architectures: MM-Scoring replaces original module with mean pooling, and Mem. Merge replaces the original module with a linear layer. Other variants remove intra-item or inter-item memory from the encoding or decoding .} {!}{ {ccccc} & & & & \\ & & & & \\ MM-scoring &0.0294 &0.0517 &0.0190 &0.0254 \\ -intra &0.0294 &0.0501 &0.0193 &0.0257 \\ -inter &0.0291 &0.0505 &0.0188 &0.0251 \\ Mem. Merge &0.0298 &0.0512 &0.0193 &0.0259 \\ -intra & 0.0297 &0.0509 &0.0194 &0.0244 \\ -inter &0.0293 &0.0516 &0.0185 &0.0249 \\ } The results of the ablation study are shown in Table~. Firstly, removing MM-guided Token Scoring leads to consistent drops, with H@5 and N@5 decreasing by 3.61\ {font=small,labelfont=bf,justification=centering} { } {FigureMaking/IntraScaling.pdf} { } {FigureMaking/InterScaling.pdf} Following the protocols in previous work~, we also examine whether the two sparse memories, intra-item and inter-item engrams, compile to a similar scaling law. The results are plotted in Figure~. For both memories, we observe a clear power-law scaling across different scaling parameters, consistent with previous findings~. These results confirm the scalability of the proposed dual-level Engram memory: increasing sparse capacity reduces hash collisions and continuously improves the quality of our representation construction. More details are provided in Appendix~. {4pt} {1.15} {!}{ {cc|cc|cc} {*}{} & {*}{} & {c|}{} & {c}{} \\ {3-4} {5-6} & & {Normal GR} & {NEZHA} & {Normal GR} & {NEZHA} \\ & & {LT} $ $ & {LT} $ $ & {LT} $ $ & {LT} $ $ \\ {*}{} & Flattened & 23.673 & 1.126 & 35.483 & 1.704 \\ & } & & & & \\ {*}{} & Flattened & 21.406 & 1.018 & 32.109 & 1.538 \\ & } & & & & \\ {*}{} & Flattened & 26.375 & 1.253 & 39.563 & 1.894 \\ & } & & & & \\ } As previously discussed, the item-level representation significantly reduces the length for GR input from $N L$ to $N$. To further estimate that, we report the inference latency of ~in Table~. The results show that, by optimizing the representation stage rather than quantization or generation, our proposed ~ achieves a remarkable $ $ average speedup through length reduction, even with the efficient NEZHA architecture. This phenomenon further validates that employing ~in the current GR pipeline can not only improve effectiveness but also efficiency. Generative recommendation reformulates item retrieval as autoregressive identifier generation conditioned on user histories. P5~ casts recommendation tasks into language processing, and TIGER~ establishes the representative Semantic ID pipeline, where each item is encoded as SID, and the LLM directly generates the next item. Following the quantization-generation pipeline~, a line of research improve SID construction with content, collaborative signals, or task-aware tokenization~, while others study SID-language alignment, long-SID generation, inductive decoding, and SID redistribution~. While these works advance SID and generator design, studies a less-explored representation bridge that reconstructs SID-token embeddings into item-aware inputs and restores them for token-level prediction. Memory-based modeling reuses recurring patterns through explicit or implicit storage. Classical $n$-gram language models store local statistics~, neural $n$-gram and hash embeddings encode reusable evidence as compact vectors~, and modern language models exhibit memory-like behavior through key-value feed-forward layers or retrieval-augmented generation~. Conditional computation and Mixture-of-Experts improve scaling by sparse activation~, while Engram~ introduces conditional memory as sparse lookup over $n$-gram patterns, with follow-up work studying its serving and indexing properties~. Our use of conditional memory differs in both object and role: instead of storing linguistic patterns, builds dual-level memories over two different SID patterns, capturing intra-item code composition and inter-item code transitions to preserve SID structure during item-level compression and restore token-level granularities during generation. In this paper, we identify representation construction as a key bottleneck in current GR pipelines, where existing item-level constructors face an identity-structure preservation conflict and an input-output granularity mismatch. Consequently, we propose a conditional memory-enhanced item representation framework that uses MM-guided token scoring to strengthen item identity, dual-level Engram memories to preserve SID structure, and memory-conditioned token merging to construct compact item-level inputs. A memory-restoring prediction head further reuses these memories during SID decoding, bridging item-level inputs with token-level generation. Extensive experiments demonstrate the effectiveness, flexibility, and scalability of . Due to the main text length limitation, this section complements Section~ with implementation details that are not fully expanded in the main text. The supplement focuses on two parts. Sections~-- provide the implementation details of Dual-level Engram Memory, including hash-table addressing, context-aware gating, and memory-specific discrete units. Section~ details Memory-restoring Prediction Head, including candidate-specific memory restoration, catalog-valid decoding, and architecture-specific decoding states. {FigureMaking/engram.pdf} The main text defines the Engram read operator $_{ } $. Here we only expand how an abstract suffix key is mapped into sparse tables. We use $K$ for the number of hash heads in a memory instance, $d_m$ for the concatenated Engram address dimension, and $d$ for the LLM hidden size used by the SID-token embeddings in Equation~. Intra-item and inter-item memories use separate tables, even though they share the same addressing form. Given $= $, the level-$ $ memory extracts ending N-gram keys from the lookup pattern. In intra-item memory, each $p_t$ is a SID code. In inter-item memory, each $p_t$ is a SID prefix $_{n}^{ }$ and is treated as one discrete unit. For order $o$, the ending key is g_o = }, ,p_T). The set of N-gram orders used at level $ $ is denoted as $_{ }$. Each key is mapped to $K$ sparse tables by deterministic hash functions, and the retrieved vectors are concatenated into an address: _{ ,o} ( ) = _{k=1}^{K} _{ ,o,k} [ _{ ,o,k} ( g_o ( ) ) ], where $ $ denotes vector concatenation, $ _{ ,o,k} $ is the $k$-th hash function, and $_{ ,o,k} ^{H_{ ,o,k} d_m/K}$ is the corresponding learnable sparse table with $H_{ ,o,k}$ buckets. The address $_{ ,o} ^{d_m}$ depends only on the discrete pattern and is therefore filtered by the context-aware gate used in Equation~: _{ ,o} = ( _{Q, } )}^{ } ) }{} ). The final read is the same operator as Equation~, written here with the explicit hash-table address: _{ } ( , ) = ( _{o _{ }} _{ ,o} _{V, ,o} _{ ,o} ). Hash table capacities and the concrete hash function are specified in Section~. Section~ already gives the intra-item read and aggregation equations. The only implementation convention needed here is the discrete lookup pattern. Since the first code has no previous prefix, intra-item patterns start from the second code: _{n, }^{S} = , =2, ,L, The encoder-side read uses $_{n}^{0}$ as the query, as in Equation~. The adapter $ $ in that equation only appears during representation construction; during decoding, Section~ directly queries the same intra-item table with $ $. Section~ defines the inter-item memory used by the representation constructor. For implementation, the important point is that one inter-item unit is an encoded SID prefix, not a raw item ID. At each level $ $, these units form ^{ } = ( _{1}^{ }, _{2}^{ }, , _{N}^{ } ), =1, ,L. For a sequence $= $, define its recent suffix ending at position $t$ as _{o} = }, ,x_t). The representation-side transition pattern is _{n, }^{T} = _{ _{ }} ( ^{ }, n ), where $ _{ } _{ }$ ensures that all N-gram orders can be extracted. Since transitions depend on more than the current item alone, we compute the transition-aware query from a local window $_{n}=\{a a n\}$ of MM-guided item contexts: _{n, }^{T} = _{a _{n}} _{a, }_{a}^{0}, _{a, } = _{a _{n}} ( _{Q, }^{}_{n}^{0} )}^{ } }{} ). Here $ _{a, }$ weights item position $a$'s contribution to the level-$ $ transition query, and $_{Q, }^{},_{K, }^{}$ are level-specific projections. The prediction head uses the same Engram tables during decoding. After the generator consumes $^{I}$, let $_{ }$ denote the architecture-specific state supplied to the prediction head at SID layer $ $. In the normal GR architecture, $_{ }=_{u}$ for all layers; in the NEZHA-style architecture, $_{ }$ is replaced by the layer-specific state defined in Section~. This notation keeps the memory-restoring equations shared by both architectures. When predicting the $ $-th code, the current generated prefix is $_{N+1}^{< }$ and a candidate code is $x$. The corresponding level-$ $ candidate prefix is _{N+1}^{ } = , _{N+1}^{ 1} = . For intra-item decoding, no intra-item memory is available at level $1$. For $ >1$, the candidate code is retrieved together with the generated prefix: }_{N+1, }^{S} ( x ) = , & =1,\\ _{ } ( _{ }, ), & >1. For inter-item decoding, we append the candidate prefix to the historical prefix sequence: _{+}^{ } = . The candidate transition pattern is $_{N+1, }^{T} =_{ _{ }} ,N+1)$. Since the target item is unknown, the transition query is computed from the architecture-specific decoding state and the recent historical item contexts: _{a, } = _{a _{N}} ( _{Q, }^{}_{ } )}^{ } }{} ), _{u, }^{} = _{a _{N}} _{a, } _{a}^{0}. The inter-item memory for candidate $x$ is _{N+1, }^{T} ( x ) = _{ } ( _{u, }^{}, _{N+1, }^{T} ( x ) ). These memories check two conditions for candidate $x$: whether it is compatible with the generated SID prefix, and whether it is compatible with the recent transition context. The memory term used in Equations~ and~ is _{ } = _{T,1}_{N+1,1}^{T} , & =1,\\ _{S, }}_{N+1, }^{S} + _{T, }_{N+1, }^{T} , & >1. The candidate embedding $_{x}$ in Equation~ is the SID-token embedding of candidate code $x$ at layer $ $, defined in Equation~. The logit therefore combines three signals: the architecture-specific decoding state $_{ }$, the candidate code embedding $_{x}$, and the memory evidence $_{ } $. For the main-text equations, $_{ }$ reduces to $_{u}$ unless the NEZHA-style variant is used. Let $$ denote the prefix tree built from all item SIDs in the catalog. At level $ $, the valid candidate set $_{ } $ contains the children of the current prefix in $$. The probability in Equation~ is normalized only over this set. During beam search, each partial SID keeps its accumulated score s_{ } = _{t=1}^{ } P ( c_{N+1}^{t} | ^{I}, _{N+1}^{<t}; ). Completed SIDs are mapped back to catalog items. If multiple valid SIDs map to the same item due to SID collisions, the item is ranked by its best valid SID score. In this section, we present the detailed statistics of the selected public datasets, Yelp, Amazon Industrial, and Amazon Instrument. For data preprocessing, we follow previous sequential recommendation and generative retrieval settings~. The statistics after preprocessing are presented in Table~. {ccccc} Dataset & \# Users & \# Items & Sparsity & Avg.length \\ Yelp & 15,720 & 11,383 & 99.89\ Industrial & 27,190 & 28,461 & 99.92\ Instrument & 40,644 & 30,676 & 99.97\ The hardware used in all experiments is an AMD EPYC 9745 platform with 2 NVIDIA RTX PRO 6000 GPUs, while the basic software requirements are Python 3.11 and PyTorch 2.10. Next, we detail the implementation of the quantization, representation, and generation stages for the adopted baselines. For , the multimodal item embedding $_{n}$ is the cached input used by the quantizer to produce SIDs, and we do not introduce an additional external-input branch $_{n}$ in the representation constructor. We adopt RQ-VAE~ to obtain hierarchical SIDs. To keep the notation consistent with the main text, the multimodal item embedding of item $v_n$ is denoted as $_{n}$. The encoder maps it into a latent vector $_n=_{Q} $. Starting from $_{n,0}=_n$, the $ $-th code is assigned by residual quantization: c_n^ &= _{j \{1, ,C_{ }\}} \| _{n, -1} - _{ ,j} \|_2^2,\\ _{n, } &= _{n, -1} - _{ ,c_n^ }, }_n = _{ =1}^{L} _{ ,c_n^ }. Here $C_{ }$ is the size of the $ $-th codebook, $_{ ,j}$ is its $j$-th vector, $_{Q}$ and $_{Q}$ are the quantizer encoder and decoder, and $_{n, }$ is the residual after the first $ $ code assignments. After quantization, we use $^{B}_{c_n^ }=_{ ,c_n^ }$ as the frozen codebook embedding in Equation~. The quantizer is trained with reconstruction and residual commitment losses: _{Q} &= \| _{n} - _{Q} ( }_n ) \|_2^2\\ & + _{ =1}^{L} ( \| ( _{n, -1} ) - _{ ,c_n^ } \|_2^2 + _Q \| _{n, -1} - ( _{ ,c_n^ } ) \|_2^2 ). where $ $ denotes stop-gradient and $ _Q$ is the commitment weight. Following common RQ-VAE settings, we set $ _Q=1.0$. In our main experiments, the SID length is $L=3$ and each codebook contains $128$ codes, yielding the three-layer SID space used by the representation and generation modules. Besides RQ-VAE, the main experiments also evaluate an RQ-KMeans quantizer. RQ-KMeans follows the same residual assignment logic as Equation~, but the codebooks are obtained by iterative KMeans clustering over residual vectors rather than by optimizing an encoder-decoder reconstruction objective. Specifically, at layer $ $, KMeans is fitted on the current residuals $\{_{n, -1}\}$, the cluster index becomes $c_n^ $, and the selected centroid is subtracted to form $_{n, }$. After all $L$ layers, the resulting SID still has the form $_n= $ and is consumed by the same representation and generation modules. Thus, changing RQ-VAE to RQ-KMeans only changes the quantization stage; all Engram memories, token merge, and prediction-head definitions remain unchanged. This subsection specifies how the sparse parameters used in the representation module are computed. Let $C$ be the codebook size per SID layer; in our main experiments $L=3$ and $C=128$. We denote the maximum allowed bucket count of a single hash table by $H_{ }=20{,}000{,}000$ and cap the integer encoding domain of one discrete unit by $D_{ }=2{,}097{,}152$. The Engram address dimension is $d_m=256$. Thus each intra-item head has dimension $d_m/K_{}=128$ with $K_{}=2$, and each inter-item head has dimension $d_m/K_{}=64$ with $K_{}=4$. The main-text statement ``base 128 for intra-item and base 16 for inter-item'' refers to the base used before applying the memory-specific scale values $s_{}$ and $s_{}$. For intra-item memory, a conditional pattern at level $ $ is $_{n, }^{S}= $ as defined in Equation~. The pattern contains $ $ SID codes, so its exact discrete domain is $C^ $; in implementation we use the encoded unit domain D_ ^{S} = . The intra-item memory uses the order set $_{ }^{S}=\{1, ,O_{ }^{S}\}$ with $O_{ }^{S}= $. Given an intra scale $s_{}>0$, we first define the scaled intra base as B^{S} = C s_{} = 128s_{}. The target bucket count before assigning hash heads is _{ ,o}^{S} = ( 2, ( {B^{S} }^o , C^o, H_{ } ) ), o _{ }^{S}. This equation is the concrete meaning of scaling up intra-item memory. Increasing $s_{}$ enlarges the effective code base $B^{S} $ before the order-$o$ power is taken. For example, with $C=128$, $s_{}=0.5$ gives target bases $64^o$, while $s_{}=1.0$ reaches the collision-free domain $128^o$ for the used intra orders unless the global cap $H_{ }$ is active. Values above $1.0$ are clipped by the exact intra domain $C^o$, because intra patterns are formed within one SID and the full code-combination domain is already enumerable. Each hash head uses a distinct prime bucket size near this target. Formally, for $k=1, ,K_{}$, $H_{ ,o,k}^{S}$ is selected from unused primes around $ _{ ,o}^{S} $ so that different heads use different moduli. The sparse parameters contributed by intra hash tables are therefore P_{} = {K_{}} _{ =2}^{L} _{o _{ }^{S}} _{k=1}^{K_{}} H_{ ,o,k}^{S}. The scaling figures in Section~ report the resulting sparse parameter count $P_{}$, not merely the raw scale value $s_{}$. For inter-item memory, one discrete unit at SID level $ $ is the prefix $_{n}^{ }= $, encoded as one integer. Thus its exact prefix domain is $C^ $ and the implementation unit domain is D_{ }^{T} = . We use $_{ }^{T}=\{1, ,O_{ }^{T}\}$ with $ = $. This allocates longer transition contexts to coarse prefixes and shorter contexts to deeper, more specific prefixes. Inter-item capacity is scaled with a fixed base of $16$. Since an inter-item unit at level $ $ is an encoded prefix rather than a single SID code, we use the level-wise scaled inter base B_{ }^{T} = ^ . This is the concrete meaning of the ``base 16'' inter-item setting in Section~. Increasing $s_{}$ enlarges the base at every level before the N-gram power is taken. For example, the default $s_{}=2.0$ gives $B_1^{T}=32$, $B_2^{T}=1024$, and $B_3^{T}=32768$. The target bucket count is then _{ ,o}^{T} = ( 2, ( {B_{ }^{T} }^o , ^o, H_{ } ) ), o _{ }^{T}. The term $ ^o$ is the full prefix-transition domain for an order-$o$ inter key at level $ $, and $H_{ }$ is the global per-table cap. As in the intra-item memory, each inter head receives a distinct prime bucket size $H_{ ,o,k}^{T}$ near $ _{ ,o}^{T} $. The sparse parameters contributed by inter hash tables are P_{} = {K_{}} _{ =1}^{L} _{o _{ }^{T}} _{k=1}^{K_{}} H_{ ,o,k}^{T}. Therefore, scaling up the inter-item Engram by $s_{}$ increases the level base before the order-$o$ power and then maps the resulting targets into prime-sized sparse hash tables. The actual plotted x-axis in Section~ is the computed parameter count $P_{} $. For a memory type $X \{S,T\}$, a key $ $ is mapped by deterministic multi-head hashing. _{ ,o,k}^{X} ( x_1, ,x_o ) = ( _{a=1}^{o} x_a m_{a,k}^{X} ) H_{ ,o,k}^{X}, where $ $ denotes bitwise XOR and $m_{a,k}^{X}$ is an odd deterministic multiplier generated from the random seed. This construction keeps the memory sparse and scalable while allowing table capacity to grow with either the exact intra-SID code-combination domain or the level-wise inter-item prefix-transition domain. User sequence $_u$, target SID $_{N+1}$, architecture type $A \{,\}$, multimodal item embeddings, SID codebooks Build SID-token embeddings by Equation~ Compute MM-guided item contexts by Equation~ Retrieve intra-item and inter-item memories by Equations~ and Set $}_{n,1}^{S}=$ and $}_{n, }^{S}=_{n, }^{S}$ for $ >1$ Merge SID evidence into item-level inputs $^{I}$ by Equation~ $} Encode $^{I}$ with the backbone to obtain $_{u}$ Set $_{ }=_{u}$ for all $ $ Append SID-layer placeholders and encode by Equation~ Initialize $_{u}^{1}=_{u}$ Use the ground-truth prefix $_{N+1}^{< }$ as the decoding prefix $} Compute $_{ }=_{ }$ from $_{u}^{ }$ and the $ $-th placeholder state by Equation~ Restore candidate-specific memories with state $_{ }$ by Equations~ and~ Compute the valid-prefix probability by Equation~ $ and $ <L$} Update $_{u}^{ +1}$ with the ground-truth code $c_{N+1}^{ }$ by Equation~ Optimize the token-level cross-entropy in Equation~ User sequence $_u$, architecture type $A \{,\}$, catalog prefix tree $$, beam size $B_{}$ Construct $^{I}$ from the historical items $} Encode $^{I}$ to obtain $_{u}$ and set $_{ }=_{u}$ for all $ $ Initialize the beam with the empty prefix and score $0$ Append SID-layer placeholders and obtain $_{u}$ and placeholder states by Equation~ Initialize the beam with the empty prefix, score $0$, and recurrent state $_{u}^{1}=_{u}$ Enumerate catalog-valid codes from $$ $} Compute $_{ }=_{ }$ from the beam recurrent state and the $ $-th placeholder state Restore candidate-specific memories with state $_{ }$ and compute layer-wise log-probabilities $} Attach the updated recurrent state from Equation~ to each expanded beam hypothesis Keep the top-$B_{}$ partial SIDs by accumulated log-probability Map completed valid SIDs to catalog items and rank them by accumulated score We instantiate with two generation architectures: a normal GR architecture and a NEZHA-style architecture~. Both use the same quantization and representation modules, and differ only in how the generator exposes hidden states to the prediction head. In the normal architecture, the backbone directly consumes $^{I}$ and returns hidden states $= $. The last valid hidden state is used as $_{u}$, and the state supplied to the prediction head is $_{ }=_{u}$ for every SID layer. The Memory-restoring Prediction Head in Section~ then performs layer-wise SID generation. This architecture is the most direct plug-in form of , because it replaces the input representation while keeping the standard autoregressive GR decoding interface. Following the NEZHA decoding architecture, we append one randomly initialized trainable placeholder for each SID layer after the item-level sequence. Since our experiments use $L=3$, the input contains three placeholders $_1,_2,_3 ^{d}$: ^{+} = ( [ ^{I}; _1; _2; _3 ] ). Let $_{u}$ be the hidden state of the last historical item and let $ $ be the hidden states of the three placeholders. The prediction head no longer reads only $_{u}$; instead, the $ $-th SID layer uses a layer-specific state _{ } = _{u}^{ } + _{ }, =1,2,3, where $_{u}^{1}=_{u}$, and $_{u}^{ }$ denotes the recurrent user state before predicting the $ $-th code. The state supplied to the prediction head is $_{ }=_{ }$, while the same candidate-specific intra-item and inter-item memories are restored. After the $ $-th code is generated, the recurrent user state is updated by a GRU transition: _{u}^{ +1} = _{ } ( _{ } ( c_{N+1}^{ } ), _{u}^{ } ), where $ _{ } $ denotes the transition input constructed from the generated code embedding and the next-level inter-item context. Teacher forcing provides $c_{N+1}^{ }$ during training, and beam search provides the selected candidate during inference. This design lets the placeholders provide layer-wise draft contexts, while the GRU carries generated-code information across SID layers. Unless otherwise specified, both generation architectures are trained with bfloat16 precision, base learning rate $1 10^{-5}$, weight decay $1 10^{-4}$, per-device batch size $64$, gradient accumulation steps $2$, and maximum training steps $100{,}000$. We use three SID layers with codebook sizes $ $, evaluate every $100$ steps, and report the average over random seeds $\{42,43,44\}$. Algorithm~ summarizes the training procedure of . The quantization and representation steps are shared by the normal GR and NEZHA-style architectures: historical SIDs are transformed into SID-token embeddings, converted into MM-guided item contexts, enriched by intra-item and inter-item memories, and merged into item-level representations $^{I}$. The two architectures differ only after $^{I}$ is constructed. Normal GR feeds $^{I}$ to the backbone and uses the last valid user state $_{u}$ for every SID layer. NEZHA appends layer placeholders, obtains placeholder states, and supplies the layer-specific state $_{ }=_{ }$ to the same prediction head. During training, both variants use the ground-truth SID prefix at each layer; NEZHA additionally updates its recurrent user state with the ground-truth code under teacher forcing. Algorithm~ describes the inference procedure. The representation constructor is still applied once to the historical sequence. Normal GR keeps a single user state throughout beam search. NEZHA keeps an additional recurrent state for each beam hypothesis, because each generated SID code changes the state used by later SID layers. At each layer, the prefix tree restricts the candidate set to catalog-valid continuations. For every partial prefix in the beam, the prediction head restores intra-item evidence from the candidate prefix and inter-item evidence from the appended historical transition pattern. The beam keeps the partial SIDs with the largest accumulated log-probabilities, and the final valid SIDs are mapped back to catalog items for ranking. In this section, we present the ablation study on the other two datasets, Industrial and Instrument, all under the same LLM backbone and Quantization . The results further validate that all of our designed modules are effective, which offer significant performance gains. MM-Scoring replaces MM-guided Token Scoring with mean pooling, and Mem. Merge replaces the memory-conditioned token merge with a linear layer. Other variants remove intra-item or inter-item memory from the encoding or decoding stage.} {!}{ {ccccc} & & & & \\ & & & & \\ MM-scoring & 0.0802 & 0.1010 & 0.0677 & 0.0744 \\ -intra & 0.0810 & 0.0970 & 0.0693 & 0.0752 \\ -inter & 0.0792 & 0.0982 & 0.0675 & 0.0723 \\ Mem. Merge & 0.0817 & 0.0995 & 0.0695 & 0.0748 \\ -intra & 0.0812 & 0.0996 & 0.0690 & 0.0711 \\ -inter & 0.0806 & 0.1002 & 0.0667 & 0.0718 \\ } MM-Scoring replaces MM-guided Token Scoring with mean pooling, and Mem. Merge replaces the memory-conditioned token merge with a linear layer. Other variants remove intra-item or inter-item memory from the encoding or decoding stage.} {!}{ {ccccc} & & & & \\ & & & & \\ MM-scoring & 0.0998 & 0.1298 & 0.0764 & 0.0847 \\ -intra & 0.0989 & 0.1258 & 0.0775 & 0.0846 \\ -inter & 0.0987 & 0.1280 & 0.0749 & 0.0828 \\ Mem. Merge & 0.1013 & 0.1295 & 0.0770 & 0.0863 \\ -intra & 0.1008 & 0.1277 & 0.0779 & 0.0815 \\ -inter & 0.0985 & 0.1305 & 0.0736 & 0.0830 \\ } This section explains how the sparse-parameter axis in Figure~ is obtained from the scale values. We vary one memory type at a time while keeping the other type at its default setting: $s_{}=2.0$ for the intra-item analysis and $s_{}=1.0$ for the inter-item analysis. The plotted x-axis is the table parameter count computed by Equations~ and~, rather than the raw scale value. The effective bases induced by these scales are straightforward. For the intra-item Engram table, $B^{S} =128s_{}$, so s_{} \{0.125,0.25,0.5,0.75,1.0\} corresponds to bases $\{16,32,64,96,128\}$. The order-$o$ target bucket count is $ {B^{S} }^o $, clipped by the exact intra domain $C^o$ and $H_{ }$, then expanded to $K_{}=2$ prime-sized heads and multiplied by the per-head dimension $128$ to obtain $P_{}$. For the inter-item Engram table, the underlying transition base is $16s_{}$, so s_{} \{0.5,1.0,1.5,2.0,2.5,3.0,3.5,4.0\} corresponds to bases $\{8,16,24,32,40,48,56,64\}$. At SID level $ $, this becomes $B_{ }^{T} = ^ $; for example, the default $s_{}=2.0$ gives level bases $\{32,1024,32768\}$. The order-$o$ target is clipped by the prefix-transition domain $ ^o$ and $H_{ }$, then expanded to $K_{}=4$ prime-sized heads and multiplied by the per-head dimension $64$ to obtain $P_{}$. The two memories exhibit different scaling behavior because their pattern spaces are different. The intra-item Engram table is relatively small: all patterns are bounded by the code combinations inside one SID, and base $128$ already reaches the theoretical capacity of the used intra-item domain. As $s_{}$ increases from $0.125$ to $1.0$, H@5 first improves almost linearly and then saturates. The gain becomes weaker around $s_{}=0.75$, because the number of observed intra-item code combinations is limited and multi-head hashing has already removed most effective collisions; further capacity mainly creates unused or rarely used buckets. The inter-item Engram table has a much larger combinatorial space, since its keys describe cross-item prefix transitions. In the tested range $s_{} \{0.5,1.0,1.5,2.0,2.5,3.0,3.5,4.0\}$, the inter table grows from about $1.4$M to $4.431$B sparse parameters, and H@5 keeps improving with the allocated sparse capacity. This indicates that reducing hash collisions remains useful for inter-item transitions within this range. We further test larger scales, $s_{} \{5.0,6.0,8.0\}$, where $s_{}=8.0$ reaches the theoretical base upper bound. These larger tables all lead to different degrees of H@5 degradation, and performance becomes worse once the scale exceeds $4.0$. We attribute this to sparse over-allocation: as the table size grows exponentially, most buckets are never activated or only receive very few updates, making it difficult to learn reliable memory vectors. This observation is consistent with the U-shaped sparsity-allocation phenomenon reported by Engram~, where excessive sparse memory relative to backbone parameters can hurt performance under a fixed allocation trade-off. In our setting, when $s_{} 5.0$, the inter-item table is already on the order of ten times larger than the Qwen3-0.6B backbone. 