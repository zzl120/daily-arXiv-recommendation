 {article} {acl} {Theorem} {Proposition} {Corollary} {Definition} =10000 =10000 , pdfauthor={Andre Bacellar}, pdfkeywords={retrieval abstention, retrieval confidence, CWAR, selective prediction, retrieval-augmented generation, uncertainty quantification}, } {} {} {} {} {} {R@5} {} {} {} {} { } { } } Multi-hop retrieval failures are not uniformly distributed across queries: they cluster in structurally predictable subpopulations. We prove two results formalizing this structure. First : confident-failure reduction is achievable if and only if retrieval features carry mutual information about success — a condition satisfied by LLM-judge pipelines but substantially weaker in dense-only settings, explaining the AUC-AC gap between regimes. Second : no single ANN score feature achieves best predictive performance across all failure regimes; the dominant feature differs between datasets (query length on MuSiQue, hop-1 concentration on HoVer), and a constructive witness pair shows each is necessary in one regime and non-contributory in the other. We instantiate these principles in , which computes a — a logistic function of up to nine query-ANN structural features, all available without any additional LLM call — and uses it to implement a calibrated abstention policy. We define the metric and evaluate across three multi-hop benchmarks and two retrieval architectures , covering five failure regimes with CWAR from 14.5\ $ $ achieves best or co-best AUC-AC in all five conditions against eight confidence baselines. On MuSiQue , $ $ reduces CWAR from 39.5\ coverage (47.8\ A model trained on MuSiQue transfers to 2WikiMultiHopQA with only $-0.5$pp AUC loss, confirming the domain-agnostic structure of regime features. Retrieval-augmented generation pipelines always produce an answer: given a query, the system returns its best-matching top-$k$ passages and feeds them to a language model. This design is sensible when the goal is to maximize recall across a broad query distribution. It is inappropriate when retrieval quality is heterogeneous and high-confidence errors are costly. Multi-hop QA is precisely such a setting: a query requires retrieving both a bridge passage and an answer passage, and the system frequently succeeds on one while missing the other. On MuSiQue with our LLM-judge pipeline , 39.5\ These are not low-confidence retrievals—the system returns a ranked list regardless, with no signal to downstream components that the evidence is incomplete. We argue that a retrieval system operating on heterogeneous queries should support a third action: . When the system predicts low retrieval quality, it can flag the query rather than silently returning an incomplete context. Abstracting this into a calibrated score opens the door to query routing, graceful degradation, and human-in-the-loop escalation—all without modifying the underlying retrieval pipeline. We study what makes a query hard at retrieval time, without using gold labels. The insight comes from the regime-conditional framework of : queries that sit near the transition between Q-dominant and B-dominant retrieval regimes are structurally ambiguous. Lightweight features from the ANN score distribution prove particularly predictive: , , and . We prove that CWAR is reducible if and only if retrieval features carry mutual information about retrieval success, formally characterizing when abstention can help . We show empirically that no single ANN score feature achieves best predictive performance across all failure regimes : a constructive witness pair exhibits opposite necessity/redundancy patterns across MuSiQue and HoVer, motivating multi-feature aggregation. We define the metric and introduce $ $, a logistic function of nine query-time features that is calibrated and requires no additional LLM inference. We evaluate $ $ against eight confidence baselines across five failure regimes spanning three datasets and two retrieval architectures, establishing consistent best or co-best AUC-AC in all conditions. We show cross-dataset transfer and identify regime-specific dominant features: query length on MuSiQue, hop-1 concentration on HoVer. The study of abstaining classifiers dates to . formalize the accuracy-coverage trade-off: at coverage $c$, the system answers only the fraction $c$ of queries it is most confident about, and accuracy among answered queries should increase as $c$ decreases. apply this framework to open-domain QA, training a confidence predictor on top of a QA model's internal states. Our setup differs: we predict success (do the retrieved passages contain the gold answers?), not answer generation accuracy, using only pre-LLM features. Post-generation uncertainty is well-studied: show that large LLMs can self-predict answer accuracy; propose semantic entropy over sampled outputs. These require a full generation pass per query. $ $ is computed in $<1$ms from ANN scores available at retrieval time--- no LLM call is needed for the abstention decision. Conformal prediction provides coverage guarantees under exchangeability. We include a split-conformal baseline and show that $ $ outperforms it on the AUC-AC metric while offering a continuous score for operating-point selection. Concurrent work by shows, on single-hop semantic, logical, and temporal benchmarks, that thresholding raw similarity magnitude is a poor abstention signal and that zero-cost distributional statistics of the retrieved score list improve abstention AUROC by up to 0.16. We reach the same conclusion from a different direction and extend it in four ways: the setting is multi-hop, where failure concentrates in structurally identifiable regimes; the signals are combined by a learned model whose output is a calibrated probability rather than used as raw rankers; the analysis covers an LLM-judge pipeline as well as dense retrieval; and Theorem~ gives the condition under which any such signal can reduce confident failure at all. show that multi-hop queries divide into two regimes: Q-dominant and B-dominant (bridge entity must be discovered from the hop-1 result). uses related features for a different task: predicting retrieval success rather than choosing a pipeline, providing a complementary abstention gate for the same regime-aware stack. Let $$ be a query set, $R: q \{p_1, , p_k\}$ a retrieval system, and $ $ the set of gold passages for query $q$. Define full success: \[ y = \! \] i.e., 1 if all gold passages appear in the top-$k$ result, 0 otherwise. Given a confidence score $s $ and threshold $ $, define the $_ = \{q : s \}$. The CWAR at threshold $ $ is: &= _ : y = 0\}|}{|_ |}\\ &= 1 - \! . When $ = 0$ , $ = 1 - $ equals the base failure rate. As $ $ increases, coverage $ = |_ |/||$ decreases; a good confidence score makes $ $ decrease faster than $ $. The plots $ $ against $ $ as $ $ varies, and its area summarises performance across all operating points. We model $s = + b)$ where $ $ is the logistic function and $ ^d$ is a feature vector computed from ANN scores available after retrieval. $d = 9$ for pipelines with hop-2 expansion; $d = 6$ for hop-1-only pipelines . $ $ is if $ = p$ for all $p$. We measure calibration by Expected Calibration Error with equal-frequency bins: \[ = _{b=1}^{B} {n} |_{B_b} - _{B_b} | \] where $B_b$ is the $b$-th bin. The central deployment question is: can confident-failure reduction be achieved at all? We show it is governed by the mutual information between retrieval features and retrieval success. Let $y \{0,1\}$ be the full-success indicator and $ $ the feature vector. Define the Bayes-optimal score $s^* = P $. Then $AC} > AC}_{}$ if and only if $I ) > 0$. Moreover, $AC} AC} $ for any score $s$ based on $ $. If $I = 0$ then $y $, so $s^* = $ for all $q$. Every threshold produces the same coverage and accuracy $$; the accuracy-coverage curve is flat and AUC-AC$ = $ $=$ AUC-AC$_{}$. If $I > 0$ then $ > 0$. By the law of iterated expectation, $E = $, so there exists a positive-measure set of queries with $s^* > $. Thresholding at any $ $ concentrates the confident set on above-average queries: $P > $. The accuracy-coverage curve is strictly above $$ for some coverage, so AUC-AC$ > $ = AUC-AC$_{}$. The optimality of $s^*$ follows from the Neyman-Pearson lemma: any monotone threshold rule achieves maximum accuracy at a given coverage when ranking by $P $, so no other function of $ $ can produce a higher accuracy-coverage curve. The theorem predicts the AUC-AC ordering across the five conditions. Dense-pipeline failures are nearly uniformly distributed across feature space ; the LLM judge concentrates failures in structurally complex queries, increasing $I $ and making failures more predictable . High CWAR alone does not guarantee reducibility: the dense conditions have the highest base CWAR (62.1\ The feature importance divergence in Table~ reflects a structural property of multi-hop failure, not a dataset quirk. No single feature in $\{ _1, , _9\}$ achieves best AUC-AC across all five failure regimes. Furthermore, there exist features $ _i _j$ such that $ _i$ is necessary in regime $R_a$ and non-contributory in $R_b$, while $ _j$ exhibits the opposite pattern --- the dominant predictive feature is regime-specific. Table~ establishes this by exhaustion: no single baseline achieves best AUC-AC in more than two of the five conditions. Query-len-inv achieves 0.947 on 2Wiki PropH but only 0.426 on MuSiQue Dense; Max-score achieves 0.839 on HoVer Dense but only 0.480 on 2Wiki Dense. Leave-one-out ablation in Table~ gives: is necessary on MuSiQue PropH and non-contributory on HoVer Dense ; is necessary on HoVer Dense and non-contributory on MuSiQue PropH . This pair forms the constructive witness. Note that entropy features are non-contributory in both conditions , consistent with the Entropy baseline's weak performance across regimes; the proposition does not claim universality for all nine features, only that the complementary structure exists. Proposition~ provides a principled motivation for multi-feature aggregation: any confidence model that uses only a single feature incurs avoidable AUC-AC loss on whichever regime that feature does not dominate. A logistic model trained jointly across regimes learns the complementary weighting — dominates on MuSiQue while dominates on HoVer — achieving regime-universal coverage that no single baseline can match. } All features are computed from ANN similarity scores available after retrieval--- no additional model inference is required. Let $\{ })\}_{i=1}^{K}$ be the top-$K$ hop-1 passage scores sorted in decreasing order, and $\{ })\}_{j}$ the hop-2 scores obtained by embedding $N=3$ SVO-extracted bridge queries and taking the maximum score per passage. When hop-2 scores are unavailable , $ _6, _7, _8$ are dropped, giving $d=6$. $ _1$ : $s_1^{ }$, the maximum hop-1 score. $ _2$ : $s_1^{ } - s_2^{ }$, the gap between the top-2 hop-1 passages. $ _3$ : mean of the top-3 hop-1 scores. $ _4$ : normalised Shannon entropy of $\{s_i^{ }\}$. $ _5$ : $s_1^{ } / }_{50}$, peak prominence relative to the top-50 mean. $ _6$ : $ _j s_j^{ }$. $ _7$ : gap between the top-2 hop-2 scores. $ _8$ : normalised entropy of $\{s_j^{ }\}$. $ _9$ : word count of the question. Given a held-out validation set $$ with gold labels $y $, we fit $, b$ by minimising binary cross-entropy with gradient descent until convergence . Features are $z$-score normalised. Predictions on the test set use $s = + b)$ where $$ uses validation-set normalisation statistics. Given a user-specified CWAR target $ $, we sweep $ $ over the validation set and select $ ^* = \{ : \}$. Because $ $ is monotone non-increasing in $ $ and equals $1 - $ at $ = 0$ and 0 at $ = 1$ (empty confident set), the desired $ ^*$ always exists for any $ 0$. We evaluate on three multi-hop benchmarks. contains compositional 2--4-hop questions over Wikipedia; we use 1000 queries . contains bridge and comparison questions; we use 1000 queries . contains 4000 multi-hop fact-verification claims over Wikipedia; we use the 2000 SUPPORTED claims . All splits use a deterministic MD5-hash 50/50 partition on query id. We evaluate on two architectures: : NV-Embed-v2 hop-1 ANN, $N=3$ SVO-expanded hop-2 queries, 20-candidate pool, 3-way LLM judge, $ =0.10$. Achieves R@5 $= 0.8138$ on MuSiQue and $0.9527$ on 2Wiki. Full-success rate: MuSiQue 60.5\ Uses 9-feature RCS . : hop-1 top-5 directly (no LLM judge, no SVO re-ranking). Applied to all three datasets; HoVer uses $d=6$ (no hop-2 scores available). Full-success rates: MuSiQue 37.9\ We compare eight confidence signals against $ $: : uniform $ $. : $1 - H }\})$. : $s_1^{ }$. : $s_1^{ } - s_2^{ }$. : $s_1^{ } / }_{50}$ . : $1 / $ . : $s_1^{ \,1/T}$ with $T \{0.5,1,2,5,10\}$ selected on the tune split. : two-layer network trained on the same 9 features. We additionally evaluate a split-conformal baseline using hop-1 max-score as non-conformity; results at discrete coverage targets are reported separately since AUC-AC is not directly applicable. Primary: AUC of the accuracy-coverage curve on the test split, with 95\ Secondary: CWAR and accuracy at fixed coverage targets (70\ ECE and Brier score for calibration . Table~ reports AUC-AC for all nine methods across five failure-regime conditions. $ $ achieves best or co-best AUC-AC in all five conditions. {figures/auc_ac_bars.pdf} {!}{ {lrrrrr} & {c}{} & {c}{} \\ {2-3} {4-6} Method & MuSiQue & 2Wiki & HoVer & MuSiQue & 2Wiki \\ Base CWAR & 39.5\ Random & 0.576 & 0.841 & 0.661 & 0.366 & 0.402 \\ Entropy & 0.743 & 0.849 & 0.741 & 0.535 & 0.490 \\ Max-score & 0.751 & 0.912 & 0.839 & 0.510 & 0.480 \\ Margin & 0.724 & 0.893 & 0.730 & 0.412 & 0.420 \\ Lift & 0.776 & 0.902 & 0.825 & 0.525 & 0.571 \\ Query-len-inv & 0.751 & $^ $ & 0.742 & 0.426 & 0.526 \\ Temp-scaled & 0.774 & 0.912 & 0.839 & 0.510 & 0.480 \\ MLP & 0.742 & 0.928 & $^ $ & $^ $ & 0.643 \\ RCS & & & 0.873$^ $ & 0.556$^ $ & \\ } On MuSiQue , $ $ achieves AUC-AC $= 0.790$, surpassing the next-best non-learned method Lift and Temp-scaled ; entropy-only $0.743$ places behind Max-score $0.751$. On 2WikiMultiHopQA, $ $ and Query-len-inv are tied , both far above entropy and random ---entropy adds only $+0.8$pp over random, while $ $ gains $+10.6$pp. On HoVer , MLP and $ $ are within bootstrap-CI overlap; both dominate all non-learned methods by at least $+3.4$pp. On the dense MuSiQue and 2Wiki conditions, $ $ and MLP are again within CI overlap, with $ $ leading 2Wiki dense . No single non-learned heuristic is consistently competitive: entropy underperforms max-score and lift across all five conditions. Table~ shows selected operating points on MuSiQue test. At 50\ CWAR drops from 39.5\ At 70\ {figures/risk_coverage.pdf} {lrrrr} Method & Cov. & Acc. & CWAR & $ $Acc \\ {*}{RCS } & 70\ & 50\ {*}{Lift} & 70\ & 50\ {*}{Entropy} & 70\ & 50\ A key question for deployment is whether $ $ generalizes across retrieval architectures. In the dense-only setting, the absence of an LLM judge substantially increases failure rates (MuSiQue: 62.1\ 2Wiki: 58.3\ Despite this regime shift, $ $ retains its relative advantage: on both dense conditions it achieves best or co-best AUC-AC. Absolute AUC-AC values are lower across all methods on dense MuSiQue , reflecting that dense-pipeline failures are more uniformly distributed and thus harder to predict from pre-retrieval features alone. The dense 2Wiki condition is more tractable , suggesting that the structured nature of 2Wiki questions preserves predictive signal even without judge re-ranking. Table~ shows calibration metrics on the MuSiQue test split. {lcc} Method & ECE & Brier \\ Majority-class baseline & — & 0.239 \\ RCS & & \\ ECE $= 0.035$ indicates good calibration: among queries where RCS $ 0.7$, approximately 70\ The Brier score improvement over the majority-class baseline shows that the probabilistic predictions carry meaningful information beyond the prior. We train $ $ on MuSiQue tune and evaluate on 2Wiki test . AUC-AC $= 0.942$, within $0.5$pp of the 2Wiki in-domain model . This near-perfect transfer holds despite MuSiQue's failure rate being $2.8 $ higher than 2Wiki's (39.5\ The result confirms that regime features capture domain-agnostic structure. Table~ reports leave-one-out AUC drop on MuSiQue and HoVer . The dominant feature differs by dataset: is strongest on MuSiQue , while dominates on HoVer . This divergence reflects architectural differences: MuSiQue Proposal~H failures are driven by compositional question complexity , whereas HoVer dense failures are driven by hop-1 retrieval concentration (multi-hop claims require multiple supporting passages, and $}_{1:3}$ directly measures whether the top passages form a strong candidate pool). {lrr} Feature removed & MuSiQue PropH & HoVer Dense \\ None & 0.790 & 0.873 \\ & $-0.012$ & $-0.002$ \\ & $-0.003$ & $-0.015$ \\ & $-0.002$ & n/a \\ & $-0.001$ & $-0.026$ \\ & $-0.000$ & $-0.015$ \\ & $+0.000$ & n/a \\ & $+0.000$ & $+0.000$ \\ & $+0.000$ & n/a \\ & $+0.001$ & $-0.002$ \\ On MuSiQue, $ $ extracts most of its signal from , , and ; entropy features (, ) add zero marginal information, consistent with the weak standalone performance of the Entropy baseline. On HoVer, $ $'s gain over max-score arises primarily from and : the average quality of the top candidate pool is a better predictor of multi-passage claim coverage than the single peak score alone. On 2Wiki, entropy-only barely exceeds random . Many 2Wiki failures occur in subtypes: the bridge passage is retrieved with high confidence , but the second gold passage is missed because SVO expansion from the bridge produces a weak query. Entropy captures only hop-1 uncertainty; $ $ includes , which captures hop-2 expansion quality and is decisive on 2Wiki. On HoVer dense and MuSiQue dense, MLP and $ $ are within bootstrap-CI overlap. MLP's competitive performance is expected: given the same features, a two-layer network can fit mild non-linearities that logistic regression misses. We prefer $ $ for deployment: it is interpretable (one scalar weight per feature), always calibrated by construction, and avoids overfitting risk on the small training sets available in production . The strong negative effect of query length is consistent with the analysis : longer questions contain more bridging clauses, increasing the chance that SVO extraction produces a malformed hop-2 query. At query time, length is perfectly observable and costs nothing to compute. A simple rule—``flag long questions for manual review''---would capture a substantial fraction of $ $'s benefit on MuSiQue, while on 2Wiki (where query-len-inv ties RCS) it is the dominant signal. On HoVer, however, length alone ranks 4th---hop-1 concentration features become primary when multi-passage coverage is the binding constraint. CWAR has a concrete product interpretation. In a deployed multi-hop fact-verification system (HoVer dense pipeline, CWAR\,$=$\,31.7\ downstream signal of unreliability — the system returns a ranked list with the same interface confidence as correct results. In a multi-hop QA system (MuSiQue PropH, CWAR\,$=$\,39.5\ of answers are built on incomplete evidence, delivered confidently to downstream components or users. The operating points in Table~ quantify the reduction: at 50\ coverage, CWAR drops from 39.5\ and reduces its confident-failure rate by 47.8\ At 70\ errors by 37\ All of this is achieved at $<$1\,ms per query from ANN scores already available at retrieval time, with no additional model inference. Theorem~ guarantees these gains are maximal for LLM-judge pipelines: $I $ is high for PropH failures, so the feature information is fully exploitable. The same theorem explains the smaller absolute gains on dense pipelines: their failures are closer to uniform in feature space, and no confidence model — however complex — can substantially reduce CWAR when $I 0$. abstains entirely when $ < $. An alternative is to re-route to a more expensive pipeline. Our experiments confirm that the rescue-judge pass from recovers 27 of the 203 MuSiQue test failures; $ $ could trigger this fallback rather than outright abstention, limiting the cost overhead to the $ $20\ We presented , a calibrated retrieval abstention framework based on a logistic Retrieval Confidence Score derived from query-time regime features. Across three multi-hop datasets and two retrieval architectures (five failure regimes, CWAR 14.5\ eight baselines in all conditions. On MuSiQue with the LLM-judge pipeline, $ $ reduces CWAR from 39.5\ 20.6\ entropy-only by $+9.8$pp AUC even though entropy is near-random on that dataset. Near-perfect cross-dataset transfer confirms domain-agnostic structure in the regime features. Feature importance varies by dataset: query length dominates on MuSiQue, hop-1 concentration on HoVer. This finding has direct implications for production deployments: a calibrated combination of a few scalar features available at retrieval time is sufficient to gate retrieval quality across diverse architectures, with interpretable failure-mode diagnostics built in. is fully composable with : run $ $ first, abstain or escalate if below $ $, otherwise route to the appropriate retrieval pipeline. This adds a well-calibrated abstention gate to any pipeline based on iterative ANN search at negligible inference cost. 