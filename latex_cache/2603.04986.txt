 {xcolor} {acmart} }} {} {XXX}{XXXX--XXXX}{XX, XX} {Definition} {Problem} {{ \ $ ${#1}}} {{ \ $ ${#1}}} {{ \ $ ${#1}}} {{ \ $ ${#1}}} {{ \ $ ${#1}}} {Figures}{Figures} {xcolor} {labelfont=normal,textfont=normal} {xcolor} {RGB}{245,245,250} { } { } { } { } { } *{ }{ } } } } } } } } {Huang et al.} {12 March 2009} {5 June 2009} To validate the effectiveness of for SR, we have conduct extensive experiments to answer the following research questions. : How does perform compared to state-of-the-art methods across different backbone recommendation models? : How do the components of improve sequential recommendation performance? : How do the different values of hyperparameters influence the recommendation performances? : In what ways does the proposed improve upon the traditional IPS? We evaluated our on four widely used recommendation datasets of varying sizes . Movielens ~ includes rich metadata on movie items and user ratings over time, making it invaluable for evaluating and developing systems across different recommendation scenarios. Music4All~ incorporates rich user–music interactions with precise timestamps, track release years, supporting research on both short-term dynamics and long-term temporal evolutions. GoodReads~ collects user-book interactions in late 2017 from publicly available user shelves on goodreads.com. To evaluate recommendation performance, we adopt two commonly used metrics: HR@K evaluates whether at least one of the ground-truth interacted items appears within the top-$K$ recommended items. This score reflects the ability of the recommendation system to successfully retrieve user-interested items. NDCG@K measures the ranking quality of the top-$K$ items in the recommendation list by assigning higher importance to correctly ranked items at higher positions. We compare our proposed against both traditional sequential models~ and generative models~, together with causal plug-in debiasing methods ~.\\ : ~ uses self-attention to model long-range dependencies in interactions.~ incorporates time interval between interactions to better capture time-sensitive user preference. ~ is a session-based recommendation model that leverages Gated Recurrent Units to capture sequential user behavior. ~ debias selection and exposure bias with IPS based on GRUs. ~ applied the self-attention mechanism to historical interaction sequences and calculate the mean value as the final user preference. ~ debiases exposure bias with a LLM-enhanced attention architecture. : ~ applies the conditional variational autoencoder to predict the next interacted item by conditioning on users’ historical interactions. ~ re-weights user interactions according to their timestamps to better capture temporal dynamics. ~ models the dynamic preferences of users with a diffusion process of a time interval. ~ utilizes a denoising diffusion probabilistic model to learn from intricate user–item interactions. ~ leverages multi-step denoising process based on diffusion models to robustify embeddings. We explore the learning rate within the range of \{5e-5, 1e-5, 5e-6\}, while searching batch sizes from \{8, 16, 32\}. The hyperparameters $ $, $ $ from the range of \{0.05, 0.1, 0.2, 0.5, 1.0, 2.0\} and \{0.05, 0.1, 0.3, 0.5, 0.7, 1.0\}, respectively. All experiments are conducted on two NVIDIA RTX-A5000-24GB GPUs. Table compares with 7 baseline models, incorporating traditional sequential models ~ and generative models ~. We applied to three backbone models: a traditional sequential recommender based on the attention mechanism~, and two generative recommenders, CVAE~ and DiffuRec~. As shown in Table , on three backbone models achieves competing performance on the four datasets with different scales and varied scenarios. For the traditional sequential model based on the attention mechanism~, incorporating TIPS achieves an average improvement of around 6\ Notably, performs particularly well on improving the performance on Music4All and ML-10M in Table , indicating the superior improvements of on diverse and large-scale recommendation scenarios. For example, achieves an improvement of up to 8.87\ In Table , models incorporating temporal information consistently outperforms their counterparts without modeling the time information. For example, TiSASRec achieves average improvements of 5.84\ We conduct ablation studies with the following variants of : 1) $ $} removes temporal information from both exposure estimation and inverse propensity adjustment, while preserving the overall framework with an end-to-end training strategy. 2) $ $} discards the inverse propensity adjustment and relies solely on the BPR loss for recommendation. The exposure estimation module is pretrained with temporal information using the BCE loss, and the whole framework is optimized in an end-to-end manner. 3) $ $} removes both the exposure estimation module and temporal information, using a pretrained propensity score, and is essentially a standard IPS-based method. Table shows these variants’ performance on ML-10M. It can be observed that all three variants perform worse than the original . This highlights both the necessity and the comprehensiveness of the design. The variant $ $, which removes both the exposure estimation module and temporal information while relying only on IPS adjustments, suffers the largest performance drop This demonstrates that both exposure modeling and temporal information are critical for recommendation performance in datasets of different scales, especially for achieving accurate ranking. In comparison, the variants $ $ and $ $ contribute less to overall performance degradation. This contrast underscores the importance and necessity of integrating temporal information with item counterfactual exposure estimation, further validating the motivation behind our . However, generative recommenders are more resilient to the removal of temporal information compared to traditional sequential models. Specifically, the variant $ $ shows average declines of only 0.59\ {figures/rq34_0.jpg} To further evaluate the effectiveness of , we compute the recommendation performance of on the attention backbone with respect to HR@10 and NDCG@10 with the hyperparameters $ $ from \{0.05, 0.10, 0.20, 0.50, 1.00, 2.00\} and $ $ from \{0.05, 0.10, 0.20, 0.50, 1.00, 2.00\}, respectively. We conduct experiments on the ML-1M with attantion as backbone and present the performance in Fig . The is a decaying ratio that controls how quickly historical user interactions lose influence as they become outdated. A smaller $ $ produces a slower decay, allowing long-term behaviors to continue contributing to user modeling, wheareas a larger $ $ emphasizes only the most recent actions. As shown in Fig. , performance rises as $ $ increases from 0.05 to about 0.20–0.50, indicating that moderately emphasizing recency improves recommendation quality, but further increases reduce HR@10. This suggests that overly aggressive decay lost useful long-term signals. We set $ =0.5$ in our experiments. The controls the contribution of the exposure estimation loss $_{EP}$ to the overall training objective. The optimizer with a smaller $ $ focuses primarily on fitting observed historical interactions, with minimal correction for exposure bias. Figures show that HR@10 improves as $ $ increases to around 0.10, indicating that moderate incorporation of exposure information enhances recommendation predictions. In this paper, we set $ $ to 0.3. In this section, we conduct a detailed analysis of the performance of compared to the traditional IPS method on the ML-1M dataset. We evaluate and IPS on three backbone recommenders: Attention~ for traditional sequential models, and diffusion~ for generative models. Specifically, we randomly sample 100 users and record the propensity scores computed by our $ in Eq. ) and by the traditional IPS method for 100 items per user, including one positive item from the test set and 99 negative items randomly drawn from the remaining item pool. Figure shows the distribution of the differences between the propensity scores of positive items and the average propensity scores of negative items. The propensity score inherently estimates the probability of an item being interacted with, while also incorporating the exposure information into account, encoded in $}_i$ Eq. . The three backbone models then leverage these propensity scores to adjust for bias. , consistently provides more discriminative propensity scores between positive and negative items, relative to the traditional IPS method.} More discriminative propensity scores for positive items is beneficial for mitigating exposure bias. Higher discriminability of positive-item propensity scores helps mitigate exposure bias: a larger difference indicates the model better identifies observed positives, allowing proper reweighting of less-exposed items. This also benefits selection bias, as it demonstrates the model’s ability to recognize exposed but not interacted items and assign them appropriate scores in the final objective function through inverse propensity weighting. Sequential Recommendation aims to predict users' next interactions by modeling the temporal order of historical interactions. Rather than treating each interaction as an independent event, SR models capture evolving preferences by leveraging sequence-aware models, allowing the system to infer both short-term and long-term interests, resulting in more accurate recommendations than methods based solely on static user profiles. These sequential recommenders fall broadly into two categories. First, traditional sequential models, such as Recurrent Neural Networks ~ and transformer-like architectures~, are employed to calculate the probabilities of candidate items based on explicit feedback in historical interaction sequences to rank the items in recommendation lists~. For example, Yuan use a transformer-based architecture to capture multiple types of positive user feedback, including purchases, clicks, and adding to cart, in order to enrich user sequential pattern modeling~. Second, generative recommendation systems, inspired by advances in natural language processing, aim to learn the distribution of historical interactions and directly generate the next interacted item~. For example, Li leverages the distributions within diffusion models to dynamically represent the diverse interests of a user and different features of an item~. In both of the categories, most current sequential recommenders rely solely on explicit interactions. While some methods attempt to incorporate contextual information to adjust for bias, most existing approaches primarily depend on explicit feedback signals from users’ interaction histories to improve prediction accuracy. However, existing SR models risk amplifying biases by ignoring item exposures. As depicted in Fig. , a SR model that relies solely on interactions while ignoring item exposures learns a biased view of user preference, as it only models user feedback on a limited set of pre-interacted items . {figures/toy_example_0.jpg} _t$ denotes user interactions at time $t$, $_t$ represents the user’s preference at time $t$, and $_t$ is the recommendation list containing all items exposed to the user at time $t$.} The absence of item exposure mainly causes two biases in SRs: i) , where items that were never exposed are implicitly treated as if the user showed no interest, ii) , where items not interacted are treated as no interest, though the lack of interaction may stem from display strategy or context. Effectively addressing these two biases requires distinguishing between items that were ``not exposed'' and those that were ``not of interest'', as missing interactions in training data may arise from either. However, in the absence of exposure logs, this distinction cannot be reliably inferred from correlations across users' historical data. To this end, counterfactual reasoning is conditioned on the hypothetical situation where the item is exposed to the user. Specifically, counterfactual reasoning distinguishes not exposed from no interest in addressing by asking, ``'', while it tackles by asking, ``'', thereby separating cases of exposed but no interest from potential interest. Existing counterfactual reasoning often relies on inverse propensity scoring , which reweights observed interactions by the inverse of their exposure probabilities. However, IPS is inherently static: it assigns weights to each interaction independently using only contextual signals , without modeling temporal dependence or cross-interaction dynamics. This static nature limits IPS’s ability to fully correct biases in SR, as these biases can accumulate over time. i) The first challenge is that static IPS ignores the sequential dependencies among items in the interaction sequences. In SR, a user’s next interaction depends not only on item features but also on past interactions, as user interests evolve over time and previous recommendations may influence subsequent actions. Simply reweighting interaction samples can disrupt the causal chain within user interaction sequences . ii) Second, while the IPS reweighting simulates fair exposure across all items, it does not reflect users’ genuine interests that often follow temporal trends . Because an item’s factual exposure probability can shift over time due to new releases, promotions, or trending popularity, current IPS methods may over-weight newly introduced items while under-weighting items that are currently trending. To accurately reweight user interactions, IPS should account for these sequential and temporal dynamics, aligning estimated exposure with the true user interests. To overcome the limitations of conventional IPS in sequential recommendation for handling selection and exposure biases, we propose a novel framework, Time-aware Inverse Propensity Scoring , which incorporates time-aware information in a model-agnostic manner. It improves both traditional sequential models and generative models by more accurately modeling users' implicit feedback and capturing sequential and time-varying causal dependencies in interactions. Specifically, instead of calculating the IPS for each user-item interaction directly, we first generate two high-quality counterfactual interactions for each factual interaction observed in the training data. One counterfactual varies the time representation of the interaction, and the other perturbs the representation for the item itself. Combined with historical interactions, these two counterfactual interactions help accurately model time-aware inverse propensity under the sequential and time-varying nature of user behavior. The main contributions of are summarized as follows: We estimate the item exposure distribution by constructing time-aware counterfactual examples in the absence of exposure logs. We mitigate exposure-related biases, including exposure and selection bias, and more accurately model user preferences by reweighting recommendations with time-aware propensities in the absence of exposure logs. We conduct extensive experiments on three backbones and compare with two categories of recommendation models to validate its effectiveness. This paper focuses on using IPS to account for sequential and temporal causal relationships in SRs and addressing exposure and selection biases, which are two major biases arising from the absence of exposure data. We introduce time-aware inverse propensity scoring , which incorporates temporal information into IPS to accurately capture the dynamics of SRs. Specifically, we propose a novel framework that integrates a plug-in exposure estimation model $f_ $ with TIPS to optimize the recommender’s training objective. The model $f_ $ estimates item exposure distributions by reasoning over counterfactual item–time pairs, which are then used to compute time-aware propensity scores for bias correction. {figures/method_0.jpg} Before performing counterfactual reasoning, we encode both items and timestamps to disentangle static item semantics from the dynamics of user preferences and item exposures, enabling IPS to adjust toward an unbiased exposure distribution. This dual encoding involves maintaining two separate item embedding lookup tables and extracting normalized temporal features. Based on the structural causal model shown in Fig. , it is necessary to separately model the interaction distribution and exposure distribution for items. To this end, we maintain two hidden embedding matrices, $^{ }$ and $^{ }$. Each item $v_i $ is associated with two trainable embeddings, defined as follows. _i = },i),\ _i = },i) where the embedding $^{ } ^{|| d}$ captures interaction information, learned from historical user-item explicit feedback such as clicks, and encodes item semantics and collaborative information that reflect user preferences. The exposure embedding $^{ } ^{|| d}$ represents the distribution of item exposure, involving factors such as popularity or promotional campaigns that influence whether an item is shown to a user. The necessity of maintaining two separate lookup embeddings arises from two key considerations: i) Interacted items represent a user’s positive preferences, whereas exposed items, especially those not interacted with, may indicate disinterest. Both types of signals are valuable but inherently distinct. ii) in further inverse propensity scoring, which requires the propensity estimated to be independent from the recommendation prediction . Shared embeddings create dependency, which increases variance and bias in subsequent adjustment process. The interaction time $t $ in the dataset are arranged sequentially in chronological order, with each $t_i$ representing the time of user $u$ interacting with item $v_i$. The gaps between two interactions matters because user preference changes. A commonly observed pattern is that more recent interactions better represent a user’s current preferences. For example, if a user watched a comedy yesterday and a documentary six months ago, the recommender should prioritize the recent comedy when recommending what to watch next. We compute the difference between the timestamp $t_i$ at position $i$ and the timestamp $t_{i-1}$ at position $i-1$, and then apply normalization. The normalized time interval is subsequently mapped into the embedding space with a multi-layer perceptron $ $. Formally, $_i= )$, where $ $ represents the normalization process, $_i ^{|| d}$ is the embedding table of time. To estimate item exposure without exposure data, we construct counterfactual samples based on three assumptions that reflect common patterns in recommendation scenarios. For each interaction $ _{}$, the item $v_i$ is positively exposed and clicked at time $t_i$, , for user $u$, there is $=1, =1$. For each factual item–time pair $ $ in the interaction data $_{}$, we assume three additional positive exposure item-time pairs where $=1, =0$: i) )$} are more likely to be exposed at the same time, as recommendation algorithms often promote related alternatives together~. Based on this assumption, we ask ``'' To answer this counterfactual question, we retrieve similar items based on the exposure embedding table $^{ }$, which aims at learning the exposure distribution of items. The exposure embedding of the counterfactual similar item $v_i^{sim}$ can be formally expressed as: _i^{sim} = _{v_j v_i} ,\ = _i^ _j}{\|_i\| \, \|_j\|} where $_i, _j ^{ }$, the cosine similarity function $ $ can be replaced by dot product or other similarity calculations. ii) )$} appear more frequently and are often displayed with other content, increasing the likelihood of concurrent exposure~. Therefore, we ask ``'' To effectively model user preference under popular trends, we also construct the positively exposed item-time pair $ $ with a counterfactual popular item. Specifically, we calculate the popularity score of items other than $v_i$ from the historical interaction data for all users $u $. Since we focus on modeling the exposure features of items and also to remain consistent with $v_i^{sim}$, we also retrieve the embedding of $v^{pop}_i$ from the exposure embedding matrix $^{ }$. ^{pop}_i = }, ) where $v^{pop} $ is the selected popular item, $ $ denotes the index function returning the id of $v^{pop}$. Specifically, we first count the occurrences of each item $v $ in $_{}$, and then randomly sample one item $v^{pop}$ for each $v_i$ from the top-$K$ most popular items. v^{pop} & \! v \}, K ) )\\ &= _{ _{}} where $ $ denotes that items are uniformly sampled from the rest of item set $ v_i$, $K$ is a hyperparameter that controls the number of top popular items considered, $_{}$ represents the training set of interaction data, $ $ controls the time window considered in the popularity counting function $ $. iii) The same item is likely to be consistently exposed to a user within a short time window, due to mechanisms such as short-term re-ranking~. Based on this findings, we ask ``''. To reasoning over the counterfactual exposure moment in this question, we applied a slight intervention $ $ on the time embedding $_i$ to construct the item-time pair $ $ via $^*_i = _i + $. The $ ^{d}$ is a random vector of small magnitude. Here, we set $ $. Thus far, we have derived the embeddings for the three counterfactual item–time pairs $ $, $ $, and $ $. In this paper, these three counterfactual item–time pairs are regarded as positive samples for exposure distribution estimation in $f_ $, but are treated as negative samples to augment interaction-based user preference inference in $g_ $. The following sections elaborate on how the three counterfactual pairs, together with the factual interaction $ $, are further leveraged in . $)} The arises because users can only interact with items they have been exposed to, causing the observed interactions to reflect the exposure mechanism rather than pure user preference. Recall that the structural causal model in Figure shows that item exposure is a necessary prerequisite for user–item interaction. Motivated by this causal relationships, we incorporate exposure estimation into users’ historical interactions with temporal information and further adjust predictions using a time-aware inverse propensity scoring framework to mitigate exposure bias. Given the embeddings of interacted items retrieved from the interaction embedding matrix $^{ }$, we arrange them according to their interaction timestamps $t_i$, resulting in the sequence $\{_0, _1, ..., _{M}\}$. We then combine this item sequence with the corresponding timestamp embeddings $\{_0, _1, ..._{M}\}$. =\{_0, _1, ..., _{M}\} \{_0, _1, ..._{M}\} where $ $ represents element-wise addition, $=\{_{m}\} ^{M d}$ denotes the basic embedding of user preferences, derived solely from historical interactions while disregarding exposure information, and $M$ is the maximum length of user's historical interaction sequences, $m$ indexes the position of element in the sequence. Before integration, to dynamically estimate the exposure distribution of the candidate items, we aggregate the item and embeddings for each factual or counterfactual item-time pairs, $ $, $ $, $ $, and $ $. For example, for item-time pair $ $, the hidden embedding $}_{i} ^{d}$ is represented as below: }_{i}=_{i}+_{i} where $_{i}$ and $_{i}$ are obtained from Sec. and Sec. , respectively. The symbol ``$+$'' represents the additive operation. We then incorporate exposure information into the interaction sequence through the cross attention mechanism. Specifically, the exposure embedding $}_{i}$ serves as the query $$, while the embeddings of each interaction in the sequence $$ are employed as both key $$ and value $$. Formally, $^{ } = }_{i} _Q^{ } + _Q^{ }$, $^{ } = _{m} _K^{ } + _K^{ }$, and $^{ } = _{m} _V^{ } + _V^{ }$, where $ ^{{N}}$ and $ ^{{N}}$ denote the weighting matrix and bias, respectively. The $ $ represents the $n$-th head of cross-attention, $N$ denotes the total number of heads, and $_m$ denotes a subsequence of historical interactions for the same user. The cross attention at the $n$-th head is formulated as: }, ^{ }, ^{ }) = } })^ }{} ) ^{ } The output embedding of the cross-attention mechanism is aggregated via an average pooling operation to produce a scalar score. This score serves as the exposure propensity, representing the probability that item $v_i$ is exposed at time $t_i$. s_{v_i,t_i}= ^t = }= _n_m }, ^{ }, ^{ })) where $ ^t $ denotes the function that estimates the time-aware exposure distribution, taking the interaction sequence $}$ and a specific exposure sample $}_i$ as inputs, and the subscript $m$ of $ $ denotes concatenating all the results from all the subsequences of historical interactions. The resulting time-aware propensity score $s$ of $ ^t $ is used to address exposure bias . To train the plug-in exposure estimation model $f_ $ to accurately capture the exposure distribution and seamlessly integrate exposure information with user–item interactions, we construct a positive set $^+$ and a negative set $^-$. The positive set $^{+}$ includes items that are considered positively exposed along with their corresponding exposure times, consisting of the factual item-time pair $ $ observed in the training interactions and three artificially constructed counterfactual exposure samples for each pair: $ $, $ $, and $ $. The negative set $^{-}$ consists of randomly sampled item–time pairs $ $ drawn from the remaining data $ ^{+}$. _{} = {||} [ _{ ^+} \\+ _{ ^-} ) ], where $||=|^{+} ^{-}|$ denotes the number of samples in the negative $^{-}=\{v', t'\}$ and positive sets $^+=\{ , , ,$\\$ \}$. The $s_{v,t}$ and $s_{v',t'}$ are the probability for positive sample $ $ and negative sample $v',t'$ to be exposed, respectively, computed with Eq. . The $ $ denotes the sigmoid function. $)} The arises because the recommender is trained solely on observed interactions in $_{}$, which includes only items interacted with the user and can therefore skew the modeling of user preference. This bias often leads to overestimating the preference for popular or frequently exposed items while underrepresenting unobserved or less-exposed items. In this paper, we leverage the counterfactual item exposure data to provide additional information for learning user preferences, thereby mitigating the effects of selection bias and improving the generalization of the model, as illustrated by the path $ $ in Fig. . We regard our as a plug-in model designed to enhance exposure information for any SR models . To demonstrate the flexibility of , we illustrate how exposure effects can be modeled in two categories of recommendation systems: attention-based sequential models, exemplified by the simple attention layers~, and generative models, exemplified by diffusion~. Following the common practice in SR, we apply a self-attention mechanism to the exposure-aware user preference $}_u$ ) to capture information between items in the interaction sequence. For modeling a user from their historical interaction sequence, the self-attention mechanism used in Eqs. can be replaced with any sequential model, such as an RNN or GRU modules. _u^{ }, _u^{ }, _u^{ } = }_{m} ^{ } + ^{ },\ = _n }, _u^{ }, _u^{ }) where $_u^{ }$, $_u^{ }$, $_u^{ }$ denotes the query, key, value for calculating user embedding $$, $ ^{{N}}$, and $ ^{{N}}$. Given the user preference embedding $$ and the interaction embedding $_i$ of candidate item $v_{i}$, the recommendation score $y_i$ can be predicted using their inner product, , $y_{i} = $. For the generative model represented by diffusion, we condition on the embedding of the exposure-aware user interaction sequence $}_u$ to generate the ideal item embedding $}_i$, which represents the user’s preference. }_i = Z_ _i = }_{m} + _i where $Z_ $ denotes the model to capture user preference from historical sequence, $ $ is the element-wise product, and $$ is the diffusion step embedding following the position encoding in ~, $ _i$ is sampled from a Gaussian distribution, $}_m$ is the exposure aware embedding for the interacted item at position $i$ in sequence $}$, and $}_i$ is the noised or reconstructed interaction embedding for candidate item $v_i$ during the forward diffusion or reverse process, respectively. The recommendation score in diffusion-based models are calculated based on the similarity between the ideal item $}$ and items in the actual item set $$. For actual item $v_i$, there is, $y_i = $. Building on the modeling of exposure influence on interactions and user preferences, we propose a novel Time-aware Inverse Propensity Scoring method that incorporates temporal information into the final recommendation objective. For the factual item-time pair $ $ in the training interaction data, there is a time-aware inverse propensity score ${s_{v_i, t_i}}$. Therefore, the training objective of the SR model can be formulated as below. _{}={N} _{ ^+, ^-}_{v_i,t_i}ln }{ _{ ^+}s_{v_i,t_i}} where $y$ is calculated with inner product or similarity measurement, for sequential model-based or diffusion-based methods, respectively. The $s_{v_i,t_i}$ is the exposure propensity for $ ^+$, as defined in Eq. . Note that the factual item-time pair $ ^+$ is regarded as the positive interaction sample, while the three counterfactual counterparts $ , , $ are regarded as the corresponding negative ones in the set $^-$. In Eq. , the $_{v_i,t_i}$ incorporates time decaying with the exposure propensity score $s_{v_i, t_i}$ for item $v_i$ at time $t_i$, rather than applying direct adjustments. Specifically, $_{v_i,t_i}$ emphasizes recent interactions and further strengthens the correction for less frequently exposed items. Formally, the weight can be calculated as below. _{v_i,t_i} = )}{ } where $ >0$ is a decay hyperparameter that controls how fast the exponential term decreases with time, a smaller $ $ means a slower decay, so past interactions remain influential for a longer time. The $ $ is a small baseline value, ensuring stability and preventing division by very small exposure probabilities. The proposed framework $f_ g_ $ is optimized through a combination of exposure distribution learning guided by $_{}$ in Eq. and recommendations adjusted with time-aware inverse propensity scoring with . Formally, the final training objective is defined as follows. = _{BPR-TIPS}+ _{EP} where $ $ is a hyperparameter that controls the extent to which the framework focuses on item exposure. In this paper, we propose Time-aware Inverse Propensity Scoring , a causality-driven framework for sequential recommendation that mitigates biases from missing exposure data, notably exposure and selection bias. constructs counterfactual item–time pairs to estimate exposure distributions with temporal dynamics, reweighting user interactions via time-sensitive propensity scores. We evaluate on four public datasets against two categories of state-of-the-art models: traditional sequential recommenders and generative models . Across three backbone models, consistently improves performance. As a versatile plug-in, it enables debiasing for existing sequential recommenders in both research and practical settings where exposure data are unavailable. In this section, we first propose the structure causal model to clarify the causal relationships in SR. Then, we introduce the traditional Inverse Propensity Scoring method, followed by the definition of problem that this paper focuses on. Suppose a user $u $ interacts with an item $v $ at time $t $, where $$, $$, $$ denote the set of users, items, and all the interaction time periods, repectively. In the causal model , the variable $$ denotes user preferences, the binary variables $ \{0,1\}$ and $ \{0,1\}$ represent whether an item is exposed to or clicked by user, respectively. In this paper, all equations are formulated for a single user $u$ for clarity. Here, $_v$ and $_v$ are binary variables indicating whether the item $v$ is clicked and exposed, respectively. For example, $_v=1$ and $_v=1$ denotes the item $v$ is exposed and interacted with by the user $u$, respectively. {figures/scm_0.jpg} Fig. illustrates the causal relationships in SRs. Different from Fig. , this Structural Causal Model illustrates the exposure-related causal relationships under a fixed time, which allows for a clearer explanation. The directed arrow in Fig. indicates the information flow from the cause variable to the effect variable. Each edge in Fig. is described as follows. $ $ represents that users' preference influence which items are exposed in their recommendation list . $ $ represents that, besides interactions data, the exposure of items in a recommendation list also reflects user's true preference . $ $ represents that users' preferences determines which items are interacted with . $ $ represents that user interactions provide information about user preferences . $ $ represents that interactions occur only among exposed items . arises because recommenders only observe a subset of items that are exposed to the user , while the user's preference toward unexposed items remains unobserved. As illustrated in Fig. , exposure $$ is a prerequisite for interaction $$. For example, when a user is only exposed to ten movies, the model cannot infer whether the user would have interacted with the unexposed movies other than these ten that never appeared in the recommendation list. Consequently, the model can only estimate the interaction probability conditioned on exposure, $P $, rather than the modeling the full distribution over both exposed and unexposed items $). Formally, the exposure bias can be formulated as follows: P P , P <1 where the left-hand side of the inequality represents the biased interaction distribution learned only from exposed items, while the right-hand side corresponds to the ideal interaction distribution conditioned on the exposure probability of items. These two distributions are intuitively not equal because the exposure probability of an item is generally less than 1, and the interactions $$ inherently depend on item exposure $$. As illustrated in Fig. , the interaction variable $$ is determined by both the exposure $$ and the user preference $$. However, in the absence of exposure logs, most recommendation models rely only on the subset of items with $=1$, interacted items. This introduces , since the model is deviated from the observed interactions with selected items. For example, a user may be exposed to ten movies but only click one of them. If the model only learns from the clicked movie, it wrongly assumes the user has no preference over the rest of nine movies. P P where the left-hand side represents the conditional distribution of user preferences over observed interactions, while the right-hand side represents the ideal preference distribution over all exposed items. These two distributions differ due to the existence of items that are exposed but not interacted with. In the absence of exposure logs, existing SR models leverages explicit user feedback mainly consider the paths $ $, $ $, and $ $, while overlooking the need to address exposure bias and selection bias . Inverse Propensity Scoring is a statistical method widely adopted to address the exposure and selection biases by reweighting user-item interactions~. IPS-based recommenders use propensity scores to reweight interactions and correct bias caused by unobserved exposures. Specifically, the propensity score models the probability that an item is exposed to a user and is used to reweight observed interactions toward an ideal distribution with uniform exposure. This reweighting prevents underestimation of user preferences for rarely exposed items. Formally, the propensity score for the interaction between item $v$ and user $u$ can be defined as: = P where $ $ denotes the exposure distribution estimation function. Therefore, lower propensity scores are assigned to items that has higher probability to be exposed, items with higher $P $. The in the recommendations arises because the recommenders treated the unexposed items as not interested. Suppose we have an unexposed item $v$. To adjust for exposure bias using IPS, consider the ``what-if'' question in Section : ``'' Our goal is to estimate the interaction probability over an ideal distribution where all items have an equal chance of being exposed. Most recommenders ignore the important fact that exposure is a prerequisite for interaction with an item . To address this ignorance, IPS-based methods reweight the recommendation score $g $ for each observed user-item interaction by the inverse of the item's exposure probability. Formally, _{ _{}} { } P _{,} _{,} = _{u } P _{v } P g The left-hand side of Eq. represents the expectation of the recommendation scores reweighted by IPS for each observed interaction, where $ $ denotes the propensity score for the specific interaction between user $u$ and item $v$. The right-hand side of Eq. represents the ideal expectation of recommendation scores, which indicate the interaction probabilities between users and items under the true user preference distribution $P $ defined in Eq. . The refers to recommendations models learns only from observed interactions of the subset of interacted items. We have an unobserved item $v$. To address selection bias using IPS, consider the aforementioned ``what-if'' question: ``'' We aim at inferring the user's true preference $$ over an ideal distribution where all items have an equal chance of being exposed. Considering the absence of exposure logs, the models do not account for the exposure probability in user modeling . Traditional IPS corrects this by applying the inverse of propensity score $ $ to the score computed by the recommendation model $g$. _{, _v=1, _v=1}} { } _{}} P The left-hand side of Eq. represents the expectation of the recommendation scores reweighted by IPS. The right-hand side represents the expectation of ideal recommendation scores, where $P $ is the preference of user $u$ in the true user preference distribution. In SRs, the exposure probabilities of items described by propensity scores $ $ as formulated in Eq. varies over time, as user interests and exposure strategies changes. Therefore, this paper addresses exposure and selection biases using IPS, while accounting for the time-varying nature of SRs. Specifically, our goal is to leverage a plug-in model $f_ $ that learns the item exposure distribution $P $ from observed factual interactions for any recommendation models $g_ $. We estimates item exposure over time and dynamically reweights interactions with the inverse of time-aware exposure probability, approximating a counterfactual setting where all items share the same chance of being exposed. This allows inferring interaction probabilities while accounting for the sequential and temporal dynamics of item exposure. For each $u $, $v $, and the interaction time $t $, there is an unbiased recommendation score $ $: where $ $ denotes the functional composition of the plug-in model $f_ $ for exposure estimation and the recommendation model $g_ $, parameterized by $ $ and $ $, respectively. Sequential recommendations aim at predicting users' next interaction based on their historical interactions with items. Existing sequential recommenders can be broadly categorized into traditional sequential models and generative models. Traditional sequential models focus on learning patterns from user interaction histories by modeling the order and temporal dependencies among items. Recent techniques adopt deep neural architectures such as recurrent neural networks~, convolutional networks~, and attention-based transformers~ to capture the sequential information of user preferences from interaction sequences. Differently, generative models treat SR as a generation problem, where the model directly generates the next item conditioned on user history, Leveraging frameworks such as variational autoencoders~, diffusion model~, and large language model~. For example, Li ~ leverages noise and reverse processes to better capture uncertainty in user preferences. Generative models are more flexible than traditional sequential models in adapting to shifts in user preferences, yet both suffer from the lack of exposure logs, leading to biased estimates of true preferences. Causality-based methods~ demonstrate the effectiveness of explicitly modeling the causal structure underlying observed interactions, rather than relying solely on model parameters to capture correlations. To mitigate biases arising from latent confounders in the causal structure, many of these methods adopt inverse propensity scoring ~ to reweight interactions during training. Wang ~ utilize the inverse propensity scores to reweight the interactions in training data to debias SRs from latent confounders such as selection bias. Yu ~ integrates time-aware debiased propensity scores from both the item and user sides with the semantics understanding capability of LLMs to improve SRs. Similar to our work, CaseRec~ also focuses on inadequate system exposure data and leverages counterfactual augmentation to explore latent user interests. Though some existing causality-based methods incorporate temporal information to address bias, our approach differs by providing explicit guidance for exposure estimation through time-aware counterfactual samples. {!}{ {2pt} {clcccc} {*}{ Backbone}&{*}{ Variant} &{c}{} \\ {3-6} &&{c}{HR@10} &{c}{NDCG@10} \\ {*}{Attention}& &0.5558&&0.3418& \\ & $ $ &0.5550&$ &0.5356&$ &0.5311&{*}{CVAE}& &0.4954&&0.3366& \\ & $ $ &0.4911&$ &0.4855&$ &0.4415&{*}{Diffusion}& &0.6712&&0.4072& \\ & $ $ &0.6581&$ &0.6537&$ &0.6217& Training set $_{}=\{ \}$, learning rate $ $ Model parameters $ , $ Initialize $ , $ = 1 E$} For each item-time pair $ _{}$, obtain embeddings $_i$, $_i$ for $v_i$, $_i$ for $t_i$ Obtain historical interaction sequence embedding $$ Obtain counterfactual exposure samples $ $, $ $, $ $ Compute exposure aware sequence $}$ $} $^+ \{ , $, $ $, $ \}$ $^- $ randomly sampled from $_{ ^+}$ Compute the time-aware propensity score $s ^t $ for each item-time pair $ ^{+} ^{-}$ Compute $_{EP} $ $} $^{+} \{ \}$ $^{-} \{ $, $ $, $ \}$ Compute recommendation score $y_i g_ $ for each item-time pair $ ^{+} ^{-}$ Compute $_{TIPS} $ Compute $ _{EP}+ _{TIPS}$ Update parameters $ , , - _{ , } L$ $ , $ {!}{ {c|cccc} Dataset &\#User&\#Item&\#Interaction&Time Period \\ ML-1M &5950 &3532 &574619 &04/25/2000-02/28/2003 \\ ML-10M &66028 &10254 &4980475 &01/09/1995-01/05/2009 \\ Music4All &14124 &99596 &5109577 &12/30/2013-03/26/2019 \\ GoodReads &17660 &29972 &409600 &02/13/2001-11/02/2017 \\ } {!}{ {2pt} {lcccccccccccccccc} {*}{ Model} &{c}{} &{c}{} &{c}{} &{c}{} \\ {2-5} {6-9} {10-13} {14-17} &HR@5 &HR@10 &NDCG@5 &NDCG@10 &HR@5 &HR@10 &NDCG@5 &NDCG@10 &HR@5 &HR@10 &NDCG@5 &NDCG@10 &HR@5 &HR@10 &NDCG@5 &NDCG@10 \\ {c}{ Traditional Sequential Models}\\ SASRec &0.3351 &0.4501 &0.2249 &0.2836 &0.5581 &0.6469 &0.4119 &0.4473 &0.4814 &0.5571 &0.3622 &0.3877 &0.4755 &0.6319 &0.3164 &0.3704 \\ TiSASRec &0.3525 &0.4819 &0.2387 &0.2968 &0.5808 &0.6924 &0.4429 & & & &0.3812 &0.4113 &0.4987 &0.6721 &0.3375 &0.3924\\ GRU &0.3467 &0.4731 &0.2482 &0.2895 &0.5460 &0.6253 &0.3983 &0.4336 &0.4607 &0.5372 &0.3501 &0.3732 &0.4593 &0.6027 &0.3061 &0.3538 \\ +USR &0.3728 &0.5123 &0.2658 &0.3119 &0.5762 &0.6501 &0.4173 &0.4581 &0.4763 &0.5567 &0.3627 &0.3862 &0.4941 &0.6502 &0.3260 &0.3807 \\ + & & &0.2808 & & &0.6716 &0.4322 &0.4728 &0.4970 &0.5842 & &0.4129 &0.5377 &0.7171 &0.3605 &0.4167 \\ Improv. &&0.3236&0.5547&&&0.4691&0.4772&0.5983&0.3782&&&&&\\ + & 0.4086 & 0.5558 & 0.2944 & 0.3418 & 0.6155 & 0.7295 & 0.4956 & 0.5325 & 0.5077 & 0.6054 & 0.4057 & 0.4273 & 0.6491 & 0.7750 & 0.4806 & 0.5355\\ Improv. &{c}{ Generative Models}\\ DDRM &0.3999 &0.5501 &0.2545 &0.3218 &0.5984 &0.7256 &0.4639 &0.5009 &0.5027 &0.6326 &0.3652 &0.4027 &0.5324 &0.7119 &0.3709 &0.4246\\ T-DiffRec &0.3336 &0.4624 &0.2206 &0.2717 &0.5339 &0.6427 &0.4179 &0.4531 &0.4873 &0.5787 &0.3628 &0.4024 &0.4444 &0.6077 &0.3091 &0.3619\\ PDRec &0.3374 &0.486 &0.2259 &0.2805 &0.5453 &0.6517 &0.4207 &0.4598 &0.4879 &0.5877 &0.3669 &0.4127 &0.4712 &0.6278 &0.3127 &0.3875\\ CVAE &0.3391 &0.4503 &0.2497 &0.2898 &0.4957 &0.5967 &0.3652 &0.3999 &0.4468 &0.5435 &0.3354 &0.3735 &0.5941 &0.7242 &0.4422 &0.4685\\ + &0.3785 &0.4954 &0.2893 &0.3366 &0.5629 &0.6623 &0.4221 &0.4457 &0.4844 &0.5799 &0.3789 &0.4123 &0.6584 &0.7786 &0.4995 &0.5088\\ Improv. & & & & & & & & & & & & & & & &\\ + & 0.5173 & 0.6712 & 0.3505 & 0.4072 & 0.6844 & 0.7964 & 0.5391 & 0.5817 & 0.5703 & 0.6885 & 0.4169 & 0.4653 & 0.7591 & 0.8794 & 0.5778 & 0.6171\\ Improv. & 