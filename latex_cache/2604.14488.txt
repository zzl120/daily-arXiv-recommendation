 {article} {acl} {Theorem} {Definition} {Proposition} {Corollary} {Remark} {Assumption} {Lemma} {} {} {} {} {TC-MQA} } In any domain where knowledge accumulates under formal authority---law, drug regulation, software security---a later document can formally void an earlier one while remaining semantically distant from it. We formalize this as : recovering the $ ))$ of the authority closure of the semantic anchor set---a different mathematical problem from $ _d s $. The two central results are: gives necessary-and-sufficient conditions on retrieved set $$ for $ = 1$---frontier inclusion and no-ignored-superseder---independent of how $$ was produced. establishes $ $ as a hard worst-case ceiling: for any scope-indexed algorithm , $@k R_{} $, proved by an adversarial permutation argument over the $ $ indistinguishable authority chains. Supporting structure: proves TCA and Recall@$k$ are decoupled---TCA $< $ while Recall@$k = 1$ for any $ > 0$---and a corollary shows TCA@$k = 0$ when $N^+ k$ documents outscore $c^*$, explaining why larger dense models are strictly worse. proves any system achieving TCA $= 1$ must perform proactive entity-scoped search. proves anchor discovery and authority resolution are each necessary; Corollary~ proves TCA $= 1$ is achievable when $ = 1$. organizes TCA $= R_{} R_{}$; Lemma~1 proves the three factors are separable failure modes by construction. establishes $ $ oracle-call lower bound and separates it from the RSSG's $O $ implementation cost. Three independent real-world corpora validate that the proved structure is consequential: security advisories , SCOTUS overruling pairs , FDA drug records . A GPT-4o-mini experiment shows the downstream cost: Dense RAG produces explicit ``not patched'' claims for In any domain where knowledge accumulates incrementally, later facts can formally override earlier ones. Legal precedents are overruled by later courts. FDA drug approvals are superseded by recall enforcement actions. Security patches void prior vulnerability disclosures. Compliance clearances are overridden by blackout announcements. In each case, the correct answer to a query does not depend on the most semantically relevant document alone---it depends on whether that document has been by a later one. We call this family of retrieval problems , and show that standard retrieval systems fail on it structurally across all three authority-governed domains we test. Retrieval-augmented generation ranks documents by semantic relevance to the query. This is silent on supersession. A RAG system asked whether a drug approval is still valid retrieves the approval document , not the recall notice . It has no mechanism to detect that the approval was voided. A security engineer asks: ``Is the prototype pollution vulnerability in lodash still unpatched?'' The most relevant document is the CVE disclosure---it matches , , and exactly, and states no fix is available. The correct answer requires the patch release note published two months later, which formally supersedes the disclosure for the same package and CVE. In a 2,300-document corpus mixing CVE disclosures and release notes, this patch note ranks~420th by dense similarity: it describes a ``release'' with ``fixed in version~4.17.12''---vocabulary orthogonal to the vulnerability query. Dense retrieval surfaces the disclosure , and TCA~$= 0$: the system answers that the vulnerability is unpatched and has ignored the controlling authority document. : CAR target $ ))$ over partially ordered corpora; operators $ $, $ $; quantities $ $ , $ $ , $ $ ; metric TCA; entity-scope axiom on the supersession relation. : : $$ achieves TCA $= 1$ it satisfies frontier inclusion and no-ignored-superseder. Necessary-and-sufficient conditions on any retrieved set, independent of retrieval method. Any future algorithm is auditable against these two conditions. : for any scope-indexed algorithm , $@k R_{} $. Proved by adversarial permutation: the $ $ authority chains are informationally indistinguishable to any scope-indexed algorithm, giving a worst-case ceiling, not an average-case claim. : Theorem~1 : TCA and Recall@$k$ decouple when $ > 0$; Corollary: TCA@$k = 0$ when $N^+ k$. Theorem~2 : any TCA $= 1$ system must perform proactive entity-scoped search. Theorem~3 : anchor discovery and authority resolution are each necessary; Corollary~ proves TCA $= 1$ when $ = 1$. Proposition~1 + Lemma~1 : TCA $= R_{} R_{}$; the three factors address separable, independently occurring failure modes. Proposition~3 : $ $ oracle lower bound; RSSG costs $O $ without oracle access. Four formal CAR generalizations as open problem statements. : Three real-world corpora confirm Dense TCA@5 $ \{0.064, 0.172, 0.270\}$; Two-Stage achieves $\{0.774, 0.926, 0.975\}$. Scale worsens Dense. GPT-4o-mini downstream: Dense causes ``not patched'' assertions for 39\ A $K = \{d_1, , d_n\}$ where each document $d_i$ has a timestamp $d_i.$, an entity scope $d_i. $, and content $d_i.$. A $R$ induces $d_1 _R d_2$ when: $ R.$, $d_2. > d_1.$, and $R. = $. The supersession relation $ $ is the transitive closure over all rules. A document $d$ is in $K$ if no $d' K$ satisfies $d d'$. We require every rule $R$ to satisfy: $R. = $ only if $d_1. d_2. $. Consequently, $d_1 d_2$ implies $d_1. d_2. $ . This axiom is satisfied by all five domain examples above and is used in the proof of Theorem~2. A retrieval task is a if the correct document to return is not the most semantically relevant document in the corpus, but rather the document that the semantically relevant one under a formal precedence or supersession relation. Controlling authority tasks form a proper subset of retrieval tasks, characterized by three properties: There exists an authoritative ordering $ $ on documents independent of semantic similarity to the query. The query vocabulary aligns with a non-controlling document . The controlling document has domain-distinct vocabulary . Let $A K$ be a set of documents and $ $ the authority order on $K$. The of $A$ is: \[ = A \{d' K : \, d A,\; d d'\} \] The of a set $S K$ is the set of maximal elements: \[ = \{d S : \, d' S d d'\} \] The operators $ $ and $ $ are standard constructions in order theory ; the contribution is identifying that standard retrieval optimizes neither, and characterizing the resulting gap. These two operators expose the central distinction of CAR: & R^*_k = _{|R|=k} _{d R} s \\ & C^*_k = )) where $A_k $ is the top-$k$ semantic anchor set. These are different mathematical problems: $$ requires traversing authority edges, and $$ requires checking domination within the retrieved set---neither operation can be expressed as a relevance ranking. For a query $q$, let $d_1 = _d s $ and $F = )$ . The is: \[ = s - _{c F } s \] $ 0$ always $ scores at most as high). $ = 0$ iff the best-scoring element of the active frontier is semantically aligned to the query, in which case standard retrieval suffices. When $ > 0$, all elements of $F $ score strictly below $d_1$, and every standard ranker prefers $d_1$. $F $ may contain more than one element ; we write $c^* F $ for any element of the active frontier. Let $D_{ } = \{d K : \}$ be the set of documents sharing the query's entity scope, and let $ = |D_{ }|/| )| 1$ be the . The of a query $q$ is: \[ = \! } ) (0, 1] \] $ = 1$ iff $ = 1$ . $ < 1$ when multiple authority chains share the query's scope, requiring rule-based disambiguation. In practice, $ $ is estimated from corpus metadata without oracle access to $ )$; the RSSG localizes scope by building the supersession graph over the retrieved neighborhood only. A retrieved set $$ has a if $ \, d $ such that $d d'$ and $d' $. Closure violations are the retrieval-level error underlying TCA failures: a system can achieve correct answer extraction with a closure violation , precisely the gap TCA is designed to detect. Standard retrieval optimizes ; CAR requires computing . This vocabulary gap is structural, not a tuning failure . The framework is domain-independent: : supersedes ; rule = for the same constitutional question. : SYSTEM\_RESTORED supersedes SYSTEM\_DOWN for the same host. : CVE PATCH\_RELEASED supersedes VULNERABILITY\_DISCLOSED for the same CVE ID. : FDA\_RECALL supersedes DRUG\_APPROVAL for the same compound. : BLACKOUT\_ANNOUNCED supersedes PRE\_CLEARANCE\_APPROVED for the same security ticker. A is $ $ where $K$ is a temporally ordered KB, $Q$ is a natural language query, and $a^* = f $ is the deterministic answer from the active documents. Let $ = \{r_1, , r_k\}$ be the top-$k$ retrieved documents. Define: $ = $ $$: for every superseded $d $, at least one $d'$ with $d d'$ is also in $$ \[ = \; \; \] is the fraction of gold documents in $$, irrespective of supersession structure. TCA and ProvRec are complementary: TCA measures compliance-safe retrieval; ProvRec measures archival completeness. TCA requires only a supersession relation $ $ and a gold answer function. It applies to any domain where documents have a formal validity lifecycle---legal, medical, regulatory, or operational. One active document answers the query. Equivalent to standard single-hop retrieval. $d_1 d_2$; the query refers to $d_1$'s subject. The correct answer requires $d_2$, which may be semantically distant from $Q$. $d_1 d_2 d_3$; the full chain must be available for TCA = 1. The superseding document $d_3$ originates from a different document source than $d_1$ and $d_2$, while sharing entity scope. This tests retrieval across provenance boundaries. Query templates for all four hop types are given in Appendix~. The theorems and Proposition~1 characterize . The following four definitions formalize the canonical extensions, each corresponding to relaxing one component of $ ))$: When the entity scope $ $ is not recoverable from the query text, it is a latent variable with posterior $p $. The is: \[ _{} \;=\; }_{ p }\! \] or the MAP approximation with $ = _ p $. Latent-scope failures are precisely the cases where $ $ cannot be estimated from query text alone; scope must be inferred from an anchor-side entity linker. The 22.6\ When authority edges carry confidence $ $ rather than being deterministic, the at threshold $ $ is: \[ _ \;=\; A \; \; \{d' K : \, d A,\; \} \] The at confidence $ $ is $_ = \{d S : \}$. Probabilistic-authority CAR arises when supersession edges must be inferred from text rather than read from structured metadata. When $m$ authority relations $ _1, , _m$ coexist , the is: \[ _{ } \;=\; A \; \; \{d' K : \, i ,\; \, d A,\; d _i d'\} \] The active frontier must satisfy all $m$ dominance relations simultaneously; the resulting structure is a lattice of authority neighborhoods. Multi-authority CAR captures law , drug regulation , and policy systems . Under real-time document arrivals $\{d_{N+1}, d_{N+2}, \}$, the active frontier changes with each insertion: a new document $d_{}$ with $d_{} d_{}$ removes $d_{}$ from $ ))$. The is to maintain $ ))$ under insertions in sublinear amortized time per query. Each insertion invalidates at most $||$ cached retrievals , giving a natural incremental update structure. {!}{ {@{}llp{7cm}@{}} & & \\ Theorem~4 & Correctness & N\&S conditions on $$: frontier inclusion $+$ no-ignored-superseder \\ Proposition~2 & Limit & Hard worst-case ceiling $ $ on all scope-indexed algorithms \\ Theorem~3 $+$ Cor.\ & Construction & Two-stage is sufficient when $ =1$; T1, T2 prove each component necessary \\ Proposition~3 & Cost & $ $ oracle lower bound; RSSG costs $O $ without oracle \\ Theorems~1--2, P1, L1 & Context / synthesis & Mismatch, closure necessity, factorization, separability \\ } All results in this section assume: : $K$ is fixed during retrieval . : $ $ is fixed; each pair $ $ has a known, time-invariant supersession status . Probabilistic-authority CAR relaxes this . : one partial order on $K$. Multi-authority CAR relaxes this. : $ $ is directly extractable from $Q$ by a perfect extractor, or $ $ is computable from corpus metadata. Latent-scope CAR relaxes this. : the TC-MQA answer function $f$ in Definition~ is a fixed deterministic map from $ ))$ to an answer; no distributional generation. : Proposition~2 ranges over scope-indexed algorithms ; Proposition~3 measures cost in authority-comparison oracle calls . A retriever $$ is if it ranks documents by $s $ without access to $ $ or timestamps. Its output set satisfies $_k = _{|R|=k} _{d R} s $. For any relevance-based retriever $$, any fixed $k 1$, and any $ > 0$, there exists a CAR instance $ $ with $ > 0$ such that $ < $ while $@k = 1$. Consequently, TCA and Recall@$k$ are decoupled: arbitrarily high relevance recall is compatible with arbitrarily low TCA when $ > 0$. Construct family $K_n$ with: $d_1$ at time $t_1$ with high relevance to $Q$ ; $n-1$ documents $d_2, , d_n$ with the same type and similar vocabulary as $d_1$ but different entity scopes, all before $t_1$; $d^*$ at $t^* > t_1$ with $d_1 d^*$, using domain vocabulary distinct from $Q$ . $d_1$ scores highest on $s $. As $n $, $d^*$ is displaced to rank $ n$. For fixed $k$: $ k/n 0$. TCA requires retrieving $d^*$ for both AnswerCorrect and NoIgnoredSuperseder . Choosing $n > k/ $ gives $ < $. Let $N^+ = |\{d K : s > s \}|$ where $c^* ))$ is the controlling document. For any relevance-based ranker : \[ k \;=\; 0 N^+ \; \; k \] A deterministic relevance-based ranker outputs the $k$ documents with highest $s $. If $N^+ k$, all $k$ slots are occupied by documents strictly outscoring $c^*$; hence $c^* $ and TCA $= 0$. : $N^+ k$ is a separately stated condition stronger than $ > 0$ . For a corpus where fraction $p$ of documents score above $c^*$, $ = p $, so the condition holds with probability $ 1$ as $N $, explaining the scale-harm pattern in Table~. {3} A retrieved set $$ achieves $ = 1$ if and only if it simultaneously satisfies: : $ )) $---every element of the active frontier is present. : $ \, d $ that is superseded $ $: $ \, d' $ with $d d'$. These are the conditions on any retrieved set, regardless of the retrieval method used to construct $$. Suppose TCA $= 1$. By Definition~, $a^* = f )))$ under the deterministic answer function $f$ ). is semantic---the derived answer must match the ground truth---whereas this theorem establishes the structural condition on $$ that makes that semantic condition hold, independent of how retrieval was performed. AnswerCorrect $= 1$ therefore requires $f$ to evaluate over the complete active frontier; since $f$ is defined on $\{d K : d\}$ and the active elements of $ )$ are exactly $ ))$, every element of $ ))$ must appear in $$. Directly from the definition of NoIgnoredSuperseder . Condition~ is precisely the structural hypothesis under which the semantic answer function $f$ evaluates over the correct active set: every $c^* ))$ is in $$, giving AnswerCorrect $= 1$. Condition~ is exactly NoIgnoredSuperseder, giving TCA $= 1$. Condition requires only that superseder of each superseded document is retrieved, not all. The chain $d_1 d_2 d_3$ admits $ = \{d_1, d_3\}$ as TCA-correct : $d_3 $; condition : $d_3 $ witnesses $d_1 d_3$) even without $d_2$. A strictly stronger condition---requiring every intermediate chain member---would be sufficient but not necessary. {1} A retrieval system performs if it promotes entity-scoped later events only when a triggering event from the same entity scope already appears in the initial retrieval. It performs if it retrieves entity-scoped events independently of the initial retrieval. Let $$ be a retrieval system. If $$ achieves TCA~$= 1$ on all TC-MQA instances, including those in Theorem~1's family where the superseder $d^*$ is arbitrarily dissimilar to $Q$, then $$ must perform proactive entity-scoped search: it must retrieve $d^*$ via a function of $d^*. $ or $d^*.$, independent of $s $. Suppose $$ achieves TCA = 1 on all instances, including those in Theorem~1's proof where $s 0$ as $n $. Since $d^*$ is never retrieved by relevance alone , $$ must use a source other than $s $ to retrieve $d^*$. Since $d_1 d^*$ implies $d_1. d^*. $ , and $d_1$ is retrieved by relevance, $$ must access $d_1. $ and $d^*.$ to locate $d^*$. This is proactive entity-scoped search by definition. Theorem~2 applies to all TC-MQA instances, including adversarially constructed ones. A bi-encoder fine-tuned on specific $ $ pairs achieves TCA $> 0$ on training examples by implicitly learning entity-scope conditioning: training pulls $f $ toward $f $ because $d^*. Q. $ appears as a supervision signal. This is a learned implementation of proactive entity search, not a counterexample. The theorem's construction is preserved for corpus instances outside the fine-tuning set: one can always add vocabulary-disjoint superseders the encoder has never seen, restoring the impossibility. Fine-tuning can reduce the semantic-authority divergence $ $ for seen instances; Theorem~2 characterizes what a system must , not how it is parameterized. {2} Under identifiable scope keys , the CAR problem decomposes into three composable steps: : $A = \{d : s \}$ --- retrieve the semantically-aligned non-controlling document. : $ $ via entity-indexed lookup --- extend to the full authority chain. : $ )$ --- expose only non-superseded documents to downstream use. Steps and are : omitting either makes TCA $= 0$ on T1 instances. Step is not necessary for TCA $= 1$ but is necessary for LLM answer quality $ exposes superseded evidence, causing confident wrong answers). The two-stage pipeline implements all three steps and achieves: \[ k \;=\; \] which is the exact upper bound when $ > 0$: no retrieval system can exceed Stage-1 anchor recall. Under vocabulary disjointness > 0$), $s 0$ for all $c^* F $. Any system achieving TCA@$k > 0$ on a T1 instance must place some $c^* F $ in its top-$k$ output. By Theorem~2, this requires proactive entity-scoped search triggered by the anchor $d_1$; hence $d_1$ must have been retrieved first. Since $d_1$ is the most semantically similar document to $Q$, it can only enter via Stage-1 relevance retrieval. Therefore TCA@$k $. The two-stage pipeline achieves equality: if $d_1 k_1$, entity-indexed lookup ) retrieves every $c^* F $ via shared entity scope and timestamp ordering; step promotes only $F $. Thus TCA@$k = $ exactly. Omitting anchor discovery means no document triggers entity-indexed lookup. Then retrieval is purely relevance-based and TCA $= 0$ by Theorem~1. Omitting authority resolution means Stage-1 results are returned without entity-indexed extension. Since $c^* F $ scores $ 0$ on $s $, it falls outside any relevance-based top-$k$, giving TCA $= 0$ . Returning $ $ instead of $ )$ includes $d_1$ alongside every $c^* F $. Since $d_1 $ has superseder $c^* $, NoIgnoredSuperseder holds; since $F $, AnswerCorrect holds. TCA $= 1$ without step . However, exposing superseded evidence to the LLM causes confident wrong answers , so step is necessary for practical downstream quality. For the two-stage pipeline on T1 instances with entity-disjoint corpora = 1$): $k = k_1$. Improving Stage-1 directly increases TCA; improving entity lookup when Stage-1 already recalls the anchor adds no benefit. When $ = 1$ and $d_1 k_1$, the two-stage pipeline achieves $ = 1$. $ = 1$ implies $ = 1$: the entity scope $ $ uniquely identifies a single authority chain. Entity-indexed lookup retrieves the unique $c^* F $ . Step filters to $\{c^*\}$. Frontier inclusion holds = \{c^*\} $); NoIgnoredSuperseder holds since $c^*$ is active . Both conditions of Theorem~4 are satisfied, giving TCA $= 1$. {1} An algorithm $$ is if its document-promotion function depends only on $\{d. ,\, d. : d \}$ and corpus scope statistics, with no direct access to authority rules $R_1, ,R_m$ or the supersession relation $ $. For any scope-indexed algorithm $$ : \[ k \; \; R_{} \] This bound holds for both deterministic and randomized $$, and is tight: there exist instances achieving equality. Fix any scope-indexed algorithm $$. Suppose the anchor is retrieved . We show $ = 1/ $. When $ < 1$, there exist $ 2$ distinct authority chains $C_1, ,C_{ }$ in $D_{ }$. By Definition~, $$'s promotion function sees only $\{d. , d. : d \}$ and corpus scope statistics---it has no access to $ $. The $ $ chains are, by construction, indistinguishable under this view: each chain contains scope documents with the same $ $ marginal statistics, and no rule-based signal distinguishes which chain contains the frontier. Consider any fixed algorithm $$ . For any permutation $ $ on $ $, define corpus $K_ $ as $K$ with chain labels permuted: what was chain $C_i$ is relabeled $C_{ }$, and the correct chain is always $C_{ ^{-1} }$. The view of $$ is identical across all $ !$ such permutations . Algorithm $$ must commit to some chain output; by symmetry, it can be correct on at most $1/ $ fraction of the $ !$ permutations. Therefore $ 1/ = $ on the worst-case permuted instance. For $ = 1$ =1$), the bound is 1 and is achieved by two-stage . For $ >1$, a randomized algorithm that guesses uniformly achieves $1/ = $, matching the bound. Combining: TCA$ = R_{} $. The RSSG is scope-indexed: it accesses authority rules $R_1, ,R_m$ directly. When those rules correctly identify the controlling chain , RSSG achieves TCA $= R_{} $, saturating the Theorem~4 conditions. The contamination recovery measures $R_{}$ rising from near-zero toward the $R_{}$ ceiling. {2} An $ \{d_1 d_2,\, d_2 d_1,\, \}$ answers single-pair supersession queries in $O $. Let $h$ be the maximum supersession chain depth within entity scope $ $, and $|_{ }|$ the number of retrieved documents in that scope. To exactly recover $ ))$, measured in oracle calls : = 1$): $|A_k D_{ }|$ lookups---one per retrieved anchor. This is achieved by Stage-2 of the two-stage pipeline. < 1$): $ $ oracle calls---every retrieved scope document must be examined at least once to determine chain membership. The RSSG, which has no oracle and must apply $m$ authority rules per pair, runs in $O $ total rule checks---an upper bound on its implementation cost, not a matching lower bound. : $O $ oracle calls suffice when chain structure is known; BFS from the anchor along $ $ visits each chain edge once. Each anchor $d A_k D_{ }$ triggers exactly one entity index lookup to extend $ $; by Theorem~3's minimality, no lookup can be skipped without risking a closure violation on some instance. Any algorithm must examine every document in $_ $ to determine its chain membership; no document can be skipped without risking a frontier error. This yields the $ $ problem lower bound on oracle calls. The RSSG has no oracle: it applies rules $R_1, ,R_m$ to each pair $ $ in $_ $, giving $_ |}{2} m = O $ rule checks---an upper bound on the RSSG's implementation cost, which exceeds the problem lower bound by a factor of $|_ | m$. BFS from the anchor along $ $ visits each chain edge once; depth $h$ bounds the number of hops needed to reach the frontier. On an entity-disjoint corpus, anchor-based probing alone achieves TCA~$= 1.000$ on all hop types T0--T3: the entity bucket contains only gold events, so no disambiguation is needed. The RSSG contributes when the corpus is : spurious same-entity distractors appear in the entity bucket, and the RSSG applies domain rules R1--R10 to demote them without oracle access . When $ < 1$ > 1$), timestamp ordering alone cannot recover $ ))$: a distractor $d_ D_{ } $ with $t_ > t^*$ is selected by any timestamp-only algorithm, giving TCA $= 0$. Disambiguation requires checking reachability from $d_1$ under $ $---exactly the role of the RSSG. {0} For a two-stage retrieval system on a CAR instance, TCA decomposes as: \[ \;=\; R_{} \; \; \; \; R_{} \] where: $R_{} = $ $ $R_{} = $ Define events: $E_a$ = ``anchor $d_1 k_1$''; $E_s$ = ``correct authority chain identified $ E_a$''; $E_f$ = ``frontier correctly resolved $ E_a E_s$''. Each factor maps to one event: $R_{} = $; $ = $; $R_{} = $. The factorization is the chain rule of conditional probability applied to the nested event sequence $E_a $: \[ = \] $ $ is a corpus property; its identification with $ $ follows from Proposition~2's adversarial argument: conditional on $E_a$, a scope-indexed lookup cannot distinguish among $ $ chains, achieving $ 1/ = $ with equality at the worst-case permutation. The three factors $R_{}$, $ $, and $R_{}$ are : for each factor, there exist CAR instances where that factor is $< 1$ while the other two are $= 1$. } < 1$, $ = 1$, $R_{} = 1$:} Let $K$ be entity-disjoint =1$, so $ =1$) and $d_1$ have moderate relevance . Then $R_{} < 1$; when $d_1$ is retrieved, the unique $c^*$ is promoted correctly . Example: FinSuperQA T1 with BM25 Stage-1 . } = 1$, $ < 1$, $R_{} = 1$:} Let $d_1$ always be Stage-1 rank 1, but $ = 2$ , with a complete RSSG rule set that always selects the correct chain. Then $R_{}=1$, $ = 1/2 < 1$, but $R_{}=1$ . Example: contaminated FinSuperQA where RSSG is applied. } = 1$, $ = 1$, $R_{} < 1$:} Let $d_1$ always be Stage-1 rank 1 and $ = 1$ , but the frontier $F $ has multiple elements and the entity lookup has partial coverage of the authority graph. Then $R_{}=1$, $ =1$, but $R_{} < 1$. Example: T2/T3 hop types in FinSuperQA with shallow entity index. $ = 1$ and $R_{} = 1$, so $ = R_{} $---reducing to Corollary~. $ < 1$ > 1$), so $$ falls below $R_{}$ by Proposition~2; $R_{}$ rises from near-zero to $0.776$ , explaining the $+12.4$~pp recovery in Table~. The corpus-level average $ = 0.774$ is the 77.4\ : A TC-MQA Benchmark} instantiates TC-MQA in SEC Rule 204A-1 employee trading compliance . The entity scope is $ = \{ \}$; supersession follows rules R1--R10 . Ground-truth answers are algorithmically derivable from active events, eliminating annotation ambiguity. Each of the 1,000 examples is assigned a unique entity pair from a pool of 6,000. This entity-disjoint design ensures the entity probe retrieves each example's gold events cleanly. The shared corpus contains 12,250 events . A contaminated variant relaxes the disjoint guarantee to study disambiguation . {!}{ {lcccc} & & & & \\ Examples & 250 & 250 & 250 & 250 \\ KB events & 1.0 & 2.0 & 3.0 & 3.0 \\ Supersession hops & 0 & 1 & 2 & 2 \\ Cross-provenance & No & No & No & Yes \\ {l}{} \\ } statistics.} and are the lexical baselines. uses sentence-transformer embeddings . ranks gold documents first . Full hyperparameters and graph construction details are in Appendix~. }: dense retrieval with supersession re-ranking. Promotes superseders of top-20 events that are superseded in the gold KB; restricted to re-ranking what dense retrieval already surfaced. This is the strongest re-ranking baseline: it has access to ground-truth supersession edges. } : dense retrieval + entity-indexed probing. Extracts entity identifiers from the query; for every entity-matched event in the initial top-20, looks up the full-corpus entity index for later-timestamped events in the same scope and promotes them. This implements the Decomposition Theorem's three stages: anchor discovery , authority resolution $ via entity index), active-status determination )$ by timestamp). Achieves TCA~$= 1.000$ on entity-disjoint corpora. } : augmented with a . After the entity probe, applies domain rules R1--R10 to all retrieved entity-scoped events---without gold KB access---to determine which events are active under the supersession relation. This implements $ )$ correctly when $ > 1$: the RSSG identifies which retrieved documents are in the true $ $ vs.\ spurious same-scope distractors, enabling accurate frontier recovery . All systems retrieve from the shared 12,250-event corpus with $k = 5$, $n_ = 10$. Metrics: TCA, Acc, R@5, ProvRec. {!}{ {lcccc|cccc} & {c|}{} & {c}{} \\ & & & & & & & & \\ BM25 & 0.607 & 0.305 & 0.503 & 0.607 & 1.000 & & 0.216 & 0.004 \\ TF-IDF & 0.621 & 0.254 & 0.491 & 0.621 & 1.000 & & 0.012 & 0.004 \\ Dense & 0.450 & 0.214 & 0.247 & 0.450 & 0.820 & & 0.036 & 0.000 \\ Oracle & 1.000 & 1.000 & 1.000 & 1.000 & 1.000 & 1.000 & 1.000 & 1.000 \\ & 0.333 & 0.499 & 0.499 & 0.333 & 1.000 & 0.000 & 0.980 & 0.016 \\ & 0.708 & & & 0.708 & 1.000 & 1.000 & 1.000 & \\ & & & & 0.708 & 1.000 & & & \\ } . TCA~$= 1.000$ for and on T0--T3 is a guaranteed consequence of the entity-disjoint corpus design pair appears in exactly one example, so the entity bucket always contains clean gold events)---not an empirical surprise. The benchmark is designed to isolate the vocabulary-mismatch failure: BM25, TF-IDF, and Dense all achieve TCA~$= 0.000$ on T1 despite Recall@$k > 0$, confirming Theorem~1 for both lexical and semantic retrievers. The contamination ablation evaluates the harder setting where the entity-disjoint guarantee is relaxed.} BM25, TF-IDF , and Dense all achieve . The superseding blackout event is ranked $ 450$th on average. Recall@5 $ 0.45$--$0.62$ , but TCA = 0 because the superseder is not in the top-5. The failure of MiniLM---a 22M-parameter semantic encoder that achieves R@5 = 0.450 overall---confirms that the gap is not a TF-IDF artifact: even state-of-the-art dense retrieval cannot surface a vocabulary-disjoint superseder in top-5. improves T2 but not T1 or T3.} achieves TCA = 0.980 on T2 by promoting superseders of events in the initial top-20. It achieves TCA = 0.000 on T1 and TCA = 0.016 on T3 for the same reason. and achieve TCA = 1.000 on all hop types.} TCA = 1.000 follows directly from the entity-disjoint corpus design: each pair appears in exactly one example, so the entity bucket retrieved by the two-stage probe contains only the example's gold events, making TCA = 1 guaranteed by construction. The result confirms that the pipeline correctly implements the CAR retrieval target; the benchmark's purpose is to isolate the vocabulary-mismatch failure of baselines , not to demonstrate that TCA = 1.000 is difficult to achieve in the entity-disjoint setting. TF-IDF achieves Acc $ 0.491$ but TCA = 0.012 on T2. This gap reflects ``right answer, wrong reason'': the system retrieves the final-state event but ignores the intermediate chain. TCA correctly flags this as a compliance failure. achieves TCA = 1.000 with ProvRec = 0.708. The system derives correct answers with sound supersession reasoning, but does not always retrieve the full provenance chain . These are separate objectives: TCA captures compliance soundness; ProvRec captures archival completeness. When the corpus contains distractors sharing the query's entity scope < 1$, equivalently $ > 1$ at the entity bucket level), Proposition~ predicts TCA degrades through the $ $ and $R_{}$ terms; Remark~ shows timestamp ordering fails. Table~ confirms this on a contaminated corpus where entity-disjoint distractor construction is relaxed. {!}{ {lcccc} & & & $ $T3 \\ & 0.911 & 0.652 & — \\ & 0.942 & 0.776 & $+12.4$ pp \\ } ). The RSSG provides $+12.4$ pp on T3 by applying domain rules to disambiguate spurious same-entity distractors without oracle access. Full results in .} On the contaminated corpus, the RSSG recovers $+12.4$ pp on T3 and $+3.1$ pp overall TCA. The RSSG is the robustness layer for production settings where entity-scope disjointness cannot be guaranteed. Theorem~1 requires $ > 0$ in natural corpora. We verify this on 120 real security advisories : for each, we compute Jaccard and BM25 between the user query and the CVE disclosure, the patch note. {!}{ {lcc} & & \\ Jaccard & 0.085 & 0.060 \\ Jaccard & 0.037 & 0.014 \\ \ BM25 score & 9.73 & 6.45 \\ \ } Disclosures are $1.51 $ more BM25-relevant to user queries than patch notes, and the gap is positive in 96.7\ The vocabulary gap analysis confirms the gap in real data, but does not directly measure whether retrieval systems because of it. We now run end-to-end retrieval on a real corpus of GitHub Security Advisories. We collect 159 real pairs from GHSA spanning critical, high, medium, and low severity advisories, where each advisory is reviewed by the GHSA team and has a GitHub URL we can fetch via the GitHub REST API. Key structural property: ($0/159$, $0\ We evaluate four retrieval systems and one entity-indexed system. uses a standard term-frequency retriever. uses sentence-transformers/all-MiniLM-L6-v2 . uses intfloat/e5-large-v2 . prompts Llama~3.3~70B to generate a hypothetical patch release note for each query, then embeds the hypothetical with MiniLM and retrieves by cosine similarity---the leading LLM-augmented approach for vocabulary mismatch. extracts the package name and CVE identifier from the query with two regular expressions, then performs entity-indexed lookup with no oracle labels. It matches the oracle entity-probe upper bound exactly because the structured query template admits 100\ All systems rank the full 2,318-document corpus. TCA@$k$ = 1 iff the real patch release note appears in the top-$k$ results. {!}{ {lccccc} & & & & & \\ BM25 & 0.641 & 0.730 & 0.761 & 144.4 & 3 \\ Dense & 0.132 & 0.157 & 0.214 & 1{,}234 & 1{,}847 \\ Dense & 0.069 & 0.120 & 0.170 & 1{,}113 & 1{,}246 \\ HyDE & 0.151 & 0.170 & 0.170 & 1{,}283 & 1{,}577 \\ Regex-NER Probe & & & & 2.0 & 2 \\ } All four retrieval systems fail; only the entity-indexed probe succeeds. Dense achieves TCA@5 = 0.069--- than MiniLM ---because superior semantic matching more strongly associates CVE-framed queries with CVE-framed disclosures, amplifying the gap. HyDE with Llama~3.3~70B achieves TCA@5 = 0.151, statistically comparable to MiniLM : the LLM generates a patch note that includes the CVE identifier in every single generated hypothetical , since the query explicitly names the CVE. The hypothetical therefore embeds closer to disclosures than to real release notes, and retrieval fails for the same structural reason as naive dense. Adding 70B parameters of LLM reasoning to the retrieval pipeline cannot bridge a vocabulary gap that originates in the query itself. The gap between HyDE and Regex-NER Probe is not a matter of model scale or generation quality: it is an architectural gap. Relevance-based retrieval---even when ``relevance'' is computed with respect to an LLM-generated ideal document---cannot recover the correct patch note because the ideal document necessarily shares vocabulary with the wrong class of documents . Only entity-indexed structured retrieval closes the gap. The critical finding is in the Regex-NER Probe row: a system that extracts ``'' and ``'' from the query via two regular expressions, then looks up documents indexed by those entities, achieves TCA = 1.000 without any oracle supervision. This demonstrates that entity-indexed structured retrieval is both and ---it requires no fine-tuned embeddings, no cross-encoder reranking, and no oracle labels; only the architectural insight that supersession retrieval must be entity-scoped. This confirms Theorem~1 on real production data: dense retrieval fails in 87\ E5-large ranks in the MTEB top-10 for retrieval and is the dominant choice in production RAG pipelines. On the GHSA corpus it achieves TCA@5 = --- than the 22M MiniLM baseline and 9.3$ $ worse than BM25 . MTEB rank and parameter count are correlated with TCA on this task: a practitioner optimizing for MTEB would select the model that performs worst here. The TC-MQA metric surfaces a failure mode that MTEB, BEIR, and standard recall@$k$ benchmarks cannot detect, because those benchmarks reward semantic similarity between query and retrieved document---the exact property that causes retrieval failure when the target document is vocabulary-disjoint from the query. The Regex-NER Probe achieves TCA~$= 1.000$ on the structured query template ``Is \{product\} still affected by \{CVE-ID\}?'', but this template encodes entity identity explicitly. In practice, developers ask free-form questions---``Has the issue with path parameters being substituted into URLs without encoding been fixed?''---that describe vulnerability behavior without the CVE identifier. The entity probe cannot be applied to such queries. We generate one free-form question per GHSA pair using Llama~3.3~70B . Each prompt provides the disclosure text and instructs the model to write a natural-language question describing the vulnerability behavior, without including the CVE identifier and without using the structured template. All 159 generated queries were verified to contain no CVE-ID string . BM25 on a free-form vulnerability question finds the CVE reliably (recall@1 = 91.8\ We exploit this asymmetry via a two-stage architecture: : BM25 retrieves the top-$k_1$ documents from the full corpus using the free-form query. : For each Stage-1 result that is a disclosure, use its CVE-ID metadata as a key into the entity index to retrieve the paired patch note. The final ranking places Stage-2 patch candidates first , followed by remaining documents. {!}{ {lcccc} & & & & \\ BM25 & 0.975 & 0.138 & 0.214 & 609 \\ Dense & 0.937 & 0.270 & 0.365 & 764 \\ Dense & 0.962 & 0.252 & 0.321 & 799 \\ HyDE & --- & 0.151 & --- & --- \\ Two-Stage & 0.975 & & --- & 43.2 \\ } .} Table~ shows the full results. The two-stage pipeline achieves TCA@5~$= $ on free-form queries---a $3.6 $ improvement over the best all-retrieval baseline and within 2.5~pp of the Regex-NER oracle . The equality TCA@$k$ = Disc@$k$ holds exactly: when the disclosure is the $j$-th document in BM25 top-$k$, the paired patch note is placed at rank $j$ in the final ranking; TCA@5 is thus determined entirely by whether BM25 retrieves the disclosure in its top-5. Increasing $k_1$ from 5 to 20 does not improve TCA@5 : the 4 remaining failures all arise from disclosures at BM25 rank~$ 9$---even when promoted to positions 9--18 in the final ranking, they fall outside the top-5. Three failures involve queries that are too generic to distinguish their specific package , and one involves a niche npm package with no BM25 vocabulary overlap. HyDE achieves TCA@5~$= 0.151$ on free-form queries--- to its performance on structured queries . Without the CVE identifier in the query, the LLM generates a patch note from the vulnerability description, which inherits the original vulnerability vocabulary rather than the patch-note vocabulary. LLM generation cannot predict the release-note vocabulary of an unknown vendor. BM25 achieves higher disclosure recall at every cutoff (Disc@1 = 91.8\ A cross-encoder reranker achieves TCA@5~$= 0.233$ on the 159 free-form GHSA queries---below any dense baseline and $4.2 $ worse than Two-Stage . The failure is architectural, not a reranking quality issue: the union pool of BM25-top20 and Dense-top20 contains the gold patch note only when at least one retriever already recovers it in top-20. Since both BM25 and Dense fail to retrieve the patch note for most queries, the cross-encoder never scores it. Applying a stronger reranker to the wrong candidate pool cannot close an architectural gap. Artifact: . Production tools such as OSV.dev are often proposed as alternatives to CAR-style retrieval. We evaluate the distinction on the same 159-pair GHSA corpus under two query modes. In , BM25 achieves TCA@5~$= 0.164$ and Two-Stage achieves TCA@5~$= 1.000$. OSV.dev was invoked by applying a CVE-ID regex to each disclosure text; only 3.77\ The gap between modes is categorical: production tools are in Mode~2, not merely degraded. They solve a different problem from CAR . The Two-Stage architecture is the only system that operates in both modes because BM25 reconstructs the anchor from query vocabulary without requiring the CVE identifier. Table~ shows TCA@5 and disclosure recall@5 across two encoder families at three parameter scales. Three findings stand out: : no dense model exceeds TCA@5 = 0.283, identical to the 22M MiniLM baseline. A 335M model is no better than a 22M model. : E5-small $>$ E5-base $>$ E5-large ---larger encoders match the query-to-disclosure semantic alignment more precisely, ranking the disclosure above the patch note with greater confidence. : BGE-small achieves $0.252$, MiniLM; BGE-base and BGE-large recover to $0.283$---the same ceiling as a 22M model. Unlike E5, larger BGE does not worsen TCA, but it does not escape the $0.283$ ceiling either. Both failure modes converge on the same conclusion: scale is irrelevant when the architecture is wrong. {!}{ {lrrcc} & & & & \\ MiniLM-L6-v2 & 22M & MiniLM & 0.962 & 0.283 \\ E5-small-v2 & 33M & E5 & 0.987 & 0.264 \\ E5-base-v2 & 109M & E5 & 0.950 & 0.239 \\ E5-large-v2 & 335M & E5 & 0.956 & \\ BGE-small & 33M & BGE & 0.981 & 0.252 \\ BGE-base & 109M & BGE & 0.975 & 0.283 \\ BGE-large & 335M & BGE & 0.987 & 0.283 \\ {l}{} & 0.975 & \\ } , a $3.4 $ improvement over any dense baseline. Artifact: .} Theorem~ implies a clean architecture: retrieve the semantically-aligned anchor document in Stage~1; follow entity-indexed supersession to the controlling authority in Stage~2. Adapting to a new domain requires specifying three attributes---, , . The three released domain adapters and evaluation code are described in Appendix~. Table~ verifies this architecture holds across three domain constructions with different query styles. {!}{ {llccc} & & & & \\ Security & Generic free-form & 0.975 & 0.138 & \\ CVE & Generic free-form & 0.860 & 0.000 & \\ CVE & + product hint & 1.000$^ $ & 0.000 & \\ Legal & Generic free-form & 0.312 & 0.000 & 0.312 \\ Legal & + party-name hint & 1.000 & 0.000 & \\ } ). $^ $CVE+product-hint exception: product names are shared across CVEs, so product hints cause false anchor retrievals; free-form queries avoid this. Artifacts: , .} TCA@$k$ of the two-stage pipeline equals Stage-1 anchor recall at $k$ exactly . Stage-1 recall depends on the vocabulary discriminativeness of the free-form query relative to the anchor corpus: CVE disclosures contain domain-specific attack vocabulary that free-form vulnerability questions echo, yielding 86--97.5\ Corollary~ states this identity precisely. The practical implication: . Any method that better retrieves anchor documents directly transfers to TCA improvement with no architectural changes. To confirm Theorem~1 is not a compliance artefact, we build two additional controlled benchmarks: and , each with a T0/T1 split matching the compliance construction. Table~ shows BM25 and TF-IDF achieve TCA~$= 0.000$ on T1 in all three synthetic domains; achieves TCA~$= 1.000$. Theorem~1 is a structural property, not a compliance artefact. {!}{ {lccc} & & & \\ BM25 / TF-IDF & 0.000 & 0.000 & 0.000 \\ & 1.000 & 1.000 & 1.000 \\ & & & \\ } The legal vocabulary gap is structural: of 82 CourtListener overruling snippets, 88\ We build a second real end-to-end benchmark from 122 SCOTUS overruling pairs . Queries: ``Is still good law and controlling precedent?'' Each opinion is prefixed with its case name and citation. Theorem~'s identity applies directly : TCA@5 equals Stage-1 anchor recall@5. {!}{ {lccc} & & & & \\ } , .} BM25 direct is competitive : overruling opinions explicitly cite the case they overrule, giving lexical retrieval a direct signal unavailable in GHSA or FDA. Dense direct fails : semantic similarity actively prefers the opinion over the overruling one. Two-Stage with Dense anchor achieves 0.926 ---a ; the 95\ A third confirmation in medical/regulatory: FDA drug recall enforcement actions formally supersede approved drug labels with completely disjoint vocabulary . We collect 500 real pairs via OpenFDA APIs . Queries use indication text without the drug name: ``Is the drug indicated for still approved?''---therapeutic vocabulary only. Only 8.2\ {!}{ {lccc} & & & & \\ } Both direct retrieval systems collapse: BM25 achieves TCA@5~$= 0.004$ and Dense achieves TCA@5~$= 0.064$ . This is the most dramatic vocabulary gap of any benchmark in this paper. Recall notices use administrative language about lots, contamination, and sterility; a query about a drug's therapeutic indication shares zero meaningful vocabulary with a typical recall notice. Relevance-based retrieval has no signal to rank the recall notice above 499 competing documents---it is architecturally blind to the supersession relation. The Two-Stage architecture achieves TCA@5~$= 0.774$, a . Theorem~ confirms this equals Stage-1 anchor recall@5 exactly: when BM25 successfully retrieves the drug's approved label in Stage-1, the entity-indexed Stage-2 lookup invariably finds the correct enforcement notice. The 77.4\ TCA measures retrieval correctness, but the ultimate risk is downstream: a language model given superseded evidence will produce confident, incorrect answers that a practitioner may act on. We test this directly by running GPT-4o-mini on top of Dense and Two-Stage retrievers on the 159 GHSA pairs. Each query is issued against a 2,318-document corpus. Dense retrieval returns MiniLM top-5; Two-Stage returns BM25 Stage-1 top-5, entity-indexed to promote the patch note. GPT-4o-mini is prompted to answer: using only the retrieved documents. The gold answer for every pair is . We classify each response as: , , or . Table~ shows the outcome. Dense retrieval correctly identifies the patch for only 61.0\ {!}{ {lcccc} & & & & \\ Dense & 28.3\ Two-Stage & 97.5\ } . ``Says Patched'' = LLM asserts the vulnerability is fixed; ``Conf.\ Wrong'' = LLM explicitly asserts it is patched. Dense retrieval causes confident wrong assertions for 39\ This result confirms that retrieval architecture directly determines downstream LLM reliability, not just benchmark TCA metrics. A practitioner relying on Dense RAG would receive explicit ``this vulnerability is unpatched'' assertions for 39\ Table~ unifies the three real-world benchmarks. The failure pattern of dense retrieval is consistent across security, legal, and medical/regulatory domains, while the two-stage architecture consistently recovers. In the legal domain, BM25 is partially competitive , but dense retrieval fails even there , confirming Theorem~1 as a domain-general law for semantic encoders, not a compliance artefact. {!}{ {lrcccc} & & & & & \\ Security & 159 & 0.138 & 0.270 & & $+70.5$~pp \\ Legal & 122 & 0.893 & 0.172 & & $+75.4$~pp \\ Medical & 500 & 0.004 & 0.064 & & $+71.0$~pp \\ } HotpotQA , MuSiQue , and 2WikiMultiHop require multi-step retrieval. IRCoT , PropRAG , and HippoRAG 2 improve multi-hop retrieval through chain-of-thought interleaving, graph propagation, and associative memory. These systems assume all required documents are retrievable by relevance. TC-MQA breaks this assumption: the superseding document may be semantically orthogonal to the query , making relevance improvement insufficient. T-GRAG resolves temporal redundancy in graph RAG. VersionRAG handles version-aware retrieval for evolving documents. TempRALM incorporates timestamps into retrieval scoring. LedgerRAG provides governance-aware agentic retrieval. These works address ---favoring newer documents---whereas TC-MQA requires detecting that a specific later document an earlier one under domain rules. The difference is structural: recency preference is a soft ranking adjustment; supersession is a hard validity constraint. Importantly, CAR is not equivalent to time-aware retrieval: the authority relation $ $ is not timestamp ordering---a superseding document can predate its superseded counterpart when effective dates differ from publication dates , so timestamp-conditioned ranking cannot recover $ ))$ in general. Chalkidis et al.\ provide the earliest NLP recognition that temporal filtering is essential for regulatory compliance IR, showing that BM25 with chronological constraints outperforms pure neural re-rankers on EU/UK directive retrieval---an empirical precursor to Theorem~1's mismatch result. Zhang et al.\ find that LLMs backed by standard RAG consistently fail to identify when legal precedents are overruled---a direct real-world instantiation of Theorem~1. Stammbach et al.\ document that existing legal retrieval benchmarks fail to transfer to practitioner contexts, underscoring the need for domain-specific retrieval objectives. Di Florio et al.\ formalize legal conflict resolution with temporal ordering, providing formal apparatus complementary to our empirical framework. CaseFacts benchmarks precedent retrieval. RIRAG and FinDER address regulatory and financial QA without supersession structure. The closest prior work is de Martim , who proposes SAT-Graph RAG---an ontology-driven graph RAG that models legislative events as queryable nodes and handles point-in-time legal retrieval. Like CAR, SAT-Graph RAG recognizes that flat-text retrieval is blind to the diachronic structure of law and uses a graph to represent norm supersession. The key distinction is theoretical: SAT-Graph RAG is an engineering system for structured legal corpora with no formal retrieval objective, no correctness characterization , and no hardness result showing that scope-indexed algorithms face an identifiable worst-case ceiling . CAR defines $ ))$ as an independent retrieval objective applicable to any authority-governed domain, and proves necessary-and-sufficient conditions on retrieved set independent of how it was produced. Temporal KG QA and TDBench address time-sensitive graph queries over structured KGs. TC-MQA is the unstructured-text analogue: supersession must be inferred from document type and timestamps rather than read from graph edges. Weller et al.\ prove theoretical limits of embedding-based retrieval; Theorem~1 is a domain-specific instantiation with an explicit supersession construction. Barnett et al.\ catalog seven RAG failure modes; is a distinct eighth mode not in their taxonomy---one where the correct answer is provably unreachable by relevance ranking. Rashtchian and Juan study retrieval sufficiency; TCA formalizes a stronger sufficiency notion that includes temporal validity. Formalizing CAR opens several research directions, each corresponding to a relaxation of one component of $ ))$. The four generalized CAR variants are defined formally in ; the directions below elaborate their open problems. . When scope is unobserved, the retrieval problem becomes inference over $p $. The key open problem: given a query-time NER error rate $ $, what is the achievable TCA ceiling? Proposition~2 gives a $ $-parameterized upper bound; the latent-scope case requires integrating over the scope posterior. . Learning $ $ from text is a new structured prediction problem: the target relation is ``B formally voids A under domain rules,'' not ``A happened before B.'' Risk-sensitive frontier recovery under uncertain edges connects to distributionally-robust optimization. . The lattice of active frontiers under $m$ interacting orders is an open combinatorial problem. For $m = 2$, frontier recovery generalizes to finding the Pareto front under two partial orders; for $m 3$, this is computationally hard in general . . Maintaining $ ))$ under document arrivals has amortized update cost $O $ per insertion---but the frontier can change discontinuously when a new document supersedes multiple existing ones. Batch-invalidation and lazy-update strategies are unexplored. Train representations so that $ $ is geometrically identifiable---anchors and their controlling authorities cluster in authority-closure space rather than semantic space. The new contrastive objective: $d d'$ should bring $d$ and $d'$ close in embedding space without conflating their semantic roles. Given lookup budget $B$, maximize $| ))|$ subject to $B$. Proposition~3 gives the lower bound on necessary lookups; the gap between the lower bound and practical budget is the open problem. This connects to adaptive submodular maximization. CAR is one instance of the broader class where the retrieval target is the normatively correct document under a rule system, not the most relevant one. Characterizing the complexity of this class---which problems reduce to CAR, which require stronger primitives---is the natural theoretical generalization. The FinSuperQA benchmark uses structured employee IDs that admit 100\ On an entity-disjoint corpus---where each pair appears in exactly one example---anchor-based probing fully solves T0--T3 . In corpora where entities recur across cases , the entity probe surfaces events from all cases, requiring additional disambiguation. The contamination ablation shows RSSG recovers $+12.4$~pp in this regime ; closing the full gap requires case-boundary metadata or temporal windowing not always available in practice. The 10 rules cover representative compliance scenarios. Production deployment would require domain-expert rule specification for a specific regulatory framework. The cross-encoder reranker and OSV.dev are now evaluated . The cross-encoder confirms that reranking within a relevance-ranked pool cannot substitute for entity-indexed Stage-2 retrieval. OSV.dev is 0\ Theorems 1--4 and Propositions 1--3 characterize . The four generalized variants formalize the extensions: latent-scope, probabilistic-authority, multi-authority, and streaming CAR. Each corresponds to relaxing exactly one component of $ ))$ and maps to a specific term in Proposition~1---scope posterior for $ $, edge confidence for $ $, order count for the lattice structure, and corpus dynamics for $R_{}$. The paper makes two primary theoretical claims about the class of authority-governed retrieval problems, independent of any particular system. gives the necessary-and-sufficient conditions on any retrieved set $$ for TCA $= 1$: frontier inclusion )) $) and no-ignored-superseder. Any future algorithm---learned, hybrid, or rule-based---can be audited against these two conditions without reference to how it was built. establishes that scope-indexed algorithms face a hard worst-case ceiling: $@k R_{} $. The proof is adversarial, not average-case: when $ $ authority chains share entity scope, no algorithm that cannot distinguish them achieves more than $1/ = $ conditional success probability. These two results are supported by three theorems that characterize the problem structure: Theorem~1 > 0$), Theorem~2 , and Theorem~3 = 1$). Proposition~1 organizes the picture into the factorization $ = R_{} R_{}$, whose three terms are separable failure modes . Proposition~3 establishes the computational cost of disambiguation under an explicit oracle model. Three independent real-world corpora confirm that the proved structure is consequential: Dense TCA@5 $ \{0.064, 0.172, 0.270\}$ across FDA, SCOTUS, and GHSA; Two-Stage achieves $\{0.774, 0.926, 0.975\}$. The 77.4\ The four formal generalizations identify the next layer of open problems: latent-scope CAR, probabilistic-authority CAR, multi-authority CAR, and streaming CAR. Each is a precisely stated problem class, not a vague direction, and each inherits Theorem~4 as the correctness target. {!}{ {llll} & & & \\ R1 & BlackoutAnnounced & PreClearanceApproved & Same ticker \\ R2 & BlackoutLifted & BlackoutAnnounced & Same ticker \\ R3 & EmergencyException & BlackoutAnnounced & Emp + ticker \\ R4 & ExceptionRevoked & EmergencyException & Emp + ticker \\ R5 & WatchlistAdded & PreClearanceApproved & Same ticker \\ R6 & WatchlistRemoved & WatchlistAdded & Same ticker \\ R7 & ConflictAmended & ConflictDisclosed & Same emp \\ R8 & ConflictCleared & ConflictAmended & Same emp \\ R9 & ConflictCleared & ConflictDisclosed & Same emp \\ R10 & PolicyUpdate & PolicyAcknowledged & Global \\ } ``What is the pre-clearance status for \{emp\}'s requested trade in \{sec\}?'' ``Is \{emp\}'s pre-clearance for \{sec\} still valid?'' ``Can \{emp\} currently trade \{sec\} given all recent compliance updates?'' ``What is \{emp\}'s current compliance status for trading \{sec\}?'' Robertson's BM25 with $k_1 = 1.5$, $b = 0.75$. TF-IDF cosine similarity with smoothed IDF. All events vectorized offline. Two graphs are used. : isolated, BFS transitive closure; $|K| 3$, used only for TCA ground truth. : built at query time from all entity-scoped events returned by the full-corpus entity index; rules R1--R10 applied pairwise by event type and timestamp comparison. Used for answer derivation in . $ $ precomputed once over the 12,250-event corpus. Active $>$ active / $>$ active $>$ active $>$ . Adapting the two-stage pipeline to a new authority domain requires three attributes: : document type that is semantically aligned to the query . : document type that formally voids the anchor . : entity fields that co-scope anchor and superseder . Three domain adapters are released alongside this paper: , , and . Each adapter is a self-contained Python script that fetches or loads its domain dataset, builds the entity index, runs all baselines, and writes results to . runs the full synthetic FinSuperQA benchmark. The custom retriever class must implement , where is the list of document dicts and the return value is a list of strings. The scorer computes TCA, Acc, ProvRec, and R@$k$ on all 1,000 examples with a single call: python evaluate.py --retriever_path my_retriever.py --k 5 Output is written to . All datasets, domain adapters, evaluation scripts, and the falsification suite are available at . 