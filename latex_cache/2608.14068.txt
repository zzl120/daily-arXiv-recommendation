 {acmart} }} =1.5em {GPT+Catalog} {Gemini+Catalog} { MACS: A Hybrid Multi-Agent Framework for Reliable Conversational E-Commerce Recommendation } } } } } } } } {Huang et al.} Conversational recommendation for e-commerce is increasingly mediated by large language models , yet many real-world deployments operate under a stricter requirement: recommendations must be drawn only from a merchant's fixed catalog, without web search or unsupported product claims. In this setting, the main challenge is reliability under hard constraints: the system must satisfy user requirements, remain grounded in available inventory, and preserve preferences across multiple conversational turns. We present MACS , a hybrid multi-agent framework for reliable conversational recommendation in fixed-catalog settings. MACS uses LLMs for language-facing tasks such as interpreting user requests, eliciting preferences, and generating responses, while correctness-critical operations, including product retrieval, hard-constraint filtering, brand exclusion, and progressive relaxation, are executed deterministically by the merchant agent. A session-persistent preference layer tracks constraints across turns, enabling consistent handling of budget overwrites and exclusion reversals. On a 140-query single-turn benchmark, MACS achieves the highest pass rate (87.1\ <ccs2012> <concept> <concept_id>10002951.10003317.10003347.10003350</concept_id> <concept_desc>Information systems~Recommender systems</concept_desc> <concept_significance>500</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010345.10010353</concept_id> <concept_desc>Computing methodologies~Question answering</concept_desc> <concept_significance>300</concept_significance> </concept> <concept> <concept_id>10010147.10010257.10010293.10010294</concept_id> <concept_desc>Computing methodologies~Multi-agent systems</concept_desc> <concept_significance>300</concept_significance> </concept> <concept> <concept_id>10003120.10003130</concept_id> <concept_desc>Applied computing~Electronic commerce</concept_desc> <concept_significance>100</concept_significance> </concept> </ccs2012> {Information systems~Recommender systems} {Computing methodologies~Question answering} {Computing methodologies~Multi-agent systems} {Applied computing~Electronic commerce} Conversational recommender systems increasingly rely on large language models to interact with users in natural language, elicit preferences, and generate recommendations. A particularly important and underexplored deployment setting is : a merchant agent using its own fixed product database without web search to enforce the recommendation of verified inventory while respecting user constraints. This setting gives merchants control over product data and avoids the latency and hallucination risks of web-augmented search. However, it places strict reliability demands on the recommender. LLMs, despite their fluency, remain unreliable in this setting: they may hallucinate product specifications not present in the catalog, forget earlier brand exclusions or other user preferences across conversational turns, or apply budget filters inconsistently. Therefore, a recommendation system that returns out-of-catalog products or violates a user's stated constraints fails a core reliability requirement. is a hybrid agent-to-agent e-commerce framework for reliable conversational recommendation in fixed-catalog settings. MACS is composed of two cooperating agents: a that handles natural-language input, intent routing, and response generation; and a that enforces hard constraints deterministically and retrieves products exclusively from the fixed catalog. The shopping agent communicates with the merchant agent via structured commerce protocols, specifically Universal Commerce Protocol ~ or Agentic Commerce Protocol ~. Because the shopping agent cannot retrieve products directly, catalog grounding is enforced at the architectural level rather than through prompt instructions. Three core contributions are made as follows: A hybrid shopping-agent to merchant-agent architecture separates LLM-based natural-language interaction from deterministic constraint enforcement, enabling reliable product recommendation over a fixed, merchant-owned catalog without web search. Session-persistent preference state management preserves user constraints across turns, supports updates such as budget overwrites and exclusion reversals, and enables consistent multi-turn recommendation behavior. A hybrid evaluation methodology separates deterministic constraint correctness from response quality, enabling faithful assessment of conversational recommenders in both single-turn and multi-turn limited-catalog settings. MACS sits at the intersection of three active research areas: conversational recommender systems, knowledge-graph-augmented retrieval, and LLM reliability under hard constraints. Prior work in each area motivates a component of the MACS design. However, none addresses the combination of deterministic constraint enforcement, session-persistent preference state, and fixed-catalog grounding that defines limited-catalog commerce-agent settings. CRSs elicit user preferences through multi-turn interaction~, ranging from bandit-based question selection~ and RL-based dialogue policy learning~ to LLM-integrated pipelines for preference understanding and response generation~. Retrieval augmentation~, cross-session memory~, knowledge-enhanced sequential modeling~, and behavioral evaluation~ have further extended the paradigm. Kostric et al.~ show that generating usage-oriented clarifying questions yields richer preference signals in multi-turn CRS, underscoring the need for explicit cross-turn preference-tracking mechanisms. MACS addresses a gap these works leave open: deterministic enforcement of hard user constraints across turns, with safeguards to combat LLM unreliability. Knowledge-aware recommenders (CKE~, KGCN~, KGAT~) incorporate a graph structure into learned representations; MACS uses a knowledge graph operationally for structural queries , compatibility) via explicit Cypher queries over a knowledge graph, so the retrieval path can be directly inspected. By contrast, learned embeddings produce similarity scores that are opaque to inspection. Shopping-agent benchmarks (WebArena~, AgentBench~, ShoppingBench~, $ $-Bench~) study the side; MACS studies both the shopping-agent and complementary design that provides structured, constraint-enforcing catalog access. LLM faithfulness under hard constraints (e.g., brand exclusions, budget ceilings, specification minimums) is a known open problem. Chain-of-thought prompting reduces but does not eliminate constraint violations in multi-turn settings~. Preference elicitation approaches such as EAR~ and UniCRS~ ask clarifying questions to narrow item sets before retrieval, but treat constraint enforcement as an LLM inference task rather than a deterministic predicate~. MACS decouples these two responsibilities: preference elicitation remains an LLM task, while constraint enforcement is delegated to SQL predicates persisted across turns in a typed session-state layer. As a result, constraint satisfaction does not depend on the quality of the LLM’s generation. Prompt-based approaches cannot guarantee this, because they still rely on the model itself to enforce constraints. We formalize limited-catalog conversational recommendation as a constrained multi-turn retrieval problem over a fixed catalog: A merchant catalog $$ contains products with structured attributes . Over a multi-turn conversation, the system must extract user constraints, retrieve $R_t $ satisfying all accumulated constraints at turn $t$, and generate factually grounded responses. It also handles add-to-cart, checkout, and service queries through the same session. Three reliability requirements follow from the limited-catalog setting: catalog grounding , constraint correctness (hard requirements enforced deterministically, not inferred), and cross-turn persistence (constraints active until explicitly revised, never silently dropped). MACS separates a (LLM-based preference elicitation, intent routing, response generation) from a (SQL constraint filtering, knowledge graph traversal, session-persistent state). The shopping agent never retrieves products directly; all calls go through the merchant agent's structured API, so the LLM cannot hallucinate catalog contents. Figure~ illustrates the two agents and their interaction diagram. Sections~ and~ describe each component. On each turn, the shopping agent interprets the user’s message, determines the action to take , extracts structured preference slots, and requests catalog results from the merchant agent before generating a response. All correctness-critical operations, including product retrieval, hard-constraint enforcement, exclusion of disallowed brands or attributes, and progressive relaxation of lower-priority constraints when necessary, are handled by the merchant agent . Each turn passes through query rewriting, intent routing, structured extraction, and catalog-grounded execution. injects structured hints before any LLM call (e.g., ``for my son'' $ $ ; ``gaming Chromebook'' $ $ a ChromeOS-compatibility note). intercepts common intents with deterministic keyword rules, while ambiguous requests are handled by an LLM router. converts the user's message into typed slot-value pairs, such as budget, excluded brands, and required specifications, and resolves follow-up references across turns, including requests like ``show me something cheaper'' or ``add the second one to the cart.'' The session state is a typed slot dictionary with three key properties: Slot values persist across turns unless explicitly revised. References such as ``add the second one'' are resolved against the most recent recommendation context. Brand exclusions are enforced at the SQL level and in a post-retrieval title filter, so no excluded brand appears regardless of database condition labels. Updated constraints override earlier ones when appropriate: a new budget ceiling replaces the previous value, and a statement such as ``actually HP is fine'' reverses an earlier brand exclusion. When the query contains no hard constraint (brand exclusion, price ceiling, or specification minimum) and the message is fewer than four words, the agent asks a targeted follow-up question rather than guessing. When at least one hard constraint is present with a sufficiently specific message, it proceeds directly to search. The recommendation output presents products that satisfy the current constraints, explains why they were selected, and provides follow-up options for refinement, comparison, and further catalog browsing. The merchant agent enforces hard constraints deterministically (SQL filtering, brand exclusion, progressive relaxation) and resolves complex structural queries via knowledge graph traversal. Operating exclusively over the merchant's catalog, it guarantees that every product exists at a verified price. {!}{ [ box/.style={ rectangle, draw, thick, rounded corners=4pt, align=center, minimum height=0.95cm, font= }, arrow/.style={ -{Latex }, thick, >=latex }, title/.style={font= , anchor=west, inner sep=2pt}, node distance=0.55cm and 0.7cm ] at { --- ``looking for a gaming laptop''}; [box, fill=gray!10, below=0.9cm of f1title.north west, anchor=north west, minimum width= ] { \\``gaming laptop''}; [box, fill=blue!12, right= of u1.east, anchor=west, minimum width= ] { \ request\ UCP request}; [box, fill=green!12, right= of ai1a.east, anchor=west, minimum width= ] { \ UCP request\ KG $ $ items\ , shipping}; [box, fill=green!12, right= of ma1a.east, anchor=west, minimum width= ] {Create UCP\ }; [box, fill=blue!12, right= of ma1b.east, anchor=west, minimum width= ] { \ response\ to user}; [box, fill=gray!10, right= of ai1b.east, anchor=west, minimum width= ] {\ results}; -- ; -- ; -- ; -- ; -- ; at { --- ``add this to cart''}; [box, fill=gray!10, below=0.9cm of f2title.north west, anchor=north west, minimum width= ] { \ product\ to cart}; [box, fill=blue!12, right= of u2.east, anchor=west, minimum width= ] { \ query\ UCP request}; [box, fill=green!12, right= of ai2a.east, anchor=west, minimum width= ] { \ UCP request\ to cart\ inventory}; [box, fill=green!12, right= of ma2a.east, anchor=west, minimum width= ] {Create UCP\ }; [box, fill=blue!12, right= of ma2b.east, anchor=west, minimum width= ] { \ response\ to user}; [box, fill=gray!10, right= of ai2b.east, anchor=west, minimum width= ] {\ updated}; -- ; -- ; -- ; -- ; -- ; } MACS exposes three protocol families through a shared backend interface, so adding a new protocol requires only a request adapter and response formatter. ~ provides four typed tool calls (, , , ) with observable response envelopes. ~ follows Google's schema for Gemini-based agents. ~ adds a checkout session lifecycle and webhooks. A configuration variable selects UCP or ACP at deployment time. Three data stores serve distinct roles. holds the authoritative product catalog (21,000+ items; 1,490+ laptops with 12 structured specification fields per product: CPU, RAM, GPU, storage type, display size, battery life, weight, OS, price, brand, review count, and rating). Constraint filtering, brand exclusion, and progressive relaxation all execute as SQL queries against this store. encodes product relationships across 2,400 nodes and 8,500 edges in two actively-queried relationship types: and . Complex intent queries (``show me similar items,'' ``find a compatible accessory'') are resolved via Cypher traversal in $ $17~ms with no LLM call. The graph degrades gracefully to SQL-based search when unavailable, ensuring no hard dependency on the graph layer. caches search results, product summaries, price data, and LLM-generated narratives using per-entry Time-To-Live expirations . This caching layer achieves $ $12$ $ latency reduction on cache hits . Extracted slot values map directly to SQL clauses. Brand exclusions are enforced both at the SQL level and in a post-retrieval title filter. This dual enforcement is necessary because some entries in our catalog store condition metadata in the brand field rather than the manufacturer name. When hard constraints produce fewer than three results, the system applies a sequence of relaxation steps ordered by constraint importance. At each step, the least-critical optional specification constraint is dropped first. Price ceilings and brand exclusions, however, are never relaxed. The user always sees real, purchasable products; the system never returns an empty result set or fabricated alternatives. When relaxation occurs, the response explicitly discloses which constraints were loosened, so the user is not misled about what was actually enforced. Figure~ shows the comparison and checkout view, in which side-by-side pros/cons and delivery options are resolved from the relational store and presented without LLM-generated fabrication. {proscons.png} The best-value pick uses a weighted scoring function tuned to the detected use case: = w_{} r_p + w_{} + \! + w_{} where $p$ denotes a product; $r_p $ is the normalized price score computed as $1 - / ({ price}_{ } - { price}_{ })$, so lower-priced products receive higher scores within the retrieved set; $$ is the star rating; $n$ is review count; $c_{}$ is a use-case-specific cap on the review-volume confidence boost ; and $ $ is a use-case specification score: a deterministic weighted sum over structured specification fields (e.g., GPU tier for gaming, RAM tier for ML/video editing, battery life for student use). Unlike a learned similarity score, $ $ is computed directly from catalog attributes. Table~ reports the full weight vectors. }=0.05$ for all).} {!}{ } Use case & $w_{}$ & $w_{}$ & $w_{}$ & $ $ components \\ Gaming & 0.15 & 0.20 & 0.60 & GPU tier + refresh rate + RAM $ $16\,GB \\ ML & 0.15 & 0.20 & 0.60 & RAM tier + GPU \\ Video edit. & 0.15 & 0.20 & 0.60 & GPU tier + RAM tier \\ Student & 0.45 & 0.30 & 0.20 & Battery tier + RAM tier \\ Programming & 0.20 & 0.30 & 0.45 & RAM tier only \\ Default & 0.35 & 0.35 & 0.20 & RAM tier only \\ } This section describes the evaluation framework and reports all results. No human participants were involved in benchmark construction or scoring; all evaluations use automated deterministic checks and LLM-as-judge scoring. First, Section~ defines single-turn evaluation and its associated metrics. Section~ defines multi-turn evaluation and scenario design. Next, Section~ describes the catalog-bound baselines and justifies their selection. Section~ reports all numerical findings. A is defined as follows: the user submits a natural-language request, the system may ask at most one clarifying question, and the system then returns a catalog-grounded recommendation. This protocol evaluates recommendation quality in isolation from multi-turn preference accumulation, which is assessed in Section~. The evaluation separates (enforcing price, brand, and specification requirements) from (helpful, well-explained recommendations). Hard constraints are evaluated deterministically via SQL verification. For response quality, G-Eval~ is applied: an LLM-as-judge evaluation framework that scores free-text responses against a rubric without requiring gold-standard reference answers. G-Eval is restricted to quality assessment to avoid circularity with the constraint-checking component. For each benchmark query, expected filter values and exclusions are derived by executing the constraints directly against the relational product store as SQL predicates (e.g., { For response narrative quality, G-Eval is applied with a custom rubric evaluating engagement , tone calibration , and factual accuracy/coherence , normalized to $ $. The judge is GPT-4o-mini at temperature 0 for reproducibility. It evaluates whether explanations are factually grounded in the returned products, not whether they resemble a reference answer. Calibration examples anchor the three discrete score levels: 0.0 (poor or irrelevant response), 0.5 , and 1.0 ; higher values are strictly better. The final per-query score combines deterministic and quality components: s = 0.40\,s_{} + 0.20\,s_{} + 0.10\,s_{} \\ + 0.05\,s_{} + 0.10\,s_{} + 0.15\,s_{} where $s_{} \{0,1\}$ is a binary response-type match (1 if the system produces a recommendation when one is expected, or a clarifying question when the query is underspecified; 0 otherwise); $s_{} $ is the fraction of returned products respecting all stated brand exclusions; $s_{} $ is the fraction of returned products satisfying price and specification constraints; $s_{} $ is the fraction of returned products that are currently available in inventory (i.e., not marked out-of-stock or discontinued in the catalog at query time); $s_{} $ measures response explainability deterministically via three equal-weight sub-checks: whether the message contains structured explanation elements (bullet lists or explanatory connectives such as or ), at least one specific product attribute , and an explicit disclosure of applied constraints ; each sub-check contributes equally, so $s_{} = / 3$; and $s_{} $ is the G-Eval narrative quality score. Weights for components inapplicable to a given query (e.g., $s_{}$ when no brand exclusion is present, or $s_{}$ for clarification-only responses) are redistributed to $s_{}$, raising its effective weight to 45--60\ This redistribution preserves the total weight budget and avoids penalizing queries that do not exercise a particular constraint type (a query with no brand exclusion receives no fixed zero for brand compliance). For text-only baselines , only $s_{}$ and $s_{}$ contribute to the composite score. Brand compliance is evaluated separately via a negation-window regex over free-text responses and is reported in Table~; it is not incorporated into the composite. To note, filter compliance is marked N/A for text-only systems, as price verification from free text is unreliable. Queries were authored by the research team to cover five user intent classes: direct purchase (e.g., ``gaming laptop under \$1{,}000 with at least 16\,GB RAM, no HP''), multi-constraint filtering, vague preference (e.g., ``I need something for school''), service inquiry (e.g., ``What is your return policy on refurbished laptops?''), and product comparison. Each query is assigned a buyer persona and classified as either (containing $ $1 extractable hard constraint: brand exclusion, price ceiling, or minimum specification) or . Query construction details are provided in ~. Two nested sets are used. The supports a three-way comparison across MACS, , and . The covers 40 query groups, including catalog exploration, follow-up service questions, post-recommendation refinements, and orchestrator routing; evaluated on MACS only . Pass threshold: score $ 0.5$. The G-Eval judge introduces $ $0.02--0.03 score variance between identical runs at temperature~0. Pass@$k$~, defined as the fraction of $k$ independent evaluation runs that pass, provides a robustness measure for this variance. Pass@5 is measured on the full 225-query benchmark using a pre-fix code snapshot: MACS achieves a macro Pass@5 of $0.742$, with mean score $0.609 0.010$ across five independent runs (per-run averages: $0.622, 0.614, 0.611, 0.603, 0.596$; pass rate $74.2\ The $ 0.010$ Standard Deviation confirms that single-run results lie within one SD of the multi-run mean; the same variance bound applies to the improved result of $0.681$ from the final code version. Single-turn evaluations cannot assess the properties that distinguish agentic systems from stateless LLM calls: constraint persistence, preference updates, and brand-exclusion reversals across turns. A 10-scenario benchmark is designed covering three fundamental slot operations: accumulation , update (existing constraint replaced), and reversal . Scenarios span six constraint-persistence patterns: constraint accumulation, use-case pivot , brand-exclusion persistence, vague-to-specific elicitation, budget overwrite, and exclusion reversal. Additionally, one 5-turn long session and one dense simultaneous-enforcement first turn are included; each scenario contains 3--5 scripted turns. Evaluating at K=5 runs per scenario yields 50 data points per system for variance-aware macro Pass@5 comparison. Table~ lists all ten scenarios by category. The multi-turn score combines a judge score with deterministic constraint checks: s_{} = 0.55\,s_{} + 0.45\,s_{} $s_{}$ is computed from deterministic checks against the system's final-turn product list: brand exclusions present/absent, budget ceiling respected. Importantly, constraint checks are computed before the judge is called, and their pass/fail results are included in the judge prompt. This prevents the judge from incorrectly marking a response as violating constraints that were enforced during retrieval but not explicitly mentioned in the text. $s_{}$ is GPT-4o-mini evaluating the full conversation transcript on constraint satisfaction , preference tracking accuracy , and response appropriateness . Pass threshold: $ 0.65$ (raised from 0.50 to eliminate floor effects: a system with $s_{}=1.0$ and $s_{}=0$ would auto-pass at threshold 0.50 since $0.55 1 + 0.45 0 = 0.55 0.50$). is the average of $s_{}$ across all $K$ independent runs for each scenario, then averaged across scenarios. is the mean over scenarios of the fraction of 5 runs exceeding the threshold (i.e., the fraction of runs that pass, averaged over all scenarios). Two catalog-bound baselines are evaluated. } uses GPT-4o-mini with MACS's live catalog injected into the system prompt before each query; it receives the same top-$K$ products as MACS, where $K$ denotes the highest-ranked products returned by MACS's catalog search for that query, but applies no SQL-layer constraint enforcement. } uses Gemini~2.5 Flash Lite under the same injection protocol. Both systems along with MACS operate on the same query set, receive the same catalog snapshot per query, use the same token budget, are prohibited from web search, and are scored by the same pipeline. This design yields a fair, controlled comparison that reduces database-access asymmetry and focuses the analysis on architectural differences in constraint enforcement. The catalog injection approach is adopted rather than vanilla LLM baselines because it controls for catalog grounding, allowing observed quality differences to reflect architectural choices rather than information asymmetry. Concretely, the baselines test constraint-aware response generation given a shared candidate pool. They are not full end-to-end retrieval systems, and the comparison is explicitly scoped to that narrower question. Both baselines share an identical five-rule system prompt instructing the LLM to recommend only from the injected catalog, respect stated constraints , avoid web search, and disclose when no matching product exists. Products are injected as a numbered list, with $K=8$ for single-turn queries and $K=10$ for multi-turn scenarios. Generation uses temperature~0.3 and a 600-token output budget. The quality judge is GPT-4o-mini at temperature~0, identical to MACS's scoring pipeline, so all quality scores are directly comparable. Table~ reports measured median latencies by pipeline phase for the single-turn recommendation path; multi-turn interactions compound per-turn latency additively across turns. LLM inference accounts for 85--92\ The knowledge graph re-ranking overlay is 11--28$ $ faster than equivalent LLM-based narrative generation and produces deterministic, auditable results. Consequently, structural queries (substitutes, best value) are routed through the graph rather than a prompt. The cache layer reduces effective latency $ $12$ $ on repeated queries . latencies by pipeline phase ($n = 10$ runs per phase, direct-call benchmark; median reported to reduce sensitivity to occasional LLM cold-start outliers). LLM calls dominate total latency, and knowledge graph traversal and cache hits are negligible.} {@{}lr>{ }p{2.5cm}@{}} Phase & Median & Notes \\ CPU phases & $<$1 & Pure Python, no I/O \\ Domain detection --- fast path & $<$1 & Keyword dictionary \\ Domain detection --- LLM & $ $1{,}630 & gpt-4o-mini; ambiguous only \\ Criteria extraction & 1{,}927 & Primary bottleneck \\ Question generation & 1{,}645 & Preference elicitation \\ Post-rec intent detection & 556 & gpt-4o-mini classification \\ Rec.\ narrative & 2{,}256 & Recommendation explanation \\ Filter refinement & 2{,}187 & gpt-4o-mini refinement \\ SQL search & 446 & Catalog store round-trip \\ SQL search & 36 & Cache GET + deserialize \\ KG re-ranking overlay & 207 & FAISS + graph + scoring \\ Total & $ $4{,}600 & 1{,}927$+$446$+$2{,}256; KG overlay adds $+$207~ms when triggered \\ Total & $ $2{,}300 & 36$+$2{,}256; criteria extraction and SQL search bypassed \\ Table~ reports the three-way comparison on 140 queries. MACS led all systems on pass rate at 87.1\ compliance and near-perfect filter compliance . achieved 72.1\ MACS's SQL-enforced constraint pipeline, which text-only systems cannot replicate structurally. Remaining MACS failures were catalog-level: absent inventory, marketplace risk (users asking whether off-platform deals are legitimate, which requires seller-trust signals outside the product database), and multi-category budgets. {4pt} {lrrrrr} System & Avg $ $ & Pass\ MACS & & & 0.662 & 68.6\ & 0.639 & 72.1\ : all evaluations use a consumer-electronics catalog . Generalization to other product domains is not claimed and represents a direction for future work. Table~ reports per-group pass rates on the full 225-query benchmark (MACS only; all queries evaluated independently with no prior session context). Across all 225 queries, MACS achieved avg.\ 0.681 (pass rate 85.3\ Gains from targeted fixes included: follow-up service QA (0\ exploration (50\ refinement (43\ multi-constraint group achieved 94.1\ that the SQL pipeline enforces four simultaneous constraints deterministically. " row uses $ 0.5$ for comparability with Table~. All queries evaluated independently .} {lrrr} Group & $N$ & Avg $ $ & Pass\ {l}{} \\ Multi-intent rigidity & 5 & 0.775 & 100.0\ One-liner purchase intent & 6 & 0.833 & 100.0\ Refinement & 3 & 0.756 & 100.0\ Follow-up service QA & 4 & 0.810 & 100.0\ Multi-constraint & 17 & 0.791 & 94.1\ Post-rec refinement & 7 & 0.738 & 85.7\ {l}{} \\ Context-free comparison & 6 & 0.393 & 0.0\ Orchestrator routing & 12 & 0.502 & 33.3\ Preference discovery & 8 & 0.473 & 25.0\ & 225 & & Table~ reports multi-turn results across all three systems . MACS led all systems on single-turn pass rate (87.1\ Pass@5 on the two hardest state-management scenarios: Exclusion Reversal , in which scored only 20\ Accumulation , in which scored 60\ On macro Pass@5, MACS led (72\ ). Mean judged response-quality scores were similar across systems . The 16-point Pass@5 gap reflects scenario-level consistency advantages that mean scores do not capture. {4pt} {@{}lrrr@{}} System & MT Avg$ $SD & MT Pass@5 & Drift \\ MACS & $ $0.095 & & 0.736$ $0.090 & 56\ & 0.717$ $0.118 & 52\ Table~ provides the multi-turn category breakdown. MACS uniquely dominated exclusion reversal (100\ Gemini) and constraint accumulation (100\ mid-conversation state updates via session-persisted slot dictionaries. MACS also led on dense constraint (100\ Budget overwrite and long session were strong across all systems. Notably, MACS brand exclusion Pass@5 (20\ Intent pivot remains hard across all systems (0\ confirming it is a structural failure mode rather than a system-specific weakness. {4pt} {@{}lccc@{}} Category & MACS & & \\ Brand Exclusion & 0.640 / 20\ Budget Overwrite & 0.793 / 100\ Budget Refinement & 0.748 / 60\ Clarification & 0.793 / 100\ Comparison & 0.658 / 40\ Constraint Accum. & 0.784 / 100\ Dense Constraint & 0.721 / 100\ Exclusion Reversal & 0.955 / 100\ Intent Pivot & 0.640 / 0\ Long Session & 0.775 / 100\ Overall & Three pillars are evaluated deterministically . : all systems scored 1.000, as all retrieve from MACS's catalog layer. : MACS and scored 0.000; recorded 0.014 . : eight catalog-impossible queries were scored 1.0/0.5/0.0 for explicit disclosure / silent relaxation / violation at pass threshold 0.7. MACS scored 0.925, achieving explicit disclosure on 6 of 8 queries via its progressive relaxation pipeline. By contrast, and both scored below 0.6, as both receive MACS's already-relaxed product list with no mechanism to detect or disclose the constraint gap. The resulting disclosure gap is the clearest architectural differentiator between the systems. {lrrr} System & Grounding $ $ & Disclosure $ $ & Drift $ $ \\ MACS & 1.000 & & \\ & 1.000 & 0.588 & \\ & 1.000 & 0.550 & 0.014 \\ To measure the contribution of the accumulated slot dictionary, a MACS-NoSession ablation is conducted in which the slot dictionary is cleared after each turn while LLM conversation history is preserved, re-evaluating all ten multi-turn scenarios at K=5. MACS-Full achieved macro Pass@5 = 72\ dropped to 52\ The drop concentrated in two scenarios: S1 (constraint accumulation across four turns, 100\ Both require constraints set in earlier turns to gate SQL predicates in later turns. Without the slot dictionary, those predicates reset to unconstrained, and the correct products were no longer filtered in. Scenarios driven by within-turn signals were unaffected: S8 (exclusion reversal, 100\ and S10 (dense first-turn constraint, 100\ This ablation provides direct evidence that the session-persistent slot state is the primary driver of cross-turn constraint enforcement in MACS. To measure the contribution of deterministic SQL predicates, a MACS-NoSQL ablation is conducted that removes all hard-constraint WHERE clauses (brand, price range, OS exclusions) while keeping catalog injection and specification filtering. Over the 225-query single-turn benchmark, MACS-Full achieved avg=0.681 / pass=85.3\ avg, $-$4.0\,pp). The sharpest effect appeared on the 19 brand-constrained queries: brand\_score fell from 1.000 to 0.684 , confirming that SQL predicates, not prompt instructions, enforce brand compliance. filter\_score was largely unchanged because some price-related filtering still occurs after retrieval, and LLM instruction-following can partially compensate for the removal of SQL-side price constraints on underspecified queries. The multi-turn composite score uses a reliability-oriented weighting of $0.55 s_{} + 0.45 s_{}$. Re-evaluating MACS macro Pass@5 across five evenly-spaced weight configurations yields: judge-heavy : 42\ : 58\ equal : 72\ default : }$: the MACS-NoSession ablation collapses both from 100\ MACS is a hybrid multi-agent framework for reliable conversational recommendation in fixed-catalog settings. In our evaluated setting, MACS consistently outperforms catalog-bound prompt baselines on reliability-oriented metrics, especially in scenarios requiring persistent constraint tracking across turns and explicit disclosure of unsatisfied requirements. On macro Pass@5, MACS achieves the strongest score (72\ Controlled ablations confirm the contribution of each component: removing the persistent slot dictionary drops multi-turn macro Pass@5 from 72\ These ablation results reduce dependence on the judge-weighted composite when interpreting the paper's main reliability claims. Taken together, these results suggest that in fixed-catalog conversational commerce, architectural separation between language interaction and correctness-critical enforcement is a practical and effective way to improve reliability without sacrificing response quality. All evaluations are limited to a single consumer-electronics domain , so the results should not be interpreted as evidence of generalization to other merchant catalogs or recommendation domains. The baseline comparison uses catalog-injected generation baselines that receive MACS-prepared candidate sets; accordingly, the comparison isolates constraint-aware generation over a shared candidate pool rather than full end-to-end retrieval quality. The benchmark was authored by the research team, and the multi-turn evaluation consists of 10 scripted scenarios , which may introduce unintentional alignment between benchmark design and system structure. Response quality is measured using an LLM judge rather than human raters, and no independent human evaluation was conducted. Session-state and SQL-enforcement component ablations are reported in Section~; the remaining multi-turn and cross-domain components are left for future work. Additional work includes evaluation on standardized external benchmarks such as the retail subset of $ $-Bench~, independent human validation of G-Eval scores, comparison against a tool-augmented agent with independent retrieval, expanding beyond consumer electronics, and extending component ablations. Generative AI tools were used for limited language editing in preparing this manuscript. The authors are fully responsible for all content, claims, and results. {Appendix~} {@{}c>{ }p{2.6cm}c>{ }p{3.2cm}@{}} ID & Name & Turns & Key challenge \\ S1 & Constraint Accumulation & 4 & Preserve all accumulated constraints \\ S2 & Mind-Change Pivot & 3 & Use-case pivot \\ S3 & Brand Excl.\ Persist. & 4 & Maintain brand exclusion across turns \\ S4 & Vague $ $ Specific & 4 & Elicit \& resolve underspecified query \\ S5 & Comparison + Follow-up & 3 & Context-free comparison \\ S6 & Price Refinement & 3 & Budget update \\ S7 & Budget Overwrite & 3 & Budget replacement \\ S8 & Exclusion Reversal & 4 & Un-exclude previously excluded brand \\ S9 & Long Session & 5 & Constraint persistence over 5-turn session \\ S10 & Dense Constraint & 4 & Simultaneous multi-constraint enforcement \\ The 225-query benchmark spans five intent classes authored across four buyer personas, yielding 138 specified and 87 underspecified queries across 40 interaction groups; the 140-query three-way-comparison subset follows the same distribution. 