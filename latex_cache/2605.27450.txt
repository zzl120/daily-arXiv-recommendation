 {acmart} {{}} {} {} {} {} {} {} {ACM Conference on Recommender Systems}{2026}{TBD} {Context Features Are Cheap:\\ Rank-Aware Decomposition for Efficient Feature Interaction in Recommender Systems} } I thank Shiran Schwartz and Maoz Cohen for their thoughtful feedback on earlier drafts of this work. This section presents our first family of contributions: a systematic, block decomposition of feature interaction and projection layers that exploits the rank asymmetry between context and target features in multi-target scoring. Each decomposition presented here produces mathematically identical model output to the standard implementation, just computed more efficiently. We first state the unifying algebraic principle , then instantiate it on FM interactions , FC layers , DCNv2 cross layers , and self-attention , and conclude with a summary of architectural applicability . Section~ extends this framework with ---designs that trade strict identity equivalence for compounded savings across depth. Consider a recommendation request comprising: $ = \{c_1, , c_K\}$ with embeddings $ _{c_k} ^D$, identical across all candidates in the request. We use ``context'' to refer to all features that are constant across candidates within a request---both user features and session/page features. Settings in which the same user is scored under multiple distinct impression contexts in one request can be modeled by treating each impression as a separate request, or by introducing an additional rank for the impression axis; we focus on the single-context-per-request case throughout. indexed $n = 1, , N$, each with target features $^{ } = \{t_1^{ }, , t_M^{ }\}$ and embeddings $ _{t_m}^{ } ^D$. When stacked across candidates, we write $ ^{N M D}$ (or $ ^{N d_t}$ where $d_t = M D$ once embedded and flattened) for the rank-3 tensor of all $N$ candidates' target features. The model must produce $N$ scores $_1, , _N$. We distinguish features by the rank of their natural tensor representation: rank~2, shape $ $---identical across candidates. rank~3, shape $ $---vary per candidate. Here $B$ is the request batch size (number of requests processed in parallel). In the remainder of the paper we treat $B = 1$; all FLOP calculations and savings scale linearly with $B$. The decompositions in -- are all instances of a single algebraic principle. Any operation that is or in its inputs admits a block decomposition when the inputs partition as $ = $. For $f = $ with the weight matrix partitioned by input block, $ = $: = _c _c + _t _t The $ _c _c$ term is computed once when $ _c$ is rank~2 (shared across candidates). This single identity covers FC layers, attention projections, DCNv2's $ _l _l$, and linear gating mechanisms. For $f = ^T $ with $ = $ and $ = $: ^T = _c^T _{cc} _c + _c^T _{ct} _t + _t^T _{tc} _c + _t^T _{tt} _t The $ _c^T _{cc} _c$ term is computed once. This covers FM interactions, FwFM, bilinear interactions , outer products , and attention scores. Broadcast context embeddings to rank~3 before any interaction computation, yielding a unified $ $ feature tensor. All downstream computation is $O $ regardless of feature type. Defer broadcasting. Compute context-only operations at rank~2, target-only and cross operations at rank~3. Broadcast results only when mixing. When an operation mixes rank-2 context and rank-3 target inputs into a single output, the output dimensions are rank-mixed and any downstream layer consuming them sees a rank-3 ``context'' that is no longer pure context. This is the obstacle in FC and DCNv2 cross layers beyond layer~0 . DIN~, TWIN~, ETA~---user representations depend on target by design, so no context-only path exists. The remaining subsections instantiate this principle on the dominant feature-interaction mechanisms in modern recommender architectures; Table~ summarizes coverage across architecture families. A Factorization Machine~ computes pairwise inner products over all $K + M$ feature embeddings. We follow the modern industrial formulation in which each $ _i ^D$ is the field-level embedding for feature field $i$---obtained by looking up the active categorical value (or, for multi-valued fields, by pooling the embeddings of the active values). This subsumes the classical formulation $ _{i<j} v_i, v_j x_i x_j$~ in which $v_i$ is a latent factor and $x_i$ a sparse feature value: the field embedding $ _i$ here plays the role of $x_i v_i$ for the active value of field $i$, so dense feature values and one-hot indicators are absorbed into the embedding lookup. In line with DLRM-style usage, we treat the FM as the vector of pairwise dot products = _{1 i < j K+M} ^{{2}} rather than as the scalar sum in the classical formulation; downstream FC layers consume this vector. The decomposition below operates purely on the $ _i$ and is independent of how each embedding is produced. Applying the bilinear principle to the FM output, we split the pairs into two groups organized by tensor rank: = [\,_{} }_{}\;;\; _{} }_{}\, ] where _{} &= _{1 i<j K} ^{{2}} \\ _{} }) &= _{i,k} \, |\, _{i<j} $_{}$ depends only on context embeddings and is identical for every candidate---computed once per request. $_{}$ captures every interaction involving target field: both target--target pairs and context--target pairs. We group these together because they share the same rank , the same per-candidate cost structure, and---as we will show---admit a single asymmetric matmul implementation. This ``target-anchored'' grouping is structurally the same pattern we will see again in rDCN and in rank-aware attention . {llll} Component & Count & Rank & Per request \\ $_{}$ & $K /2$ & 2 & Once \\ $_{}$ & $K M + M /2$ & 3 & $N$ times \\ Two building blocks realize this decomposition: $_{}$ computes as the upper-triangle of the $K K$ context inner-product matrix. All operands are rank 2; the result is a rank-2 tensor of size ${2}$ computed once per request. $_{}$ computes in a single asymmetric matmul: each target field embedding is multiplied against the stacked $ $ block, covering both the context--target and target--target pairs simultaneously. Context embeddings are broadcast to rank~3 only within this operator. The standard implementation computes all ${2}$ interactions at rank 3, costing $N {2} D$ FLOPs per request. Our decomposition costs ${2} D + N D$. The absolute savings are: _{} = {2} D a quadratic function of $K$. The savings fraction of total FM FLOPs is: }}{^{}_{}} = {N } The savings fraction is larger when $M$ is smaller. The decomposition above is written for the standard FM formulation but applies unchanged to any architecture whose interaction layer computes pairwise embedding products between feature fields, including DLRM~, DeepFM~, NFM~, and PNN~. For field-aware variants such as FFM~, the same two-way decomposition holds with field-specific embeddings substituted on each side of the inner product. Recommender models commonly stack one or more FC layers after the interaction layer. We treat the first FC layer's input as a concatenation of a rank-2 context part $ _c$ and a rank-3 target part $ _t$---either passed directly or, as in DLRM-style models, produced as the outputs of $_{}$ and $_{}$. The rank-aware FC decomposition is a direct application of the linear case of . The first FC layer exploits the block structure: &= \\ &= where $ _c$ is rank-2, $ _t$ is rank-3, and $ = $ is the kernel partitioned along the input dimension. It is the same $ _c, _t$ split as in Equation~ (here we write the matrix as concatenated row blocks because the input is concatenated; the algebra is identical). We compute $ _c _c$ once at rank~2, broadcast to rank~3, and add to the per-candidate $ _t _t$ before applying the nonlinearity. Savings: _{} = | _c| U where $U$ is the number of units in the FC layer and $| _c|$ is the size of the rank-2 context input. After $ $, the context and target signals are mixed at every neuron by the nonlinear activation. All subsequent layers operate on fully target-dependent tensors of shape $ $; no further rank-separated computation is possible without approximation. When the FC layer sits , the context-only FM output $ _c = _{} $ has size ${2}$ ---itself quadratic in the context field count. Substituting into Equation~: _{} = {2} U also scales with $K$. Combined with the FM-layer savings of Equation~, a DLRM-style model thus enjoys quadratic-in-$K$ savings at stages of the forward pass: once in the pairwise interaction layer where the ${2}$ context-context pairs are computed, and again in the first FC layer where those pairs are projected. This compounding is what makes the optimization so impactful for interaction-heavy models and is absent in MLP-only architectures, where the context input to FC scales only linearly with $K$. A DCNv2 cross layer~ has the form: _{l+1} = _0 + _l The matmul $ _l _l$ is precisely the block-output linear case of , with the input partition $ _l = $ and the weight matrix partitioned into four blocks: _l _l = _{cc} & _{ct} \\ _{tc} & _{tt} _l^c \\ _l^t = _{cc} _l^c + _{ct} _l^t \\ _{tc} _l^c + _{tt} _l^t At layer $l=0$ , the terms $ _{cc} _0^c$ and $ _{tc} _0^c$ depend only on context features and are computable once per request. Savings fraction for the first cross layer: { ^2} The savings above apply only at layer~0. The blocker beyond layer~0 is the same one identified as the second failure mode in : any linear operation whose inputs include both rank-2 context and rank-3 target produces output rows whose ``context dimensions'' depend on target features. Concretely, the context-output rows of $ _l _l$ are $ _{cc} _l^c + _{ct} _l^t$. Once layer~0 emits this rank-mixed output, every subsequent layer's $ _l^c$ is target-dependent, and $ _{cc} _l^c$ is no longer a rank-2 quantity that can be precomputed. The Hadamard product $ _0 $ that follows the matmul is incidental: it preserves rank-3-ness but does not create it. Replacing $ $ with a sum or any other elementwise operator would not recover rank separation, because the rank mixing has already happened in $ _l _l$ itself. Beyond layer~0, practical savings diminish to zero. Section~ proposes rDCN, a redesigned cross layer that prevents this re-entanglement. The original DCNv2 paper~ also proposes a low-rank parameterization $ _l = U_l V_l^ $ with $U_l, V_l ^{D r}$ to reduce parameter count. This composes directly with the rank-aware block decomposition: partitioning the rows as $U_l = $ and $V_l = $ induces the block factorization $ _{cc} = U_c V_c^ $ at no extra parameter cost, and the once-per-request context matmul $ _{cc} _0^c = U_c $ remains rank~2. Attention-based interaction layers (e.g., AutoInt~, InterHAt~) apply self-attention over field embeddings: = \! \!V with $Q = _Q E$, $K = _K E$, $V = _V E$, where $E$ is the stacked field embedding matrix. Each projection is a MatMul over concatenated field embeddings and is an instance of Equation~: _Q E = _Q = _{Q,c} E_c + _{Q,t} E_t The context-side projections $ _{Q,c} E_c$, $ _{K,c} E_c$, $ _{V,c} E_c$ are computed once per request. Target-side projections are per candidate. With fields partitioned into context and target , the full attention matrix decomposes via the bilinear case : A = \! ( Q_c K_c^T & Q_c K_t^T \\ Q_t K_c^T & Q_t K_t^T ) The $Q_c K_c^T$ block is rank~2 and computed once. The remaining blocks are rank~3. The softmax normalization mixes context and target contributions in each row's denominator. However, the unnormalized context-context block is precomputable, and the row softmax can be assembled via FlashAttention-style block-wise log-sum-exp---combining precomputed context partial sums with per-candidate target contributions in $O $ extra work per query position. Savings in attention scale with the fraction of operations involving only context fields, approximately $K^2 / ^2$ for the score computation, again quadratic in $K$. Due to the final mixing of different ranks in the attention layer, these savings, again, apply only to the first layer. Table~ summarizes which architecture families admit rank-aware decomposition. The decomposition applies fully to DLRM-style architectures , partially to cross-network-based architectures (limited beyond layer~0 by the cross-block matmul; see for the rDCN redesign that lifts this restriction), and to attention-based architectures (with the softmax caveat). {ll} Architecture & Interaction layer \\ DLRM~ / DeepFM~ & FM pairs: Full \\ FM~ / FFM~ / NFM~ / PNN~ & FM/bilinear: Full \\ FinalMLP~ & Two-stream: Full \\ DCNv2~ / GDCN~ & Cross: first layer only \\ AutoInt~ / InterHAt~ & Attention: partial \\ FiBiNET~ & Bilinear: partial \\ MaskNet~ & Gated: partial \\ xDeepFM~ & Outer products: partial \\ DHEN~ & Per-submodule \\ We evaluate the rank-aware decomposition in three stages. First, we compare rank-aware component contributions across two architecture families on the production dataset, reporting FLOPs and offline metrics . Second, we characterize per-pod throughput on synthetic models by sweeping feature counts on production hardware . Third, we report production deployment results from Taboola's recommendation platform . Table~ compares identity-equivalent rank-aware decomposition and the architectural variant rDCN across two architecture families on our production dataset. Within each family, all configurations share the same model shape; the families themselves operate in different regimes and are listed separately. { }{@{}lrrrr@{}} & {c}{$ $ FLOPs} & \\ {2-4} Variant & Int. & FC & Total & LogLoss ($ \ {l}{26$, $M{=}16$, $d_c{=}514$, $d_t{=}577$, $L{=}4$)}} \\ DCNv2 + FC & $-18\ rDCN + FC & $-72\ rDCN** + FC & $-72\ {l}{27$, $M{=}4$, $d_c{=}3456$, $d_t{=}512$)}} \\ FM & $-87\ FM + FC & $-87\ Within each family, the identity-equivalent rank-aware decomposition reproduces vanilla predictions to within noise levels , as expected from a mathematically exact rewriting. The architectural variant rDCN+FC trades strict identity equivalence for a much smaller compute and parameter footprint: it matches DCNv2 within $ 0.0002$ logloss while using $67\ the $ _{ct}$ block. The parallel context stream is essential: removing it yields negligible additional FLOP savings but degrades logloss by $ 0.001$---an order of magnitude larger than the within-noise differences elsewhere in the table, confirming that the $c{ }c$ interactions the parallel stream captures are load-bearing. The throughput and production evaluations that follow focus on the DLRM-style architecture currently in production; rDCN has been validated offline above but is not yet deployed online. While rDCN substantially reduces compute within the DCN family , the DLRM-style FM+FC variant reaches the same offline accuracy at lower total compute. Looking forward, rDCN's $ $ per-layer cost makes expanding context within the DCN family (currently $d_c{=}514$ vs.\ DLRM-style's $3456$) substantially cheaper than DCNv2 admits---an exploration we leave to future work. We validate the theory empirically on synthetic DLRM-style models (FM interaction layer followed by FC layers), varying one feature-count parameter at a time. Each configuration is built twice---once with the vanilla implementation and once with the rank-aware decomposition---and both are served on the same hardware: a 16-vCPU pod with 16~GiB RAM, pinned to an Intel Xeon Silver 4510 host (Sapphire Rapids, 2.4--4.1~GHz, AMX/AVX-512 VNNI), running TensorFlow Serving 2.x with oneDNN enabled. The client issues 64 concurrent in-flight requests in a closed loop , and we record the steady-state request rate per configuration. We report : each measurement is divided by the RPS of the smallest-model vanilla baseline, $ $, so that the baseline reads $1.00$ and all other curves can be read relative to it. Figure~ plots normalized RPS as $K$ grows from 8 to 24. The vanilla curve degrades steeply , while the rank-aware curve degrades much more gradually , so the rank-aware advantage from $+13\ at $K=24$. Figure~ plots normalized RPS as $M$ grows from 4 to 24. Both curves degrade in near-lockstep (vanilla: 1.00 to 0.29; rank-aware: 1.13 to 0.31); the rank-aware advantage stays in a narrow $+7$--$+21\ context-only rank-aware savings become a smaller fraction of total work. {0.48 } [ width= , height=0.72 , xlabel={Context fields $K$ }, ylabel={Normalized RPS}, xtick={8,12,16,20,24}, ymin=0.25, ymax=1.25, grid=both, grid style={line width=0.2pt, draw=gray!30}, legend pos=south west, legend cell align={left}, legend style={font= }, tick label style={font= }, label style={font= }, ] coordinates { }; coordinates { }; {0.48 } [ width= , height=0.72 , xlabel={Target fields $M$ }, ylabel={Normalized RPS}, xtick={4,8,12,16,24}, ymin=0.25, ymax=1.25, grid=both, grid style={line width=0.2pt, draw=gray!30}, legend pos=north east, legend cell align={left}, legend style={font= }, tick label style={font= }, label style={font= }, ] coordinates { }; coordinates { }; 4$ fixed; right panel varies $M$ with $K{=}8$ fixed. The vertical gap between the two curves in each panel is the throughput gain delivered by rank-aware decomposition at that configuration.} We deployed the DLRM-style FM+FC rank-aware variant online in Taboola's recommendation platform and compared it against the vanilla baseline. The production ranking model runs on CPU infrastructure with autoscaled Kubernetes pods; the autoscaler maintains a fixed latency SLA, so per-pod throughput (RPS per pod) is the direct measure of serving efficiency. Table~ reports per-pod throughput gain and mean latency at peak traffic for the two optimized configurations. {lrrr} Variant & RPS/pod & p50 & p99 \\ FM rank-aware & $+25\ FM + FC rank-aware & $+87.5\ The FM optimization alone increases per-pod throughput by $25\ the FC optimization compounds the gain to $+87.5\ $47\ merely an incremental refinement over the FM optimization---it captures a meaningful share of the remaining redundancy. This is consistent with the theoretical analysis in : both stages contribute quadratic-in-$K$ savings that compound across the forward pass. The right two columns of Table~ report mean p50 and p99 latency. Both percentiles reduce monotonically with more rank-aware optimization, and p99 reduces more than p50, consistent with context-context redundancy disproportionately affecting tail latency in the vanilla implementation. We presented a rank-aware decomposition for multi-target recommendation scoring built on a single algebraic principle: any linear or bilinear operation over a rank-partitioned input admits an exact block decomposition that moves context-only computation from once-per-candidate to once-per-request. We instantiated this principle across the dominant interaction mechanisms in modern ranking architectures---FM pairwise products, FC projection layers, DCNv2 cross layers, and self-attention---without changing model predictions. Closed-form analysis and controlled ablation reveal an asymmetry: savings scale quadratically with the number of context features and remain insensitive to target feature count---an asymmetry not available in pure MLP architectures, where FC-layer savings scale only linearly. Applied to a production DLRM-style ranker without any architectural change, the decomposition increased per-pod throughput by $87.5\ $47\ reducing both p50 ($-30\ The identity-equivalent decomposition has a structural limit: in cross networks and attention, each layer mixes ranks in its output, so it applies only at the first layer. To propagate rank discipline across the full depth of the stack, we introduced rDCN---an architectural variant of DCNv2 that structurally forbids the off-diagonal target-to-context coupling---and sketched an analogous variant for self-attention. We validated rDCN offline: it matches DCNv2 accuracy within training noise at $67\ Two directions remain open. First, online validation: the DCN family's small per-field context dimension makes rDCN a natural candidate for scaling context substantially beyond what DCNv2 currently admits, and empirical evaluation of the rank-aware attention sketch awaits a production implementation. Second, extending rank-aware approaches to architectures that early-fuse the target into the user-side representation via shared self-attention. Target-attention models (DIN~, ETA~, TWIN~) are accommodated by the rank-aware attention sketch of ; the harder case is self-attention with target-token injection as in TransAct~, where the target's presence in the user-action sequence makes history representations per-candidate at the first layer---breaking rank discipline before any decomposable structure can be applied. Modern industrial recommender systems use a deep ranking model to score $N$ candidates against the same user and context features. Standard implementations broadcast context features early in the forward pass, redundantly computing context-only operations $N$ times per request. We present a rank-aware decomposition applicable to the dominant interaction mechanisms in modern recommender architectures---Factorization Machine pairwise products, Deep Cross Network cross layers, self-attention, and fully connected projection layers---built on a single algebraic principle: any linear or bilinear operation over a rank-partitioned input admits an exact block decomposition that moves context-only computation from once-per-candidate to once-per-request, identity-equivalent to the original model. Closed-form analysis and controlled ablation verify that savings scale quadratically with the number of context features. Applied to a production DLRM-style ranker without any architectural change, the decomposition increases per-pod throughput by $87.5\ identical model predictions. The identity-equivalent decomposition applies only at the first layer of cross networks and self-attention, since each layer mixes ranks in its output. To extend savings across depth, we further introduce rDCN, an architectural variant of DCNv2 that maintains rank discipline across depth and matches DCNv2 accuracy within training noise at $67\ total FLOPs, and sketch an analogous architectural variant for self-attention. Modern recommender architectures differ primarily in how they combine features into learned interactions. We briefly review the dominant families; Section~ analyzes each with respect to rank decomposability. A broad family of architectures uses FM-style pairwise embedding products as their interaction layer. DeepFM~ combines a parallel DNN branch with the bilinear pairwise interactions; NFM~ introduces bi-interaction pooling followed by an MLP; PNN~ uses inner/outer product layers feeding an MLP. DLRM~ is the dominant industrial template: FM-style pairwise interactions followed by a top MLP. DCN~ and DCNv2~ compute feature crosses via residual layers of the form $ _{l+1} = _0 + _l$. GDCN~ adds information gating. DCNv3~ extends the formulation with exponential cross networks. AutoInt~ applies multi-head self-attention over field embeddings. InterHAt~ adds hierarchical aggregation. FiBiNET~ combines SENET-based feature reweighting with bilinear interactions. MaskNet~ introduces instance-guided masks derived from the full input. All of the above architectures combine context and target features, creating the redundancy we address. Prior work attacks this cost from several angles, summarized below. LightSUAN~ explicitly identifies the cost of multi-target scoring under early-fusion architectures, such as target attention over user-behavior sequences (DIN , ETA , TWIN ) or shared bottom modules, where context-context computations are repeated $N$ times per request. It addresses this through distillation: a heavier teacher (SUAN, a Stacked Unified Attention Network) is compressed into a lighter student that retains early fusion but uses sparse self-attention and parallel-inference batching, trading capacity for efficiency rather than restructuring the fusion mechanism. Two-tower architectures~ take a more extreme structural path: independent user and item towers with scoring reduced to a dot product, efficient enough for retrieval over millions of candidates but sacrificing joint user--item features and deep cross-interactions, making them rarely used for final ranking---the regime our work targets. Both lines of work trade expressiveness for serving efficiency. Our rank-aware decomposition contrasts: it is mathematically exact and preserves expressiveness fully, but applies only where context and target features remain separately accessible at the interaction layer. Extending it to early-fused architectures is an open direction for future work. ROO~ restructures training data so that each row represents a full user request---one set of user features plus an array of $N$ candidate items---rather than one $ $ pair per row. This deduplicates user features and embedding lookups across the candidates of a request, yielding 32--100\ late-stage ranking. AIF~ executes interaction-independent paths (user-only, item-only) asynchronously and approximates the skipped cross-interactions via a learned bridge embedding ($+5.80\ negligible added latency). Both operate at the system level and are complementary to our algebraic approach. Low Rank FwFM extends the linear-time FM evaluation trick of Rendle~ to the field-weighted variant: it decomposes the Field-Weighted FM interaction matrix as $R = U^T U + $ , and exploits the resulting structure to compute the context-side sum once per request. Reports $34\ latency reduction in a production advertising system. The approach is mathematically elegant but scoped to FwFM and requires an approximation of $R$; it does not extend to FM-based deep networks where pairwise outputs feed downstream layers. Closest to our work, MaRI~ automatically detects redundant FC/MatMul computations via a graph coloring algorithm. When the input to a MatMul has the tiled form $ = $, MaRI decomposes the weight matrix to compute $ + _i _i + _c _c$, reporting $5.9\ to MatMul nodes: savings are linear in the context dimension $O $, and the analysis does not cover pairwise FM interactions , cross-network layers, or attention. Industrial recommender systems are typically organized as a two-stage retrieval and ranking pipeline. A cheap retrieval stage narrows a corpus of millions of items down to $N$ candidates per request; a deep ranking model then scores all $N$ candidates against the same user and context signals . The ranking stage faces a fundamental tension between model expressiveness and serving cost: as models incorporate richer feature interactions---through pairwise products, cross layers, or attention---the total cost of multi-target scoring grows with both model depth and the number of candidates evaluated per request. For production systems serving billions of daily requests, this cost directly translates into infrastructure footprint. Most modern ranking architectures---DLRM~, DeepFM~, DCNv2~, and their variants---share a common structural property: they combine context and target embeddings into a unified feature vector before feeding it through interaction and projection layers. Many implementations broadcast context features to the candidate batch dimension before any computation begins. Downstream operations that involve only context fields---context-only pairwise interactions, linear projections, attention---are redundantly computed $N$ times. For a model with $K$ context fields and $M$ target fields scoring $N$ candidates per request, the context-context FM interactions alone waste $ {2} D$ FLOPs per request, where $D$ is the embedding dimension. Recent work on serving optimization addresses this redundancy in fully connected layers, where the savings scale with the number of context fields. In DLRM-style architectures, the dominant industrial paradigm, the bulk of feature-dependent computation occurs in the layer, where pairwise combinations cause savings to scale , $O $. As models incorporate richer context , $K$ grows and the gap between linear and quadratic savings widens. Any linear or bilinear operation over a rank-partitioned input admits an exact, approximation-free block decomposition. We instantiate this principle across the dominant interaction mechanisms in modern ranking architectures: FM pairwise interactions , DCNv2 cross layers, self-attention, and FC projection layers. Rank-aware decomposition makes context features substantially cheaper to serve than target features. This suggests a new tradeoff between adding context vs. target features. Closed-form analysis reveals this asymmetry, and our empirical ablation shows that increasing $K$ by $3 $ reduces RPS by $39\ The standard rank-aware decomposition applies only at the first layer in cross networks and attention, because context and target features mix in subsequent layers. This motivates a structural design principle: build recommendation architectures from layers that admit rank-aware decomposition throughout. We propose rDCN, a rank-aware cross network that lifts this restriction across depth, and sketch the analogous decomposable attention layer. Applied without any architectural change to a DLRM-style ranker, the decomposition increases per-pod throughput by $88\ model predictions. The remainder of the paper is organized as follows. Section~ reviews feature-interaction architectures and prior work on serving efficiency. Section~ presents the rank-aware decomposition framework and its instantiations for FM, FC, DCNv2 cross, and attention layers. Section~ presents rank-aware architectural designs: rDCN and a sketch of rank-aware self-attention. Section~ presents the FLOP analysis, cross-architecture comparison, controlled ablation, and production deployment results. The rank-aware, identity-equivalent decompositions of optimize existing architectures without changing their predictions, but their savings are bounded: in cross networks and attention, each layer mixes ranks in its output, leaving subsequent layers without rank-separated inputs to decompose, so the decomposition applies only at the first layer. This section introduces a complementary class of that lift this restriction by accepting a constraint on the layer form to avoid rank-mixing. The variants are not identity-equivalent to their standard analogues, but offline evaluation shows accuracy within training noise alongside savings that compound across depth . The rank-mixing has a specific source in each architecture: in DCNv2, the off-diagonal $ _{ct}$ block maps rank-3 target inputs into the context-dimension half of the layer state; in self-attention, context-field outputs attend over target-field keys and values, again producing context outputs that depend on every candidate. This motivates the : architectures should load context$ $target interactions onto the target stream only, while the context stream evolves purely through context$ $context dynamics---so rank separation propagates across the full depth of the stack. Whether this directional constraint meaningfully limits expressiveness is an empirical question that requires hyperparameter tuning of both architectures to compare properly. We instantiate this principle for cross networks and attention , with initial offline results for the cross-network instantiation reported in . The principle composes with the FC decomposition of : any rank-aware interaction stack---FM or the architectural variants introduced below---terminates in a rank-2 context representation and a rank-3 target representation, which feed directly into a rank-decomposed FC layer. We instantiate the asymmetric loading principle for DCNv2 cross networks by structurally removing the $ _{ct}$ block and maintaining two separately-typed states---a rank-2 context stream $_l$ and a rank-3 target stream $_l$---each updated in parallel by its own DCNv2-style layer: _{l+1} &= _0 ( }_l\, _l}_{} \; \; }_l\, _l}_{} + ^{}_l ) + _l \\ _{l+1} &= _0 + _l with $ ^{}_l ^{D_c D_c}$, $ ^{}_l ^{D_t D_c}$, $ ^{}_l ^{D_t D_t}$, and $ $ broadcast-and-add along the $N$ axis. Equivalently, for each candidate $j$: $_{l+1}^{ } = _0^{ } } + ^{}_l) + _l^{ }$. Figure~ visualizes one rDCN layer using the equivalent block-matrix form for the target stream: $ \, $, where the $_l$ slice of the concatenated input also serves as the input to the context stream's cross layer below. [ font= , Tout/.style={circle, draw, fill=red!70, minimum size=2.5mm, inner sep=0pt}, T0c/.style={circle, draw, fill=red!25, minimum size=2.5mm, inner sep=0pt}, Tlc/.style={circle, draw, fill=yellow!60, minimum size=2.5mm, inner sep=0pt}, coutc/.style={circle, draw, fill=blue!70, minimum size=2.5mm, inner sep=0pt}, c0c/.style={circle, draw, fill=blue!25, minimum size=2.5mm, inner sep=0pt}, clc/.style={circle, draw, fill=cyan!50, minimum size=2.5mm, inner sep=0pt}, Wcell/.style={circle, draw, fill=gray!30, minimum size=2.5mm, inner sep=0pt}, Wctcell/.style={circle, draw, fill=gray!55, minimum size=2.5mm, inner sep=0pt}, bTc/.style={circle, draw, fill=red!10, minimum size=2.5mm, inner sep=0pt}, bcc/.style={circle, draw, fill=blue!10, minimum size=2.5mm, inner sep=0pt}, tbox/.style={draw, dashed, rounded corners=1.5pt}, tboxouter/.style={draw, dashed, rounded corners=2pt, thick}, hd/.style={font= }, ] at {Output}; at {Feat.\ Crossing}; at {Bias}; at {Input}; rectangle ; in {-0.33, 0, 0.33} at {}; at {$_{l+1}$}; at {$=$}; rectangle ; in {-0.33, 0, 0.33} at {}; at {$_0$}; at {$ $}; at {$ ($}; rectangle ; in {2.80, 3.13, 3.46} in {-0.33, 0, 0.33} at {}; in {3.79, 4.12} in {-0.33, 0, 0.33} at {}; -- ; at {$ ^{}_l$}; at {$ ^{}_l$}; at {$ $}; rectangle ; rectangle ; in {-0.33, 0, 0.33} at {}; at {$_l$}; rectangle ; in {-1.60, -1.30} at {}; at {$_l$}; at {$+$}; rectangle ; in {-0.33, 0, 0.33} at {}; at {$ ^{}_l$}; at {$ )$}; at {$+$}; rectangle ; in {-0.33, 0, 0.33} at {}; at {$_l$}; rectangle ; in {-1.60, -1.30} at {}; at {$_{l+1}$}; at {$=$}; rectangle ; in {-1.60, -1.30} at {}; at {$_0$}; at {$ $}; at {$ ($}; rectangle ; in {3.30, 3.62} in {-1.60, -1.30} at {}; at {$ ^{}_l$}; at {$ $}; at {$+$}; rectangle ; in {-1.60, -1.30} at {}; at {$ ^{}_l$}; at {$ )$}; at {$+$}; rectangle ; in {-1.60, -1.30} at {}; at {$_l$}; _l$ tensor is shared between the two streams.} The context stream is a standard rank-2 DCNv2 stack on $_l$ alone, evolving the context representation through higher-order ctx$ $ctx crosses across depth. The target stream is the asymmetric layer: it reads the evolved context $_l$ via the rank-2 cross matmul $ ^{}_l _l$ (computed once per request and broadcast along $N$), combines it with the per-candidate state $ ^{}_l _l$, and applies the Hadamard with $_0$. Since no weight matrix produces context-dimension output from target inputs, the two streams stay rank-2 and rank-3 respectively at every depth. Empirically, this parallel context stream proves essential: removing it causes non-trivial accuracy degradation despite negligible FLOP savings, confirming that the higher-order ctx$ $ctx dynamics it captures are load-bearing. In the four-block partition of , rDCN structurally omits the $ _{ct}$ block (target inputs producing context-dimension output): the context stream reads only $_l$, so no target-to-context coupling is needed. Baseline DCNv2 has $ ^2$ weight parameters per layer; rDCN's three matrices total $D_c^2 + D_c D_t + D_t^2$, a saving of $D_c D_t$ per layer. The context stream contributes $ $ . The target stream contributes $ $ for the once-per-request cross matmul plus $ $ for the per-candidate target matmul. Baseline DCNv2's per-layer cost, without first-layer rank-aware optimization, is $ = )$, where $D = D_c + D_t$. In rDCN this collapses to $ + )$: the context-driven matmul is removed from the $N$-loop entirely, and the remaining per-candidate work scales linearly in the context dimension where the baseline scales quadratically. The same two-stream design applies to attention-based interaction stacks. As in rDCN, we maintain a rank-2 context stream $_l$ and a rank-3 target stream $_l$, each updated by its own attention sub-layer. A standard rank-2 self-attention layer over context field embeddings: queries, keys, and values are all drawn from $_l$. Cost per layer is $ $, independent of $N$. Queries are drawn only from the target side, $ = _l _Q$ . Keys and values are split by source: $^c = _l _K^c$ and $^c = _l _V^c$ at rank 2 (computed once per request), alongside $^t = _l _K^t$ and $^t = _l _V^t$ at rank 3. Target queries attend to the concatenation $ $, and the output is a weighted sum of $ $. Because context keys and values are produced at rank 2, their projection cost is amortized across all $N$ candidates. A production implementation and empirical evaluation are left to future work.