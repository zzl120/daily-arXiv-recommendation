 {acmart} {algorithm2e} {tcolorbox} }} } } } } {Zhang et al.} Sequential Recommendation aims to predict the next interaction of a user based on their behavior sequence, where complementary relations often provide essential signals for predicting the next item. However, mainstream models relying on sparse co-purchase statistics often mistake spurious correlations for true complementary relations. Identifying true complementary relations requires capturing the fine-grained item semantics that simple cooccurrence statistics would be unable to model. While recent semantics-based methods utilize discrete semantic codes to represent items, they typically semantic codes into coarse item representations. This aggregation process blurs specific semantic details required to identify complementarity. To address these critical limitations and effectively leverage semantics for capturing reliable complementary relations, we propose a omplementary-ware emantic ransition framework that introduces a new modeling paradigm built upon transitions. Specifically, a semantic-level transition module is designed to model dynamic transitions directly in the discrete semantic code space, effectively capturing fine-grained semantic dependencies often lost in aggregated item representations. Then, a complementary prior injection module is designed to incorporate LLM-verified complementary priors into the attention mechanism, thereby prioritizing complementary patterns over co-occurrence statistics. Experiments on multiple e-commerce datasets demonstrate that CAST consistently outperforms the state-of-the-art approaches, achieving up to 17.6\ <ccs2012> <concept> <concept_id>10002951.10003317.10003347.10003350</concept_id> <concept_desc>Information systems~Recommender systems</concept_desc> <concept_significance>500</concept_significance> </concept> </ccs2012> {Information systems~Recommender systems} Sequential recommendation aims to predict the next interaction of a user by modeling the temporal dynamics of historical behaviors . However, several factors complicate accurate prediction: user sequences may span multiple months, and purchase decisions are often driven by the intrinsic semantics of items that determine their compatibility. Furthermore, users interact with relatively few items from the large-scale item set, and the recorded sequences often diverge from true user preferences due to accidental clicks, naturally resulting in severe data sparsity and noise~. In such sparse and noisy environments, purely statistical signals become unreliable, as they frequently conflate accidental co-occurrences with genuine user interests. To robustly model user intent under these conditions, it is crucial to leverage signals that are invariant to statistical noise: . Research has demonstrated that complementary relations play a crucial role in next-item prediction, as they capture user intent rather than merely the preference for similar items. Incorporating these relations allows models to transcend simple temporal adjacency and uncover the that drive item transitions~. {figs/intro.pdf} However, existing approaches generally infer complementary relations from co-purchase frequency . Such statistical signals capture mere correlations rather than reliable functional dependencies. Consequently, they are susceptible to external factors like exposure bias and seasonality~: . The classic ``beer and diapers'' retail case illustrates this pitfall: such co-purchase patterns may stem from distinct user intentions rather than reflecting true functional complementarity, as described in Figure~ . Relying solely on these unstable statistical artifacts risks learning spurious relations that generalize poorly to sparse or cold-start scenarios, where functional logic is required to infer user intent, not just historical frequency. To distinguish true complementary relations from spurious co-purchases, it is essential to look beyond behavioral statistics and ground recommendations in , which are crucial for functional compatibility. However, mainstream sequential recommendation models treat items as discrete ID embeddings, lacking the semantic rationale behind item transitions. Although recent semantic-aware methods have introduced semantic codes to capture textual information, they typically aggregate semantic codes into a coarse item representation before sequence modeling. This ``aggregation-then-modeling'' paradigm restricts semantic-aware methods to item-level transitions, masking the specific semantic attributes that actually drive compatibility. As illustrated in Figure~ , complementarity is often driven by the matching of specific attributes rather than whole items. Therefore, effectively modeling complementary relations requires shifting the focus from item-level transitions to . To address these limitations, we propose , a omplementary-ware emantic ransition framework for sequential recommendation. Specifically, we encode the textual features of items with a pre-trained language model and discretize the representations into a shared semantic codebook, so each item is represented as a sequence of semantic codes. Instead of modeling item-level transitions, CAST explicitly models semantic transitions and learns a dynamic transition matrix in the semantic code space to capture functional complementarity beyond spurious co-occurrence . To mitigate noisy co-purchase signals, we construct a complementary transition prior from complementary relations verified with item texts and inject it as an additive bias into self-attention, guiding the model toward plausible semantic transitions even under sparsity and noise. The main contributions of this work are summarized as follows: We propose , which shifts the modeling paradigm from item-level to . By explicitly learning dynamic dependencies in the discrete semantic space, CAST captures compatibility patterns beyond static co-occurrence statistics. We develop a strategy to maintain the orthogonality of semantic codes, and a mechanism that incorporates LLM-verified complementary priors as adaptive transition biases to guide semantic dependencies. Extensive experiments demonstrate that CAST consistently outperforms state-of-the-art baselines. Further analysis verifies its ability to reveal distinct complementary patterns, while achieving over 65$ $ training acceleration compared to the strongest baseline. Let $$ and $$ denote the sets of users and items, where $||$ and $||$ are the respective numbers of users and items. $ = \{x_v\}_{v }$ denotes the set of textual features associated with the items. Each user has an interaction sequence $s= $, where $v_t $ is the item interacted with at the time step $t$, and $n$ is the length of the sequence. The sequential recommendation task aims to predict the next item with which a user will interact, given the observed history up to $t$ . During training, the model takes the first $t$ items $ $ as input and learns to predict the next item $v_{t+1}$. The overall architecture of the proposed CAST framework is illustrated in Figure~. Generally, CAST consists of four core components: , which leverages LLM-based verification to construct reliable complementary relations, filtering spurious co-occurrences from intrinsic functional dependencies; , which discretizes item textual features into fine-grained semantic codes via Optimized Product Quantization and aligns them with textual semantics through a subspace-preserving MLP projector; , which explicitly captures the dynamic evolution of user interests by injecting LLM-verified complementary priors into the self-attention mechanism via a learnable semantic transition tensor; and , which trains the model end-to-end using a next-item prediction objective augmented by a transition consistency regularization to ensure the reliability of learned semantic dependencies. = \{s_1, s_2, , s_{||}\}$, item texts $ = \{x_v\}_{v }$, window size $w$, frequency threshold $ _f$, LLM complementary confidence threshold $ _c$, textual similarity threshold $ _s$} _c$} $} { $ + 1, i < j, j - i < w$ } Initialize complementary relation set $_c^{0} \{ _f \}$ \; Initialize complementary relation set $_c $\; _c^0$} { $w_{ij} $ _c$} { $_c _c $ } } Initialize substitutable relation set $_s $\; $ $} { , ) _s$} { $_s _s $ } } ) _c$} { _s$} { $_c _c $ } } _c$} ) _c$} { $_c _c $, { if} $ _c$ } $_c$ [ colback=gray!5, colframe=black, rounded corners, boxrule=0.8pt, fontupper= , left=2mm, right=2mm, top=2mm, bottom=2mm ] You are an assistant who determines to what extent two products are complementary on a scale. Are they often used together for the same intent? Does one enhance the functionality of the other? Considerations of market co-occurrence. Product 1: Product 2: Step-by-step reasoning referencing the criteria. A single numeric score from 0 to 1. We construct the complementary relation set $_c$ following Algorithm . First, we generate raw candidates from co-purchase data using a sliding window $w$ and a frequency threshold $ _f$ . Second, for each candidate pair $ $, an LLM evaluates the two items based on their textual metadata and outputs a complementary confidence score $w_{ij} $ . The prompt template used for this evaluation is detailed in Figure , designed to focus on direct interaction and functional enhancement . We keep pairs with $w_{ij} _c$ and form the initial set $_c=\{ \}$ . Third, we build an auxiliary substitutable set $_s$ containing item pairs with high textual similarity . Finally, we expand $_c$ using a transitive rule : if $ _c$ and substitutable pair $ _s$, then the pair $ $ is added to $_c$ $). Subsequently, $_c$ is used to inform the model about the transition probabilities between semantic tokens, thereby embedding complementary knowledge directly in item representations. Specific parameter settings and their justifications are detailed in Section . {figs/model.pdf} _c$) and OPQ-based semantic codes. First, the module aligns semantic code embeddings with textual embeddings to generate item representations . Subsequently, the module utilizes a semantic-level transition tensor guided by the complementary priors to modulate the self-attention mechanism. Finally, the model is optimized using a joint objective combining recommendation and transition consistency losses.} Recent works on vector-quantized item representations~ demonstrate that discrete semantic coding effectively captures item semantics. Following this paradigm, we adopt , which discretizes the textual embedding of each item in a sequence of discrete semantic codes that serve as generalizable semantic representations. Given an item $v_i$ with textual feature $x_i$ we obtain its textual embedding $}_i ^{d_{}}$ via a frozen pre-trained language model encoder. }_i = . Subsequently, the OPQ method discretizes $}_i$ into an item-specific code sequence $_i = }, , c_i^{ })$, corresponding to the column vectors in the ``Semantic Codes'' block of Figure~ . Concretely, OPQ partitions $}_i$ into $D$ disjoint sub-vectors and quantizes each sub-vector to its nearest centroid in a codebook of size $C$ . For instance, if $D=3$, an item might be encoded as $_i = $. Here, each code represents a derived from clustering, capturing specific semantic patterns in the subspace. Formally, this yields a discrete code $c_i^{ } \{1, 2, , C\}$ for each subspace $k \{1, ,D\}$. Based on these learned discrete codes, we construct a set of $D$ semantic code embedding matrices $\{^{ }, , ^{ }\}$, where each $^{ } ^{C d}$ is shared across all items and the $m$-th row $^{ } $ stores the learnable embedding of centroid $m$ in the $k$-th subspace. Given the code sequence $_i = }, , c_i^{ })$ of an item, we retrieve its subspace-wise semantic embeddings by lookup: _i^{ } = ^{ } , k=1, ,D. We denote the retrieved sequence of subspace embeddings for item $v_i$ as $_i = }, , _i^{ })$, which serves as its semantic representation in the subsequent modules. Built on OPQ, the generated semantic IDs are decomposed so that tokens $ $ at different positions correspond to disjoint quantized subspaces . However, standard aggregation techniques will mix these subspace-specific positions, blurring the orthogonality of the subspace codebooks and hindering the fine-grained alignment from text semantics to distinct subspaces. To address this, we propose a strategy. We first preserve the positional independence of subspaces by flattening the semantic embeddings for item $v_i$: $^{}_{i} = _{i}^{ } _{i}^{ } _{i}^{ } ^{D d}$, where $ $ denotes the vector concatenation operation. To align the dimensionality, we first project the textual embedding $}_i$ to the hidden dimension $d$ via a learnable linear projection $_{} ^{d d_{}}$: _i = _{}}_i ^{d}. To bridge the gap between the disjoint semantic subspaces and the textual representations, we employ a Multi-Layer Perceptron as a non-linear fusion projector. Drawing inspiration from the efficacy of non-linear transformations in representation learning , we map the composite features into a unified latent space via a dimensionality-reduction architecture: _i = = _2\, _ + _1) + _2, where $ _ $ denotes the function consisting of the GeLU activation followed by a dropout operation with probability $ $. $_1 ^{d' d}$ and $_2 ^{d d'}$ are learnable weight matrices , and $_1, _2$ are learnable bias vectors. This module serves two functions: : it learns the specific correlations between the text and each semantic subspace, mapping specific text semantics to the corresponding codebook representations; and : it compresses the high-dimensional flattened input back to the hidden dimension , effectively removing redundancy while retaining the essential semantic signals aligned with the textual context. Notably, this MLP-based method significantly improves the efficiency of the alignment task, replacing conventional methods like cross-attention $)~ with a linear MLP projection $). This lightweight design substantially reduces computational overhead without compromising alignment efficacy , making the model scalable for large-scale candidate sets. Conventional SRs typically rely on self-attention mechanisms to capture local sequential dependencies but ignore global complementary relations absent from the current context. To bridge this gap, we propose a complementary-aware sequence modeling framework. Instead of static injection, CAST establishes a mutually reinforcing mechanism, where the global transition matrix $$ serves as a learnable bias to guide attention distribution, while updating $$ through training to learn refined semantic transition patterns. To model the complementary relations in the latent semantic space rather than the sparse item space, we define a semantic-level complementary transition tensor $ ^{D C C}$, where $D$ corresponds to the number of semantic subspaces , and $C C$ represents the transition matrix between semantic tokens in each semantic subspace. Specifically, $_{k, c, c^{ }}$ represents the transition score from semantic token $c$ to token $c^{ }$ in the $k$-th subspace. . To inject high-quality relation priors and accelerate convergence, we initialize $$ using the complementary relation set $_c$ constructed in Algorithm . Since $_c$ is defined at the item-level while $$ models transitions between semantic tokens, we need to map complementary item pairs into subspace-specific token transitions. Specifically, for each complementary item pair $ $ with complementary weight $w_{ij}$ in $_c$ , we retrieve their corresponding semantic token sequences $_i = }, , c_i^{ }),\, _j = }, , c_j^{ }) \{1, ,C\}^{D}$. For each semantic subspace $k \{1, ,D\}$, we construct a token-level co-occurrence matrix $_k ^{C C}$ initialized to zeros. For every complementary pair $ _c$, we update: _k \; \; _k + w_{ij}, \, k = 1, ,D. Since complementary relations are undirected, we symmetrize the subspace-specific matrix, }_k = {2} . To avoid numerical issues caused by zero counts, we add a small smoothing constant $ $ and transform the counts into transition logits, _{k, c_i^{ }, c_j^{ }}^{ } = , used to initialize the learnable transition tensor $$. During training, $$ is further updated end-to-end together with other parameters. To incorporate the learned complementary transition prior into sequence modeling, we inject $$ into the self-attention mechanism. At initialization and during training, we standardize the transition scores to stabilize optimization, obtaining $_k = $, where values in $_k$ represent the Z-score normalized transition logits between semantic tokens in subspace $k$. This ensures that the transition priors are on a scale comparable to the attention logits. We then modify the self-attention mechanism by injecting a transition score into the attention logits. Given the hidden state $_i$ of item $v_i$ in user sequence, a self-attention head computes its query, key, and value as $_i = _ _i$, $_i = _ _i$, and $_i = _ _i$, where $_, _, _ ^{d d}$ are learnable projection matrices. To incorporate the complementary transition prior into the attention computation, we first define a semantic transition scoring function $T $ that measures the transition strength from item $v_a$ to item $v_b$. Let $c_a^{ }$ and $c_b^{ }$ denote the semantic tokens of items $v_a$ and $v_b$ in subspace $k$. The score is calculated as: T = _{k=1}^{D} _k _k , where $ _k$ represents the learnable importance weight of subspace $k$ . $_k$ contains standardized log-probabilities . In the self-attention mechanism, when attending from the current item $v_i$ to a historical item $v_j$ , we inject the semantic transition bias directly into the attention logits. Note that the transition score $T $ explicitly models the probability from the historical item $v_j$ to the current item $v_i$ . By injecting this directional prior, we ensure that the attention mechanism prioritizes historical items that are functionally likely to lead to the current item. The semantic-aware attention weights are computed as: _{i,j} = _i^ _j}{} + T )}{ _{p=1}^{i} \! )}, where $ _{i,j}$ denotes the attention weight assigned to the historical item $v_j$ at position $j$ and $$ is the scaling factor based on the embedding dimension $d$. The scalar $ $ controls the strength of the transition score $T $. Based on the attention weights $ _{i,j}$ in Eq.~, each attention head computes its output as $_i = _{j i} _{i,j} _j$. These weights $ _{i,j}$ are exactly the attention coefficients used in the relation-aware multi-head attention . We stack $L$ layers of such relation-aware Transformer blocks to capture sequential dependencies. Formally, let $^{ } = $ denote the input sequence embeddings, where $_i$ represents the fused item embedding in Section and $_i$ is the absolute position embedding. The hidden states are updated iteratively through the layers: ^{ } = } ) ), l \{1, , L\}, where $} $ denotes the multi-head attention mechanism explicitly modulated by the semantic transition score $T $, and $ $ represents the feed-forward network. Following previous studies , we utilize the hidden state of the last item at the final layer, denoted by $ = _n^{ }$, as the overall sequence representation to compute the prediction scores with candidate items. We employ a joint optimization strategy to train the model end-to-end, aiming to maximize next-item prediction accuracy while explicitly structuring the semantic transition patterns via auxiliary supervision. The primary objective is to maximize the likelihood of the ground-truth next item $v_{t+1}$ given the sequence representation $$. We employ the Cross-Entropy loss with softmax normalization: _{} = - ^ _{v_{t+1}} / )}{ _{v' } }, where $$ is the sequence representation , $_v$ denotes the item representation , and $ $ is the temperature coefficient. While the Cross-Entropy loss optimizes the final item retrieval accuracy, it provides only implicit supervision to the internal semantic transition tensor $$. To explicitly structure the semantic space and ensure that $$ captures discriminative transition patterns, we introduce a auxiliary task. This objective enforces a pairwise ranking constraint, requiring the semantic transition score of a ground-truth successor to be strictly higher than that of unrelated items. Formally, for each positive transition $ $ in the training sequence, we contrast it against negative items $v^-$ sampled from the current mini-batch $$. The regularization loss is formulated as: _{} = - _{ } _{v^- P_{}} , where $ $ denotes the sigmoid function, and $T $ represents the semantic transition score derived from $$ . Here, $P_{}$ represents the in-batch negative distribution, defined as a uniform distribution over all items in the mini-batch $$ excluding the target item. This strategy ensures computational efficiency by reusing in-batch computations for negative sampling. The final objective function is a weighted sum of the next-item prediction loss and the auxiliary transition regularization: = _{} + _{}, where $ $ is a hyper-parameter controlling the weight of the transition regularization. This joint optimization ensures that while the model learns to predict user behaviors via $_{}$, the learnable transition tensor $$ is simultaneously supervised by $_{}$ to respect item-to-item complementary priors. In scenarios with sparse or noisy interactions, this dual supervision prevents the model from overfitting to accidental co-occurrences by maintaining semantic consistency in the transition space. We conduct extensive experiments to evaluate the performance of . Specifically, we focus on the following research questions: How does CAST perform compared against state-of-the-art SR baselines regarding accuracy and efficiency? What is the impact of the individual components of CAST on the model's performance? How do hyper-parameter settings and the selection of pre-trained language models affect the performance of CAST? Can CAST effectively distinguish between complementary item pairs and random ones? To evaluate the performance of the CAST framework, we conduct experiments on three category-based datasets derived from the Amazon Review dataset{https://huggingface.co/datasets/McAuley-Lab/Amazon-Reviews-2023}}: , , and . We select these domains because they exhibit rich functional complementary relations driven by objective compatibility -- a proper setting for validating semantic complementarity. Following established protocols~, we adopt the 5-core setting for filtering users and items with fewer than five interactions. Consistent with recent work~, we construct textual features by concatenating , , and . The statistics of the datasets are summarized in Table~. {lccccc} & & & & & \\ Industrial & 50,985 & 25,848 & 361,962 & 99.972\ Office & 223,308 & 77,551 & 1,577,570 & 99.991\ Baby & 150,777 & 36,013 & 1,090,306 & 99.977\ To evaluate the effectiveness of our method, we compare it with : SASRec~, BERT4Rec~, SINE~, CORE~, CL4SRec~, DuoRec~, FEARec~, SASRecCPR~, : TedRec~, and : VQRec~, CCFRec~. We employ the leave-one-out strategy~, using the last and second-to-last interactions for testing and validation, respectively, while utilizing the remaining items for training. To ensure an unbiased evaluation, we follow the protocol used in~ by performing the full-ranking evaluation, which ranks the ground-truth item across the entire item set. The performance is reported using standard metrics~: Recall@$K$ and NDCG@$K$, where $K \{5, 10, 20\}$. For fair comparison and reproducibility, we implement our proposed model and all baselines using the open-source recommendation library RecBole~} . All experiments are conducted on the Linux server equipped with one NVIDIA A100 GPU . The maximum sequence length is set to 20. For the complementary relation construction in Section , we set the sliding window size $w=3$ and the frequency threshold $ _f=2$ to capture co-purchase patterns while filtering out accidental co-occurrences. The LLM confidence threshold $ _c=0.5$ is empirically selected to filter out low-confidence pairs while maintaining sufficient coverage of complementary relations. We set the textual similarity threshold $ _s=0.85$ to identify substitutable items with high semantic overlap. Optimization is done with Adam optimizer learning rate of 0.001, batch size 1024, and early stopping with a patience of 10 epochs based on the validation set's NDCG@10. The code for all the baselines is publicly available~}. For ID-based baselines, we follow the optimal hyper-parameters from RecBole. To ensure a fair comparison, we unify the backbone architecture for all Transformer-based models to 2 Transformer layers, 2 attention heads, and the hidden dimension of $d = 128$, and the FFN inner dimension of 256. The number of semantic subspaces $D=32$ and the codebook size $C=256$. The transition strength is set to $ = 1.2$, and the regularization weight is set to $ = 1.0$. The alignment dropout rate $ $ is set to 0.2 for Industrial and Office datasets, and 0.1 for the Baby dataset. The temperature $ $ is set to 0.07. Furthermore, we use ~ as the PLM encoder for textual feature extraction, where the text embeddings are reduced to $d_{} = 128$ dimensions using PCA, and the Faiss library for semantic ID generation. We employ ~ for complementary relation inference. , and the second-best performances are .} {ccccccccccccc} & {c}{} & {c}{} & {c}{} \\ {2-5} {6-9} {10-13} {*}{} & {c}{} & {c}{} & {c}{} & {c}{} & {c}{} & {c}{} \\ & @5 & @10 & @5 & @10 & @5 & @10 & @5 & @10 & @5 & @10 & @5 & @10 \\ SASRec & 2.45 & 3.90 & 1.38 & 1.84 & 2.56 & 3.83 & 1.49 & 1.90 & 2.27 & 3.66 & 1.39 & 1.84 \\ BERT4Rec & 1.64 & 2.73 & 1.05 & 1.40 & 1.67 & 2.59 & 1.10 & 1.39 & 1.69 & 2.80 & 1.10 & 1.45 \\ SINE & 2.27 & 3.60 & 1.47 & 1.90 & 2.17 & 3.27 & 1.42 & 1.77 & 1.90 & 3.10 & 1.21 & 1.59 \\ CORE & 1.54 & 3.27 & 0.77 & 1.33 & 1.90 & 3.20 & 1.01 & 1.43 & 1.61 & 2.84 & 0.92 & 1.32 \\ CL4SRec & 2.65 & 4.20 & 1.66 & 2.16 & 2.55 & 3.75 & 1.65 & 2.04 & 2.37 & 3.79 & 1.47 & 1.93 \\ DuoRec & 2.28 & 3.54 & 1.34 & 1.75 & 2.36 & 3.48 & 1.40 & 1.76 & 2.14 & 3.44 & 1.27 & 1.68 \\ FEARec & 2.51 & 3.87 & 1.43 & 1.86 & 2.59 & 3.80 & 1.50 & 1.89 & 2.25 & 3.64 & 1.36 & 1.81 \\ SASRecCPR & 2.18 & 3.45 & 1.43 & 1.83 & 2.15 & 3.11 & 1.49 & 1.80 & 2.11 & 3.41 & 1.39 & 1.80 \\ {c}{} \\ TedRec & 2.58 & 4.01 & 1.67 & 2.13 & 2.51 & 3.78 & 1.62 & 2.03 & 2.20 & 3.58 & 1.36 & 1.80 \\ VQRec & 2.81 & 4.40 & 1.50 & 2.02 & 2.30 & 3.39 & 1.51 & 1.86 & 2.12 & 3.40 & 1.30 & 1.71 \\ CCFRec & & & & & & & & & & & & \\ & & & & & & & & & & & & \\ & 8.49\ Table~ provides a comparative analysis of CAST against all the baselines evaluated across three real-world datasets with relative improvements (Improv. (\ First, within traditional ID-based methods , FEARec and SASRec consistently achieve slightly better performance than the others. SASRec utilizes a self-attention encoder to model both short-term and long-term item transitions. FEARec builds on SASRec by integrating hybrid time–frequency attention to capture both high- and low-frequency patterns in user sequences. In contrast, SINE emphasizes multi-interest retrieval and representation consistency, while CORE and SASRecCPR, respectively, focus on output-layer adjustments and duplicate-item copying mechanisms, which appear to have less advantage under our full-ranking evaluation setting. Second, text-enhanced SR methods generally outperform the above ID-centric methods by leveraging item textual information as richer semantic carriers. TedRec and VQRec both surpass SASRec and FEARec on most metrics, and CCFRec stands out as the strongest baseline overall, demonstrating the clear advantage of representing items in a semantic space rather than relying solely on discrete IDs. However, these methods still keep semantics in a continuous, model-specific latent space, making it difficult to interpret why two items should be complementary and limiting the ability to explicitly model fine-grained semantic transitions that underlie cross-item complementarity. Finally, CAST achieves the best performance across all datasets and evaluation metrics. Compared with the strongest baseline CCFRec, our model yields consistent relative improvements ranging from 3.36\ To analyze the individual contribution of CAST's components, we compare our full model with three variants that remove key modules . : We remove the semantic ID embedding modeling and directly feed PLM-derived text embeddings into the sequential model; this aims to verify whether the discrete semantic IDs provide discriminative information beyond raw textual representations. : We replace the subspace-preserving semantic-text alignment with a simple mean pooling operation over semantic code embeddings, followed by element-wise addition to text embeddings; this examines the necessity of the proposed non-linear fusion and subspace preservation. : We disable the transition modeling by setting both the relation weight $ =0$ in Eq.~ and the regularization weight $ =0$ in Eq.~; this simplifies the encoder to a standard self-attention mechanism, validating the importance of incorporating semantic transfer signals. As shown in Table~, removing any component leads to performance degradation across all datasets. Specifically, removing semantic codes leads to the most severe performance degradation, indicating that OPQ-based semantic IDs are essential for capturing high-level item semantics beyond textual similarity. also shows a consistent decrease, suggesting that the transition $ T_{i,j}$ incorporates global semantic-level transition priors, which are crucial for guiding predictions when the standard self-attention is dominated by semantic similarity under data sparsity. Finally, underperforms the full model, highlighting that our non-linear projection effectively bridges the semantic gap between codes and text, whereas simple pooling leads to information loss. {lcccccc} & {c}{} & {c}{} & {c}{} \\ {2-3} {4-5} {6-7} & R@10 & N@10 & R@10 & N@10 & R@10 & N@10 \\ & & & & & & \\ Sem. Codes & 3.91 & 1.99 & 2.72 & 1.51 & 2.87 & 1.48 \\ Alignment & 4.87 & 2.49 & 4.32 & 2.31 & 4.19 & 2.18 \\ Trans. Guide & 4.99 & 2.58 & 4.45 & 2.38 & 4.29 & 2.24 \\ We investigate the impact of semantic transition strength $ $, transition consistency loss weight $ $, and alignment dropout probability $ $ on model performance . Additionally, the influence of different PLM encoders is explored in Section~. {figs/hyperparam.pdf} The parameter $ $ balances the contribution of the global semantic transition scores against the self-attention scores . The initial improvement validates that injecting transition scores effectively alleviates the sparsity issue of pure interaction data. However, the degradation observed when $ > 2.0$ indicates that a large $ $ forces the model to over-rely on global transition patterns, thereby suppressing personalized user preferences. The parameter $ $ controls the weight of the transition consistency loss $_{}$ in the overall objective . The lower performance at $ =0$ suggests that despite the prior initialization, without explicit supervision, the learnable transition tensor $$ may fail to retain meaningful complementary dependencies during the optimization process. The performance remains robust within $ $, validating that enforcing transition consistency via $_{}$ is essential for learning reliable semantic relations. The parameter $ $ controls the dropout probability in the subspace-preserving alignment module . The results show that moderate regularization yields the optimal performance, balancing generalization and feature preservation. However, a large dropout rate degrades model performance, indicating that the alignment between fine-grained semantic codes and textual embeddings is sensitive to information loss. We investigate the performance of the model under different pre-trained language models used to generate semantic codes and semantic-text alignment in Section~. Specifically, as shown in Table~, we compare three representative models: T5-base~, T5-xxl~, and Qwen3-4B~. Empirically, Qwen3-4B delivers the best performance across all evaluation metrics, highlighting the advantage of high-quality, large-scale pre-training corpora in modeling fine-grained semantics. Although T5-xxl has larger parameters than T5-base, we observe that both models yield comparable performance. {lcccccc} & {c}{} & {c}{} & {c}{} \\ {2-3} {4-5} {6-7} & & & & & & \\ T5-base & 5.36 & 2.75 & 4.46 & 2.41 & 4.32 & 2.24 \\ T5-xxl & 5.21 & 2.67 & 4.52 & 2.44 & 4.32 & 2.23 \\ Qwen3-4B & & & & & & \\ Table~ reports the average training time per epoch and maximum GPU memory usage on the Office dataset using a single NVIDIA A100-40G. In contrast to CCFRec, which is restricted to a batch size of 128 due to memory constraints caused by its heavy architecture, CAST trains efficiently with standard batch sizes on the same hardware. Consequently, it yields a 65.05$ $ acceleration in training time and a 9.26$ $ reduction in memory usage compared to CCFRec, while maintaining superior Recall. The observed efficiency gains are underpinned by our lightweight design. Existing semantic-based methods typically employ heavy cross-attention mechanisms to align semantic codes with textual representations, resulting in a computational complexity of $ $ where $N$ is the number of tokens. In contrast, CAST reduces the alignment complexity to $ $ via subspace-preserving MLP and maintains standard attention complexity $ $ for transition modeling . {llll} & & & \\ SASRec & 34 & 2,480 & 3.83 \\ FEARec & 59 & 2,579 & 3.80 \\ CL4SRec & 373 & 3,114 & 3.75 \\ TedRec & 60 & 3,384 & 3.79 \\ CCFRec$^ $ & 4944 & 35,803 & 4.51 \\ & & & \\ To analyze whether semantic transition scores differentiate complementary item relations from unrelated pairs, Figure~ compares the score distributions for complementary item pairs and randomly sampled pairs. Complementary pairs exhibit substantially higher scores than random pairs , with only limited overlap between the two distributions. This observation indicates that transition scores significantly differ between complementary and unrelated item pairs, reflecting the structured semantic dependencies captured by the transition module in Section . {figs/transition_score.pdf} Early methods~ integrated Markov chains with Matrix Factorization to model item-to-item transitions. Development of deep learning introduced neural network approaches, including CNNs~, RNNs~, and GNNs~. Recently, Transformer-based architectures have been widely adopted as the backbone for sequential recommendation. SASRec~ and BERT4Rec~ leverage self-attention mechanisms to model long-range dependencies, while more recent variants, like FEARec~, perform sequence modeling in the frequency domain to mitigate noise in time-domain interactions. Despite their success, these methods fundamentally rely on atomic , treating items as independent tokens. Consequently, item ID-based methods suffer from the cold-start problem and data sparsity, as they fail to capture the latent semantic reasons for transitions and rely on historical co-occurrence statistics. To overcome the limitations of item ID-based methods, researchers have explored learning semantic representations directly from item text. Early works like UniSRec utilize PLMs to encode item text into universal representations. However, such continuous representations may over-emphasize text features, limiting the capture of collaborative signals. To address this, VQ-Rec and CCFRec introduce discrete semantic IDs derived from text, decoupling text encoding from representation learning. However, a critical limitation of these semantic ID-based methods lies in their coarse-grained sequence modeling. Despite utilizing discrete semantic codes, they essentially aggregate these fine-grained semantics into a unified item representation before sequence modeling. This obscures the sequential dependencies between specific semantic attributes, forcing the model to capture transitions solely at the item-level. CAST models transitions directly in the semantic code space, capturing the dependencies of item semantics during sequence modeling. Identifying complementary items is crucial for capturing diverse user needs beyond simple similarity~. While early works~ utilized relation graphs to model these dependencies, they relied on co-purchase statistics, which often conflate true complementary relations with spurious correlations ~. Recent advances in LLMs allow us to leverage extensive world knowledge to infer item relationships. LLMRec~ utilizes LLMs to denoise interaction graphs and infer missing edges, while other recent works~ employ LLMs to generate explainable complementary recommendations directly. However, these methods typically treat LLM knowledge as static external signals, disconnected from the dynamic sequence modeling process. CAST addresses this by internalizing LLM-verified complementary priors into learnable semantic-level transitions. This allows the model to adaptively refine functional compatibility patterns through user interactions, effectively bridging the gap between static relations and dynamic user preferences. This work presents CAST, a framework for complementary-aware sequential recommendation that models fine-grained semantic-level transitions. Our findings demonstrate that explicitly modeling dynamic dependencies in a discrete semantic space effectively uncovers latent functional compatibility, which is often obscured in traditional ID-based approaches. By modeling latent semantic dependencies, CAST can distinguish true complementary patterns from spurious correlations. Despite these advancements, two limitations remain. First, while the functional complementarity assumption fits e-commerce, it may be less effective in content-consumption domains driven by topical similarity. Second, our semantic codes remain latent, lacking direct mapping to explicit attributes for explainability. Future work will explore to generalize across domains by balancing functional and topical information, and to map latent codes to concrete item features. 