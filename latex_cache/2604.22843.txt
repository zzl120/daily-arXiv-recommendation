 {article} {acl} {Theorem} {Corollary} { } {fontenc} {inputenc} , Xiao Zhang, Yi Li, Chunli Lv \\ College of Information and Electrical Engineering, China Agricultural University, China \\ College of Computing and Data Science, Nanyang Technological University, Singapore \\ Key Laboratory of Agricultural Informatization Standardization, Ministry of Agriculture and Rural Affairs, China \\ } Retrieval-Augmented Generation has been proposed to mitigate hallucinations in large language models , where generated outputs may be factually incorrect. However, existing RAG approaches predominantly rely on vector similarity for retrieval, which is prone to semantic noise and fails to ensure that generated responses fully satisfy the complex conditions specified by factual queries. To address this challenge, we introduce a novel research problem, named xact etrieval roblem }. To the best of our knowledge, this is the first problem formulation that explicitly incorporates structural information into RAG for factual questions to satisfy all query conditions. For this novel problem, we propose tructure uided etrieval-ugmented eneration }}, which models the retrieval process as an embedding-based subgraph matching task, and uses the retrieved topological structures to guide the LLM to generate answers that meet all specified query conditions. To facilitate evaluation of , we construct and publicly release xact etrieval uestion nswering }} , a large-scale dataset comprising $120{,}000$ fact-oriented QA pairs, each involving complex conditions, spanning $20$ diverse domains. The experimental results demonstrate that significantly outperforms strong baselines on , delivering absolute gains of $20.68$--$50.88$ percentage points, corresponding to $34$\ LLMs suffer from a well-documented limitation: they often produce hallucinations that are fluent but factually incorrect~. Such hallucinations have been observed in medical QA~, legal drafting~, and scientific writing~, where factual errors may mislead users and undermine trust~. RAG alleviates hallucinations by incorporating external knowledge, shows strong performance in QA and assistant tasks. Current RAG methods can be categorized into two paradigms: chunk-based RAG, represented by NaiveRAG~, and graph-based RAG, exemplified by GraphRAG~. Both paradigms have been adopted in real-world systems such as Dify~ and LangChain~. {fig/overall.pdf} However, in real-world applications, many fact-oriented queries require that answers satisfy all conditions in the query. As shown in Fig~, in a medical QA scenario, a user may ask: This query imposes four distinct conditions on the target disease: massage is used as adjuvant therapy; it is prone to induce hypertension; it tends to cause adrenal incidentaloma; it requires differential diagnosis from subclinical Cushing's syndrome. Each condition represents a constraint, and collectively they define a complex query intent. Unlike single constraint queries, such multi-constraint queries demand that a correct answer satisfy all specified constraints. Current RAG methods such as NaiveRAG and GraphRAG often struggle to achieve this objective. The limitation lies in their reliance on semantic similarity to rank retrieval results and generate responses based on local text chunks or subgraphs from external knowledge sources. Thus, they fail to retrieve the full set of supporting information required to satisfy all the constraints in the query, leading to incomplete answers. To address this gap, we define a novel research problem called xact etrieval roblem }. Given a user query involving multiple constraints, the goal of is to retrieve information that precisely and comprehensively satisfies all specified conditions, from external knowledge sources, ensuring that the generated answer complies with every constraint in the query. To solve the , we propose tructure uided etrieval-ugmented eneration }. Unlike existing RAG methods that rely on vector similarity, introduces a retrieval mechanism guided by structure that better respects the multi-constraint structure of the query. The core idea is to model knowledge retrieval as a subgraph matching task based on embeddings, enabling the system to precisely and comprehensively retrieve information from external knowledge sources that satisfy the constraints specified in the query. The information of the retrieved subgraphs is then converted into prompts that guide the LLM in generating answers that satisfy all constraints. In the example of Figure~, retrieves a subgraph that satisfies all query constraints and produces the correct answer, . To systematically evaluate the ability of to solve , we construct and publicly release a large-scale benchmark dataset called xact etrieval uestion nswering)}. consists of three subsets built from diverse domains: , an English encyclopedic graph contributing $80{,}000$ queries; , a multi-disciplinary English dataset built from textbooks covering $18$ distinct domains, contributing $10{,}000$ queries; and , a Chinese medical knowledge graph contributing $30{,}000$ queries. Each query includes multiple constraints, with a ground-truth answer for evaluation. Based on , we compare with several RAG baselines. achieves significant and consistent gains across metrics, yielding $20.68$--$50.88$ percentage-point absolute improvements, corresponding to $34$\ Our main contributions are as follows. To address the challenges observed in real-world applications, we propose a novel RAG query paradigm, called . To solve the , we propose , a method that leverages an embedding-based subgraph matching mechanism. To enable a systematic evaluation of , we construct and release , a benchmark containing $120,000$ factual QA pairs across $20$ domains. On , outperforms strong baselines by $20.68$--$50.88$ points, corresponding to $34$\ was introduced by to improve QA by integrating vector similarity-based retrieval with the generation of LLMs. Subsequent research has progressed in two major directions: semantic alignment controlled by the query and structure-aware retrieval with explicit knowledge modeling. In the first line, HyDE~ introduced hypothetical answer generation for backward retrieval. MEMORAG~ incorporated a memory module for multi-turn coherence, while RQ-RA~ improved multi-hop QA via structured query rewriting. In the second line, GraphRAG~ pioneered entity graph integration and community-based paragraph retrieval. LightRAG~ reduced the cost of graph construction through a simplified structure. HopRAG~ introduced multi-hop subgraphs for long-range access, GRAG~ focused on dynamic graph evolution, and G-Refer~ employed dominant embeddings with contrastive learning to enforce structural consistency. For domain-specific QA, MedRAG~ integrated medical ontologies, HippoRAG~ mimicked hippocampal memory encoding, and AMAR~ enabled multi-view retrieval of entities, attributes, and paths. In summary, existing RAG methods effectively mitigate LLM hallucination, but their reliance on vector similarity limits their ability to precisely handle real-world queries with complex conditions. techniques fall into join-based and backtracking-based categories. Join-based approaches include BiGJoin and its variants~ achieving worst-case optimal joins even in dynamic graphs, fractional-cover-based joins~, and distributed matching via Timely Dataflow~. Systems such as EmptyHeaded~ utilize SIMD joins with high-level queries, while cost-based optimizers refine join orders~. Hybrid methods such as RapidMatch~ merge joins with exploration; SEED~ applies clique/star units with bushy joins; and TwinTwigJoin~ achieves optimality on MapReduce. The visual and partial matching are addressed by FERRARI~ and PANDA~, respectively. Backtracking-based methods include VF3~ and VF2 Plus~ for dense and sparse graphs, QuickSI~ with optimized orderings, and redundancy reduction strategies via Cartesian product postponement~ or algebraic pruning~. CECI~ leverages embedding clusters, while parallelized exploration~ scales to billion-node graphs. Pruning-free methods~ also perform well in biological networks. Recently, GNN-PE~ enables exact subgraph matching using dominant path embeddings. Although these methods improve scalability and efficiency, they typically assume fully labeled graphs. However, in RAG, the query target is often unknown, making such methods difficult to apply. {fig/system.pdf} In this section, we present the core notations and formal definitions. Symbols are summarized in Appendix~. A graph $G$ is a quadruple $ $, where: $V$ is a set of vertices, each $v_i V$ represented as $ , , )$, with unique id, semantic label, and textual description. $E$ is a set of directed edges $e_{ij} = $, each encoded as $ , v_i, v_j, )$. $ : V V E$ maps a pair of vertices to their edge. $L$ is a labeling function assigning each vertex $v_i V$ a label $ $. Given two graphs $G_A$ and $G_B$, if they are isomorphic, we write $G_A G_B$~. Given: a natural language query $q^{o}$ with a set of constraints $C = \{c_1, ..., c_k\}$ ; a knowledge corpus ${K} = \{k_1, ..., k_n\}$ and a large language model $ $, aims to retrieve from ${K}$ the most accurate knowledge that satisfies all the constraints in $C$, and use it to prompt the $ $ to generate a factual answer. Each constraint $c_i$ typically corresponds to an entity related to the target answer. When such precise information cannot be retrieved, the goal is to identify relevant evidence to guide the $ $ in generating a reliable answer. To address , $q^{o}$ can be structured into a query graph $q$, and $K$ can be represented as a knowledge graph $G$. This enables to be transformed into a subgraph matching task: Find a subgraph $g G$ such that $g q$, ensuring that all query constraints are structurally satisfied. However, since the subgraph isomorphism is , applying it effectively in the RAG setting poses a significant challenge. To overcome this challenge, we propose . As shown in Figure~, we formalize as a tuple: = where $ $: document structuring module that converts knowledge corpus $K$ into a knowledge graph $G$; $$: GNN model for generating path dominance embeddings; $ $: index construction module that builds R*-Tree $I_l$ over path embeddings; $ $: query graph construction module that extracts, normalizes, completes and decomposes $q^{o}$; $ $: path-level retrieval module using $I_l$; $ $: subgraph assembly module that forms exact subgraphs; $ $: answer generation module that prompts LLM with retrieved subgraphs. The overall system execution is defined: = ), ))) operates in three stages, as illustrated in Algorithm~: : the document corpus $K$ is converted into a knowledge graph $G$ via $ $. Label embeddings of entity and path $o_0 $ and $o_0 $ are calculated, and the GNN model $$ is trained. The path dominance embeddings $o $ are computed and indexed by $ $. : the user query $q^{o}$ is parsed into a query graph $q^*$ with label embeddings $o_0 $. Entity normalization, path decomposition, and label completion are performed by $ $. Each completed query path set $Q' P$ is embedded in $$, and exact path candidates are retrieved using $ $. : subgraphs are assembled by $ $, and used by $ $ to generate the answer $a$ based on the best available subgraph. External documents $K$, User query $q^{o}$ Answer $a$ Extract knowledge graph $G $ Generate label embeddings $o_0 $ for all $v_i V $ via LLM Generate path label embeddings $o_0 $ for all paths $p_z$ of length $l$ Train GNN model $$ Compute path dominance embeddings $o $ using $$ Build R*-Tree index $I_l$ over $o_0 $ and $o $ Extract query graph $q^*$ from $q^{o}$ and generate $o_0 $ for all $q_i V $ via LLM Normalize entities in $q^*$ to obtain $q$ Decompose $q$ into path set $Q$ of length $l$ Compute $o_0 $ for each $p_q Q$ Predict possible labels using $I_l$ Complete $Q$ into fully labeled path sets $P$ $S $ Compute $o $ for each $p_q Q'$ using $$ Retrieve $$ Assemble $g$ from $$, add to $S$ Generate $a$ using information of $g S$ as prompt Construct fallback subgraph $g''$ by retrieving 1-hop neighbors of known entities in $q$ Generate $a$ using information of $g''$ as prompt $a$ This stage corresponds to the components $ $, $$, and $ $ in the formal definition of the . first converts the raw corpus $K$ into a structured knowledge graph $G$, where semantically meaningful entities are represented as nodes and explicit relations as edges . The node label embeddings are then computed using a pre-trained LLM. For each node $v_i V $ with the label $ $, the embedding is defined as $o_0 = ) ^F$. To represent the semantics of the path, we define the label embedding of a path $p_z = $ as the concatenation of its embeddings of the constituent node: $o_0 = ^{k F}$. It preserves both semantic content and node order for alignment during path-level retrieval. To enable exact subgraph matching, introduces a dominant embedding mechanism based on GNN. This mechanism learns structural representations of each node and its surrounding subgraph. For each node $v_i$, a 1-hop star subgraph $g_{v_i}$ is constructed and encoded using a Graph Attention Network -based architecture. The final dominant embedding $o $ captures the topological context around $v_i$. To ensure structural containment, we enforce a dominance constraint: for any suitable substructure $s_{v_i} g_{v_i}$, we require $o o $. To implement this constraint during training, we introduce the following loss function: = \| - o ) \|_2^2 This loss penalizes any violation of the dominance condition, encouraging the GNN to learn embeddings where substructures are embedded in semantically smaller regions than their supersets. For a path $p_z = $, the dominant embedding at the path level is computed as the concatenation $o = $. These embeddings are used for pruning and matching: a query path $p_q$ is considered a match if $o o $ in all dimensions, indicating that the candidate structurally contains the query. This embedding formulation enables efficient structure-aligned filtering in the retrieval phase. See Appendix~ for full training details and architectural illustration. To enable path-based subgraph retrieval, constructs an R*-Tree index over path embeddings. I_l = ,\; o \;|\; |p_z| = l \} ), where each path $p_z$ of length $l$ is encoded by both its semantic embedding $o_0 $ and its structural embedding $o $. These embeddings are derived as follows: $o_0 $ is the concatenation of the node label embeddings to capture the semantics of the path, while $o $ is formed by concatenating the dominant embeddings of all nodes along the path $p_z$, where the dominant embedding of each node is calculated using the pre-trained GNN model $$. The resulting embedding pairs $ , o )$ are indexed for efficient retrieval. Each index node stores different content based on type: stores $o_0 $ and $o $; stores minimum bounding rectangles over $o_0 $ and $o $ for semantic and structural filtering, respectively. At query time, the index is traversed in a heap-based best-first manner. Nodes whose $MBRs$ do not overlap with the query embedding region are pruned early, accelerating candidate filtering. This stage corresponds to the components $ $ and $ $ in the formal definition of the . As part of the $ $ module, first transforms a query in natural language $q^{o}$ into a structured query graph $q^*$ using an LLM to extract entities and relations. It also computes the label embeddings $o_0 = ) ^F$ for each query node $q_i^*$. To ensure consistency with the knowledge graph $G$, performs entity normalization using FAISS~: for each $q_i^*$, the most semantically similar node $q_i V $ is retrieved and used to replace $q_i^*$, producing a normalized query graph $q$. Prompt and normalization algorithm are in Appendix~. To enable path-level retrieval, decomposes the query graph $q$ into a set of linear query paths and completes unknown node labels to produce fully specified path sets $P$. We employ a cost-aware decomposition algorithm to iteratively select query paths of length $l$ that minimize edge overlap and retrieval cost. Each path is scored using a degree-based path weight and the optimal path set $Q$ is selected to fully cover $E $. For paths containing unknown nodes, performs wildcard-based label completion by traversing the R*-Tree index $I_l$ to identify candidate labels, generating a mapping $U$ from unknown nodes to candidate label sets. All possible label combinations from $U$ are instantiated in $Q$ to form completed query path sets $P = \{ Q_1', Q_2', , Q_n' \}$ for downstream matching. The complete procedures are in Appendix~. The module $ $ performs path-level retrieval on the $I_l$ for each fully labeled query path $p_q P$. Each query path is encoded with: a semantic embedding $o_0 $ through the LLM; a structural embedding $o $ via the GNN model $$. uses a heap-based best-first traversal strategy over $I_l$. During traversal: , for each query path $p_q$ and each child node $N_i$, the system checks two constraints: : whether $o_0 $ intersects with the label MBR $_0 $; : whether the dominance region ) = \{ z ^d o z , i \} overlaps with $ $. Only when both constraints are satisfied is $p_q$ forwarded to $N_i$. , for each stored path $p_z$, it is added to the result based on : if $o_0 = o_0 $ and $o o $. This dual filtering strategy ensures both semantic alignment and structural inclusion. All valid candidates are accumulated in $p_q.$. The algorithm is in Appendix~. The module $ $ composes candidates for full subgraphs from path-level matches to produce a set: exact matches $S$ that are isomorphic to the query graph. For exact matches, enumerates all combinations of candidate paths from $p_q.$ and verifies whether they can be merged into a conflict-free subgraph. This process enables to reconstruct structurally consistent subgraphs that satisfy all query constraints. The assembly logic is in Appendix~. The module $ $ generates answers based on matched subgraphs and supports two strategies. If the exact match set $S$ is nonempty, extracts semantic labels and descriptions from all subgraphs $g S$ and combines them with the original query $q^{o}$ to construct a structured prompt for the LLM, ensuring all constraints are satisfied. When exact subgraphs are not found, falls back to building a 1-hop neighborhood subgraph $g''$ around entities mentioned in $q^{o}$ as the context of last resort. This ensures can still produce informative responses. The prompt construction process is in Appendix~. We analyze the time complexity of the retrieval phase, which consists of three components: entity normalization, unknown label completion, and exact subgraph matching. The overall retrieval complexity is the following. O ( & |V | N + _{i=0}^{h} |Q_u| f^{\,h-i+1} \\ & + _{i=0}^{h} |Q| f^{\,h-i+1} ). where $|V |$ is the number of nodes in the query graph, $N$ is the number of entities in the knowledge graph, $u$ is the number of unknown nodes, $t_j$ is the candidate label count for node $j$, and $h, f, PP_i$ denote the height, fan-out, and pruning ratio of the R*-Tree. In practice, most queries involve only one unknown node with a small number of candidates , simplifying the complexity to: O | N \\ + _{i=0}^{h} |Q| f^{h - i + 1} ) This enables near-linear scalability, with modular execution and efficient index pruning ensuring low overhead. See Appendix~ for proof details. {4pt} {lccc} & {\\} & {\\ {1pt} {!}{ {lccccccccccccccc} {*}{} & {c}{} & {c}{} & {c}{} \\ {2-6} {7-11} {12-16} & Hit@1 & Precision & Recall & F1 & Emp.S & Hit@1 & Precision & Recall & F1 & Emp.S & Hit@1 & Precision & Recall & F1 & Emp.S\\ GPT-5.1 & 19.1\ NaiveRAG & 14.8\ RAPTOR & 61.1\ GraphRAG & 61.8\ LightRAG & 20.3\ SubgraphRAG & 33.4\ HyperGraphRAG & 30.6\ LinearRAG & 25.2\ DyPRAG & 28.5\ KAG & 40.7\ & Since most existing open RAG and QA benchmarks target single constraint queries ~) and rarely verify constraint satisfaction with verifiable evidence, we construct and publicly release ERQA to systematically evaluate SG-RAG on ERP. comprises three subsets: contains $80{,}000$ English QA pairs derived from FB15K-237 with $6.1$ constraints per query on average; contains $10{,}000$ QA pairs from a cross-domain academic corpus spanning $18$ disciplines with $4.7$ constraints per query on average; contains $30{,}000$ Chinese QA pairs from the CPubMed-KG~ with $5.4$ constraints per query on average. To further assess the realism of ERQA queries, we conduct a human validation study on $2,000$ randomly sampled queries. Three expert annotators, including one PhD in Nutrition and two PhDs in Computer Science, independently evaluate each query in terms of fluency, answerability, and ambiguity. For cases that are difficult to judge, the annotators verify them through literature search. The results show high fluency , high answerability , and low ambiguity, suggesting that queries are generally natural, understandable, and answerable. Detailed statistics, example queries, and the question-generation prompt are provided in Appendix~ and Appendix~. We evaluate on for and additionally on NQ dataset to verify its generalization to single-constraint queries. All experiments were conducted on a local workstation running Ubuntu $22.04$ LTS. The hardware configuration includes a $16$ core, $32$ thread Intel Core ~CPU and an NVIDIA GeForce RTX $4060$ GPU . During dataset construction, we employed the to generate structured queries. For all vector embedding tasks, we used to maintain consistency between methods. We fix the model and the text preprocessing pipeline. In the answer generation stage, we adopted with a context window fixed at $1,200$ tokens per query. To comprehensively evaluate the generation performance of , we compare it against ten representative retrieval-augmented or generation-based baselines: NaiveRAG~, RAPTOR ~, GraphRAG~, LightRAG~, SubgraphRAG~, HyperGraphRAG~, LinearRAG~, DyPRAG~, KAG~, GPT-5.1. We additionally design two naive baselines to isolate the effect of structure guided exact retrieval. A constraint wise evidence union alternative can be strong, but we omit it because its performance is sensitive to evidence aggregation under a fixed context budget. returns chunks that mention any query entity. expands each query entity to its two-hop neighbors. We adopt a hybrid evaluation that combines objective metrics and subjective scoring to assess the accuracy and reasoning quality of the answer. We use four widely adopted metrics: Precision, Recall, F1 Score, Hit@1 as the objective metrics. We adopt as , a subjective metric, to evaluate the answers' reasoning quality . {@{}>{ }p{1.3cm}p{0.65cm}p{0.65cm}p{0.65cm}p{0.65cm}p{0.65cm}p{0.65cm}@{}} & {c}{4 Constraints} & {c}{5 Constraints} & {c}{6 Constraints} \\ {2-3} {4-5} {6-7} & Recall & Hit@1 & Recall & Hit@1 & Recall & Hit@1 \\ GPT-5.1 & 9.2\ NaiveRAG & 16.1\ RAPTOR & 29.6\ GraphRAG & 37.7\ LightRAG & 21.5\ {Subgraph\ } & 42.3\ {HyperGraph\ } & 35.8\ LinearRAG & 27.1\ DyPRAG & 24.9\ KAG & 32.4\ & As shown in Table~, consistently outperforms all baselines for all metrics on the three subsets. For Hit@1, achieves relative gains of $34$\ For Recall, relative improvements over the strongest baseline reach $35$\ For F1, relative improvements over the strongest baseline of $34$\ Table~ also reports the subjective evaluation results on all subsets, where GPT-5.2 is used as the evaluator. again achieves the highest Emp.S on all three subsets, exceeding the strongest baseline by $0.423$, $0.234$, and $0.333$, respectively, which indicates that it not only improves answer accuracy but also produces more coherent and informative reasoning. {lccc} & & & \\ Recall & 80.10\ Hit@1 & 80.00\ {lccc} & & & \\ NaiveRAG & 8.23\ Entity-based Retrieval & 9.31\ LightRAG & 8.29\ 2-hop Graph Retrieval & 8.71\ & reports performance under different numbers of query constraints. Since more constraints require the system to satisfy more conditions simultaneously, they represent more challenging retrieval settings. consistently remains the best-performing method across all settings. Even under six-constraint queries, still achieves a Hit@1 over $69$\ We further evaluate robustness under graph incompleteness caused by extraction errors. We randomly sample $1{,}000$ queries from and construct perturbed query sets by deleting entities or edges from the knowledge graph evidence, or adding spurious constraints, yielding graph edit distances~ $x \{1,2,3\}$ from the original query subgraph. As shown in Table~: for $x=1$ and $x=2$, Recall stays at $80.1$\ To verify that the gains of stem from structure-guided exact retrieval, we conduct a progressive comparison on against naive variants spanning from purely semantic retrieval to shallow neighborhood expansion, including NaiveRAG, Entity-based Retrieval, LightRAG, and 2-hop graph retrieval. {lcccc} & & & & \\ NaiveRAG & 79.51\ GraphRAG & 85.92\ LightRAG & 88.41\ & As shown in Table~, enlarging the retrieval scope brings only limited improvements, and these naive methods still remain in a low Precision and low F1 regime overall. In contrast, consistently achieves substantially higher Precision, Recall, and F1 than the naive baseline, indicating that entity co-occurrence signals and local neighborhoods are insufficient for satisfying multi-constraint queries. In addition, although is designed for multi-constraint queries, we further evaluate its robustness on single constraint queries using the NQ dataset, where queries can be represented as minimal graphs with two nodes and one edge. As shown in Table~, achieves the best performance, reaching $89.33$\ These results indicate that remains stable and competitive even when the query contains one constraint. We include a case study, details are in Appendix~. Figure~ shows the end-to-end runtimes of and the baselines under different levels of constraint. remains close to NaiveRAG and significantly outperforms GraphRAG. RAPTOR is the fastest, but its generation quality is the lowest, while offers a better balance between runtime and response quality. We further measure the index construction time of and other structure-aware RAG systems. As shown in Table~, the offline construction time is approximately $8$h for GraphRAG, $10$h for LightRAG, and $11$h for on , indicating only a marginal overhead of about $1$ hour over LightRAG. {lccc} & & & \\ & 10h & 13h & 14h \\ & 21h & 23h & 28h \\ & 8h & 10h & 11h \\ {lcccc} & Hit@1 & Precision & Recall & F1 \\ l=1 & 71.12\ l=2 & 70.45\ We analyze two key factors in the pipeline: path length $l$ and LLM selection. affects query decomposition for retrieval. To ensure edge coverage, $l$ should satisfy: $l , $ $d_q$ is the diameter of query graph $q$. In most queries have $d_q=2$, allowing $l=1$ and $l=2$. As shown in Table~, accuracy remains nearly identical in both settings, since reconstructs the full subgraph prompts after matching. On , we compare six generation models in identical settings, covering representative flagship LLMs for both English and Chinese. As shown in Table~, maintains consistent quality across different LLMs, with only marginal differences in both objective and subjective metrics. {lcccc} & & & & \\ GPT-5.1 & 80.19\ GPT-4o-mini & 79.86\ GLM-4V & 80.22\ GLM-4-Flash & 78.94\ Gemini-2.5 & 80.10\ Qwen-3 & 79.95\ We introduce a novel research problem in RAG for factual queries. To solve it, we propose , a structure guided RAG method based on subgraph matching. We construct a large-scale benchmark for systematic evaluation across domains. Experiments demonstrate that consistently outperforms strong baselines by large margins across metrics. *{Limitations} is a multi-stage pipeline errors in query graph extraction, entity normalization, or label completion may propagate to downstream retrieval and generation. In particular, parts of the pipeline rely on LLM-based information extraction during knowledge graph construction, which can introduce upstream inaccuracies that may affect subsequent indexing and retrieval. In addition, our evaluation focuses on English and Chinese LLMs. We do not study other languages, and the observed trends may not directly transfer to those settings. Finally, while is designed as a general framework, we do not explore optimizations for specific domains. Tailoring components to particular vertical domains may yield further gains beyond what is reported here. *{Acknowledgements} This work was supported by the China Agricultural University ``Young Researcher'' Start-up Fund No.~QNYJY2024144 and the Visiting Scholar Program of the China Scholarship Council No.~202506350123. { {1.1} { }{>{ }p{2.1cm} p{5.9cm}} & \\ $K$ & external knowledge documents \\ $G$ & knowledge graph \\ $q^{o}$ & a user natural language query \\ $q^*$ & initial query graph \\ $q$ & the normalized query graph \\ $a$ & the final answer generated by SG-RAG \\ $g$ & exact matched subgraphs \\ $g''$ & fallback matched subgraphs \\ $S$ & Sets of matched subgraphs $g$\\ $d_G$ & the diameter of the $G$ \\ $d_q$ & the diameter of the $q$ \\ $I_l$ & an R*-Tree index over paths of length $l$ \\ $p_z$ & path in $G$ \\ $p_c$ & path in candidate set \\ $v_i$ & a node in $G$ \\ $e_{ij}$ & an edge in $G$ \\ $$ & a GNN model of $G$ \\ $Q$ & query paths before label completion \\ $Q'$ & query paths after label completion \\ $U$ & a mapping from unknown nodes to label \\ $P$ & fully labeled query path sets \\ $p_q.{cand\_list}$ & exact candidate paths\\ $g_{v_i}$ & a star subgraph centered at $v_i$ \\ $o $ & a star subgraph embedding \\ $o_0 $ $) & a node label embedding \\ $o $ $) & a path dominance embedding \\ $o_0 $ $) & a path label embedding \\ } We employ a large language model to extract graph components from input text, following a standardized prompt template . Each extracted node is formatted as a triplet: , , )$}, and each edge as a quadruple: , v_i, v_j, )$}. This structured output ensures compatibility with downstream graph-based reasoning and retrieval modules. {1.3} {p{1.2cm} p{6cm}} & \\ & Given a paragraph, identify all semantically meaningful entities and extract structured relations among them. Each entity becomes a node and each relation becomes an edge in the output graph. \\ & Identify all entities. For each entity, extract: • $}: Unique node identifier • : Node label • : Node description $><|><$ $><|><$ $>)} \\ & For each related entity pair, extract: • $}: Unique edge identifier • , : Start and end node IDs • )$}: Description of the relation $><|><$v_i$><|><$v_j$><|><$ $>)} \\ & List all nodes and edges using as a separator. End with . \\ & } \\ We use to obtain semantic embeddings of node labels. These embeddings are stored for downstream path-level embedding composition and retrieval matching. Given a path $p_z = $, its label embedding is defined as the concatenation of node-level embeddings: o_0 = ^{k F}, preserving order-sensitive semantic representation for high-fidelity matching. {fig/GNN.pdf} This appendix describes the training process of the GNN-based dominant embedding module used in , including GAT layer formulation, subgraph aggregation, and structural dominance constraints. As shown in Fig~, the GNN adopts a standard GAT-based architecture with an input projection layer, an attention-based message passing layer, a readout layer, and a fully connected projection head. Given input label embeddings $x_j$ for node $v_j$, the attention coefficient between $v_i$ and $v_j$ is computed as: a_{v_i v_j} = a where $a $ is a shared attention function $a: ^{F'} ^{F'} $, capturing the importance of $v_j$ to $v_i$. Let $N $ denote the neighbor set of $v_i$. The attention coefficients are normalized using softmax: _{v_i v_j} = = )}{ _{v_k N } } Then the updated node representation is: x'_i = } _{v_i v_j} W x_j ) where $ $ is a nonlinear activation function, and $x'_i ^{F'}$. The dominant embedding of a node is computed from its local star-shaped subgraph $g_{v_i}$ via aggregation: y_i = _{v_j V } x'_j o = , o = o {fig/star.pdf} As shown in Fig~, each star subgraph includes the central node and its neighbors, and all of its proper substructures are used for enforcing dominance. For any proper substructure $s_{v_i} g_{v_i}$, the model enforces: o o = \| - o ) \|_2^2 This constraint ensures that smaller subgraphs embed into smaller vector regions, enabling efficient pruning. Given a path $p_z = $, its embedding is defined as: o = ^{k d} training data $D_{train}$, learning rate $ $ trained GNN model $M$ $D_{train} $ extract star subgraph $g_{v_i}$ and substructures $s_{v_i}$ add all $ $ to $D_{train}$ shuffle $D_{train}$ $} compute embeddings and loss $ $ update model: $M. , )$ $_e 0$ $} $_e _e + $ _e = 0$} trained model $$ {fig/Dominance.pdf} As shown in Fig~, verifies whether a query path $q_2 q_1 q_3$ is contained in a candidate path $v_2 v_1 v_3$ by checking: o o If the condition fails in any dimension, the match is rejected. This dominance check underpins the path-level pruning in the retrieval phase . The full training process is shown in Algorithm~. It uses a star-subgraph contrastive loss over multiple pairs to learn structure-aware embeddings that generalize to unseen queries. {1.3} {p{1.2cm} p{5.5cm}} & \\ & Given a user question, identify all semantically independent entities and extract their implicit structural relations to construct a query graph. If the question contains a target entity , it should be extracted as a node with label ``UNK''. \\ & Identify all entities. For each entity, extract: • $}: Unique node identifier • : Node label , use ``UNK'' if it is the target to be queried • : Node description $><|><$ $><|><$ $>)} \\ & For each related entity pair, extract: • $}: Unique edge identifier • , : Start and end node IDs $><|><$q_i$><|><$q_j$><|>)} \\ & List all nodes and edges in order, separated by . End with . \\ & } \\ The initial query graph $q^*$ is constructed by prompting a large language model to identify entities and their relationships from the natural language query $q^{o}$. The prompt format and examples are listed in Table~. For each node $q_i^* V $, the system computes its label embedding: o_0 = ) ^F. These embeddings serve as semantic keys for aligning query nodes with their canonical counterparts in $G$. Due to possible lexical variations in user queries, employs a FAISS-based nearest neighbor search over the label embedding space of $G$: A deep copy of the initial graph $q^*$ is made: $q $; Each $o_0 $ is used as a query vector; FAISS retrieves the most similar $q_i V $ based on cosine similarity; The node $q_i^*$ is replaced with $q_i$ in $q$. The normalization process is summarized in Algorithm~. Initial query graph $q^*$ with raw entities Normalized query graph $q$ $q $ $o_0 )$ $q_i , V )$ Replace $q_i^*$ with $q_i$ in $V $ $q$ Given a query graph $q$ and predefined path length $l$, we initialize $Q = $ and $_Q = + $. Starting from the node with highest degree, we enumerate all initial paths of length $l$ as $$, and iteratively construct the path set $_Q$ using: minimal edge overlap with existing $_Q$; minimal path weight $w = - _{q_i p} $. The optimal set $Q$ minimizing $_Q = w $ is retained. Query graph $q$, path length $l$ Query path set $Q$ representing the query plan $ $ $Q $; $_Q + $ Select starting vertex $q_i$ with the highest degree Obtain initial path set $$ of length $l$ starting from $q_i$ $} $_Q \{p_q\}$; $ 0$ _Q$} Select path $p$ of length $l$ that connects with $_Q$, minimizing: Edge overlap with $_Q$ Path weight $w $ $_Q _Q \{p\}$ $ + w $ < _Q $} $Q _Q$ $_Q $ $Q$ For each query path $p_q$ with unknown nodes, we insert zero vectors to obtain wildcard label embeddings $o_0 $. We then traverse the R*-Tree index $I_l$ using a max-heap, comparing known dimensions of $o_0 $ with MBRs to collect aligned candidate paths $p_z$, and extract candidate label values into a mapping $U$ from node IDs to label sets. Query path set $Q$; R*-Tree index $I_l$ over data graph $G$ $U$: a map from unknown vertex ID to candidate labels $U $ $ $ // Paths containing unknown nodes Select a path $p_q Q$ that contains an uncovered unknown vertex $ \{p_q\}$ $} Compute $o_0 $ using LLM // All-zero embedding for unknown nodes $ . $ Insert $ , 0)$ into heap $H$ $ ) H.$ < _{p_q } \|o_0 \|_1$} $} _0$ on known positions} $N_i. N_i. \{p_q\}$ $} Insert $ )$ into heap $H$ $} Extract labels from $p_z$ for unknown positions in $p_q$ Update $U $ accordingly $U$ Given: \[ U = \{ q_1: ,\; q_2: \}, \] we enumerate the Cartesian product of label options, e.g., $ , $, and apply each combination to a deep copy of $Q$ to obtain a label-complete set $Q'$. Repeating this for all combinations gives: \[ P = \{ Q_1', Q_2', , Q_n'\}. \] This ensures that all query paths are structurally complete and semantically instantiable for path-level matching. Algorithms~; Algorithms~ and Algorithms~ formally describe the decomposition and completion process. Query path set $Q$; candidate label map $U$ $P \{Q' Q'$ is a label-completed instantiation of $Q\}$ $ $ keys of $U$ $ ]$ $$ $P $ $} $ $ - 1$} $ ] $ $Q' $ = $ $v. $} $v. $ Append $Q'$ to $P$ $P$ In this appendix, we formalize the time complexity of the SG-RAG retrieval phase. The retrieval phase consists of three components: entity normalization, unknown label completion, and exact path-level matching. Let $|V |$ denote the number of nodes in the query graph, $N$ the number of entities in the knowledge graph, $Q$ the set of query paths after decomposition, and $Q_u Q$ the subset of query paths containing unknown nodes. Let $u$ be the number of unknown nodes, and let $t_j$ denote the number of candidate labels for the $j$-th unknown node. Let $h$ be the height of the R*-Tree, $f$ the average fan-out at each level, and $PP_i$ the pruning ratio at level $i$. The time complexity of the SG-RAG retrieval phase is O | N \\ &+ _{i=0}^{h} |Q_u| f^{h-i+1} \\ &+ _{i=0}^{h} |Q| f^{h-i+1} ). We analyze the retrieval phase in three parts. Each query node is aligned to the closest entity in the knowledge graph using FAISS-based approximate nearest neighbor search over $N$ entities. Since this search is performed once for each node in $V $, the total cost of this stage is O | N). Let $|Q_u|$ be the number of query paths containing unknown nodes. For each such path, SG-RAG traverses the R*-Tree to collect candidate labels. At level $i$, the traversal expands with average fan-out $f$, while only a fraction $ $ of branches survives after pruning. Summing over all levels gives the total cost of unknown label completion: O ( _{i=0}^{h} |Q_u| f^{h-i+1} ). After candidate labels are collected, SG-RAG enumerates all completed query path sets. If the $j$-th unknown node has $t_j$ candidate labels, then the number of completed query path sets is $ _{j=1}^{u} t_j $. For each completed query path set, SG-RAG performs path-level retrieval over all query paths in $Q$ using the same R*-Tree traversal strategy. Therefore, the cost of this stage is O ( _{i=0}^{h} |Q| f^{h-i+1} ). Combining Eqs.~, , and , we obtain Eq.~. Under the practical setting where most queries contain only one unknown node and the number of candidate labels is small , the retrieval complexity reduces to O ( |V | N + _{i=0}^{h} |Q| f^{h-i+1} ). When $u=1$, we have $ _{j=1}^{u} t_j = t_1 $. Moreover, since $Q_u Q$, the unknown label completion term can be upper bounded by _{i=0}^{h} |Q_u| f^{h-i+1} _{i=0}^{h} |Q| f^{h-i+1} . Substituting this bound into Eq.~ yields O ( |V | N + _{i=0}^{h} |Q| f^{h-i+1} ), which proves Eq.~. The above result shows that, in the common case where the number of unknown nodes is small and candidate label sets remain limited, the retrieval cost of SG-RAG remains tractable in practice. {lccccc} & & & & & \\ CM-ERQA & 1.66M & 4.58M & 5.4 & 5.7 & 3.6 \\ FB-ERQA & 14.5K & 272.1K & 6.1 & 5.4 & 3.1 \\ UD-ERQA & 314.4K & 53.0K & 4.7 & 5.1 & 3.3 \\ Datasets} As shown in Table~, ``\#Ent.'' and ``\#Rel.'' indicate the number of entities and relations, ``AvgE'' and ``AvgR'' are the average number of entities and relations per query graph, and ``\#$d$'' is the average maximum path length. These statistics guide the selection of hyperparameters such as the path length $l$. {fig/bridge-star.pdf} Each QA pair is constructed over a , defined as a pair of high-degree star nodes connected via shared bridge nodes . Each star node forms a 1-hop subgraph, while bridge nodes enable multi-hop constraint reasoning. For each subgraph: One star node is hidden and designated as the ground-truth answer; The visible star node's 1-hop neighbors and all bridge nodes are included in the question context. The structured input is then converted into a natural language question via LLM prompting. The prompt format in Appendix~. {fig/case-study.png} {1.2} {p{1.8cm} p{1.6cm} p{3.4cm}} & & \\ GPT-5.1 & Unable determine & ``...could not identify a single disease associated with all listed complications...'' \\ NaiveRAG & Unable determine & ``...no disease found to be directly related to all the listed complications...'' \\ RAPTOR & Sleep disorder & ``Sleep disorder'' \\ GraphRAG & Breast cancer & ``...may experience thrombosis, infection, and fracture...'' \\ LightRAG & Unable determine & ``...no disease clearly causes all these complications simultaneously...'' \\ SubgraphRAG & Breast cancer & ``...may experience thrombosis, infection, and fracture...'' \\ HyperGraphRAG & Unable determine & ``...could not identify a single disease associated with all listed complications...'' \\ LinearRAG & Unable determine & ``...could not identify a single disease associated with all listed complications...'' \\ KAG & prolonged inactivity & ``...The disease that is likely prolonged inactivity....'' \\ & & ``Answer: Neurovascular injury. Reasoning: Based on the description, this condition is associated with all listed complications...'' \\ In Figure~, “Magnesium” and “Zinc” are star nodes. “Magnesium” links to “Bone” and “Hormone Metabolism”; “Thyroid Health” and “Bone Formation” are bridge nodes. A generated question is: Here, “Magnesium” is the correct answer, while “Zinc” is a plausible distractor. This design supports precise multi-constraint evaluation of RAG systems. The evaluation focuses on the following three aspects. This score measures whether a query is natural, clear, and grammatically well-formed as a question. A score of $1$ indicates that the query is very unnatural or difficult to understand, while a score of 5 indicates that the query is fluent and easy to read. This metric measures whether the query can be answered based on the available knowledge graph evidence. Annotators assign ``Yes'' if the query contains enough information to support a unique factual answer, and ``No'' otherwise. This metric measures whether the query may reasonably correspond to more than one answer. Annotators assign ``Yes'' if multiple answers are plausible, and ``No'' if the query is sufficiently specific. We report the percentage of ambiguous queries, so lower values are better. For cases that are difficult to judge, the annotators verify them through literature search before making a final decision. For Fluency, we report the average score across sampled queries. For Answerability and Ambiguity, we report the percentage of queries labeled as ``Yes''. {1.3} {p{1.4cm} p{5.5cm}} & \\ & Given a structured subgraph and a user query, infer the most probable answer by reasoning over the entities and their relations. The answer must be one of the entities mentioned in the graph. \\ & Each node contains: • $id $ • $ $ • $ $ Each edge contains: • $id $ •$id $ • $ $ \\ & Convert each edge into a natural language sentence: )$].''} Concatenate all such sentences into a coherent paragraph as background knowledge. \\ & Combine the following parts into the final prompt: • Fixed instruction explaining the task • The relation paragraph from Step 2 • The user’s question • A constraint: the answer must be one of the mentioned entity labels. If undecidable, return unable to determine. \\ & \\ & } } \\ To further illustrate the effectiveness of in handling constraint-rich queries, we present a representative case from the subset : The entities included in this query and their neighbors are shown in Figure~. The comparison result is shown in Table~ {1.3} {p{1.4cm} p{5.7cm}} & \\ & Generate a fluent and concise natural language question that starts with ``Which \{CORE\_TYPE\}...''. The question must simultaneously reference: • Unique neighbors of the core node } • Shared bridge neighbors } This ensures that the answer must satisfy all structural constraints while avoiding ambiguity caused by bridge entities. \\ & • }: The type of the core entity • }: Descriptions of neighbors exclusive to the core node • }: Descriptions of shared bridge neighbors \\ & You are a Chinese language expert. Based on the placeholders provided, polish and generate a fluent, natural Chinese question that: • Starts with ``Which\{CORE\_TYPE\}...'' • Mentions both: } } • Do not reveal or explain the answer. Return only the question sentence. \\ & LLM should return a single sentence only, without any explanation or metadata. \\ & = Element} = bone formation , hormone metabolism } = thyroid health , immune system } \\ & \\ & } \\ {1.3} {p{1.3cm} p{5.9cm}} & \\ & Automatically evaluate answers from multiple methods to the same question. The LLM acts as a reviewer and scores each answer independently according to the criteria below. \\ & • } • } • \{answer\_A\} \{answer\_B\} \{answer\_F\} \\ & You are a senior evaluator. Please carefully read the question, the gold-standard answer, and the list of candidate answers. For each answer, assign scores based on the criteria below. Return the evaluation as a structured JSON. \\ & • : Is the reasoning clear, complete, and well-sequenced? • : Does the answer offer new insight or helpful suggestions? \\ & Return the scores as follows: ,} ,} } } \\ Below we provide detailed observations for each method's performance: generates generalized scenarios with no precise answer, failing to enforce multi-constraint reasoning. retrieves texts related to individual entities, but lacks a mechanism to ensure global constraint satisfaction. retrieves some relevant candidates but is affected by noisy context, leading to inaccurate answer selection. partially matches constraints , but ignores ``Achilles tendon rupture,'' resulting in hallucination of ``breast cancer.'' covers all constraint terms but fails to reason over their intersection due to lack of co-occurrence modeling. capturing only part of the evidence subgraph and overemphasizing locally relevant complications, which again leads to the incorrect answer ``breast cancer.'' fails to identify a disease satisfying all listed complications and therefore returns an indeterminate answer. also fails to produce a specific answer, indicating that although it may retrieve semantically related information efficiently. produces ``prolonged inactivity,'' which is not even a disease entity, indicating that it is distracted by a loosely related local clue rather than identifying the target condition . successfully reconstructs the full constraint structure, retrieves a matching subgraph, and generates a precise answer. This prompt guides the model to infer an answer from a matched subgraph by reasoning over structured relations and answering a user’s natural language query. The model is instructed to restrict its final answer to one of the entities explicitly mentioned in the graph, the detail shown in Table~. This prompt is used to generate a natural and concise question based on a bridge-star subgraph. The question must start with “Which \{CORE\_TYPE\}...” and mention both the **unique neighbors** of the core node and the **bridge neighbors**, ensuring the structural constraints are embedded and bridge nodes are disambiguated, the detail shown in Table. query graph $q$; fully labeled query path sets $P$; trained GNN model $M$; R*-Tree $I_l$ over data graph $G$. exact match subgraph set $S$. $S $ $p_q. $ Obtain $o $ via GNN Obtain $o_0 $ via LLM $ . Q_i'$ Insert $ , 0)$ into heap $H$ $ ) H. $ < _{p_q Q_i'} \| o \|_1$} $} _0 $ $DR ) $} $N_i. N_i. \{p_q\}$ $} Insert $ )$ into $H$ $} $p_q. p_q. \{p_z\}$ Assemble $S$ from all $p_q.$ using Algorithm 7 $S$ To assess the reasoning quality of generated answers, we adopt a subjective evaluation metric termed , designed to approximate human judgment. Each answer is scored by an LLM evaluator along two dimensions: : Does the response follow a clear and structured reasoning process? : Does the response provide useful insights, elaboration, or interpretative depth? The total score ranges from $0$ to $3$. All answers from and baseline systems are assessed under the same prompt setting by the same LLM judge to ensure fairness. We report average Empowerment Scores across pairwise comparisons. Evaluation prompt format and scoring rubric are included in Table~. Query graph $q$; Fully labeled query path set $Q'$;\ Each $p_{q_i}$ is associated with $p_{q_i}.$ Exact subgraph set ${S}$ ${S} $ $F p_{q_1}. p_{q_2}. p_{q_k}.$ , , p_{f_k}\} F$} $g $ empty graph $ $ empty map $ $ $v_s $ mapped node of $v_f$ $} v_s$} $ $; $ v_s$ add $v_s$ to $g$ if not present add all edges in $p_f$ to $g$ $} $S S \{g\}$ ${S}$ 