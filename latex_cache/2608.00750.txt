 Generative recommendation has emerged as an alternative to direct item selection by generating item identifiers autoregressively, token by token~. Many approaches adopt Semantic Identifiers , which assign each item a short sequence of discrete codes derived from a structured scheme such as a tree or trie~. Under this formulation, token positions induce a coarse-to-fine hierarchy over the item space: early tokens select a coarse partition, while later tokens progressively refine within that partition until the sequence uniquely identifies an item. This hierarchical factorization decomposes an enormous item universe into a small number of sequential token decisions, substantially improving scalability~. In practice, SID generators are typically trained in two stages~. First, supervised fine-tuning learns a valid SID decoder by maximizing next-token likelihood on logged SID trajectories, yielding a stable and readily deployable initialization~. However, SFT is inherently likelihood-based: it models the conditional distribution of the next token given a logged prefix under the behavior policy, rather than directly optimizing the outcome-based utility used in downstream evaluation. In contrast, large-scale recommender and advertising systems are evaluated using multi-objective outcomes that are observed after an item is generated and exposed, reflecting implicit user preferences and business constraints~. This mismatch between imitation-driven training and outcome-driven evaluation motivates a second-stage post-training procedure, which initializes from the SFT model and leverages logged outcomes to improve expected utility~. A central challenge is how to transform item-level outcomes into effective learning signals for token-by-token SID decoding. In existing SID-based generators, post-training often remains item-centric, mapping the terminal outcome of the exposed item to token-level updates~. Common approaches include uniformly broadcasting the same terminal reward to all tokens along the decoded SID path, and PPO-style optimization, in which token advantages are derived from terminal feedback~. Figure~ illustrates why such item-level treatments can be suboptimal for hierarchical SID decoding. Because many items share early SID prefixes and diverge only at deeper positions , uniform broadcasting yields weak and non-discriminative supervision and may even induce conflicting gradients on shared prefixes. More generally, item-level updates obscure which SID layer should be adjusted to improve utility. This motivates token-level credit assignment , which decomposes item outcomes into layer-specific credits, enabling post-training to update decisions at the appropriate depth of the SID hierarchy. {FigureMaking/token_credit_assignment_figure.pdf} While token-level credit assignment is desirable , deriving reliable token-wise learning signals from exposure-limited logs is challenging. A practical signal should be estimable for long-tail SID prefixes, attributable despite delayed end-of-sequence outcomes, and comparable across candidates at the same token position to support stable optimization. However, logged recommendation data rarely satisfies all three requirements simultaneously. We summarize three key obstacles. Interactions are highly uneven across SID prefixes; for long-tail prefixes, observations are insufficient to reliably estimate prefix-level utility, resulting in noisy and unstable credits~. Feedback is observed only after a SID path is completed , so terminal outcomes do not directly indicate which intermediate token decisions should be adjusted~. Credits conditioned on different prefixes can differ substantially in scale and variance, making position-wise advantages difficult to compare and destabilizing PPO-style updates in large-scale settings~. To address these challenges, we propose , a post-training framework that transforms item-level outcomes into dense, token-aligned learning signals for conservative token-wise improvement. HRPO integrates three key ideas. First, it employs feature-based grouping to obtain smoothed utility estimates for SID prefixes under limited log coverage. Second, it introduces residual token credits to enable incremental, layer-aware attribution. Third, it constructs position-aligned credit-to-go signals to stabilize optimization~. Building on these token-aligned signals, we instantiate as HRPO’s conservative optimization objective. RRPO performs clipped updates with group-normalized advantages and KL regularization, targeting stable improvement in large-scale systems~. Finally, we benchmark HRPO offline on KuaiRand~, an open public dataset, with session-level evaluation. We further report results from a post-launch online A/B test conducted in a large-scale advertising system, demonstrating consistent improvements under real-world operational constraints~. Our main contributions are: We identify a token-level credit assignment bottleneck in post-training SID generators when learning from item-level outcomes. In particular, reward broadcasting provides weak supervision on shared prefixes, while prefix-conditioned credits are often incomparable across positions, destabilizing PPO-style updates. We propose , a structured post-training framework that densifies sparse feedback via feature-based grouping and derives token-aligned learning signals through residual credits and credit-to-go returns. We further instantiate as a conservative optimization objective. We validate HRPO on a public dataset and complementary online A/B test evidence, demonstrating both controlled gains in simulation and positive production effects on target business metrics. Large-scale retrieval and recommendation search enormous action spaces under efficiency constraints~. Score-based sequential recommendation predicts/ranks the next item from history , while structured retrieval exploits hierarchical indexing to scale search. Generative retrieval replaces nearest-neighbor search with constrained identifier decoding~. Building on structured identifiers, SID-based generative recommenders scale recommendation via validity-constrained decoding. Recent tokenization studies improve action/item and user-context representations: LETTER learns item tokenizers, ActionPiece tokenizes action sequences with context, Pctx tokenizes personalized context, and MTGRec pre-trains with multi-identifier item tokenization~. However, they mainly use next-token imitation~, and post-training often remains item/sequence-level~. This leaves a log-only gap: turning exposure-limited item feedback into token-aligned, layer-sensitive signals for stable token-wise updates~. We address this by token-wise credit design for SID decoding, complementing item-level post-training~. Preference-based post-training aligns generative models beyond imitation via RLHF-style PPO updates~. Preference-only objectives such as DPO optimize a supervised log-ratio without reward modeling~, and group-based variants improve stability via within-group normalization~. In recommendation, S-DPO adapts DPO to list-wise learning with Plackett--Luce objectives and multiple negatives~, while SPRec uses self-play style data construction for LM-based recommenders~. RL-based post-training has also been explored, e.g., ReRe applies RL-driven reranking over generated candidates~. Yet most methods optimize at the item/list level, providing coarse supervision for internal token decisions of hierarchical identifier generators. We instead derive structured token-wise credits from logged outcomes and optimize the SID decoder with conservative, PPO-style token-level updates~. Recommendation is sequential with delayed, multi-objective feedback. Sequential recommenders target next-item prediction, while RL formulations optimize session-level objectives~; for slate actions, SlateQ decomposes slate value for tractable learning~. Prior work studies off-policy RL , pairwise learning from negative feedback~, latent-action exploration and regularization~, and hierarchical RL for delayed feedback and competing objectives~. Because online iteration is costly, simulators support controlled evaluation: RecSim~ is a general framework, and KuaiSim~ is an industrial-scale simulator benchmarked on logs such as KuaiRand~. Reward shaping motivates densifying sparse rewards under suitable conditions~. We use simulators for policy evaluation and continued improvement. Our PPO-compatible, token-level objective enables a hierarchical semantic-ID generator to start from logs and seamlessly fine-tune further with rollouts, motivating comparisons to RL baselines while we focus on stable optimization under sparse multi-behavior feedback via reward densification and cross-layer token credit assignment. Thus, HRPO bridges contextual bandit-style logged learning and simulator-interactive RL by keeping the same token-level update rule. This section formalizes the problem setting and introduces the notation used throughout the paper. We highlight the key design choices and assumptions needed for offline learning from exposure-limited logs. We then summarize SID decoding as the underlying sequential decision process that our method builds on for post-training. We consider generative recommendation~, in which a policy selects an item by generating a structured identifier sequence conditioned on a request context. Each training instance corresponds to one exposure in offline logs~. Let $$ be the user set, $$ the item set, and $$ the space of request contexts. For each impression, we represent the record as $ $ with $u_i $, context $x_i $ (user/profile features, truncated interaction history, and situational features), displayed item $v_i $, and multi-feedback vector $_i \{0,1\}^M$, where $M$ is the number of feedback channels and $b_{i,m}=1$ indicates that the $m$-th feedback signal is recorded . The offline dataset is \[ = \{ \}_{i=1}^N. \] To optimize multi-objective behavior~, we scalarize $_i$ via a configurable linear utility r = _{m=1}^{M} w_m b_{i,m}, where $\{w_m\}_{m=1}^M $ are pre-specified weights. For conciseness, we define $r_i := r $. Our goal is to learn a generative policy $ _ $ that maximizes the expected utility of exposed items under deployment constraints. [ width=0.75 , trim=0 0 0 0, clip ]{FigureMaking/model.pdf} Following recent work on SID-based generative retrieval~, each item $v $ is mapped to a fixed-length SID, i.e., a depth-$L$ token path produced by an encoding function $ $: == _1 _L, where $_t$ denotes the token vocabulary at depth $t$. For any $t \{0,1, ,L\}$, we call the first $t$ tokens of an SID a SID prefix , denoted by $_{1:t}$: _{1:t} , _{1:0} , where $ $ denotes the empty sequence. A prefix $_{1:t}$ corresponds to a node in the SID trie and represents the subset of items whose SIDs share the same prefix: \{\, v _{1:t}=_{1:t}\,\}. Intuitively, decoding proceeds by selecting tokens to refine the SID-prefix, thereby progressively shrinking $ $ until a unique item is reached at depth $L$. In the logged data, each exposed item $v_i$ can be equivalently represented by its SID path $_i= $, yielding records $ $ as needed. Each item is represented by a semantic identifier $= $. We generate $$ autoregressively: _ = _{t=1}^L _ , where $y_{<t}$ denotes the SID prefix . Decoding is validity-constrained by the SID trie: at step $t$, we restrict the next token to an admissible set $ $ induced by the current SID-prefix and renormalize the distribution over this set. In this section, we introduce the HRPO framework and describe its main components step by step. Fig.~ summarizes , a post-training framework for SID-based generative recommendation. The key difficulty is that offline logs provide utility feedback only for the final exposed item , while the policy makes decisions at every SID token. addresses this action--feedback mismatch by turning logged item-level outcomes into dense, position-aligned token-level learning signals, and by updating the policy conservatively to mitigate offline distribution shift~. Given logged interactions and a deployable base generator , proceeds in three stages, aligned with Fig.~: We deterministically map each user to a feature-defined group $c$ and aggregate logged outcomes on the SID trie to estimate a smoothed expected utility for each prefix, denoted $ $. This step transforms sparse, heavy-tailed prefix statistics into stable utilities that can be queried for any prefix encountered during decoding. For any candidate SID path $= $, we convert prefix utilities into token-level residual credits (marginal gains from choosing $y_t$ given the current prefix) and accumulate them into a position-aligned credit-to-go $ G_t $, which represents the remaining utility beyond the current prefix baseline. This yields comparable token-wise signals across different candidates at the same position. For each logged request context $x$, we construct a small proposal set $Y =\{^{ }\}_{w=1}^{W}$ of valid SID paths using a frozen reference policy $ _{ _{}}$, optionally including the logged SID path and adding unique trie-valid candidates decoded under $ _{ _{}}$. Importantly, $ _{ _{}}$ is an internal reference/proposal policy for RRPO, rather than the unknown behavior policy that produced the historical log. We then update the policy with RRPO, a PPO-style clipped token objective that uses within-set, position-wise normalized advantages derived from $_t$, while regularizing toward the same frozen reference policy to prevent over-optimistic updates on out-of-distribution tokens. Offline logs provide feedback only for the exposed final item, while token-level learning requires utility estimates for intermediate SID prefixes. A naive approach estimates a prefix utility by averaging terminal utilities over all log records that share the prefix, but this is unreliable for long-tail prefixes~. To better align terminal outcomes with the sequential token decisions made during trie-constrained decoding, we estimate smoothed prefix utilities that can be queried for any encountered prefix ). The challenge is that prefix statistics are highly imbalanced and heavy-tailed: many long-tail prefixes (and many user--prefix pairs) have only a few exposures, making per-user estimates noisy or undefined. To obtain well-defined, low-variance prefix utilities while retaining meaningful personalization, we pool data within feature-defined user cohorts. Since this aggregation is used in deployment, we use a deterministic grouping function; concretely, we assign each user $u$ to a cohort $c \{0,1, \}$ based on a small set of stable user features : c =g , where $g $ is a fixed mapping implemented in the system; Appendix~ reports the concrete KuaiRand features, cohort support, and grouping sensitivity. For each record with cohort $c=c $ and path $$, we update cohort-conditional prefix statistics along the trie path: n n +1, s s +r, t , Empirical means $s /n $ can be unstable in the long tail, so we apply a lightweight shrinkage with pseudo-count $ $ toward the global mean $ _{}$: =)+ \, _{}}{n + }, _{}={||} _{ } r . In implementation, for rarely observed cohort-specific prefixes, we default to a broader estimate. Finally, we score a candidate SID path $$ under context $x$ using the terminal-prefix estimate $ ,y_{1:L} )$. Given cohort-conditioned prefix utilities on the SID trie , we construct position-aligned residual returns for SID tokens and accumulate them into a credit-to-go signal. These dense, token-level targets address the action--feedback mismatch in log-only training and enable stable within-proposal normalization in RRPO ). Offline feedback is only observed at the terminal leaf , whereas the policy acts at every token position during trie-constrained decoding. A common workaround in existing approaches---including terminal-reward RL and item-level reranking based post-training---is to treat the whole SID path as a single action and broadcast the same terminal reward to all tokens on the path~. However, this uniform assignment blurs which SID layer actually contributed to the outcome, and it often yields noisy, high-variance token updates because all positions share an undifferentiated supervision signal. Our solution is to residualize prefix utilities along the prefix sequence ). Specifically, we view $ $ as the estimated value of the trie node corresponding to prefix $y_{1:t}$ under cohort $c$, and assign each token $y_t$ the marginal utility gain of moving from the parent prefix $y_{1:t-1}$ to the child prefix $y_{1:t}$: r_t &= - ,\\ y_{1:0} & , _{}. This residual credit removes the cohort-specific prefix baseline and attributes utility locally to the token choice at position $t$. Moreover, it is additive and consistent with trie traversal: the sum telescopes to \ = - _{}\), i.e., token credits form a decomposition of the terminal prefix score up to a constant baseline. Using only the instantaneous increment $ r_t$ is insufficient: two candidates may have similar local increments at position $t$ but very different remaining utility depending on how the suffix will unfold. Conversely, broadcasting the terminal score to every position discards the current prefix state and degenerates to undifferentiated supervision. We therefore aggregate residual credits from the current position onward, which preserves the prefix baseline at $y_{1:t-1}$ while accounting for the remaining potential beyond it. Specifically, we define the cumulative residual return as: G_t = _{k=t}^{L} r_k , which telescopes to G_t = - . Eq.~ shows that $ G_t$ measures the remaining utility beyond the current prefix baseline $ $. As a result, $ G_t$ is naturally comparable across candidates at the same position $t$ while remaining consistent with the cohort-conditioned terminal prefix score. This makes $ G_t$ a suitable learning signal for within-group normalization and conservative token-wise optimization. RRPO performs conservative offline updates of the trie-constrained generative policy by treating the token-level credit-to-go $ G_t $ as the return signal. Offline policy refinement is sensitive to distribution shift, and under trie constraints, increasing the probability of an early token can redirect decoding to a different feasible subtree, inducing amplified changes in later-token distributions~. RRPO mitigates these risks with within-proposal, position-wise normalization, PPO-style clipped updates, and an explicit trust-region penalty. For a fixed context $x$ , let $c c $. We first sample a proposal set $Y =\{ ^{ } \}_{w=1}^{W}$ from $ _{ _{}}$, where $W$ is the proposal size. For each candidate, define $ G_t^{ } G_t })$ . We normalize $ G_t^{ }$ within the proposal group at each position $t$ to obtain a scale-free advantage: A_t^{ } = -{W} _{j=1}^{W} G_t^{ } }{ \! }\}_{j=1}^{W} )+ }. We then form token-level likelihood ratios under the same autoregressive factorization and trie-valid conditional distributions as in Eq.~. For candidate $w$ at position $t$, with prefix $^{ }_{1:t-1}$ and token $y_t^{ }$, the ratio is _{w,t} = ( _ }_t x, ^{ }_{1:t-1}) - _{ _{}} }_t x, ^{ }_{1:t-1}) ). To prevent overly large offline updates, we apply PPO-style clipping: _{w,t} \;=\; \! ,\,1- _{},\,1+ _{} ). RRPO maximizes a clipped token-wise surrogate weighted by $A_t^{ }$, and penalizes deviation from $ _{ _{}}$ via a trust-region KL term: _{ } &{W} _{w=1}^{W} _{t=1}^{L} \! \,A_t^{ },\ _{w,t} \,A_t^{ } ) - _{}\,_{} , _{} =_{ _{ _{}} } . Training alternates between proposal generation under $ _{ _{}}$, computing $ G_t$ and $A_t$ from cohort-conditioned prefix utilities , and updating $ _ $ by maximizing Eq.~. We periodically synchronize $ _{} $; full pseudocode is provided in Appendix~ for reference. In this section, we conduct a comprehensive evaluation of for generative recommendation. To thoroughly assess 's effectiveness and understand when and why it works, we aim to answer the following research questions: How does compare to mainstream post-training methods for optimizing immediate utility versus long-horizon return? How sensitive are the results to key hyperparameters and design choices? How do individual components of contribute to the final performance? Do the improvements persist online with validated gains? We construct offline training and evaluation logs from the public KuaiRand dataset~. We use the subset and sessionize the impression stream into user-day sessions for sequential recommendation. Each impression record contains a user $u$, context $x$, an exposed item $v$, and multi-behavior feedback $$, which is scalarized into a utility $r $ via Eq.~ . Table~ summarizes the resulting sessionized logs used throughout our experiments. Our objective is to better align token-level SID decisions with users' latent preferences under multi-objective feedback. In practice, preference signals are revealed through sequential interactions, and platform utility is typically a scalarization of multiple behaviors ) rather than a single immediate metric. Purely offline evaluation from static logs is limited for diagnosing such interaction-induced effects and for comparing methods that require interaction. Therefore, following prior work, we additionally use a controlled simulator built on top of the same public logs to provide step-wise feedback and state transitions, enabling session-level evaluation of cumulative simulator reward and simulator-interactive baselines. In the public KuaiSim rollout tables, we report the simulator's click-based Avg./Total reward as the primary reward metric, and report Long separately as an auxiliary behavior rate. We treat the simulator as a proxy rather than a perfect replica of production, and corroborate conclusions with validated online A/B evidence in production. Accordingly, simulator results are used for controlled relative comparison under a shared environment, whereas production conclusions are drawn from the online A/B test. We use the public KuaiRand-Pure logs and KuaiSim rollout protocol following the simulator's standard preprocessing and evaluation setup. {3pt} {0.86} {l r} & \\ Time span & 2022-04-09 $ $ 2022-05-08 \\ \#Users & 19{,}574 \\ \#Items & 5{,}659 \\ \#Interactions & 1{,}341{,}250 \\ \#Sessions & 303{,}472 \\ Avg.\ interactions/user & 68.5 \\ Avg.\ interactions/session & 4.42 \\ Avg.\ active days/user & 15.5 \\ Density $||/ $ & 0.012109 \\ We implement all methods in PyTorch and evaluate them in KuaiSim~. For SID-generating policies, the action is a fixed-depth SID path with depth $L{=}4$ and per-level vocabulary size $|_t|{=}32$. Decoding follows the shared SID trie admissible set $y_t $ ; by default, we use constrained beam search and take the top-1 beam. All SID post-training baselines and share the same small Transformer decoder backbone and start from the same SFT checkpoint trained with teacher-forced next-token prediction for 5 epochs. For log-only post-training, all methods use AdamW with learning rate $1{ }10^{-5}$, weight decay $0.01$, batch size 1024, gradient clipping at 1.0, and one epoch over the offline logs. For , we use proposal size $W{=}18$, PPO clip $ _{}{=}0.2$, KL coefficient $ _{}{=}0.1$, smoothing pseudo-count $100$, and synchronize the frozen reference policy every 20 update steps. Experiments are run on a single NVIDIA RTX~4090 GPU , representing a small-model regime in our offline study. For production-backbone scaling diagnostics, we additionally report 0.005B--0.2B model families in Appendix~. We benchmark against representative baselines spanning three families: score-based sequential recommenders trained on offline logs, SID-generating policies optimized by offline post-training objectives, and simulator-interactive RL methods trained in KuaiSim. All methods are evaluated under the same experimental pipeline, with the shared backbone, decoding, and optimization settings summarized in Sec.~. DT ~, GRU4Rec~, SASRec~, TIGER~. SFT, DPO~, S-DPO~, SPRec~, GRPO~. TD3~, DDPG~, A2C~, HAC~, and an interactive DT~. Across all methods, we follow KuaiSim's whole-session evaluation protocol. EpisodeLen corresponds to KuaiSim's , i.e., the number of request--feedback interactions before the user leaves the session . In the public KuaiSim rollout evaluation, we use the simulator's click-based immediate reward, denoted by $r_t=b_t^{} \{0,1\}$, as the primary reward metric for Avg./Total reward. We report the undiscounted session return as the average sum of immediate click rewards per session, ${N} _{i=1}^N _{t=1}^{T_i} r_{i,t}$, and Avg.\ reward as the per-request mean return, i.e., Total reward divided by EpisodeLen. We also report Coverage as the number of distinct exposed items, and impression-level Click/Long rates as the empirical frequencies of the corresponding binary behavior indicators, respectively. Long is reported as an auxiliary behavior metric rather than being included in the Avg./Total reward columns. For one-step evaluation, EpisodeLen $=1$ and Avg.\ reward equals Total reward. Tables~-- summarize three complementary evaluation settings: offline training with session-level rollouts, offline training with one-step evaluation , and simulator-interactive training with session-level rollouts. Table~ reports whole-session performance for log-only methods. Score-based recommenders improve over Random but remain behind SID-generating policies, suggesting that validity-constrained SID decoding provides a stronger action space for long-horizon optimization in this simulator. Among SID-generating post-training baselines, achieves the best session return: Total reward increases from 8.137 to 10.528 and EpisodeLen increases from 14.14 to 15.33. Coverage stays comparable to other offline baselines (56\ indicating that the gain is not explained by overly concentrating exposure on a small set of items. To isolate immediate utility, Table~ evaluates a single decision . attains the highest one-step reward , consistent with stronger local preference alignment at the first decision. The lower one-step Coverage indicates that is more selective in assigning probability mass to high-utility items, and over longer horizons these local gains can compound across multiple steps. Table~ compares to simulator-interactive KuaiSim RL baselines under a matched interaction budget. These baselines achieve moderate returns but exhibit very low Coverage (e.g., 3\ which is consistent with mode-seeking behavior under nearest-neighbor action realization. achieves both the best return and substantially higher Coverage (57\ indicating more stable exploration--exploitation behavior. {3.5pt} {1.12} {!}{ {lcccccc} & & & & & & \\ Random & 11.16 & 0.1898 & 2.1170 & & & & 57\ GRPO & 14.07 & 0.5690 & 8.0060 & 58\ & & & & 56\ } {3.5pt} {1.12} {!}{ {lcccccc} & & & & & & \\ Random & 1.0 & 0.2130 & 0.2130 & & & 23\ & 1.0 & & & 15\ } {3.5pt} {1.12} {!}{ {lcccccc} & & & & & & \\ Random & 11.16 & 0.1898 & 2.1170 & & & & 12\ & & & & Figure~ summarizes sensitivity to four key knobs in HRPO/RRPO under offline training with session-level evaluation. Across sweeps , performance is stable in a broad neighborhood, and the best region consistently corresponds to moderate aggregation/regularization that balances robustness and learning capacity. For group-based updates , increasing group size $W$ improves return and EpisodeLen up to a saturation region . Larger groups strengthen within-context normalization and expose more informative hard negatives, making token credits more contrastive; beyond saturation, extra candidates add diminishing information and can introduce overly hard/redundant negatives, increasing gradient noise and slightly hurting stability. For KL regularization , a non-zero $ _{}$ is beneficial , indicating that constraining updates to stay close to the frozen reference/SFT policy prevents overfitting to a narrow set of hard negatives under exposure-limited logs. However, overly large $ _{}$ makes the update too conservative and caps improvement in both EpisodeLen and return. For PPO clipping , moderate $ _{}$ yields the best trade-off. Too small $ _{}$ suppresses likelihood-ratio changes and slows learning, while too large $ _{}$ allows abrupt policy shifts that amplify token-wise variance across SID layers and can destabilize decoding. This aligns with RRPO's goal of conservative, incremental improvement: clipping controls step size when token credits are high-variance, especially for early SID tokens that gate large subtree changes. For shrinkage in prefix-utility estimation , $ $ controls a variance--bias trade-off in the long tail. Weak shrinkage leaves rare-prefix utilities noisy and propagates high-variance credits, whereas overly strong shrinkage collapses prefix differences toward the global mean and reduces the discriminativeness needed for layer-specific credit assignment; the best region corresponds to sufficient smoothing to denoise without washing out prefix-level structure. Taken together, these sweeps support that HRPO is not brittle: it benefits from sufficiently informative groups , conservative policy improvement , and calibrated smoothing for long-tail prefixes , aligning with the roles of group normalization, conservative optimization, and robust reward estimation in our design. {0.48 } {FigureMaking/groupsize.pdf} {0.48 } {FigureMaking/KL.pdf} }$.} {0.48 } {FigureMaking/clip.pdf} }$.} {0.48 } {FigureMaking/smooth.pdf} {3.5pt} {1.05} {!}{ {lccc c} & {c}{} & \\ {2-4} {5-5} & & & & \\ & & & & \\ w/o cohorting & 14.72 & 0.6338 & 9.3310 & 0.4280 \\ w/o residual credit & 15.13 & 0.6696 & 10.1320 & 0.4880 \\ w/o credit-to-go & 15.14 & 0.6522 & 9.8720 & 0.4670 \\ w/o KL & 15.10 & 0.6657 & 10.0490 & 0.4930 \\ } {FigureMaking/architecture.pdf} Table~ studies four ablations of by removing one component at a time: cohorting, residual credit, credit-to-go, and KL regularization. The results reveal a clear division of labor across the pipeline. Cohorting has the largest effect on signal quality: removing it reduces session return from 10.528 to 9.331 and one-step reward from 0.548 to 0.428. This indicates that stable prefix-utility estimation is the main bottleneck under exposure-limited logs, where raw prefix feedback can be sparse and heterogeneous. Removing cohorting yields the largest drop in both one-step and whole-session metrics, highlighting the importance of cohort-conditioned prefix utilities for stabilizing group-based updates. Without cohorting, prefix utilities become less calibrated across heterogeneous contexts, making within-group comparisons noisier and amplifying spurious advantages early in the SID path. Removing residual credit reduces session return from 10.528 to 10.132 and one-step reward from 0.548 to 0.488. This supports the need for token-aligned residual advantages that disentangle per-layer contributions along the SID path. Such localization prevents late-token noise from being incorrectly attributed to early coarse decisions, which is crucial for coarse-to-fine decoding. Removing credit-to-go further reduces session return to 9.872 and one-step reward to 0.467. This shows that accumulating residual credits over the remaining suffix provides a more informative and lower-variance training signal than using only local token credit. In particular, credit-to-go makes advantages more comparable at the same SID position by accounting for remaining utility beyond the current prefix baseline, reducing myopic updates. Finally, removing KL regularization causes a consistent degradation, reducing session return to 10.049 and one-step reward to 0.493. This suggests that anchoring updates to the SFT initialization is beneficial when optimizing against hard negatives in group-based objectives. KL acts as a trust region that limits overreaction to a finite proposal set, which otherwise can overfit rare tokens/prefixes and hurt constrained decoding stability. {FigureMaking/online.pdf} We validate whether the offline improvements translate to a real production setting with an online A/B experiment in a large-scale advertising system. Figure~ shows the production backbone used for A/B serving. The encoder memory sequence is formed by concatenating interaction-history tokens and static-feature tokens, and the SID decoder uses cross-attention to this memory while autoregressively decoding SID tokens. Figure~ summarizes the deployment loop shared by online/simulator serving and RRPO-style continuation. In panel , the deployed SID policy serves requests and generates impressions; user responses are recorded and stored as interaction logs. In panel , we convert logged outcomes into token-level credits via hierarchical credit assignment and optimize the SID decoder with RRPO to obtain an updated policy, which is periodically deployed to refresh the online policy. Here, the old policy denotes the frozen reference policy used to generate proposal groups and compute PPO-style likelihood ratios during RRPO training, rather than an estimate of the historical online behavior policy. Thus, RRPO does not require logged serving probabilities; instead, the old and updated policies are evaluated on the same SID prefixes, and the frozen reference serves as a conservative anchor for token-level updates. Table~ reports the relative changes of versus control on three target IAA traffic segments. Exposure measures served traffic volume and is used as a guardrail. Cost measures observed business cost, while Target Cost is the calibrated target-cost metric used in the launch review. The results show positive Target Cost lifts across all three segments, with the strongest gain on Fiction IAA, while Exposure and Cost remain close to the control policy. This pattern suggests that the online gain is not driven by a large traffic-volume shift or by uniformly increasing cost. Instead, improves the target metric within the existing serving distribution, which is consistent with its conservative token-level update design and the deployment requirement of preserving stable business traffic. {4.0pt} {1.05} { }{@{}lrrr@{}} Segment & Exposure & Cost & Target Cost \\ Short-Drama IAA & -0.117\ Mini-Game IAA & -0.132\ Fiction IAA & +0.750\ In this work, we present , a post-training framework for SID-based generative recommendation under exposure-limited offline logs with only terminal, item-level feedback. estimates reliable utilities for SID prefixes and decomposes them into residual, token-aligned credits, which are optimized via a conservative RRPO objective for stable offline improvement. Experiments show that delivers substantial gains in simulator rollouts and transfers to production, improving Target Cost across target IAA traffic segments while keeping exposure and cost close to the control policy in an online A/B experiment. Future work will extend residual credit assignment beyond trie-structured SIDs and improve robustness under distribution shift and long-tail sparsity in broader industrial serving settings. This appendix summarizes the offline post-training loop used in the paper. The procedure optimizes the RRPO objective using cohort-conditioned smoothed prefix means and residual credit-to-go . Logged dataset $=\{ \}$; mapping $ $; admissible set $ $. Pretrained policy $ _ $; frozen reference policy $ _{ _{}}$. Max cohorts $C$; group size $W$; smoothing $ $; clip $ _{}$; $ _{}$; small constant $ $; sync period $M_{}$. Build cohort ids $c $ and include a global cohort $c=-1$ . Initialize prefix stats $n ,s $ for $c \{-1,0, ,C-1\}$ and all trie prefixes $y_{1:t}$. ) $} $y\! \! $,\; $r\! \!r $ ; update $ $ for all prefixes of $y$ for both $c $ and $c=-1$ . Compute smoothed prefix means $ $ . Sample a minibatch $\{ \}_{i=1}^{B}$ from $$. _i)$ in the minibatch} $c_i c $;\;\; $y_i^{ } $. $Y \{y_i^{ }\} $ up to $W\!-\!1$ additional unique candidates via constrained decoding under $ _{ _{}}$ with $y_t $, then deduplicate. For each $y_i^{ } Y $, compute $\{ G_t^{ }\}_{t=1}^L$ from $ $ by Eq.~, then normalize across $w$ per position to obtain $\{A_t^{ }\}$ . Update $ $ by maximizing Eq.~ on the minibatch with trust-region penalty $_{}$ . } = 0$} Sync reference: $ _{} $. Offline training first precomputes cohort-conditioned prefix statistics from $$ and then repeats: proposal generation under the frozen reference policy $ _{ _{}}$, token-wise advantages from prefix tables via residual credit-to-go, and RRPO updates with periodic reference-policy synchronization . This procedure yields a stable post-trained decoder while keeping updates aligned with the logged-data support. For the default cohort mapping $g $ in Eq.~, we use deterministic tuple bucketing over stable, coarse-grained user-side attributes available in KuaiRand. The mapping is fixed before post-training, ignores empty buckets, and yields 120 effective cohorts in the default setting. Tables~-- report grouping sensitivity and cohort-support statistics. These results show a support--granularity trade-off. Without cohorting, global prefix statistics are too coarse for heterogeneous users. Coarse buckets under-partition users, whereas overfine buckets fragment support and make prefix estimates noisy. Default buckets and KMeans clusters remain close, suggesting that HRPO benefits from cohort-level smoothing rather than from a specific handcrafted partition. {2.0pt} {1.03} {!}{ {l c c c c} Strategy & Session length & Avg. reward & Total reward & One-step reward \\ No cohorting & 14.72 & 0.6338 & 9.3310 & 0.4280 \\ Coarse buckets & 15.01 & 0.6615 & 9.9300 & 0.4960 \\ Default buckets & 15.33 & 0.6869 & 10.5280 & 0.5480 \\ KMeans clusters & 15.27 & 0.6812 & 10.4010 & 0.5330 \\ Overfine buckets & 15.09 & 0.6704 & 10.1180 & 0.5070 \\ } {2.0pt} {1.03} {!}{ {l c c c c c} Strategy & Effective cohorts & P25 & Median & P75 & Mean \\ Coarse buckets & 63 & 87 & 133 & 367 & 380.14 \\ Default buckets & 120 & 34 & 62 & 137 & 216.34 \\ KMeans clusters & 192 & 12 & 42 & 84 & 142.11 \\ Overfine buckets & 424 & 1 & 5 & 24 & 64.35 \\ } To further examine whether the benefit of transfers beyond the KuaiRand/KuaiSim environment, we conduct an additional evaluation on the public Agent4Rec-MovieLens simulator~. Agent4Rec constructs LLM-powered user agents from MovieLens-1M profiles and simulates page-by-page recommendation sessions. We evaluate all methods under the same simulator setup and report two session-level metrics: AvgR, the average reward over simulated interactions, and Avg. Session Length, the average number of recommendation pages consumed before the user leaves. Table~ shows that TIGER+ achieves the best performance on both metrics. Compared with TIGER, improves AvgR from 0.853 to 0.884 and session length from 8.33 to 9.12, also outperforming DPO and GRPO on the same backbone. Since Agent4Rec differs from KuaiSim in simulator construction and user modeling, this result suggests the gain is not tied to one rollout environment. {6pt} {1.08} {0.85 }{@{}lcc@{}} Model & AvgR & Avg. Session Length \\ Random & 0.558 & 4.67 \\ GRU4Rec & 0.797 & 6.96 \\ SASRec & 0.859 & 7.79 \\ TD & & 8.85 \\ DDPG & & \\ HAC & 0.873 & 8.94 \\ A2C & 0.869 & 8.73 \\ TIGER & 0.853 & 8.33 \\ TIGER+DPO & 0.861 & 8.48 \\ TIGER+GRPO & 0.869 & 8.70 \\ TIGER+ & & \\ In addition to the main HRPO experiments, we report auxiliary scaling-law diagnostics from six A/B model sizes used in production training logs. Table~ summarizes the model-size and training settings used in the scaling study. Here, Size denotes the number of model parameters . Batch size is chosen per model scale based on our production training recipe; learning rate is tuned per scale accordingly. Figure~ plots the corresponding training loss curves. Larger models generally reach lower loss faster, and all curves decrease smoothly without unstable spikes. We include these logs as a diagnostic complement to the online experiment, showing that production training remains numerically stable across the A/B model-size range, while the main HRPO conclusions still come from the controlled offline, simulator, and online comparisons. }$ , \#L=\#layers, \#H=\#heads, emb=embedding dimension, bs=batch size, lr=learning rate.} {5.0pt} {1.05} {!}{ {l r r r r r r r} Size & FLOPs & $d$ & \#L & \#H & emb & bs & lr \\ 0.005B & 20M & 64 & 6 & 4 & 16 & 896 & 0.0030 \\ 0.01B & 40.6M & 128 & 8 & 4 & 16 & 512 & 0.0010 \\ 0.02B & 83.2M & 256 & 12 & 8 & 16 & 256 & 0.0005 \\ 0.05B & 160.5M & 512 & 6 & 8 & 16 & 128 & 0.0004 \\ 0.1B & 243.8M & 768 & 8 & 12 & 16 & 64 & 0.0003 \\ 0.2B & 332.7M & 1024 & 12 & 16 & 16 & 32 & 0.0001 \\ } {FigureMaking/scaling_law.pdf} {acmart} {KL} {Std} {clip} {} -0.8em }}} {Proceedings of the 32nd ACM SIGKDD Conference on Knowledge Discovery and Data Mining V.2}{August 09--13, 2026}{Jeju Island, Republic of Korea} } } } } } } } } } {Kaifeng Guo et al.} Generative recommenders select items by autoregressively decoding semantic identifiers , whose token positions induce a coarse-to-fine hierarchy over the item space. In practice, SID decoders are trained via supervised next-token prediction, which imitates logged trajectories rather than directly optimizing downstream utility. This motivates post-training with outcome feedback to guide decoding toward higher utility. However, logged feedback is only observed for the final exposed item, causing most post-training methods to operate at the item level and broadcast the same terminal signal across all SID tokens. As a result, token-level credit assignment becomes sparse, high-variance, and layer-dependent. To this end, we propose ierarchical esidual olicy ptimization , a post-training framework that converts item-level outcomes into dense, token-aligned learning signals for conservative token-wise improvement. Specifically, HRPO first estimates SID prefix-level utilities via group-wise reward smoothing over feature-based user clusters. It then decomposes these utilities into residual token credits and accumulates them into credit-to-go signals. Finally, esidual-eturn olicy ptimization optimizes the residual credits using clipped updates, group-normalized advantages, and KL regularization to preserve stability. Experiments on a public dataset and an online A/B test in a large-scale commercial system show consistent gains in session-level utility and key business metrics. Source code and the archived artifact are available for reproduction~; DOI: .}. <ccs2012> <concept> <concept_id>10002951.10003317.10003347.10003350</concept_id> <concept_desc>Information systems~Recommender systems</concept_desc> <concept_significance>500</concept_significance> </concept> </ccs2012> {Information systems~Recommender systems} This research was partially supported by National Natural Science Foundation of China , Hong Kong Research Grants Council , Institute of Digital Medicine of City University of Hong Kong , and Kuaishou . 