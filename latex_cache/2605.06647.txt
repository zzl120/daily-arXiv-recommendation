{ colback=gray!5, colframe=gray!40, boxrule=0.4pt, left=6pt, right=6pt, top=3pt, bottom=3pt, fontupper= } {hyperref} {cleveref} {fairmeta} {inputenc} {fontenc} {Zeyu Yang} {Qi Ma} {Jason Chen} {Anshumali Shrivastava} {Meta Superintelligence Labs} {Rice University} {Work done at Meta} , which defines in retrieval as the ability to compress multi-round exploratory search into a single corpus-discriminative retrieval action. SIRA does not merely ask what terms are relevant to the query; it asks which terms are likely to separate the desired evidence from corpus-level confusers. On the corpus side, an LLM enriches each document offline with missing search vocabulary; on the query side, it predicts evidence vocabulary omitted by the query; and document-frequency statistics as a tool call to filter proposed terms that are absent, overly common, or unlikely to create retrieval margin. The final retrieval step is a single weighted BM25 call combining the original query with the validated expansion. Across ten BEIR benchmarks and downstream question-answering tasks, SIRA achieves the significantly superior performance outperforming dense retrievers and state-of-the-art multi-round agentic baselines, demonstrating that one well-formed lexical query, guided by LLM cognition and lightweight corpus statistics, can exceed substantially more expensive multi-round search while remaining interpretable, training-free, and efficient. } and Anshumali Shrivastava } {Will be available at } SIRA's retrieval layer is a BM25 implementation in Rust with Python bindings, providing the following capabilities. The engine maintains two index tiers: ~a standard inverted index over unigrams with exact vocabulary lookup, and ~a inverted index for bigrams through 4-grams using MurmurHash3 with $2^{23}$ hash slots. The hashed tier provides memory-bounded n-gram support regardless of corpus size; hash collisions can only over-estimate document frequency , which conservatively dampens rather than inflates scores. The engine exposes an enrichment operation that registers an LLM-proposed multi-word phrase as a in a document's posting list. Unlike standard tokenization, which decomposes ``diagnostic accuracy'' into sub-bigrams, this operation preserves the phrase as one retrieval unit, ensuring that matching requires the full phrase to appear in the expansion. A batch variant processes all enrichments in parallel during index construction. All text is processed through Unicode NFKD normalization, lowercasing, Porter stemming, and stopword removal. This ensures that enrichment terms are comparable to corpus terms at the stem level, maximizing lexical overlap. lists the BM25 scoring parameters used throughout all experiments. We use the Lucene variant, which guarantees non-negative IDF for all terms. {lll} & & \\ Scoring variant & Lucene & IDF always $ 0$ \\ $k_1$ & 1.5 & TF saturation rate \\ $b$ & 0.75 & Document-length normalization \\ $ $ & 0.5 & BM25L/BM25+ additive smoothing \\ N-gram tiers & Unigram + hashed 2--4-grams & MurmurHash3, $2^{23}$ slots \\ Expansion weight $w$ & 0.5 & Weight of $q_{}$ relative to $q_{}$ \\ We provide detailed descriptions of the ten baseline systems compared in~, grouped by retrieval paradigm: lexical, neural , and agentic. .} BM25 is the standard unsupervised sparse retrieval function derived from the probabilistic relevance framework. Given a query $q$ and document $d$, BM25 scores each query term by its inverse document frequency weighted by a saturating term-frequency component controlled by two parameters: $k_1$ and $b$ . Scoring requires only integer arithmetic over inverted-index posting lists, making BM25 the fastest retrieval function in our comparison and the backbone of most production search systems. BM25 serves as the lower bound for lexical methods: it uses no learned components and no external knowledge. .} SPARTA is a learned sparse retrieval model. At indexing time, a pretrained transformer encodes each document's tokens into contextualized embeddings and precomputes, for every vocabulary term, a matching score via max-pooled dot products followed by a log-saturation function )$); these scores are stored in an inverted index. At query time, each query token is represented by a non-contextualized word embedding, and scoring reduces to an $O $ lookup per query--term pair plus summation over matched terms, preserving inverted-index efficiency while learning which terms are most important for relevance matching. We use the BeIR re-implementation checkpoint trained on MS~MARCO , which substitutes DistilBERT for the BERT-base encoder used in the original paper. .} Doc2Query performs corpus-side document expansion: a T5 sequence-to-sequence model is fine-tuned on query--document pairs from MS~MARCO to predict queries that a document would be relevant to. At indexing time, each document is augmented with sampled predicted queries , which are appended to the original document text before BM25 indexing. This expansion injects query-side vocabulary into the document representation, improving recall for queries that use different terminology than the document. We use the T5-base checkpoint . Doc2Query is the closest supervised analogue to SIRA's corpus-side enrichment, though it relies on MS~MARCO supervision rather than a frozen LLM. .} SPLADE combines learned sparse representations with vocabulary expansion. It applies a masked language model head on top of a BERT-based transformer and passes the logits through a log-saturation function )$) to produce a sparse weight vector over the full vocabulary for both queries and documents. The resulting representation expands beyond the original terms by assigning nonzero weights to semantically related vocabulary items, while maintaining sparsity through FLOPS-based regularization that penalizes the expected number of floating-point operations at retrieval time. SPLADE achieves strong performance by combining the interpretability and efficiency of sparse retrieval with the semantic generalization of neural models. We use the ensemble-distilled CoCondenser checkpoint , which initializes from CoCondenser and is trained on MS~MARCO with knowledge distillation from a cross-encoder ensemble. .} E5 is a dense bi-encoder model trained in two stages. First, a BERT encoder is contrastively pretrained on CCPairs, a curated collection of ${ }270$M text pairs mined from heterogeneous web sources . Second, the model is fine-tuned on labeled retrieval datasets including MS~MARCO. At inference, queries and documents are encoded independently into dense vectors using the shared encoder, and relevance is scored via cosine similarity. We use the base-sized checkpoint , which represents the state-of-the-art dense retrieval baseline in our comparison. E5 is the strongest supervised baseline on average and the only system that outperforms SIRA on any individual dataset . All agentic baselines use Qwen2.5-3B-Instruct as the backbone LLM and retrieve with BM25 over the same index used by the lexical baseline. .} HyDE prompts an instruction-following LLM to generate a hypothetical document that would answer the query. In its original formulation, this hypothetical document is encoded by an unsupervised dense encoder and used for nearest-neighbor search, forming a zero-shot dense retrieval pipeline requiring no relevance labels. In our setting, we adapt HyDE for sparse retrieval: the generated hypothetical document is used directly as the query for BM25 search. The intuition is that the hypothetical document contains vocabulary closer to the target document than the original query. However, because the LLM generates text unconstrained by corpus statistics, the hypothetical document often introduces terms absent from the target index, resulting in wasted query capacity and the lowest average performance among all methods in our comparison. .} We apply chain-of-thought prompting to query expansion: the LLM is instructed to reason step-by-step about what information is needed to answer the query and what terms a relevant document would contain. The reasoning chain is concatenated with the original query and used for BM25 retrieval. CoT produces more targeted expansions than HyDE because it reasons about the information need rather than generating a full hypothetical answer. However, like HyDE, the expanded terms are not grounded in corpus statistics and may include vocabulary with zero or near-zero IDF in the target index. CoT performs comparably to BM25 on average, with slight gains on some datasets offset by regressions on others. .} Search-R1 trains an LLM with reinforcement learning to interleave reasoning and search over multiple rounds. The model generates search queries enclosed in special tokens, retrieves documents from an external search engine, and reasons over the results before deciding whether to issue additional searches or produce a final answer. Training uses an outcome-based reward and applies loss masking over retrieved tokens to stabilize optimization. Unlike HyDE and CoT, which perform a single query expansion, Search-R1 can iteratively refine its search strategy based on intermediate results. Despite this multi-round capability, Search-R1 does not outperform BM25 on average in our retrieval-only evaluation. The RL training optimizes for end-to-end answer correctness rather than retrieval quality, so the learned query-reformulation policy does not consistently improve the relevance of retrieved documents. We use Search-R1's publicly available checkpoint and evaluate its retrieval component against the same metrics applied to other baselines. .} GrepRAG generates grep-like pattern queries using an LLM to perform retrieval without a vector database. The model produces regular-expression-style patterns optimized for code-search settings, where structured syntax and identifier naming conventions make pattern matching effective. In our evaluation, we adapt GrepRAG to natural-language document retrieval by having the LLM generate grep patterns for BM25 queries. GrepRAG achieves the second-lowest average Recall@$10$ among all methods: the pattern-matching idioms learned from code corpora transfer poorly to natural-language documents, where relevant vocabulary is less structured and more varied. .} ShellAgent uses an LLM to generate keyword-search-based retrieval queries, combining agentic tool use with iterative query refinement to achieve retrieval without vector databases. The original work demonstrates that LLM-generated keyword queries can approach RAG-level QA accuracy on targeted benchmarks. In our evaluation, ShellAgent achieves the lowest average Recall@$10$ across the ten BEIR datasets: the keyword-search retrieval patterns fail to cover the lexical diversity of heterogeneous IR corpora, where queries and documents span a much wider range of domains and vocabulary than the benchmarks the approach was designed for. After the weighted BM25 call , SIRA retrieves the top-$200$ candidates and re-ranks them via pointwise LLM relevance scoring. For each candidate document~$d_i$, the LLM receives the original query~$q$ and the full document text and outputs a single integer relevance score on a $0$--$100$ scale. The prompt defines five score bands that anchor the LLM's judgement to concrete relevance criteria ; task-specific variants adjust these criteria as summarized in~. The output is constrained to a JSON object } to enable deterministic parsing. Candidates are then sorted by descending score, and the top-$10$ are returned as the final result. This stage is optional: the retrieval-only pipeline already outperforms all baselines. Post-processing adds $9.4$pp average Recall@$10$ by applying fine-grained semantic assessment that BM25 scoring alone cannot capture, particularly on tasks where the enriched retrieval surfaces a broad candidate set containing both relevant and topically related but non-relevant documents. summarizes how each task-specific prompt adapts the default templates. All variants preserve the same output format and structural rules; only the task description and focus areas change. { }{@{}l l X X@{}} & & & \\ Factoid QA & NQ & Corpus: prioritize proper nouns, aliases, specific facts, category terms; avoid broad topic words. Query: explicitly forbid guessing answer entities ; expand with topic/domain vocabulary only. & Step-by-step: identify info type needed, scan for answer presence sentence-by-sentence; entity reference articles score ${ }41$. \\ Multi-hop QA & HotpotQA & Corpus: default. Query: distribute expansion terms across all entities and reasoning hops; forbid answer guessing. & Partial evidence highly valued ; documents covering one hop of a multi-hop query score high; entity articles score ${ }41$. \\ Claim verif. & FEVER, C-FEVER & Corpus: fact-checker vocabulary . Query: generate terms from the opening paragraph of each entity's reference article; ignore the claim predicate. & Entity-by-entity evaluation; contradicting evidence equally useful; reference articles for claim entities score ${ }41$. \\ Argument & ArguAna & Generate opposing-side vocabulary: counter-evidence terms, alternative frameworks, concessions. & Rate counter-argument quality; documents with the query score low . \\ Financial QA & FiQA & Prefer informal, practical terms and brand names over textbook jargon. & Informal language and personal anecdotes not penalized; practical tips valued over formal explanations. \\ Citation pred. & SciDocs & Cross-cutting terms: shared methods, problem formulations, datasets, application domains. & Rate citation relationship likelihood; cross-field methodological connections valued highly. \\ Duplicate det. & Quora, CQADup & Synonym substitutions preserving intent; no topic expansion beyond the original question scope. & Quora: rate semantic intent similarity. CQADupStack: strict duplicate detection; scope/constraint differences capped at $84$. \\ We evaluate SIRA in two stages. First, we test pure retrieval quality on ten BEIR benchmarks~, an unforgiving setting that removes answer generation and measures only whether a system can rank relevant evidence above corpus-level distractors. Second, we ask whether this retrieval advantage transfers to downstream question answering by measuring answer coverage against recent RL-trained search agents. We evaluate SIRA on ten BEIR datasets~ spanning seven retrieval task types: question answering , opinion retrieval , fact-checking , argument retrieval , citation prediction , and duplicate detection . summarizes corpus sizes and the number of relevant documents per query , covering a broad range of retrieval regimes. We report Recall@10 and NDCG@10. Recall@10 measures the fraction of relevant documents appearing in the top-10 results, directly capturing a system's ability to surface evidence for a downstream reader. NDCG@10 additionally rewards placing relevant documents at higher ranks, providing a complementary view of ranking quality. BEIR is a particularly stringent testbed for retrieval agents because it evaluates retrieval directly rather than end-to-end answer generation. Many recent agentic search systems focus on QA-style benchmarks where a reader model, prior knowledge, or multi-round interaction can partially mask weak retrieval. In contrast, BEIR exposes the core retrieval problem: given a fixed corpus and a query, can the system surface the relevant documents in the top ranks? This makes it the right setting for evaluating SIRA as a retrieval agent rather than as a general QA agent. denotes the average number of relevant documents per query.} {!}{ {4pt} {lccccl} & & & & & \\ NQ & Question Answering & 3,452 & 2.68M & 1.22 & Retrieval for real-world Google search questions. \\ HotpotQA & Question Answering & 7,405 & 5.23M & 2.00 & Multi-hop reasoning over Wikipedia paragraphs. \\ FIQA & Opinion Retrieval & 648 & 57K & 2.63 & Financial QA over StackExchange data. \\ ArguAna & Argument Retrieval & 1,401 & 8.67K & 1.00 & Matching counter-arguments for debate topics. \\ CQADupStack & Duplicate Question & 1,570 & 40K & 2.40 & Duplicate detection across StackExchange forums. \\ Quora & Duplicate Question & 10,000 & 523K & 1.57 & Duplicate detection for Quora questions. \\ SciDocs & Citation Prediction & 1,000 & 26K & 29.93 & Predicting citations for scientific papers. \\ FEVER & Fact-Checking & 6,666 & 5.42M & 1.19 & Verifying claims against Wikipedia text. \\ Climate-FEVER & Fact-Checking & 1,535 & 5.42M & 3.05 & Verifying climate change claims. \\ SciFact & Fact-Checking & 300 & 5K & 1.13 & Verifying scientific claims against abstracts. \\ } We compare against ten baselines spanning three retrieval paradigms. BM25~ is the standard sparse lexical baseline. Among neural methods, E5~ is a dense bi-encoder trained on large-scale relevance data; SPLADE~ and SPARTA~ are learned sparse retrieval models that preserve inverted-index efficiency while learning term importance weights; Doc2Query~ performs document expansion by predicting queries a document would answer. Among LLM-based methods, HyDE~ generates a hypothetical document as a BM25 query expansion; CoT~ uses chain-of-thought prompting to expand the query with reasoning-derived terms; Search-R1~ trains an RL policy for multi-round search; GrepRAG~ generates grep-like pattern queries originally designed for code retrieval; and ShellAgent~ relies on grep-based keyword-search tools within an agentic loop for multi-step retrieval. HyDE, CoT, GrepRAG, ShellAgent, and SIRA use the same frozen LLM ; Search-R1 uses its publicly available trained checkpoint with the most potent E5 backend. Neural baselines use official checkpoints . SIRA uses Qwen3.6-35B-A3B-FP8 as its frozen LLM for both corpus-side and query-side enrichment. In , we report Recall@10 and NDCG@10 across all ten benchmarks, comparing SIRA against sparse, dense, and agentic baselines. We structure the analysis around three research questions: : Can a training-free agentic system match or surpass trained sparse and dense retrievers? : Why do generic LLM search agents lag behind retrieval-native systems on BEIR, and does SIRA close this gap? : Does SIRA's retrieval quality translate to downstream QA performance competitive with RL-trained agentic QA systems? evaluates the central claim of this paper: a frozen LLM can turn BM25 from a lexical baseline into the strongest retriever in the comparison. SIRA uses the LLM not as a reader and not merely as a query-expansion module, but as a controller for the BM25 engine itself. It proposes missing vocabulary, grounds those proposals with corpus statistics, weights the surviving terms through BM25's IDF-sensitive scoring surface, and executes the result as a single ranked retrieval call. This comparison is deliberately strict. The baselines include supervised dense and sparse retrievers trained on large-scale relevance data, as well as LLM-based query-expansion and search-agent baselines. Thus, the key question is not whether LLMs can generate plausible search text or perform more search rounds, but whether they can produce a corpus-discriminative retrieval action that ranks gold evidence above confusers. ; second best .} {!}{ {3.5pt} {l cccccccccc c} & ArguAna & C-FEVER & CQADup & FEVER & FIQA & HotpotQA & NQ & Quora & SciDocs & SciFact & Avg \\ {l}{} \\ BM25 & .7738 & .1764 & .4163 & .6747 & .3198 & .6141 & .4543 & .9014 & .1636 & .8078 & .5302 \\ Doc2Query & .7824 & .1761 & .4339 & .6769 & .3397 & .6527 & .5068 & .8975 & .1663 & .8270 & .5459 \\ SPARTA & .6181 & .1050 & .3100 & .7246 & .2450 & .5356 & .5584 & .7445 & .1303 & .7084 & .4680 \\ SPLADE & & .2881 & .4924 & .8954 & .4139 & .7027 & .7381 & .9206 & .1654 & .8230 & .6253 \\ E5 & .7909 & .2899 & & & & & .7877 & & .1962 & & \\ HyDE & .7091 & .2598 & .3299 & .7132 & .2845 & .5051 & .4918 & .5151 & .1530 & .8344 & .4796 \\ CoT & .7752 & .1867 & .4020 & .6765 & .3086 & .5789 & .4961 & .8704 & .1595 & .7961 & .5250 \\ Search-R1 & .5760 & & .5008 & .9010 & .4499 & .6705 & & .9355 & & .8349 & .6161 \\ GrepRAG & .5746 & .0176 & .2400 & .1635 & .1009 & .3434 & .1628 & .5105 & .1027 & .5883 & .2804 \\ ShellAgent & .2263 & .0305 & .2084 & .2557 & .1035 & .4327 & .1884 & .3330 & .0843 & .6685 & .2531 \\ SIRA & & & & & & & & & & & \\ {l}{} \\ BM25 & .4874 & .1372 & .3481 & .5036 & .2532 & .5851 & .2916 & .8055 & .1565 & .6791 & .4247 \\ Doc2Query & .4946 & .1381 & .3667 & .5122 & .2731 & .6299 & .3302 & .7960 & .1589 & .6920 & .4392 \\ SPARTA & .3890 & .0852 & .2497 & .6101 & .1925 & .5132 & .3983 & .6294 & .1272 & .5894 & .3784 \\ SPLADE & .5253 & .2293 & .4083 & .7933 & .3478 & .6869 & .5369 & .8344 & .1586 & .7025 & .5223 \\ E5 & & & & & & & & & .1855 & & \\ HyDE & .4366 & .2004 & .2463 & .5507 & .2223 & .4451 & .3315 & .3924 & .1402 & .6565 & .3622 \\ CoT & .4951 & .1471 & .3354 & .4932 & .2486 & .5595 & .3168 & .7608 & .1518 & .6647 & .4173 \\ Search-R1 & .3658 & & .4040 & & .3765 & .6520 & .5790 & & & .7094 & .5216 \\ GrepRAG & .3555 & .0122 & .2048 & .0971 & .0763 & .2927 & .0908 & .4557 & .0921 & .4129 & .2090 \\ ShellAgent & .1114 & .0206 & .1558 & .1298 & .0686 & .3417 & .1036 & .2476 & .0727 & .4374 & .1689 \\ SIRA & & .2288 & & .8037 & & & & .8490 & & & \\ } SIRA achieves the highest average Recall@10 on BEIR, reaching 0.691 compared with 0.648 for E5, 0.625 for SPLADE, and 0.530 for BM25. It does so without relevance labels, without fine-tuning a retriever, and without building an embedding index. The advantage also holds for ranking quality: SIRA reaches 0.572 average NDCG@10, compared with 0.543 for E5 and 0.522 for SPLADE. This is the central result: a training-free retrieval agent built on BM25 can outperform supervised dense and learned sparse retrievers trained on large-scale relevance data. The gains are broad rather than driven by a single dataset. SIRA obtains the best Recall@10 on eight of ten benchmarks; the two exceptions are NQ, where Search-R1 is ahead by 0.06 percentage points, and Quora, where E5 is ahead by 0.4 percentage points. The largest improvements over E5 appear on datasets with structural query--document vocabulary gaps: +36\ The LLM-based baselines remain below SIRA on pure retrieval metrics, but Search-R1 clarifies an important distinction. HyDE and CoT remain close to BM25 on average, with Recall@10 of 0.480 and 0.525 compared with 0.530 for BM25. Search-R1 gains substantially from its E5 backend, reaching 0.616 Recall@10 and 0.522 NDCG@10, but still trails SIRA's 0.691 Recall@10 and 0.572 NDCG@10. Stronger backend retrieval clearly helps, but a multi-round search policy on top of that backend still does not close the gap to corpus-grounded BM25 control. The gap is even larger for grep-style tool-use agents. GrepRAG and ShellAgent use the same LLM backbone as SIRA, but treat retrieval as pattern generation rather than corpus-aware ranking, yielding average Recall@10 of only 0.280 and 0.253. Because the backbone is shared, the gap isolates the retrieval interface: grep-style agents search with patterns that lack BM25's document-frequency and IDF-weighted term scoring, while SIRA turns LLM proposals into weighted retrieval signals. SIRA therefore outperforms GrepRAG and ShellAgent by 41.0 and 43.8 absolute Recall@10 points. Recent LLM search agents also show how much performance depends on the retrieval backend: Search-R1 improves markedly when paired with E5, yet still remains below E5 on average Recall@10 and below SIRA on both Recall@10 and NDCG@10. This does not mean E5 reasons better; it means retrieval-native systems are optimized for the object BEIR measures: ranking relevant documents above corpus-level distractors. SIRA closes this gap by using the LLM to program the retrieval engine itself: proposed terms are grounded by corpus statistics, filtered for discriminative value, weighted through BM25's IDF-sensitive scoring surface, and executed in a single ranked retrieval call. Our goal is to test whether SIRA's retrieval advantage translates into downstream question answering. Since SIRA is a retrieval agent rather than an answer generator, the evaluation requires QA datasets with an associated retrieval corpus and gold evidence/answer annotations. Among the BEIR benchmarks, this leaves NQ and HotpotQA: both provide fixed corpora for retrieval and gold answer strings that allow us to measure whether retrieved documents contain the evidence needed to answer. This setting directly tests the claim that a better retrieval agent can become a better QA agent by supplying stronger evidence to the reader. It is also a difficult comparison for SIRA. The baselines are recent agentic QA systems designed for these tasks, and many are trained or reinforced on QA-style objectives closely aligned with NQ and HotpotQA. They report end-to-end generated-answer accuracy, so they may receive credit even when retrieval is incomplete, because the reader can use parametric knowledge or answer synthesis. In contrast, SIRA is counted correct only when the gold answer string appears in the retrieved text. We compare against six recent RL-trained agentic QA systems, including Search-R1~, TIPS~, A$^2$Search~, E-GRPO~, SSP~, and HiPRAG~. We take the best reported numbers directly from the original papers, giving each baseline the benefit of its full end-to-end QA pipeline. SIRA uses no reader model in this comparison. It also uses no RL fine-tuning, no task-specific supervised training, and no multi-round search; the same one-shot SIRA retriever used in the BEIR experiments is evaluated directly. SIRA is evaluated only by , the fraction of queries for which its retrieved documents contain verifiable answer evidence. {_knowledge/sira/analysis/plots/render_sira-vs-searchr1.pdf} Despite this disadvantaged evaluation, SIRA's retrieval-only answer coverage exceeds the reported end-to-end QA accuracy of all six agentic baselines at top-10. This comparison gives the baselines both their trained search policies and their answer generators, while SIRA contributes only retrieved text. As shown in , SIRA reaches 84.7\ The result is also strong at a tighter retrieval budget. At top-5, SIRA achieves 80.4\ SIRA assumes that the frozen LLM can understand the query and provide useful semantic priors about the target corpus. We have not evaluated settings where the corpus is far outside the LLM's pretraining distribution; in such domains, corpus-side adaptation or fine-tuning may be needed before the LLM can propose reliable enrichment terms. Information retrieval has evolved from lexical matching, exemplified by BM25~, to neural retrieval dominated by dense embeddings~. Embedding-based retrievers perform well when trained with abundant in-domain supervision: large-scale relevance labels and interaction logs that calibrate the model to a platform's user population~. This regime has fueled modern systems that ground LLM outputs in external corpora~. The user interface for information access is changing rapidly: search is increasingly and , with LLMs mediating multi-turn information seeking~. A key consequence is that the classic training signal for supervised ranking, clickthrough, becomes sparse, delayed, and biased: users often terminate sessions without clicking, or accept an on-page summary as the final answer. A large-scale browsing analysis by Pew Research Center finds that when Google presents an AI-generated summary, users click standard result links substantially less often~. Click-based supervision is therefore becoming unreliable at scale, precisely as query behavior is shifting. At the same time, are moving away from short keyword strings toward longer, compositional requests that combine constraints, exclusions, and multi-step intent. This shift is a hallmark of conversational search. Pure similarity search is an awkward fit for this regime: dense retrieval exposes only a black-box nearest-neighbor operator and provides weak handles for enforcing structure . Neural sparse methods such as SPLADE~ partially restore lexical controllability while preserving learning-based ranking, but they are still used as fixed retrievers inside pipelines rather than as controllable components of an agent policy. Classical lexical retrieval, exemplified by BM25, possesses underappreciated strengths that become decisive when paired with LLM reasoning. BM25 is : an agent can boost keywords, enforce constraints, and decompose queries with predictable effects on retrieval outcomes. It naturally rewards via IDF weighting, so domain-specific jargon that would be diluted in a dense embedding becomes a powerful retrieval signal. It is : one can trace exactly which keywords matched and why, while avoiding the latency and memory costs of dense indices. The missing ingredient has been a mechanism to surface the right rare terms and constraints; LLMs, with their vast parametric knowledge, are uniquely positioned to fill this role. The limitations of dense retrieval are not merely engineering inconveniences; they expose a deeper mismatch between single-vector representations and compositional information needs. Recent theoretical and empirical work shows that fixed-dimensional embeddings can realize only a limited family of relevance patterns~, that static embeddings are information bottlenecks whenever relevance requires cross-attention-style interaction~, and that vector databases impose substantial cost, latency, and objective-mismatch burdens in production~ In parallel, LLM reasoning frameworks such as Chain-of-Thought, Tree-of-Thoughts, and Graph-of-Thoughts demonstrate that LLMs can plan and explore structured intermediate states~. Tool-using agents extend this capability to external actions, including search~, and recent reinforcement-learning approaches train LLMs to interleave reasoning with multi-turn web search~. In most agentic search systems, however, retrieval itself remains an opaque tool: the agent can rewrite queries and judge snippets, but cannot directly manipulate retrieval primitives such as keyword weighting, constraint selection, or decomposition tied to index-time signals. This exposes why current search agents remain brittle despite strong reasoning capabilities. Agentic systems such as ReAct, IRCoT, and recent RL-trained search agents improve performance by interleaving reasoning with repeated retrieval calls . However, this success is partly obtained through a : after each search, the agent absorbs returned snippets, discovered entities, surface vocabulary, and near misses into the LLM context, then uses this accumulated evidence to formulate later queries. In other words, the agent compensates for weak retrieval control by learning the corpus through interaction. This strategy is expensive and noisy, and it relies increasingly on long-context LLMs to retain and use many intermediate passages---a regime known to be unreliable when relevant evidence is buried in long contexts . Thus the failure mode is not simply that LLMs cannot reason; it is that the search interface gives them too little direct control, forcing them into hit-and-miss exploration. Classical IR theory clarifies the missing ingredient. Retrieval is not just a question of whether a query is semantically related to the desired document; it is a comparative ranking problem in which the gold evidence must outrank many non-gold . The probability ranking principle and learning-to-rank methods formalize this as ordering relevant documents above non-relevant alternatives , while BM25 operationalizes corpus contrast through document frequency and IDF . A query can therefore be plausible in isolation yet fail because its terms also match many distractors, or because its most expert-sounding terms are absent, too common, or weakly discriminative in the target index. Dense single-vector retrieval adds a further bottleneck: recent theory shows that fixed-dimensional embedding retrievers cannot realize all top-$k$ relevance patterns and can fail even on simple realistic constraint structures . This points to the core gap SIRA addresses: an LLM may know what relevant evidence should look like, but it needs index-visible statistics and explicit retrieval controls to make that expectation discriminative against the confusing articles that corpus may be flooded with. SIRA formalizes the setting of one-shot, controllable BM25 retrieval across broad IR and QA benchmarks, building on recent evidence that LLM agents can generate discriminative keyword, grep, and ripgrep queries approaching RAG-level QA performance~. Existing results, however, are either practitioner-level, code-centric, or multi-turn and context-accumulating~; SIRA targets the stronger single-query regime. The central limitation of today's LLM-driven retrieval agents is that retrieval remains a they explore through repeated interaction. Existing agents issue a query, inspect returned snippets, and reformulate using accumulated evidence, a where later searches are conditioned on information exposed by earlier ones. This resembles a domain newcomer learning an unfamiliar database through exploration, rather than an expert who anticipates relevant terminology and evidence patterns before reading retrieved passages, as illustrated in . We define as the ability to replace this multi-round process with a single expert-level retrieval action: ~form a domain-informed expectation of what relevant evidence looks like, ~ground that expectation using lightweight index-aware signals , ~compile the result into explicit retrieval controls, and ~execute retrieval efficiently and transparently. {assets/comparison.pdf} ~Dense retrieval encodes queries and documents into a shared embedding space and performs nearest-neighbor search; the process is one-shot but opaque and requires in-domain supervision. ~Multi-step agent retrieval uses an LLM to iteratively formulate queries, read retrieved passages, and reformulate over $N$ rounds; later queries benefit from accumulated retrieval context. ~SIRA produces an expert-level retrieval action in a single shot: the LLM generates an expected-response sketch, validates proposed terms against corpus statistics, and compiles a controlled BM25 query with weighted keywords and constraints, all without reading any retrieved passages.} We propose the , a retrieval-centric agent that searches like a domain expert. Modern LLMs encode substantial parametric knowledge such as terminology, entities, relations, but lack a mechanism to convert this knowledge into precise retrieval actions over a target corpus. SIRA provides that mechanism through a scalable two-stage framework. First, the LLM produces an : a compact hypothesis of the concepts, entities, and discriminative terms likely to appear in relevant evidence. This sketch acts as a retrieval prior, not as evidence. Before issuing the final BM25 query, SIRA consults lightweight corpus-statistics tools to validate and prune proposed terms without returning answer passages, avoiding the retrieval-context advantage enjoyed by multi-round agents. Second, conditioned on the sketch and index statistics, SIRA compiles a : a single controlled BM25 query with weighted keywords, optional exclusions, and structured composition. We evaluate SIRA in this strict one-shot setting: one LLM reasoning step, optional index-statistic checks, and one BM25 call. Multiple queries could improve performance, but the one-query regime isolates the central question: how far can retrieval go when the agent must formulate the right lexical action without reading retrieved snippets? BM25's reliance on exact matching becomes a strength when the LLM supplies the right vocabulary and verifies its discriminative value through corpus statistics. Across BEIR-style IR evaluation~ and downstream QA, SIRA consistently outperforms strong dense retrievers, SPLADE, and state-of-the-art agentic baselines. These results suggest that the bottleneck in retrieval-augmented agents is the agent's ability to formulate expert-level retrieval actions without relying on accumulated retrieval context, not the sophistication of the retriever or the number of search iterations. BM25~ is the dominant lexical ranking function in modern search systems. It scores a document~$d$ against a query~$q = $ by summing per-term contributions: = _{i=1}^{n} {n + 0.5} )}_{ } \; \; {f + k_1 \! }, where $f $ is the frequency of term~$q_i$ in~$d$, $|d|$ is the document length in tokens, $$ is the average document length across the corpus, $N$ is the corpus size, and $n $ is the number of documents containing~$q_i$. The formula decomposes into two interpretable factors. We use the Lucene variant, which applies $ $ to the classical Robertson--Sp\"arck Jones ratio, guaranteeing non-negative IDF for all terms. The IDF term down-weights common words and up-weights rare, discriminative terms: a query term appearing in most documents contributes near-zero IDF, while one appearing in only a handful of documents receives a large weight. The TF saturation term ensures that repeating a word within a document yields diminishing returns, controlled by~$k_1$. The parameter~$b$ governs length normalization: at $b{=}1$ the score fully normalizes for document length, while at $b{=}0$ document length is ignored. BM25 is implemented over an inverted index, which maps each vocabulary term to the documents and term frequencies in which it appears. BM25 makes corpus contrast observable through the inverted index: before retrieval, an agent can check whether a candidate term appears in the corpus, how many documents contain it, and how much IDF weight it can contribute. These signals do not reveal answer passages, but they expose whether LLM-proposed vocabulary is absent, too common, or likely to create retrieval margin. SIRA uses this information to validate, prune, and weight expansion terms so the final query is not merely plausible in isolation, but discriminative within the target corpus. We introduced SIRA, a retrieval-centric agent that turns LLM reasoning into controllable lexical retrieval. Instead of using the LLM to repeatedly query a black-box search tool, SIRA uses it to program the retrieval action itself: enrich the corpus with missing user vocabulary, enrich the query with likely evidence vocabulary, validate proposed terms with corpus statistics, and execute a single weighted BM25 call. The main result is that this simple interface changes the role of BM25. Across ten BEIR benchmarks, SIRA achieves the highest average Recall@10 and NDCG@10 in our comparison, outperforming BM25, E5, SPLADE, and recent LLM-based search agents while using no relevance labels, no retriever fine-tuning, and no embedding index. Its gains are broad, with the best Recall@10 on eight of ten datasets and especially large improvements on tasks where query and document vocabularies diverge. The downstream QA results show that this retrieval advantage matters beyond BEIR. On NQ and HotpotQA, SIRA's retrieval-only answer coverage exceeds recent RL-trained agentic QA systems, even though those systems use trained search policies and answer generators while SIRA contributes only retrieved evidence. This supports a simple conclusion: for corpus-grounded QA, the ability to surface the right evidence can matter more than adding more search rounds. SIRA suggests a different path for retrieval-augmented agents. Rather than making agents search longer and accumulate more context, we can make the retrieval action itself more expert, corpus-aware, and interpretable. The remaining open question is how far this idea extends to corpora far outside the frozen LLM's knowledge, where corpus adaptation or fine-tuning may be needed before reliable enrichment is possible. Dense retrieval encodes queries and documents as single vectors and retrieves via approximate nearest neighbors~. These models achieve strong in-domain performance but depend on large-scale relevance supervision and degrade under domain shift, as BEIR evaluations consistently show~. Learned sparse methods such as SPLADE~ and SPARTA~ preserve inverted-index compatibility while learning term importance weights, but still require supervised training. Document expansion via Doc2Query~ enriches the index with predicted queries, a corpus-side strategy that SIRA generalizes by using an LLM to inject missing vocabulary rather than synthetic queries, and by adding a complementary query-side enrichment stage. SIRA requires no relevance labels, no fine-tuning, and no learned representations. HyDE~ generates a hypothetical document and retrieves its dense neighbors; Query2Doc~ appends LLM-generated text to the query. Both treat the retriever as a black box: the LLM cannot control term weights, enforce constraints, or observe index statistics. Multi-turn agentic systems extend LLM reasoning to iterative search: ReAct~ and WebGPT~ interleave reasoning with tool calls, while Search-R1~ trains an RL policy for multi-round web search. These agents gain a retrieval-context advantage but incur proportional latency and context cost. A parallel trend shows that LLM-generated keyword and grep queries can approach RAG-level QA accuracy without vector indices~, with practitioner-level demonstrations in code search~. SIRA targets the stronger single-query regime and exposes BM25 internals directly to the LLM, turning retrieval from an opaque API into a programmable component. Generative indexing stores the corpus parametrically: DSI~ and DSI++~ train a Transformer to map queries to document identifiers. While elegant, parametric indices complicate incremental updates and sacrifice the explicit token-level controls needed for auditable search. More fundamentally, recent theoretical work shows that fixed-dimensional single-vector retrieval can realize only a dimension-limited family of relevance patterns, and the LIMIT benchmark confirms that state-of-the-art embedding models fail on realistic queries designed to stress this bound~. An equivalent attention-based argument shows that any static query or document embedding is an information bottleneck whenever relevance requires cross-attention-style interaction~. A production-oriented critique reaches the same conclusion from cost, latency, privacy, and objective-mismatch perspectives~. SIRA sidesteps these representational limits entirely by operating in token space: the LLM reasons over terms and the retriever matches them exactly, so expressive capacity scales with vocabulary rather than embedding dimension. SIRA is, to our knowledge, the first system to combine four properties: ~two-sided vocabulary enrichment, where both the corpus index and each incoming query are expanded by an LLM; ~a document-frequency filter that provides a training-free guardrail against hallucinated or uninformative expansions; ~execution via a single weighted BM25 call whose scoring surface is fully transparent to the agent; and ~no trained retrieval model, relevance labels, or embedding index. Prior work addresses subsets of these goals: DocT5Query enriches the corpus only, HyDE enriches the query only, and multi-turn agents compensate for weak first queries with iterative refinement. None closes the vocabulary gap from both sides in a single, controllable, training-free retrieval action. Most retrieval-augmented agents interact with a search engine through a loop: issue a query, inspect results, reformulate, and repeat until useful evidence emerges. SIRA replaces this multi-round loop with a pipeline that bridges the vocabulary gap between queries and documents from simultaneously. The full system requires no training, no relevance labels, and no supervised query--document pairs; it operates with a frozen LLM and corpus statistics alone. {assets/diagram.pdf} Corpus-side enrichment is performed once offline; query-side enrichment runs per query. Both stages apply a DF filter to reject uninformative terms; the query-side filter additionally requires $ > 0$ to ensure each expansion term exists in the index. The original query $q_{}$ bypasses enrichment and is combined with the expansion terms in a single weighted BM25 call.} Given an unseen corpus, SIRA builds domain expertise from both the corpus side and the query side, as illustrated in . On the , the LLM reads each document, anticipates the search vocabulary a user would need to find it, and proposes candidate terms absent from the document text. A document-frequency filter validates each candidate against the corpus index, discarding uninformative terms whose frequency exceeds an upper bound . The surviving terms are injected into the BM25 index as atomic n-gram entries. On the , the LLM first produces an : a compact set of concepts, entities, and discriminative terms likely to appear in a relevant document but absent from the query. The same DF filter grounds the sketch in the enriched index. Conditioned on the validated sketch, SIRA then compiles a and executes a single weighted BM25 call : \;=\; \;+\; w , where $q_{}$ is the original query, $q_{}$ is the filtered expansion, and $w$ is the expansion weight. After the BM25 retrieval top-$k$ candidates are picked from the retrurned candidates based on relevance with the query. SIRA bridges the query--document vocabulary gap by enriching both sides with LLM-proposed terms. The goal of enrichment is not to add more text indiscriminately, but to surface compact lexical signals: concepts, aliases, and phrases that are likely to identify the desired evidence while remaining rare enough to distinguish it from the rest of the corpus. This requires grounding LLM proposals in corpus statistics. A term that sounds expert-like may be useless for query-side enrichment if it never appears in the enriched index, and weak if it appears across many unrelated documents. SIRA therefore treats document frequency and BM25/TF--IDF-style salience as lightweight corpus-statistic tools: they check query-side term existence, estimate whether proposed terms are too common to discriminate, and retain terms that can contribute meaningful retrieval margin. To ensure that only corpus-grounded and discriminative terms enter the retrieval pipeline, both enrichment stages share a . The filter enforces an upper bound $ |C|$, pruning terms that are repeated across too much of the corpus and therefore receive little useful IDF weight. For query-side enrichment, the filter additionally requires $ > 0$, ensuring that every expansion phrase actually exists in the enriched index and can affect BM25 scoring. Corpus-side enrichment does not require this lower bound, since enrichment itself introduces new vocabulary into the index. The result is a compact set of terms that are plausible under the LLM's domain knowledge and measurable as useful search signals in the target corpus. The goal is to anticipate how a user would search for a document when the vocabulary they would use is absent from the document text. The prompt explicitly instructs the LLM to generate terms not already present in the document, focusing on discriminative vocabulary: synonyms, abbreviations, alternate names, and domain-specific phrasings that maximize lexical contrast with existing index terms. Crucially, the prompt is task-aware: for claim-verification corpora, it emphasizes entity aliases and factual cues; for argument retrieval, it targets opposing-side vocabulary; for duplicate detection, it focuses on intent-preserving synonym substitutions. Phrases that pass the DF filter are decomposed into sliding-window n-grams and injected into the corpus index as additional posting-list entries. The goal is the mirror image of corpus-side enrichment: predict vocabulary that a relevant answer document would use but that is absent from the query. The prompt instructs the LLM to generate discriminative that narrows the search space, while explicitly forbidding it from guessing the answer itself. This distinction is critical for factoid queries, where predicting a named entity would bias retrieval toward a single candidate rather than broadening coverage of relevant evidence. As on the corpus side, the prompt is task-aware: factoid QA targets contextual terms surrounding the answer, multi-hop QA distributes expansion across all entities and reasoning hops, and duplicate detection focuses on intent-preserving synonym substitutions. 