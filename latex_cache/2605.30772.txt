 {0.8} {max width= } {lcccc} Dataset & \#Interactions & \#Items & \#Users & Avg Length \\ Games & 559,128 & 35,995 & 61,639 & 9.07 \\ Foods & 251,085 & 45,833 & 25,079 & 10.01 \\ Yelp & 214,722 & 5,927 & 17,272 & 12.43 \\ In this section, we conduct experiments to evaluate our method. Specifically, we are interested in answering the following research questions : How does our method compare against other coreset selection and dataset distillation approaches? How does each proposed components contribute to the final performance and method efficiency? Can our generated data be used to effectively and efficiently train other downstream backbones? How sensitive is our method to different hyperparameters? Due to page limit, we defer additional experimental results to the Appendix, including: further hyperparameter sensitivity analysis on numbers of sampled items and synthetic sequences ; further efficiency benchmarking for distillation and downstream training ; and synthetic data visualization . We evaluate on three datasets: Amazon Games, Amazon Foods , and Yelp~ . All datasets are split chronologically using an absolute time-based split with an 8-1-1 train-validation-test ratio. The pre-processing steps are specified in Appendix . For dataset distillation and coreset selection, we use 20 sequences for Amazon Games and Amazon Foods, and 60 sequences for Yelp. For all datasets, the maximum sequence length is set to 20, with sliding window pre-processing applied. Table show the dataset statistics. The efficiency experiment is conducted on a GPU workstation with an i7-13700K CPU, NVIDIA RTX A5000 GPU, and 32GB RAM. For the text encoder, we employ a pre-trained TinyBERT , and SASRec as the recommendation backbone. The model is optimized using AdamW with a learning rate of 2e-4. For synthetic and coreset datasets, a weight decay of 1e-4 is applied. The synthetic sequences are optimized with a learning rate of 1e-3 and weight decay of 1e-4. The sampled item set size per step $N$ is set to 512, reset interval $R$ is set to 100, and $ _r$ is set to $0.1$. Appendix provide more experiment details for baselines and datasets. Table shows the main experiment results. Our method achieves the best performance among all approaches and surpasses the full-dataset baseline on several metrics for Games and Foods, demonstrating that our distilled dataset can occasionally exceed full-data training -- likely due to the noise-reduction effect, a phenomenon also reported in prior work . Furthermore, our method exhibits a pronounced advantage at smaller $K$ values: gains over the full dataset are more evident at R@10/N@10 than at R@20/N@20. All coreset selection methods perform similarly and fall noticeably short of the Full dataset. Notably, Random selection remains competitive, consistent with observations in . TD3 achieves a meaningful improvement over coreset methods and approaches full dataset performance. {} {} Figure depicts the ablation study results for inner parameter reset logic. Without resetting the inner parameter, the performance drop drastically, demonstrating the necessity of periodically re-initializing $ $. Our proposed approach generally provide a better performance than only using random initialization. Table provides the ablation study results for various optimization approaches. With the first-order optimizer, when K-means is in use, instead of using on-the-fly sampling, we select $K$ items that are closest to the centroid, and keep this selection fixed through the data distillation process. While OTF incurs a small overhead, its performance is superior as the model can see the whole item set. Also, using all layers for data distillation demonstrates a clear performance advantage. It is worth noting that, when first-order optimizer is turned off in FOSTER, we substitute it with the bi-level BPTT approach. This bi-level approach is evaluated only under the last-layer setting as it requires higher compute resources. As can be seen from the results, FOSTER's efficiency enables it to scale to the all-layer setting, surpassing BPTT in both efficiency and effectiveness. We conduct transfer learning experiments to evaluate the generalizability of our distilled data across different model architectures. We employ TinyBERT as the backbone for data distillation, then transfer the distilled data to train a larger downstream model, Qwen3-4B, whose detail information is specified in Appendix . Table shows the experiment results. Across all three datasets, our method consistently outperforms Random by a large margin on both R@10 and N@10, while remaining close to full dataset performance. This demonstrates that the distilled data retains sufficient quality to generalize well even when the downstream model differs from the one used during distillation. In terms of training efficiency, our method is considerably faster than full dataset, completing in a fraction of the time. }{ } }{ } Figure shows results on Games and Foods datasets with different $ _r$. A small $ _r=0.05$ caused collapse on Games, while perform well on Foods. A large $ _r=0.2$ demonstrated the opposite results. The middle ground $ _r=0.1$ seems to provides the best balance, suggesting it effectively regularizes without over-constraining the optimization. Figure shows that the optimal reset step $R$ varies across datasets. On Foods, smaller values perform better, whereas on Games, larger values yield the highest Recall@10 and NDCG@10. This suggests that different datasets require different reset frequencies. Suppose $L_{} $ is continuously differentiable in both arguments, and $ ^* _{ } L_{} $. We define the constraint q L_{} - L_{} ). Then $q 0$ with equality iff $ _{ } L_{} $. The bi-level problem in Eq.~ is equivalent to _{, }\; L_{} q 0, which can be solved by first-order gradients alone. In this section, we will provide preliminary on text-based sequential recommendation and dataset distillation. After that, we provide the initial pipeline for sequential recommendation introduced by . Given user set $$ and item set $$, with each item $v_i$ is associated with a textual description $d_i$, such as title or item description, from which a dense embedding vector $e_i$ is extracted through a language model $l $, i.e. $_i=l $. For each user $u$, let $_u= $ denotes their chronological ordered interaction sequences. A sequential recommendation model $g_ $ consumes the sequence of item embeddings $ $ and produces a user representation $_u = g_ $. At inference, the relevance score between user $u$ and a candidate item $v$ is computed as the dot product $s = _u^ _v$, and the top-$K$ items are recommended accordingly. Both $l_ $ and $g_ $ are trained end-to-end by minimizing a binary cross-entropy loss over observed and sampled negative interactions, creating the final recommendation model $f=l g$. Given a large training dataset $ = \{_u\}_{u }$, dataset distillation aims to synthesize a compact synthetic dataset $$ consisting of $ $ sequences, each of maximum length $ $, such that $|| ||$, and a model trained on $$ achieves performance comparable to one trained on $$. Formally, this is defined as a bi-level optimization problem: &^* = _{} L_{} ,\, ) \\ & ^* = _{ }\, L_{}\! , where $L_{}$ is the recommendation loss function of the inner problem that learns SeqRec parameters $ $ on the synthetic data $$. $L_{}$ is the outer loss function that evaluates $ $ on the real data $$, so as to facilitate the optimization of $$. Since the exact solution $ ^* $ is intractable, Backpropagation Through Time approximates it by unrolling $T$ steps of gradient descent on the inner objective, and differentiating the outer loss through the entire unrolled computation graph. However, full unrolling with a fixed window suffers from short-horizon bias and unstable gradients. Random Truncated BPTT ~ mitigates this by sampling a random window to train the inner loop, and truncated the last $T$ step only. However, it is still subsumed under a bi-level optimization formulation, inheriting the high computational cost. Following~, we represent the synthetic dataset as a three-dimensional probabilistic tensor $ ^{ ||}$, where $_{ij:} ^{||}$ encodes a soft distribution over the entire item set for synthetic user $i$ at timestep $j$, allowing gradient-based optimization over the otherwise discrete interaction space. Directly optimizing $$ is intractable when $||$ is large, as the tensor size scales with $ ||$. To address this, $$ is parameterized via Tucker decomposition : = _1 _2 _3 =: ; , , , where $ _n$ indicates the tensor product along the n-th mode, $ ^{ d_1}$ is the synthetic user latent factor, $ ^{ d_2}$ is the temporal dynamics latent factor, $ ^{|| d_3}$ is the item factor shared with the pretrained item embedding table , and $ ^{d_1 d_2 d_3}$ is the relation core with $d_1, d_2, d_3 ||$. In , the learnable parameters are optimized with bi-level optimization, where the detailed training objective is discussed in Appendix . Although the number of learnable parameters reduces drastically, as we mentioned above, this approach requires to compute all item probability in all inner steps, limiting its scalability for text-based sequential recommendation with large item and token sets. In what follows, we present our solution FOSTER that addresses this efficiency challenge while preserving data distillation quality. Let $L_{} $ denote the inner loss on the synthetic dataset $S$ with model parameter $ $. The inner condition $ = ^* _{ '} L_{} $ enforces $ $ to be optimal on $S$. Let L_{}^* = _{ '} L_{} be the minimum achievable inner loss. Since $L_{} L_{}^* $ for any $ $, the inner optimality condition can be written as the constraint _{, }\; L_{} q 0, Because $q $ is always non-negative, this constraint is satisfied only when $ $ reaches the inner optimum on $S$. To optimize Eq.~, we need the constraint gradient _S q = _1 L_{} - _S L_{}^* , where the second term depends on the inner optimum $ ^* $. Directly computing this term would seem to require the implicit gradient $ _S ^* $. Next, we will show that this dependence vanishes under the inner first-order optimality condition. By the chain rule, _S L_{}^* &= _1 L_{} ) \\ &+ J_{ ^*} ^ _2 L_{} ), where $J_{ ^*} $ is the Jacobian of $ ^* $ with respect to $S$. Since $ ^* $ is an inner optimum, the first-order optimality condition gives \[ _2 L_{} )=0. \] Therefore, the second term vanishes, yielding \[ _S L_{}^* = _1 L_{} ). \] Thus, the value-function gradient does not require differentiating through $ ^* $, which is also a direct consequence of Danskin's theorem . Therefore, Eq. can be solved by first-order gradients alone. Sequential recommendation is the task of suggesting new, unvisited items based on a user's historical interaction sequence. Unlike traditional ID-based methods that directly map item IDs into embeddings for modeling interaction sequences, text-based sequential recommenders leverage language models such as BERT~ and LLMs~ to encode item textual metadata . With such semantic-aware representations, text-based sequential recommenders benefit from improved generalizability and cold-start robustness, positioning them as a strong direction for next-generation recommender systems~. However, these benefits often come with trade-offs in training efficiency, which is vital for recommendation models as they require periodic updates~. This is due to not only the increased data size as each item is now represented by a set of tokens instead of a single ID, but also the overhead from the additional text encoder -- commonly a heavy language model -- for generating item embeddings. A natural solution is to reduce the training data volume, where existing methods fall into two main categories. Coreset selection methods identify a small, representative subset of the original dataset, preserving real interactions that best capture the underlying data distribution . Dataset distillation, by contrast, generates a compact set of synthetic training samples that compress information learned from the entire dataset, resulting in a more information-dense dataset and superior model performance. These benefits have motivated recent efforts to extend dataset distillation to recommender systems, such as CGM~ and TD3~. Despite the promises, applying dataset distillation to text-based recommendation introduces significant challenges. First, existing methods like TD3 represent each synthetic interaction as a probability over the entire item space, instead of discrete items in real sequences. Though this ensures coverage of all items, replacing discrete items with dense distributions requires computing all item embeddings for every synthetic interaction. The computational cost is further amplified in text-based recommendation, where each item is represented by a collection of tokens instead of a single ID. This gives rise to the first challenge: Second, dataset distillation is typically formulated as a bi-level optimization problem that iteratively refines the synthetic data conditioned on a downstream model trained in the inner loop, therefore incurring high computational costs . Though recent advances in vision tasks introduce first-order alternatives~, these methods are constrained to a small and fixed label space when optimizing synthetic samples. This assumption breaks down in recommendation, where the prediction space corresponds to a large item catalog and each item may serve as a distinct target. Since existing first-order methods optimize separate synthetic samples for each class, their synthetic set size and computational cost scale linearly with the number of items, making them prohibitively expensive and unsuitable for recommendation. Therefore, the second challenge is: Third, recommender models commonly use tied embeddings, i.e., sharing the same item embedding space for input encoding and output prediction. This practice is theoretically justified only when the underlying data satisfies the distributional hypothesis, where semantically related items tend to appear in similar contexts~. However, existing recommendation dataset distillation methods overlook the associative patterns among co-occurring items when compress interactions, leading to loss of important semantics that are crucial to recommendation tasks. Thus, the third challenge is: Motivated by these challenges, we propose irst-rder dataet distillation for xt-based sequential ecommendation . Specifically, for , rather than involving all items at each training step, we instead sample a small random subset of items per batch and compute sequence probabilities directly from their embeddings via Tucker decomposition , significantly reducing the per-step computational cost. This is similar with negative sampling: rather than optimizing against the full item catalog at each step as in full softmax, we sample a random subset of items per step. To tackle , inspired by BOME , we propose a first-order optimization strategy for sequential recommendation, which jointly optimizes the inner and outer parameters. To keep the inner model well-behaved, we employ trajectory-anchored regularization that periodically reverts them to checkpoints sampled from the real training trajectory, preventing drift toward degenerate states unreachable by any realistic training run. Lastly, for , we introduce a regularization term that encourages the synthetic dataset to satisfy the distributional hypothesis, enabling more reliable dataset results. Figure shows the overview of our method and our contributions are as follows: We formalize the challenges of dataset distillation for text-based sequential recommendation, highlighting the key challenges: expensive full corpus calculation at each time-step, the bi-level trap for text-based sequential recommendation, and the misalignment in preserving the original items' co-occurrence distribution in synthetic datasets. We propose , an efficient dataset distillation framework for text-based sequential recommendation. It improves scalability through stochastic item subset sampling, stabilizes first-order bi-level optimization with trajectory-anchored regularization, and preserves item co-occurrence semantics under embedding tying. Extensive experiments on $3$ representative datasets, namely: Games, Foods , and Yelp, show that we can approximate the model performance using only 20 synthetic sequences for Games and Foods, and 60 sequences for Yelp. Furthermore, FOSTER is significantly more efficient and effective than existing recommendation dataset distillation methods. }{ {latex/figures/overview-v3.drawio.pdf} } }{ {latex/figures/BPTTvsBOMEv2.drawio.pdf} } $ , we distill it into a compact synthetic dataset $$ via stochastic item sampling, a single-level computational graph with inner/outer variable updates, and a regularization loss $L_{reg}$. Comparison of BOME and BPTT unrolling strategies, illustrating how BOME avoids full trajectory backpropagation by using constraint $q$.} {article} {acl} {fontenc} {algorithm2e} {xcolor} {inputenc} {} {} {Theorem} {Lemma} {Proposition} {Corollary} }, }, }, }, }, },\\ The University of Queensland, Griffith University, Amazon International Machine Learning, \\ @uq.edu.au; junliang.yu@griffith.edu.au; jul@amazon.com } } Text-based sequential recommender systems, while greatly improving recommendation accuracy by incorporating item contexts, are undeniably more expensive to train. By condensing a large dataset into a compact set of synthetic samples for model training, dataset distillation offers a promising solution. However, its adoption in text-based sequential recommendation is non-trivial given the large pool of discrete items. This challenge is further compounded by language model-based item encoding, which makes bi-level optimization commonly used in dataset distillation prohibitively expensive. To this end, we propose irst-rder dataet distillation for xt-based Sequential ecommendation , which facilitates effectiveness and efficiency via three novel components: stochastic item subset sampling that replaces costly full-corpus embedding extraction at each distillation step; first-order optimization with trajectory-anchored parameter reset to avoid expensive bi-level gradient computation; and regularization that explicitly promotes co-occurrence between semantically similar items in the synthetic sequences. Extensive experiments on three benchmarks show that FOSTER consistently outperforms existing dataset distillation and coreset selection baselines, approximating full-dataset performance using as few as 20 synthetic interaction sequences. *{Limitations} As shown in Fig. , FOSTER consists of three novel components. First, the stochastic item sampling significantly reduces memory usage and computational time at each iteration. Second, a first-order algorithm is designed to bypass the high computational overhead of bi-level optimization. Third, a regularization loss is in place to explicitly enforce distributional hypothesis, preserving items' co-occurrence semantics and uplifting the synthetic data utility. We discuss their details below. A critical scalability bottleneck in Tucker-based distillation in Eq. arises from the item dimension of $$, whose size grows linearly with $ $. In the inner loop, all item embeddings must be computed at every step, making distillation on large-catalog datasets prohibitively expensive. To address this, we propose stochastic item subset sampling, which approximates the full-item objective by uniformly drawing a subset of $N $ items at each step, which we empirically show that suffices to maintain distillation quality while reducing per-step complexity drastically. Formally, at each inner step, we sample a subset $_k $ of $N$ items and construct a sampled synthetic batch via: _k = ; , , _k , where $_k ^{N d}$ is the reduced version of the pretrained item embedding table $$ of $_k$. Crucially, we apply this approximation consistently across both optimization loops. Although the objective in Eq.~ has wide applicability, it is a bi-level optimization problem and typically requires differentiating through the inner training trajectory, leading to expensive second-order gradients. Inspired by~, we reformulate the bi-level problem as a single-level constrained optimization problem for SeqRec. The proof is provided in Appendix~. In practice, the exact $ ^* $ is unavailable. We approximate it by $ ^T$, obtained after $T$ gradient steps on the inner objective, and use q = L_{} - L_{} ) to approximate $q $, where $ $ denotes stop-gradient. This prevents back-propagation through the construction of $ ^T$ and removes the expensive second-order computation. To solve Eq.~, we employ a dynamic barrier update. At the $k$-th outer step, let $L_k=L_{} $ and $ q_k= q $. The update direction is _k &= L_k + _k _k, \\ _k &= \| \|^2, \\ _k &= , where $ =0.5$ controls the barrier strength. We iteratively update $ $ to reduce $L_{out}$, while decreasing $q$ to ensure the constraint was held. However, naively applying such update to dataset distillation leads to degenerate solutions. Since the synthetic dataset $$ and the model parameters $ $ are optimized jointly from scratch, $$ tends to overfit to the specific trajectory of $ $, resulting in a distilled dataset that fails to generalize to independently trained models. To demonstrate this, we calculate the average entropy of the distilled sequences with a fixed 512 synthetic items in Games dataset over the first 20 training epoch. As shown in Fig. , $$ quickly collapses onto a narrow subset of items, while $ $ gradually drifts away from the original parameter space. To address this issue, we avoid maintaining a single $ $ during optimization. Instead, we anchor the inner variable by sampling from a pre-recorded training trajectory. Let $=\{ _1, _2, , _M\}$ denote the anchor pool. After $R$ outer loops, we perform a reset step by sampling $ _k $ and setting $ _k _k$. Our approach differs from MTT~, which aim to match the original training trajectory, our method aim to improve the performance of the sampled checkpoint. And unlike naive joint optimization, $ _k$ is reset after a few outer iterations rather than inherited from the previous step. This constrains optimization to valid and diverse model states observed during real training, decouples the distilled data from a single co-evolving model, and encourages $$ to capture transferable recommendation patterns. { { latex/figures/entropy_evolution.pdf }} { { latex/figures/trajectory_pca_3d.pdf }} Recommendation models commonly adopt embedding tying, where the input and output sides share the same item embedding matrix. However, as shown by , this design is theoretically justified only when the distributional hypothesis holds. However, there is no guarantee that the distillation process naturally produces sequences that satisfy this property. Thus, synthetic data may distort item-level conditional distributions, making semantically similar items conditionally inconsistent. This causes DH to fail, so the tied embedding matrix receives conflicting optimization signals and leads to unstable training and degraded accuracy. To address this, we introduce a regularization loss $L_{r}$ that explicitly encourages the synthetic dataset to satisfy the distributional hypothesis. The core idea is to align two notions of item similarity -- and -- for any randomly sampled pair of items $ $. The first is the semantic distance $d_s $, where we use the fixed pretrained model $f$ as a proxy to judge whether $u$ and $v$ are semantically similar: if substituting one for the other at the same position in a context $p$ barely changes $f$'s output, the two items are treated as semantically equivalent. Formally, we calculated the semantic distance as: d_s = _{p , i } F^p_i - F^p_i where $p$ and $i$ is a random sequence and position from the synthetic dataset $$, respectively; $F^p_i $ is the estimated probability of $u$ given the context $p $. The $d_c $ measures how differently $u$ and $v$ are distributed within the synthetic sequences, and can be computed directly from the synthetic data distribution $P$. d_c = _{p , i } P_i^p - P_i^p , where $P_i^p $ is the conditional probability of $u$ at the $i$-th position given context $p$. The regularization loss is then defined as: L_{r} = _{u,v} - d_c ) ^2, where $u$ and $v$ are uniformly sampled from $$. By minimizing $L_{r}$, we encourage the semantically similar items to have similar distribution in the synthetic dataset. To instantiate our method for text-based sequential recommendation, we build upon the TD3 framework~ and add our regularization to the outer-loop objective $L$: L = L_D + L_{FM} + _{r} L_{r} where $L$ is the total outer loop loss; $ _$ is the model trained on the real data $$. $L_D$ is the binary cross-entropy loss, $L_{FM}$ is the feature matching loss as per , and $L_{r}$ and $ _{r}$ is the regularization loss and balance weight. The details about $L_D$, $L_{FM}$ and pseudo-code for the whole process are provided in Appendix and , respectively. Text-based recommender systems leverage pretrained language models to encode rich semantic information from item textual metadata, overcoming the cold-start limitations of ID-based approaches~. Notable works include RecFormer~, ONCE~, and others~, which demonstrate that modality-based recommenders are increasingly competitive with, and in some settings surpass, their ID-based counterparts. Our work builds on this paradigm and specifically targets the training efficiency bottleneck that arises when language models are involved in the training loop. For training efficiency, coreset selection methods identify a representative subset of real interactions to reduce training cost, such as DEALRec~, GORACS~, and NaCS~. Dataset distillation goes further by synthesizing compact artificial training samples, with recent works such as $ $-AE~, CGM~, TD3~, and DIET~ exploring this direction in recommendation. However, all of these methods employ expensive bi-level optimization, and only TD3 focused on sequential recommendation settings. There are other works that focuses on recommendation models training efficiency , but they are orthogonal to our works. Dataset distillation was first introduced in~ and has since been extensively studied in image classification, where approaches such as trajectory matching~ and distribution matching~ have achieved strong results. In NLP, various dataset distillation methods is also proposed. However, these methods either rely on a small, fixed class space , an assumption that fundamentally breaks down in recommendation, or rely on an expensive bi-level optimization . For further reading, we refer the reader to~.While our method demonstrates strong empirical results across multiple benchmarks, we identify two limitations that warrant further investigation. Our approach introduces several hyperparameters, including the number of inner steps $T$, the reset interval $R$, and the number of items sampled each step $N$. Although we provide sensitivity analyses in Sec. , selecting these values in practice requires validation effort. Developing more principled or adaptive strategies for hyperparameter selection remains an open direction. . There is still a small performance gap between full dataset and the synthetic dataset in Yelp dataset. Furthermore, while our distilled data transfers effectively to conventional sequential models, the gap between the full dataset and the synthetic dataset for a large language model on the Games and Yelp datasets are still large. We hypothesize this is partly due to the representational gap between the source backbone and the target LLM models, and regard closing this gap as a promising avenue for future work. In this paper, we propose FOSTER, namely first-order dataset distillation for text-based sequential recommendation. Our proposed approach includes three main components to further improve the efficiency and the performance. First, instead of using full item set for each timestep, we employ an on-the-fly approach, where we will randomly sample a smaller subset of items. Second, we employ BOME with periodical re-anchor to the inner parameters by the pre-train trajectory checkpoints, drastically reducing the compute resource requirement, while ensuring the synthetic dataset performance. Third, we introduce a regularization loss based on distributional hypothesis to further enhance the final results. Extensive experiments show that our method is not only more efficient but also more accurate than previous state-of-the-art approaches. The distillation is framed as a bi-level optimization problem where the inner loop trains the sequential encoder on the synthetic summary and the outer loop evaluates it on the real data. Concretely, the inner objective employs KL divergence between the model's predicted distribution and the synthetic soft targets, while the outer loop will optimize the actual objective -- improving the synthetic data $$. In this section, we will introduce about the optimization process of outer and inner loops. Following standard practice in sequential recommendation , we employ a binary cross-entropy formulation $L_{}$ over autoregressive next-item predictions as the recommendation model's training objective, given by: L_{} &= _{i=1}^N \\ &+ BCE ), where $}_i$ is the contextual representation produced at position $i$ used to predict the next item, $ , $ is dot product, $^{neg}_i$ is the negatively sampled samples at i-th timestep. extends the outer loop objective beyond naive performance matching by incorporating a feature space alignment term: } ^* &= _{} \; _{} + L_{FM} \\ L_{FM} &= {2} \| f - f \|_2^2, \\ ^* &= _ _{} where $L_{FM}$ is feature matching loss, $f $ and $f $ are the recommendation models trained on the real and synthetic data, respectively. This alignment loss encourages the synthetic-data-trained model to converge to a similar feature space solution as the real-data-trained model, promoting a more favorable loss landscape. The bilevel problem is solved via RaT-BPTT. In the inner loop, we first define: _i &= , _i = , where $ $ follows NumPy slice notation, $_i ^{ }$ is the input, $_i ^{ }$ denotes the synthetic target distribution. As $_i$ represents a probability distribution over items rather than a discrete item sequence, it cannot be directly processed by the recommendation model $f$. We therefore compute the embedding of $_i$ as a weighted average of all or sampled item embeddings $$, with weights given by $_i$: '_i = l = _i, , where $l$ is the language model, $$ is the embedding of all items. Thus, we can process $'_i$ normally by the recommendation model $g$, producing output representation $_i = g = f $. Then, the final output: } = ^ _i '_i Finally, we employ KL Divergence instead of BCE as the loss function for the inner training loop: L_{in} = D_{KL} . Using KL divergence is natural in this setting because each synthetic position is not assigned to a single discrete item; instead, it represents a soft supervisory signal that may place non-trivial probability mass on multiple semantically related items. Algorithm shows this process. We first sampled a smaller item set $_k$ . Then, we get the embedding $_k$ from $_k$ , note that $_k$ is a pre-computed embedding table using a pre-trained model, not the embedding table extracted from the currently in-training $l$. Then, we sample the input $_i$ and the target $_i$ . Then, we perform standard pipeline of forward , calculating loss and update model . , , $, embedding table $$, learning rate $ $, Real item list $$} {\# }{} $_k $ Random sample from $$\; $'_k $ Get embedding corresponding for $_k$\; $_i, _i , , , _k $\; $'_k l $\; $}_k g $\; $L $ Eq. \; $g,l g - _g L, l - _lL$\; $, learning rate $ $, inner parameter reset intervals $R$, number of inner loop steps $T$} {\# }{} Prepare step} Train $ $ on $$ to get pre-train trajectory\; $ $ Encode all real items $_$\; Main dataset distillation step} Initialize $$ \; Iteration $i 0$\; { { $ $ Sample from the pre-train trajectory\; } )} $ ^* $ Train $ $ on $$ for T steps\; $q L_S - L_S $\; $ _k $ Eq. \; $L $ Eq. \; $ - $\; $i i + 1$\; } Algorithm shows the pseudo-code for the proposed algorithm. First, we need to train the model on the real dataset . This prepare step is common in various dataset distillation approaches, such as MTT and TD3 . Then, we encode item embedding to get the item embedding used in calculating $$. Then, we can start to distill the synthetic dataset. At each iteration, we first reset $ $ every $R$ steps from pre-trained trajectory; . Then, we train $ $ on $$ for T steps to estimate $ _{}$ , detailed in Alg. . Note that in these T steps, we will sample a different item set for each step. After that, we compute constraint $q $ ; and update $ $ via the first-order gradient step . The process is iterated until convergence. In this section, we provide details on baselines used in the main experiments. randomly samples a subset of users and uses their interaction as the coreset. represents each user by their last-timestep embedding and applies the greedy $k$-center algorithm to select the subset. ~ scores each training sample using an effort score derived from a pre-trained model, pruning low-effort and redundant sequences. ~ selects coresets by bounding the test loss via optimal transport distance and gradient information, avoiding repeated model retraining during selection. ~ generates a compact set of pseudo-sequences via Tucker decomposition to match the distributional statistics of the full training set. For TD3, due to the high computational cost, we only unfreeze the last layer during the inner loop training. We source all datasets from RecBole and apply standard pre-processing steps. Interaction records are first sorted chronologically by timestamp, then filtered using the 5-core setting . For Yelp, we additionally restrict items to those located in Nashville city prior to applying the 5-core filter. During training, we apply a sliding window strategy to generate additional samples from long interaction sequences , while at evaluation time each sequence is truncated to the last 20 interactions. The inner steps $T$ is searched in $\{40, 50, 60\}$. The model and synthetic data is selected with the best results in validation dataset in R@10. We calculate metrics using all items. Follow , we employ LLM directly as a text encoder. We apply LoRA with rank 32 and alpha 128, yielding a scaling factor of 4 to the last two transformer layers , with dropout of 0.1. All of the other layers are frozen. To speed up the training process, we adopt a caching strategy wherein we pre-compute and store the hidden states from the first layers for all item embeddings. Figure shows the experiment results on how the number of sequences affects the downstream model performance. { } Figure shows the experiment results on how the number of sampled items per step $N$ affects the downstream model performance. Table~ reports per-epoch runtime and peak GPU memory on Foods. TD3 immediately exceeds the 24GB VRAM budget, confirming that naive bi-level optimization does not scale to large catalogues. Reducing to a 512-item subset via stochastic sampling yields the largest gains: FOSTER requires only 1,296MB and 0.58 minutes per epoch --- a $25.46 $ memory reduction over TD3. Even under all-layer fine-tuning, FOSTER remains substantially cheaper than TD3 , demonstrating that first-order optimization is critical for tractable training under consumer-grade GPU constraints. {max width= } {0.8} {l|ccc|ccc} Backbone & {c|}{Full} & {c}{Ours} \\ {2-4} {5-7} & R@10 & N@10 & Time & R@10 & N@10 & Time \\ TinyBERT & 0.0228 & 0.0108 & 22.2m & 0.0292 & 0.0148 & 3.7m \\ Qwen3-0.6B & 0.0292 & 0.0148 & 7.5h & 0.0368 & 0.0179 & 54.2m \\ Qwen3-4B & 0.0391 & 0.0182 & 13.4h & 0.0435 & 0.0208 & 1.2h \\ Table shows the training efficiency on Foods dataset. Our method successfully not only approximate the performance of the full training, but also drastically reduce the required training time. {latex/figures/item_popularity_overlay.pdf} In this section, we further analyze the lower results on Yelp. Figure illustrates the normalized item popularity distributions across three datasets. All three datasets exhibit a power-law decay, where a small fraction of items account for the majority of interactions. However, the datasets differ substantially in catalog size and tail behavior. Both Foods and Games maintain extended long tails spanning over 30,000 items. In contrast, Yelp has a substantially smaller catalog with a steeper decay and an abrupt tail cutoff. Because Yelp's naturally short and sparse tail provides almost no gradient signal for tail items during optimization process, the synthetic sequences collapse toward the popular items. We calculate the probabilities of all items, and for each user, we visualized the top 10 items that has highest probabilities. {latex/figures/data_vis/synth_data_games_top10.pdf} {latex/figures/data_vis/synth_data_foods_top10.pdf} = [ ybar, bar width=5pt, width=0.5 , height=3cm, enlarge x limits=0.6, ylabel={NDCG@10}, symbolic x coords={Foods, Games}, xtick=data, ymin=0, ymax=0.03, yticklabel style={/pgf/number format/fixed, /pgf/number format/precision=2}, tick align=inside, grid=major, grid style={dashed, gray!30}, ] coordinates { }; coordinates { }; coordinates { }; = [ ybar, bar width=5pt, width=0.5 , height=3cm, enlarge x limits=0.6, ylabel={Recall@10}, symbolic x coords={Foods, Games}, xtick=data, ymin=0, ymax=0.055, yticklabel style={/pgf/number format/fixed, /pgf/number format/precision=2}, legend style={ font= , draw=none, fill=none, at={ }, legend columns=3, /tikz/every even column/.append style={column sep=0.4cm}, }, legend cell align={left}, tick align=inside, grid=major, grid style={dashed, gray!30}, legend to name=lambda_r ] coordinates { }; coordinates { }; coordinates { }; = [ ybar, bar width=5pt, width=0.5 , height=3cm, enlarge x limits=0.6, ylabel={NDCG@10}, symbolic x coords={Foods, Games}, xtick=data, ymin=0, ymax=0.03, yticklabel style={/pgf/number format/fixed, /pgf/number format/precision=2}, tick align=inside, grid=major, grid style={dashed, gray!30}, ] coordinates { }; coordinates { }; coordinates { }; = [ ybar, bar width=5pt, width=0.5 , height=3cm, enlarge x limits=0.6, ylabel={Recall@10}, symbolic x coords={Foods, Games}, xtick=data, ymin=0, ymax=0.055, yticklabel style={/pgf/number format/fixed, /pgf/number format/precision=2}, legend style={ font= , draw=none, fill=none, at={ }, legend columns=3, /tikz/every even column/.append style={column sep=0.4cm}, }, legend cell align={left}, tick align=inside, grid=major, grid style={dashed, gray!30}, legend to name=reset_step ] coordinates { }; coordinates { }; coordinates { }; = [ ybar, bar width=5pt, width=0.5 , height=3cm, enlarge x limits=0.6, ylabel={NDCG@10}, symbolic x coords={Foods, Games}, xtick=data, ymin=0, ymax=0.03, yticklabel style={/pgf/number format/fixed, /pgf/number format/precision=2}, tick align=inside, grid=major, grid style={dashed, gray!30}, ] coordinates { }; coordinates { }; coordinates { }; = [ ybar, bar width=5pt, width=0.5 , height=3cm, enlarge x limits=0.6, ylabel={Recall@10}, symbolic x coords={Foods, Games}, xtick=data, ymin=0, ymax=0.055, yticklabel style={/pgf/number format/fixed, /pgf/number format/precision=2}, legend style={ font= , draw=none, fill=none, at={ }, legend columns=3, /tikz/every even column/.append style={column sep=0.4cm}, }, legend cell align={left}, tick align=inside, grid=major, grid style={dashed, gray!30}, legend to name=item_k ] coordinates { }; coordinates { }; coordinates { }; [ ybar, bar width=6pt, width=0.27 , height=3cm, symbolic x coords={Foods, Games}, xtick=data, enlarge x limits=0.5, ymin=0, ymax=0.048, ylabel={Recall@10}, ymajorgrids, grid style={dashed, black!15}, tick label style={font= }, label style={font= }, legend style={ font= , draw=none, fill=none, at={ }, anchor=south, legend columns=3, /tikz/every even column/.append style={column sep=0.4cm}, }, legend to name=legend_v2 ] coordinates { }; coordinates { }; coordinates { }; {max width= } {0.8} {cccc ccc ccc} First- & {*}{Item} & {*}{Layers} & {*}{$L_r$} & {c}{Games} & {c}{Foods} \\ {5-7} {8-10} order& & & & R@10 & N@10 & Time & R@10 & N@10 & Time \\ & OTF & Last & & 0.0332 & 0.0181 & 9.03 & 0.0266 & 0.0126 & 4.77 \\ & OTF & Last & & 0.0310 & 0.0202 & 1.43 & 0.0252 & 0.0115 & 0.58 \\ & KM & All & & 0.0376 & 0.0222 & 2.25 & 0.0278 & 0.0133 & 0.98 \\ & OTF & All & & 0.0373 & 0.0223 & 2.33 & 0.0239 & 0.0114 & 0.95 \\ & OTF & All & & 0.0386 & 0.0217 & 2.42 & 0.0292 & 0.0148 & 1.02 \\ [ ybar, bar width=6pt, width=0.27 , height=3cm, symbolic x coords={Foods, Games}, xtick=data, enlarge x limits=0.5, ymin=0, ymax=0.027, ylabel={NDCG@10}, ymajorgrids, grid style={dashed, black!15}, tick label style={font= }, label style={font= }, legend style={ font= , draw=none, fill=none, at={ }, anchor=south, legend columns=3, /tikz/every even column/.append style={column sep=0.4cm}, }, ] coordinates { }; coordinates { }; coordinates { }; {max width= } {0.8} {l ccc ccc ccc} Method & {c}{Games} & {c}{Foods} & {c}{Yelp} \\ {2-4} {5-7} {8-10} & R@10 & N@10 & Time & R@10 & N@10 & Time & R@10 & N@10 & Time \\ Full & 0.0516 & 0.0284 & 21.25h & 0.0456 & 0.0209 & 13.37h & 0.0355 & 0.0195 & 5.77h \\ Ours & 0.0449 & 0.0252 & 1.55h & 0.0435 & 0.0208 & 1.23h & 0.0314 & 0.0160 & 1.33h \\ Random & 0.0229 & 0.0112 & 40.7m & 0.0129 & 0.0059 & 28.5m & 0.0207 & 0.0128 & 42.87m\\ [ group style={ group size=2 by 1, horizontal sep=1.8cm, }, width=0.52 , height=0.42 , grid=both, grid style={line width=0.3pt, draw=gray!30}, major grid style={line width=0.4pt, draw=gray!50}, tick align=outside, tick pos=left, label style={font= }, tick label style={font= }, legend style={ font= , at={ }, anchor=south, legend columns=2, /tikz/every even column/.append style={column sep=0.3cm}, draw=none, fill=none, }, xlabel={$ $}, ] [ xtick={5, 10, 20}, xticklabels={5, 10, 20}, xmin=3, xmax=22, ylabel={Score}, legend to name=sharedlegend, title={ Games}, legend entries={R@10, N@10, Full R@10, Full N@10}, ] [ color=blue!70, mark=*, mark size=2.5pt, line width=1.2pt, ] coordinates { }; [ color=orange!90!black, mark=square, mark size=2.5pt, line width=1.2pt, ] coordinates { }; [ xtick={20, 40, 60}, xticklabels={20, 40, 60}, xmin=14, xmax=66, title={ Yelp}, ] [ color=blue!70, mark=*, mark size=2.5pt, line width=1.2pt, ] coordinates { }; [ color=orange!90!black, mark=square, mark size=2.5pt, line width=1.2pt, ] coordinates { }; at !0.5! + $) {}; and the second best one is . } {max width= } {0.8} {ll|cccc|cccc|cccc} {c|}{{*}{Method}} & {c|}{Games} & {c|}{Foods} & {c}{Yelp} \\ {3-6} {7-10} {11-14} & & R@10 & N@10 & R@20 & N@20 & R@10 & N@10 & R@20 & N@20 & R@5 & N@5 & R@10 & N@10 \\ {c|}{Full} & 0.0350 & 0.0178 & 0.0564 & 0.0232 & 0.0228 & 0.0108 & 0.0368 & 0.0143 & 0.0213 & 0.0131 & 0.0390 & 0.0188 \\ {48pt}{Coreset Selection} & Random & 0.0317 & & 0.0388 & 0.0217 & 0.0210 & 0.0097 & 0.0295 & 0.0119 & 0.0133 & 0.0086 & 0.0208 & 0.0110 \\ & KCenter & 0.0311 & 0.0197 & 0.0380 & 0.0215 & 0.0203 & 0.0095 & 0.0292 & 0.0117 & 0.0108 & & 0.0133 & 0.0101 \\ & DEALRec & 0.0313 & & 0.0382 & 0.0216 & 0.0202 & 0.0094 & 0.0287 & 0.0115 & 0.0094 & 0.0055 & 0.0152 & 0.0074 \\ & GORACS & 0.0311 & 0.0195 & 0.0384 & 0.0214 & 0.0204 & 0.0094 & 0.0292 & 0.0116 & 0.0122 & 0.0086 & 0.0176 & 0.0103 \\ {c}{Data} & TD3 & & 0.0185 & & & & & & & & & & \\ Distillation & FOSTER & & & & & & & & & & & & \\ {max width= } {3pt} {0.8} {llcrr} Method & \#Items & Layers & Time & Mem \\ TD3 & All & Last & OOM & 33,002 \\ FOSTER & All & Last & 10.43 & 20,654 \\ TD3 & 512 & Last & 2.20 & 5,004 \\ FOSTER & 512 & Last & 0.58 & 1,296 \\ TD3 & 512 & All & 5.07 & 19,368 \\ FOSTER & 512 & All & 0.98 & 5,198 \\ 