 {IEEEtran} {inputenc} {fontenc} , plaindiam/.style={ diamond, draw=black, fill=white, text=black, font= , aspect=2.0, minimum width=2.4cm, minimum height=0.7cm, align=center, inner sep=1pt }, greenbox/.style={ rectangle, draw=black, fill=green!25, text=black, font= , minimum width=3.2cm, minimum height=0.55cm, align=center, inner sep=3pt }, orangebox/.style={ rectangle, draw=black, fill=orange!25, text=black, font= , minimum width=3.2cm, minimum height=0.55cm, align=center, inner sep=3pt }, arr/.style={-Stealth, black, line width=0.5pt}, } =10000 =10000 =4pt =3em =2000 \\ harish.saragadam@vodafoneidea.com} \\ sudhanshu.sharma@vodafoneidea.com} \\ meghana.pujari1@vodafoneidea.com} } Getting accurate, grounded answers out of large enterprise document repositories is a difficult problem. Dense vector retrieval alone frequently performs poorly on queries that mix technical terminology, vendor-specific acronyms, or require reasoning across several non-adjacent sections. was built to address exactly this gap --- an offline, multi-agent document intelligence system developed and evaluated in a production telecom network operations environment. Rather than relying on a single retrieval signal, DocuSearch pulls together three complementary sources of evidence: semantic search over a Qdrant vector store using BGE-Large embeddings, BM25 full-text search over an SQLite FTS5 index, and Knowledge Graph neighbour expansion from a structured edge table. These three ranked lists are merged through Reciprocal Rank Fusion with signal weights $w_v = 0.50$, $w_{b} = 0.35$, and $w_{kg} = 0.15$, giving: \[ s_{} \;=\; _{i \{v,\,b,\,kg\}} {k + r_i }, k = 60 \] where $r_i $ is the rank of chunk $d$ in signal list $i$. A cross-encoder then reranks the fused list, and Maximal Marginal Relevance with $ = 0.65$ prunes it for both relevance and diversity. What makes DocuSearch distinctive is a loop that treats each selected chunk as its own mini-retrieval problem: an LLM decides whether the chunk needs additional neighbouring context, whether it is actually sufficient to answer the query, and whether the answer it generates is genuinely grounded in the retrieved text. Ungrounded answers are not returned; the system falls back to a multi-chunk merge instead. On an internal telecom document corpus, DocuSearch reaches Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\ points over a dense-only RAG baseline. retrieval-augmented generation, knowledge graph, reciprocal rank fusion, enterprise document search, agentic evaluation, BM25, cross-encoder reranking, on-premise deployment, LangGraph, telecom AI In telecom network operations centres, engineers frequently must search through hundreds of vendor manuals for a specific configuration procedure under time pressure. These environments accumulate vast repositories of technical documentation --- SOPs, RCA reports, audit records, multi-vendor configuration guides --- and the gap between what keyword search surfaces and what an engineer actually needs can be substantial. Conventional search returns too many loosely related hits. Standard single-signal Retrieval-Augmented Generation ~, while an improvement, still struggles with specialised technical vocabulary, fragmented document chunks, and the reality that relevant information is often spread across non-adjacent sections from different vendors. This paper describes , a hybrid multi-agent RAG system designed and deployed within a real telecom network operations environment. Three limitations motivated its design: , where a single retrieval function misses chunks that are relevant in semantic, lexical, and structural senses simultaneously; , where a retrieved chunk is only half of a procedure; and , where a generated answer sounds plausible but is not actually supported by the retrieved evidence. DocuSearch addresses all three by combining dense vector retrieval, BM25 sparse retrieval , and Knowledge Graph expansion~ into a single fused ranking via Reciprocal Rank Fusion , followed by cross-encoder reranking, Maximal Marginal Relevance selection , and --- the primary novel contribution --- a that verifies context sufficiency and answer grounding before committing to a response. The pipeline runs entirely on locally hosted models and is orchestrated via LangGraph , exposed through an interactive Dash web application with vendor-scoped retrieval, session document upload, and integrated Knowledge and Learning Hub modules. The main contributions are as follows: A three-signal hybrid retrieval architecture fused via weighted RRF with signal weights $w_v{=}0.50,\; w_b{=}0.35,\; w_{kg}{=}0.15$. A per-chunk agentic evaluation loop with LLM-driven context need detection, sufficiency scoring, and groundedness verification. Vendor-aware retrieval scoping for multi-vendor enterprise document corpora. A production-ready LangGraph-orchestrated pipeline with interactive Dash UI and fully local LLM inference. The remainder of this paper is organised as follows. Section~ formalises the problem, Section~ describes the proposed solution, Section~ presents the system architecture, Section~ details the methodology, Section~ elaborates novel contributions, Section~ covers implementation, Section~ presents results, and Section~ concludes. Let $ = \{d_1, d_2, , d_N\}$ be a large heterogeneous corpus of enterprise documents, where each document $d_i$ belongs to one or more vendor namespaces $ = \{v_1, v_2, , v_M\}$. Each document is pre-processed into overlapping text chunks: \[ = _{i=1}^{N} \{ c_{i,1},\, c_{i,2},\, ,\, c_{i,k_i} \} \] where $c_{i,j}$ is the $j$-th chunk of $d_i$ and $k_i$ is the total chunk count for that document. Given a natural language query $q$, the goal is to retrieve a subset $ $ and generate an answer $a$ such that: \[ a = \! \] where $$ synthesises the answer strictly from retrieved evidence, without hallucinating content that does not appear in $$. This deceptively simple formulation hides four concrete sub-problems that standard pipelines fail to handle: No single retrieval function $f: q $ can capture all relevant chunks, because relevance spans semantic, lexical, and structural dimensions at the same time. A chunk $c_{i,j}$ retrieved in isolation is often incomplete when the relevant procedure spans $c_{i,j-1}$ through $c_{i,j+1}$. Naive top-$k$ selection tends to return clusters of near-duplicate chunks, squandering context window capacity on repeated information. Standard RAG hands retrieved chunks to $$ without ever checking whether the generated answer $a$ is genuinely supported by $$, which is a direct path to confident but wrong responses. DocuSearch addresses all four within a unified pipeline that runs entirely on locally hosted models with no dependency on external API services. DocuSearch tackles the four sub-problems through a layered multi-agent pipeline split into two phases, illustrated in Figure~ and Figure~. When a query $q$ arrives, a Template Selection agent first classifies it into one of five answer templates $$. A BM25 Keyword agent then pulls out domain-specific terms, and a Vendor Detection agent optionally narrows all three retrieval signals to a specific vendor namespace $v $. Dense vector search, BM25 full-text search, and KG neighbour expansion run in parallel, and their ranked results are merged by weighted RRF, reranked by a cross-encoder, and filtered by MMR to produce the evidence set $$. Each chunk $c $ is evaluated individually by four sequential LLM-driven nodes: context need detection, sufficiency scoring against a threshold $ = 7$, answer generation, and groundedness verification. The first chunk that clears all four checks produces the final answer. When no individual chunk passes, a fallback merges all expanded contexts, generates a consolidated answer, and runs one final groundedness check before returning anything to the user. The system is organised across six functional tiers spanning the two phases shown in Figures~ and~. [ node distance=0.45cm, every node/.style={font= } ] {Step 1\ Selection}; {Step 2\ 25 Keyword Extraction}; {Step 2b\ Detection}; [plainbox, minimum width=1.7cm, below left=0.6cm and 0.95cm of s2b] {Step 3\ Search}; [plainbox, minimum width=1.7cm, below=0.6cm of s2b] {Step 4\ 25 Retrieval}; [plainbox, minimum width=1.7cm, below right=0.6cm and 0.95cm of s2b] {Step 5\ Expansion}; {Step 5b\ Fusion}; {Step 6\ -Encoder Re-ranking}; {Step 7\ Selection}; {Ordered MMR chunk list}; -- ; -- ; -- ++ -| ; -- ; -- ++ -| ; -- ++ -| ; -- ; -- ++ -| ; -- ; -- ; -- ; : Step 1 --- Template Selection ; Step 2 --- BM25 Keyword Extraction ; Step 2b --- Vendor Detection ; Steps 3/4/5 --- Parallel Vector Search, BM25 Retrieval, KG Expansion; Step 5b --- RRF Fusion ; Step 6 --- Cross-Encoder Reranking; Step 7 --- MMR Selection . Output: ordered MMR chunks passed to Post-MMR phase. } [ node distance=0.45cm, every node/.style={font= } ] {Step 2 \ Chunk Assessment}; {Needs more\ ?}; [greenbox, minimum width=2.2cm, right=1.1cm of d1] {Fetch Neighbouring\ }; {Sufficient?}; [plaindiam, minimum width=2.0cm, left=1.0cm of d2] {More chunks?}; {Step 4\ Answer Generation}; {Step 5\ Verification}; {Grounded?}; {TERMINATE\\{ method = per\_chunk}}; {Fallback\ All Expanded Contexts}; {Fallback Step 4\ Multi-chunk Answer}; {Fallback Step 5\ Check}; {Return Answer\\{ method = fallback\_merge}}; -- ; -- node {Yes} ; -- ++ -| -- ; -- node {No} ; -- node {No} ; -- ++ -- ++ |- ; -- ++ |- ; -- node {Yes} ; -- ; -- ; -- ++ node {No} -- ++ -| ; -- node {Yes} ; -- ; -- ; -- ; -- ; : Per-chunk loop --- LLM evaluates current chunk; if more context needed, neighbouring chunks are fetched ; sufficiency check gates answer generation; groundedness LLM verifies the generated answer; if grounded, pipeline terminates ; otherwise retries next MMR chunk. When all MMR chunks are exhausted, fallback merges all expanded contexts and returns a consolidated answer . } The handles parsing and chunking, producing overlapping segments ($s = 900$ chars, $ = 140$ chars) stored in both Qdrant~ and SQLite FTS5. The runs three parallel agents , each optimised for a different aspect of relevance. Fusion, reranking , and evidence selection happen in the . The then runs four sequential LLM checks on each candidate chunk, which accounts for most of the observed quality gains. Above all of this sits the , a LangGraph DAG with five nodes: , , , , and . Users interact through the , a Plotly Dash application that exposes a conversational search interface alongside real-time pipeline visualisation, a Knowledge Hub, a Learning Hub, and document ingestion panels. Before any retrieval happens, every query $q$ goes through template classification and keyword extraction. The Template Selection agent maps $q$ to one of five types in $ = \{$, , , , $\}$ using an LLM with a deterministic heuristic as fallback when the model is uncertain. Accurate template classification is important because it shapes the structure of the generated answer downstream. The BM25 Keyword agent then extracts domain-specific terms from a curated telecom vocabulary covering standard networking and protocol constructs, supplemented with query-specific expansions. Finally, the Vendor Detection agent combines LLM classification with regex matching to identify vendor references, scoping all retrieval signals to the detected vendor's documents when applicable. The query $q$ is encoded using BGE-Large-EN-v1.5 to get $ ^{1024}$. Cosine similarity is computed against all chunk embeddings in Qdrant, returning the top $k_v = 40$ chunks: \[ _v = _c} {\|\| \|_c\|} \] Extracted keywords query the SQLite FTS5 index. Standard BM25 scoring applies: \[ _b = _{q_i Q} {f + k_1(1 - b + b {avgdl})} \] with $k_1 = 1.2$, $b = 0.75$, where $|c|$ is chunk length and $avgdl$ is the average chunk length across $$. Top $k_b = 40$ chunks are retrieved. BM25 is particularly useful here because technical acronyms and model numbers that have no meaningful semantic neighbourhood still produce strong lexical matches. Seed chunks from the vector and BM25 results traverse a KG edge table stored in SQLite, pulling in structurally connected chunks . This catches content that is topically related but lexically and semantically distant --- for instance, a configuration snippet that references a procedure described in a different section. The three ranked lists are fused following : \[ s_{} = _{i \{v,\, b,\, kg\}} {k + r_i } \] \[ w_v{=}0.50, w_b{=}0.35, w_{kg}{=}0.15, k{=}60 \] Chunks that surface in multiple lists receive additive contributions, naturally elevating content that is relevant across different notions of similarity. The smoothing constant $k = 60$ prevents very high-ranked items from dominating the fused score entirely. The top $k_r = 12$ fused chunks are reranked with a cross-encoder that jointly encodes the query and each chunk: \[ s_{} = 0.65 s_{} + 0.35 s_{} \] The cross-encoder score $s_{} $ is weighted higher because it captures fine-grained query-chunk interactions that bi-encoder cosine similarity cannot. Retaining the RRF component prevents the cross-encoder from completely discarding evidence that multiple retrieval signals agreed on. The reranked list is filtered iteratively by MMR until $|| = 10$: \[ c^* = _{c R S} [ s_{} - _{c' S} ] \] where $S$ is the growing set of already-selected chunks, $R$ is the remaining pool, and $0.65$ favours relevance slightly over diversity. In practice this eliminates the clusters of near-duplicate chunks that would otherwise fill the context window. Each $c $ passes four sequential LLM-driven checks, as shown in Figure~: The LLM assesses whether $c$ is a fragment that requires neighbours $c_{}$ or $c_{}$ to make sense. If so, neighbours are fetched and appended, for up to $L = 2$ iterations. The expanded chunk $$ proceeds only if the LLM assigns a sufficiency score $ = 7$ on a $1$--$10$ scale. Chunks that do not clear this bar are skipped immediately. Given $$, the query $q$, and the selected template $t^*$, the LLM generates candidate answer $$ at temperature $T = 0.0$ for reproducibility. A separate LLM call checks whether $$ is fully supported by $$. If it is, $a$ is set to $$ and the pipeline terminates . When no chunk clears all four checks, the fallback merges all expanded contexts into $}$, generates a consolidated answer , and runs one final groundedness check before anything is returned to the user. Five aspects of DocuSearch distinguish it from existing RAG systems. Most RAG pipelines concatenate all retrieved chunks and generate one answer in a single forward pass. DocuSearch instead evaluates each $c $ independently through four sequential checks. A chunk must score $ = 7$ before generation is even attempted, and the returned answer is explicitly verified as grounded before being committed. This eliminates a whole class of hallucinations that arise when an LLM is asked to synthesise an answer from a mixed bag of partially relevant context. Most hybrid RAG systems stop at two signals --- typically dense plus sparse. A third, KG neighbour expansion, is added as an explicit signal in the RRF formula, with empirically tuned weights $w_v = 0.50$, $w_b = 0.35$, $w_{kg} = 0.15$. The KG signal's contribution is modest but consistent: it reliably surfaces structural neighbours that neither cosine similarity nor BM25 would rank highly on their own. Rather than using larger fixed chunk sizes at indexing time (which reduces retrieval precision), DocuSearch detects fragmentation at query time. When a retrieved chunk is identified as a fragment, up to $L = 2$ levels of neighbouring chunks are fetched from the metadata database on the fly, recovering coherent multi-step procedures without any pre-processing trade-offs. A vendor registry is built from the document metadata that maps each $v $ to its set of document identifiers. When the Vendor Detection agent identifies a vendor reference in the query, all three retrieval signals are scoped to that vendor's documents. The noise reduction for vendor-specific configuration and troubleshooting queries is substantial. The entire pipeline runs as a stateful LangGraph DAG with five explicitly defined nodes. Every intermediate state is observable, serialisable, and independently replaceable --- which turned out to be practically important during development, since individual components could be swapped without disturbing anything upstream or downstream. DocuSearch is implemented entirely in Python~3 and served from a single application. Table~ summarises the core technology stack. {1.2} {ll} & \\ Vector Store & Qdrant \\ Embedding & BGE-Large-EN-v1.5 \\ Reranker & Cross-Encoder \\ FTS Index & SQLite FTS5 \\ Metadata Store & SQLite \\ LLM Inference & Local REST or vLLM \\ LLM Model & Mistral \\ Pipeline Graph & LangGraph \\ UI Framework & Plotly Dash \\ Doc Parsing & Docling, PyMuPDF, python-docx, openpyxl \\ Auto Ingest & watchdog directory monitor \\ Documents reach the system through three paths: server-side path submission, browser upload via Dash , or automatic detection through a directory monitor. All formats are parsed and chunked with $s = 900$ characters and $ = 140$ characters overlap. These values were settled on after experimenting with several chunk sizes; smaller chunks hurt sufficiency scoring while larger ones degraded retrieval precision. Table~ summarises the full retrieval configuration used across all experiments. {1.2} {lll} & & \\ Vector top-$k$ & $k_v$ & 40 \\ BM25 top-$k$ & $k_b$ & 40 \\ KG top-$k$ & $k_{kg}$ & 15 \\ Rerank top-$k$ & $k_r$ & 12 \\ Final evidence & $||$ & 10 \\ RRF smoothing & $k$ & 60 \\ RRF vector weight & $w_v$ & 0.50 \\ RRF BM25 weight & $w_b$ & 0.35 \\ RRF KG weight & $w_{kg}$ & 0.15 \\ MMR lambda & $ $ & 0.65 \\ Sufficiency thresh. & $ $ & 7 \\ Max neighbour loops & $L$ & 2 \\ Chunk size & $s$ & 900 chars \\ Chunk overlap & $ $ & 140 chars \\ LLM temperature & $T$ & 0.0 \\ Session-only uploads are also supported: documents uploaded during a session are embedded in memory using a local NumPy array without being written to the persistent knowledge base. All LLM calls use $T = 0.0$ to keep outputs deterministic across repeated queries, which is important for reproducibility in an operational setting. All evaluations were run on an enterprise telecom corpus covering documentation from multiple major equipment vendors --- configuration manuals, SOPs, RCA reports, and audit records. The diversity of this corpus makes it a reasonable stress test for any retrieval system. Table~ compares Precision@$k$ and Recall@$k$ across three configurations on $120$ manually annotated queries spanning all five template categories. {1.2} {lcccc} & & & & \\ Dense only & 0.61 & 0.48 & 0.54 & 0.63 \\ Dense + BM25 & 0.68 & 0.56 & 0.61 & 0.71 \\ DocuSearch & & & & \\ The full three-signal system outperforms both baselines across every metric. The most pronounced gain is in Recall@10, where KG expansion consistently recovers structurally related chunks that neither dense nor BM25 retrieval surfaces on its own --- particularly for troubleshooting queries where the relevant fix appears in a section referenced by, but not adjacent to, the initial symptom description. Table~ compares the per-chunk evaluation loop against a conventional single-pass RAG baseline. {1.2} {lccc} & & & \\ Single-pass RAG & 71.2\ DocuSearch & Mean end-to-end latency across $200$ queries is $12.6$ seconds, with the per-chunk evaluation phase accounting for $8.4$ seconds of that. Retrieval itself finishes in under one second thanks to Qdrant ANN search and SQLite FTS5. For a document intelligence application where an engineer is waiting for a reliable answer rather than streaming tokens, this trade-off is considered acceptable. That said, reducing evaluation latency is an obvious target for future work. Table~ shows grounding rate when each component is removed individually, holding everything else constant. {1.2} {lc} & \\ Full DocuSearch & 89.6\ Without groundedness check & 74.3\ Without sufficiency scoring & 78.1\ Without neighbour expansion & 82.4\ Without cross-encoder reranking & 83.7\ Without KG expansion & 85.2\ Without vendor scoping & 86.1\ Groundedness verification has the largest individual impact at $15.3$ percentage points, followed by sufficiency scoring at $11.5$ points. It is worth noting that even the ``smallest'' contributors --- vendor scoping at $3.5$ points and KG expansion at $4.4$ points --- add up meaningfully in combination, indicating that no component is redundant. This paper presented DocuSearch, a hybrid multi-agent RAG system built for enterprise document intelligence in multi-vendor telecom environments. The system was designed around three failure modes observed in production: signal incompleteness, context fragmentation, and unverified answer grounding. The two-phase pipeline --- hybrid retrieval fusion followed by per-chunk agentic evaluation --- directly addresses all three. Results show Precision@10 of $0.69$, Recall@10 of $0.79$, and a grounding rate of $89.6\ hallucinations dropping to $6.8\ that groundedness verification and sufficiency scoring drive the largest improvements, though no single component is redundant. A key practical finding is that production-quality document intelligence is achievable entirely on locally hosted models. DocuSearch requires no external API calls, which matters a great deal in telecom operations environments where sensitive network configuration data cannot leave the organisation's infrastructure. The sequential LLM calls in the per-chunk evaluation phase are the main latency bottleneck, currently averaging $8.4$ seconds. Parallelising chunk evaluation across inference threads is a natural first step. The RRF signal weights were determined empirically on telecom documentation and may not transfer well to other domains; learning them from query-relevance feedback via a differentiable ranking objective would make the system more broadly applicable. There is also potential in using named entity recognition to populate the KG with typed entity-relation triples, which would strengthen multi-hop query handling. Finally, extending DocuSearch to multimodal retrieval --- covering embedded diagrams and specification tables that are pervasive in vendor documentation --- is a compelling direction worth pursuing. One promising future direction is treating DocuSearch's multi-step retrieval workflow as a sequential decision problem. The intuition comes from Karpathy's interactive GridWorld temporal-difference learning demo~, where an agent learns to navigate a grid by trial-and-error using TD updates --- without any hard-coded path rules. The analogy to the pipeline is direct: just as that agent discovers which moves lead to reward by accumulating Q-values over many episodes, a retrieval agent in DocuSearch could learn to invoke BM25, to expand KG neighbours, and to stop --- adapting dynamically to each incoming query rather than applying the same fixed configuration every time. Formally, each query-answer interaction can be modelled as an episode in a Markov Decision Process $ = (, , , , )$. The state $s $ encodes the current query intent, the set of chunks retrieved so far, reranker confidence scores, KG expansion status, and evidence coverage. The action space $$ includes discrete choices such as triggering a BM25 search, expanding KG neighbours by one hop, tightening the cross-encoder top-$k$, fetching a neighbouring chunk, or terminating retrieval and proceeding to answer generation. The reward $$ is shaped by answer groundedness (which is already measured at inference time), engineer satisfaction feedback collected through the Dash UI, citation accuracy, and a latency penalty to discourage unnecessarily long retrieval chains: \[ = + - _l \] To give this direction a concrete anchor, a composite system accuracy $ $ is introduced that jointly captures retrieval quality and answer fidelity: \[ = 0.30 P@10 \;+\; 0.30 R@10 \;+\; 0.40 \] Under the current rule-based configuration, plugging in the reported numbers gives: \[ _{} = 0.30 0.69 + 0.30 0.79 + 0.40 0.896 80.2\ \] An offline-trained Q-Learning policy, optimised on logged production trajectories, is projected to lift $P@10$ to approximately $0.76$ , $R@10$ to approximately $0.86$ , and the grounding rate to approximately $94.5\ . These are deliberately conservative estimates: because DocuSearch already incorporates cross-encoder reranking, MMR selection, and a per-chunk sufficiency gate, the remaining headroom for any single optimisation layer is bounded. What the RL agent would learn to do differently from the current fixed policy is route precision-heavy queries through BM25 rather than relying solely on dense retrieval, expand KG neighbours more aggressively on recall-sensitive troubleshooting queries, and halt retrieval earlier when accumulated evidence already clears the sufficiency threshold --- decisions that are currently hard-coded but could be learned from query-level feedback. Under these assumptions: \[ _{} = 0.30 0.76 + 0.30 0.86 + 0.40 0.945 86.4\ \] This represents an overall improvement of approximately $$ percentage points, lifting composite accuracy from $ 80\ Table~ summarises the projected gains per metric. {1.2} {lccc} & & & \\ $P@10$ & 0.69 & 0.76 & $+7$\,pp \\ $R@10$ & 0.79 & 0.86 & $+7$\,pp \\ Grounding & 89.6\ $ $ & 80.2\ Off-policy Q-Learning is a natural fit here. Document retrieval is largely deterministic --- the same action in the same state reliably produces the same next chunk set --- so off-policy updates converge faster than on-policy SARSA in this setting, mirroring the behaviour Karpathy's demo exhibits in deterministic grid environments. In practice, this involves logging state-action-reward trajectories from production interactions, training an offline policy on these traces, and shadow-testing it against the current rule-based pipeline before any live deployment. Over time, this would allow DocuSearch to move from hand-tuned hyperparameters towards a system that continuously improves from operational feedback --- a meaningful step toward truly adaptive enterprise document intelligence. {99} P. Lewis et al., ``Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks,'' in , vol.~33, pp.~9459--9474, 2020. S. Robertson and H. Zaragoza, ``The Probabilistic Relevance Framework: BM25 and Beyond,'' , vol.~3, no.~4, pp.~333--389, 2009. J. Carbonell and J. Goldstein, ``The Use of MMR, Diversity-Based Reranking for Reordering Documents and Producing Summaries,'' in , pp.~335--336, 1998. S. Xiao et al., ``C-Pack: Packaged Resources to Advance General Chinese Embedding,'' , 2023. LangChain AI, ``LangGraph: Building Stateful, Multi-Actor Applications with LLMs,'' , 2024. Qdrant Team, ``Qdrant: Vector Database for the Next Generation of AI Applications,'' , 2023. G. V. Cormack, C. L. A. Clarke, and S. Buettcher, ``Reciprocal Rank Fusion Outperforms Condorcet and Individual Rank Learning Methods,'' in , pp.~758--759, 2009. N. Reimers and I. Gurevych, ``Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks,'' in , pp.~3982--3992, 2019. M. Edge et al., ``From Local to Global: A Graph RAG Approach to Query-Focused Summarization,'' , 2024. A. Q. Jiang et al., ``Mistral 7B,'' , 2023. A. Karpathy, ``REINFORCEjs: Gridworld with Temporal Difference Learning,'' , 2015. 