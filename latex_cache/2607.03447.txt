We presented , a stage-aware instrumentation framework that instruments automated, document-grounded graph-RAG pipelines end to end, with construction-stage metrics that apply to any extracted KG, automated or not. Informed by the engineering methodology~ and adapted to the automated setting, attaches stage-specific, independently interpretable confidence metrics to three phases without requiring a single composite score and without relying on gold annotations at deployment time. Its central question is deliberately twofold: not only whether the graph underpinning retrieval can be trusted, but at what retrieval and computational cost it can be used. Three ideas distinguish beyond the metric definitions themselves. First, it organizes metrics by two computability axes, the external reference each needs and the visibility of the extraction , making explicit which signals survive in gold-free, schema-free, or black-box settings and showing that the core assessment degrades gracefully rather than failing when conditions are not ideal; the gold-requiring metrics sit apart as offline calibration yardsticks rather than deployment signals. Second, it separates primary signals from derived ones through a dependency analysis, yielding a minimal set worth monitoring. Third, and most distinctively, it shows that the usage-stage metrics form a diagnostic chain whose first broken link localizes a failure, and that this localization maps to the stage levers that can remedy it: extraction, graph-and-schema, or retrieval. Targeting the update to the diagnosed stage is what separates from a lifecycle that triggers an undifferentiated update; the relative cost of the levers is deployment-specific and left to empirical measurement . A usage-stage proof of concept gives preliminary, single-probe evidence for this diagnostic claim: a capable reader can answer from its parametric knowledge when retrieval has missed the needed evidence, so retrieval adequacy must be scored before and independently of the answer . covers the of for one KG version. Metric breaches, such as a rising or low aggregate / over a query class, or rising , provide principled triggers for a new cycle, targeted at the diagnosed stage. This closes the loop with the and points toward continuous, monitored KG evolution in operational deployments. Several limitations should be acknowledged. First, validation is still partial: a usage-stage proof of concept exercises the predictive claim, but the full suite, the remediation map, and the cross-stage links are specified rather than tested at scale; gives the protocol for the remaining validation. Second, relies on LLM token log-probabilities as a confidence proxy, which are known to be imperfectly calibrated for factual correctness~; a miscalibrated may flag correct triples or pass incorrect ones. Third, the expert step is specified methodologically but its cost and reliability at scale remain open. Fourth, the schema-dependent metrics assume an available ontology; without one, assessment relies on the schema-free subset, as makes explicit. Fifth, uses lexical relation matching, which may miss paraphrased or implicit relational claims. Sixth, the separation of primary from derived metrics is definitional rather than statistical: genuine non-redundancy can only be established empirically , and some primaries may prove correlated in practice. Seventh, the usage-stage links are necessary but not sufficient conditions: a structural signal such as cannot distinguish a correct connecting path from a spurious one, so a satisfied chain certifies retrievability, not answer correctness. Eighth, the usage metrics depend on reliable query- and answer-entity extraction, so errors in $M_q$, $M_a$, or their grounding into $V_q^g$ and $V_a^g$ propagate into , , , , , and , and the decision thresholds $ $ estimated on a benchmark may not transfer unchanged to deployment. Beyond empirical validation, opens several directions: ~extending to multi-hop, multi-entity query decomposition; ~automating triggers from metric thresholds over a rolling query window, with the diagnosis selecting the levers; ~formalizing the expert step with active learning to reduce annotation cost; ~replacing lexical with an NLI-based relational-faithfulness check; and ~surfacing the stage-localized metrics in a per-phase diagnostic interface that maps metric drifts onto the remediation map, enabling continuous, cost-aware KG health monitoring in operational deployments.This section makes one empirical point and frames the rest. Because 's deployed instrument is gold-free, its evaluation is a question of resting on three claims: that the suite is gold-free on real graphs, of downstream failure, and of where a failure originates. Two of these we establish here, on the metrics. we show by construction, computing the full gold-free usage suite across five retrievers and $480$ query-by-retriever observations on a real KG with no gold annotation at scoring time . The proof of concept provides preliminary evidence of predictive validity: the structural metrics indicate whether a faithful answer’s evidence is present in the retrieved subgraph, when the reader may often produce an answer that correct from parametric memory, although retrieval has failed. In that case end-answer accuracy would certify a graph our metrics correctly flag as deficient; this silent-success residual is a concrete case for 's design, scoring each stage gold-free and the answer rather than trusting end-to-end accuracy. The claim and threshold calibration need larger, more varied benchmarks; rather than run them here we specify them as a falsification protocol that a later instantiation can execute to test, not assume, them. The metrics, which need a document-grounded corpus, are left to that protocol's extensions. We instantiate the usage stage on ~ over its 3-hop knowledge base . Because natural PathQuestion items name a single topic entity, leaving and saturated at $1$, we build a : 108 synthetic questions naming two or more KB entities at controlled distances. Each category stresses one link of the diagnostic chain: off-topic questions contain only non-KB entities, semi questions contain one KB entity and one fictional entity, pair-k questions contain two entities at undirected distance k = 1,...,4, disc questions contain two entities in different components, and triple/quad questions contain three or four entities on a connected path. The 108 questions are designed for diagnostic coverage rather than as a natural query distribution. The 12 off-topic questions test the out-of-scope link of the chain, but they are excluded from the predictive-validity analysis because their gold answers are undefined with respect to the KG. The predictive analysis therefore uses 96 distinct questions. Since each question is run through five retrievers, the pooled analysis contains 480 question-by-retriever observations. These 480 observations should not be read as 480 independent questions; they are five retrieval views of the same 96-question probe. Entities are sampled at the target structure, verbalized into a fixed template , and re-extracted into $M_q$ and grounded into $V_q^g$ by the deployment pipeline; answers come from an LLM reader . The structural metrics need no gold answer, and the probe is reproducible from the KB alone. We do not evaluate these retrievers as systems in their own right. Instead, we use five retrieval configurations as a representative diagnostic testbed: they induce different coverage, connectivity, and cost profiles, allowing us to test whether the proposed structural metrics track evidence availability. Each configuration is a lightweight re-implementation following an existing approach’s core principle rather than an exact reproduction. The first four share a common $n$-hop beam expansion and differ only in their seed entities: , , and, following LightRAG's dual-level retrieval~, and . The fifth, , follows PathRAG~: it forgoes beam expansion and extracts flow-pruned relational paths between anchors. On the PQ-3H probe set we ask whether the structural metrics are associated with the framework’s two gold-anchored outcome metrics . For each question-retriever pair, we store the retrieved subgraph and the generated answer once. We then compute CC and Hits@1 offline from these stored outputs, without rerunning retrieval or generation: we pool all five retrieval configurations into one population of 480 question-by-retriever observations , and for each we score two outcomes against the probe’s gold answers: Context Coverage and Answer Correctness . The predictors ERC and RRS are computed without gold annotations; gold answers are used only afterwards to compute CC and Hits@1 as validation outcomes. We then stratify the population by entity coverage and by connectivity . reports both outcomes per stratum. Consider a query ``How are \ and \ related?'', whose gold answer requires an intermediate entity \ . A retriever may return both query entities \ and \ , giving \ , but fail to retrieve a connecting path between them, giving \ . If \ is absent from the retrieved subgraph, then \ , because the evidence needed for a faithful answer is missing. A strong reader may nevertheless answer with \ from parametric memory, yielding \ . In such a case, answer correctness marks the run as successful, whereas the structural metrics flag that the retrieved evidence was insufficient. } Reader-free rises with and jumps sharply once retrieval is complete, from $0$) covers the gold answer only $ Reader Answer Correctness tracks the same metrics but far more weakly: it climbs only from $, here measured directly: a correct answer co-occurs with broken retrieval, so end-answer accuracy alone would certify a graph the structural metrics correctly flag as deficient. This is the concrete motivation for scoring retrieval adequacy and the answer. It also bounds what this probe can show: because the reader floor is high, a clean Hits@1 threshold needs harder questions and a weaker-prior reader, and a calibrated $ $ with significance testing is deferred to the larger benchmarks of the protocol below. Two construct caveats accompany this probe. The predictor and outcome are disjoint by construction: scores grounded query mentions while scores entities, so the separation is not tautological, though the same extraction-and-linking pipeline builds $M_q$, $V_q^g$, and the retrieval, which can inflate independently of graph quality . The threshold $ _{ }$ is fit in-sample on these $480$ pooled, non-independent observations , so it is descriptive rather than a validated operating point, with calibrated thresholds and clustered confidence intervals deferred to the protocol below. {=}0.67$. Boldface marks the values discussed in the text. The table is descriptive: confidence intervals are not reported here because the pooled observations are not independent, with each question appearing under five retrievers; calibrated clustered uncertainty estimates are deferred to the full protocol in Section~.} {6pt} {lccc} Stratum & $n$ & (\ {l}{ } \\ $ <0.5$ & 70 & & \\ $0.5 <1$ & 165 & & 84.2 \\ $ =1$ & 245 & & \\ {l}{ } \\ $ =0$ & 261 & & 82.8 \\ $ >0$ & 219 & & 91.8 \\ The proof of concept above is deliberately small and structurally controlled. It is sufficient to show that the usage metrics are computable and that, in one setting, they separate evidence-present from evidence-missing cases. It is not sufficient to validate thresholds, prove localization, or demonstrate operational triage utility. We therefore treat the remaining claims as hypotheses and specify, for each one, the expected effect, the concrete test, and the outcome that would it, so that a later instantiation tests the claim rather than searching for support. All comparisons share one reader; answer quality uses Hits@1 and answer-set F1; confidence intervals use BCa bootstrap resampling~ and paired comparisons a paired bootstrap, and a claim holds only when its effect lies in the predicted direction with a confidence interval excluding the null. Hits@1 increases monotonically across quartiles, with a sharp drop below a threshold $ $. stratify queries by quartile on ~ and report per-quartile Hits@1 with paired-bootstrap significance for the Q4--Q1 gap; estimate $ $ by the Youden index for predicting answer failure; repeat per hop depth on ~ and MetaQA~. the Q4--Q1 gap is null or the ordering is non-monotone; passing extends the proof of concept to natural questions and measures the parametric silent-success residual of , where the reader succeeds despite low . low upstream graph-health metrics co-move with low downstream / , and the five diagnostic signatures of are mutually distinguishable by their metric profiles. report per-query Spearman $ $ between each upstream metric and / , with confidence bands and stratification by query type, together with the fraction of failures in each signature; this is an alignment study, not a causal-propagation claim. the correlations are null or the signatures are not separable, in particular if the relational and generation gaps fail to isolate failures that survive the structural links . routing low- / queries before generation, to a fallback retrieval strategy or a low-confidence warning, improves end-task reliability over no triage. compare the same reader over the same KG with and without / pre-inference triage, reporting Hits@1/F1 and the fraction of silent failures averted. triage yields no reliability gain at any operating point. Two threads are left to future work. Triple-level metrics that depend on human judgment require double annotation against the source, labelling each triple , , or with a third annotator adjudicating, and reporting Cohen's $ $~ and Krippendorff's $ $~. Three validations also remain open: the metrics on a document-grounded corpus , whether ontology grounding raises structural quality , and the remediation map, namely that the lever indicated by a diagnosed stage resolves a substantial share of failures at a measurable, deployment-specific cost. Large language models have transformed information access by enabling fluent, query-driven generation over vast document collections. Yet their most persistent failure mode remains hallucination: the generation of plausible but unsupported statements~. Retrieval-Augmented Generation was introduced precisely to mitigate this risk by grounding generation in externally retrieved evidence~. However, when retrieval operates over flat vector indexes, it recovers semantically similar passages without capturing the relational structure that multi-hop or entity-centric questions demand~. Knowledge graphs offer a principled alternative: by representing factual knowledge as typed, directed triples $ $~, they make relationships explicit and support structured, traceable reasoning paths. Graph-based RAG systems exploit this structure to improve retrieval precision and answer faithfulness~. Yet the KG itself is increasingly built automatically by LLM-driven pipelines rather than curated by experts, introducing a new layer of uncertainty, even as we continue to judge such systems only by whether the final answer looks right. The central question of this paper is therefore twofold: and we can trust the graph that underpins retrieval, and at what , in pathfinding time, retrieval latency, and compute, it can be queried. A graph that is trustworthy but too costly to traverse is no more deployable than one that is cheap but wrong, so an end-to-end account of graph-RAG quality must speak to both. Recent work on trustworthy KG engineering, in particular the methodology~, provides a rigorous lifecycle framework covering construction, validation, deployment, and governance of KGs in safety-critical settings, and defines formal effectiveness metrics for correctness, completeness, and logical consistency. However, and related approaches assume largely expert-driven knowledge elicitation and do not address the automated, LLM-driven extraction pipelines that underpin modern graph-RAG systems. Conversely, graph-RAG systems such as GraphRAG~, LightRAG~, and PathRAG~ are engineered for accuracy and efficiency, but provide no stage-specific trustworthiness instrumentation: failures in extraction, graph structure, or retrieval are invisible until they surface as wrong answers~. Recent evaluation benchmarks have begun to characterize graph structure helps~, and faithfulness-oriented work has studied whether answers are grounded in retrieved evidence~. Yet none of these efforts instruments the pipeline at the level of individual stages with observable, ground-truth-free metrics that can localize failures and trigger principled remediation. fills this gap: it instruments the entire construction, validation, and use pipeline with gold-free, stage-localized trust and cost metrics, turning an opaque end-to-end failure into a diagnosis that pinpoints its stage and the levers that can fix it. We focus on automated, LLM-driven construction because that is how KGs are increasingly built, while evaluation has not kept up; the instrumentation, however, is not specific to it: the metrics apply to any extracted KG artifact, whether produced by LLM extraction or a conventional, non-AI pipeline. is deployment-first: its core trust and cost signals need no gold annotations, so they are computed on every live query, not only on a labeled benchmark. The contribution of is not any single metric but a usable system: it locates where trust breaks across the pipeline, acts on each breach through the stage lever it implicates, surfaces which signals remain computable in a given deployment, and catches failures before the answer is generated. Many of these metrics are individually simple, and several are adapted from prior work; what is new, and what makes them useful, is their organization into a stage-localized, deployment-ready instrument. This paper makes the following contributions: Informed by the methodology~, we define as a three-stage trustworthy engineering process for automated graph-RAG pipelines: , , and . Rather than following prescriptively, adapts its phase structure and makes independent design choices suited to automated extraction, each stage instrumented with stage-specific confidence metrics . We define a suite of stage-specific, independently interpretable metrics , most of which are : computable at deployment without any reference answer or graph. We further characterize them by two computability dimensions: what external reference each needs , and whether the extraction is observable to us or only the final graph is given . At : Triple Confidence Score , Source Coverage Ratio , Extraction Consistency , Cross-Source Corroboration , Schema Compliance , Constraint Violation Rate , Canonicalization Success Rate , and Evidence Attribution Rate . At : the graph-level, ground-truth-free Ontology Coverage , Dead Node Ratio , Semantic Redundancy Rate , and Logical Consistency , alongside correctness and completeness imported from~ for benchmark settings. At : Query Grounding Rate , Entity Retrieval Coverage , Entity Retrieval Precision , Reasoning Readiness Score , Answer Grounding Rate , Answer Utilization Rate , Answer Reasoning Faithfulness , Retrieval Path Cost , and Reasoning Hop Depth . Where gold paths or answers are available, Gold Path Coverage and Retrieval Path Precision/Recall serve as offline checks, and the imported outcome metrics as the dependent variables against which the gold-free metrics are validated . The usage-stage metrics form a chain of necessary conditions whose first broken link localizes a failure to a single condition, and that diagnosis maps to the stage levers that can remedy it: metrics to extraction levers , metrics to graph-and-schema levers , and metrics to retrieval levers, with aggregate metric drops triggering a new cycle that closes the loop toward continuous, monitored KG evolution . We assume no universal cost order across these levers: which is cheapest is deployment-specific and is what our protocol measures . As a proof of concept we instantiate the usage metrics on a multi-hop KGQA probe and find that a capable reader can still answer from its parametric knowledge when retrieval misses the needed evidence, so the answer can look correct while retrieval has in fact failed; this motivates scoring retrieval before and independently of the answer, and a pre-specified, reproducible protocol then specifies the full validation . surveys related work. presents the architecture and the remediation map. defines the metric suite. presents the proof of concept and the evaluation protocol. concludes and outlines future work. {llncs} { } { } { } {} } {} } {} } {} } {} } {} } {}} } {} } {} } {TI}} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {} } {}}} {}}} {}}} {} } {} } {} } {}} {^{*}}} {}} {}} {} #1{{ }} #1{{ }} #1{{ }} #1{{ }} #1{{ }} #1{#1} #1{{ }} {3} } Knowledge graphs that underpin Graph-based Retrieval-Augmented Generation are increasingly built automatically by LLM-driven extraction rather than curated by experts. Proper evaluation would require instrumenting all pertinent stages: extraction, graph construction, and inference, coherently enough to localize failures, so that a failure at one stage is not discovered as a wrong answer at the end. We introduce , a stage-aware instrumentation framework for automated, document-grounded graph-RAG that asks not only whether the underlying graph can be trusted but at what cost it can be queried. attaches stage-specific, independently interpretable metrics to three stages: the , the by expert , and the ; the deployed metrics need no gold annotations, the gold-requiring ones serving only as offline calibration. At usage time these metrics form a diagnostic chain of necessary conditions whose first broken link localizes the failure, and the diagnosis maps to the stage levers that can remedy it: extraction, graph and schema, or retrieval. is a theoretical framework with a proof of concept and a reproducible evaluation protocol. *{Acknowledgments} The authors thank Sabrina Chaouche and Emna Amdouni for their help and remarks. The phase produces a set of extracted triples $E=\{_j\}$, each $= $ a candidate fact from the source documents. Evaluating them requires three complementary perspectives. metrics assess correctness relative to gold triples or source evidence . metrics are intrinsic signals computable without gold annotations. metrics characterize the structural quality of the graph and contextualize why trustworthiness signals are needed alongside accuracy. is each extracted triple trustworthy? The signal to watch first is the Triple Confidence Score ; the other Implementation metrics refine and complement it. Triple fidelity metrics measure how well extracted triples match gold knowledge or source evidence; they are benchmark-setting metrics, applicable when a reference set $G$ or source text $P$ is available. The encoder $f$ is a free parameter: sentence-transformer encoders are a fast, inexpensive default well suited to short triple strings, while LLM-based embedders capture relational nuance at higher cost and latency. The metric is only as reliable as the encoder's ability to place semantically equivalent triples near one another. Given the embedding function $f$, the cosine similarity between an extracted triple $$ and a gold triple $t$ is~ = ) f } {\|f \|\,\|f \|}. Each triple is linearized into a string before embedding, capturing semantic relatedness even when triples differ lexically. Exact matching is too strict for LLM outputs. Using the shared component similarity $ = ,f )$ from , component-wise triple similarity is~ = {3} ( + + ). Set-level Soft-Precision and Soft-Recall are computed via maximum-weight bipartite matching $M$ between $E$ and $G$~: P} = {|E|}\!\! _{ M}\!\! , R} = {|G|}\!\! _{ M}\!\! , F1} = P} R}} {P} + R}}. To measure grounding in source evidence, triple validation is reframed as Natural Language Inference . NLI classifies a pair into one of three labels: , , or . Here the premise is the $p $ from which $$ was extracted , and the hypothesis $H $ is a natural-language verbalization of the triple $ ``$s$ $p$ $o$''). Writing $P_{} ,H)$ for the probability mass an NLI model assigns to the entailment label, the corpus-level entailment ratio is~ = {|E|} _{ E} P_{}\! , H ). Triples with low entailment and high contradiction probability are flagged as unsupported, a grounding signal complementary to Soft-F1. , benchmark-only.} To summarize the three fidelity signals into a single $ $ score, paralleling the trustworthiness aggregate below: = _1\, + _2\, F1} + _3\, , _i _i = 1, where $$ is the mean best-match cosine over $E$ and the weights $ _i$ reflect the relative importance of semantic match, set-level agreement, and source grounding. Because two of its three components require the gold set $G$, is a benchmark-setting aggregate and is computable in gold-free deployment, even though its third component, EntRatio, needs only the source passages $P$. Unlike fidelity metrics, trustworthiness metrics are : they require neither gold triples nor source alignment, and so are operational in any deployment. They capture extraction confidence , source coverage , run-to-run stability , cross-source agreement , structural validity , and grounding to canonical identifiers . .} LLM token probabilities provide an intrinsic proxy for extraction confidence~. Treating the emitted triple as its token sequence $w_1, ,w_{| t|}$, where $w_k$ is the $k$-th token and $w_{<k}$ its predecessors, and writing $P_{} $ for the model's next-token probability, we define as the token probability, equivalently the exponentiated mean token log-probability: = \! ({||} _{k=1}^{||} P_{} ) \;=\; ( _{k=1}^{||} P_{} )^{1/||} \; (0,1]. Under this convention (the model was more confident), the score is length-normalized and bounded, and a triple is flagged when $ < $ for a threshold $ $. Being already in $(0,1]$, enters the aggregate index below directly, with no rescaling. requires access to token log-probabilities from the extracting LLM ; it is undefined for a black-box graph. .} Correctness does not imply completeness. measures how broadly the input yields at least one confident triple. With passages $=\{p_1, ,p_n\}$ and threshold $ $: = : \, E \ \ \} |} {||}\; . Low indicates that large portions of the input fail to yield confident triples, mapping to the coverage notion of~. .} LLM extraction is sensitive to stochastic decoding~. Given $k$ independent extractions $E^{ }, ,E^{ }$ of the same input, is the average pairwise Jaccard similarity~: = {k } _{i < j} E^{ }|}{|E^{ } E^{ }|}\; . A semantic variant replaces set overlap with Soft-F1 matching to absorb paraphrastic variation across runs. .} Whereas measures agreement across of the same input, measures agreement across : a triple extracted independently from two or more documents is far more likely to be true. Let $ $ be the set of distinct source documents from which a triple $$ is extracted. Then = E : | | 2\}|}{|E|}\; . A higher indicates a graph whose facts are corroborated across the corpus rather than resting on single, possibly idiosyncratic, mentions. requires multi-document provenance and therefore applies in the white-box setting. .} Schema compliance measures conformance to the ontology's relation vocabulary and type constraints~. With predicate set $$, domain/range constraints $ , $, and inferred type $ $: _{ } = , = {|E|} _{ E}_{ } \; . requires an ontology and is undefined without one. .} Where is a per-triple pass/fail signal, quantifies how frequently each constraint type is violated. The constraint set is exactly the three checks composing in , $=\{,,\}$ , with \ = 1 - I_c \), where \ \) denotes the \ -th indicator composing \ \), so that decomposes by constraint type rather than introducing new checks: = {|E|} _{ E} ) \; . can be reported per constraint type (e.g., $ _{}, _{}$) to localize systematic schema drift; it too requires an ontology. .} A triple may be correct at the string level yet unusable if its entities cannot be grounded to canonical identifiers~. Let $C $ map surface forms to canonical identifiers, returning $ $ when no link is found: = {|E|} _{ E} \; . The meaning of ``canonical'' depends on the regime of . In the regime, $C$ maps to schema or KB identifiers and measures external grounding success. In the regime , $C$ maps to the internally induced canonical set produced by entity resolution, and measures internal consistency rather than external grounding. This is the point at which the no-ontology consequence becomes concrete: when , , , and are undefined for lack of a schema, the trustworthiness load shifts onto , , , and . , optional.} For auditability, measures how often a provenance evidence span is available for a triple, enabling the verification motivated in . With $ $ the evidence set: = {|E|} _{ E} \; . , optional.} To summarize complementary trustworthiness signals into one indicator : &= _1 TCS + _2 SCR + _3 EC \\ & + _4 CSC + _5 SC + _6 CSR_{}, & _i _i = 1 . where weights $ _i$ reflect application risk (e.g., higher $ _5$ in ontology-critical domains). Because is now bounded in $(0,1]$, it enters the sum directly with no rescaling, so is comparable across datasets and runs. Each component remains independently interpretable. is gold-free but not schema-free: its term is undefined without an ontology. When no ontology is available, is dropped and the remaining weights are renormalized to sum to one, so stays computable and comparable on the schema-free subset. Low Implementation metrics call for , which leave graph structure and retrieval untouched: refine the extraction prompt, lower decoding temperature, add or rebalance few-shot demonstrations, or re-chunk the input. For example, low or points to under-extraction or single-source fragility ; low points to decoding instability ; low / points to schema drift . These are the extraction levers of the remediation map . Knowledge Graph Completion , or link prediction, evaluates a model's ability to predict missing in an existing graph: given $ $ or $ $, the relation fixed, it ranks candidate entities so the correct one appears near the top~. $ exists but is not the setup these rank-based metrics target.) As discussed above, KGC is primarily relevant in the regime: when a graph arrives without source text, log-probabilities, or extraction provenance, the extraction-oriented trustworthiness metrics are undefined, and the meaningful question shifts from ``was this graph extracted well?'' to ``is this graph complete?'', which is precisely what link prediction probes. Reporting is in the setting~: : $={|Q|} _{q}_q$; ~: $={|Q|} _{q}{_q}$; ~: $={|Q|} _{q} $, with $_q$ the rank of the correct entity and $K \{1,3,10\}$. _{}$) .} Standard KGC ranks the correct entity against candidates, including type-incompatible ones that the schema already rules out. When an ontology is available, restricts the candidate set to the type-valid entities for the relation, so that completion is judged on candidates rather than trivially excluded ones. Let $_{} $ be the set of entities compatible with the domain or range of relation $p$ under the ontology, and let $^{}_q$ be the rank of the correct entity within $_{} $: _{} = {|Q|} _{q Q}{^{}_q}. $_{}$ ties link prediction to the same schema axis as and : it rewards a model for placing the correct entity ahead of other alternatives, not merely ahead of entities the ontology would reject anyway. It requires an ontology and is undefined without one. $), adopted.} A black-box graph is sometimes accompanied by per-edge confidence scores, or a completion model produces them; their usefulness depends on whether they reflect true correctness likelihood. We adopt the Expected Calibration Error~ to assess this, binning predictions into $B_1, ,B_M$ by confidence and comparing per-bin accuracy with confidence: = _{m=1}^{M}{n}\, | - |. ECE is a standard metric rather than a contribution of this work; what adds is its in the framework: a low ECE licenses using a graph's confidence scores as a trust signal at , while a high ECE warns that those scores cannot be trusted and the schema-free structural metrics should carry the assessment instead. This mirrors, on the black-box side, the calibration concern that affects on the white-box side . Three limitations motivate caution: rank metrics depend on candidate-set size, hampering cross-dataset comparison~; closed-world evaluation treats unobserved triples as negatives~; and benchmark artifacts such as inverse-relation leakage inflate scores~. therefore complements KGC reporting with the intrinsic trustworthiness metrics above rather than relying on rank metrics alone.This section formally defines the metric suite, summarized by phase in . We first fix a shared notation , then introduce the two axes that govern which metrics are computable in a given setting, and finally present the metrics phase by phase, each followed by a short note on the remediation it triggers. Metrics marked are defined or operationalized in this work for the TRIAGE framework; several instantiate standard measurement ideas, such as coverage, consistency, schema violation, precision/recall, or path connectivity, in a stage-localized graph-RAG setting; the contribution is their joint organization into a deployable diagnostic chain and remediation map. All others are imported using their original definitions and cite their source, whether the methodology~ or an external graph-RAG benchmark. .} {ll} & \\ $= $ & an triple \\ $E=\{_j\}$ & set of extracted triples for a document or corpus \\ $G=\{t_i\}$ & set of gold triples \\ $ $ & triple set of the assembled TRIAGE-KG \\ $ $ & triple set of the reference KG \\ $V$ & vertex set of the assembled KG $ $ \\ $ ,\ _{}$ & reference ontology and its query-relevant subset \\ $f $ & text-embedding function \\ $ = ,f )$ & component-level similarity built from $f$ \\ $=\{p_1, ,p_n\}$ & set of input passages \\ $ _S,\ V_S$ & retrieved subgraph: triples $ _S $, vertices $V_S V$ \\ \ & surface mentions extracted from query \ \\ \ & grounding function \ \\ \ & grounded query-node set \ : m M_q,\ _q \} V\) \\ \ & surface mentions extracted from generated answer \ \\ \ & grounding function \ \\ \ & grounded answer-node set \ : m M_a,\ _a \} V\) \\ $ $ & degree of node $v$ \\ $ ,\ $ & confidence / decision thresholds \\ The suite is generated by crossing three trust questions, namely whether each is sound, whether the assembled is sound, and whether succeeds, with the two computability axes introduced next. A metric earns its place by occupying a cell with a distinct failure mode and a distinct remediation, which is also why we report no single composite score: graph quality for retrieval is multi-objective, with trade-offs such as connectivity against parsimony that a scalar would hide. Most of the metrics are individually simple and several are adapted from prior work; what is new is their organization into a stage-localized instrument whose core signals need no gold annotations at deployment. Two orthogonal axes determine which metrics can be computed in a given setting, and we use them to organize the suite. The first axis is the a metric needs, which is what governs its availability in a given setting. Most metrics need : they are gold-free and schema-free, computed on any KG from the artifacts the pipeline already produces . A second group needs a reference ; since an ontology can be supplied at deployment, these stay live wherever a schema is on hand and become undefined otherwise, the measurement-side counterpart of the two canonicalization regimes of . A third group needs a ; gold is never available at inference, so these are computable only offline and sit below the dashed line in each phase of , acting as yardsticks for the gold-free metrics rather than as deployment signals. is a white-box instrument: each phase's metrics require observing that phase's internal artifact, and with only black-box access they are unavailable . At , white-box access means observing the extraction process, its source text, token log-probabilities, and run variance, which enables , , , , , , and the fidelity metrics; a black-box graph supplied by a third party exposes none of these, and link prediction becomes the completeness probe instead . At , white-box access means observing the retrieved subgraph, the mapped query entities, and the connecting paths, which is what makes , , , and the answer-side metrics computable; with only the query and the final answer, one is back to answer correctness and LLM-as-judge answer-level dimensions. always observes the assembled graph, so its metrics do not depend on visibility. marks ontology-dependent metrics with $ $ and sets off the gold-dependent ones below a dashed line within each phase; summarizes the per-phase visibility requirements, and separates primary signals from derived ones. A first reading can follow the signals that form the spine of the suite and treat the derived, aggregate, and imported metrics as optional diagnostic detail. The suite is deliberately broad, and several metrics are related by construction. We make these relationships explicit so that a practitioner can distinguish signals, worth monitoring and triggering remediation on, from ones that add diagnostic detail. We do claim statistical independence, which can only be established empirically ; the relationships below are definitional or hypothesized. We group them into three kinds. : precision/recall pairs over the same sets, where one is the dual of the other , and , which fixes the query-mention scope underlying and . : and are weighted functions of other metrics, and is a per-constraint refinement of . : low is expected to lower , since counts confident triples; high is expected to lower , since dead nodes cannot form connecting paths; high is expected to raise , since redundant structure inflates expansion. For remediation, the primary metrics are the triggers; the derived metrics help localize a primary metric moved. At inference time, a correct graph-grounded answer requires a chain of conditions to hold in sequence , each one necessary for the next, and each observable without gold annotations. The query entities must exist in the KG , be retrieved into the subgraph , be connected there by a path , have their relation stated correctly by the answer , and appear in an answer grounded on the retrieved evidence . Each condition presupposes the previous one: a query entity cannot be retrieved unless it is in the KG, cannot be connected unless it is retrieved, and so on. For the first two links this is exact, $ $ by construction; for the later links it is a logical precondition rather than a numeric bound. This ordering is what makes the chain diagnostic: reading the metrics in sequence, the link that falls below its threshold $ $ localizes the failure to a single condition. We call the resulting label the failure's : out-of-scope or grounding failure , retrieval gap , connectivity gap , relational gap , and generation gap . The first three links are computable before the LLM is invoked, enabling pre-generation triage; the last two require the produced answer and so apply post hoc. Each link is a necessary, observable proxy rather than a guarantee: a satisfied link is required for success but does not certify it, since a structural signal such as cannot distinguish a correct connecting path from a spurious one. The thresholds $ $ separating ``low'' from ``adequate'' are not assumed; specifies how to estimate them empirically. [ font= , >=Latex, node distance=4mm and 14mm, chk/.style={draw=blue!55, fill=blue!5, rounded corners, align=center, minimum width=22mm, minimum height=7mm}, sig/.style={draw=orange!70!black, fill=orange!8, rounded corners, align=left, text width=52mm, minimum height=7mm, inner sep=3pt}, ok/.style={draw=green!50!black, fill=green!10, rounded corners, align=center, minimum width=22mm, minimum height=7mm}, ] {$ < $?}; {: entities not grounded in the KG}; {$ < $?}; {: in KG, not retrieved}; {$ < $?}; {: retrieved, not connected}; {$ < $?}; {: connected, relation wrong}; {$ < $?}; {: answer not grounded}; {answer supported}; -- node {yes} ; -- node {yes} ; -- node {yes} ; -- node {yes} ; -- node {yes} ; -- node {no} ; -- node {no} ; -- node {no} ; -- node {no} ; -- node {no} ; ; if all pass, the answer is structurally supported. Each signature then maps to the levers that can remedy it; the chain localizes the failing , whose root cause may lie upstream.} The chain is a usage-stage detector: it operates on a query and the validated KG, and it localizes condition failed, not . The cause of a broken link may lie upstream, in extraction or graph construction, so the build- and validation-time metrics are the candidate explanations, and the localization test in Section~ examines whether they co-move with chain failures. Diagnosis is the contribution; what to do about a signature is a map from the failing stage to the levers that can act on it, not a fixed order. Each stage has characteristic levers: Low , , , , or / points to extraction, addressable by prompt refinement, lower decoding temperature, rebalanced few-shot demonstrations, re-chunking, or targeted re-extraction of low-coverage regions, with no change to graph structure or retrieval. Low , high , high , low $ $, or low $ $ points to the assembled graph or its schema, addressable by improved entity resolution and canonicalization, duplicate pruning, contradiction repair, ontology refinement or extension, and document acquisition, all of which touch the KG but not the retrieval algorithm. A retrieval or connectivity gap, or high , that persists once extraction and graph health are adequate points to the retrieval and reasoning algorithm; example directions are given in the Usage remediation note above. We deliberately do not rank these levers by cost: re-extraction reprocesses the whole corpus, a graph fix reprocesses the assembled graph, and a retrieval change may be a configuration swap or a new method, so their relative cost is deployment-specific rather than universal. Which lever is cheapest and most effective for a class of failures is an empirical question for the validation protocol in Section~ and future remediation studies. What adds over a lifecycle that triggers an undifferentiated update is this targeting: the diagnosis names the stage and the levers that can address it. Each lever set corresponds to re-entering a cycle at a different depth: extraction levers re-run extraction, graph-and-schema levers revise the assembled graph or ontology, and retrieval levers alter the retrieval algorithm. A KG Update modifies the shared graph, so it must be justified by population behavior rather than a single query: the build- and validation-time metrics are already graph-level, whereas the usage-stage metrics are per-query and must be aggregated before they can trigger an update. For a per-query metric $m$ and a query window $W$, an update fires when $_{q W}\, m < $, with $$ a mean, a low quantile, or the rate of queries failing the threshold; a single low $m $ drives only per-query triage, a fallback or a warning, not a change to the graph. A persistent aggregate breach then triggers an update targeted at the diagnosed stage, which operationalizes the lifecycle dimension~ as a control loop for automated graph-RAG deployments. : defined or operationalized here for the TRIAGE framework; imported metrics cite their source. $ $ requires an ontology; $ $ requires a gold reference. Within each phase, the metrics above the dashed line are gold-free and form the deployable instrument; those below need a gold reference and are used only offline, to calibrate and validate the gold-free metrics. The link-prediction rows are a black-box completeness probe requiring held-out triples, not gold-free deployment signals.} {5pt} {1.05} {llll} & & & \\ {*}{KG Implementation} & Triple Confidence Score & & \\ & Source Coverage Ratio & & \\ & Extraction Consistency & & \\ & Cross-Source Corroboration & & \\ & Entailment Ratio & $$ & \\ & Schema Compliance Rate$^{ }$ & & \\ & Constraint Violation Rate$^{ }$ & & \\ & Canonicalization Success Rate & & \\ & Evidence Attribution Rate & & \\ & Aggregated Trustworthiness Index & & \\ & Mean Rank / MRR / Hits@K & $$ & \\ & Ontology-constrained ranking$^{ }$ & $_{}$ & \\ & Calibration of completion conf. & $$ & \\ & Embedding Similarity$^{ }$ & $ $ & \\ & Soft-F1$^{ }$ & $F1}$ & \\ & Triple Fidelity Index$^{ }$ & & \\ {*}{KG Validation} & Logical Consistency$^{ }$ & & \\ & Ontology Coverage$^{ }$ & & \\ & Dead Node Ratio & & \\ & Semantic Redundancy Rate & & \\ & Correctness$^{ }$ & & \\ & Completeness$^{ }$ & & \\ {*}{KG Usage} & Query Grounding Rate & & \\ & Entity Retrieval Coverage & & \\ & Entity Retrieval Precision & & \\ & Reasoning Readiness Score & & \\ & Retrieval Path Cost & & \\ & Answer Grounding Rate & & \\ & Answer Utilization Rate & & \\ & Answer Reasoning Faithfulness & & \\ & Reasoning Hop Depth & & \\ & KG Net Impact & & \\ & Gold Path Coverage$^{ }$ & & \\ & Retrieval Path Precision/Recall$^{ }$ & $$ & \\ & Answer Correctness$^{ }$ & $$ & \\ & Context Coverage$^{ }$ & & \\ & Reasoning Failure Rate$^{ }$ & & \\ & Hard Hits Rate$^{ }$ & & \\ instrument: each phase's metrics require observing that phase's internal artifact. With only access those metrics are unavailable, and one falls back to the right-hand column. Visibility is independent per phase, and is separate from ontology-dependence, marked $ $ in .} {4pt} {1.35} {@{}l p{0.40 } p{0.34 }@{}} & & \\ Implementation & extraction process: source text, token log-probabilities, run variance & only the output triples: link prediction \\ Validation & the assembled graph & the graph is the artifact under review, so it is observed by construction \\ Usage & the retrieved subgraph, mapped query entities, and connecting paths & only the query and the answer: answer correctness and LLM-as-judge answer-level dimensions \\ metrics are independent signals worth triggering remediation on; metrics are definitional complements, aggregates, or expected correlates of a primary metric, and serve as diagnostic detail. No claim of statistical independence is made; relationships are definitional or hypothesized .} {lll} & & \\ & primary & extraction confidence \\ & primary & source coverage \\ & primary & run-to-run stability \\ & primary & cross-source agreement \\ & primary & schema validity \\ & derived & per-constraint refinement of \\ & primary & grounding success \\ , & derived & weighted aggregates of the above \\ & primary & graph-level consistency \\ & primary & schema coverage \\ & primary & structural inertia \\ & primary & structural redundancy \\ & derived & gold-anchored counterpart of \\ & primary & query in-scope for KG \\ & primary & retrieval recall of grounded query nodes \\ & derived & precision complement of \\ & primary & subgraph connectivity \\ & primary & answer grounding \\ & derived & recall complement of \\ & primary & relational faithfulness \\ & primary & retrieval cost \\ & derived & path-length distribution behind \\ The phase queries the validated KG via graph-based retrieval and inference. We first fix the object these metrics operate on. For a query $q$ over the validated KG $ $ with vertex set $V$, the retrieval function induces a $ _S $ with vertex set $V_S V$. A path ``in the subgraph'' uses only edges in $ _S$. All intrinsic metrics below are ground-truth-free and computable before or during generation; gold-requiring metrics are collected at the end. does retrieval set up a correct answer? The signal to watch first is Entity Retrieval Coverage , with Reasoning Readiness close behind; the answer-side metrics then check faithfulness. We sort the usage metrics into two roles and label each group below accordingly. A takes a graded value on a single query, so it can flag or triage that query before the answer is judged, and it can also be aggregated as the averaged score over a set of queries. A instead summarizes end-task success over a of queries, as a rate or ratio, and serves as an indicator. This predictor/outcome split is orthogonal to the primary/derived distinction of : the former concerns what a metric measures , the latter whether it is an independent signal or a definitional complement. Usage metrics compare of the query or answer against $ _S$. Resolution proceeds in two stages: an step turns free text into candidate entity mentions , and a step resolves each mention to a graph node by significant-token overlap against node labels. Let \ be the set of surface mentions extracted from query \ , and let \ be the grounding function, where \ denotes an unresolved mention. We define the grounded query-node set as \[ V_q^g = \{ _q : m M_q,\ _q \} V . \] The query-side denominator remains the number of extracted surface mentions \ , so that missing or unresolved mentions are penalized. Thus , , and measure, respectively, the fraction of surface mentions grounded to the KG, the fraction whose grounded nodes are retrieved into the subgraph, and the fraction of query-mention pairs whose grounded nodes are connected there. The retrieved subgraph has node set \ . All intersections such as \ are therefore intersections between KG node identifiers, not between text mentions and graph nodes. Over-segmented multi-word mentions are then coalesced into the largest matched entity, so each real-world entity contributes a single node. All query-side metrics are measured over the same mention universe \ : QGR and ERC use \ directly, while RRS uses query-mention pairs through \ . Thus unresolved or unretrieved mentions are penalized consistently across scope, coverage, and connectivity. Throughout this section, a ratio with an empty denominator is reported as N/A rather than assigned a numeric value: , , and are N/A when $|M_q|=0$ ; and are N/A when $|V_S|=0$ ; and is N/A when $|M_a|=0$ . For , queries whose grounded entities are disconnected in $ _S$ $) are collected in a separate $ = $ bucket, so the reported distribution sums to one over $\{0,1,2, , \}$. These degenerate cases are themselves diagnostic and are reported as counts alongside the metric. These characterize the retrieved subgraph the LLM is invoked. They are all per-query predictors. .} Fraction of extracted query mentions that exist as graph nodes at all, a KG-coverage signal distinct from retrieval quality: = }|}{|M_q|}\; . .} Fraction of extracted query mentions whose grounded nodes are present in the retrieved subgraph: = } V_S|}{|M_q|}\; . Sharing the denominator $|M_q|$ with gives $ $ always; the gap $ - $ is exactly the retrieval failure . Because $M_q$ is extracted from the query surface, never inspects the answer: it is disjoint from answer-side outcomes such as , so using to predict them is not circular by construction. .} The precision complement of : are retrieved nodes mostly query-relevant, or bloated by neighborhood expansion? = } V_S|}{|V_S|}\; . Because the denominator $|V_S|$ counts retrieved nodes, typically far larger than the handful of query anchors, is low in absolute terms by construction; it is therefore read , comparing retrieval strategies or configurations on the same queries rather than against a fixed threshold. Low alongside high indicates over-retrieval: a large context with few anchors to the question. .} Whether the subgraph is connected enough to support reasoning across the query's entities: the fraction of query-entity pairs joined by a path in $ _S$, with a convention for the single-entity case where the pair count is zero: = V_q^{} : \, \ \ _S\}|}{{2}}, & |M_q| 2,\\ , & |M_q|=1, By construction, \ . The denominator counts pairs over extracted query mentions, so an off-graph entity is penalized. For a single-entity query, readiness reduces to whether that entity is grounded and retrieved or not . Because this denominator also charges for entities that were never grounded or retrieved, a low can reflect a grounding or retrieval gap rather than a genuine connectivity failure. is therefore read as a connectivity signal only conditional on high : the diagnostic ``high , low '' connectivity gap of is exactly the regime in which the entities are present yet disconnected. A more localized variant could replace the denominator by the number of grounded, retrieved query-entity pairs, e.g., pairs in $V_q^g V_S$, but we keep the global denominator over $M_q$ because RRS is used in the diagnostic chain as an end-to-end readiness signal: it should decrease not only when retrieved entities are disconnected, but also when grounding or retrieval has already failed. .} The intrinsic metrics above say nothing about , yet the central question of includes the price of traversal. captures it as the retrieval effort expended per query, measured as the number of nodes expanded during subgraph construction (a hardware-independent proxy) and, optionally, wall-clock path-search time: = |\{v V : v\ \ q\}|. makes the coverage/cost trade-off observable: a retrieval strategy may raise and only by expanding far more of the graph, and exposes that price. It connects directly to , since a redundant graph inflates expansion, and to the traversal-budget failure mode of . These measure faithfulness of the generated answer to the retrieved subgraph. Let \ be the set of surface mentions extracted from the generated answer \ , and let \ be the answer grounding function. We define \[ V_a^g = \{ _a : m M_a,\ _a \}. \] , , and are per-query predictors; is a set-level metric and so reads as a diagnostic rather than a single-query signal. .} Fraction of answer entities present in the retrieved context, detecting hallucinated entities never retrieved: = {|M_a|}\; . .} The recall complement of , how much of the retrieved context the answer actually used: = {|V_S|}\; . As with , the denominator $|V_S|$ makes structurally low in absolute terms, so it is interpreted relatively across queries or configurations rather than against a fixed threshold. High with low suggests a correct answer drawn from a narrow slice of available evidence. .} measures faithfulness: when the answer discusses two entities the KG connects, does it state their relationship correctly? This isolates relational hallucination, which entity-level cannot see. Rather than parsing the answer for explicit relational claims (unreliable, because extracting relations from generated text propagates the same errors that afflict any extractor and compounds them at each stage~), uses the retrieved triples as the reference set of relations the answer assert. Let $C_q$ be the retrieved triples whose endpoints are both grounded in the answer; is the fraction whose relation also appears in the answer text $A$: = |}{|C_q|}, \\ C_q=\{ _S : s V_a^g V_S o V_a^g V_S\}, where $p A$ tests lexically whether the relation label appears in $A$. is reported as N/A when $|C_q|=0$ . , diagnostic.} characterizes the structural complexity actually exploited, as the distribution of shortest-path lengths $ $ connecting the grounded query entities in $ _S$, with \ =0\) for single-entity queries whose grounded entity is retrieved, and \ = \) when the grounded query mentions are not connected in \ or cannot be evaluated because grounding/retrieval failed. = : =k\}|}{||}, k \{0,1,2, , \}, so that $ _k =1$ including the disconnected bucket. Mass concentrated at $k 1$ indicates the pipeline rarely exploits multi-hop structure, suggesting the KG adds little over flat retrieval for that query class, while mass at $ $ flags queries the subgraph cannot connect at all. is reported as a distribution to preserve diagnostic detail. When gold reasoning paths or gold answers are available , the Usage phase admits gold-anchored metrics that give it the same two-regime treatment as the earlier phases. and Answer Correctness are global outcome metrics; RPP/RPR are per-query but, unlike the predictors above, need a gold path. .} Fraction of queries whose gold reasoning path $P^*_q$ is fully contained in the retrieved subgraph: = : P^*_q _S\}|}{||} \; . is the gold-anchored counterpart of / . .} Of the retrieved-subgraph edges, the fraction lying on the gold path , and of the gold-path edges, the fraction retrieved : = {| _S|}, = {|P^*_q|}. The ultimate gold-anchored outcome, Hits@1 and F1 over answer sets~, used in as the dependent variable the intrinsic metrics are designed to predict. To validate the intrinsic metrics and close the loop, we import four outcome metrics from recent graph-RAG benchmarks. They are not contributions; we adopt them as dependent variables. Unlike the intrinsic usage metrics above, three of these require a gold answer or correctness label, not live gold-free signals monitored at deployment; only , built from grounding alone, is gold-free. All four are global outcome metrics: and average a per-query label into a rate, whereas and are population-level ratios with no single-query value. .} Counterfactual contribution of the KG: answer grounding the retrieved triples minus that them. A drift toward $ 0$ across a query class signals stale or irrelevant KG content. .} Fraction of queries whose gold answer is present in the retrieved context, the answer-string analogue of . .} Fraction of queries where the answer is in context yet the model still answers wrongly, isolating the generation-stage residual from retrieval failure. .} $ =/$, robustness under an incomplete KG; complements by exposing, at usage time, the queries that KG incompleteness breaks. Only -stage breaches implicate the retrieval algorithm itself. Concretely, the signature selects the lever: a retrieval gap calls for a wider seed set or traversal budget; a connectivity gap calls for a retrieval primitive that recovers the connecting path, such as backward or path-based traversal; and high calls for pruning redundant expansion or a sparser primitive. A retrieval or connectivity gap that persists once extraction and graph health are adequate, or high , motivates architectural changes to retrieval and reasoning, for example super-relation reasoning that adds backward traversal and aggregates relational paths~, logic-aware multi-hop traversal~, GNN-based structural retrieval~, RL-optimized graph indexing~, agentic restructuring~, or cross-document construction that adds connective structure~. A low instead marks an out-of-scope query , and a generation gap points to the generator itself, for example prompting it to cite the retrieved evidence or constraining decoding to it. These are the retrieval levers, not a prescription; the remediation map is described next. The phase assesses the assembled KG before use. A key shift from is one of : where Implementation metrics are computed per triple, Validation metrics are computed over the whole graph $ $. Logical consistency is the clearest example: here it is a property of $ $ under ontology axioms, detected by a reasoner over the entire graph, not a per-triple schema check. Validation metrics fall into two groups: metrics, computable in any deployment, and metrics requiring a reference KG $ $. does the assembled graph hold together well enough to query? The schema-free signal to watch first is the Dead Node Ratio , with Logical Consistency and Ontology Coverage adding schema-based checks when an ontology is available. Absence of internal contradictions in $ $, detected by OWL reasoning against the ontology~. Let $ $ be the set of triples that participate in at least one violated ontology axiom; defining the numerator as this triple count keeps the score in $ $: = 1 - |}{| |}\; , where a triple is in $ $ if it occurs in some triple set violating an ontology axiom , as found by a reasoner such as HermiT over the full graph. requires an ontology. .} measures what fraction of the query-relevant ontology is instantiated in $ $: = } : \, t \ \ c\}|} {| _{}|}\; . is the structural-completeness signal of this phase: not whether triples are correct but whether the graph covers the domain as defined by the ontology. Low on a class suggests either missing source content or extraction failure for that class, both actionable. .} A has degree $ 1$: isolated or a dangling leaf . Such nodes cannot serve as intermediate bridges in multi-hop reasoning: a degree-1 node may still be a valid path endpoint , but it cannot connect two other entities along a reasoning chain. With node set $V$ of $ $: = |}{|V|}\; . High signals disconnected entities: surface-form variants, over-segmented mentions, or hallucinated entities with no relational context. It complements and by detecting content, and is computable without an ontology. .} Where detects too little connectivity, detects too much duplication: triples that are semantically equivalent or subsumed by others, inflating the graph without adding information. Using the component similarity $ $ and a threshold $ $, call two triples if all three components match above $ $; let $ $ be the set of triples that duplicate or are subsumed by another: = |}{| |}\; . High indicates a bloated graph that raises retrieval cost ( , ) without improving coverage; it is computable without an ontology. When a reference KG $ $ is available , the following metrics from~ provide gold-anchored evaluation and calibrate the ground-truth-free metrics above. Let $ _{}= |_{ _{ }}$ be the correct subset of $ $ and $ _{}= |_{ _{ }}$ the covered subset of $ $. .} = 1 - }|}{| |}\; . .} = 1 - }|}{| |}\; . Low is the gold-anchored counterpart of low : both signal structural incompleteness, but is measured against gold triples and against the ontology schema. In the lifecycle~, low is a direct trigger for a . Low Validation metrics call for that touch the graph and its schema but not the retrieval algorithm: improve entity resolution and canonicalization , prune or merge duplicates , repair contradicting triples or axioms , and, importantly, refine, loosen, or extend the ontology, or acquire targeted documents, when or is low. Ontology refinement is the natural Validation-stage lever, available only when a schema exists; without one, the resolution-based fixes above carry the load. These are the graph-and-schema levers of the remediation map .Trustworthiness in a graph-RAG pipeline is not a single property but a layered one: it must hold at the level of , at the level of the they form , and at the level of . Prior work has addressed each level largely in isolation. We organize this section accordingly. We first review triple-level evaluation, distinguishing the assessment of extracted triples from their assessment . We then review graph-based RAG methods, their evaluation protocols, and the related work on faithful and grounded reasoning over knowledge graphs. Finally, we discuss trustworthy KG engineering methodologies and connect them to broader trustworthy AI principles, positioning as a framework that instruments all three levels within a single, stage-aware methodology. Knowledge graphs represent structured factual knowledge as directed labeled edges, commonly written as triples $ $, and a large body of work evaluates models and pipelines by how well they recover or validate such triples~. We distinguish two evaluation objectives that differ in both their assumptions and their metrics, and that correspond to two successive moments in the construction of a KG: first assessing whether an extracted triple is trustworthy at all, and only then assessing how well a set of trusted triples completes a graph. The first question is whether a candidate triple, freshly proposed for insertion into a KG, is correct and well-formed. Such triples may be produced by rule-based information extraction, neural relation extraction, Open Information Extraction , or, increasingly, LLM prompting. Assessing them is a problem: given a complete, already-formed triple $ $, decide whether it is true and admissible, a per-triple yes/no judgment rather than an ordering of alternatives. This judgment must account for three distinct concerns: , , and ~. Ensuring consistency with ontology constraints and type restrictions is central here, as violations often signal structural or semantic errors in extracted knowledge~. Once a body of trusted triples exists, a second question arises: given the graph they form, which edges can be inferred? This is Knowledge Graph Completion, also known as link prediction, and unlike triple validation it is fundamentally a problem. For a query of the form $ $ or $ $, where the relation is given and a missing is sought, a model scores every candidate entity in the KG and ranks them, with success measured by how highly the correct entity is ranked~. The distinction matters: validation asks ``is triple true?'' and returns a judgment about a single, complete triple; completion asks `` entity best completes this gap?'' and returns an ordering over many candidates. Accordingly, KGC relies on rank-based metrics such as Mean Reciprocal Rank and Hits@K, computed over both head- and tail-prediction tasks~. Standard KGC evaluation further distinguishes from settings, the latter removing other known true triples from the candidate set so that a model is not penalized for ranking a different correct answer above the target~. A recurring methodological pitfall in KGC evaluation is benchmark contamination through inverse relations. In early benchmarks such as FB15k, many relations have near-inverse counterparts: for instance, a edge and a edge encode the same fact in opposite directions. A model can then achieve high test scores not by learning meaningful relational structure, but by memorizing that one relation is the inverse of another: having seen $ $ at training time, predicting $ $ at test time is trivial. This inflates reported performance without reflecting genuine inference ability. Corrected benchmarks such as FB15k-237 remove these redundant inverse and duplicate relations precisely so that the evaluation measures inference rather than memorization~. Before the adoption of LLMs, triple extraction relied on classical information-extraction methods , which are deterministic and structurally consistent but brittle under linguistic variability and limited in implicit or cross-sentence reasoning~. For the open-domain, paraphrase-rich setting that graph-RAG pipelines operate in, these approaches have been largely superseded by LLM-based extraction, which we focus on here. LLM-based extraction uses generative models to map natural language directly into structured triples. Compared to classical pipelines, LLMs are more effective at handling paraphrases, implicit relations, long-context dependencies, and some forms of multi-sentence reasoning~. They are highly adaptable across domains with minimal feature engineering and can capture synonymy and nuanced language. Crucially for trustworthiness, LLMs can be prompted to emit, alongside each triple, the source span from which it was derived, a natural-language justification or character offset into the input. This provenance enables three things downstream: verification of the triple against its cited evidence, confidence estimation based on the strength of that evidence, and human audit of contested facts~. We operationalize exactly this signal later as the Evidence Attribution Rate . However, these advantages introduce new challenges. LLMs may hallucinate plausible but unsupported facts~, and their outputs can vary depending on prompting and decoding strategies~. This leads to issues such as schema drift, non-deterministic formatting, and inconsistencies across runs. The two standard prompting regimes trade one failure mode for another. In zero-shot extraction, the model relies solely on instructions and input text, providing strong generalization but increasing the risk of hallucination and structural inconsistency~. Few-shot extraction augments prompts with demonstrations, improving precision and format control~, but the demonstrations themselves introduce : the model tends to over-produce relation types and structures resembling the examples, while under-extracting valid triples that do not match the demonstrated patterns. Neither regime is free of failure modes, which is precisely why dedicated trustworthiness metrics are needed rather than reliance on extraction accuracy alone. The diversity of failure modes introduced by LLM-based extraction, such as hallucination, schema drift, demonstration bias, and output variability, exposes the limitations of evaluation metrics that rely solely on aggregate accuracy or exact matching. In trustworthy graph construction, it is essential to quantify not only correctness but also confidence, coverage, and stability of extracted knowledge. This motivates dedicated trustworthiness metrics such as the Triple Confidence Score , which we operationalize here for extraction-level reliability within end-to-end graph-based RAG pipelines. Graph-based RAG~ addresses a limitation of classical text RAG, namely its inability to capture structured dependencies across a document corpus. Instead of indexing flat text chunks, these methods first construct an $G = $ whose nodes $v V$ represent entities extracted from the documents and whose edges $ E$ encode their typed relations $ $. At retrieval time, the graph structure is exploited to assemble a query-relevant subgraph that is verbalized as context for the generator. Three landmark systems define the current design space, and the cleanest way to contrast them is by their , the granularity at which evidence is extracted from $G$ in response to a query $q$. ~ retrieves whole . A hierarchical community detection partitions the graph into $\{C_1, , C_m\}$, and an LLM produces a textual summary $s $ for each community. The retrieved context for a query $q$ is the set of summaries deemed most relevant to $q$: R_{} \;=\; \{\, s \,:\, C_i _{i}\, ) \, \}, where $ $ is an LLM-rated relevance score. the community primitive excels at global, dataset-spanning summarization queries where the answer draws on a whole topic region. it produces verbose, redundant outputs whenever only a sub-region of a community is pertinent, and the offline community-summarization step is computationally heavy to build and to refresh as the graph changes. ~ retrieves the of cosine-matched entity nodes. An LLM extracts a keyword set $K_q$ from $q$ that is then matched against entity embeddings via dense vector similarity: V_q \;=\; _{k K_q} _{\,v V} \! ,\, e ), where $e $ denotes the embedding function, and the retrieved subgraph is the 1-hop neighborhood of $V_q$. it is substantially lighter and faster than GraphRAG, with no offline community-summarization cost, and its dual-level design adapts to both specific and abstract queries. the ego-network of any query-related node still contains many edges that are not on the reasoning chain linking the query's entities, introducing structural noise that can mislead the generator. ~ argues that the principal limitation of GraphRAG and LightRAG is the of the retrieved subgraph rather than its insufficiency, and instead returns sparse multi-hop between anchors. The anchor set $V_q$ is computed by dense matching as in LightRAG; for each anchor a unit resource is propagated through the graph, decaying by $ $ at every hop, and each path is scored by its average resource, with the top-$K$ paths forming the retrieved context. the path primitive is more compact than ego-networks and more directly aligned with the multi-step structure of the query, reducing the surface area over which the generator can hallucinate. path enumeration is expensive on dense graphs, where the number of candidate paths grows combinatorially; the resource-propagation scoring adds retrieval-time latency; and a sparse-path primitive can miss answers that require a fuller neighborhood rather than a single chain. In short, the three systems trace a trade-off axis rather than a strict ranking: GraphRAG maximizes coverage at the cost of verbosity and offline expense, LightRAG minimizes cost at the risk of structural noise, and PathRAG maximizes precision at the cost of path-search latency. Which point on this axis is appropriate depends on the query distribution and the available compute budget, a dependence that motivates measuring retrieval cost explicitly, as does at . Because these systems target open-ended generation tasks for which no canonical ground-truth answer exists, the dominant evaluation protocol is pairwise comparison along a small set of qualitative dimensions, introduced by GraphRAG and adopted, with minor variation, by LightRAG and PathRAG. GraphRAG scores answers on Comprehensiveness, Diversity, and Empowerment, later extended with a Directness dimension~; LightRAG uses Comprehensiveness, Diversity, and Empowerment together with an aggregate Overall judgment~; PathRAG follows the same protocol~. For each triplet, an LLM selects a winner per dimension, and aggregate win-rates are reported. This protocol is appropriate when no ground truth is available, but it has several limitations relevant to trustworthiness. Judgments are stochastic and order-sensitive; the judge and the system under test often share the same underlying model family, raising self-preference concerns; and a win-rate is a coarse, black-box end-to-end signal. The deeper problem is one of : these protocols score only the generated , never the retrieved . In plain terms, they check whether the final answer looks well-supported, but never check whether the retrieved subgraph actually contained the facts needed to answer the question. When retrieval misses something, the LLM can quietly fill the gap from its own parametric memory and still produce a fluent, plausible answer, so a genuine retrieval failure looks identical to a genuine retrieval success. Answer-level scores therefore cannot separate a sound reasoning path through the graph from a lucky guess by the model, which is exactly the distinction a trustworthy pipeline needs to make. Beyond per-system judging, a first wave of recent benchmarks asks graph structure helps at all, but still scores only the end task~. ~ stresses the same task-level metrics under noisy, heterogeneous wild-source corpora, and adds a system-level dimension for graph-RAG versus dense RAG in agentic search. Across this line, evaluation remains task-dependent and exposes no graph-aware signal: failures cannot be attributed to extraction, retrieval, or generation. A second, faithfulness-oriented wave begins to open the black box, but its signals are still computed on the generated answer rather than on the retrieved subgraph. introduce a alongside context coverage and token-F1 on multi-hop QA; ~, a financial-QA benchmark, frames groundedness as binary hallucination detection, where an answer counts as grounded only if supported by both text snippets and KG triples; the tripartite RAG-Eval framework~ aggregates query relevance, factual accuracy, coverage, coherence, and fluency into an overall confidence score; and probes robustness under KGs with Hits@Hard and the Hard Hits Rate. These works diagnose whether an answer is grounded, but none instruments the structural adequacy of the retrieved subgraph itself, leaving the retrieval failure described above invisible. addresses this gap by attaching observable, ground-truth-free metrics directly at : measures whether a query is on-topic for the KG; , , and characterize the structural quality of the retrieved subgraph independently of the downstream generation step; , , and measure the alignment between the generated answer and the retrieved evidence; and measures the computational cost of retrieval itself. These metrics complement the existing answer-level qualitative dimensions rather than replacing them: they isolate retrieval-stage failures from generation-stage failures with deterministic, structural signals that can trigger a cycle in a principled way. A complementary line of work studies over knowledge graphs: whether a system's answer can be traced, step by step, to explicit graph evidence rather than to parametric knowledge in model weights. Graph-Constrained Reasoning ~ operationalizes this directly by restricting the LLM's decoding to paths that exist in the KG, so that every reasoning step corresponds to a verifiable graph edge. This substantially reduces hallucination on multi-hop questions, but it also exposes a new failure mode: when the KG itself is incomplete or incorrectly extracted, graph-constrained decoding fails silently, producing no answer, or a wrong one, with no signal indicating whether the fault lies in the graph or in the model. A second, scale-related failure mode is equally important. On large KGs, the number of candidate reasoning paths grows combinatorially with hop depth, so practical systems impose cutoffs: a maximum number of hops, a top-$K$ beam over paths, or a bounded expansion budget. These cutoffs keep retrieval tractable, but they are double-edged: when the true reasoning path is longer than the hop limit, or is pruned by the beam, the correct answer becomes unreachable even though it is present in the graph. This is a failure of , not of knowledge, and it is entirely invisible to answer-level metrics, directly motivating the Reasoning Hop Depth and Retrieval Path Cost metrics introduced in . The same path primitive underlies earlier multi-hop KGQA systems~, which address faithfulness from the retrieval side: restricting context to sparse relational paths between query-relevant entities shrinks the surface over which hallucination can occur, the property PathRAG brings to graph-RAG retrieval. Evaluation on PathQuestion~ and MetaQA~ has shown that path coverage, whether the gold reasoning path lies in the retrieved subgraph, is a strong predictor of answer correctness on multi-hop questions. This observation directly motivates the Reasoning Readiness Score and Entity Retrieval Coverage , which operationalize path coverage as observable, pre-inference metrics that do not require gold answers. Faithfulness has also been studied at the answer level, measuring whether generated statements are entailed by the retrieved evidence~. As with the answer-level evaluation protocols above, such metrics require a generated answer and cannot separate insufficient retrieval from faulty reasoning; the subgraph-level signals and precede generation, enabling proactive triage rather than post-hoc auditing. The methodology~ is the closest work to our framework in spirit. It proposes an end-to-end trustworthy engineering methodology for KG-based systems, structured along three complementary dimensions: a , a , and a transverse covering governance, provenance, and quality assessment across all phases. Crucially, defines a formal suite of , correctness , completeness , logical consistency , representativeness, and timeliness, which we directly import into the stage . However, was designed for expert-driven knowledge elicitation in safety-critical industrial settings and does not address automated, LLM-driven extraction pipelines or the phase. is informed by its methodology and extends it to fill both gaps, while making independent design choices appropriate to automated graph-RAG. Beyond , broader trustworthy AI frameworks, establishing principles of transparency, accountability, and robustness for AI systems, motivate the need for end-to-end, stage-aware evaluation rather than single-point quality checks~. Recent surveys on KG quality further consolidate the requirements for correctness, completeness, and consistency that operationalizes for the graph-RAG setting~. To the best of our knowledge, is among the first frameworks to operationalize these principles end-to-end for automated, document-grounded graph-RAG pipelines, instrumenting extraction, validation, and usage within a single methodology. TRIAGE instruments a graph-RAG pipeline so that a failure can be read off its metrics rather than guessed at. The payoff, developed in , is twofold. At usage time the metrics form a diagnostic chain of necessary conditions for a correct answer, and the first one that fails localizes the failure to a single condition. That diagnosis then points to the stage levers that can remedy it: extraction, graph and schema, or retrieval. The rest of this section sets up the three phases that carry these metrics, and defines the metrics themselves; threads one query through all three. is informed by the engineering methodology~ and adapts it to automated, document-grounded graph-RAG pipelines. We take from its phase-structured view of KG engineering and its trustworthiness dimension, but we do not follow it prescriptively: the automated, LLM-driven setting calls for independent design choices that , conceived for expert-driven elicitation, does not address, notably the phase and the remediation map developed in . Adapting the , covers three phases of a single KG version cycle: , , and . Each phase produces both a and a set of stage-specific confidence metrics. The metrics do not flow between stages; rather, they are each independently observable quality signals that together provide an end-to-end trustworthiness picture of the pipeline. illustrates this structure. The operates transversally: aggregate metric results may trigger a cycle, and the stage at which a metric drops points to the levers that can remedy it . [ font= , >=Latex, node distance=5mm, proc/.style={draw=blue!60, fill=blue!5, rounded corners, align=center, inner sep=3pt, minimum width=30mm, minimum height=8mm}, artifact/.style={draw=green!50!black, fill=green!6, rounded corners, align=center, inner sep=3pt, minimum width=24mm, minimum height=6.5mm}, readout/.style={draw=gray!70, fill=gray!12, rounded corners, align=center, inner sep=3pt, minimum width=28mm, font= }, update/.style={draw=orange!75!black, fill=orange!8, rounded corners, dashed, align=center, inner sep=3pt, minimum width=22mm, font= }, tap/.style={gray!60, dashed, shorten >=1pt} ] } {Documents}; {Phase 1: KG Implementation\\ { extraction + grounding}}; {KG$_v$}; {Phase 2: KG Validation\\ { expert review}}; {KG$_{validated}$}; {Phase 3: KG Usage\\ { graph-based inference}}; {answer}; {query}; -- ; -- ; -- ; -- ; -- ; -- ; -- ; {Implementation metrics\\ { see }}; {Validation metrics\\ { ,\ ,\ ,\ }}; {Usage metrics\\ { see }}; -- ; -- ; -- ; at !0.5! $) {lens}; at !0.5! $) {lens}; at !0.5! $) {lens}; {KG Update\ cycle}; to ; -- ; to ; .. controls + and + .. ; together with stage-specific confidence metrics . Low aggregate metric values may trigger a new cycle; the stage of the failing metric points to the levers that can remedy it .} [ font= , >=Latex, node distance=6.5mm, proc/.style={draw=blue!60, fill=blue!5, rounded corners, align=center, inner sep=3pt, minimum width=32mm, minimum height=7mm}, artifact/.style={draw=green!50!black, fill=green!6, rounded corners, align=center, inner sep=3pt, minimum width=32mm, minimum height=7mm}, card/.style={draw=green!50!black, fill=green!6, rounded corners, align=center, inner sep=3pt, minimum width=34mm, minimum height=13mm}, onto/.style={draw=green!50!black, fill=green!4, rounded corners, dashed, align=center, inner sep=3pt, minimum width=24mm, font= }, readout/.style={draw=gray!70, fill=gray!12, rounded corners, align=center, inner sep=3pt, minimum width=18mm, font= }, faded/.style={draw=gray!45, fill=gray!4, rounded corners, align=center, inner sep=3pt, text=gray!55, minimum width=26mm, font= }, tap/.style={gray!60, dashed, shorten >=1pt} ] } {Documents}; {Segmenter\\ { hierarchical chunking}}; {Chunk tree\\ { sec / para / sentence spans}}; {Extractor\\ { sentence-wise LLM}}; {candidate triple\\ $ $\\ { $ $doc\,7, sent\,12$ $,\ $0.94$}}; {Canonicalize\\ { entity resolution}}; {Canonical entity\\ + predicate map}; {Graph builder}; {KG$_v$}; -- ; -- ; -- ; -- ; -- ; -- ; -- ; -- ; {ontology $$\\ }; -- ; { }; { ,\ ,\ ,\\ ,\ }; { ,\\ ,\ }; -- ; -- ; -- ; at !0.5! $) {lens}; at !0.5! $) {lens}; at !0.5! $) {lens}; {versioned artifact trail\\ raw\,$|$\,canon\,$|$\,dedup\,$|$\,EL\\ }; to ; stage. The vertical spine is a representative construction pipeline (documents $ $ segmentation $ $ extraction $ $ canonicalization $ $ graph); each magnifier probe taps one observable artifact: the chunk tree yields ; each candidate triple, carrying a provenance pointer and confidence, yields , , , , and ; and the canonical map yields together with the schema checks and ( , requiring the ontology $$, supplied as an optional side input). The persisted, versioned artifact trail is what makes provenance auditable. Markers match : \ needs a gold reference, \ needs an ontology.} To make the metrics concrete we describe a extraction pipeline ; instruments any pipeline of this shape rather than prescribing this one. It turns document structure into candidate triples through an automated process of segmentation, extraction, grounding, canonicalization, and trust scoring, described step by step below. The pipeline performs hierarchical segmentation before extraction: document into sections and subsections, subsection text into paragraphs, paragraph into sentence-like spans. The segmentation strategy determines which textual units are presented to the extractor, directly influencing the coverage and recall of the resulting KG. Triple extraction is performed sentence-wise. The extraction model follows an explicit instruction policy: emit only triples in format; preserve adverbs, modals, and negation in the predicate; and decompose conjunction-heavy clauses into multiple atomic triples. This sentence-level granularity ensures that every extracted triple carries a precise provenance pointer and that confidence scores are computed at the finest possible resolution. Each extracted triple is stored with provenance fields , enabling passage-level traceability and downstream confidence scoring. Canonicalization keeps the graph coherent by ensuring that the same real-world entity or relation is represented by a single node or predicate, regardless of surface-form variation across source documents. applies it in one of two regimes, depending on whether a reference ontology is available: (when a schema is available): predicates and entities are normalized against the ontology; this enables schema checks and the Schema Compliance and Constraint Violation Rate metrics. (when no suitable ontology exists): rule-based or embedding-based entity resolution, alias and coreference merging, and deduplication act as the fallback that keeps the KG connected and reduces dead nodes without a schema. This two-regime design reflects a broader property of : it operates with or without a reference ontology, degrading gracefully from schema-driven to resolution-driven coherence. The implications of ontology availability for , in particular which trustworthiness metrics remain computable without a schema, are discussed in . When an ontology is available, extracted triples are additionally aligned to it through predicate canonicalization against the target predicate inventory and optional schema checks evaluating predicate, domain, and range validity. These checks produce a per-triple trust signal feeding and . The conceptual model is provided as input rather than constructed from scratch; how the relevant subset of the ontology is delineated in practice is an operational choice discussed in . A Triple Confidence Score is assigned to each extracted triple as a proxy for extraction reliability. The formal definition of and its role in the full metric suite are presented in . At the pipeline level, feeds document-level confidence aggregation and serves as an early-warning signal: triples whose indicates low confidence can be flagged for expert review during rather than silently entering the graph. After extraction, the pipeline persists intermediate artifacts per stage in JSON and GraphML format. This versioned artifact trail directly supports the : every triple in the final KG can be traced back to its source sentence and to the confidence score assigned at extraction time, which is what makes the Evidence Attribution Rate computable and the pipeline auditable. Adapting the phase~, the produced KG is reviewed by a domain expert before use. acts here as a : it connects the automated extraction of Phase~1 to a human checkpoint, and makes that checkpoint actionable by surfacing interpretable, graph-level quality metrics as decision support. Crucially, the core metrics of this phase require , making them operational in any deployment setting; their formal definitions are given in . When a reference KG is available , correctness and completeness from~ can additionally be computed to calibrate the ground-truth-free metrics. [ font= , >=Latex, node distance=7mm, proc/.style={draw=blue!60, fill=blue!5, rounded corners, align=center, inner sep=3pt, minimum width=30mm, minimum height=7mm}, artifact/.style={draw=green!50!black, fill=green!6, rounded corners, align=center, inner sep=3pt, minimum width=30mm, minimum height=7mm}, readout/.style={draw=gray!70, fill=gray!12, rounded corners, align=center, inner sep=3pt, minimum width=20mm, font= }, triage/.style={draw=orange!75!black, fill=orange!8, rounded corners, dashed, align=center, inner sep=3pt, minimum width=28mm, font= }, faded/.style={draw=gray!45, fill=gray!4, rounded corners, align=center, inner sep=3pt, text=gray!55, minimum width=26mm, font= }, tap/.style={gray!60, dashed, shorten >=1pt}, gnode/.style={circle, draw=green!50!black, fill=green!55!black, minimum size=2.4mm, inner sep=0pt}, onode/.style={circle, draw=gray!60, fill=gray!20, minimum size=2.4mm, inner sep=0pt} ] } {query $q$}; {Entity Extraction\\ { NER / noun-chunk}}; {surface mentions $M_q$}; {Grounding $ _q : M_q V { }$\\ { entity linking}}; {grounded query nodes $V_q^{}$}; {Retriever $R $\\ { ego / path / community}}; {}; at {{ retrieved subgraph $ _S$}}; {Verbalize \& Reader LLM}; {answer}; -- ; -- ; -- ; -- ; -- ; -- ; -- ; -- ; at {}; at {}; at {}; at {}; -- -- ; -- ; { }; { ,\ }; { ,\ ,\ \\ $^{ }$}; { ,\ ,\ }; -- ; -- ; -- ; -- ; at !0.5! $) {lens}; at !0.5! $) {lens}; at !0.5! $) {lens}; at !0.5! $) {lens}; {pre-inference triage\\ \ or\ $< $ reroute / warn}; to ; {black-box fallback: LLM-as-judge\\ }; -- ; at !0.5! $) {lens}; stage. The vertical spine is the inference path; each magnifier probe taps one artifact and lists the metrics computable from it: the surface mentions \ and grounded query nodes \ yield ; the retrieved subgraph $ _S$ yields , , and ; the retrieval trace yields the cost metrics and ; and the answer, compared against $ _S$, yields , , and . The chain heads , , are read the reader runs, enabling pre-inference triage . A black-box evaluator sees only the query and the answer, so it cannot localize a retrieval failure. Inside $ _S$, filled nodes are anchors and the bold edge is the reasoning path connecting them.} The phase is identified in ~ as a gap in prior KG engineering methodologies. operationalizes it for graph-RAG systems via graph-based retrieval and inference: a query is grounded to the KG, a query-relevant subgraph is retrieved, and that subgraph conditions answer generation. Graph-constrained decoding~ is one such inference method, but the usage-specific metrics introduced here are agnostic to the choice of retriever and reader; they characterize the structural adequacy of the retrieved subgraph, the faithfulness of the generated answer, and the computational cost of retrieval . Given a validated KG and a natural language query $q$, the inference layer proceeds in three steps . First, query grounding maps $q$ to a set of anchor entities in the KG via entity linking and embedding-based similarity. Second, subgraph retrieval extracts a query-relevant subgraph from the full KG; the specific retrieval strategy is configurable and does not affect metric computation. Third, the retrieved subgraph is verbalized and supplied to an LLM to generate the final answer, for example via graph-constrained decoding~. A key design principle of the phase is that structural quality signals are computed generation, enabling proactive triage rather than post-hoc diagnosis. measures whether the entities required to answer $q$ are present in the retrieved subgraph, and measures whether those entities are connected by a valid reasoning path within it. Queries for which or fall below a configurable threshold can be flagged before the LLM is invoked, routed to a fallback retrieval strategy or returned with a low-confidence warning, avoiding the silent failure mode in which the LLM compensates with parametric knowledge and produces a plausible but unsupported answer. ``Did the director of also direct ?'' Extraction had produced the bridging triples and , both with high confidence , and $ =0.91$ over the source passages, so extraction was not the weak link. On the assembled graph $ =0.18$ was mildly elevated because the surface form had resolved to its own near-isolated node. For this query $ =1.0$ and $ =1.0$ , yet $ =0$: the two films sit in the retrieved subgraph with no path between them, because their shared director is split across two unmerged nodes. Reading the chain , the first broken link is , a ; the indicated remediation is a graph-and-schema lever, improved entity resolution to merge into , which restores the two-hop path. } ) localizes the failure to a single condition, here a connectivity gap, which then indicates the stage lever that can fix it. Values are illustrative .} Four practical points summarize the framework. First, localize a failure to a stage and act on it with that stage's levers, rather than only observing it at the answer. Second, do not wait for gold labels: the core signals are computable in deployment on the artifacts a pipeline already has. Third, measure the retrieved subgraph, not only the generated answer, so a retrieval failure cannot hide behind a fluent response. Fourth, triage before generation, since the first three chain links are available before the LLM runs. If only a handful of signals can be monitored, we suggest , , and the chain heads , , and .